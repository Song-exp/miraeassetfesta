# 줄바꿈 정규화 — 팀원이 Windows/macOS 를 섞어 써도 줄바꿈만 바뀐 diff 가 뜨지 않도록
# 저장소에는 LF 로 통일해 저장합니다.
* text=auto eol=lf

# 바이너리 (줄바꿈 변환 금지)
*.xlsx  binary
*.db    binary
*.pdf   binary
*.png   binary
*.jpg   binary

# Jupyter 노트북 — JSON 이라 병합 충돌 시 자동 병합을 막습니다
*.ipynb -merge
