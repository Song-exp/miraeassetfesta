# 🤖 금융상품 AI Agent 구축 프로젝트 (`PROJECT.md`)

> **미래에셋 금융상품 데이터 기반 Agent RAG & QA 시스템 개발**  
> 대규모 금융 상품 마스터 데이터(국내채권, 국내/해외 ETF, 공모펀드)를 스스로 탐색·필터링·연산하고 근거에 기반해 답변하는 **HyperCLOVA X 기반 AI Agent**

---

## 1. 핵심 요약 및 프로젝트 개요 (Core Summary & Overview)

### 📌 과제의 본질
단순 RAG(문서 검색)를 넘어, **금융 데이터의 정합성과 신뢰성을 온톨로지(Ontology)와 지식 그래프(Knowledge Graph)로 보장**하는 금융 에이전트 구축

- **최종 제출 형태**: 평가용 REST API 서버 (HTTPS 엔드포인트)
- **제출 마감일**: 2026년 9월 6일 23:59 (GitHub Organization Private Repository Push 기준)
- **평가 방식**: 주최 측 오토배치 평가 시스템이 cURL / HTTP GET Call을 통해 **35개 문항(상 10 / 중 10 / 하 10 + 답변불가 5)** 을 자동 호출하여 평가 (8/24 공지로 30→35 정정)
- **데이터 기준일**: **2026년 8월 22일** (해외는 8/23 KST) — 🔴 8/24 배포된 **2차 데이터**가 기준. 7/11 1차본은 정합성·코드 이슈로 **폐기** (`docs/DATA_V2_2026-08-24_impact.md`). 외부 자료는 **8/24까지 발행분** 사용 가능
- **필수 LLM**: 최종 답변 생성 및 메인 에이전트 오케스트레이션은 **HyperCLOVA X 고정** (권장 모델: `HCX-005`. 전처리, 인텐트 분석 등 보조 작업은 타 기법/모델 자율 활용 가능)
- **질의 형태**: **단일 질의 - 단일 응답 (Single-turn Q&A)** 형태 평가 — 8/25 주최 재확인: **멀티턴 없음**, 역질문을 보내도 후속 질의는 오지 않음. **교차질의는 한 호출 안에서 복수 테이블**(ETF+펀드 등)을 함께 검색해야 함

---

## 2. 배점표 및 감점 방지 체크리스트 (Scoring & Penalty Checklist)

### 💯 배점표 (100점 만점)

| 평가 항목 | 배점 | 핵심 요소 및 심사 포인트 |
| :--- | :---: | :--- |
| **기술 제안서** | **40점** | 온톨로지 설계 의도, 시스템 구성도, 데이터 활용 전략, **확장성/사업화 고려 (5번 항목 강조)** |
| **API 성능 및 답변** | **40점** | 답변 정확도, 근거 제시(`retrieved_context`, `think_trace`), 환각 방지, 응답 속도(권장 15초 이내) |
| **소스코드** | **20점** | 코드 완성도, `ontology.ttl` (또는 OWL/RDF 온톨로지 정의 파일) 포함 여부, Dockerfile 재현성 |

### ⚠️ 감점 유의사항 (멘토 경고 사항)
1. **없는 데이터 / 없는 상품 답변 (환각 / Hallucination)**:
   - 예: *"KODEX AI 로봇 ETF 알려줘"*, *"신용등급 AAAA 채권"* 등 존재하지 않는 질의에 멋있게 대답하면 **즉시 대형 감점**.
   - 35문항 중 **답변 불가 질의 5문항** (별도 카테고리). 전체 배점의 유의미한 비중.
   - 정답 처리되는 응답은 **두 가지** (8/25 주최 확인) — ① **불가응답** ("정보 없음/확인할 수 없음", 멘트 자유), 또는 ② **역질문** ("혹시 ~를 질의하신 것인가요?").
     → 둘 다 정답이며 **역질문이 필수는 아님**. 주최가 "모호한 질의는 최대한 내지 않는다"고 했으므로, 명백히 없는 상품·등급은 ①로 단문 응답하고, 조건이 빠진 경우에만 ②를 쓴다. 어느 쪽이든 `think_trace`에 근거를 남긴다.
2. **미래 데이터 유출 (Data Leakage)**:
   - 🔄 **8/24 공지로 완화**: 공시·시장데이터는 **2026-08-24까지 발행된 자료** 사용 가능 (6/30 기준 반기보고서가 8/15 발행된 경우도 OK). 그 이후 자료·최신 이슈로 답하면 감점. 기존 7/10 스냅샷 Holdings는 유효하나 구식.
3. **타임아웃 (응답 시간 SLA)** — 🔴 **세 층을 구분합니다**:

   | 층 | 값 | 성격 | 출처 |
   | :--- | :---: | :--- | :--- |
   | 실무 Target | **15초** | 응답 소요 시간이 평가 항목이므로 목표치 | 멘토 |
   | **설계 기준 (권장)** | **60초 이내** | 🔴 **이것이 우리 예산의 기준선입니다** | 과제소개자료 |
   | 하드 컷 (타임아웃) | 300초 | 초과 시 실패 처리. 안전 마진이지 예산이 아님 | 엔드포인트 안내 |

   - 타임아웃·5xx 시 **최대 2회 재시도**가 주어집니다 → §7-0 [!CAUTION] 참조.
   - ⚠️ **300초를 예산으로 삼지 않습니다.** 권장 60초를 넘긴 시점에 이미 감점 구간이고,
     300초는 "넘겨도 즉시 무응답 처리되지는 않는다"는 안전망일 뿐입니다.
4. **근거 미표시**:
   - API 응답 결과 JSON에 답변 생성의 근거(`retrieved_context`, `think_trace`)가 명확히 포함되어야 함.

### 📦 제출 규격 및 운영 일정 (과제소개자료 원문 명시)

| 항목 | 규격 |
| :--- | :--- |
| 제출 채널 | 주최 측 제공 GitHub Organization 내 **Private Repository** Push (대용량은 클라우드 스토리지 다운로드 링크) |
| 제출 마감 | **2026-09-06 23:59** — 마감 이후 코드·결과물 변경 시 **실격** |
| 평가 API 운영 | **2026-09-07 ~ 09-20 중 별도 공지하는 기간** — 주제별 **최대 1주**, 정확한 일자 추후 공지 |
| 예선 평가 | 2026-09-07 ~ 09-30 → 결과 발표 10-01 (예정) |

> [!IMPORTANT]
> **코드 프리즈와 서버 운영은 별개 요건입니다.**
> 9/6에 코드를 얼린 뒤, 손대지 않은 상태로 공지된 기간(**최대 1주**) 동안 서버가 살아 있어야 합니다.
> ✅ **다만 서버 장애 등 불가피한 사유로 인한 재기동은 실격이 아닙니다** (엔드포인트 안내 §서버구성-6).
> 실격 대상은 어디까지나 **"제출한 결과물의 변경"** 입니다 — 프로세스를 살리는 조치는 허용됩니다.
> NCP 크레딧 만료(9/30)보다 운영 종료가 앞서므로 크레딧으로 커버되나,
> 비용 아끼려고 인스턴스를 내리면 그 시점부터 **채점 자체가 불가**합니다.

**제출 항목 3종** (예선):

1. **소스코드** — 구현체 + 재현 가능한 개발 환경 정의(`requirements.txt` 등)
   + `README.md` (**환경 구성·실행 명령어 + 🔴 API End-point 주소 명시 필수**) + **도메인별 Ontology 파일** (→ §5 파일 규격)
2. **기술 제안서** — 자유 양식, PDF 권장 (→ §8 권장 목차)
3. **평가용 API 서버 정보** — **End-point URL + API 명세서(요청/응답 JSON 스키마) 필수 명시**
   → 🔴 **엔드포인트 주소가 들어가는 곳은 `README.md`입니다** (엔드포인트 안내 명시). 명세서는 별도 산출물입니다.

---

### 📮 주최 Q&A 확정 사항 (2026-08-25 정리 — 원문 `docs/QNA_REVIEW_2026-08-25.md`)

주최 채널 답변으로 **확정**된 규칙. 설계·답변 정책의 근거로 이 표를 인용한다. 미답변 질의는 위 문서 §3 관리대장 참조.

| 주제 | 확정 내용 | 우리 대응 |
| :--- | :--- | :--- |
| 평가 턴 | **단일턴**. 역질문을 반환해도 후속 질의 없음 | 역질문은 답변불가 문항의 정답 형태 중 하나로만 사용 |
| 답변불가 채점 | [불가응답]·[역질문] **둘 다 정답**, 멘트 자유 | 명백 불가 → 불가응답, 조건 누락 → 역질문 |
| 모호 질의 | **최대한 내지 않음** (TOP-N 개수·기준·운용사 등 조건 명시 예정) | 조건 해석 분기를 과설계하지 않음 |
| 교차질의 | 한 호출에서 **2개 이상 상품군** 비교/검색 (예: 삼성전자 보유 국내/해외ETF+펀드 연수익률 TOP10) | 플래너 라우팅 "해당 테이블 전부". 단위(KRW/USD)·기간(1y/1d) 불일치는 답변에 명시 |
| 문항 수 | **35** = 상10/중10/하10 + 답변불가 5 | — |
| 추가 예시 질의 | **없음** — 공개된 8개(+교차 예시 1)가 전부 | 테스트셋은 이 9개 유형을 축으로 설계 |
| 데이터 버전 | **8/24 배포본(기준일 8/22)** 이 기준. 7/11본 폐기 | `1.금융상품/`·DB 교체 완료 (8/25) |
| 코드값 테이블 | **제공 불가 확정**, 관련 질문 답변 없음 | 자력 판정. 2차 데이터의 `*_nm` 컬럼 우선 활용 |
| 0 / 결측 | **의도된 값**. 조회 시 **빼고 보여주거나 "없다"고 응답** | 수치 정렬·필터에서 0·NULL 기본 제외, 0이 실제값인 컬럼만 yaml에 예외 선언 |
| 외부 데이터 | 자유 (크롤링 허용). **~8/24 발행분**까지. 단 **원천 신뢰성·무결성 관리도 과제의 일부** | 출처·기준일·교차검증을 `SOURCES.md`/`fetch_log`로 관리 |
| 출처 기재 범위 | **수집 방법 · 적재 Flow · 활용 로직을 보고서에 기재**. 레포에 데이터만 두면 정성평가 없음 | 제안서 §데이터 파이프라인 절 필수 (§8) |
| 구매가능 채권 | `BUYABLE_QUANTITY` **무효**. 상폐/리스팅종료 제외 **전부 "구매가능"** | 채권: 만기 경과·상폐만 제외 / ETF: `pd_lste_dt`·`pd_sale_yn` / 펀드: `sale_yn='판매중' AND prvo_pbff_desc='공모'` (2차에 사모 8,960행 유입) |
| 응답시간 | 기준 **비공개**. 300초 = 물리적 미응답. **늦어도 응답(300초 미만)은 미응답과 점수 차등** | 타임아웃 임박 시 부분응답 fallback. 목표는 여전히 15초 |
| 정량평가 방식 | [공지사항] 참고 | — |

## 3. 데이터 명세 및 결측 특성 (Data Specification & Missing Data)

**기준일**: 2026-08-22 (2차 배포본, 8/24 배포 · 총 4개 테이블 53,375행)

### 📋 데이터 수집·활용 규칙 (원문 제약 — 평가 직결)

| 규칙 | 내용 |
| :--- | :--- |
| 수집 **가능** | 금융상품 관련 데이터라면 **비정형 포함 참가팀이 자유롭게 수집·활용 가능** |
| **기준** 데이터 | 주최 측 제공 4개 마스터가 '평가' 시 활용하는 기준 데이터. 단 **"기준 데이터만 물어보지 않음"** |
| **우선순위** | 외부 수집 데이터와 상충할 경우 → **주최 측 제공 데이터가 우선** |

> [!NOTE]
> *"기준 데이터만 물어보지 않는다"* 는 **외부 데이터 없이는 못 푸는 문항이 있다는 공식 신호**입니다.
> 제공 4개 테이블에는 **구성종목(Holdings)이 아예 없는데**, 예시 질의에는
> *"캠브리콘이 편입된 중국 반도체 ETF"*, *"에코프로의 자회사를 편입한 ETF"* 가 등장합니다 (→ §9).
> 따라서 외부 Holdings·기업관계 데이터 구축은 선택이 아니라 사실상 필수이며,
> 수집 시 **2026-08-24까지 발행된 자료**만 허용됩니다 (8/24 공지로 7/11에서 완화).
>
> 동시에 **상충 시 주최 데이터 우선** 규칙 때문에, 외부 데이터는 별도 출처로 표기하고
> 마스터 값과 충돌하면 마스터를 채택하도록 병합 로직에 우선순위를 박아야 합니다.
> `retrieved_context`에도 어느 쪽 출처인지 구분해 표기합니다.

> [!WARNING]
> **Agent 예외 처리 및 결측 대응 필수**  
> DB에 값이 결측되어 있거나 미제공 속성에 대해 Agent가 임의로 수치를 추측하지 않고, `"확인할 수 없음"`으로 응답하도록 Validation Guardrail 구축.

| 테이블 ID | 테이블명 | 레코드 건수 (2차) | 주요 정보 | ⚠️ 데이터 결측/특이사항 (Agent 주의 — 2026-08-25 실측) |
| :--- | :--- | :--- | :--- | :--- |
| `prbd01n001` | **국내채권마스터** | 21,882행<br>(종목 `pd_no` 20,497) | 종목 기본정보, 채권종류, 이자지급/금리/모집구분, 신용/위험등급, 발행/만기일, 표면금리, 듀레이션, 평가가격, 장내종가, 판매 LOT 등 (58컬럼) | 🔴 **PK가 복합키** `pd_no+pd_exg_mkt+info_base_dt+info_seq` — 동일 종목 2~4행 1,078건, 종목 단위 질의는 `GROUP BY pd_no`. **매수·세후 수익률은 판매행 634건에만** (97.1% 결측). 신용등급 결측 18.4%(15종 표기, `C0`·`B-` 등 투기등급 포함), 듀레이션 결측 0.1%. `buyable_quantity` **무효**(주최 공지) — 만기 미경과면 전부 구매가능. 🆕 `bd_intp_tcd`(이표/복리/할인/단리)·`bd_inrt_tcd`(고정/변동)·`bd_ofr_tcd`(공모/사모)·`pd_risk_nm` 신설 → 이름 파싱 대신 컬럼으로 판정. 위험등급은 `'00','11'~'16'` 문자열. 장내종가 0값 75% = "값 없음". 고정폭 공백 패딩 여전 — `TRIM` 필수 |
| `pref01n001` | **국내ETF마스터** | 1,780행<br>(ETF 1,235 + ETN 545) | 운용사, 위험등급, 자산군/지역, 기간별 수익률·변동성, 순자산, 거래대금, 분배금 상세, 기초지수, 티커/ISIN/RIC, 채권ETF 듀레이션 등 (98컬럼) | 🔴 **ETN 545건(30.6%) 혼입** — `pd_grp_no` 분리 필수. 🟢 기초지수 `ref_base_index` ETF 97.8% 수록(905종; 구 `cu_base_index`는 59건뿐 → REF 1순위). 🟢 분배금·분배주기·지급월·추적오차·괴리율 정상화(1차 "전부 0" 판정 폐기). 🔴 **총보수 실질 67건**·기타비용 전부 0 → "미수록" 응답. 🔴 **`cu_lev_fector` 부호 소실** — "KODEX 인버스"가 1.0 → 인버스는 상품명 `'%인버스%'`로 판정. 1년수익률 `-100` 센티넬 110건. 구성종목은 외부 `ext_etf_holdings`(8/21 스냅샷, ETF 93.9%) |
| `pref02n001` | **해외ETF마스터** | 6,037행<br>(ETF 5,972 + ETN 65) | 티커, ISIN, 상장시장, 거래통화, 기초지수, 운용사, 총보수, 운용전략(영문), AUM, 종가, 거래량 등 (49컬럼, 1차와 동일) | 기초지수 **센티넬 문자열 2,920건**(`Index is not provided…`) — 실질 51.4%, `IS NULL`로 안 잡힘. 🔴 **수익률은 `du_er_1d`(1일)만** — 연/기간 수익률 없음 → 교차 TOP-N에서 "연수익률 미수록·제외" 명시 + 1일수익률 병기. 위험등급 컬럼 없음(`absent`). 총보수 결측 0%(0값 419), AUM USD(국내 KRW와 통합 정렬 시 `fx_rate.csv` 8/21 적용). 거래소 코드 `'101'/'102'` 31건 미확정. 구성종목 외부 `ext_ovs_etf_holdings`(NPORT-P, 3/31~6/30) — **커버리지 낮음**(확장 수집 중), 답변에 모수·`report_date` 병기 |
| `prfd01n001` | **공모펀드마스터** | 23,676행<br>(`itm_no` 유일 = PK) | 종목 기본정보, 운용속성, 벤치마크, 투자지역, 환헤지, 기간별 수익률, 순자산, 위험등급, **보수 4종**, 클래스 체계, 제로인 유형/자산구성비, 속성태그 등 (75컬럼) | 🟢 **`itm_no`가 행 단위 PK** (1차 속성코드 중복행 해소 — `GROUP BY std_itm_no` 규칙 폐기; `std_itm_no`는 클래스 묶음 키, 결측 515·`'00000 '` 위장결측). 🔴 **사모 8,960행 유입** + 판매완료 12,714행(평가컬럼 99% 결측) → **기본 모수 = `sale_yn='판매중' AND prvo_pbff_desc='공모'` 8,969행**. 🟢 **보수 4종(운용/판매/수탁/사무) 결측 0%, 단위 ‰**(값÷10 = %, 설명서 8,925건 대조로 확정). 운용사는 숫자 코드 275종 — `asset_manager.csv`(판매중 법인명 커버 90%). 판매중 기준 위험등급 결측 22.0%, 1년수익률 36.9%. 속성코드 `prfd_attr_cds` 210종 ↔ `fund_attr_code.csv`(`zrin_attr_nms`와 위치 대응 아님). `fd_wk1_ern_r` 100% 결측 |

총 **53,375행** · 테이블별 변화·결측 판정 대조 상세는 `docs/DATA_V2_2026-08-24_impact.md` (§1·§2·§6)
---

## 4. 개발 환경 & 클라우드 구성 가이드 (NCP Infra)

1. **크레딧 쿠폰**: 
   - 기본 20만 원 쿠폰 지급 + NCP 검색 후 '웰컴 크레딧' 10만 원 신청 시 **총 30만 원 확보 가능**.
   - ⚠️ **유효기간**: **9월 30일까지**. 이후 리소스를 삭제하지 않으면 개인 신용카드로 청구되므로 유의!
2. **권장 서버 스펙**: Ubuntu/Windows, `2 vCPU / 4GB RAM / SSD 20GB` (월 5~7만 원 수준).
3. **네트워크 설정**: VPC 및 Subnet 생성 후 ACG(방화벽)에서 SSH(22), RDP(3389), Web(80, 443 및 API 포트) Inbound 열기, Outbound 전체 TCP 열기.
   - 🔴 **Public 망에서 접근 가능한 네트워크 구성은 필수**입니다 (서버 환경 자체는 자유).
   - 원할 경우 **주최 측 발신 IP 대역만 허용**하도록 좁힐 수 있습니다 (대역은 추후 공지).
4. **엔드포인트 노출** — ⚠️ **요구 수준이 당초 인식보다 낮습니다**:
   | | 실제 요구 | 우리 현황 |
   | :--- | :--- | :--- |
   | 프로토콜 | **HTTP 기본**, HTTPS는 선택 · **self-signed 무방** | Caddy + Let's Encrypt (요구 이상) |
   | 도메인 | **불필요** — 공인 IP 직접 제출 가능 | `nip.io` 와일드카드 DNS |
   | 포트 | HTTP 80 / HTTPS 443 표준 포트 | ✅ |
   | 개인 환경 | 노트북 + **터널링(ngrok 등) 허용** | NCP 서버 |

   > 💡 **이건 리스크 완화 정보입니다.** 기존 로드맵은 SSL·도메인을 *"실패하기 쉬운 작업"* 으로 보고 8/19 데드라인을 잡았는데,
   > **HTTP 80 단독도 적법한 제출 형태**이므로 인증서가 끝내 안 되면 그대로 내려가면 됩니다.
   > 이미 구축된 Caddy 스택은 유지하되(요구 이상이라 손해 없음), **HTTPS 실패가 더 이상 40점을 위협하지 않습니다.**

---

## 5. 핵심 기술 아키텍처: Ontology-Grounded Hybrid Retrieval

> [!NOTE]
> **용어 정정 (2026-08-16, 워크샵 안건 A-6).** 주최 측 테크세션 자료 20이 두 용어를 명시적으로 구분합니다 —
> ***"Hybrid는 한 소스 내 결합, Federated는 여러 소스 간 결합."***
> 우리는 KG를 별도 엔진이 아니라 **SQLite 매핑 테이블**로 두기로 했으므로
> (`ontology-design-and-roadmap.md` §1.3 — 재현성·의존성 근거), 정의상 **Hybrid**입니다.
> 제안서에서는 *"Federated Query Plan을 단일 저장소에 사상했다"* 로 서술하고 **그 이유를 밝힙니다.**
> 두 단어를 섞어 쓰면 심사자에게 이해도 부족으로 읽힙니다.

```plaintext
[ 사용자 API 질의 (cURL / HTTP GET) ]
       │
       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 1. Question Understanding (질의 의도 파악 Layer)                            │
│    - 질문 파싱 (자산군, 위험등급, AUM, 보수율, 구성종목 등 추출 & 온톨로지 그라운딩)│
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 2. Hybrid Retrieval (단일 저장소에 사상한 Federated Query Plan)               │
│    ├───> [ RDB (SQLite) ] : 14.5만 건 정형 데이터 (수익률, AUM, 보수 연산)   │
│    ├───> [ KG (SQLite 매핑 테이블) ] : 조인 키·정규화 값 — 별도 엔진 아님    │
│    └───> [ Vector DB ] : 🔶 보류 — 외부 수집 결과 보고 결정 (안건 A-5)       │
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 3. Hallucination Guardrail (환각 검증 & Validation Layer)                    │
│    - DB 내 존재 여부 검증 (없는 상품/신용등급인가?) ──► [없음 / 확인 불가]     │
│    - 2026-07-11 기준일 준수 여부 검증               └─► "확인할 수 없음" 즉시 반환│
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │ (데이터 유효 시)
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 4. Answer Generator (HyperCLOVA X API - HCX-005)                            │
│    - 검증된 retrieved_context + think_trace 기반으로 근거 중심 최종 답변 생성│
└──────────────────────────────────────┬──────────────────────────────────────┘
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ 5. API Response 반환 (JSON Schema)                                          │
│    { question_id, question, retrieved_context, think_trace, answer }      │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 🧩 주최 측 명시 "Agent 필수 구성요소" 4종 대응표

원문이 요구하는 4개 구성요소이자 제안서·소스코드 채점 항목입니다. 위 런타임 다이어그램은 3·4번만 다루므로,
**1·2번(오프라인 구축 파이프라인)이 코드와 제안서에 별도로 드러나야 합니다.**

| # | 필수 구성요소 | 원문 세부 요구 | 본 프로젝트 대응 |
| :-: | :--- | :--- | :--- |
| 1 | 정형·비정형 분석 & 정제 | **`[Parsing]` pdf·ppt 등을 Markdown으로 변환**<br>`[Ontology]` 상품별 LLM 가이드라인 제작 | 펀드 설명서·투자설명서 등 비정형 원문 → MD 변환 단계 필요<br>(현재 미구현 — 예시 질의 *"국민성장펀드의 구조와 투자전략"* 이 여기에 걸림) |
| 2 | 금융상품 KnowledgeBase | `[Extraction]` 정제 데이터에서 지식 추출<br>**`[EntityResolution]` 불필요·모호 데이터 판별 및 정리** | ER은 본 데이터셋에서 특히 중요:<br>· 공모펀드 `std_itm_no` 중복 8.6배 (동일 펀드 다행)<br>· 운용사명 표기 불일치·오염 (국내ETF)<br>· 공모펀드 운용사 숫자코드 67종 ↔ 이름 매핑<br>· 센티넬 문자열(`Index is not provided…`) → NULL 판정 |
| 3 | Intent Analysis & Retrieval | `[NL2SQL]` 자연어 → SQL<br>`[Retrieval]` 우선순위·탐색 순서 조정 | 위 다이어그램 1·2단계 |
| 4 | Answer Generator | `[근거기반]` Evidence 기반 생성<br>`[환각 방지]` 데이터에 없는 내용 미추출 | 위 다이어그램 3·4단계 |

### 🕸️ 온톨로지 (Ontology) 제출 파일 규격 — **필수**

> [!CAUTION]
> **파일 경로와 이름이 규격으로 고정되어 있습니다.** 단일 `ontology.ttl` 하나로는 요건 미충족입니다.
> 원문: *"도메인별 Ontology.ttl 제출 기준"* — 4개 도메인 파일 + 공통 상위 클래스 분리.

```
repo/
├─ src/                    # 구현체 소스코드
├─ ontology/
│  ├─ common.ttl           # 공통 상위 클래스 (fp:Product) — 반드시 분리
│  ├─ bond_kr.ttl          # 국내채권
│  ├─ etf_kr.ttl           # 국내 ETF
│  ├─ etf_gl.ttl           # 해외 ETF
│  └─ fund_pub.ttl         # 공모펀드
├─ requirements.txt
└─ README.md
```

**네임스페이스 · 작성 예시** (원문 제시 그대로 — prefix를 임의로 바꾸지 말 것)

```turtle
@prefix fp:  <http://mafest.ai/product#> .
@prefix owl: <http://www.w3.org/2002/07/owl#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

fp:ForeignETF a owl:Class ;
    rdfs:subClassOf fp:ETF ;
    rdfs:label "해외 ETF"@ko .

fp:expenseRatio a owl:DatatypeProperty ;
    rdfs:domain fp:ETF ;
    rdfs:range xsd:decimal .
```

### 🕸️ 온톨로지 작성 팁
- **파일 형식**: Turtle (`.ttl`). 원문 표기상 형태는 무관하나 제출 기준표가 `.ttl` 경로를 명시하므로 `.ttl`로 맞춥니다.
- **클래스 및 제약 조건 명시**:
  - `ETF`와 `ETN`은 `DisjointWith` 관계로 설정 (동시 소속 불가).
    → ⚠️ 장식이 아니라 **실제 필터**입니다. 국내ETF마스터에 ETN 532건, 해외ETF마스터에 59건이 실제로 섞여 있습니다.
  - `riskGrade`: **0~6등급** 범위 제약 (`Constraint`). ⚠️ **1~5가 아닙니다** — 6등급이 채권 10,408건(24.6%),
    펀드 2,397건, 국내ETF 21건 존재하며, `0`(미분류 추정)이 채권 58건 있습니다. 1~5로 제약하면 정상 데이터가 차단됩니다.
- **역관계 (`inverseOf`) 정의**:
  - *"미래에셋자산운용이 TIGER 코스피를 운용한다"* ↔ *"TIGER 코스피는 미래에셋자산운용에 의해 운용된다"*.

---

## 6. 개발 4단계 (Phases)

### 1단계: 환경 및 데이터 세팅
1. **NCP 크레딧 신청 & 쿠폰 등록**: 팀 대표가 크레딧 20만 원(+웰컴 크레딧 10만 원)을 확보합니다.
2. **클로바 스튜디오 접속**: 네이버클라우드 콘솔에서 HyperCLOVA X API Key / Secret Key를 발급받아 보관합니다.
3. **데이터 확인**: 제공된 엑셀 파일(14.5만 건 금융상품 마스터 데이터)을 다운로드하고 컬럼 정의(Schema)를 숙지합니다.

### 2단계: 온톨로지(Ontology) & DB 구축
1. **RDB(SQLite) 저장**: 엑셀 4개 시트(`국내채권`, `국내ETF`, `해외ETF`, `공모펀드`) 데이터를 Python `pandas`로 파싱하여 SQLite 데이터베이스 파일로 저장합니다.
2. **온톨로지(`.ttl`) 작성**: 파일 분할 규격은 **§5 제출 파일 규격**을 따릅니다 (`common.ttl` + 도메인 4종).
   - 금융상품 간의 관계(운용사 - 상품 - 위험등급 - AUM - 구성종목 등)를 Turtle 구문으로 작성합니다.
   - *"위험등급은 **0~6등급**이다"*, *"ETF와 ETN은 `DisjointWith`"* 같은 제약조건을 정의합니다.
     ⚠️ 1~5로 쓰면 실데이터가 차단됩니다 — 근거는 §5 작성 팁 참조.

### 3단계: 에이전트 핵심 로직 개발 (VS Code / Python)
1. **질의 의도 파악 (Intent Router)**: 질문을 분석해 어떤 상품군(ETF/채권/펀드)을 조회할지, 어떤 조건(보수율/수익률/위험등급)으로 검색할지 판단합니다.
2. **검색 & 연산 (Hybrid Search)**: SQL 쿼리로 DB를 조회하고, 온톨로지 지식 그래프로 근거 관계를 추출합니다.
3. **환각 방지 레이어 (Guardrail / Validator)**: DB 조회 결과가 없거나 2026-07-11 기준일 이후의 질문이라면 HyperCLOVA X로 넘기지 않고 즉시 `"확인할 수 없음"`을 반환합니다.
4. **최종 대답 생성 (HyperCLOVA X)**: 검증된 검색 데이터와 근거를 HyperCLOVA X에게 전달하여 자연스러운 최종 대답을 생성합니다.

### 4단계: 네이버클라우드(NCP) 배포 & 평가 API 오픈
1. **FastAPI 서버 작성**: 제출 규격에 맞는 JSON 응답 스키마 (`question_id`, `question`, `retrieved_context`, `think_trace`, `answer`)로 API 엔드포인트를 개설합니다.
2. **NCP Linux 서버 개설**: NCP 콘솔에서 우분투 서버(2vCPU / 4GB RAM)를 개설하고 FastAPI 서버를 올려 구동합니다.
3. **HTTPS & Domain 연결**: Public IP를 열고 SSL 인증서를 적용해 HTTPS 엔드포인트를 완성합니다.
4. **GitHub 제출**: 소스코드, **온톨로지 5종**(`common/bond_kr/etf_kr/etf_gl/fund_pub.ttl`), `Dockerfile`,
   `requirements.txt`, `README.md`(환경 구성·실행 명령어 포함), 기술제안서(PDF),
   **평가용 API 서버 정보**(End-point URL + API 명세서)를 주최 측 GitHub Organization의 Private Repo에 푸시.
   → 상세 제출 규격은 **§2 제출 규격 및 운영 일정** 참조. 푸시로 끝이 아니라 **9/20까지 서버 유지**가 남습니다.

---

## 7. REST API 인터페이스 명세

### 0) 계약 조건

> 📌 **출처: 주최 측 「평가용 API End-point 제출 안내」** (과제소개자료보다 상세하며, 충돌 시 이쪽이 최신입니다).

| 항목 | 규격 | 구현 상태 |
| :--- | :--- | :--- |
| Method / Path | `GET /answer` — **경로 고정** | ✅ `src/api/main.py` |
| Content-Type | `application/json` | ✅ (charset 강제 아님) |
| 인증 | **인증 헤더 사용 안 함**. 접근 제어는 **주최 측 발신 IP 허용** 방식 | ✅ 없음 |
| Body | POST 바디 사용 안 함 (파라미터는 전부 쿼리스트링) | ✅ |
| 미정의 파라미터 | 들어와도 500 없이 처리 | ✅ 추가 쿼리 무시 |
| 필드 타입 | **5필드 전부 `string`** | ✅ 전부 `str` |
| 필드 길이 | 제한 없음. ⚠️ 단 **극단적으로 길면 초과분이 평가에 미반영**될 수 있음 | → `retrieved_context` 상한 필요 |
| `retrieved_context` 형식 | 여러 문서는 **문자열 안에서 구분 태그 자유**. **어떤 태그를 쓰는지는 평가 대상 아님** | — |
| **응답 시간** | **권장 60초 이내**(설계 기준) · **하드 타임아웃 300초**(안전망) · 목표 15초 | → §2-3 |
| **재시도** | 🔴 **타임아웃 또는 5xx 발생 시 최대 2회 재시도** | ⚠️ 아래 [!CAUTION] |
| **호출 방식** | 🔴 **팀당 순차 1건씩. 동시 요청 없음** | — |
| 확인 불가 질의 | 200 OK + 동일 5필드 스키마 유지 | ✅ `_fallback()` |
| 프로토콜 | **HTTP 기본**. HTTPS는 선택이며 **self-signed 무방** | ✅ Caddy+LE (요구 이상) |
| 포트 | HTTP 80 / HTTPS 443 표준 포트 | ✅ |
| 도메인 | **불필요 — 공인 IP 직접 제출 가능**. 터널링(ngrok 등) 자유 | ✅ nip.io |

> [!CAUTION]
> **🔴 "무조건 200" 설계는 재검토 대상입니다** (워크샵 안건 A-7).
> 지금 `main.py`는 모든 예외를 200 + 5필드로 덮습니다. 근거는 *"채점기에게 5xx는 무응답"* 이었는데,
> **주최가 5xx·타임아웃에 최대 2회 재시도를 주는 것으로 확인**되면서 전제가 바뀌었습니다.
>
> | 오류 성격 | 예 | 유리한 응답 | 이유 |
> | :--- | :--- | :---: | :--- |
> | **일시적** | HCX 타임아웃 · 네트워크 · OOM · 컨테이너 재기동 중 | **500** | 재시도 2회를 받아 살아날 수 있음. 200으로 *"확인할 수 없습니다"* 를 내면 **그 문항은 오답으로 확정** |
> | **영구적** | 파라미터 누락·형식 오류 | **200** | 재시도해도 같은 결과. 5필드 유지가 이득 |
> | **정상 판정** | 데이터에 근거 없음 | **200** | 답변불가 정답 경로 |
>
> 즉 **전역 200 fallback은 공짜 재시도 2회를 버리는 구조**입니다. 예외를 성격별로 갈라야 합니다.

> [!NOTE]
> **새 정보가 실제로 바꾸는 것은 하나입니다 — 순차 호출.**
> · ✅ *"병목은 rate limit — 분당 질의 3.6개 천장"*(`docs/bench/hcx_latency.md` 결론3) → **동시 요청이 없으므로 해소**
> · ➖ **레이턴시 예산은 그대로입니다.** 권장 60초가 유효하므로 `agent_architecture_notes` §5의 **누적 12초 예산**은
>   근거(*"60초 초과가 감점이므로 절대 방어선"*)가 유지됩니다. 300초는 예산이 아니라 안전망입니다.
> · ➖ `HCX-007(low)` 탈락 근거(**지연이 입력 크기에 단조롭지 않아 예산 자체를 못 세움**)도 상한값과 무관하므로 **유지**됩니다.
> · 🆕 재시도 2회는 **장애 복원력**을 주지, 느려도 된다는 허가를 주지 않습니다.

### 1) Request 규격 (주최 측 -> 참가팀 API)
```bash
# cURL 예시
curl -G "https://{team-endpoint}/answer" \
  --data-urlencode "question_id=Q-001" \
  --data-urlencode "question=국내 상장 ETF 중 최근 1년 수익률이 가장 높은 채권형 ETF 3개를 알려줘."
```

### 2) Response JSON Schema (참가팀 API -> 주최 측)
```json
{
  "question_id": "Q-001",
  "question": "국내 상장 ETF 중 최근 1년 수익률이 가장 높은 채권형 ETF 3개를 알려줘.",
  "retrieved_context": "PRBD01N001/PREF01N001 조회 결과: 1. KBSTAR KIS국고채30년EN (코드: 456780, 1년수익률: 12.5%), 2. ACE 미국30년물국채엔화노출(합성 H) (코드: 472160, 1년수익률: 11.8%), 3. KODEX 미국30년국채액티브 (코드: 461580, 1년수익률: 10.2%)",
  "think_trace": "1. [Intent Classification] 질의 분류: 국내 ETF 대상 조건 검색 및 정렬\n2. [Query Generation] PREF01N001 마스터 테이블에서 '자산군=채권' 필터링 후 1년 수익률 내림차순 정렬 Top 3 추출 SQL 실행\n3. [Execution Result] 상위 3개 종목 데이터 확보\n4. [Answer Synthesis] HyperCLOVA X를 활용해 수치 및 근거 기반으로 최종 답변 구성",
  "answer": "국내 상장 ETF 중 최근 1년 수익률이 가장 높은 채권형 ETF 상위 3개 종목은 다음과 같습니다.\n\n1. **KBSTAR KIS국고채30년EN** (1년 수익률: 12.5%)\n2. **ACE 미국30년물국채엔화노출(합성 H)** (1년 수익률: 11.8%)\n3. **KODEX 미국30년국채액티브** (1년 수익률: 10.2%)\n\n*본 답변은 2026년 7월 11일 기준 마스터 데이터(PREF01N001)에 근거한 정보이며, 미래 투자 수익률을 보장하지 않습니다.*"
}
```

---

## 8. 기술 제안서 권장 목차 (40점 — 최대 배점 항목)

자유 양식(**PDF 권장**). 주최 측 제시 목차이므로 그대로 따라가는 것이 채점자 대조에 유리합니다.

| # | 목차 | 원문이 요구하는 내용 |
| :-: | :--- | :--- |
| 01 | **전체 아키텍처 및 특화기능** | 시스템 전체 구조와 **차별화 포인트** |
| 02 | **제안 방법** | 아래 **세 질문에 답하는 형태**로 작성:<br>· 어떤 특화 데이터를 어떻게 수집·정제했는가<br>· 어떤 **온톨로지 엔지니어링**을 적용했는가<br>· 어떤 **검색 방법론**을 선택했고 **왜 그렇게 설계했는가** |
| 03 | **시스템 구성도** | 적재 → 색인 → 검색 → 생성 파이프라인 |
| 04 | **주요 기능 흐름도** | 데이터 수집 → 온톨로지 설계 → 그래프 구축 → 검색 |
| 05 | **기대효과 · 확장성** | **현업 적용 지점**과 상품군 확장 방안 |
| 06 | **부록** | 온톨로지 스키마 · **평가 재현 절차** |

> [!TIP]
> 02는 "무엇을 만들었는가"가 아니라 **"왜 그렇게 설계했는가"** 를 묻습니다.
> 본 프로젝트의 설계 근거는 대부분 **데이터 결함**에서 나옵니다 (§3 결측표) —
> ETN 혼입 → `DisjointWith`, `std_itm_no` 중복 → `GROUP BY`, 센티넬 문자열 → NULL 판정,
> 총보수 `0` 위장 결측 → 실질 67건. 이 인과를 그대로 서술하면 02·05가 채워집니다.

---

## 9. 주최 측 공개 예시 질의 (평가 성격 파악용)

원문 수록 8문항. 정답 세트는 아니지만 **어떤 능력을 보는지**를 드러냅니다.
→ 머신 리더블 사본: `eval/questions_official_sample.jsonl`

### 답변 가능 (난이도 혼합)

| # | 질의 | 카테고리 | 난이도 | 이 문항이 요구하는 능력 |
| :-: | :--- | :--- | :--: | :--- |
| 1 | 현재 **판매 가능한** 원화채권 중 **AA- 이상** 종목 알려줘 | 채권 | 하 | 판매가능 플래그 + 신용등급 **순서 비교**(문자열 등급의 서열화). 신용등급 결측 41.1% 주의 |
| 2 | **국민성장펀드**의 구조와 투자전략 동향 등 찾아서 알려줘 | 펀드 | 중 | 🔴 **비정형 문서 필요** — 마스터에 '구조·전략 동향'은 없음. §5 필수구성요소 1번(pdf→MD) 대응 |
| 3 | **캠브리콘이 편입된** 중국 반도체 ETF를 알려줘 | 해외ETF | 중 | 🔴 **구성종목(Holdings)** — 제공 데이터 미수록. 외부 구축 필수 |
| 4 | 최근 6개월 동안 **우주항공 테마와 연결 이력**이 있는 ETF 정리 | ETF | 상 | 🔴 **테마↔종목 시계열 연결 이력** — 스냅샷 1개로는 '이력'이 성립 안 됨 |
| 5 | **에코프로의 자회사를 편입한** ETF 중 순자산이 큰 상품의 **위험요인** | 국내ETF | 상 | 🔴 **기업 지배구조 그래프**(모회사→자회사) + Holdings + 위험요인 텍스트. 3-hop |

### 답변 불가 — 생성하면 감점

| # | 질의 | 왜 불가한가 | 우리 시스템이 잡아야 할 지점 |
| :-: | :--- | :--- | :--- |
| 1 | **신용등급 AAAA**인 채권 찾아줘 | 존재하지 않는 등급값 | **Enum 검증** — 등급 도메인에 없는 값 → 즉시 반려 (`ontology/enums/` 활용) |
| 2 | **Kimi** 관련 투자 상품 있어? | 매칭되는 개체 없음 | **Entity Resolution 실패 처리** — 유사어로 억지 매칭 금지 |
| 3 | **KODEX AI로봇 ETF** 정보 알려줘 | 실재하지 않는 상품명 | **상품명 존재 검증** — 부분일치(`KODEX`, `AI`)로 엉뚱한 종목 반환 금지 |

> [!CAUTION]
> 답변 불가 3종은 전부 **"그럴듯한 부분일치가 존재하는"** 케이스입니다.
> `LIKE '%AI%'` 류의 느슨한 검색은 반드시 무언가를 반환하며, 그게 곧 감점입니다.
> 존재 검증은 **검색 결과 유무**가 아니라 **정확일치 · Enum 소속 여부**로 판정해야 합니다.
> (난이도 상 5문항은 반대로 **3-hop 그래프 탐색**을 요구 — 상·하 양극단의 요구가 서로 다릅니다.)
