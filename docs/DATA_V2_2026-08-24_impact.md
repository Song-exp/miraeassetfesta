# 📦 2차 배포 데이터(2026-08-24) 분석 — 우리 프로젝트에 미치는 영향

> 작성 2026-08-25 · 원본: `~/Downloads/ai-festival2026_금융상품Agent_DtataSet260824/` (4테이블 × data/schema = 8파일)
> 비교 대상: 1차 배포(`1.금융상품/`, 2026-07-11 스냅샷). 본 문서는 실측 수치 기반이며 추정치는 "추정"으로 표기.

---

## 0. 주최 공지 요지 → 우리 설계에 대한 의미

| 공지 | 의미 / 조치 |
| :--- | :--- |
| 7/11 데이터는 정합성·코드 이슈 → **8/24 배포본 기준으로 교체** | 🔴 `1.금융상품/` 교체 + `build_db.py` 재빌드. **기준일 2026-07-11 → 2026-08-22**(해외 8/23 KST). `src/runtime/gate.py:DATA_CUTOFF`, `src/api/main.py` description, 코드북 `as_of`, 각종 문서의 7/11 전부 갱신 대상 |
| **코드값 테이블 제공 불가** (확정) | 코드 의미는 우리가 자력 판정 — 그런데 2차 데이터가 **코드 옆에 "명(NM)" 컬럼을 대거 추가**해 상당 부분 자체 해소됨 (`PD_RISK_NM`, `ZRIN_BTYP_NM`, `ZRIN_PTN_NM`, `HAN_CLAS_*`, `BD_INTP_TCD`가 '이표채' 같은 한글값). 남는 코드: 펀드 `PRFD_ATTR_CDS`(C101…)는 `ZRIN_ATTR_NMS`가 동일 순서로 명칭 제공 → 코드북 자동 생성 가능 |
| **0/결측 = 의도된 값**. 조회 시 빼거나 "없음" 응답 | 기존 3원칙("값을 고치지 말고 배제") 그대로 유효. 다만 0을 "값 없음"으로 취급하는 `query_rules`가 도메인별로 필요 (예: 채권 `EXG_CLOSE_PRICE` 0 = 75%) |
| 평가 35문항 = 상10/중10/하10 + **답변불가 5** | PROJECT.md의 30문항 → 35문항으로 정정. 답변불가 5는 별도 카테고리로 명시됨 |
| **교차질의** 예시: "삼성전자 보유 국내/해외ETF·공모펀드 연수익률 TOP10" | 🔴 Holdings 교차(ETF+펀드 동시) + 종목→상품 역인덱스 + 도메인 간 수익률 정렬 = 명시적 요구. 해외ETF Holdings(186종만 수집)가 병목 |
| 공시·시장데이터 **~8/24 발행분까지 사용 가능** | 🔴 외부 수집 제약이 7/11 → **8/24로 완화**. 기존 7/10 스냅샷 Holdings는 "유효하지만 구식". 재수집 여부는 §4 참조 |
| `BUYABLE_QUANTITY` 무효. 상폐/리스팅종료 제외 **전부 "구매가능"** 가정 | 채권 매수가능수량 질의 → 컬럼 무시. "구매 가능?" 질의는 상장폐지 여부(ETF `PD_LSTE_DT`/`PD_SALE_YN`, 펀드 `SALE_YN`, 채권 `MAT_DT` 경과)로만 판정 |
| 응답시간 기준 비공개, 300초 = 물리적 미응답. 늦어도 응답하면 점수 차등 | 기존 3층(15/60/300) 해석 유지. "늦어도 응답"이 무응답보다 낫다 → 타임아웃 시 부분응답 fallback 설계 |

---

## 1. 테이블별 변화 요약

| 테이블 | 행 (구→신) | 컬럼 (구→신) | 키 겹침 | 한 줄 평 |
| :--- | :---: | :---: | :--- | :--- |
| 국내채권 `prbd01n001` | 42,394 → **21,882** | 40 → **58** (+19 / −1) | PD_NO 기준 공통 16,965 · 신규 3,532 · **소멸 25,429** | 만기 경과분 정리 + 판매 LOT 구조(`INFO_SEQ`) 도입. 매매/장내종가/이자지급구분 등 실질 컬럼 대폭 추가 |
| 국내ETF `pref01n001` | 1,734 → **1,780** | 73 → **98** (+33 / −8) | 공통 1,734 · 신규 46 · 소멸 0 | 🟢 **기초지수 결측 96.7% → 2%** (`REF_BASE_INDEX`). 변동성·분배금 상세·채권ETF 듀레이션 추가. ⚠️ 레버리지 부호 소실 |
| 해외ETF `pref02n001` | 5,646 → **6,037** | 49 → 49 (동일) | ISIN 공통 5,568 · 신규 395 · 소멸 20 | 구조 불변, 시세만 8/21로 갱신. 기초지수 센티넬 여전(2,920건) |
| 공모펀드 `prfd01n001` | 95,619 → **23,676** | 45 → **75** (+31 / −1) | STD_ITM_NO 공통 11,128 · 신규 7,821 · 소멸 0 | 🟢 **속성코드 중복행 해소 → `ITM_NO`가 진짜 PK**. 🟢 **보수 4종 제공**. 판매완료 12,714건 추가 수록 |

---

## 2. 도메인별 상세

### 2.1 국내채권 — 구조가 바뀌었다 (가장 손 많이 감)

**행 감소 원인**: 소멸 25,429건 중 67.5%가 만기 경과(MAT_DT < 8/22). 나머지는 장외 정리(장외 17,645 → 4,136). 신규 3,532건 중 7/11 이후 발행은 18%뿐 → 나머지는 1차에 누락됐던 기존 종목(주최가 말한 "정합성 이슈"로 추정).

**PK 변경**: `PD_NO` 단독 → **`PD_NO + PD_EXG_MKT + INFO_BASE_DT + INFO_SEQ`** (스키마 NOT NULL 4개). 동일 PD_NO 2~4행 1,078건. 채권 조회 시 `GROUP BY PD_NO` 또는 대표행 규칙 필요.

**신규 컬럼 (19)** — 질의 커버리지 직접 확장:
| 컬럼 | 내용 | 활용 |
| :--- | :--- | :--- |
| `BD_INTP_TCD` | 이표채 18,059 / 복리채 2,867 / 할인채 689 / 단리채 267 | 🔑 할인채 `SRFC_IRT` 수수께끼(§D.15)의 **공식 근거**. 이름 파싱 불필요 |
| `BD_INRT_TCD` | 고정 / 변동 / 고정+변동 | FRN 판정 공식화 |
| `BD_OFR_TCD` | 공모 19,875 / 사모 2,007 | 사모 필터 |
| `PD_RISK_NM` | '낮은위험(5등급)' 등 7종 | 코드 11~16 해석 확정 (0 = 해당없음 19건) |
| `PD_PEN_TR_YN` | 퇴직연금 편입가능 Y 1,931 | 연금 질의 |
| `EXG_CLOSE_PRICE/YIELD/_BASE_DT` | 장내 종가 | 0이 75% → "값 없음" 규칙 필수 |
| `TRADE_PRICE`, `BDBNS_ABL_CHNL_NM`, `SALE_YIELD_BASE_DT` | 판매 LOT 정보 | 판매행 634건(1차 881) |
| `EXRT_GRTE_ERN_R(_TCD)`, `EXRT_RPY_R` | 만기보장수익률·상환율 | TCD 99=해당없음 21,338 |
| `BD_TISU_A`, `DEPO_EQUIV_YIELD_495` | 총발행금액, 49.5% 세율 환산 | |

**삭제**: `PD_EVCO_CRD_GRD`(평가사별 등급 "AAA, AAA, AAA") → `CRD_GRD` 단일값만. 신용등급 결측 41.1% → **18.4%** 개선. 등급 표기 20종 → 15종 (`C0`, `B-` 등 투기등급 등장 — `credit_grade_scale.csv` 서열 재검토).

**주의**: `CRD_GRD_DT`가 "등급 미변경 시 과거 일자로 유지될 수 있음"이라고 스키마가 명시 → 최신성 판단 근거로 쓰지 말 것. 고정폭 공백 패딩은 여전(`BD_KND`, `PD_PBCM`, 영문명) → TRIM 규칙 유지.

### 2.2 국내ETF — 데이터가 좋아졌다, 한 군데만 나빠졌다

**🟢 개선**
- `REF_BASE_INDEX` 905종 고유값, ETF 1,235건 중 결측 2% (ETN은 100% 결측 = 구조상 정상). 기존 `CU_BASE_INDEX`(182건)는 그대로 남아 있음 → **REF_ 계열을 1순위**로. 우리 `index_master_draft.csv`(94종)는 REF_BASE_INDEX 기준으로 재작성 대상.
- `REF_FUND_MGMT_CO`(영문 정규 법인명 29종) → `CU_FUND_MGMT_CO` 100종 오염("메리츠증권 주식회사"/"한국투자증권(주)" 혼재) 정규화의 정답지.
- `REF_AST_TYPE`(Equity/Bond/…) · `REF_GEO_FOCUS`(23개국) → `asset_type_map.csv`·`region_map.csv` 교차검증.
- `PD_TICKER`/`PD_ISIN_CD`/`PD_RIC` → 외부 Holdings 조인 키 단순화 (6자리 티커).
- 변동성 4종(`DU_VLTY_1M/3M/6M/1Y`), 분배금 상세(`PD_DIVD_AMT_ANN`, `PD_DVID_PAY_MONTHS` — "월배당" 질의 직답), 채권ETF `FN_*`(듀레이션·만기, 199건).

**🔴 회귀: `CU_LEV_FECTOR` 부호 소실**
1차: −2.0 105건 / −1.0 65건 → 2차: −2.0 16건 / −1.0 7건. **"KODEX 인버스"·"ACE 인버스"가 1.0**으로 수록됨. 인버스 이름 225건 중 음수는 22건뿐.
→ 방향(인버스 여부)은 **상품명 '인버스' 키워드 + `CU_LEV_FECTOR` 절댓값**으로 판정해야 함. yaml `query_rules`에 명시. 주최 우선 원칙이 있으나 이 컬럼은 "값이 틀린" 게 아니라 "부호 정보가 빠진" 것이므로, 답변 시 "레버리지 배수 2배(인버스)"처럼 이름 근거를 병기.

**삭제 8종**: `PD_SECT_NM`, `PD_NAV_PSHR`, `PD_NET_AST_PSHR`, `PD_NET_PRFT_PSHR`, `PD_NET_RT_AST_PSHR`, `NRU_MKT_INAV`, `NRU_MKT_DIFF_RT`, `PD_LST_PRICE`. `ontology/enums/domestic_etfs.yaml`에 4건 참조 → 제거. `etf_sector.csv` 코드북은 `PD_SECT_CD`(5종)만 남았으니 NM 없이 판정.

**기타**: 신규 46건 중 28건이 7/11 이후 상장(최소 6/23). ETN 545건(30.6%)은 여전히 혼입 → `PD_GRP_NO` 분리 규칙 유지. 총보수 `CU_CHARGE_RT` 실질 67건 — **개선 없음**(외부 보완 여전히 필요).

### 2.3 해외ETF — 갱신만, 구조 동일

- 신규 395건 중 157건이 7/11 이후 상장. 소멸 20건은 상폐(Tradr 2X Short NBIS 등 레버리지 단기상품).
- 시세 기준일 `DU_CLPR_BASE_DT` 8/21 5,687건. 일부 6/9, 7/28 등 구식 잔존 → 답변 시 기준일 병기 규칙 유지.
- 기초지수 센티넬 2,705 → 2,920건(**악화**). `CU_INDEX_REPL_MTHD`(Optimized/Swap/Full) 60% 결측 여전.
- `CU_LEV_FECTOR` 부호는 **정상**(−2.0 92, −3.0 34). 국내ETF와 다름.
- 거래소 코드 `PD_EXG_MKT_CD`에 `'102'`, `'101'` 31건 혼입(AMX/NAS/NYS 외) → `market.csv` 코드북 보강.
- Holdings: 우리 수집 186종(NPORT-P, 기준일 3/31~5/31) vs 마스터 5,972종 → 교차질의("삼성전자 보유 해외ETF") 커버리지의 최대 약점. 8/24까지 허용되므로 6/30 기준 NPORT-P 재수집 가치 있음.

### 2.4 공모펀드 — 구조 정상화 + 보수 제공 (가장 큰 이득)

**🟢 PK 정상화**: `ITM_NO` 23,676 = 행수 (유일). `STD_ITM_NO`는 18,948 고유 + **결측 515건**(전부 판매완료). 소수(32건)의 STD_ITM_NO가 ITM_NO 2~6개를 가짐 → 클래스 묶음 키로 해석. 기존 "GROUP BY std_itm_no 필수" 규칙 → **행 단위 조회 가능**으로 단순화.

**🟢 속성 코드 집약**: `PRFD_ATTR_CD`(행 폭발 원인) → `PRFD_ATTR_CDS`("C101,V101,D102,C103") + `ZRIN_ATTR_NMS`("추가,국내,개방,국내위탁판매") + `PRFD_ATTR_CNT` + `PRFD_ATTR_SEARCH_TEXT`. ⚠️ 두 컬럼은 **위치 대응이 아님**(yaml 재작성 중 실측: 개수 같은 11,253행 중 위치 일치 22건) — 코드북은 `prfd_attr_search_text`('코드 명칭' 인접쌍)에서 도출해야 함. 결측 52.4%(판매완료분 대부분).

**🟢 보수 4종 신규** — `OR_CO_RWRD_R`(운용) / `SALE_CO_RWRD_R`(판매) / `TRUSC_RWRD_R`(수탁) / `OFWK_TRUS_RWRD_R`(사무관리). 결측 0%, 전부 0인 행 3%.
→ **기존 간이투자설명서 8,443건 파싱(총보수 8,227건)의 위상 변경**: 주최 데이터 우선 원칙에 따라 마스터 보수를 1순위, 설명서는 "총보수(TER)·환매수수료·판매수수료" 등 마스터에 없는 항목 보완용으로 강등. 단위 검증 필요 — 샘플 `SALE_CO_RWRD_R` 8.0, 10.0 등 % 단위로 보기 어려운 값 존재(bp? 또는 ‰?) → 설명서 파싱값과 대조해 단위 확정 (검토 지시 F 항목 추가).

**🟢 기타 신규**: `HAN_CLAS_NM/FEE_TYPE/SALES_CHANNEL/POLICIES`(클래스 체계 — 수수료선취/미징구, 온/오프라인, 퇴직연금/개인연금 등 → "연금 가능 펀드" 질의 직답) · `ZRIN_BTYP_NM`(18종 대분류) · `ZRIN_PTN_NM`(102종 소분류: 중국주식, 북미주식…) · `ZRIN_*_CMST_RT`(국내주식/해외채권 등 자산구성 비율 7종) · 최근 분배(`FD_LAST_DSTB_R` 등) · `BNS_BPR`(기준가) · `FD_PRSV_R`.

**주의**
- 🔴 **사모 8,960행 유입** (`prvo_pbff_desc='사모'`: 판매중 1,993·판매완료 6,967 — 1차엔 공모 95,451/95,619). 공모펀드 마스터인데 사모가 섞였으므로 **기본 모수 = 판매중 AND 공모 = 8,969행**.
- `SALE_YN` 판매완료 12,714건이 추가됐고 이들은 위험등급 99%·1년수익률 99% 결측 → Top-N 집계는 **판매중·공모 8,969건으로 한정**하는 규칙 필요. 판매중 기준 결측률: 위험등급 22%, 1년수익률 37% (1차 19%·33%와 유사).
- 운용사 `OR_CO_XTN_ITT_CD` 275종(1차 67종) — 원본 셀은 문자열 '00040011'로 선행0 보존됨. `build_db.py`의 `CODE_COLUMNS` 그대로 유효. 코드북 `asset_manager.csv`(67종) → 275종으로 확장 필요(판매완료분 포함이라 실질 판매중 기준은 별도 집계).
- `FD_WK1_ERN_R` 100% 결측(신규 결측), `ITM_EABRV_NM` 99% 결측.
- 벤치마크 `BMRK_NM` 결측 52% (1차 대비 악화 — 판매완료 포함 효과).
- 위험등급이 공통 키 29%에서 값 변경(예: 2→1) → 1차 기준 검토 결과·문서의 등급 수치는 재확인.

---

## 3. 우리 코드·자산에 대한 영향 체크리스트

| 자산 | 영향 | 조치 |
| :--- | :--- | :--- |
| `scripts/build_db.py` | 파일명 패턴 `*_datarows.xlsx`/`_schema.xlsx` 키워드 매칭 실패 (신규는 `prbd01n001_data.xlsx`), 스키마 시트 구조 변경(`header=1` 가정 깨짐, 순번/컬럼명/데이터타입/Nullable/컬럼코멘트 5열) | 🔴 파일 탐색·스키마 파서 수정. 컬럼명이 소문자로 옴(1차 채권만 대문자) → 대소문자 통일 정책 결정 |
| `data/financial_products.db` | 전면 재빌드. `ext_*` 테이블은 유지 가능 | 재빌드 후 `kg_coverage_report.md` 재생성 |
| `ontology/enums/*.yaml` (4) | 삭제 컬럼 참조 23건(펀드 14·채권 5·ETF 4) + 신규 컬럼 31+19+33 미기술 + 결측률·판정 수치 전부 구식 | 🔴 도메인 담당자별 재작성. 검토 지시서(`review_request_2026-08-20.md` 21건)는 **2차 데이터 기준으로 재판정** 필요 — 상당수(B 할인채, F 보수, E 기초지수)는 데이터로 해소 |
| `ontology/ontology.ttl` | 속성 추가(보수·클래스·이자지급구분 등), 채권 PK 복합화 | 클래스/속성 보강 |
| `ontology/codebooks/*` | `as_of` 7/11 → 8/22. `asset_manager` 67→275, `market` 코드 추가, `etf_sector` NM 소실 | 갱신 |
| `scripts/gen_entity_map.py` | 삭제 컬럼 3건 참조 | 수정 |
| `src/runtime/gate.py` | `DATA_CUTOFF = "2026-07-11"` | `2026-08-22` |
| `src/api/main.py` | description 기준일 | 갱신 |
| 외부 수집물 (`data/external/`) | 국내 Holdings 7/10, 해외 NPORT-P ≤7/11, 환율 7/10 — 제약은 8/24로 완화 | §4 |
| 문서 (PROJECT.md 등 30+파일) | 기준일·행수·결측률 | PROJECT.md §3 표 우선 갱신, 나머지는 "1차 데이터 기준" 주석 |

---

## 4. 외부 수집 트랙 재평가

| 수집물 | 2차 데이터로 대체됨? | 판단 |
| :--- | :---: | :--- |
| 펀드 총보수 (설명서 8,227건) | **부분** (마스터 보수 4종) | 마스터 1순위, 설명서는 TER·환매수수료 보완. 단위 대조 선행 |
| 펀드 개방/폐쇄·추가/단위형 | **대체** (`ZRIN_ATTR_NMS`) | 설명서 추출본은 검증용으로 강등 |
| 국내ETF 기초지수 (`index_master_draft` 18종) | **대체** (`REF_BASE_INDEX` 905종) | 재작성 |
| 국내ETF Holdings 7/10 | 아니오 | 유효. 8/24까지 허용되므로 **8월 스냅샷 재수집 권장**(교차질의 예시가 Holdings 기반) |
| 해외ETF Holdings 186종 | 아니오 | 커버리지 3% — 가장 큰 구멍. 6/30 NPORT-P 재수집 + 최소 대형 ETF 수백 종 확장 |
| 신용등급 서열 코드북 | 아니오 | `C0`, `B-`, `BB-` 등 투기등급 추가 검증 |
| 환율 7/10 | 아니오 | 8/21 기준으로 교체 |
| 채권 할인채 SRFC_IRT 판정 | **대체** (`BD_INTP_TCD`) | 문서 §D.15 결론을 공식 컬럼으로 승격 |

---

## 5. 착수 순서 제안

1. **`1.금융상품/` 교체 + `build_db.py` 수정·재빌드** (파일명·스키마 파서·컬럼명 케이스) — 모든 후속 작업의 전제
2. **`gate.py` / `main.py` 기준일 8/22로**, PROJECT.md §3 표 갱신 (35문항·기준일·행수)
3. 도메인별 yaml 재작성 — 삭제 컬럼 제거 → 신규 컬럼 기술 → 결측/0 규칙 (펀드: 판매중 한정 / 채권: PK 복합·LOT 대표행 / ETF: 인버스 이름 판정)
4. 펀드 보수 단위 확정(설명서 대조) → 보수 질의 경로 마스터로 전환
5. Holdings 재수집 여부 결정(국내 8월분, 해외 확장) — 교차질의 예시가 명시된 만큼 우선순위 높음
6. 검토 지시서 21건 중 데이터로 해소된 항목 닫기, 남은 항목 2차 기준으로 재발행

---

## 6. 우리가 "비었다"고 판정했던 칸 — 2차 데이터 대조 (2026-08-25 실측)

기준: `ontology/enums/*.yaml`의 `missing_reason`/`answer_policy` 판정 vs 2차 데이터 실측. 🟢 = 채워짐(정책 폐기·완화), 🟡 = 부분, 🔴 = 그대로, ⚫ = 컬럼 자체 삭제.

### 국내ETF (ETF 1,235건 기준 · ETN 545는 구조상 대부분 결측)

| 우리 판정(1차) | 2차 실측 | 판정 |
| :--- | :--- | :---: |
| 기초지수 `cu_base_index` 유효 58건, "질의는 58건 한정" | `cu_base_index`는 그대로 59건이나 **신규 `ref_base_index` ETF 1,208/1,235 (97.8%) · 905종** | 🟢 정책 폐기 → REF_ 1순위 |
| 분배금 `pd_divd_amt_pshr` "전부 0 = 미입력, 데이터 미제공" | 유효 967 (78%) · 0값 241 (무분배 추정) | 🟢 배당액·배당수익률 Top-N 가능 |
| 분배주기 `pd_dvid_cycl` "유효 0건, 100% 결측" | ETF 1,235 전건 유효 (Q/M/A 등 4종) + `pd_dvid_pay_months` 지급월 15패턴 | 🟢 "월배당 ETF" 직답 가능 |
| 배당수익률 `pd_dvid_yield` 미제공 | 유효 967 (78%) | 🟢 |
| 추적오차 `du_chas_errt` "전부 0" | 유효 1,160 (94%) | 🟢 |
| 괴리율 `du_diff_rt` "전부 0" | 유효 1,131 (92%) | 🟢 |
| 실시간가 `ru_mkt_price`·`ru_mkt_volume` 100% 결측 | 유효 95%/93% (종가·거래량으로 채워짐, 해외와 같은 방식) | 🟢 단, "실시간"은 아님 — 8/21 종가 명시 |
| iNAV `nru_mkt_inav`·`nru_mkt_diff_rt` 100% 결측 | 컬럼 삭제 | ⚫ yaml에서 제거 |
| `pd_nav_pshr`·`pd_net_*_pshr`·`pd_lst_price`·`pd_sect_nm` | 컬럼 삭제 | ⚫ |
| AUM `du_last_aum` 결측 16.2% + 0값 411 | ETF 기준 결측 4.8% · 0값 6 | 🟢 |
| 총보수 `cu_charge_rt` 실질 67건 | **그대로 67건** (0값 150) | 🔴 외부 보완 여전히 필요 |
| 기타비용 `cu_charge_etc_rt` 전부 0 | 그대로 전부 0 | 🔴 |
| 1년수익률 `-100` 센티넬 98건, 2,738% 이상치 | `-100` 110건(ETF 35) 여전 · max 926% · 2,000% 초과 0건 | 🟡 센티넬 규칙 유지, 이상치 단서 완화 |
| 레버리지 `cu_lev_fector` 유효 | 값은 95% 유효하나 **부호 소실** (인버스 225건 중 음수 22) | 🔴 **회귀** — 이름 판정 규칙 신설 |
| (신규) 변동성 `du_vlty_1y` | 유효 79% | 🟢 "변동성 낮은 ETF" 질의 신규 가능 |

### 해외ETF (6,037건)

| 우리 판정(1차) | 2차 실측 | 판정 |
| :--- | :--- | :---: |
| 기초지수 센티넬 2,705 → 유효 51.9% | 센티넬 2,920 · 유효 3,106 (51.4%) | 🔴 그대로 (약간 악화) |
| 1일수익률 `du_er_1d` "전부 0, 수익률 질의 불가" | **유효 5,898 (97.7%)** | 🟢 단 1일치뿐 — 기간수익률 컬럼은 여전히 없음 |
| `cu_lev_fector` 100% 결측 | 유효 901 (14.9%) — 레버리지 상품엔 대부분 수록, **부호 정상** | 🟡 `cu_inverse_short_yn`(Y 183)과 병행 |
| `wu_core_yn` 98.2% 결측·전부 N | 그대로 (N 106건뿐) | 🔴 사용 금지 유지 |
| `cu_index_tracking_yn` 전부 Y | 그대로 (58~60% 결측) | 🔴 |
| `du_last_nav` 87.9% 결측 | 87.4% | 🔴 |
| `pd_lstg_dt` '00000000' 8건 위장결측 | 0값 **11건** | 🔴 규칙 유지, 건수 갱신 |
| `du_diff_rt` | 100% 결측(3건 제외) | 🔴 |
| 총보수 결측 0% | 그대로 (0값 419) | 🟢 유지 |

### 공모펀드 (판매중 10,962건 기준 — 판매완료 12,714는 평가컬럼 99% 결측이라 별도 취급 · ⚠️ 이 중 사모 1,993 포함, 공모만은 8,969)

| 우리 판정(1차) | 2차 실측 (판매중) | 판정 |
| :--- | :--- | :---: |
| **보수 미제공** → "확인 불가" 필수, 설명서 8,227건으로 대체 | `or_co/sale_co/trusc/ofwk_trus_rwrd_r` **결측 0%** (3종 전부 0인 행 101건) | 🟢 마스터 보수로 전환. ⚠️ 단위 미확정 — 예: 차이나솔로몬 C2 운용 10.15·판매 14.0·수탁 0.6 → "값÷10 = %" 가설(운용 1.015%·수탁 0.06%). 설명서 대조로 확정 필요 |
| 위험등급 결측 16.6%(판매중 0.3%) | 판매중 **22.0% 결측** (1차보다 악화) | 🟡 "판매중 결측 27종목" 서술 무효 — 재산출 |
| 1년수익률 결측 32.6% | 판매중 36.9% · 수익률 8기간 전부 null 3,675건 · **전부 0인 행 0건** | 🟡 `zero_as_missing.수익률_9기간전부0(69종목)` 규칙 불필요, null 규칙만 |
| 환헤지 `exchdg_yn` "국내 87.7% 결측 = 해당없음" | 국내 4,460/4,767 NULL(93.6%) · 해외 639/4,944 NULL(12.9%) | 🟢 판정 유지, 수치 갱신 |
| 순자산 0값 294종목 배제 규칙 | 판매중 0값 **0건** · 결측 23.9% | 🟢 zero 규칙 삭제, null 규칙만 |
| 벤치마크 `bmrk_nm` | 판매중 결측 22% · 255종 | 🟡 |
| 운용사 = 숫자코드 67종만 | 275종 코드 (선행0 보존) · **영문/한글명 여전히 없음** | 🔴 코드북 확장 필요 (67→275) |
| (신규) 유형 `zrin_btyp_nm`(18)·`zrin_ptn_nm`(85) | 판매중 결측 22% | 🟢 "중국주식형 펀드" 등 유형 질의 신규 가능 |
| (신규) 클래스 `han_clas_nm` | 판매중 결측 20% · 182종 | 🟢 연금/온라인/보수체감 질의 가능 |
| (신규) 자산구성비 `zrin_*_cmst_rt` | 결측 24% + 0값 다수 (국내주식 유효 8.8%) | 🟡 0 = "해당 자산 미보유"로 해석 (공지 원칙) |
| (신규) `fd_wk1_ern_r` | 100% 결측 | 🔴 신규 100% 결측 컬럼 |

### 국내채권 (21,882건)

| 우리 판정(1차) | 2차 실측 | 판정 |
| :--- | :--- | :---: |
| 약어명·영문명 TRIM 후 빈 문자열 296~298건 | 결측 3~12건 | 🟢 |
| `bd_knd` 결측 924 · `pd_pbcm` 918 | 152 · 149 | 🟢 정책 유지, 건수 갱신 |
| 발행일 0값 336건 위장결측 | 0값 **25건** · 만기일 0값 4건 | 🟢 규칙 유지, 건수 갱신 |
| 신용등급 결측 41.1% | **18.4%** · 표기 15종 (`C0` 103·`B-` 5 등 투기등급 신규) | 🟢 `credit_grade_scale.csv` 보강 |
| 듀레이션 결측 31.6% | **0.1%** | 🟢 |
| 매수/세후수익률 결측 97.9% (881건) | 97.1% (634건) — 판매 LOT만 수록 | 🔴 구조 동일 |
| `srfc_irt = 0` 2,758건 "결측 아님, 53%가 전환사채" | 0값 579건 — 이표채 499·복리채 57·**할인채 21** | 🟡 전환사채 서술 재검증. 할인채는 `bd_intp_tcd`로 직접 판정 |
| 평가사별 등급 `pd_evco_crd_grd` | 컬럼 삭제 | ⚫ |

### 요약: "비어 있음" 정책 중 폐기·완화 가능한 것

- **폐기 (데이터로 채워짐)**: 국내ETF 기초지수·분배금·분배주기·배당수익률·추적오차·괴리율·현재가 / 펀드 보수 / 채권 듀레이션·신용등급 상당분 / 해외ETF 1일수익률
- **유지**: 국내ETF 총보수·기타비용 / 해외ETF 기초지수 센티넬·기간수익률 부재·core_yn / 채권 매수수익률(판매LOT만) / 펀드 운용사명
- **신설 필요**: 국내ETF 인버스 부호 / 펀드 판매완료 분리 / 채권 복합 PK·대표행 / 펀드 보수 단위 / 신규 100% 결측(`fd_wk1_ern_r`, ETF `fn_effective_duration` 등 3종, `pd_dvid_inc_dist`)
