# 📦 인수인계 — 2026-08-18 (jeonghyeon 세션)

> 이 문서 하나로 오늘 무엇이 바뀌었고, 어떻게 이어받는지 알 수 있게 합니다.
> 커밋 범위: `be98b2e`(어제 main) → 오늘 push 분 전체.

---

## 0. 한 줄 요약

**4개 브랜치를 main 에 통합**하고, **온톨로지·KG 생성 파이프라인**을 구축했으며,
**3개 도메인 판정 전체를 실데이터로 감사·정정**하고, **런타임 파이프라인(Ground·Gate·Guard·Execute)을
가동 상태**로 올렸다. 남은 큰 조각은 HCX Planner 연결·Holdings 수집·평가 하니스다.

## 1. 브랜치 통합 (전원 공지 사항)

- `eda/domestic_bonds`(+3) · `eda/domestic_etfs`(+16) · `research/2025-winner-benchmark` 를
  **전부 main 에 병합했다.** 커밋·파일 손실 0 검증 완료 (각 브랜치 `main..브랜치` = 0).
- 🔴 **이제 main 만 pull 하면 최신이다.** 병합된 eda 브랜치는 각자 정리(삭제)해도 된다.
- 충돌은 `.gitignore` 1곳(코드북 예외 중복)뿐이었고 main 쪽 유지로 해소.

## 2. 새로 생긴 것 — 사용법 순

### 2.1 온톨로지·KG 파이프라인 (`scripts/build_ontology.py`)

```
EDA 발견 → ontology/shared/*.yaml 또는 enums/*.yaml 에 기록 (미확정은 status: pending)
→ python scripts/build_ontology.py
→ 검증(죽은 alias·충돌·codebook 규정) → ontology.ttl + DB kg_* 4테이블 전체 재생성(멱등)
→ data/kg_coverage_report.md 리포트 — 미매핑 distinct 가 다음 EDA 우선순위
```

- `ontology/shared/` 6축 초판: currency · risk_grade · asset_class · region(계층 closure) ·
  index(펀드↔ETF 완전일치 17쌍) · organization(코드북 45브랜드)
- 현재 kg_node 103 · kg_alias 192 · 검증 오류 0
- 🔴 **`.ttl`·kg_* 는 생성물 — 손으로 고치지 말 것.** yaml 만 고치고 재생성한다.

### 2.2 런타임 파이프라인 (`src/runtime/`) — `/answer` 에 연결됨

```
질의 → [Ground] KG 개체 매핑 → [Gate] HCX 0회 기각 3종 → [Plan: 미연결] → [Guard] SQL 검사 → [Execute]
```

- **가동 중**: absent 기각("위험등급 낮은 해외ETF" → 온톨로지 근거 문장으로 기각) ·
  enum 기각(신용등급 AAAA · 위험등급 범위 밖) · cutoff(기준일 이후) · KG 매핑 ·
  SQL 사후검사(SELECT 단일문·화이트리스트·LIMIT) · read-only 실행 · 0건 시 환각 방지
- **미연결**: Plan(HCX SQL 생성)·Answer(문장 생성) — `pipeline.Planner` 프로토콜에
  `plan_sql`/`compose_answer` 구현체를 꽂으면 된다 (`src/hcx/client.py` 재사용).
- 테스트: `python -m pytest tests/test_runtime.py` — 11개, 전부 오프라인 (HCX·네트워크 불요)

### 2.3 로컬 환경 (이 세션에서 만든 것 — git 밖)

- `.venv/` (Python 3.14 + requirements) · `data/financial_products.db` (build_db.py 재빌드, 145,393행)
- 원본 엑셀 8개는 `1.금융상품/` 에 배치됨

## 3. 판정 감사 (2차 검증) — 3도메인 완주

각 enums yaml 의 수치 주장을 실데이터로 재현하고 교차 함정 패턴을 적용했다. **판정 파일이
수정됐으므로 각 도메인 담당자는 자기 yaml 의 `2026-08-18 감사` 표시 부분을 확인할 것.**

| 도메인 | 핵심 변경 | 상세 문서 |
| :--- | :--- | :--- |
| 공모펀드 | row_grain 완전증명(행=종목×prfd_attr_cd) · zrin dtype 정정 · 수익률정상 규칙 개정 | `docs/eda/public_funds_audit_2026-08-18.md` |
| ETF | 🔴 **du_nav_yday 판정 뒤집힘 — ETN 379건은 정상** · du_clpr=0 167건 정정 · pd_dvid_cycl 유효 0건 정정 · ISIN 중복 50종 | `docs/eda/etf_audit_2026-08-18.md` |
| 국내채권 | EVAL 10000.0 파킹 전수 스윕(1,011+만기전 1,813) · DUR 위반 실질 14건 · 플래그 4종 패턴 불일치 | `docs/eda/domestic_bonds_audit_2026-08-18.md` |

감사 절차 템플릿: 펀드 감사 문서 §5 — 재사용 3회 검증 완료.

## 4. 워크샵 안건 — 한 장으로 취합됨

🔴 **`docs/workshop_agenda_2026-08-18.md`** — 안건 22건을 5분류(인프라/판정/규칙/KG축/외부요청)로
정리. 최우선 2개:
1. **정본 DB 확정** — 배포 서버 DB(dtype=str)와 재빌드 DB(REAL)의 dtype 이 다를 수 있다
2. **주최 측 질문지(⑰ 5건) 발송** — 회신 대기가 기니 먼저

## 5. 남은 큰 조각 (착수 순 제안)

> ⚠️ 같은 날 병행된 **웹 수집 트랙**(`70df09f` · 인수인계 `fc0c502`, `docs/PROGRESS_2026-08-18.md`)이
> 이 목록의 일부를 이미 해소했다 — 반영해 갱신함.

1. **HCX Planner** — §2.2 의 빈 자리. query_rules 자동 주입(질의 컬럼 매개변수형 수익률정상 포함)
2. **Holdings** — 🔵 펀드 구성종목 5.9만 행 수집됨(ext_fund_holdings 테이블). 남은 것:
   kg_edge 로의 반영 + ETF Holdings(FunETF 트랙) + ISIN 중복 50종 정체 확정 (워크샵 D-1)
3. **평가 하니스** — eval/ 에 채권·펀드 질의 10문항 없음 + 채점 스크립트
4. 코드북 — 🔵 운용사 67/67 법인명 완료 · 수탁사 16/18. 잔여: 수탁사 2종 · KG D/E 잔여 반영 확인

## 6. 일정 리마인더

- **8/30**: 서버 재기동 데드라인 (늦어도 9/5 · 9/7~20 정지 불가 — `docs/INFRA.md`)
- 재기동 시 DB 반입 여부는 워크샵 A-1(정본 DB)과 함께 결정
- **9/6**: 제출 마감

## 7. 병행 트랙 참조

같은 날 웹 수집 트랙이 별도로 진행됐다 — 운용사·수탁사 법인명 확보, 펀드 Holdings 수집,
판정 확정 다수(fd_set_pcd '20'=단위형 · or_attr_desc '06'=파생형 · fd_nast_suma 0=센티넬 확정 ·
M109⇒자펀드). 상세는 `docs/PROGRESS_2026-08-18.md` 와 커밋 `70df09f`·`fc0c502` 참조.
→ 워크샵 안건 중 **B-3(fd_nast_suma)·D-6(운용사 코드북) 은 사실상 해소**됐다.
