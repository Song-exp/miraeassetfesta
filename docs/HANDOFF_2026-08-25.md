# 🤝 세션 인수인계 — 2026-08-25 (2차 데이터 전환 완료 · 교차질의 재료 적재)

> 무엇이 바뀌었나: `DATA_V2_2026-08-24_impact.md` (테이블별 변화 · §6 결측 판정 대조)
> 주최가 확정한 규칙: `PROJECT.md` §2 "주최 Q&A 확정 사항" (원문 `QNA_REVIEW_2026-08-25.md`, 미답변 대장 §3)
> 검토 지시서 상태: `review_request_2026-08-20.md` 상단 표 (종결 9 · 재검증 8 · 재작성 1)
> 브랜치: **`data/v2-20260824`** — main 에 병합 전. `git checkout data/v2-20260824` 후 작업.

## 오늘 한 일 (전부 커밋됨)

| 영역 | 내용 |
| :-- | :-- |
| 데이터 | `1.금융상품/` 을 2차 배포본(8파일)으로 교체, 1차는 `_v1_20260711/` 보관. `build_db.py` 재작성(파일명·스키마 형식·선행0 15컬럼·`build_info`). DB 재빌드 — 채권 21,882 · 국내ETF 1,780 · 해외ETF 6,037 · 펀드 23,676 |
| 기준일 | `gate.py`·`main.py`·`build_ontology.py`·수집 스크립트 3종 → 마스터 8/22, 외부자료 ≤ 8/24 |
| 온톨로지 | 4개 도메인 yaml 전면 재작성(삭제 컬럼 제거·신규 83컬럼 판정·query_rules 신설) · `risk_grade.yaml` 채권 `'11'~'16'` · 검증기 대소문자 무시 · `*.auto.yaml` 재생성 · `build_ontology` 통과(kg 120노드·ttl 202줄) · `gen_entity_map` 75/75 |
| 코드북 | 운용사 275종(`gen_asset_manager_codebook.py`, 판매중 법인명 커버 90%) · 시장 `'101'/'102'` 접미 추정 · 환율 **8/21 = 1,384.23** · 전 코드북 `as_of` 8/22 |
| 런타임 | 컷오프 8월 허용(9월+ 기각) · `RuntimeContext.planner_context()`(yaml 규칙 → 플래너 프롬프트) · SQL 가드 `ext_*` 조인 허용(단독 금지) · 교차질의 감지 `is_cross_query` · 테스트 16/16 |
| 외부 데이터 | 국내·해외 ETF 구성종목 DB 적재(`load_external_holdings.py` → `ext_etf_holdings` 75k · `ext_ovs_etf_holdings` 275k). "삼성전자 보유" 실측 국내ETF 233 · 해외ETF 13 · 펀드 941 |
| 확정 사실 | **펀드 보수 4종 단위 = ‰** (마스터 = 설명서 % × 10, 8,925건 대조) |
| 평가·문서 | `eval/questions_cross_and_unanswerable.jsonl` 9문항 · 제안서 데이터 파이프라인 절 초안 `proposal_data_pipeline_section.md` |

## 🔴 새로 발견한 함정 (yaml 에 반영됨 — 답변 로직이 지켜야 할 것)

1. **펀드에 사모 8,960행 유입** — 기본 모수는 `sale_yn='판매중' AND prvo_pbff_desc='공모'` = **8,969행** (판매중 전체 10,962 아님)
2. 국내ETF `cu_lev_fector` **부호 소실** — 인버스는 `pd_abrv_nm LIKE '%인버스%'` 로 판정
3. 채권 PK 복합화 — 종목 단위 질의는 `GROUP BY pd_no`, 판매 정보는 `buy_yield IS NOT NULL` 행
4. `prfd_attr_cds` ↔ `zrin_attr_nms` 는 위치 대응 아님 — 코드북은 `prfd_attr_search_text` 에서
5. `fd_prsv_r`(판매보수 복제 추정)·`fd_sbpr`(의미 불명) 사용 금지, `std_itm_no` `'00000     '` 위장결측 145행
6. 해외ETF 는 여전히 1일수익률만 — 교차 TOP-N 에서 "연수익률 미수록·제외" + 1일수익률 병기 (OQ-2 미답변)

## 남은 일 (우선순위순)

| # | 작업 | 담당 제안 | 비고 |
| :-: | :-- | :-- | :-- |
| 1 | **HCX 플래너 연결** — `Planner` 프로토콜 구현, `ctx.planner_context(tables)` 를 프롬프트에 주입, 교차질의면 `is_cross_query` 로 ext 조인 허용 | Song-exp | 현재 파이프라인은 Ground→Gate→Guard→Execute 골격만 동작 |
| 2 | **OQ-2 재질의 발송** (해외ETF 연수익률 부재 시 제외가 정답인지) + OQ-1(임베딩 모델 제약) | 팀장 | `QNA_REVIEW_2026-08-25.md` §3 |
| 3 | **Holdings 재수집 결정** — 국내 8월 스냅샷(`fetch_etf_holdings.py` 날짜 상수), 해외 6/30 NPORT-P + AUM 상위 확장(현재 3.1%) | ETF 담당 | 8/24 까지 허용. 몇 시간짜리 크롤 |
| 4 | 속성코드 코드북 — `prfd_attr_search_text` 인접쌍에서 코드↔명칭 도출 스크립트 | 펀드 담당 | yaml 에 방법 기록됨 |
| 5 | 검토지시서 "재검증 8건"(B1·B2·B3·B5·B8·E2/4/5/7·F2) 을 2차 수치로 재현 | 각 담당 | 대부분 신규 컬럼으로 대체 가능 |
| 6 | `gen_entity_map.py` 본문 다이어그램의 `prfd_attr_cd`·"45컬럼" 하드코딩 정리 | 아무나 | 생성 문서 서술만 구식 |
| 7 | 운용사 코드북 derived 208종 중 판매중 상위(타임폴리오 99·피델리티 47…) 법인명 확정 | 펀드 담당 | 웹 `mgmt_co_nm` 이 없는 코드들 |
| 8 | PROJECT.md §3 표를 2차 수치로 교체 (현재 경고문으로 대체) | 문서 | impact 문서 §1 복사 |
| 9 | main 병합 + 팀 `git pull` 안내 + 드라이브에 새 DB(`ext_*` 4종 포함) 업로드 | 팀장 | DB 는 git 제외 |

## 착수 순서 (팀원)

1. `git fetch && git checkout data/v2-20260824` → `PROJECT.md` §2 주최 Q&A 표 → 자기 도메인 yaml 상단 이력 주석
2. 드라이브에서 새 `financial_products.db` 받기 (또는 `1.금융상품/` 2차 8파일 + `data/external/` 로 `build_db → load_external_web → load_external_holdings → build_ontology` 재현)
3. `python -m pytest tests -q` 16 passed 확인
