# 🤝 세션 인수인계 — 2026-08-30 (펀드 식별자 결측 축 정비 · ETF 브랜치 병합)

> 작업 구간: **8/29 밤 ~ 8/30 새벽**. 앞 세션은 `docs/HANDOFF_2026-08-27.md`(리드) · `docs/HANDOFF_ETF_2026-08-29.md`(ETF 트랙).
> 한 줄 요약: **"비어 있음"의 정의를 규칙 층위에서 다시 세웠고, 그 과정에서 KG 가짜 노드 19개와 교차질의 조인키 오부착을 잡았다.**
> 🆕 **그리고 그 전부가 서버에 반영됐다** — 리드 세션이 트레이스 관찰성을 붙이고 전체 배포까지 마쳤다 (§1-9·§1-10).

| 확인 | 상태 |
| :-- | :-- |
| 브랜치 | ✅ `eda/domestic_etfs` 19커밋 병합 완료 · `main = origin/main` (`9157724`) · 미커밋 0 |
| 테스트 | ✅ `pytest` **49 passed** (35 → 49, 리드 세션이 trace·qa_log·api 테스트 14건 추가 → §1-9) |
| gold | ✅ `run_gold_check` **63문항 · 통과 63 · 실패 0** |
| KG | ✅ 재생성 완료 — `kg_node` **39,658** · `kg_alias` **62,558** · `kg_edge` 7,414 · `kg_closure` 9,919 |
| 검토표 | 🟡 **208/408** (A 29/41 · B 112/138 · C 63/87 · D 0/38 · E 4/104) |
| 엔드포인트 | ✅ `agent_ready:true` · `planner:"hcx"` — **8/30 코드·DB 반영 완료** |
| 배포 | ✅ **완료 (8/30 01:51).** 서버 `9157724` · DB `3437db7f…` 278,503,424바이트 · 덮기 전 백업 보존 (§1-10) |

---

## 1. 한 일

### 1-1. 🔴 이번 세션의 축 — "결측을 만드는 규칙이 하나가 아니다"

앞 회차(`공모펀드_검토기록_2026-08-28.md`)가 식별자 9피처의 결측 **수치**를 맞췄다면, 이번엔 그 다음 질문을 팠다: **"`dummy_as_missing` 말고 결측을 만드는 규칙이 또 있는가?"**

4개 도메인 yaml 의 `normalization` 을 전부 열어 본 결과:

- **위장결측(더미) 규칙을 가진 도메인은 공모펀드 하나**뿐이다.
- 공모펀드만 해도 결측은 **여섯 갈래**다 — ⓐ NULL ⓑ 더미 ⓒ 0 ⓓ 센티넬(`fd_last_dstb_r = -100`) ⓔ 보수 3종 동시 0 ⓕ 표기 변형.
- **`missing_reason` 한 글자가 이 전부를 대표하지 못한다.**

### 1-2. 🔴 최대 소득 — 규칙을 남의 도메인에 복사하면 안 되는 반례

공모펀드 더미 규칙을 4개 도메인 전 텍스트 컬럼에 적용해 스캔했더니 **걸린 것의 다수가 정상값**이었다.

| 컬럼 | 규칙에 걸리는 값 | 실제 정체 |
| :-- | :-- | :-- |
| `domestic_bonds.crd_grd` | **`'AAA'` 8,722행** | 🔴 **최고 신용등급.** 반복문자 규칙 ④(`^(.)\1+$`)에 정확히 걸린다. 걸면 "신용등급 AAA인 채권" 이 통째로 죽는다 |
| `overseas_etfs` 티커 | `TTT`·`YYY`·`FFF`·`VV`·`XXX`·`ZZZ` | 🔴 **전건 실재 ETF** (ProShares UltraPro Short 20+ Year Treasury · Amplify CEF High Income · Founders 100 · Vanguard Large-Cap) |
| `domestic_bonds.pd_ctry_cd` | `'KR'` 21,881 | 국가코드 한국 |
| `domestic_bonds.pd_risk_gcd` | `'11'` 1,441 | 위험등급 11~16 체계 |

→ `public_funds.yaml` `dummy_as_missing.🔴_화이트리스트_필수` 주석에 **이 반례들을 박아 넣었다.** 이 주석은 `loader.planner_context()` 로 프롬프트에도 실린다.

> 📌 **교훈 — 규칙에 걸렸다는 것과 결측이라는 것은 다르다.** 더미 후보는 반드시 이름 컬럼(`pd_nm`·`itm_nm`)과 함께 조회해 실물을 확인한 뒤 판정한다. 이번에 해외ETF 티커를 "플레이스홀더"로 오판할 뻔했고, 검토표 `B-1-47` 의 자동 근거(`distinct 6,037` = 전건 유일)를 보고 되돌렸다.

### 1-3. 규칙 2종 신설

| 규칙 | 무엇 | 실측 |
| :-- | :-- | :-- |
| **`code_width`** (정규화 축 · **결측 아님**) | 고정 자릿수 기관코드의 선행 0 유실 → `zfill(8)` | `trusc '0016022'` **202행**(판매중 178) = **미래에셋증권** · `or_co '0040106'` 2행 = **DS증권** |
| **`malformed_as_missing`** (결측 축) | 12자 고정 식별자의 길이 이탈 | `std` 127 · `ksd` 67 · `rptt` 16 · `fss` 0 = **210행** |

🔴 **둘을 하나로 묶으면 안 된다.** "길이가 다르면 결측" 으로 처리했으면 미래에셋증권 수탁 202행이 "수탁사 없음" 이 되고, 설명 텍스트 컬럼 2,429행(`ovrs_fd_desc '국내외혼합'` 등)이 통째로 사라진다. `mtco_itm_no` 는 **가변 길이라 적용 금지**(짧은 값 7,962행).

### 1-4. KG 가짜 노드 19개 제거

| 무엇 | 내용 |
| :-- | :-- |
| 가짜 `Fund` 노드 **16종 / 28행** | `gen_fund_structure_auto.py` 가 센티넬을 **`.strip()` 전에** 판정했고 반복문자 규칙이 없었다. `'KR'`→`label='삼성KODEX'`(무관한 KODEX 2건) · `'0'`→무관한 사모 4건 병합 · `'wtrewrwe'`→맵스/TIGER+한화아리랑 6건이 한 노드로 |
| 센티넬 `Organization` **3종** | `Org_fund_99999999`(**`label_ko='현대와이즈03사'`** — 코드북이 스스로 "법인명 아님" 이라 적은 값) · `Org_trustee_99999999`·`Org_trustee_00000000` |

`Fund` 노드 6,883 → **6,867**(1차 0계열·반복문자 8종 · 2차 길이 이탈 8종). YAML 규칙으로 구한 기대집합과 **가짜 0 · 누락 0** 으로 일치 검산했다.

> 🔴 **생성기를 두 번 고쳤다.** 1차 수정(`strip` 후 `(KR)?0+ | (.)\1+`)은 **`'KR'` 을 못 잡았다** — 0 이 하나도 없어 패턴에 불일치한다. 노드가 사라진 게 아니라 **ID 만 바뀌어 살아 있었다.** 그래서 정규식을 버리고 **YAML `dummy_as_missing` 규칙 ③④를 그대로 옮겼다.**
> **교훈: 규칙을 다시 짜지 말고 원문을 이식한다.**

### 1-5. 🔴 교차질의 조인키 (A-3-03) — 처방을 뒤집었다

`결정대기_2026-08-29` §1 과 초안은 `grp` → `itm_no` **교체**를 처방했다. 실측하니 **틀린 처방**이었다.

`grp` 조인이 만드는 (클래스, holding) 쌍 179,333 을 holdings 원소유 클래스의 운용사와 대조하면:

| 구성 | 쌍 | 성격 |
| :-- | --: | :-- |
| 같은 운용사 | **174,234 (97.2%)** | ✅ **정당한 형제 클래스 확장** — holdings 는 클래스 하나에만 수집돼 있고, 같은 펀드의 A/C/Ce 클래스로 퍼뜨리는 것이 `grp` 조인의 **본래 목적**이었다 |
| 다른 운용사 | 5,099 (2.84%) | ❌ 오부착 |

`itm_no` 단독은 쌍이 **59,206** 으로 줄어든다 — 2.84% 를 고치려고 정당한 115,028 을 버린다("삼성전자 보유 펀드" 3,056 → 941건).

**채택: `(grp, or_co)` 복합키.** `ext_fund_holdings` 에 수집원 클래스의 운용사 코드를 컬럼으로 굳혔다.

| | 쌍 | 오부착 | 삼성전자 보유 펀드 |
| :-- | --: | --: | --: |
| `grp` 단독 (기존) | 179,333 | 5,099 | 3,056 |
| `itm_no` 단독 (초안 처방) | 59,206 | 0 | 941 |
| **`(grp, or_co)` (채택)** | **174,234** | **0** | **2,854** |

반영 4곳 — `load_external_web.py`(컬럼 생성·backfill, 재현성) · 로컬 DB backfill · `pipeline.py` `JOIN_KEYS` · `CROSS-001.gold_sql` + `gold_sql_fill.py`. **CROSS-001 결과 20행 불변.**

> 📌 **교훈 — "키가 유일하지 않다" 는 사실만으로 조인을 바꾸면 안 된다.** 그 조인이 **무엇을 하고 있었는지**를 먼저 세어야 한다. 오류율(2.84%)과 커버리지 손실(64%)을 나란히 놓고서야 옳은 처방이 보였다.
> 🔴 **`gold_verified: false` 는 이걸로 안 풀린다.** 미해소 이상치 `마이다스아시아리더스성장주(H) Ce클래스 fd_yr1_ern_r=1436.24%` 는 조인 산물이 아니라 마스터의 실제 값이다 — **펀드 도메인 판정이 여전히 필요**(HANDOFF 8/27 §2-2 유지).

### 1-6. yaml 기술 정정 2건

| 무엇 | 정정 |
| :-- | :-- |
| `invalid_values` | "1차 이탈값 5종 전부 소멸" → 3종만 소멸. 🔴 **`'00080008'` 4,536행은 이탈값이 아니라 미래에셋자산운용**(코드북 `confirmed`). 1차가 최대 운용사 코드를 오염값으로 잘못 올린 것 — **배제하면 미래에셋 펀드가 통째로 사라진다** |
| `constant_columns` | "`ofsfd_yn` 공모 한정 시 사실상 상수" → **공모에도 110행이 `1`.** "공모에는 역외펀드가 없다" 는 취지의 답변 금지. (`hdge_fd_yn` 은 공모 전건 0 으로 기술이 맞다) |

### 1-7. 수치 정정 — 여섯 컬럼 전부 같은 원인

`dummy_as_missing.실측_2026-08-25` 에 **1차 모수가 통째로 남아 있었다.** 원인은 전부 **규칙 ④(반복문자) 미적용**이다.

| 컬럼 | 구 | 실측 |
| :-- | --: | --: |
| `rptt_ksd_itm_no` | 1,761 | **7,086** (29.9%) |
| `std_itm_no` | 145 | **4,668** (19.7%) |
| `ksd_itm_no` | 592 | **2,370** (10.0%) |
| `kofia_fd_ccd` | 11,431 | 11,476 |
| `fss_itm_no` | 11,611 | 11,655 |
| `mtco_itm_no` | 869 | 1,057 |

`missing_profile` 4줄도 맞췄다. 🔴 `ksd_itm_no` 는 **한 파일 안에서 `answer_policy` 10.0% vs `missing_profile` 2.5%** 로 어긋나 있었다 — 같은 수치가 두 블록에 중복 기재되는 구조라 한쪽만 고치면 이렇게 된다.

### 1-8. 브랜치 병합 — `eda/domestic_etfs` 19커밋

ETF 담당이 main 을 먼저 당겨 놨으나 그 뒤 `41e567d` 가 올라가 검토표 2건이 **다시** 충돌했다. **판정 겹침이 아니라 표에서 줄이 붙어서**였다.

- 행 ID 단위로 병합 — A: 로컬 3 / ETF 4, B: 로컬 3 / ETF 52
- **양쪽 다 채워진 행 3건**(`B-1-45/47/48` 해외ETF) — 판정은 양쪽 ✅ 일치, 문구만 달라 **ETF 담당 문구를 본문**으로 두고 반례를 덧붙였다
- **보존 검사 전수 실행** — 파일 유실 0 · 채움 칸 유실 0(브랜치 191칸 · 로컬 37칸) · yaml 양쪽 원본과 완전 동일
- 푸시는 **fast-forward**(`79c020a..9157724`), `--force` 미사용

### 1-9. 🆕 (리드 세션) 트레이스 관찰성 — 잘린 SQL 로는 검토가 성립하지 않았다

팀에 *"챗봇의 thinking 을 보면서 KG·온톨로지를 의도대로 쓰는지 검토하라"* 고 배정한 뒤, **그 검토에 필요한 증거가 트레이스에 없다**는 것이 드러났다.

| 무엇이 문제였나 | 실체 |
| :-- | :-- |
| `think_trace` 의 SQL | `sql[:120]` 로 **잘려** 있었고, 그마저 **`LIMIT` 보정 전 원문**이라 실제 실행문과 달랐다 |
| 로그(`chat-*.jsonl`·`api-*.jsonl`) | 5필드를 그대로 복사 → **같은 잘림을 상속**. `sql` 필드 자체가 없었다 |
| 근거문서 | `5,495자` 처럼 **길이만** 기록 → 고친 yaml 이 실제로 프롬프트에 실렸는지 사후 확인 불가 |

→ `[Ground]` 매핑이 틀렸는지 `query_rules` 조건식이 틀렸는지 **가를 수단이 없었다.** 고친 것(커밋 `96602fa`, TDD — 테스트 10건을 먼저 실패시키고 구현):

- `PipelineResult` 에 `sql`·`grounding` 신설. `think_trace` 에 **보정 후 실행 SQL 전문**
- `[Plan] 근거문서 조립` 줄에 **구성 블록 표시** — `KG 개체 매핑 + 도메인 규칙 + 스키마`
- `src/runtime/qa_log.py` 신설 — 서버·CLI 공용 레코드. `sql` 상시 · `grounding` 은 `LOG_GROUNDING` 토글(평가 기간엔 0)
- **`GET /chat/ask`** 신설 — 5필드 + `sql` + `grounding`. `CHAT_TOKEN` 불일치 시 404(존재 은닉)
- `/chat` 화면에 **실행 SQL**(기본 펼침) · **근거문서 원문**(접힘) 패널
- 🔴 **`/answer` 응답은 5필드 고정 유지** — 검토용 필드가 채점 스키마로 새지 않도록 회귀 테스트로 잠갔다
- `TEAM_WORKFLOW.md` §2 에 "이걸로 무엇을 판정하나" + 자주 나오는 판정 2종

> 📌 **바로 검토거리가 하나 나왔다.** `[Ground]` 가 미래에셋 별칭 43종을 넘겼는데 플래너는 `IN ('미래에셋')` 하나만 썼다(배포 후 재실측: `IN ('미래에셋','TIGER')` 2종). **KG 결함이 아니라 프롬프트 지시가 덜 먹은 것** — 이 구분이 가능해진 것 자체가 이번 변경의 목적이다.

### 1-10. 🆕 (리드 세션) 배포 — 서버를 8/27 → 8/30 로 올렸다

**두 번 나눠서** 했다. 경로가 다르기 때문이다(→ 새로 쓴 `DEPLOY.md` §0-B·§0-C).

| 회차 | 모드 | 무엇이 갔나 | DB |
| :-- | :-- | :-- | :-- |
| 1차 | `--code-only` | 트레이스 관찰성 코드 (`f0a3a7a`) | **무변경** (md5 `91c2f12a…` 배포 전후 동일) |
| 2차 | 전체 | 24커밋 = 펀드 식별자·조인키·ETF 174건·KG 재생성 (`9157724`) | **교체** 275,701,760 → **278,503,424** · md5 `3437db7f…` 로컬 일치 |

배포 전 확인한 것 — ① 워킹트리 깨끗·`main = origin/main` ② `build_ontology.py` 재실행이 **멱등**(카운트 동일·ttl 무변경)임을 확인해 *DB 가 커밋된 yaml 과 일치*함을 보장 ③ `pytest` 49·gold 63/63 ④ **덮기 전 서버 DB 백업** `data/financial_products.v2-20260826.bak`(275MB, 디스크 41GB 여유).

서버 실검증 (UTF-8 직접 호출):

```
"신용등급 AAAA인 채권"       → [Gate] 기각 — enum 20종에 없음        ✅
"KODEX AI로봇 ETF 정보"      → 통과 후 0행 → "확인되지 않습니다"      ✅
"삼성전자를 보유한 국내 ETF"  → ext_etf_holdings JOIN, 30행           ✅
/chat/ask  무토큰 404 · 오토큰 404 · 정상토큰 200(sql·grounding 포함)  ✅
/answer    응답 필드 5개 그대로                                        ✅
kg_node 39,658 · kg_alias 62,558 (로컬과 일치)                        ✅
```

### 1-11. 🆕 (리드 세션) 배포 경로 문서화 + 잔여 계획서

- **`DEPLOY.md` §0-B 신설** — *"배포는 내 파일을 서버로 보내는 작업이 아니다"*. 코드·yaml·문서는 **로컬 → GitHub → 서버 `git pull --ff-only`**, 로컬 직송은 **DB(`scp`) 하나뿐**, `.env` 는 전송되지 않음(서버 전용), 이미지는 서버에서 빌드. → **push 하지 않은 수정은 어떤 배포 모드로도 반영되지 않는다.**
- **`DEPLOY.md` §0-C 신설** — `enums`(파일 그대로 읽음) vs `shared`(`kg_*` 로 구워짐) 갈래표. 🔴 *`shared/` 를 고치고 `--yaml-only` 를 돌리면 아무 일도 일어나지 않는다* — 증상("yaml 고쳤는데 답이 그대로")과 확인법(근거문서 패널)까지.
- `TEAM_WORKFLOW.md` §6 · `EXPERIMENT_LOOP.md` §3 에서 위 절로 연결. `DEPLOY.md` §6 의 *"지금은 서버에 DB가 없습니다"* 낡은 서술 정정.
- **`docs/PLAN_2026-08-29.md` 신설** — 잔여 8일을 **배점 축**(제안서 40 / 답변 40 / 소스코드 20)으로 재정렬한 계획서. 기존 `WORK_PLAN_2026-08-26.md` 가 *데이터 품질 트랙*이라면 이쪽은 *제출·채점 트랙*이고, 충돌 시 이쪽이 우선한다.

---

## 2. 앞으로 할 일

### 2-1. 🔴 결정이 필요한 것

**① 확답 ② — `security_alias_manual.csv` 103종 (ETF 담당 대기 중, 급함)**

`docs/ask_lead_2026-08-28.md` §2. 실측으로 문제는 확인됐고 **보고보다 크다** — NVIDIA 가 Security 노드 **6개**로 갈려 있고 노드별 27~356개, **합집합 701개**다.

🔴 **그런데 CSV 를 채워도 답변은 안 바뀐다.** 검증 결과:

- `security_alias_manual.csv` 의 `nvidia` 행은 **이미 있고**, `kg_closure` 에 계층도 **실제로 만들어져 있다**
  (`Sec_m_nvidia` → `Sec_lei_549300S4KLF…` · `Sec_o_67066G104` · `Sec_o_67066GAF1`).
- ~~**`src/runtime/loader.py`·`pipeline.py` 어디에도 `kg_closure` 를 읽는 코드가 없다.**~~ → ✅ **8/30 오후 ㉡ 구현 완료** (아래).
- 게다가 CSV 가 붙일 수 있는 키는 `kr_ticker`·`isin`·`cusip`·`lei` 넷인데, 국내ETF 구성종목 노드는 `Sec_d_{sha(ticker)}` 라 **어디에도 안 걸린다**(NVIDIA 의 155개짜리가 그것).

| | 무엇 | 담당 |
| :-: | :-- | :-- |
| ㉠ | CSV 103종 채우기 | ETF 담당 (제안대로) — **이제 채우면 답변에 바로 반영된다** |
| ㉡ | ~~`pipeline.target_aliases` 가 `kg_closure` 후손까지 확장~~ ✅ **완료** — `loader.py` 가 `kg_closure`·`subsidiaryOf` 를 읽고, `expand_node()` 가 정본 → 후손(+ "자회사" 질의면 자회사와 그 후손)을 편다. `tests/test_closure.py` 7건(채권 `투자등급`→AAA~BBB- 10종 · 펀드 `캠브리콘`→`Sec_f_CNE…` · ETF `엔비디아` 3분신 합집합 · 공식 예시 #5 자회사) | 런타임 |
| ㉢ | `Sec_d_*` 를 붙일 키 추가(CSV `dom_ticker` 열 등) | 생성기 — "생성기 안 건드림" 이 깨지는 지점. **"엔비디아 편입 국내 ETF" 는 ㉡ 후에도 국내 155개를 못 잡는다** — ㉢ 이 남은 이유 |

> 🔴 **㉡ 구현 중 정정** — ETF 담당 문서(`PENDING_DECISIONS_ETF` B-2)의 *"㉡ 만 해도 에코프로 건은 바로 해결"* 은 **반만 맞다.** `Sec_m_ecopro` 의 closure 후손은 본체 주식(`Sec_kr_086520`) 하나뿐이고 자회사 3개는 **`subsidiaryOf` 엣지**로만 이어져 있다. closure 만 폈으면 "에코프로 자회사 편입 ETF" 는 여전히 본체만 잡았다 → 관계 1단 탐색을 함께 넣었다(질의에 `자회사|계열사|계열회사|종속회사` 가 있을 때만 — "에코프로 편입 ETF" 는 본체만).
>
> 같이 들어간 **E**(채권 담당 전수조사 §2-E): 대상 테이블에 alias 없는 노드는 **버린다**(fallback 제거). "한국전력 채권"·"삼성전자 채권" 에 `ext_etf_holdings` 값이 더 이상 실리지 않는다. 교차질의면 `ext_*` 도 대상에 넣어 종목 노드가 살아남게 했다.
>
> ⚠️ **부수 효과 — 지역 계층도 펴진다.** "아시아에 투자하는 국내 ETF" 가 `wu_inv_rgn IN ('아시아','국내','중국','일본','인도','베트남')` 으로 넓어진다(`Region_Asia` ⊃ `Region_Korea`…). 온톨로지 계층이 그렇게 선언돼 있어서다 — 좁히려면 계층을 고치지 코드를 고치지 않는다.

**② 규칙 후속 (`공모펀드_검토기록_2026-08-30.md` §9-4)**

| # | 사안 |
| :-: | :-- |
| 1 | 12자를 **채운** 쓰레기값(`'KRdfdfsdfsdf'`·`'kR5205A22251'`) 형식 정규식까지 갈지 — 소수, 비용 대비 판단 |
| 2 | `mtco_itm_no` 짧은 값 **7,962행** — 가변 길이라 정상일 수 있다. 별도 판정 |
| 3 | 운용사 코드북 파생 라벨 **157/275**(점유<50% 가 36건, 판매중 종목 포함) — `PLAN_2026-08-29` P2-3 |

**③ ~~서버 배포~~** — ✅ **완료 (8/30 01:51).** `--db-only` 가 아니라 **전체 배포**로 갔다 — `pipeline.py`(조인키) 코드 변경이 함께 있어 이미지 재빌드가 필요했기 때문이다. 상세는 §1-10.
→ 남은 것은 **배포 이후 규칙 변경분의 반영 판단**뿐이다: `enums/*.yaml` 만 고쳤으면 `--yaml-only`(5초), `shared/*.yaml` 을 고쳤으면 `build_ontology.py` → `--db-only`(1분). 갈래는 `DEPLOY.md` §0-C.

**⑤ 🆕 `deploy.sh` 스모크 검증 수정 여부** — 검증이 fail-open 이다(§2-3 표 마지막 줄). 수정안은 3줄: 질의를 stdin/파일로 넘겨 인코딩을 고정하고, 응답의 `question` 에코가 보낸 값과 **바이트 동일한지 먼저 단언**한 뒤(다르면 "환경 문제"로 중단) 그때만 기각 여부를 판정한다. 프리즈 전에 고쳐 두는 편이 안전하다 — 9/6 이후에는 이 스크립트가 마지막 안전망이다.

**④ ETF 담당 확답 ①③④** — 회의 안건. 다만 ③(NVIDIA 회사채 `Sec_o_67066GAF1`)은 **㉡ 을 도입하면 closure 에 이미 매달려 있어 자동으로 합쳐진다.** "이을지" 가 아니라 "자동으로 이어지는데 그래도 되는지" 가 실제 질문이다 — 서현이 판단 전에 이 사실을 알려야 한다.

### 2-2. 검수·판정 대기

- **`CROSS-001` `gold_verified=false`** — 조인키로 해소되지 않았다(§1-5). `fd_yr1_ern_r=1436.24%` **펀드 도메인 판정 필요**
- 검토표 **200칸 남음** — D 38칸은 손도 안 댔고, E 는 100칸 남았다
- 채권 **B3·B5·B6 수치 2차 재산출** (8/27 인수인계에서 이월)

### 2-3. 🔴 알려진 함정 (이번 세션에서 실제로 밟은 것)

| 함정 | 대응 |
| :-- | :-- |
| **동시 작업자** | 이 세션 중 팀원 커밋이 **두 번** 유입됐고, 한 번은 **다른 세션이 내 미커밋 편집을 자기 커밋에 쓸어 담았다**(`fed6a87`). 작업 전 `git pull`, 커밋 전 `git status` 확인 |
| **NFD 파일명** | `1.금융상품.zip` 등은 파일시스템에 NFD 로 존재. `git restore --staged "1.금융상품.zip"` 이 **pathspec 불일치로 실패**한다. `-z` + `--pathspec-file-nul` 로 원시 바이트를 넘길 것 |
| `git add -A` | 미추적 zip·PDF·`docs/핵심문서모음/` 까지 담는다. 병합 커밋 전 `--diff-filter=A` 로 신규 파일 목록을 반드시 확인 |
| 규칙 재작성 | 정규식을 새로 짜면 원본과 어긋난다(`(KR)?0+` 가 `'KR'` 을 놓쳤다). **YAML 원문을 이식**할 것 |
| 한글 리터럴 | `sale_yn='판매중'` 은 파라미터 바인딩으로. `PYTHONIOENCODING=utf-8` 병행 |
| 검토표 재생성 | `gen_review_workbook.py` 재실행 금지 — 채운 208칸이 날아간다 |
| 🔴 **`deploy.sh` 스모크가 fail-open** | 마지막 확인이 한글 질의를 `curl` 로 보내는데 **Windows 콘솔 코드페이지에서 깨져 나간다.** 배포 2회 모두 `"신용등급 AAAA인 채권"` 이 뭉개져 **`AAA` 조회 30행**을 반환했다 — 로그만 보면 환각 방지가 깨진 것처럼 보인다. 실제로는 정상(UTF-8 재호출로 확인). 반대로 **깨진 문자열이 우연히 기각 조건에 걸리면 게이트가 망가져도 "정상"으로 보고**된다. → 배포 후 판정은 반드시 UTF-8 클라이언트로 재확인할 것. **미수정 (§2-1 ⑤)** |

---

## 3. 운영 정보

| 항목 | 값 |
| :-- | :-- |
| 엔드포인트 | `https://49.50.134.229.nip.io` — `{"status":"ok","agent_ready":true,"planner":"hcx"}` (2026-08-30 확인) |
| ✅ 서버 버전 | **`9157724` · DB `3437db7f…`** — 이번 세션 변경(조인키·KG·yaml) **반영 완료** (8/30 01:51) |
| 실험 UI | `/chat?t=<CHAT_TOKEN>` — **실행 SQL·근거문서 패널 추가됨** (§1-9). JSON 은 `/chat/ask?question=…&t=…` |
| 로컬 DB | `data/financial_products.db` **278,503,424바이트** · `kg_node` 39,658 · `ext_fund_holdings.or_co` 59,206행 채움 |
| 서버 DB 백업 | `data/financial_products.v2-20260826.bak` 275,701,760바이트 (8/30 배포 직전 스냅샷) · v1 백업도 유지 |
| git | `main = origin/main = 9157724` · 미커밋 0 |
| 미추적(의도적) | `1.금융상품.zip` · PDF 2 · `docs/핵심문서모음/` — 리드 로컬 파일 |

---

## 4. 착수 순서 (다음 세션)

1. `git pull` — **팀원 3명이 동시에 커밋 중이다.** 먼저 당기고 시작할 것
2. `pytest tests -q`(49) · `python eval/run_gold_check.py`(63/63) 기준선 확인
3. `docs/eda/공모펀드_검토기록_2026-08-30.md` **§9-4 결정 목록** 읽기 — 이 세션이 남긴 판단 대기 목록이다
4. `docs/ask_lead_2026-08-28.md` §2 에 대한 리드 답을 ETF 담당에게 전달 (㉠㉡㉢ 중 어디까지인지)
5. ~~배포 여부 결정~~ → ✅ 완료. 이제는 **`/chat` 에서 실행 SQL·근거문서를 보며 오답을 수집**하는 단계다 (`TEAM_WORKFLOW.md` §2)
6. `docs/PLAN_2026-08-29.md` 로 잔여 일정 확인 — **제안서 40점이 20% 상태**이고 채권·펀드 eval 문항이 0건이다

---

## 5. 이 세션이 틀렸다가 고친 것

기록으로 남긴다. 셋 다 **"실측 없이 규칙만 보고 판단"** 이 원인이었다.

| 무엇 | 어떻게 드러났나 | 교훈 |
| :-- | :-- | :-- |
| 해외ETF `TTT`·`YYY`·`FFF`·`VV` 를 **위장결측으로 오판**할 뻔 | 검토표 `B-1-47` 자동 근거의 `distinct 6,037`(전건 유일)을 보고 재조회 → 전부 실재 ETF | 더미 후보는 **이름 컬럼과 함께** 조회해 실물을 확인한다 |
| 조인키를 `itm_no` 로 **교체하라고 처방** | 교체 시 "삼성전자 보유 펀드" 가 3,056 → 941 로 떨어져 원인을 파 보니 형제 클래스 확장 174,234 쌍을 버리고 있었다 | 조인을 바꾸기 전에 **그 조인이 무엇을 하고 있었는지** 센다 |
| 센티넬 정규식 1차 수정이 **`'KR'` 을 놓침** | 노드가 사라진 줄 알았는데 **ID 만 바뀌어 살아 있었다**(`label='삼성KODEX'`) | 규칙을 다시 짜지 말고 **원문을 이식**한다 |

> 그리고 하나 더 — "펀드 식별자 다 처리했나" 라는 질문을 받고 다시 훑어서 **가짜 노드 8종/16행과 길이 이탈 210행을 추가로 찾았다.** 완료 선언 전에 같은 축을 한 번 더 훑는 것이 값을 했다.
