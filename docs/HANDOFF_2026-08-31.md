# 🤝 세션 인수인계 — 2026-08-31 (KG 후손 탐색 · 펀드 규칙 정비 · 3도메인 삼각 대조)

> 작업 구간: **8/30 오후 ~ 8/31 오전**. 앞 세션은 `docs/HANDOFF_2026-08-30.md`(리드 · 8/30 새벽까지).
> 한 줄 요약: **정본 노드가 답변에 값을 못 주던 결함(㉡)을 고쳤고, 펀드 규칙을 "컬럼 대신 규칙"으로 확정했으며, 3도메인 yaml 수치 주장을 DB·KG로 전수 재현해 불일치 0으로 맞췄다.**
> 🔴 **서버는 아직 `9157724`(8/30 01:51 배포본)다.** 그 뒤 **21커밋**(채권 14 · 리드 7)이 쌓였고 `src/`·4도메인 yaml 이 전부 바뀌었다 — §2-1 ①.

| 확인 | 상태 |
| :-- | :-- |
| 브랜치 | ✅ `main = origin/main = e7b6e1e` · 미커밋 0 · 미추적 `docs/핵심문서모음/`(리드 로컬) |
| 테스트 | ✅ `pytest` **150 passed** (49 → 150: 채권 라우터·거절·값검사기 + 펀드 규칙 7 + closure 7) |
| gold | ✅ `run_gold_check` **87문항 · 통과 87** (63 → 87: 채권 24문항 신규) |
| KG | `kg_node` 39,658 · `kg_alias` 62,558 · `kg_edge` 7,414 · `kg_closure` 9,919 — **배포본과 동일** |
| 삼각 대조 | ✅ **펀드 58/58 · ETF 60/60 · 채권 127/127** — yaml 수치 주장 ↔ DB ↔ KG 불일치 0 (§1-5) |
| 검토표 | 🟡 **227/408** (208 → 227: 펀드 A 9 · 이름계열 10) |
| 엔드포인트 | ✅ `agent_ready:true` — 단 코드는 **8/30 01:51 시점** |
| 배포 | ❌ **미반영 21커밋.** 이 세션에서 `deploy.sh` 실행이 권한 분류기에 차단됨 → 리드가 직접 (§2-1 ①) |
| ETF 브랜치 | 🟡 `origin/eda/domestic_etfs` **9커밋 미병합** (`enums/*_etfs.yaml` + `PENDING_DECISIONS_ETF.md`) |

---

## 1. 한 일

### 1-1. 🔴 KG 계층 후손 탐색(㉡) + 대상 밖 alias fallback 제거(E) — 세 도메인 검증 (`004c919`)

**결함** — 정본 노드(`Sec_m_*` 18개·`CG_*`·`Idx_a_*`)는 alias 가 0개고 실물 노드가 `kg_closure` 로 매달려 있는데, `loader.py`·`pipeline.py` 어디에도 closure 를 읽는 코드가 없었다. 정본에 매칭될수록 SQL 에 넣을 값이 비었다.

| 도메인 | 질의 | 전 | 후 |
| :-- | :-- | :-- | :-- |
| 채권 | `투자등급 채권` | `CG_Investment` 매칭, **값 없음** | `crd_grd IN (AAA…BBB-)` 10종 |
| 채권 (E) | `한국전력 채권` · `삼성전자 채권` | 주식 노드의 `ext_etf_holdings` 값이 근거에 실림 | 매핑 없음 → 채권 규칙 `발행사조회`(LIKE) |
| 펀드 | `캠브리콘 편입 공모펀드` | 값 없음 | `ext_fund_holdings.isin='CNE1000041R8'` + 표기 2종 |
| ETF | `엔비디아 편입 해외 ETF` | 표기 1종 | 3분신(주식·회사채·LEI) 합집합 |
| ETF | `에코프로의 자회사 편입 ETF` (공식 예시 #5) | 🔴 **채권 발행사 `(주)에코프로`** 로 오매칭 | 자회사 3 + 본체, 티커 4·이름 3 |

구현 — `loader` 가 `kg_closure`·`subsidiaryOf` 적재 → `pipeline.expand_node()` 가 노드 + closure 후손(+ 질의에 `자회사|계열사|계열회사|종속회사` 있을 때 자회사와 그 후손) → `target_aliases()` 합집합 · **fallback 없음** · 교차질의면 `ext_*` 도 대상 · 동점이면 값 많은 노드(정본) 우선 · 트레이스 `[+후손 N: id…]`. `tests/test_closure.py` 7건(RED → GREEN).

> 🔴 **ETF 담당 진단 정정** — *"㉡ 만 하면 에코프로 해결"* 은 반만 맞았다. `Sec_m_ecopro` 의 closure 후손은 본체 주식 하나뿐이고 자회사는 **`subsidiaryOf` 엣지**로만 이어져 있다. 관계 1단 탐색을 같이 넣지 않았으면 테스트는 통과해도 그 예시는 본체만 잡았다.
> ⚠️ **부수 효과** — 지역 계층도 펴진다: "아시아 투자 국내 ETF" → `wu_inv_rgn IN ('아시아','국내','중국','일본','인도','베트남')`. 온톨로지가 `Region_Asia ⊃ Region_Korea` 로 선언돼 있어서다. 좁히려면 계층을 고치지 코드를 고치지 않는다.
> 📌 **㉢ 는 남았다** — "엔비디아 편입 **국내** ETF" 는 국내 노드 `Sec_d_…`(155개)가 정본에 안 매달려 있어 못 잡는다. ETF 담당의 `dom_ticker` 11종이 그 자리.

### 1-2. 🔴 결정 — 이름 파생 컬럼(`nm_*`) 철회, 규칙으로 회귀

같은 날 오전 다른 세션이 *yaml `name_features` → 파서 → `public_funds.nm_*` 27컬럼* 으로 갔고(DB 에까지 들어가 있었다), 저녁에 리드가 되돌렸다. 근거는 실측 하나 — 컬럼의 유일한 이유였던 **클래스 도배**(한화2.2배레버리지 6클래스가 TOP5 3칸)가 **원본 구조 컬럼 `(or_co_xtn_itt_cd, mtco_itm_no)` GROUP BY 로 풀린다**(6클래스 = grp `0166750` 하나 · 판매중·공모 `mtco` NULL 0행).

- 제거: 미추적 3파일(스크래치패드 백업) · yaml 블록 221줄 · `nm_*` 참조 규칙 5개 · **로컬 DB 컬럼 27개 DROP + `schema_metadata` 27행** → 75컬럼·280행 = 배포본 형태 · 파생 전제로 바뀐 추적 파일 5개 checkout
- 대체: `query_rules.대표행` · 호수 검색 `(itm_nm LIKE '%제2호%' OR itm_nm GLOB '*[^0-9]2호*')`
- **판단 사다리** — ① 질문 모호 → `clarify` ② HCX 가 다르게 계산 → "설명" 을 **복사할 조건식**으로 ③ KG 로 정확값 주입 ④ 그래도 불안정 + **오답 실측** + **행을 묶어야** 할 때만 컬럼. 지금 ④ 는 없다.

상세: `docs/eda/공모펀드_검토기록_2026-08-30.md` **§12-1**. 메모리 `name-conventions-become-derived-columns.md` 도 뒤집힌 결정으로 재작성.

### 1-3. 펀드 수치 축 규칙 — 컬럼 정책이 답변 단계에 안 가던 구멍

75컬럼 전수(`missing_reason` 72/75 · 정책 40/75)에서 문제는 `columns.*.answer_policy` 가 **`compose_answer` 에 전달되지 않는 것**(채권 §3-H 와 같은 구멍, 펀드 `answer_rules` 0줄).

| 추가 | 근거 실측 |
| :-- | :-- |
| `query_rules.수익률극단값` — 빼지 않고 알린다(채권 8/29 결정의 공통 패턴) | 1년수익률 >100% **889행**(10%) · 최대 1,436% · 3년 >300% 20행 |
| `answer_rules` **12줄** — 누적 수익률 · 주의문구 · 제로인 위험등급 **작을수록 위험** · 보수 **‰** · 클래스 n개 · 분배율 유무만 · 기준가 비교 기각 · 1주수익률 미수록 · 모수·기준일 · 역외 110행 | 컬럼 정책 원문 이관 |
| `clarify` — 다의어 4 · 사람의_선택 3(**규모_MMF포함** 등) | 순자산 상위 = 법인 MMF 138행 |

넣지 않은 것 — 기준일 낡음(751행은 1년수익률 전부 NULL, 이미 배제) · 보수 동시 0(기존 규칙 29행) · **교차 척도**(위험등급 방향·보수 단위·통화 — 출처가 다른 데이터의 속성이라 교차 층 소관, 리드 지시로 보류).

### 1-4. 게이트 — `absent(CreditGrade, public_funds)` 가 한 번도 발동한 적이 없었다 (검토표 D-4-04)

`gate._ENTITY_HINTS` 에 "신용등급 → CreditGrade" 항목이 없어 "신용등급 AAA인 공모펀드" 가 통과했다(답변불가 5문항 감점 구간). 힌트 1줄 + 테스트 2(펀드 기각·채권 비기각). 잔여: "신용등급 높은 **ETF**" 는 라우터가 국내+해외 2테이블로 잡아 absent 검사(1테이블 조건)를 비켜간다.

### 1-5. DB ↔ yaml ↔ KG 삼각 대조 — 3도메인 불일치 0

| 도메인 | 검사 | 도구 | 초회 불일치 → 정정 |
| :-- | --: | :-- | :-- |
| 공모펀드 | 58 | `scripts/audit_funds_claims.py` 🆕 | 6 → yaml 3(패딩 리터럴 폭 12자 · Fund 합성키 **14,467/14,409** · 규칙 컬럼명) + 검사식 3 |
| ETF 국내·해외 | 60 | `scripts/audit_etf_claims.py` 🆕 | 4 → 검사식 4 (yaml 오류 0) |
| 채권 | 127 (+KG 4) | 담당자 `audit_bonds_claims.py` | 0 |

검사 축: 모수 · `normalization` 수치 재현 · `answerable_n`·`value_semantics` 실재 · `query_rules` 조건식 실행 + 참조 컬럼 실존 · `entities` count · KG alias 사어·커버리지·가짜 노드·센티넬 노드·모펀드·지수 교차. 정상으로 확인된 "미부착" — 펀드 `trusc '00000000'`(센티넬 선언) · 국내ETF `'.'`·`'Index is not available on Lipper Database'` · 해외 CUSIP `'000000000'` 더미 5,741행.

### 1-6. 검토표 A 9칸 + yaml 정정

A-1-18/19/20/23/24 ❌ — 기간별 수익률 `answerable_n` 이 `fd_yr1_ern_r` 의 6,913 을 `applies_to` 로 물려받은 것. 컬럼별 실측을 `fd_yr1_ern_r.answerable_n_by_column` 에 표로(두 생성기 모두 명시 항목이 있으면 상속을 끊으므로 컬럼별 항목은 안 만듦). `fd_yr3_ern_r` 판정 보강(C-2-03) · `fd_nast_suma` 8,340. `thco_sale_yn` 은 `none` 유지가 맞음(NULL = 'N' 의 자리).

### 1-7. 팀원 요청 정리 — 이 세션 시작 시점의 인벤토리 (§2 참조)

채권 담당 문서(전수조사 §2·§3·§9)와 ETF 담당 문서(`PENDING_DECISIONS_ETF`·`ask_lead`·`HANDOFF_ETF`)의 리드 요청을 담당별로 정리했다. **채권 B·E·F·G·H 는 그 뒤 채권 담당이 직접 구현해 올렸다**(`cdf09f6` — `router.py`·되묻기·`answer_rules`·연도 사후검사). 그래서 리드 몫으로 남은 코드 요청은 사실상 ㉡(완료)뿐이고, 나머지는 **결정**이다.

---

## 2. 앞으로 할 일

### 2-1. 🔴 결정·실행이 필요한 것

**① 배포 — 21커밋 미반영, 전체 재빌드 필요**

`src/`(라우터·게이트·거절 3단·closure 후손 탐색·값 검사기) + 4도메인 yaml + 채권 24문항이 전부 서버에 없다. `--yaml-only` 로는 안 되고 **`deploy.sh --code-only`**(이미지 재빌드). 이 세션에서는 명령이 권한 분류기에 막혔다 — 리드가 직접:

```
! bash deploy/deploy.sh --code-only
```

배포 후 판정은 **UTF-8 클라이언트로 재확인**(스모크는 fail-open, 8/30 §2-3). 확인 질의: "에코프로의 자회사를 편입한 ETF" 트레이스에 `[+후손 7: …]` · "신용등급 AAA인 공모펀드" → `[Gate] 기각` · "투자등급 채권" → `crd_grd IN (…)`.

🔴 **`--db-only` 는 지금 보내지 말 것.** 로컬 DB 는 `nm_*` 추가/삭제 흔적으로 **283,484,160바이트**(배포본 278,503,424)로 커졌다 — 내용은 동일(75컬럼·280행·kg 4테이블 동일)하나 md5 가 다르다. 보낼 일이 생기면(§9-4 KG 재생성·region pending) `VACUUM` 후 **한 번에**.

**② ETF 담당 확답 4건 — 회의에서 안 다뤄져 8/31 재상정 예정**

| # | 물음 | 답 초안 |
| :-: | :-- | :-- |
| ② ㉠ CSV 103종 | ㉡ 이 됐으니 **채우면 답변에 바로 반영** — 승인 | |
| ㉢ `dom_ticker` 11종 | 승인 시 "엔비디아 국내 ETF" 155개 살아남 — 승인 권고 (`isin` 컬럼 쓰지 말 것 함정은 담당이 이미 적음) | |
| ① ISIN 단독조인 금지 위치 | `overseas_etfs.yaml` `external_join` 에 이미 있음 + `query_rules` 한 줄 | |
| ④ `TOP_RANK=50` | 공식 예시 #3·#5 가 rank≤50 안에서 답이 나왔으므로 **유지** | |

**③ `origin/eda/domestic_etfs` 9커밋 병합** — `enums/domestic_etfs.yaml`(+106) · `overseas_etfs.yaml`(+35) · `PENDING_DECISIONS_ETF.md`. `enums/` 만이라 충돌 없음. 병합 후 `audit_etf_claims.py` 재실행.

**④ 펀드 결정 5건** (`공모펀드_검토기록_2026-08-30.md` §12-6)

| # | 사안 |
| :-: | :-- |
| 1 | `fd_yr1_ern_r = 1,436.24%`(마이다스아시아리더스 Ce) 진위 — 기준가 리셋 의심. CROSS-001 `gold_verified=false` 원인 |
| 2 | B-4-03~12 단위 어휘 enum 고정 여부 — 채권 컨벡시티 `연²` 도 대기 |
| 3 | `shared/region.yaml` `status: pending` 3종 13행 — `Asia Pacific`(9, **`Region_AsiaPacific` 노드 기존**) · `Greater China`(2) · `EuroZone`(2) |
| 4 | §9-4 의 3·4·5 (rptt 가짜 노드 잔여 5 · 12자 쓰레기값 정규식 · `mtco` 짧은 값 7,962) |
| 5 | 사실상 빈 컬럼(`fd_wk1_ern_r` 100% · `itm_eabrv_nm` 99.4% · 답변금지 `fd_prsv_r`·`fd_sbpr`)을 스키마 프롬프트에서 뺄지 |

### 2-2. 다음 작업 순서 (권고)

1. **채점기** — 4도메인 공용. `gold_sql` 결과 집합 vs HCX SQL 결과 집합 일치율 + `must_include`/`must_not_include` + `expected_behavior`(reject/clarify) 판정. 가짜 플래너에 gold SQL 을 물려 100% 가 나오는지로 자체 검증. **지금 87문항 gold 는 "정답 SQL 이 실행되나" 만 확인한다 — 규칙이 HCX 에서 먹는지 확인된 것은 하나도 없다.**
2. **펀드 평가 문항 ~20건** — `대표행`·극단값·보수 ‰·위험등급 방향·되묻기·신용등급 기각이 각 1문항 이상. 채권 `scripts/gen_bond_eval.py` 패턴(규칙 조건식 → gold SQL → DB 실측). 지금 펀드 전용 문항 **0**.
3. 펀드 규칙 **압축** — planner_context **10,081자**(채권 압축 전 9,105 보다 큼). 채권 `_compress_rules_2026-08-30.py` 재사용. 문항으로 회귀가 잡힌 **뒤에**.
4. `PLAN_2026-08-29` 재확인 — **D-6**. 제안서 40점이 여전히 20%, 9/2 착수 데드라인.

### 2-3. 🔴 알려진 함정 (이번 세션에서 실제로 밟은 것)

| 함정 | 대응 |
| :-- | :-- |
| **병렬 세션이 내 미커밋 편집을 자기 커밋에 담는다** | 이번엔 `b61fa36`·`9293bd8` 이 게이트·테스트·yaml·검토표·검토기록을 흡수했다. 유실은 없었으나 커밋 메시지로는 추적 불가 — **검토기록 §12 가 이력의 정본**. 내 몫은 즉시 커밋하고, 남의 미커밋 산출물은 지우지 말고 **스크래치패드로 옮긴다** |
| **같은 날 반대 결정** | 오전 세션의 "nm_* 컬럼" 이 메모리에 "리드가 두 번 확인한 원칙" 으로 적혀 있었다. 설계 결정 메모리는 **근거 실측과 시각**을 함께 적고, 뒤집히면 덮어쓰되 경위를 남긴다 |
| **진행 중 파일이 수집을 깨뜨림** | 다른 세션의 `test_name_features.py`(없는 모듈 import) · `test_improvements.py`(없는 함수 import)가 `pytest` 를 exit 2 로 만들어 `deploy.sh` 0단계가 막혔다. `--ignore=` 로 우회 검증하고, 배포 전 전체 수집 확인 |
| **배포 명령 차단** | `deploy/deploy.sh` 는 SSH·재빌드라 자동 실행이 분류기에 막힌다. 리드가 `!` 로 직접 |
| **팀원 스크립트의 경로 하드코딩** | `audit_bonds_claims.py`·`gen_bond_eval.py`·`route_prototype.py` 가 `os.chdir(r"C:\Users\bella\...")`. `re.sub` 로 그 줄을 지우고 `exec` 해서 돌렸다 — 담당자에게 저장소 상대경로 전환 요청 |
| **생성기 상속 규칙** | 데이터 사전·검토표 생성기 모두 **명시 항목이 있으면 `applies_to` 상속을 통째로 끊는다.** `fd_yr3_ern_r` 이 그래서 판정이 비어 있었다. 컬럼별 수치는 원천 항목 안에 표로 |
| **생성 문서 재실행 금지** | `funds.md`(데이터 사전)는 커밋본으로 되돌려 둔 상태 — 8/30~31 판정이 안 보인다. 재생성은 `gen_data_dictionary.py` 인데 사용자 편집을 덮으니 시점을 정해서 |

---

## 3. 운영 정보

| 항목 | 값 |
| :-- | :-- |
| 엔드포인트 | `https://49.50.134.229.nip.io` — `agent_ready:true` · `planner:"hcx"` |
| 🔴 서버 버전 | **`9157724` (8/30 01:51)** — 로컬 HEAD `e7b6e1e` 보다 **21커밋 뒤** |
| 서버 DB | `3437db7f…` 278,503,424바이트 — **로컬과 내용 동일, 바이트 상이**(§2-1 ①) |
| 실험 UI | `/chat?t=<CHAT_TOKEN>` — 실행 SQL·근거문서 패널. 서버 배포 후엔 `[+후손 …]`·`[Refuse]`·`[Route]` 줄이 보임 |
| 로컬 DB | 283,484,160바이트 · 75컬럼 · `schema_metadata` 280 · `kg_*` 39,658/62,558/7,414/9,919 |
| 감사 | `python scripts/audit_funds_claims.py` · `audit_etf_claims.py` · (채권) `audit_bonds_claims.py` — 각각 불일치 0 이 기준선 |
| 백업 | 스크래치패드 `removed_name_features_2026-08-30/`(파서·테스트·`nm_*` 포함 DB) — 세션 종료 시 사라질 수 있음, 필요하면 옮길 것 |

---

## 4. 착수 순서 (다음 세션)

1. `git pull` — 채권·ETF·연구 트랙이 **동시에** 커밋 중. 이 세션 동안 21커밋이 들어왔다
2. `pytest tests -q`(150) · `python eval/run_gold_check.py`(87/87) · 감사 3종(불일치 0) 기준선 확인
3. **배포**(§2-1 ①) — 리드가 직접. 배포 후 UTF-8 재확인 3문항
4. ETF 담당에 확답 4건 전달(§2-1 ②) → 브랜치 병합(③)
5. 채점기 착수(§2-2) — 그 전까지 규칙 변경은 "먹는지 확인 안 된" 상태
6. `docs/PLAN_2026-08-29.md` — 제안서 9/2 데드라인

---

## 5. 이 세션이 틀렸다가 고친 것

| 무엇 | 어떻게 드러났나 | 교훈 |
| :-- | :-- | :-- |
| "㉡ 만 하면 에코프로 해결" 을 그대로 받아들일 뻔 | DB 에서 한 질문을 손으로 끝까지 풀어 보니 자회사는 closure 가 아니라 엣지에 있었다 | 타인의 진단은 **예시 질문 하나를 데이터에서 끝까지** 따라가 검증한 뒤 착수한다 |
| `thco_sale_yn` `none` 을 기재 오류로 꼽음 | `value_semantics` 에 "NULL = 'N' 의 자리, 결측 아님" 이 이미 적혀 있었다 | 판정 필드 하나만 보지 말고 **같은 항목의 다른 필드**를 읽는다 |
| "56 passed" 보고 | 내 grep 이 `error` 줄을 걸러 수집 오류를 못 봤다 — 실제 exit 2 | 테스트 결과는 **종료 코드**로 확인한다 |
| 감사 초회 불일치 10건 중 7건이 내 검사식 문제 | 운용사 라벨 약칭·`itm_eng_nm` 자체 정의·pending alias·이름 패턴 정의 | 불일치는 "yaml 이 틀렸다" 가 아니라 "둘 중 하나" — 먼저 **주장의 정의**를 확인한다 |
| 파생 컬럼을 "정확도" 문제로 논함 | 이름 기반이면 규칙이든 컬럼이든 정보량이 같다는 리드 지적 | 저장 위치는 **일관성·묶기** 의 문제지 정확도의 문제가 아니다 |
