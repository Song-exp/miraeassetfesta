# 🤝 세션 인수인계 — 2026-09-03 제안서 트랙 (제출물 정리 → 목차·분담 → 도메인 템플릿 → 폴더 신설)

> 작업 구간: **9/2 저녁 ~ 9/3**. 이 문서는 **제안서(40점)와 제출물 3종** 트랙만 다룬다. 코드 수리 루프(7R)는 별도 세션·별도 HANDOFF 가 정본이며 이 문서는 건드리지 않는다.
> 앞 문서: `docs/HANDOFF_2026-09-02_pm.md`(코드 루프). 이 트랙의 정본 폴더: **`docs/기술제안서/`**.
>
> 🔴 **D-3** — 제출 마감 **2026-09-06 23:59**. 제안서 원고는 템플릿 단계, 본문은 아직 0쪽.

---

## 0. 지금 상태

| 항목 | 상태 |
| :-- | :-- |
| main | `e7cb429` push 완료 (`docs/기술제안서/` 신설 + `docs/proposal/README.md` 갱신). origin/main 과 동기화 |
| 제안서 원고 | **템플릿·설계 완료, 본문 미작성**. 08 파일 8개(00~07) |
| 제출물 1 소스코드 | 코드·Dockerfile·requirements·`ontology/*.ttl` 5분할 ✅ / **루트 `README.md` 없음** 🔴 |
| 제출물 2 기술 제안서 | 🔴 미작성 (이 트랙) |
| 제출물 3 API 서버 정보 | 서버 `https://49.50.134.229.nip.io/answer` 가동 ✅ / **API 명세서 파일 없음** 🔴 (`PROJECT.md` §7 이 원고) |
| 제출 채널 | 🔶 원격이 개인 레포(`Song-exp/miraeassetfesta`). **주최 Organization Private Repo 주소 확인 필요** |
| 수치 출처 | 🔴 `docs/proposal/NUMBERS.md` KG 수치 stale (kg_node 39,659 → 실측 41,580 · Security 26,271 → 27,996 · FundAttribute/Country 행 누락). 원고 착수 전 `python scripts/gen_proposal_numbers.py` 재실행 |

---

## 1. 이번 구간에 한 일

### 1-1. 제출물 규격 정리 (PROJECT.md §1·§2·§7·§8·§9 + QNA_REVIEW + DEPLOY_CHECKLIST 대조)

- 제출 3종: ① 소스코드(+README 에 엔드포인트 필수 + ttl 5분할) ② 제안서 PDF(주최 6대 목차) ③ 엔드포인트 URL + API 명세서.
- 배점 40/40/20. 제안서 5번 항목(확장성·사업화) 강조. 결측 보완·외부 데이터 수집 흐름·무결성 관리는 **보고서에 안 쓰면 정성평가 없음**(주최 Q&A).
- 발견: 루트 README 없음, API 명세서 없음, 원격이 개인 레포.

### 1-2. 온톨로지·KG 구현 실태 인벤토리 (탐색 에이전트 실측, 2026-09-02)

제안서 소재의 원천. 요지만:
- ttl 5개는 전부 `scripts/build_ontology.py` 생성물. `common.ttl` 58,967줄(스키마 45행 + 개체 인스턴스), 도메인 ttl 21~36줄(테이블 클래스 1 + ABSENT + 위험등급 범위).
- KG = SQLite 4테이블 `kg_node 41,580 · kg_alias 66,592 · kg_edge 7,414 · kg_closure 9,965`. 노드 11종. edge 4종(hasAssetClass·coversRegion·feedsInto·subsidiaryOf 3). **`holds` 는 edge 가 아니라 `ext_*` 테이블 자체**.
- 핵심 구조 결정 3: 테이블 축(DomesticETF 등 4) / 상품종류 축(ETF⟂ETN) 분리 · `range_by_table` 로 ttl 제약과 게이트 상수 동일 원천 · 값 사전(1층)과 값 검사기(3층)가 같은 kg_alias.
- 런타임 소비: Route(자동 어휘) → Ground(`_ground`·`expand_node`) → Gate(ABSENT·등급 표준표 4분기·range·상수) → Plan(`build_grounding`, 규칙 2층) → `validate_sql`+값 검사기 → 조립.
- 도메인×공유개체 연결 매트릭스(kg_alias 실측) — `docs/기술제안서/03_구성도_설계.md` §4.

### 1-3. 산출물 — `docs/기술제안서/` (커밋 e7cb429)

| 파일 | 내용 | 담당 | 상태 |
| :-- | :-- | :-- | :-: |
| `00_README.md` | 폴더 안내 · **합본 순서** · 근거 자료 위치 · 전원 규칙 5 | 공용 | ✅ |
| `01_작성방향_분담.md` | 분담 원칙(도메인 수직 1 + 횡단 장 1~2) · 3인 매트릭스 · 도메인 절 다섯 줄 템플릿 · **용어 규율**(Federated·그래프 탐색 금지, 펀드 두 뜻) · 일정 | 공용 | ✅ |
| `02_목차.md` | **절 22개 압축판**(초판 60여 절은 "파이프라인 설명서"로 읽혀 폐기) · 분량 배분: 02-② 온톨로지 12쪽/40% | 공용 | ✅ |
| `03_구성도_설계.md` | 그림 A(허브-스포크 통합) · 그림 B×3(도메인, A 와 같은 개체 순서) · 표 C(4도메인×11개체 매트릭스) · 선의 문법 · mermaid 초안 | 리드 | ✅ 설계 / ⬜ 조판 |
| `04_도메인_채권.md` | §A 데이터 · §B 온톨로지(그림 B-1 · 소재 8건 · 완성 예시 1) · §C 흐름도(공식예시 #1) · §D 현업 · §E 부록(ttl·ABSENT·yaml 발췌) | 채권 | ⬜ ▶ 채우기 |
| `05_도메인_ETF.md` | 국내·해외 대칭 구조도 · 기존 `ontology_engineering_etf.md` 8건에 층/런타임/실측 열 보강 · §C 에코프로 자회사 · 2.1 절 통합·부록 편집 겸임 | ETF | ⬜ ▶ 채우기 |
| `06_도메인_펀드.md` | grain·복합키 중심 · 소재 10건 · §C 국민성장펀드 · Fund 두 뜻 표기 규칙 | 리드 | ⬜ ▶ 채우기 |
| `07_리드_공통절.md` | 0 요약 · 01 · 2.2.1 · 2.2.5 · 2.3 · 03 · 4.4 답변불가 뼈대 + 부록 리드분 | 리드 | ⬜ |

### 1-4. 분담 확정 (3인, 리드 = 펀드 담당 가정)

| 사람 | 도메인 수직 (§A~§E) | 횡단 장 |
| :-- | :-- | :-- |
| 리드(펀드) | 06 | 0 · 01 · 2.2.1 · 2.2.5 · 2.3 · 03 · 4.4 · NUMBERS 재생성 · 합본 용어 검수 |
| ETF | 05 | 2.1 절 통합(04·06 §A 취합) · 06 부록 편집(04·05·06 §E + 07 부록분) |
| 채권 | 04 | 04 흐름도 4개 취합(§C ×3 + 07 §4.4) · 05 기대효과 집필(§D ×3) · PDF 조판 · **README·API 명세서** |

---

## 2. 🔴 앞으로 해야 할 일 (우선순위)

### P0 — 제출물 필수 파일 2개 (제안서와 무관, 9/6 전 아무 때나 1시간)

1. **루트 `README.md`** — 환경 구성 · 실행 명령(`docker compose up -d --build`) · **엔드포인트 `https://49.50.134.229.nip.io/answer`** · ttl 5분할 경로 · 재현 절차(`scripts/build_ontology.py` → `eval/run_gold_check.py`). 담당: 채권(또는 리드).
2. **API 명세서** — `docs/API_SPEC.md` 또는 README §. `PROJECT.md` §7 의 표(GET /answer · 쿼리 `question_id`·`question` · 응답 5필드 string · 200 유지 · 60초 권장/300초 하드) + cURL + 응답 예시 그대로. 제안서 부록 D 와 같은 원고.
3. **제출 채널 확인** — 주최 Organization Private Repo 주소. 받으면 `git remote add org …` 후 마감 전 push. 대용량(DB 263MB)은 클라우드 링크로.

### P1 — 원고 (9/3 초안 · 9/4 완성)

| 일 | 리드 | ETF | 채권 |
| :-- | :-- | :-- | :-- |
| 9/3 | NUMBERS 재생성 → `03` §4 매트릭스 값 대조 · `07` 0·01·2.3 초안 · `06` §B 착수 | `05` §B 보강(층/런타임/실측 3열) · 2.1 골격 확정 | `04` §B 초안 · 흐름도 형식 확정 · 3인에게 think_trace 요청 |
| 9/4 | `07` 2.2.1·2.2.5·03 · `06` 완성 · **합본 1차** 용어 검수(`01` §0-4) | 2.1 단락 취합 · 부록 편집 | 흐름도 4개 취합 · 05 기대효과 · README·API 명세서 |
| 9/5 | 합본 2차 · 수치 대조 | 교차 리뷰(남의 도메인 절) | PDF 조판 · 교차 리뷰 |
| 9/6 | push (org 레포) | 버퍼 | 버퍼 |

think_trace 실측 명령은 각 도메인 파일 §C 와 `07` §4.4 에 적혀 있다 (`eval/probe_server.py`, `PYTHONIOENCODING=utf-8`).

### P2 — 원고 채우기만으로는 안 끝나는 것 (9/5)

1. **그림 조판** — `03` 의 그림 A·B×3·표 C 는 mermaid 초안. 01·03 장의 파이프라인 도해 2장은 아직 없음. 도구·담당 미정 → 9/3 에 정할 것.
2. **뼈대 없는 절** — 05 기대효과(채권 집필)·06 부록(ETF 편집)은 목차에만 있고 받는 파일이 없다. 필요하면 `08_기대효과_부록.md` 추가.
3. **수치 대조** — NUMBERS 재생성 후 모든 원고 숫자 재대조. 매트릭스 값 수는 NUMBERS 에 없는 항목 → `gen_proposal_numbers.py` 에 "테이블×개체" 절 추가하거나 조판 직전 재실측.
4. **합본·PDF** — `00_README.md` 합본 순서대로 이어 붙여 PDF. 채권 담당.

### P3 — 열린 판단 (리드)

- 리드가 펀드 담당이라는 가정이 맞는지. 아니면 `01` §0-2 매트릭스·`06` 담당 표기 수정.
- ETF 흐름도 질의: 에코프로 자회사(3-hop) vs 캠브리콘. 마스터에 "위험요인" 텍스트가 없으면 ABSENT 안내가 정답이므로 실측 후 결정.
- 07 §2.3.2 의 선행연구 인용은 `ontology_3layers_frame` §3 등급표 준수. `run_paired.py` 결과가 있으면 그 수치만.

---

## 3. 파일 지도

| 경로 | 역할 |
| :-- | :-- |
| `docs/기술제안서/00~07*.md` | **제안서 원고 정본** (이 트랙) |
| `docs/proposal/NUMBERS.md` | 수치 단일 출처 (stale — 재생성) |
| `docs/proposal/ontology_3layers_frame_2026-08-31.md` | 3층 서술 프레임 · 인용 등급 규율 |
| `docs/proposal/ontology_engineering_etf.md` | ETF 결함→선언 8건 원고(8/26) |
| `docs/proposal_data_pipeline_section.md` | 2.1 절 골격 초안 |
| `docs/ontology_rules/01~12*.md` | 규칙 12종 생성 문서 — 도메인 절이 번호로 인용 |
| `docs/ontology_audit_2026-09-02.md` | ETF 감사(지역 오분류 43건·`ref_geo_focus` 복구) |
| `PROJECT.md` §2 §7 §8 §9 | 배점표 · API 계약 · 주최 목차 · 공식 예시 질의 |
| `docs/DEPLOY_CHECKLIST.md` §5 | 엔드포인트 통과 기준·URL |

---

## 4. 이 구간의 교훈

1. **목차는 시스템 구조가 아니라 채점 구조를 따라야 한다** — 파이프라인 8단계를 절로 풀었더니 60여 절짜리 설명서가 됐고, 배점 비중대로 재배분하니 22절로 줄었다. 절 수가 채점 항목 수보다 훨씬 많으면 과세분화 신호.
2. **"도메인별로 나눠 쓴다"와 "온톨로지가 셋"은 다르다** — 실물은 common 1 + 도메인 4 파일. 도메인 절의 주어는 "공통 개체를 내 데이터가 어떻게 썼고 무엇을 부재로 선언했나"여야 하고, 그림 B 는 A 의 스포크 확대여야 한다(개체 순서·색 고정).
3. **생성 문서는 수치 출처가 하나여도 재생성 시점이 다르면 어긋난다** — NUMBERS·HOW_IT_WORKS·실측 세 값이 달랐다. 조판 직전 한 번의 재생성을 절차로 박는다.
