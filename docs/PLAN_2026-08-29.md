# 🗓️ 잔여 8일 구현 계획 — 2026-08-29 (D-8)

> **제출 2026-09-06 23:59 · 평가 API 운영 09-07 ~ 09-20 (무변경 상시 가동)**
> 앞 문서: `docs/HANDOFF_2026-08-27.md`(직전 세션) · `docs/WORK_PLAN_2026-08-26.md`(트랙 A·B) · `docs/meeting_agenda_2026-08-26.md`(안건)
>
> **이 문서의 성격** — "무엇이 끝났고, 남은 8일에 무엇을 어떤 순서로 하는가"를 **배점 기준으로** 정리한다.
> 기존 계획서(`WORK_PLAN_2026-08-26.md`)가 *데이터 품질 트랙*이라면, 이 문서는 **제출·채점 트랙**이다. 충돌 시 이 문서가 우선한다 — 남은 기간이 품질 완주를 허용하지 않기 때문이다.

---

## 0. 오늘(8/29) 실측한 기준선

| 확인 | 결과 | 방법 |
| :-- | :-- | :-- |
| 테스트 | ✅ **35 passed** | `pytest tests -q` |
| gold | ✅ **63문항 · gold_sql 45 · 통과 63 · 실패 0** | `eval/run_gold_check.py` |
| 서버 | ✅ `agent_ready:true` · `planner:"hcx"` | `curl .../health` |
| DB | ✅ 14테이블 (마스터 4 · `ext_*` 4 · `kg_*` 4 · 메타 2) | sqlite 직접 조회 |
| KG | `kg_node` 39,677 · `kg_alias` 62,577 · `kg_edge` 7,414 · `kg_closure` 9,919 | 동일 |
| 외부 | `ext_ovs_etf_holdings` 906,848 · `ext_etf_holdings` 75,859 · `ext_fund_holdings` 59,206 · `ext_fund_page` 10,565 | 동일 |

**남은 기간**

| 날짜 | 요일 | 성격 |
| :-- | :-- | :-- |
| 08-29 ~ 08-30 | 토·일 | 회귀 파급이 큰 것 먼저 (KG 재생성·조인키) |
| 08-31 ~ 09-03 | 월~목 | 답변 품질(40점) + 제안서(40점) 병렬 |
| 09-04 ~ 09-05 | 금·토 | 제출물 규격·재현성·최종 QA |
| **09-06** | 일 | **제출 · 코드 프리즈** |
| 09-07 ~ 09-20 | — | 무변경 운영. 서버를 내리면 그 순간부터 채점 불가 |

---

## 1. 지금까지 구현된 것 (완료 기준선)

배점 항목별로 접어서 본다. **괄호 안은 근거 파일**이다.

### 1-1. 데이터 계층 — ✅ 사실상 완료

| 항목 | 상태 | 규모 |
| :-- | :-- | :-- |
| 2차 마스터 적재 (기준일 8/22) | ✅ | 채권 21,882 · 국내ETF 1,780 · 해외ETF 6,037 · 펀드 23,676 = **53,375행** |
| 원본 재현 경로 | ✅ | `1.금융상품/` 2차 8파일 배치 · xlsx↔DB 전수 대조 일치 (`build_db.py`) |
| 외부 수집 4종 | ✅ | 국내ETF 구성종목 93.9% · 해외 구성종목 AUM가중 88.6% · 펀드 웹 10,565 · 간이설명서 사실 8,446 |
| 코드북 도출 | ✅ | 운용사 275종 · 속성코드 210종 · 보수 단위 ‰ 확정 (`ontology/codebooks/`) |
| 컬럼 사전 | ✅ | 마스터 280 + 외부 50 **전 컬럼** (`docs/data_dictionary/`) |

### 1-2. 온톨로지 · KG — ✅ 제출 규격 충족

| 항목 | 상태 |
| :-- | :-- |
| **ttl 5분할** `common` · `bond_kr` · `etf_kr` · `etf_gl` · `fund_pub` + `fp:` 네임스페이스 | ✅ 규격 p.9 이행 |
| 2층 구조 (`enums/*.yaml` → `shared/*.yaml` → `build_ontology.py` → ttl + `kg_*`) | ✅ 멱등 재생성 |
| 검증기 V1~V6 (컬럼 실존 · 죽은 alias · 판정 충돌 · 포인터 · 코드북 규정 · 부재 선언) | ✅ 통과해야 산출 |
| 개체 9종 / edge 4종 | ✅ Security 26,271 · Fund 7,600 · Index 3,172 · Organization 2,530 외 |
| 규칙 문서화 12종 (결함→규칙→전수 인벤토리→근거) | ✅ `docs/ontology_rules/` |

### 1-3. 런타임 — ✅ 가동 중

```
질의 → gate(불가·역질문 기각) → ground(KG 매핑+yaml 규칙+조인키+스키마)
     → HCX plan_sql → SQL guard → 실행 → HCX compose_answer → 5필드 JSON
```

| 구성요소 | 파일 | 상태 |
| :-- | :-- | :-- |
| API `GET /answer` (5필드 전부 string) | `src/api/main.py` | ✅ |
| 게이트 · 교차질의 감지 | `src/runtime/gate.py` | ✅ |
| 근거문서 조립 · 조인키 4종 | `src/runtime/pipeline.py` | ✅ |
| HCX 플래너 (SQL 512 / 답변 1024 토큰) | `src/hcx/planner.py` | ✅ |
| 실험 UI `/chat` · 질의 로그 | `src/api/chat_ui.py` · `logs/api-*.jsonl` | ✅ |

### 1-4. 인프라 — ✅ 요구 이상

NCP 서버 · Docker Compose · Caddy + Let's Encrypt(`49.50.134.229.nip.io`) · `unless-stopped` 자동복구 · `deploy.sh` 4모드(전체/코드/DB/yaml 5초) · 반입 전 DB 14테이블 검사.
→ 주최 요구는 **HTTP 80 단독·공인 IP·self-signed 무방**이므로 현 구성은 초과 달성이다.

### 1-5. 검증 자산

| 자산 | 규모 | 상태 |
| :-- | --: | :-- |
| 회귀 테스트 | 35 | ✅ 전부 통과 |
| gold 평가셋 | 63문항 (`gold_sql` 45) | ✅ 실행 통과 63/63, **판정 검증 55 · `partial` 1 · `false` 1 · 미판정 6** |
| 검토 채움표 | 408항목 | 🟡 **판정 기입 19건 (4.7%)** — 채권 17 · 그 외 2 |

### 1-6. 제안서 — 🟡 20%

| 목차 | 상태 |
| :-: | :-- |
| 01 아키텍처·특화기능 / 03 시스템 구성도 / 04 기능 흐름도 / 05 확장성 / 06 부록 | ⬜ 미작성 |
| 02-① 데이터 파이프라인 | 🟡 초안 (`docs/proposal_data_pipeline_section.md`) |
| 02-② 온톨로지 엔지니어링 | 🟡 ETF 트랙만 (`docs/proposal/ontology_engineering_etf.md`) |
| 수치 단일출처 | ✅ `docs/proposal/NUMBERS.md` |

---

## 2. 점수판으로 본 격차 — 무엇이 점수를 못 받고 있나

| 배점 | 항목 | 현 위치 | 격차의 실체 |
| --: | :-- | :-- | :-- |
| **40** | 기술 제안서 | 🟡 20% | 🔴 **최대 단일 리스크.** 만든 것(외부 98만 행·코드북 9종·설명서 8,443건·결측 판정)이 **보고서에 없으면 정성평가 대상이 아니다**(주최 명시). 남은 8일 중 가장 큰 작업량 |
| **40** | API 답변 | 🟡 | 채권·펀드 **전용 평가문항이 0건**(63문항 = ETF 46 + 교차/불가 9 + 공식예시 8). 두 도메인이 사실상 무검증으로 평가에 들어간다 |
| **20** | 소스코드 | 🟡 | 🔴 **`README.md` 가 저장소에 없다** — 규격상 엔드포인트 주소·실행 명령이 들어가는 필수 파일. ttl 5종·Dockerfile·requirements 는 충족 |

**추가로 확인된 것**

- `src/api/main.py:249` 전역 예외 → **무조건 200**. 주최는 5xx·타임아웃에 **재시도 2회**를 주므로, 일시적 장애를 200 "확인 불가"로 덮으면 그 문항은 오답으로 확정된다 (안건 A-7, 미반영).
- 60초 예산 초과 시 **부분응답 fallback 미구현**.
- 제출물 3종 중 **API 명세서** 산출물 없음.

---

## 3. 남은 8일 계획

### 3-1. 일자별 (3인 병렬)

| 날짜 | 리드 (song) | 채권 (seohyun) | ETF (Leebyungchul) |
| :-- | :-- | :-- | :-- |
| **8/29 토** | 결정 3건 확정 → **KG 재생성 착수** (P1-4) | 4-1 초고수익률 728% 랭킹 제외 yaml 반영 | 채움표 B·C ETF분 (답변 직결 절 우선) |
| **8/30 일** | 펀드 조인키 `grp→itm_no` 교체 + gold 재현 (P1-3) | **채권 eval 12문항 신설** (P1-1) | 국내ETF 총보수 외부 보완 착수 (P2-1) |
| **8/31 월** | 비정형 최소구현 — 설명서 텍스트 `ext` 적재 (P1-6) | 채권 문항 `gold_sql` 검증 | 총보수 before/after 표 |
| **9/1 화** | **펀드 eval 12문항 신설** (P1-1) | 오답 루프 (`/chat` → yaml) | 제안서 02-② 원고 확정 |
| **9/2 수** | 제안서 01·03·04 집필 | 답변불가 5유형 방어 회귀 (P1-2) | 제안서 06 부록 (평가 재현 절차) |
| **9/3 목** | 제안서 02-①·05 + 전체 초안 통합 | **35문항 모의 평가** 1차 | 모의 결과 반영 |
| **9/4 금** | **README · API 명세서 · HCX 키 재발급** (P0) | 회귀 확인 | 클린 빌드 재현성 검증 |
| **9/5 토** | 제안서 PDF 확정 · 최종 QA · 프리즈 리허설 | 모의 평가 2차 | 모의 평가 2차 |
| **9/6 일** | **제출 · 프리즈** (cron off · `/chat` 내림 · ACG 정리) | — | — |

> 순서 원칙 — **회귀 파급이 큰 것을 먼저**(KG 재생성·조인키), **문서는 뒤로 미루지 않는다**(제안서는 40점이고 마지막 날 몰면 반드시 미완성된다).

### 3-2. 작업 카드

#### P0 — 제출 요건 (없으면 점수가 아니라 자격 문제)

| # | 무엇 | 왜 | 어디 | 소요 |
| :-: | :-- | :-- | :-- | :-- |
| P0-1 | **`README.md` 신설** — 환경 구성·실행 명령 + **엔드포인트 URL 명시** | 규격이 "엔드포인트가 들어가는 곳은 README" 라고 못 박음. 현재 파일 자체가 없다 | `/README.md` | 0.5일 |
| P0-2 | **API 명세서** — 요청/응답 JSON 스키마·예시·오류 규약 | 제출 3종 중 하나 (엔드포인트 URL + 명세서) | `docs/API_SPEC.md` | 0.5일 |
| P0-3 | **기술 제안서 6절 + PDF** | 40점. 만든 것을 쓰지 않으면 정성평가 0 | `docs/proposal/` | **3인·일** |
| P0-4 | **HCX 테스트 키 폐기·재발급** + `.env`·`secrets/` 이력 점검 | 대화 로그 노출 이력 있음 | 서버 `.env` | 0.5일 |
| P0-5 | **클린 재현성 검증** — 빈 클론 → `docker build` → `/answer` 응답 | 소스코드 20점의 "Dockerfile 재현성" | — | 0.5일 |

#### P1 — 답변 40점

| # | 무엇 | 왜 | 어디 |
| :-: | :-- | :-- | :-- |
| P1-1 | **채권·펀드 eval 문항 각 12건 신설** (+`gold_sql`) | 63문항 중 두 도메인 전용이 **0건**. 35문항 평가에 두 도메인이 무검증으로 들어간다. 가장 큰 미커버 영역 | `eval/questions_domestic_bonds.jsonl` · `questions_public_funds.jsonl` |
| P1-2 | **답변불가 5유형 방어 회귀** — 정확일치·Enum 소속으로만 판정, `LIKE '%..%'` 억지 매칭 금지 | 35문항 중 5문항이 이 카테고리. 확실한 득점 구간 | `src/runtime/gate.py` + 테스트 |
| P1-3 | **펀드 외부 조인키 `grp` → `itm_no`** | `grp` 가 전역 유일이 아니라 **103 grp 가 복수 운용사에 오부착**(실측 재현) → 교차질의 오답 | `pipeline.py` JOIN_KEYS · `CROSS-001.gold_sql` |
| P1-4 | **`rptt` 센티넬 수정 + KG 재생성** | 생성기가 `.strip()` 전에 `(KR)?0+` 판정 → 0계열 가짜 Fund 노드. 미매핑 `KR0000000000` 5,308 · `000000000000` 1,645 | `gen_fund_structure_auto.py` · `public_funds.yaml` |
| P1-5 | **오류 성격별 상태코드 분리 + 60초 부분응답** | 일시적 장애를 200으로 덮으면 **재시도 2회를 버린다** | `src/api/main.py:249` |
| P1-6 | **비정형 최소구현** — 간이설명서 텍스트를 `ext_fund_doc` 로 적재, "구조·투자전략" 질의에 근거 제공 | 필수구성요소 1번 `[Parsing]` + 공식예시 2 (`국민성장펀드의 구조와 투자전략`). **풀 RAG 가 아니라 근거 제시형으로 최소 구현** | `scripts/load_external_doc.py`(신설) |
| P1-7 | 남은 gold 8문항 판정 종결 (`false` 1 · `partial` 1 · 미판정 6) | 검증 안 된 정답으로 회귀를 돌리면 회귀가 아니다 | `eval/*.jsonl` |

> **P1-4 재실측 정정** — 인수인계 §5 의 *"Security `label_ko` 88% 빈칸"* 은 **범위를 다시 봐야 한다.**
> 오늘 실측: Security 26,271 중 `label_ko` 3,151 · `label_en` 23,139 이고, **`label_en` 에 한글이 들어간 것은 101건뿐**이다.
> 즉 대부분은 해외 구성종목(90.7만 행 출처)이라 영문 라벨이 정상이며, 실제 결함은 **101건 규모**다.
> → KG 전면 재생성을 이 항목 때문에 할 필요는 없다. **P1-4(`rptt`)와 한 번에 묶어서** 처리한다.

#### P2 — 정성 가점 (시간이 허락하는 만큼)

| # | 무엇 | 왜 |
| :-: | :-- | :-- |
| P2-1 | **국내ETF 총보수 결측 보완** — 유효 67/1,235 (5.4%) → 외부 보완 + before/after 표 | 주최가 결측 보완을 **정성평가 "중요한 작업"** 으로 명시. "보수 낮은 ETF TOP5" 답변 품질과 제안서 서술을 동시에 푸는 유일한 항목 |
| P2-2 | 검토 채움표 잔여 389항목 — **답변 직결 절 우선**(A 정합성 · C 결측의미 · E-1 라벨 · E-3 별칭) | 전건 완주는 포기하고, 답변을 바꾸는 것만 |
| P2-3 | edge `source: rule` 상위 200 검수 | "미국 지수 추종 ETF" 류 답변 직결. 그 외는 "추정" 병기로 방어 유지 |

### 3-3. 이번 기간에 **하지 않는 것** (명시적 범위 제외)

| 제외 | 이유 |
| :-- | :-- |
| Vector DB 도입 | 안건 A-5 보류 유지. KG 레이블 매칭으로 현 문항 유형은 커버되고, 8일에 새 엔진을 넣으면 회귀만 늘어난다 |
| 서버 자동 반영 cron | 9/6 프리즈에 반드시 꺼야 하는 것을 도입해 실격 리스크를 만든다. `deploy.sh --yaml-only`(5초)로 충분 |
| 검토 채움표 408 전건 완주 | 4.7% 진행. 완주에 필요한 시간이 제안서 40점과 정면 충돌한다 |
| 채권 B3·B5·B6 2차 재산출 중 답변 미사용분 | 판정이 답변을 바꾸지 않으면 이번 기간에는 순위가 없다 |
| 해외ETF 연수익률 대응 | 주최가 **출제하지 않겠다고 확정**(OQ-2) |

---

## 4. 프리즈 체크리스트 (9/6)

| # | 항목 | 확인 |
| :-: | :-- | :-- |
| 1 | `pytest` 전건 · `run_gold_check` 전건 · `check_yaml_dupkeys` 0 | ⬜ |
| 2 | 클린 클론 → `docker compose up` → `/answer` 정상 5필드 | ⬜ |
| 3 | `README.md` 엔드포인트 URL·실행 명령 | ⬜ |
| 4 | ttl 5종 + `requirements.txt` + `Dockerfile` + 제안서 PDF + API 명세서 | ⬜ |
| 5 | HCX 키 재발급본으로 서버 기동 확인 | ⬜ |
| 6 | `/chat` 경로 내림 · `CHAT_TOKEN` 회수 | ⬜ |
| 7 | ACG — `/answer` 를 **주최 발신 대역에 열어 둔 상태**로 전환 | ⬜ |
| 8 | 서버 `unless-stopped` · 재부팅 복구 재확인 | ⬜ |
| 9 | Private Repo push (= 제출 확정 시점) | ⬜ |

**9/7 ~ 9/20 운영 규칙** — 코드·데이터 **무변경**. 장애 시 **재기동은 허용**(실격 아님). 크레딧 만료 9/30 이 평가 종료보다 뒤이므로 **서버를 절대 내리지 않는다.**

---

## 5. 리스크

| 리스크 | 영향 | 대응 |
| :-- | :-- | :-- |
| **제안서가 마지막 날로 밀림** | 40점 직격 | 9/2 착수를 데드라인으로 고정. 초안은 `NUMBERS.md` 인용만으로도 성립하게 써 둔다 |
| HCX rate limit 분당 3.6질의 | 35문항 모의 1회에 최소 20분, 팀 동시 실험과 충돌 | 모의 평가 시간대를 예약하고 그 시간엔 `/chat` 실험 중단. 키 상향 여부는 즉시 결정 |
| KG 재생성 파급 | `kg_*` 전부 갈림 → gold·테스트 재확인 필요 | **주말에 처리**해서 평일 작업이 회귀 위에 쌓이지 않게 한다 |
| ACG 22번이 리드 PC 유동 IP 고정 | IP 변경 시 배포 불가 | 배포 전 `curl ifconfig.me` 습관. 팀원 443 접속은 무관 |
| 채권·펀드 평가문항 미신설 | 두 도메인 오답을 **평가 당일에 처음 본다** | P1-1 을 8/30~9/1 로 앞당겨 배치 |
