# 📮 주최 Q&A 검토 — 2026-08-25

> 대상: 주최 채널에 올라온 질의 9건(우리 팀 + 타 팀) + 주최 답변 3묶음(역질문 답변 · 기존 질의 일괄 답변 · 8/24 데이터 공지).
> 목적: ① 데이터 공지로 해소된 질의 식별 ② 우리 프로젝트에 반영할 항목 ③ 미답변 질의 관리대장.
> 데이터 실측은 `DATA_V2_2026-08-24_impact.md` 기준.

---

## 1. 질의별 판정

| # | 질의 (요지) | 상태 | 근거 |
| :-: | :--- | :---: | :--- |
| Q1 | 역질문 — 멀티턴 평가인가, 단일턴이면 역질문 요구는 어디에 적용되나 | ✅ **답변됨** | 단일턴 확정. 답변불가 질의에 **[불가응답]·[역질문] 둘 다 정답**. 멘트 자유 |
| Q2 | 국내ETF 기초지수 결측 → 상품명에서 추출해 검색조건으로 써도 "제공 데이터 기반"인가 | 🟡 **사실상 해소** (직접 답변 없음) | ① 2차 데이터 `ref_base_index`가 ETF 97.8% 수록 → 전제 자체가 소멸 ② 공지 "어떤 데이터로 지식을 구축할지는 자율" ③ 크롤링도 허용 → 상품명 파싱은 당연히 허용 범위 |
| Q3 | 교차질의 = 한 호출에서 4테이블 교차인가, 호출당 한 주제인가 | ✅ **공지로 해소** | "ETF 및 펀드를 함께 검색해야 함" 예시 = **한 호출에서 복수 테이블 교차** |
| Q4 | 6/30 기준 공시가 8/15 반기보고서로 나온 경우 사용 가능한가 | ✅ **공지로 해소** | "~8/24까지 발행된 자료 사용 가능" → 8/15 발행 반기보고서 사용 가능 |
| Q5 | 컬럼 의미 개별 채팅 문의 요청 | ⛔ **답변 없을 예정** | 공지 "코드값 테이블 제공 불가, 이 부분 질문은 없을 예정" → 자력 판정으로 확정 |
| Q6-1 | LLM: HCX 전용 확정인지, 임베딩 모델도 동일 제약인지 | ❌ **미답변** | — |
| Q6-2 | "현재 판매 가능한 채권" 기준 (BUYABLE_QUANTITY? BUY_YIELD 881건?) | ✅ **공지로 해소** | BUYABLE_QUANTITY 무효. **상폐/리스팅종료 제외 전부 "구매가능"** |
| Q6-3 | 채권 PD_RISK_GCD 방향 — 국고채 다수인데 1등급 최다(15,950) | ✅ **데이터 정정으로 해소** | 1차의 0~6 정수는 "코드 이슈". 2차는 `'11'~'16'` + `pd_risk_nm`('낮은위험(5등급)' 등) 수록 → 5등급 9,849·6등급 8,929 최다. 방향은 ETF와 동일(1=매우높음) |
| Q6-4 | 국내ETF `cu_strtegy` 'C'(409건), 펀드 `or_attr_desc` '06'(5,436건) 코드북 | 🟡 **절반 해소** | `or_attr_desc` '06' → 2차에서 **'파생상품'**(2,302건)으로 명칭 수록. `cu_strtegy` **'C' 422건은 여전** — 코드북 제공 불가이므로 자력 판정(실물복제 763·합성복제 82·액티브 358과 배타 → 'C'는 미분류/기타로 취급, 추정치 명시) |
| Q6-5 | 응답시간 배점 기준 — 60초 권장 vs 300초 타임아웃 | ✅ **공지로 해소** | 기준 비공개. 300초 = 물리적 미응답. **늦어도 응답(300초 미만)은 미응답과 점수 차등** |
| Q7 | 해외ETF는 `du_er_1d`뿐 → 교차질의 TOP10에서 "연수익률 없다"고 답하는 게 맞나 | ❌ **미답변** (부분 근거만) | 공지 "0/결측은 의도된 값 → 빼거나 없다고 응답"이 일반 원칙. 그러나 교차 예시가 해외ETF를 연수익률로 정렬하라는 뜻이면 충돌 → **재질의 필요** |
| Q8-1 | "0 = 의도된 값"이면 수익률·총보수 질의 시 0인 행을 아예 제외하라는 뜻인가 | 🟡 **공지가 곧 답** | "값을 빼고 보여주거나 없다고 응답" = 해당 컬럼 기준 정렬·필터에서 0행 제외 + 개별 조회 시 "수록 없음" 응답. 추가 확인 불필요하다고 판단 |
| Q8-2 | 결측값만 자체 보완하면 "충돌 없음"으로 인정되나 | ❌ **미답변** | 기존 원칙(상충 시 주최 데이터 우선)으로 위험은 없음. 다만 2차에서 결측이 대거 채워졌으므로 **1차 결측 기준으로 보완한 값은 2차 값과 충돌 가능** → 병합 로직 재점검(§2-8) |

**일괄 답변 묶음(질문 없이 온 것)** — 전부 반영 대상:
- 크롤링 허용, 단 **원천 신뢰성/무결성 관리도 과제의 일부**
- 출처: **수집 방법 / 적재 Flow / 활용 로직을 보고서에 기재**. 레포에 데이터만 두면 정성평가 못 받음
- 추가 예시 질문 없음 (8개가 전부)
- 정량평가 방식은 [공지사항] 참고
- **모호한 질의는 최대한 내지 않음** ("TOP3" 개수·기준·운용사 등 조건 명시 예정)

---

## 2. 우리 프로젝트 반영 항목

| # | 반영 | 대상 | 우선순위 |
| :-: | :--- | :--- | :---: |
| 1 | **채권 위험등급 매핑 교체** — `PD_RISK_GCD` raw `"1"~"6"` → `"11"~"16"`, `"00"`=해당없음(19건). `pd_risk_nm` alias 추가. 주석의 "6등급: 채권 10,408 / 0: 58건" 수치 갱신 | `ontology/shared/risk_grade.yaml` | 🔴 지금 KG 매핑이 0건 매치 |
| 2 | **답변불가 처리 정책 확정**: 역질문은 "정답 처리되는 또 하나의 형태"일 뿐 필수 아님. 모호 질의를 안 낸다고 했으므로 **역질문 분기 우선순위 하향** — 존재하지 않는 상품/등급 등 명백 불가는 "확인 불가" 단문, 조건이 빠진 경우만 역질문. 두 응답 모두 근거(`think_trace`)에 "왜 불가/무엇이 빠졌는지" 기록 | `src/runtime/gate.py`, `pipeline.py`, PROJECT.md §2 | 🔴 |
| 3 | PROJECT.md 정정: 30문항 → **35문항(상10/중10/하10 + 답변불가 5)**, 기준일 8/22, "역질문 필수" 뉘앙스 제거, 응답시간 3층 표에 "늦어도 응답 > 미응답" 추가 | `PROJECT.md` §1·§2·§3 | 🟠 |
| 4 | **"구매가능" 판정 규칙 신설** — 채권: `buyable_quantity` 무시, `mat_dt` 경과·상폐만 제외. ETF: `pd_lste_dt < 99991231` 또는 `pd_sale_yn=0` 제외. 펀드: `sale_yn='판매완료'` 제외. 그 외 전부 "구매 가능" | 도메인 yaml `query_rules` | 🟠 |
| 5 | **0값 배제 규칙 일반화** — 수치 컬럼 정렬/필터 시 `IS NOT NULL AND <> 0` 기본, 0은 "수록 없음"으로 응답. 단 0이 실제값인 컬럼(레버리지 없음=1.0, 무분배 0, 채권 전환사채 표면금리 0 등)은 yaml에 `zero_is_value: true`로 예외 선언 | 도메인 yaml `normalization` | 🟠 |
| 6 | **교차질의 = 단일 호출 내 복수 테이블** 설계 확정. 계획 단계에서 도메인 라우팅을 "하나 고르기"가 아니라 "해당하는 전부"로. 도메인별 결과를 합칠 때 단위(KRW/USD)·기간(1y vs 1d) 불일치를 답변에 명시 | `src/runtime/pipeline.py` 플래너 | 🔴 |
| 7 | **해외ETF 연수익률 부재 대응** (Q7 미답변 대비): 교차 TOP-N에서 해외ETF는 "연수익률 미수록 — 제외" 명시 + 별도로 1일수익률 순위 병기하는 fallback | `overseas_etfs.yaml` `answer_policy` | 🟠 |
| 8 | **외부 보완값 병합 로직 재점검** — 1차 결측을 채우려고 수집한 값(펀드 총보수·개방/폐쇄형·ETF 기초지수 등)이 2차에서 마스터 값과 공존 → 마스터 non-null이면 마스터, null이면 외부, 출처 태그를 `retrieved_context`에 표기 | `scripts/load_external_web.py`, 답변 조립부 | 🟠 |
| 9 | **외부 수집 기준일 8/24로 완화** — `fetch_*` 스크립트의 7/11 필터 상수, `EXTERNAL_DATA.md` 제약 서술 갱신. 6/30 기준 반기보고서·8월 Holdings 수집 가능 | `scripts/fetch_*.py`, `docs/EXTERNAL_DATA.md` | 🟠 |
| 10 | **기술 제안서에 데이터 파이프라인 절 필수** — 수집 방법·적재 Flow·활용 로직·신뢰성 검증(원천 교차검증, 기준일 관리). 레포의 `SOURCES.md`·`fetch_log`를 보고서 본문으로 끌어올림 | 제안서 목차 (PROJECT.md §8) | 🟠 |
| 11 | `cu_strtegy` 'C' 422건 판정 — "미분류(추정)"로 두고 복제방식 질의 시 모수에서 제외·명시 | `domestic_etfs.yaml` | 🟢 |
| 12 | `or_attr_desc` '06' 매핑 삭제 — 2차에서 '파생상품' 명칭 직접 수록 | `public_funds.yaml` (해당 판정 있으면) | 🟢 |
| 13 | 추가 예시 질문 없음 → 현재 8개 예시 + 교차 예시 1개가 평가 유형의 전부라는 전제로 테스트셋 설계 | `eval/` | 🟢 |

---

## 3. 미답변 질의 관리대장

| ID | 질의 | 제기 | 우리 영향 | 임시 가정 (답변 올 때까지) | 조치 |
| :-: | :--- | :--- | :--- | :--- | :--- |
| OQ-1 | HCX 전용 확정 여부 · **임베딩 모델**도 HCX 제약인지 | Q6-1 | 검색 계층 설계 (임베딩 자유면 오픈 모델 사용 가능) | 최종 답변·오케스트레이션만 HCX 고정, 전처리·임베딩은 자유 (과제소개자료 "보조 작업은 타 기법 자율" 문구 근거) | 재질의 시 "임베딩" 한 단어로 좁혀서 |
| OQ-2 | 해외ETF 연수익률 부재 시 교차 TOP10에서 "없다"고 답하는 것이 맞는지 | Q7 (우리 팀) | 교차질의 채점 직결 | "미수록 — 제외" 명시 + 1일수익률 병기 (§2-7) | **재질의 권장** — 예시 질의 원문을 인용해 "해외ETF는 결과에서 제외하는 것이 정답 처리되는지" 로 |
| OQ-3 | 결측만 외부값으로 채운 경우 "충돌 없음" 인정 여부 | Q8-2 | 외부 데이터 전략 전반 | 마스터 우선 병합(§2-8)이면 정의상 충돌 없음 | 재질의 불필요 판단. 보고서에 병합 우선순위 명시로 갈음 |
| OQ-4 | 상품명 파싱을 "제공 데이터 기반"으로 인정하는지 | Q2 | 2차 데이터로 필요성 급감 | 허용으로 간주 (자율·크롤링 허용 근거) | 재질의 불필요 |
| OQ-5 | `cu_strtegy` 'C' 의미 | Q6-4 | 복제방식 질의 모수 | 미분류(추정) | 코드북 미제공 확정 → 종결 |

> 관리 규칙: 답변이 오면 §1 표의 상태를 갱신하고 이 표에서 제거. 재질의는 OQ-2 한 건을 우선 발송하고, OQ-1은 제안서 작성 전(8월 말)까지 답이 없으면 임시 가정대로 확정.
