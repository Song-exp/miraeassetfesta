# ✅ 리드 확답 — ETF 미해결 6건 (2026-08-31 · [ask_lead_2026-08-31.md](ask_lead_2026-08-31.md) 답글)

> 6건 전부 확답함. §1·§3·§4·§5·§6 은 **반영까지 완료** — 검증(dupkeys 0 · build_ontology 오류 0 ·
> gold 111/111 · pytest 163) 통과, `--db-only` 배포 완료. SQL 생성 경로 추적 바로 들어가면 됨.

## §1 `dom_ticker` — ✅ 승인 + 반영 완료

- `gen_security_auto.py` 에 2줄 추가(㉢), CSV 에 `dom_ticker` 열 + 조사분 11종 행 추가.
- 재생성 검증: **11/11 종이 정본 노드(Sec_m_*) 아래 국내(Sec_d_)·해외(Sec_o_/lei) alias 동시 보유.**
  국내ETF 연결쌍 198(티커 단위) 복구 — 조사 문서의 102건(회사-ETF 단위)과 정합.
- **생성기 수정 허락 건**: 이번 건은 리드 쪽에서 직접 반영했음. 앞으로 생성기는 ㉢ 처럼
  **안건 문서 + 실측 근거가 붙은 건에 한해** 수정 OK — 근거 없는 즉흥 수정만 금지 유지.
- ⚠️ 조사표의 "종목 ISIN" 열은 CSV `isin` 에 **넣지 않았음** — 생성기 isin 처리(170행)가 `|` 분할을
  안 해서 다중값이 깨짐. 필요해지면 (분할 지원 + 값 검증) 별건으로.

## §2 ISIN 조인 금지 — ✅ ⓐ 확정 (이미 반영돼 있음)

`overseas_etfs.yaml` `query_rules.ISIN조인금지`(B-3 확답분) + `external_join.forbidden_key` 로 이중 기록.
B-2/D절 승격은 불필요 — 플래너가 읽는 곳은 query_rules 라 ⓐ 가 실효 위치임.

## §3 NVIDIA 회사채 — ✅ "포함하되 밝힌다" + 규칙 신설

회의 공통 패턴("빼지 않고 알린다") 적용. 발행사 기준으로는 맞는 답이므로 **막지 않는다** —
대신 결과에 채권이 섞이면 답변에 밝힌다. `overseas_etfs.yaml` `query_rules.종목질의_회사채포함` 신설
(판별법: CUSIP 발행사부 동일 + 발행물부 상이 · holding_name 의 만기·이율 표기).

## §4 TOP_RANK — ✅ 국내만 해제, 해외·펀드 50 유지 (반영 완료)

실측 — 국내 해제 **+1,772 노드**(3,314→5,086) vs 해외 해제 **+239,280**(24,330→263,610, KG 4배).
주최 예시(에코프로·캠브리콘)가 국내 구성종목 유형이라 국내는 소액 편입까지 grounding 필요,
해외 희귀 종목은 ext_* LIKE 폴백으로 조회 가능하므로 50 유지. 생성기 §1 쿼리에서 rank 조건 제거함.

## §5 섹터 축 — ✅ 우회 3축 규칙 신설, 외부 수집은 보류

`domestic_etfs.yaml` `query_rules.섹터테마질의` 신설 — pd_sect_cd 사용 금지(상품유형 구분임),
① 기초지수명 ② 상품명 ③ 구성종목 3축 우회 + 답변에 근거 축 명시.
외부 섹터 데이터 수집은 효과 대비 비용(수집원 선정·정합성 검증) 커서 **보류** — 엣지케이스에서
3축으로 안 풀리는 유형이 쌓이면 재론.

## §6 총보수 — ✅ 즉시 규칙 보강, 외부 수집은 후속 검토

`cu_charge_rt` answer_policy 에 개별 조회 반쪽 보강 — 0 이면 '0%' 가 아니라 **"수록되어 있지 않습니다"**
(주최 답변 준수). 정렬·Top-N 쪽은 `보수유효`(서버 실검증분)로 이미 커버.
운용사 공시 수집(A-4)은 후속 — 수집하게 되면 ext_fund_page 형식으로.

## 서버 실검증 (배포 후 · 2026-08-31)

- `"Li Auto Inc 편입한 ETF"` → Ground 가 `Sec_m_li_auto` + 후손 3(Sec_d_ 2·LEI) 을 타고
  교차 조인 → TIGER 차이나전기차·KODEX 차이나항셍테크 등 정답. **㉢ 경로 동작 확인.**
- 🔴 엣지케이스 발견 — `"Li Auto를 담은 국내 ETF"` 는 두 군데서 어긋남:
  ① 부분 이름 'Li Auto' 가 라벨 'Li Auto Inc' 에 매칭 안 됨(라벨⊂질문 방향이라 접미어 탈락 필요),
  ② '국내 ETF'(상장 의미)의 '국내' 가 wu_inv_rgn 필터로 들어감 — clarify.국내 규칙이 Ground 매핑엔
  안 걸림. 엣지케이스 단계에서 같이 볼 것.

## §7·§8 — 확인함

name_encoding 블록(f87f47c) 병합·배포 완료. 선물·환헤지·단일종목 규칙은 계획대로 진행하면 됨.
배포 상태: main 최신 + KG 재생성 DB 반입 완료. **HCX 붙는 서버에서 Plan 단계 추적 가능.**
