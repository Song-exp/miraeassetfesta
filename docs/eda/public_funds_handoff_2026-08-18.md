# 🔁 인수인계 — 공모펀드 값 의미 · 결측 처리 (2026-08-18)

> 대상 `public_funds` (95,619행 × 45컬럼 · **종목 `itm_no` 11,139 · 오염 배제 모수 11,130**)
> 기준일 2026-07-11 · 마감 2026-09-06 (D-19)
> **이 문서만 읽고 이어서 작업할 수 있게 썼습니다. §1 작업 규칙을 먼저 읽으세요.**
>
> 앞 인수인계 → `public_funds_codes_handoff.md` (주제 1 식별 코드 · ⚠️ §3 셀 구성표는 낡음)

---

## 0. 세 줄 요약

1. **값 의미 22개 범주형 중 17개 확정 · 결측 처리 범위가 45컬럼 → 19컬럼으로 확정**됐습니다.
   산출물은 `public_funds_value_dictionary.md`(1,257줄) 하나이고, 판정은 `public_funds.yaml` 에 반영돼 있습니다.
2. **결측의 축이 두 개**임이 확정됐습니다 — 식별코드축(상품 나이)과 평가데이터축(판매상태).
   같은 조건에서 배수가 10.81배 vs 0.86배로 **역전**합니다. 하나로 뭉치면 반드시 틀립니다.
3. **운용사 코드↔브랜드를 96.1% 유도**해 `shared/organization.yaml`(마감 8/19)의 차단 요인을 풀었습니다.
   다음 작업은 §6 ①입니다.

---

## 1. 작업 규칙 — 이어받는 사람이 먼저 읽을 것

| # | 규칙 | 배경 |
| :- | :--- | :--- |
| 1 | **모든 건수는 종목(`itm_no`) 단위** | 행(95,619)으로 세면 `prfd_attr_cd` 태그 수만큼 부풀어(평균 8.58배) 비율이 전부 틀린다. 45컬럼 중 종목 안에서 갈리는 것은 `prfd_attr_cd` 하나뿐 |
| 2 | **결측 수치는 `yaml` `missing_profile` 이 단일 원천** | 기존 문서들의 수치가 행 기준과 종목 기준으로 섞여 있었다. 인용은 이 블록에서 |
| 3 | 🔴 **값의 존재/부재 판정은 원본 엑셀에서** | 파생 테이블은 결측을 한 종류로 뭉갠다. "미입력이다" 는 DB에서 내릴 수 없다 — 실제로 `fd_set_pcd '00'` 을 미입력으로 오판했다가 원본 대조로 뒤집었다 |
| 4 | 🔴 **더미 규칙은 컬럼 화이트리스트와 함께** | `^(KR)?0+$` 를 전 컬럼에 걸면 `fd_estb_ctry_cd '000'`(10,854종목) 등 정상값 3종이 결측으로 오판된다. 적용 대상은 식별코드 6개뿐 |
| 5 | **포착률과 적중률을 항상 함께 적을 것** | 예외를 100% 포착하는 조건이 적중률 0.91%일 수 있다. 포착률로 인과를 서술하면 대다수 사례에서 거짓이 된다 |
| 6 | **인과 서술 금지 — 단방향은 병렬 서술** | `번호 없음 → 판매완료`(100%)는 성립하나 역은 불성립. `sale_yn` 을 직접 읽어 두 사실을 나란히 쓴다 |
| 7 | 셀은 관측만 출력 · 판정은 노트북 밖 | 주제 1·2 규칙 유지 (`public_funds_codes_handoff.md` §2) |

---

## 2. 이번 세션 산출물

| 파일 | 상태 | 내용 |
| :--- | :--- | :--- |
| **`docs/eda/public_funds_value_dictionary.md`** | 🆕 **1,257줄** | 주 산출물. §1~§3 값 사전 · §4 외부자료 · §5 정정이력 · §6 착수조건 · §7 결측 처리 |
| `ontology/enums/public_funds.yaml` | 갱신 **1,053줄** | 판정 13 → **16컬럼** · `missing_profile` 신설 · `zero_as_missing` 신설 |
| `docs/DATA_NEEDS.md` | 갱신 339줄 | ①~⑩ → **①~⑰** (7건 신설) |
| **`ontology/codebooks/public_funds_asset_manager.derived.csv`** | 🆕 67행 | 운용사 코드↔브랜드 유도 (`source: rule`) |
| `docs/eda/public_funds_codes_conclusions.md` | 갱신 | §9 기각 목록에 thco 가설 등재 |
| `.gitignore` | 갱신 | `!ontology/codebooks/*.csv` 예외 — 코드북이 `*.csv` 에 걸려 커밋 불가였다 |

---

## 3. 확정된 것

### 3.1 값 의미 — 범주형 22종 (값 사전 §1.1)

```
✅ 확정 17종   or_attr_desc · ovrs_fd_desc · fd_ivst_rgn_desc · fd_estb_ctry_cd · curr_cd ·
              exchdg_yn · prvo_pbff_desc · prvo_fd_desc · pers_corp_desc · sale_yn ·
              thco_sale_yn · ofsfd_yn · hdge_fd_yn · zrin 2종 · int_dvd_desc · frc_bpr_itm_yn
🔶 부분  3종   fd_set_pcd('00') · pfiv_sale_cntl_tcd('01'·'02') · prfd_attr_cd(코드형 210종)
🔴 불가  2종   or_co_xtn_itt_cd · trusc_xtn_itt_cd  → 3.4 에서 96.1% 해결
```

**새로 확정된 값 의미 (근거 포함)**

| 대상 | 판정 | 근거 |
| :--- | :--- | :-: |
| `prvo_fd_desc = '일반사모(2015년전)'` | 공모 마스터에 사모 15종목이 있는 이유. 2015년 개정 전 체계의 잔여 | A |
| `or_attr_desc = '06'` = **파생형** | 686종목 중 678(98.8%)의 종목명에 `파생` | B |
| `fd_set_pcd = '20'` = **단위형** | 부동산펀드 전량(임대형 15/15·대출형 2/2) + 이름의 `목표전환형`·`N년만기` | B |
| `fd_set_pcd = '00'` | 🔴 **뜻 미확정.** 원본에 458행 **실값**(빈칸 아님) · 110/110 판매완료 · 108/110 당사 미취급 | ❌ |
| `fd_estb_ctry_cd = '000'` = **미입력** | ISO 3166에 없는 값 + 역외펀드 0건 → 전 종목 국내 설립인데 97.4% | B |
| `thco_sale_yn` 빈칸 = **미취급** | 원본 전수에도 `'N'` 없음 (Y 91,594 / 빈칸 4,024) | A |
| `pfiv_sale_cntl_tcd '00'` = 기본값 | 원본 빈칸 66행 = 오염 66행 → 오염 외 결측 0건 | A |
| `curr_cd ↔ frc_bpr_itm_yn` **완전 중복** | KRW↔`0.0` 11,067 / USD↔`1.0` 71, 엇갈림 0건 | A |

### 3.2 🔴 결측 처리 — 범위가 19컬럼입니다

```
오염 9종목만 배제하면 25컬럼이 실질결측 0건  →  결측 처리 대상은 19컬럼
그중 std_itm_no · or_co_xtn_itt_cd · trusc_xtn_itt_cd 의 '결측 9건' 은
컬럼의 성질이 아니라 contaminated_rows 사고 한 건이다 (다른 문서의 표기에 주의)
+ thco_sale_yn 은 빈칸이 값이라 제3범주 → 25 + 19 + 1 = 45
```

**새로 찾은 위장결측 — 수치 컬럼의 `0`** (문자열 더미만 다루고 있었다)

| 대상 | 종목 | 판정 | 근거 |
| :--- | ---: | :-- | :--- |
| `fd_nast_suma = 0` | 294 | 위장결측 **B** | 226(76.9%)은 같은 펀드 형제가 0이 아님 · 판매중 192종목의 1년수익률 보유 **0건** |
| 수익률 9기간 전부 0 | 69 | 위장결측 **B** | 5년 누적까지 0.00은 불가능 |
| 5기간 0 + 장기 비영 | 107 | 🔶 미확정 **C** | 최근 1년 완전 무변동. 운용 정지 후보 |
| 0과 비영 섞임 | 299 | ✅ 정상 | 단기 무변동은 MMF 등에서 정상 — **배제 금지** |

### 3.3 🔴 결측의 축이 두 개 (값 사전 §7.5)

```
                  판매완료 안에서 thco(당사취급) 로 다시 갈랐을 때
축 A · 식별코드     rptt 10.81배 · ksd 6.94배 · mtco 3.58배 · fss/kofia 1.58배
  지배 변수: 상품 나이   대리 지표: thco 빈칸 AND 판매완료
축 B · 평가데이터    위험등급 0.86배 · 수익률 0.93배   🔴 역전
  지배 변수: 판매상태   zrin 판매완료 94.5% vs 판매중 0.3% (295.7배)
```

**해소된 예외 2건**

- **판매중 위험등급 결측 27종목** — 전원 순자산 **150억 미만**(최대 142.5억). 판매중 & 150억 이상
  5,327종목에서 결측 **0건**. ✅ *"순자산 150억 이상 판매중 상품은 위험등급 전건 수록"* 을 단정할 수 있다.
  🔴 단 적중률 최고 4.85% — *"소규모라서 없다"* 는 **거짓**이다. 형제 우회도 불가(형제 전원 보유 0종목)
- **수익률 4갈래** — 판정 유효. 오염 배제로 구멍 127→126 · 전무 3,536→3,528.
  🔵 `zero_as_missing` 69종목이 '완전→전무' 이동분과 정확히 일치해 규칙과 분류가 상호 검증됐다.
  🔴 접미결측 근거를 재정렬: **형제 대조(32.7%→32.5%, 정의 무관)가 주근거**,
  '우측결측 94.1% : 5.9%' 는 정의 의존(신 정의 82.6% : 17.4%)이라 보조로 내렸다

### 3.4 운용사 코드 ↔ 브랜드 96.1% 유도

| 등급 | 코드 | 종목 | 비율 |
| :-: | ---: | ---: | ---: |
| A ETF 교차검증(확정) | 4 | 4,214 | 37.9% |
| B 접두 90%+ | 25 | 5,038 | 45.3% |
| C 구 상호 혼재 | 17 | 1,440 | 12.9% |
| **A+B+C** | **46/67** | **10,692** | **96.1%** |
| D 접두가 상품명 (수작업 20분) | 19 | 131 | 1.2% |
| E 다세대 혼재 | 2 | 307 | 2.8% |

🔴 **한 코드에 구 상호가 섞여 있습니다** — 실제 사명변경 이력과 일치합니다.

```
00040007 우리 ← 동양      00080052 키움 ← 우리      00040006 DB ← 동부
00040013 다올 ← KTB       00040018 브이아이 ← 하이 ← 제일     00040087 KCGI ← 메리츠
```

> 🔴 `'우리'` alias 가 **두 코드**에 걸립니다 (`00040007` 현재 · `00080052` 과거).
> 브랜드가 시간에 따라 법인 사이를 이동했으므로 **시점 없는 단일 매핑은 틀립니다.**
> `former_names` 를 alias 에서 빼면 *"동양자산운용 펀드"* 가 0건이 됩니다.

### 3.5 오염 9종목 — 귀속표와 복구 가능성

```
❌ 완전 파손 1종목 (itm_no='"')  복구 불가
   std 'K55301CE3408' · ksd 'KRZ50226929C' 가 DB 어디에도 없다 → 마스터에서 사라진 1종목
   흩어진 값 11개의 귀속은 확정 (yaml contaminated_rows.귀속표)
   🔴 오프셋이 −39~+14 로 불규칙 → 기계적 역변환 불가

✅ 파손 8종목 (미래에셋글로벌포커스4.0)  형제 우회 조회 가능
   or_co='00080008' · trusc='00020054' · or_attr='06' 은 형제 전원 동일
   🔴 값을 채워 고치지 말고 같은 mtco 형제에서 조달할 것
```

### 3.6 🔴 결측과 무관한데 더 위험한 것 — 순자산 `SUM` 3.31배 과대계상

```
삼성MMF법인제1호 C / Cp → 둘 다 12,440,162,407,400원 (반복이지 분할이 아님)
다클래스 펀드 1,513개 중 1,144개(75.6%)가 전 클래스 동일값
전체 SUM 1,545조  vs  펀드별 MAX 합 466조   →  SUM 이 3.31배
```

`yaml` `answer_policy` 가 *"GROUP BY 필요"* 라고만 적어 집계 함수를 명시하지 않았습니다 →
**`MAX` 로 수정 완료.** 단 0 배제 후에도 값이 갈리는 펀드 268개는 `MAX` 도 근사값입니다.

---

## 4. 기각된 것 — 재시도 금지

| 시도 | 결과 |
| :--- | :--- |
| **판매중 `fss` 결측 = 당사 미취급(`thco`)** | ❌ 판매중·미취급이 **11종목뿐** — 전부 결측이라 가정해도 1,139의 **1.0%가 상한** |
| 운용사별 관행 차이 (`fss` 결측) | 🔶 상관 `r=+0.785` → 판매완료 한정 `+0.307` → 판매중 한정 `+0.084`. 대부분 `sale_yn` 교란 |
| 판매중 등급결측 = 미등록 펀드 | ❌ `fss`·`kofia` 를 **27/27 전원 보유** (기준선보다 높음) |
| 판매중 등급결측 = 대체 위험신호 보유 | ❌ `prfd_attr_cd` T축 7.4% · P축 7.4% — 기준선 **이하** |
| 판매중 등급결측 = 특정 운용사·유형 | ❌ 특정 **펀드 2개**에 몰린 것이 운용사 편중처럼 보인 것 |
| `0.00` 을 전면 결측으로 처리 | ❌ 구멍이 126 → 408(3.2배)이 되고, 그중 194가 좌측결측(성질이 다름) |
| 유도 규칙 "개선" 2회 | ❌ 두 번 다 악화. 1차(50% 임계 최장접두)가 정답 — **이전 결과와 대조 필수** |

> 이전 기각 목록(모자형·kofia 자리·다클래스 등)은 `public_funds_codes_conclusions.md` §9 에 있습니다.

---

## 5. 미해결 — 열린 항목 2건

| # | 항목 | 상태 | 경로 |
| :-: | :--- | :--- | :--- |
| 1 | 판매중 `fss`·`kofia` 결측 1,139종목의 원인 | 🔴 **EDA 도달 불가 확정** (가설 3개 기각) | 금감원 등록 원부 (`DATA_NEEDS` ⑯). **포기 후 `unresolved` 확정도 합리적** |
| 2 | `fd_nast_suma = 0` 이 센티넬인지 실값인지 + 클래스마다 값이 갈리는 268개 펀드 | 구조 관측만 (B급) | 🔴 **원본 대조로도 갈리지 않음.** 주최 측 확인 (`DATA_NEEDS` ⑰ Q5) |

부수 미확정: 수익률 5기간 0 + 장기 비영 107종목(기준가 시계열 필요) · `pfiv '01'↔'02'` 축 ·
`kofia_fd_ccd` 자리별 의미 · `prfd_attr_cd` 11축.

---

## 6. 다음 작업 (우선순위)

### ① 🔴 `ontology/shared/organization.yaml` — **마감 8/19**

`ONTOLOGY_BUILD_PLAN.md` §5 담당분입니다. **차단 요인이 §3.4로 풀렸습니다.**

```
입력  ontology/codebooks/public_funds_asset_manager.derived.csv (67코드)
      + domestic_etfs.cu_fund_mgmt_co · overseas_etfs.cu_fund_mgmt_co · domestic_bonds.PD_PBCM

🔴 반드시 지킬 것
   · 8자리 전체를 키로 (뒤 4자리만 쓰면 67 → 55, 12건이 뭉친다)
   · former_names 를 alias 에 포함 (없으면 '동양자산운용' 질의가 0건)
   · '우리' 처럼 시점에 따라 법인이 바뀐 alias 는 두 코드를 후보로 올리고 역질문
   · 유도분은 source: rule + 추정 딱지. 코드북 회신 시 덮어쓴다
   · 수탁사 18종은 이름이 없다 → absent 로 두거나 DATA_NEEDS ① 회신 대기
```

같은 담당분인 `currency.yaml` 은 값이 `KRW`/`USD` 2종뿐이라 함께 끝낼 수 있습니다.

### ② D등급 19코드 브랜드 절단 — 20분

종목 131개(1.2%)뿐입니다. `derived.csv` 에서 `evidence=D` 행의 `brand` 를 첫 단어로 자르면 끝입니다
(타임폴리오 · 알파 · 라자드코리아 · 칸서스 · VIP · 머스트 · 브레인 · 더제이 · GB · 디에스 ·
이지스 · 골드만삭스 · 푸르덴셜 · 베어링 · 블랙록 · 하나티마크 …).
E등급 `00040011`(299종목)은 **멀티에셋**(118·39.5%) ← 산은(55) ← 서울로 수작업 확인이 필요합니다.

### ③ `value_semantics` 45컬럼 `yaml` 이관 — 여전히 **0/45**

재료는 값 사전 §2·§3에 다 있습니다. 규격은 `NEXT_STEPS.md` §5.1.
반영 계획과 컬럼별 분류는 값 사전 **§5** 에 표로 있습니다.

### ④ `eval/questions_public_funds.jsonl` 10문항 — **0문항**

ETF 담당은 각 10문항을 냈습니다(`ONTOLOGY_BUILD_PLAN.md` §1). 함정 1개 = 문항 1개 원칙으로
채울 후보가 값 사전 §7.6 · §7.7 에 준비돼 있습니다:

```
순자산 Top-N → MAX 집계 (SUM 이면 3.31배 오답)   ·  zrin '6' 0건 vs '6.0' 338건
'높은위험' 공백 변형                              ·  더미 000000000000 노출 금지
판매완료 결측의 병렬 서술(인과 금지)                 ·  접미결측 → 주어를 클래스로
재간접 3,022 중 1,604가 주식형(25% 누락)          ·  국내 펀드 환헤지 → '해당사항 없음'
위험등급 방향 역전                                ·  상장지수 23종 이중계상
```

### ⑤ `DATA_NEEDS` ⑰ 주최 측 확인 질문 5건 발송 — A4 한 장

수집이 아니라 질문이라 비용이 거의 0입니다. **Q4·Q5는 결측률 자체를 바꿉니다**(0.00% ↔ 97.52%).

### ⑥ 정정 대기 — 다른 문서의 낡은 수치

| 문서 | 내용 |
| :--- | :--- |
| `docs/domain/public_funds.md` §1.2 | 운용사 55개·수탁사 17개 → **67 / 18** |
| `docs/domain/public_funds.md` §2.3 §7.1 | `thco_sale_yn` = `Y`/`N` → **원본에도 `N` 없음** |
| `docs/domain/public_funds.md` §3.1 | *"순자산이 클래스마다 동일"* → 다클래스 펀드 1,513개 중 1,144개(75.6%)만 동일 |
| `public_funds_codes_handoff.md` §3 | 셀 구성표 16셀·6~11 미실행 → 현재 31셀·전 셀 실행 |
| `public_funds_entity_map.md` | `Fund` = `mtco` 단독 4,660 → 합성키 **4,643** 기준 재생성 (이미 지적됨) |

---

## 7. 다른 장비에서 이어받는 방법

```
필요한 것: 원본 데이터 1.금융상품/*.xlsx  (data/ · 1.금융상품/ 은 .gitignore 대상)

1. pip install -r requirements.txt
2. python scripts/build_db.py        # 1.금융상품/*.xlsx → data/financial_products.db
3. python scripts/profile_table.py   # DB → ontology/enums/*.auto.yaml + *.values.txt (약 40초)
4. (선택) python scripts/gen_entity_map.py   # yaml → docs/eda/public_funds_entity_map.md
```

**이걸로 전부 복구됩니다** — 2026-08-18 부터 EDA 노트북도 추적하기 때문입니다(`.gitignore` 정책 변경).

| 항목 | 상태 |
| :--- | :--- |
| 판정·근거·답변 정책 | ✅ 커밋됨 (값 사전 · `public_funds.yaml` · conclusions · DATA_NEEDS · 코드북) |
| **EDA 노트북 (관측 근거)** | ✅ **커밋됨** — `public_funds_eda2.ipynb`(31셀, 주제1·2) |
| DB · `*.auto.yaml` · `*.values.txt` | 🔄 재생성 (위 2·3단계) |
| `secrets/` (HCX 키 등) | 🔴 미추적 — EDA 에는 불필요, Agent 실행 시 필요 |

> ⚠️ **`notebooks/public_funds_eda.ipynb`(1차)는 선행 0 손실 상태에서 쓴 것이라 코드 구조
> 분석 부분이 무효입니다.** 이력 보존용으로만 추적합니다 — 근거로 인용하지 마세요.
> 유효한 작업장은 `public_funds_eda2.ipynb` 입니다.
>
> ⚠️ **주제 2의 ⓪ 준비 셀이 실행번호 64로 하위 셀(59~63)보다 나중에 재실행돼 있어
> A-1~C-1 출력이 stale 합니다.** 주제 2 출력을 근거로 쓰기 전에 25번 셀 이후를 순서대로 재실행하세요.

### 7.1 검증 스니펫

```bash
# yaml 파싱 + 45컬럼 전수 커버 검증
python -c "
import yaml,io,sqlite3
d=yaml.safe_load(io.open('ontology/enums/public_funds.yaml',encoding='utf-8'))
mp=d['missing_profile']
a=set(mp['결측없음_25컬럼']['columns']); b=set(mp['결측있는_19컬럼']); c=set(mp['빈칸이_값인_1컬럼']['columns'])
con=sqlite3.connect('file:data/financial_products.db?mode=ro',uri=True)
real=set(r[1] for r in con.execute('PRAGMA table_info(public_funds)'))
print('45컬럼 전수:', (a|b|c)==real, '· 중복:', bool(a&b or b&c or a&c))
"
```

```
오염 배제 조건  (itm_no = '"' OR mtco_itm_no LIKE '%"%')     ← 종목 9개
펀드 단위 키    (or_co_xtn_itt_cd, zfill(mtco_itm_no,7))     ← query_rules.펀드단위
더미 규칙       normalization.dummy_as_missing (컬럼 6개 한정!)
```

---

## 8. 관련 문서

| 문서 | 내용 |
| :--- | :--- |
| **`public_funds_value_dictionary.md`** | **주 산출물** — 값 사전 + 결측 처리 |
| `ontology/enums/public_funds.yaml` | 판정·답변 정책 (런타임이 읽음) · `missing_profile` 단일 원천 |
| `public_funds_codes_conclusions.md` | 주제 1 식별 코드 확정 결론 · §9 기각 목록 |
| `public_funds_codes_handoff.md` | 주제 1 인수인계 (⚠️ §3 낡음) |
| `docs/DATA_NEEDS.md` | 외부 수집 ①~⑰ |
| `docs/ONTOLOGY_BUILD_PLAN.md` | 워크샵 결정 · `shared/` 규격 · 일정 |
| `docs/domain/public_funds.md` | 도메인 이해 (⚠️ §6 정정 대기 3건) |
| `notebooks/public_funds_eda2.ipynb` | 관측 근거 (31셀 · ⚠️ 주제2 ⓪셀이 나중에 재실행돼 하위 출력이 stale) |
