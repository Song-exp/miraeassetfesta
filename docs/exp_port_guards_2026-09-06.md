# 펀드 결정층 기법의 ETF·채권 이식 실험 — 2026-09-06

> 브랜치 `exp/port-guards`(af7f386 위 4커밋) · 서버 호출 0회 · 로컬 재생 세트 `eval/exp_port_guards.py` 9케이스 · 전체 회귀 1,229 passed(4커밋 반영 후)
> 목적: 채권·ETF 오답 기록에서 **아직 코드가 없는** 결함을 골라, 펀드 트랙 3일(R1~R14)에서 세운 기법을 옮겨 로컬에서 통과한 것만 남긴다.
> 담당 트랙이 아니므로 **도메인 정의를 새로 정하지 않았다** — 조건식은 전부 yaml 선언(inverse_direction · zero_as_missing) 또는 오답 기록의 정답표에서 가져왔다.

## 1. 결과 한 줄

기준선(af7f386) **1/9 → 8/9**. 남은 1은 채권 #84 P2(하이일드 = 투기등급 확정식) — yaml enforce 슬롯 선언이고 정의(BB+ 이하 vs BBB+ 이하)가 리드 결정 대기라 손대지 않았다.

## 2. 재생 방법

```
cd <worktree>
PYTHONPATH=. DB_PATH=<db> python eval/exp_port_guards.py        # 전체 · 접두(B/E)로 필터
```

오답 기록의 HCX 원문 모양을 가짜 플래너(`plan_sql` 이 원문을 그대로 반환)로 넣고, 가드 체인을 지난 SQL·실행 행·답변을 검사한다.
케이스마다 검사 여러 개, 하나라도 ❌면 그 케이스는 FAIL. 원문은 `ETF_오답_마스터기록.md` §4·`채권_오답기록 §2-24` 에서 옮겼다.

## 3. 커밋 — 무엇을 어떤 기법으로

| 커밋 | 대상 오답 | 펀드 기법 | 넣은 것 | 재생 |
| :-- | :-- | :-- | :-- | :-- |
| `7a16615` | 채권 #84 P1 (서수 `ORDER BY 3` 이 가드 넷 우회) | U14 "정렬축은 이름으로 보인다" | `resolve_ordinal_order_by` — 채권 사슬 맨 앞, 별칭>식, 범위 밖·UNION·서브쿼리 불개입 | B84·B59 3형 ✅ · 2차키·추천제외·머리줄 발동 |
| `9aa7413` | ETF E5·E25 (기본모수 가드가 사용자 조건 삭제 → `WHERE  LIMIT` 거절 · 전체 1,160) | "접지 결과가 곧 질문의 조건" · 가드는 넓히는 방향으로 실패 | `_col_asked` 에 yaml korean_name 병합 · `_literal_grounded`(KG 별칭 리터럴) · 빈 WHERE 금지 | E5 조건 보존 · E25 `cu_lev_fector>1` 보존 · U8 정답 유지 |
| `240cfd8` | ETF #5 배수 환각 · #7 추적오차 0 · #25 부정조건 (📄 규칙만) | 축 낱말 → 확정식 주입(ensure_etf_axis_filter 표 확장) | 인버스(국내 상품명 / 해외 컬럼) · `NOT (…)` 부정 · `ABS(cu_lev_fector)=N` · `du_chas_errt>0`(정렬 컬럼일 때만) · JOIN 문장 불개입 | 62 · 57 · 1위 양수 ✅ |
| `808049a` | ETF #13 괴리율 부호 미고지 (조립 🟡) | DOM-08·T13 "숫자 옆 한 마디는 코드가" | `ensure_etf_diff_sign_note` — du_diff_rt 쓸 때만, 이미 말했으면 침묵 | E13 ✅ |

각 커밋에 회귀 테스트 파일(`tests/test_exp_*.py`)이 딸려 있다. 정답 케이스 불변(멱등)을 첫 테스트로 박았다.

## 4. 채택·롤백 절차

- 채택: `git cherry-pick <커밋>` 을 main 에서 커밋 단위로. 넷은 서로 독립(같은 파일이지만 다른 함수).
- 롤백: 체리픽 전이면 브랜치를 두면 끝. 체리픽 뒤면 `git revert <커밋>`.
- 서버 검증은 아직 없다 — `--code-only` 배포 뒤 재생 세트의 9문항을 서버에 그대로 던져 대조하면 된다(질문 문장은 `eval/exp_port_guards.py` CASES).

## 5. 남긴 것 · 주의

| 항목 | 내용 |
| :-- | :-- |
| 채권 #84 P2~P5 | 투기등급 확정식(yaml `투기등급.enforce`)·우회 어휘 통합·머리줄 정의·C0 주의 — 서현 계획표대로, 정의는 리드 결정 |
| 서수 정규화 범위 | domestic_bonds 만. 펀드는 `_fund_sort_target` 이 서수를 자체 처리하고 위치로 헤더를 찾는 가드(FND-005)가 있어 건드리면 회귀 위험. ETF 로 넓힐지는 이병철 판단(#12 서수가 배수를 가리킨 사고가 같은 부류) |
| 인버스 국내 판정 | 컬럼이 없어 상품명 '인버스' 키워드 — yaml derivation_rules 선언과 같다. 72 중 10건 |
| 기본모수 '날조 술어 제거' | 판별을 넓혔지만 여전히 삭제 가드다. 재발하면 "0·NULL·placeholder 비교만 지운다" 로 좁히는 것이 다음 처방 |
| 재생의 한계 | 원문 모양은 기록에서 옮긴 것 — 서버 HCX 가 다른 모양을 내면 다시 열린다(펀드 교훈: 원문 형태별로 닫는다) |
