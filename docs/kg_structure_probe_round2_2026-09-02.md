# 온톨로지·KG 구조 검증 35문항 — 2라운드 서버 실측(6bad723) 채점 — 2026-09-02

> 채점자: 에이전트 B(도메인·온톨로지 심사). 코드는 손대지 않았다.
> 대상: `eval/probe_recheck_2026-09-02_r4.json` 중 KG-001~035(배포 6bad723) ↔ 기준선 `eval/probe_kg_structure_2026-09-02.json`(31e72ef) ↔ gold `eval/kg_structure_questions.jsonl`.
> 기준: `docs/domain/public_funds.md`(최우선) · 설계 `kg_structure_probe_design_2026-09-02.md` · 1R `kg_structure_probe_round1_2026-09-02.md`(구조 큐 S1~S9 · 런타임 큐 R1~R16 · 0행 정책).
> 실측: 재빌드 DB `data/financial_products.db`(kg_node 41,580 · kg_alias 66,592 · kg_edge 7,414 · kg_closure 9,965) · `git show 6bad723:` 의 ttl 5·yaml·`src/runtime/{pipeline,guard,gate,loader}.py` 읽기만. 판정 JSON: `eval/verdicts_kg_2026-09-02.json`(`"2라운드(6bad723)"` 키 추가).
> A 가 반영했다고 한 것: 구조 S1·S3·S4·S5·S6 + 런타임 R1·R2·R3·R5·R10·R11·R12·R16. 미착수: S2·S7·S8·S9·R4·R6~R9·R13~R15.

**집계: ✅ 17 · 🟡 6 · ❌ 12** (기준선 ✅ 5 · 🟡 7 · ❌ 23).
기준선 대비 — 개선 16(❌→✅ 9 · 🟡→✅ 4 · ❌→🟡 3) · 유지 18(✅ 4 · 🟡 3 · ❌ 11) · **회귀 1(KG-024 ✅→❌)**.
❌ 유지 11건 안에서도 해악의 형태가 바뀌었다 — 좋아진 쪽 3(025 거짓→거절 · 018 날조→유보 · 002 오류→답변) / **나빠진 쪽 2(026 거절→거짓 단언 · 028 오류→환각)** / 동일 6.

설계 예측 대조 — 게이트 3건(013·014·027) **3/3 즉답** · KG 단독 판별 6건(001·004·009·026·029·031) 4✅ 2❌(026·031 은 미착수 S7·S8/R6 자리) · closure 6건(012·019~024) 4✅ 1🟡 **1❌(024 회귀)**.

---

## ① 판정 표 (35행, 기준선 → 2R)

결정층 마커: `ABSENT` = enums absent_properties→ttl→게이트 어휘 · `range` = risk_grade.yaml range_by_table→게이트 · `slot` = Organization 라벨 슬롯(label_official/former_names/successor/provenance)+정규화 키 · `Country`/`FundAttr` = token alias Ground · `scope` = closure_as_descendant · `precheck` = `_sql_precheck`(코드 리터럴 실존·템플릿 잔재·TOP·라우팅 밖 테이블) · `R1` = 0행 정책 문구.

| qid | 축 | 기준선 | 2R | 변화 | 결정층 마커 | 요지 (gold ↔ 2R 실측) | 원인 층(잔여) |
| :-- | :-- | :-: | :-: | :-: | :-- | :-- | :-- |
| KG-001 | A 개체 | ❌ | ✅ | 개선 | slot(label_en 'Mirae Asset Global Investments Co., Ltd.' + `_mgr_brand_en` 'Mirae Asset') · derived 'Asset' 키 제외 | 823/2,066 + "역외 5개(00130006)는 별도 법인" 병기. gold 완전 일치 | — |
| KG-002 | A | ❌ | ❌ | 형태 변화(오류→오답) | slot(former_names → `IN ('00040022','00040007')` + Ground 주석 "현재 우리자산운용이 운용") · 별칭-함수 접두 버그 해소(f6485ab) | 결정층은 옳게 내려갔다(코드 2건·27행 조회·판매중클래스수 열 병기). HCX 가 ① Ground 주석을 버리고 **"프랭클린템플턴이 운용하며 판매중"** 으로 서술(must '우리자산운용' 0회) ② 판매중클래스수 0 인 판매완료 13펀드를 '판매중' 으로 나열. 실측: 판매중은 00040007 21클래스뿐 | 조립(개별조회 경로 HCX 전사 — 기계 조립 미적용) |
| KG-003 | A | ❌ | ✅ | 개선 | slot(former_names '메리츠자산운용' → Org_00040087 후계 조회) | "케이씨지아이자산운용 27개(133) · '메리츠자산운용' 은 구상호 — 현재 KCGI 기준" — 날조 코드 `'A011'` 소멸. gold 의 '없' 토큰 대신 구상호 안내(허용된 KCGI 추정)이며 도메인 사실(2023 KCGI 개명)에 부합. 기계 주석 말투("조회한다") 노출은 🟢 | — |
| KG-004 | A | ❌ 거짓0 | ✅ | 개선 | slot(`_flex_match` 공백 삽입 정규화) + 제2코드 합산 고지 | 143/541 + "운용사 코드 2건(00040024·00040105) 합산". gold 완전 일치 | — |
| KG-005 | A | ❌ | 🟡 | 개선 | 공백무시 접두 LIKE + 펀드단위 집계 | 217/906 정확(1문). 그러나 **2문(삼성자산운용 207/850 · 삼성액티브 10/56)이 통째로 빠짐** — Ground 는 Org_00040010 을 잡았는데 HCX SQL 이 접두 COUNT 1개만 내고 기계 조립기가 1행을 그대로 옮김 | 플래너(2집계→1집계) → 조립(1행 완결 판정) |
| KG-006 | B 관계 | ❌ 오거절 | ❌ | 유지 | (S2 미착수) | 1차 `mtco_nm·trusc_nm` 환각 → 재생성 `mgmt_co_nm + trusc_xtn_itt_cd` 는 실행 가능했는데 **`ambiguous_columns` 가 서브쿼리 스코프를 못 보고 itm_no 를 모호로 기각**(외부 FROM ext_fund_page 단일 — 실제로는 모호하지 않음) → 거절. 통과했어도 수탁사는 코드 '00020088' 만 나옴 | 온톨로지(S2 ref_organization 부재) + 가드(모호 검사 과잉) |
| KG-007 | B | ✅ | ✅ | 유지 | — | 823/2,066 + 역외 5 병기. inverseOf 는 여전히 문서 장식(트리거 무관하게 KG 매핑으로 수렴) | — |
| KG-008 | B | ❌ 오거절 | ❌ | 유지 | (S2 미착수) | `trusc_itt_nm_ko` 환각 2회 → 거절. 수탁사 이름이 SQL 층에 없는 구조 결함 그대로 | 온톨로지(S2) |
| KG-009 | B | ❌ 반전 | ✅ | 개선 | 목록 답변 기계 조립(HCX 0회) + TRIM '0016022' | "전체 2개(14클래스) — iM에셋 타이거 포커스 1,456억·6클래스 / 머스트원앤온리 968억·8클래스". 반전 서술 소멸 | — |
| KG-010 | B | ❌ | ❌ | 유지 | (S8/R15 미착수) | '코어테크' 부분일치 41행(형제 6펀드) → 조회 30행에 정답 '미래에셋코어테크증권모투자신탁(주식)' 이 **10회 실려 있는데도** HCX 가 형제 모펀드 2개를 답하고 정답 누락 + '재간접형' 노출(must_not). feedsInto edge 1,704 미사용 | 가드(stem 완전일치 우선 부재) → 조립(HCX 선택) |
| KG-011 | B | 🟡 | ✅ | 개선 | R1(a) "각 조건의 개체·값은 데이터에 실재하며 그 교집합이 0" · 머리줄 두 조건 반영 | "KB자산운용이 운용하고 국민은행이 수탁하는 공모펀드는 0개" — 거짓 머리말 해소. 국민은행 제2코드 00160005·실제 수탁 분포(씨티 170…) 미병기는 만점 요건(가점)이라 ✅ | (R8 이웃 분포 병기) |
| KG-012 | C 분류 | ❌ 거짓0 | 🟡 | 개선 | Country_CHN Ground → R10 '유형' 어휘 → `zrin_ptn_nm='중국주식'` 치환 · 템플릿 잔재 precheck | 205/522 정확. 머리줄 "조회 조건에 해당하는" 뿐 — gold 가 요구한 "유형 기준" 고지·'중국주식' 토큰 없음(must 미충족). 값은 결정층에서 닫힘 | 조립(R8 축 고지) |
| KG-013 | C | 🟡 | ✅ | 개선 | range(public_funds 1~6) → 게이트 즉답 HCX 0회 | "1(매우 높은 위험)~6(매우 낮은 위험) … 7등급 없음 (0등급 없음 — 미수록은 NULL)". 방향 안내까지 gold 가점 충족 | — |
| KG-014 | C | 🟡 | ✅ | 개선 | range → 게이트 기각(0 은 정의역 밖) | "0등급은 없다 · 미수록 클래스는 NULL(0 아님)" — SQL 0회. NULL 422 오독 위험 원천 차단 | — |
| KG-015 | C | ❌ | ✅ | 개선 | slot(`_ALIAS_KEY_ENTITIES` RiskGrade 이름형 alias raw 승격) → `IN ('높은위험','높은 위험')` | 995/2,994 정확. '2등급' 코드 병기는 없음(R8) | — |
| KG-016 | C | 🟡 | 🟡 | 유지 | — | 389/933(약관 `or_attr_desc`) 정확 · 기준 미고지·제로인 240/534 미병기. '혼합형' 오치환 없음 | 조립(R8) |
| KG-017 | C | ❌ 거짓0 | ✅ | 개선 | FundAttr_C104 token Ground + R11 속성 태그 확정식(han_clas_policies 절 제거) | 3/6 정확. 잔여 `prfd_attr_cds LIKE '%C104%'` 절은 canon 과 중복이라 무해. 태그 미보유 418행 '확인 불가' 병기 없음(가점) | — |
| KG-018 | C | ❌ 날조 | ❌ | 형태 변화(날조→거짓 유보) | R11 canon(C102∧C103) 주입 ✓ · R1(c) | canon 은 들어갔으나 HCX 가 덧붙인 **`itm_abrv_nm LIKE '%단위%' AND LIKE '%개방%'`** 를 확정식이 못 걷어냄('단위형/개방형' 통칭 낱말로만 절 제거 — '단위'·'개방' 어근 불일치) + 기본모수(sale_yn) 미주입 → 0행 → **"조건 중 일부는 … 상품 자체가 없습니다"**(실재 31/189 를 부재로 단정). 도메인 날조("단위형=설정액 조건")는 소멸 | 가드(절 제거 어근·목록 경로 모수) → R1(c) 오진(SQL 결함을 '상품 부재' 로) |
| KG-019 | D 국가 | 🟡 | ✅ | 개선 | Country_JPN token + 목록 기계 조립(30행 전량·총량 머리줄) | "전체 31개(79클래스)" + 피델리티재팬 239억 1위 ✓. 이름 보조(38/103)는 태그 1행+ 이면 미병기 규칙(3R C-2) — gold 허용. 같은 이름 펀드가 펀드키로 4~6행 갈려 보이는 건 약점 #1 노출(🟢) | — |
| KG-020 | D | ✅ | ✅ | 유지 | Country_USA token | 98/333 | — |
| KG-021 | D | ❌ 오거절 | ✅ | 개선 | Country_TWN token + 희소 판정(기본모수 0행) → 이름 폴백 OR | "피델리티대만펀드 1클래스, 판매중". 설립국 컬럼 오용 소멸. '태그 0·이름 기준' 고지 없음(가점) | — |
| KG-022 | D | 🟡 | 🟡 | 유지 | Region_Europe (+후손 18, public_funds alias 0 → 무해) | 61/193 정확 · 기준 미고지 · 확장(태그 W114~W118∪이름 91/257) 미병기 | 조립(R8) |
| KG-023 | D | ❌ | ✅ | 개선 | scope(Region_Korea closure 제외 — Ground "[+후손 12]", '국내' 없음) + 펀드 대표행 GROUP BY | KB중국본토A주 1,453 · 차이나솔로몬1호 1,393 · 한투베트남그로스 1,066 · 신한중국의꿈2호 1,038 · 다이와넥스트AP 1호 668 — gold 5/5. 상품명 전사 교정 가드까지 발동('다이내믹'→'다이와넥스트') | — |
| KG-024 | D | ✅ | ❌ | **회귀** | FundAttr_N144 token Ground (신규) | **10/50 (gold 12/78)**. Ground 가 '반도체' 를 FundAttr_N144 로 잡자 ① HCX 가 wrap 없는 `prfd_attr_cds LIKE '%,N144,%'` 를 써 첫 토큰 행 14클래스 누락(64→50) ② 기준선이 쓰던 규칙 '태그 ∪ 이름'(이름만 있는 2펀드 14클래스)이 KG 매핑에 밀려 소실. 속성 태그 확정식은 C축 통칭(개방형…)에만 걸려 N축은 canon 미주입 | 가드(token canon 전 축 미적용) + 규칙(테마 축 이름 병기 소실) |
| KG-025 | E 교차 | ❌ 거짓 | ❌ | 형태 변화(거짓→오거절) | precheck(코드 리터럴 실존: `IN ('삼성','삼성KODEX')` 기각 2회) | "'미수록'/1,534" 거짓은 소멸 — R3 가 잡았다. 그러나 재생성도 같은 오류 → 거절. Ground 라인이 펀드 코드와 ETF alias 문자열을 한 줄에 실어 HCX 가 컬럼을 섞는 구조 그대로(R4 미착수). 'ETF' 낱말이 FundAttr_M113 으로 그라운딩되는 오염 신규 | 가드(R4 교차 확정식) + Ground(FundAttr 라벨 충돌) |
| KG-026 | E | ❌ 오거절 | ❌ | **형태 악화(거절→거짓 단언)** | (S7 미착수) · 교차 경로 기본모수 가드·R1 미적용 | **"펀드 2,888개 · ETF 는 수록되지 않았다"** — ① 펀드: `bmrk_nm LIKE '%KOSPI200%'` 에 sale_yn 없음·클래스 수(기본모수 815펀드/2,217클래스, 정확일치 421/1,266) ② ETF: `cu_base_index='KOSPI200' AND pd_sale_yn=1` → 0 (9건 전부 pd_sale_yn=0; 실제 추종은 ref_base_index 'KOSPI 200 CR' 25 등 50). UNION 분기 0 을 "수록되지 않음" 으로 단언 — **신규 거짓 0개**. Ground 가 auto 노드 `Idx_a_fa21f0fb15` 를 curated `Idx_KOSPI200` 의 조상으로 매핑(기준선과 동일, 잠재→발현) | KG(S7 지수 alias) + 가드(UNION 분기별 모수·0행 정책) |
| KG-027 | E | ❌❌ 환각 | ✅ | 개선 | ABSENT(hasUnitsOutstanding · vocab `좌[ ]?수`) → 게이트 HCX 0회 | "좌수 컬럼이 없어 답할 수 없다 · 순자산(fd_nast_suma)·기준가(bns_bpr 스냅샷)로 안내 가능". '10좌' 환각 소멸. must_not 준수 | — |
| KG-028 | E | ❌ 오거절 | ❌ | **형태 악화(오류→환각)** | precheck 라우팅 밖 테이블 검사 **미발동**(교차 플래그) | **"IBK K-AI반도체코어테크 ETF 57.12%"** — 펀드 질의인데 `domestic_etfs JOIN ext_etf_holdings WHERE pd_nm LIKE '%코어테크%' ORDER BY quantity` 가 통과. `_sql_precheck` 의 R3-④ 는 `not cross` 조건이라 게이트가 '구성종목' 을 교차로 표시하면 모든 마스터가 열린다. 상품 고유명 '코어테크' 가 라우팅 테이블의 이름 컬럼에 핀되지 않음. gold 삼성전자 24.95%(ext_fund_holdings 실재) | 가드(교차 시 테이블 범위) + Ground(상품 고유명 핀) |
| KG-029 | F 키 | ✅ | ✅ | 유지 | — | 88 (rptt). 현행키 235·클래스 403 병기 없음 | — |
| KG-030 | F | ❌ | 🟡 | 개선 | 미특정 라우팅 → 사후 보정 → 이름 검색 17행 정확 | "3호까지" ✓ · 1호 2 ✓ · **2호 5(gold 7)·3호 5(gold 8)·합 12(gold 17)** — 17행을 받고 HCX 가 세다 틀림. '5호' 날조·정책 컬럼 필터 소멸 | 조립(LLM 산술 — R14 호수별 GROUP BY 확정식 미착수) |
| KG-031 | F | ❌ | ❌ | 유지(+모순 문장) | precheck 는 `00080030`(실존 코드) 통과 · `_POP_WIDEN` '역외' 로 sale_yn 미주입 · `_offshore_sibling_note` | 167/350 동일(JP모간 판매완료 56행 포함·판매중 조건 없음) + **"역외펀드 47개(00130001)는 별도 법인이라 포함하지 않았다"** — 질문이 '역외까지 포함하면' 인데 가드가 반대로 병기. 47 은 알면서 안 더함. gold 153/293 | 가드(R6: 역외=코드 집합 확장 + Ground 밖 코드 리터럴 기각) |
| KG-032 | G 시간 | ✅ | ✅ | 유지 | 기준일 이후 시점 게이트 | "기준일 2026-08-22 · 이후 시점 확인 불가" | — |
| KG-033 | G | 🟡 | 🟡 | 유지 | ext JOIN(설명서 트리거) | KB K-성장과 지배구조 30 2026-06-12 ✓ · LIMIT 1 → 동률 미래에셋국민참여형 누락 · 커버리지 미병기 | 플래너·조립(R9 동률 전부) |
| KG-034 | G | ❌ 오거절 | ❌ | 형태 변화(거절→거짓 유보) | R1(c) | HCX 가 Ground 상품 고유명 '코어테크' 를 **'코어텍'** 으로 옮겨 적음 → 0행 → "조건 중 일부는 … 상품 자체가 없습니다". SQL 에 설정일(estb_dt)조차 없음('운용한 지' 트리거 부재 — R9). 실측 2019-10-23(8클래스) → 6년 10개월 | 가드(이름 리터럴↔Ground 핀 부재) + 라우터(R9) + R1(c) 오진 |
| KG-035 | G | ❌ | ❌ | 유지 | 외부 테이블 JOIN 주입(estb_dt → LEFT JOIN) 신규 발동 | "124개"(클래스) — JOIN 경로엔 펀드단위 가드 미적용(R7). JOIN 주입 가드는 옳게 붙었다 | 가드(R7) |

---

## ② 감점 위험 5건 해소 여부 + 거짓 0개 계열의 0행 정책 문구 평가

### 5건(027·004·012·017·025)

| qid | 1R 유형 | 2R | 닫은 결정층 | 판정 |
| :-- | :-- | :-- | :-- | :-: |
| KG-027 | 환각 "10좌" | 게이트 즉답, HCX 0회 | S5 ABSENT 선언 → vocab 정규식 → gate ①-0 | **닫힘** |
| KG-004 | 거짓 0개(143) | 143/541 + 코드 2건 합산 고지 | S1 `_flex_match`(공백 삽입 정규화) + Ground 코드 집합 | **닫힘** |
| KG-012 | 거짓 0개(205) | 205/522 | precheck 템플릿 잔재 + R10 '유형' → zrin_ptn_nm | **닫힘**(값) — 축 고지만 R8 |
| KG-017 | 거짓 0개(3) | 3/6 | S3 FundAttr_C104 token + R11 확정식(타 컬럼 절 제거) | **닫힘** |
| KG-025 | 거짓 미수록 + 과대 1,534 | 오거절 | R3 코드 리터럴 실존 검사(`'삼성'` 기각) — 거짓은 소멸, 정답은 R4 자리 | **반만 닫힘**(거짓→거절) |

4/5 닫힘, 025 는 감점 등급만 낮아짐(거짓 단언 → 오거절). **대신 같은 등급의 신규 2건이 열렸다** — KG-028(엉뚱한 ETF 종목 환각)·KG-026(2,888 무모수 + "ETF 수록되지 않음" 거짓 0). 둘 다 **교차 플래그 경로**에서 났다: 교차로 판정되면 (i) 라우팅 밖 테이블 검사가 꺼지고(028) (ii) 기본모수·0행 정책이 UNION 분기에 안 붙는다(026). 감점 상위는 이제 028 > 026 > 018/034(거짓 유보) > 002(운용사 오귀속) > 031(모순 병기) 순.

### 거짓 0개 계열(003·011·014) 의 0행 정책 발동 문구

| qid | 실체(1R 분류) | 2R 문구 | 도메인·채점 평가 |
| :-- | :-- | :-- | :-- |
| KG-003 | (b) 조건 식별 실패 → 실은 구상호 | "케이씨지아이자산운용 27개(133) · '메리츠자산운용' 은 구상호 — 현재 KCGI 기준" | **적절 — 단언이 맞다.** 구상호는 식별 실패가 아니라 해소된 개체라 유보할 이유가 없다. 문장 하나만 보강: "메리츠자산운용 명의로 운용 중인 펀드는 없다" 를 앞에 두면 gold 형식('없')과 되묻기 요건을 동시에 만족 |
| KG-011 | (a) 진짜 0 | "…0개(클래스 0개) (각 조건의 개체·값은 데이터에 실재하며 그 교집합이 0입니다)" | **적절 — 단언.** 전제(WHERE 코드 리터럴이 precheck 로 실존 확인)가 코드 안에 명시돼 있어 R1 규칙 그대로. 이웃 분포(씨티 170·HSBC 120·신한 116) 병기가 없어 "왜 0 인지" 는 못 보여준다 — R8 로 이월 |
| KG-014 | 정의역 밖 | "0등급은 없다 · 미수록은 NULL(0 아님)" | **적절 — 게이트가 0행 이전에 끊었다.** 1R 의 "게이트가 먼저 잡아야" 요구 충족 |

**문구 규칙의 구멍(신규)** — 목록·개별조회 경로의 0행은 `guard.diagnose_zero_rows().user_text()` 가 붙이는데, 단독 조회 0 인 절이 있으면 **"수록된 데이터에는 …인 상품 자체가 없습니다"/"조건 중 일부는 … 상품 자체가 없습니다"** 로 나간다(KG-018·034). 이건 유보가 아니라 **존재 부정 단언**이고, 두 사례 모두 SQL 결함(c)(HCX 오타 '코어텍' · itm_abrv_nm 이름절 날조)이라 거짓이다. R1 의 (b)/(c) 분기 — "리터럴이 값사전·KG alias·Ground(상품 고유명 포함) 밖이면 그 리터럴을 지목해 '식별하지 못했다' 로 유보" — 가 개수 경로(`_count_answer`)에만 들어가고 목록 경로엔 안 들어갔다. 일반 규칙 재진술: **0행 사유 문장은 "죽은 절의 리터럴이 검증 집합 안" 일 때만 '없다' 를 쓰고, 밖이면 '질문의 「코어텍」을 데이터 표기로 식별하지 못했다(가까운 표기: 코어테크)' 로 쓴다.** `_suggest_similar_products` 가 이미 후보를 찾는 자리이므로 층은 같다.

---

## ③ 온톨로지 정합성 검토 — S1·S3·S4·S5·S6 실물 대조 · 4도메인 부작용

### 실물 요약(재빌드 DB ↔ 6bad723 ttl/yaml)

| 큐 | yaml 선언 | ttl 사영 | kg_* 실물 | 설계 의도 대비 |
| :-- | :-- | :-- | :-- | :-- |
| S1 | `organization.yaml` former_names 4노드(신한·우리·iM에셋·KCGI) · successor 1(프랭클린→우리) | `fp:formerName`(AnnotationProperty) 5 triple · `fp:successor`(ObjectProperty, domain/range Organization) 1 triple | kg_node 9열(label_official 115 · former_names 4 · successor 1 · provenance curated 64/auto 2,308/derived 155) · label_en 영문 복원(Organization 410) | **의도대로**. 슬롯 4종 중 `label_ko_short`(브랜드)는 기존 label_ko 가 맡음. 미비: 알리안츠(00040023, label_official 우리글로벌자산운용)·키움슈로더(00040013 ↔ 00080052 동일 법인)·도이치→DWS 는 yaml 주석에만 — former_names/sameAs 미반영 |
| S3 | `fund_country_auto.yaml` Country 17(flat, `tag_sparse`) · `fund_attribute_auto.yaml` FundAttr 179(코드북 210 중 status·판매중 0행 31 제외) · `match: token` · enums `synonyms`(차이나→중국 · 개방형→개방 …) | `fp:Country`/`fp:investsInCountry` · `fp:FundAttribute`/`fp:hasAttribute`(domain PublicFund) · 개체 196 prefLabel | kg_alias `match_kind='token'` 196(정확히 +196) · closure 0 · edge 0 | **의도대로**(token alias 유형 신설 ✓ · 하드코딩 `_COUNTRY_TAGS` 제거 ✓ — `_country_tag_map` 이 Country 노드+synonyms 에서 생성). 이탈: 국가 flat(아래 판정) · 유형 축 `FundType_*` 노드는 미생성(R10 이 zrin_ptn_nm 값사전으로 대체 — 결정층엔 충분) |
| S4 | `region.yaml Region_Korea: closure_as_descendant: false` | 사영 없음(주석 속성 `excludedFromRegionalQuery` 미출력) | kg_closure 9,967→9,965 (Region_Asia→Korea · Region_Global→Korea 2행 제거) · Region_Korea 조상 0 · alias 4종 유지 | **의도대로**(별도 scope 컬럼 대신 빌드 시 제외 — 런타임 비용 0, 단 "국내 포함" 명시 시 포함하는 분기는 없음). ttl 에 근거 주석이 안 남는 건 제출물 품질(🟢) |
| S5 | `enums/public_funds.yaml absent_properties` 4(hasUnitsOutstanding·hasFundManager·hasNavHistory·hasHoldingsHistory, vocab 정규식·substitute) · ETF 2 · 채권 1 | `# ABSENT:` + `rdfs:comment` 4+1(기존 hasCreditGrade) / ETF 3 / 채권 4 | loader `absent_props` → gate ①-0 | **의도대로**("선언 = 게이트 어휘"). 도메인 §5 표 대비 누락: '운용 전략 서술'(어휘 미선언) — 보수·설정일·환매조건은 ext 에 있어 제외 타당. 게이트는 `len(tables)==1` 일 때만 — 교차·미특정 라우팅에선 미발동(잠재) |
| S6 | `risk_grade.yaml range_by_table` 3테이블(펀드·ETF 1~6, 채권 0~6 unclassified 0) | `fp:riskGradeValue_PublicFund ⊂ riskGradeValue` + `owl:withRestrictions [1,6]`(ETF 동일, 채권 [0,6]) | loader `grade_ranges` → gate ② 테이블별 | **의도대로**. 상위 `fp:riskGradeValue` 0~6 union domain 은 유지 + 서브속성으로 좁힘 — OWL 상 정합 |

### 4도메인 부작용 실측

- **alias·edge 보존**: kg_alias 증분 +196 = token 신설분과 정확히 일치 → 채권 1,841·국내ETF 1,114·해외ETF 2,296·ext 3종 eq alias 수 불변. kg_edge 7,414 불변. closure −2 는 Korea 2행뿐(CreditGrade·Index closure 손실 0).
- **Ground 누출**: 4R 84문항 중 KG 밖 49문항의 think_trace 에서 `Country_`/`FundAttr_` 매핑은 **펀드 질의 14건에서만** 발생(R3·S4·S6·S7·T4·T5·T10·T13·T14·V8·V9·V10·V14·V15) — ETF·채권 라우팅으로의 누출 0. 채권 Org_issuer 자동 노드는 4자 하한·derived 제외 규칙의 영향권 밖(provenance auto)이라 키 변화 없음.
- **잠재 충돌 2종(미실측)**: ① Country 라벨 16쌍이 Region 라벨과 동음(중국: Country_CHN ↔ Region_China …). 우선순위가 `_in_target` 뿐이라 **펀드+ETF 교차 질의**에서 '중국' 이 Country_CHN 에 소비되면 ETF 쪽 Region_China(wu_inv_rgn) 필터가 사라질 수 있다. ② FundAttribute 라벨이 권역·자산군·상품군 명사와 충돌 — V101 '국내'·W101 '글로벌'·W104 '북미'·W108 '아시아'·N114 '원자재'·Q107 '부동산'·**M113 'ETF'**. 이번에 KG-025·026 의 Ground 라인에 `'ETF' → FundAttr_M113` 이 실렸다(결정엔 무해했으나 교차 질의 프롬프트 오염). 같은 라벨 노드 3쌍(럭셔리 N118/N147 · 인프라 N116/N155 · 친환경 N119/N168)은 Ground 가 하나만 잡아 반쪽 조회가 된다.

### A 가 설계와 다르게 한 3가지 — 판정

| 결정 | 판정 | 근거 |
| :-- | :-: | :-- |
| **국가 노드 flat**(Region 자식 아님) | **타당** | `fd_ivst_rgn_desc`(7값 권역 축)와 `prfd_attr_cds` 국가 토큰은 다른 컬럼·다른 축이다. Country 를 Region 자식으로 두면 '아시아' 질의가 `fd_ivst_rgn_desc='아시아' OR CHN OR JPN …` 로 커져 지역 컬럼 모수(947)와 태그 모수가 섞이고, KG-023 이 닫힌 이유(권역 = 지역 컬럼 하나)가 다시 무너진다. 대가는 위 동음 16쌍 — Country 노드에 `region: Region_*` 주석 속성(skos:related)을 두어 교차 질의에서 양쪽 alias 를 함께 싣는 것으로 보완하면 ETF 형제(U13) 도 같은 노드로 풀린다 |
| **Organization alias raw 승격 제외** | **타당** | Org_00080008 의 domestic_etfs alias 43종 중 40종이 상품명 오염 raw('미래에셋TIGER200IT증권상장지수투자신탁…')이고 'TIGER'·'KODEX' 같은 브랜드도 있다. 승격하면 상품명 질의가 운용사로 빨린다(FND-016 계열 재발). 영문 요구(KG-001)는 `label_en` 슬롯 + `_mgr_brand_en` 접미 절단으로 충족됐다. 남는 건 오염 raw 자체의 정리(`_PRODUCT_NAME_RAW` 로 표시만) — S1 의 `provenance=derived_product_name` 강등은 빌더에서 안 됐고 런타임 필터로만 산다(🟢) |
| **희소 태그 판정 = 기본모수 0행**(런타임 DB 실측 `_sparse_country_tags`) | **타당하나 경계 1건** | 태그가 1행이라도 있으면 태그가 축(이름절은 통화 표기·역외 무태그 행을 끌어온다 — 3R C-2 실측)이라는 규칙은 도메인적으로 옳다(태그 = 제로인 검증값, 이름 = 마케팅 표기). 대만·호주·태국은 0행이라 이름 폴백이 붙어 KG-021 이 닫혔다. 경계: 태그 1~2행짜리 국가(스페인·프랑스 등)는 이름에만 있는 역외 펀드가 빠진다 — "희소 = n행 미만" 이 아니라 "이름-only 행이 태그 행보다 많으면 병기" 로 두면 임계값 없이 일반화된다. yaml 의 `tag_sparse` 필드는 빌드 시점 값이고 런타임은 DB 를 다시 세므로 두 원천이 어긋나면 yaml 이 죽은 필드가 된다(🟢 — 한쪽으로 통일) |

---

## ④ 이월·신규 큐 — 우선순위 재정렬 (일반 규칙 · 층 · 의존)

이번 실측으로 **실제 결함이 드러난** 미착수 큐를 앞에, 신규를 N 으로 붙였다. 특정 문항은 검증 사례일 뿐이다.

| # | 우선 | 항목 | 층 | 일반 규칙 | 드러난 문항 | 의존 |
| :-- | :-: | :-- | :-- | :-- | :-- | :-- |
| N1 | 🔴 | **교차 플래그 시 테이블 범위** | 가드 `_sql_precheck` | 교차 판정이어도 허용 집합은 **라우터가 정한 마스터 + 그 짝 ext_*** 이며, 다른 마스터는 질문에 그 상품군 명사가 있을 때만 추가한다. `not cross` 로 검사를 끄지 않는다 | 028(펀드 질의에 domestic_etfs+ext_etf_holdings 통과 → 엉뚱한 ETF 환각) | 없음 — 즉시 |
| N2 | 🔴 | **상품 고유명 리터럴 핀** | 가드(R3 확장) | Ground 가 상품 고유명 토큰을 실었으면 SQL 의 이름 리터럴(itm_nm/pd_nm LIKE)은 그 토큰을 포함해야 하고 **라우팅 테이블의 이름 컬럼**에 붙어야 한다. 불일치 → 토큰으로 치환(1회) 후 재검 | 034('코어텍' 오타 → 거짓 유보) · 028(pd_nm 에 붙음) | N1 과 같은 자리 |
| N3 | 🔴 | **교차 UNION 분기별 모수·0행 정책** | 가드·조립 | 교차 집계 SQL 의 각 분기(UNION 원소)에 상품군 기본모수(펀드 판매중·공모 / ETF pd_sale_yn=1)와 펀드단위 집계를 따로 주입하고, 분기 결과 0 은 R1 과 같은 검증(리터럴이 값사전 안이라도 **모수 교집합 0** 이면 "판매중 0 · 전체 9" 로 병기)을 거친다 | 026(2,888 무모수 · ETF "수록되지 않음") | S7 과 병행 |
| S7 | 🔴↑ | 지수 alias 를 ETF 정본 컬럼에 | KG(`index.yaml`) + 빌더 V-검증 | 수동 지수 노드는 각 테이블 정본 컬럼(ETF `ref_base_index`)에 alias ≥1 · auto 노드(`Idx_a_fa21f0fb15`)가 curated 노드의 조상이 되는 역전은 빌더가 기각 | 026(cu_base_index 9건 전부 pd_sale_yn=0) | 없음 |
| R4 | 🔴↑ | 운용사 교차 확정식 | 가드 | 상품군별 정본 필터 템플릿을 Ground 가 **컬럼별로 분리해** 싣고(펀드 `or_co='코드'` / ETF `TRIM(cu_fund_mgmt_co)='약칭' AND pd_sale_yn=1`), UNION 조립은 기계 | 025(오거절) | N3 |
| N4 | 🟡 | **token canon 을 전 축으로** | 가드 `ensure_fund_attr_tag` | FundAttribute 노드가 Ground 에 잡히면 축 불문 `','||prfd_attr_cds||',' LIKE '%,code,%'` canon 으로 접고, 축 메타(`fund_attribute_auto.yaml`)에 `name_union: true` 인 축(테마 N·섹터 O)은 `OR REPLACE(itm_nm,' ','') LIKE '%라벨%'` 을 병기한다(국가 희소 폴백과 같은 기계). 같은 라벨 노드(럭셔리·인프라·친환경)는 빌더가 alias 집합을 병합 | **024 회귀**(50 vs 78) | 없음 |
| N5 | 🟡 | 절 제거 어근·목록 경로 모수 | 가드 | 확정식의 "같은 낱말 타 컬럼 절 제거" 기준은 통칭('단위형')이 아니라 노드 라벨 어근('단위')이며, 기본모수 주입은 개수·랭킹뿐 아니라 목록·개별조회 SQL 에도 붙는다 | 018(itm_abrv_nm 절 잔존 + sale_yn 없음) | — |
| R1-c | 🟡 | 0행 문구의 (b)/(c) 분기 | 조립 `diagnose_zero_rows.user_text` | 죽은 절의 리터럴이 검증 집합(값사전·KG alias·Ground 코드·상품 고유명) 밖이면 "…상품 자체가 없다" 금지 → "질문의 「X」를 데이터 표기로 식별하지 못했다(가까운 표기: …)" | 018·034 | N2 |
| N6 | 🟡 | 개별조회 목록의 기계 조립 | 조립 | 이름 검색 결과(대표_itm_no·클래스수·판매중클래스수)는 기계 조립(이미 있는 `목록 답변 기계 조립`)으로 옮기고 Ground 주석(구상호·후계)은 count 경로처럼 기계 병기. 판매중클래스수 0 행은 '판매완료' 로 표기 | 002 | — |
| R6 | 🟡↑ | 역외 = 코드 집합 확장 | 가드·KG(`related_codes`) | '역외 포함' 은 sale_yn 완화가 아니라 0013 계열 코드 추가(S8 related_codes) · Ground 코드 집합 밖 `or_co` 리터럴은 실존해도 기각(00080030 날조) · `_offshore_sibling_note` 는 질문에 '포함' 이 있으면 합산 문장으로 | 031 | S8 |
| N7 | 🟡 | 복수 집계 질문 | 플래너·조립 | 질문에 집계 요구가 2개면(…몇 개고 … 몇 개야) SQL 은 2행(UNION ALL 라벨) 이어야 하고, 기계 조립기가 1행만 받으면 미완 → 재생성 | 005 | — |
| N8 | 🟡 | 서브쿼리 스코프 모호 검사 | 가드 `ambiguous_columns` | `IN (SELECT … FROM 단일테이블)` 내부 컬럼은 외부 스코프와 분리 | 006(정답 직전 오거절) | S2 전이라도 코드만은 나옴 |
| S2 | 🔴 | 역할 분리 + `ref_organization` | ttl·yaml·build_db·enums | 1R 그대로 — 코드 컬럼엔 같은 코드체계의 이름 참조 테이블, 정규화는 빌드 1회 | 006·008 | build_db 조율 |
| R7 | 🟡 | 펀드단위 가드 JOIN·재생성 경로 | 가드 | 1R 그대로 — JOIN 주입 가드(신규 발동 ✓) 뒤에 펀드단위 치환이 이어져야 | 035 | — |
| R8 | 🟡 | 머리줄 축·기준 고지 | 조립 | 1R 그대로 + Ground 개체 전부 반영 + 이웃 분포 | 012·015·016·022·011 | — |
| R9 | 🟡 | 경과·기간 트리거 + julianday | 라우터·가드 | 1R 그대로 | 034·033 | — |
| R14/R15 | 🟡 | 호수별 GROUP BY 확정식 · stem 완전일치 | 가드 | 1R 그대로 | 030(LLM 산술 12≠17)·010 | S8 |
| N9 | 🟢 | FundAttribute 라벨 충돌 | 빌더 V-검증 | 라벨이 Region/AssetClass 라벨 또는 상품군 명사('ETF'·'국내')와 같은 FundAttribute 는 매칭 키에서 제외하고 alias 로만 산다 | 025·026 Ground 오염 | — |
| N10 | 🟢 | Country↔Region 동음 16쌍 | KG(`fund_country_auto.yaml` `region:`) | Country 노드에 대응 Region id 를 두어 교차 질의에서 양쪽 alias 를 함께 싣는다 | U13 형제 위험 | — |
| S9 | 🟢 | 문서·서술 정정 | yaml·ttl | 1R + `tag_sparse` 원천 통일 · ttl 에 closure 제외 주석 · 기계 주석 말투("조회한다") 정리 | 003 | — |

**적용 순서**: N1·N2(같은 함수, 즉시 — 환각 2건) → N3+S7+R4(교차 집계 묶음, KG 재빌드 1회) → N4·N5·R1-c(펀드 태그·0행 묶음) → N6·N7·R6·R8(조립) → S2(스키마) → R7·R9·R14/15 → S9·N9·N10.

---

## ⑤ 형제 검증 질문 (다음 라운드 — `ID<TAB>질문`)

gold 는 DB 실측(판매중·공모, 2026-08-22). 괄호는 시험 규칙.

```
X1	미래에셋코어테크 펀드가 가장 많이 담은 종목 3개 알려줘	(N1/N2 — 삼성전자 24.95·SK하이닉스 15.90·삼성전기 7.96, ext_fund_holdings 2026-06-01)
X2	KB중국본토A주 펀드가 가장 많이 보유한 종목은 뭐야?	(N1 형제 — ETF 테이블 유출 0)
X3	미래에셋 코어택 펀드 순자산 알려줘	(N2/R1-c — 오타 유보 "식별하지 못했다 · 가까운 표기: 코어테크")
X4	친환경 테마 공모펀드는 몇 개야?	(N4 — N119∪N168 병합 ∪ 이름)
X5	2차전지 테마 공모펀드는 몇 개야?	(N4 — N145 canon + 이름 병기)
X6	개방형이면서 추가형인 공모펀드는 몇 개야?	(N5 — C101∧C103, 이름절 미주입)
X7	KOSPI200을 추종하는 국내 ETF는 몇 개야?	(S7 — ref_base_index 50 · 계열 93, 판매중)
X8	S&P500을 벤치마크로 쓰는 공모펀드와 S&P500 추종 국내 ETF는 각각 몇 개야?	(N3+S7 — 분기별 모수·0 분기 검증)
X9	미래에셋자산운용이 운용하는 공모펀드와 국내 ETF는 각각 몇 개야?	(R4 — 823 · 188)
X10	이름이 KB로 시작하는 공모펀드는 몇 개고 그중 KB자산운용이 운용하는 건 몇 개야?	(N7 — 두 집계 2행)
X11	동양자산운용이 운용하는 공모펀드 알려줘	(N6 — 후계 우리자산운용 병기·판매완료 표기)
X12	슈로더가 운용하는 공모펀드는 역외펀드까지 포함하면 몇 개야?	(R6 — 국내 코드 + 0013 계열 합산, JP모간 유입 0)
X13	KB자산운용 펀드의 수탁사 분포 알려줘	(S2 — 씨티 170·HSBC 120·신한 116…)
X14	삼성코리아대표 펀드의 수탁사는 어디야?	(S2/N8 — 이름, 코드만이면 부분)
X15	중국에 투자하는 공모펀드와 국내 ETF는 각각 몇 개야?	(N10 — Country_CHN 소비 후 ETF Region_China 유지)
X16	ETF에 투자하는 공모펀드는 몇 개야?	(N9 — 'ETF' 낱말이 FundAttr_M113 태그로 풀리는 게 이 질문에선 정답)
X17	미래에셋차이나솔로몬 2호는 클래스가 몇 개야?	(R14 — 7)
X18	미래에셋코어테크증권자투자신탁의 모펀드는 뭐야?	(R15 — 모투자신탁(주식) 1건)
X19	2025년에 설정된 공모펀드는 몇 개야?	(R7 — 107/305)
X20	가장 최근에 설정된 공모펀드 2개 알려줘	(R9 — 동률 2건·커버리지)
X21	스페인에 투자하는 공모펀드 있어?	(희소 경계 — ESP 태그 n행 vs 이름-only 역외)
X22	KB자산운용 펀드 중 국민은행이 수탁하는 공모펀드는 몇 개야? 실제 수탁사는 어디야?	(R8 — 0 + 이웃 분포)
X23	미래에셋코어테크 펀드 운용역이 누구야?	(S5 — hasFundManager 즉답, 운용사 대체 안내)
X24	미래에셋코어테크 펀드 기준가 추이 알려줘	(S5 — hasNavHistory 즉답, 스냅샷 대체)
X25	미래에셋코어테크 펀드 기준가 알려줘	(S5 대조군 — 4,726.41원 종류A 답변)
```
