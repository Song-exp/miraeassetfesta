# 온톨로지·KG 구조 검증 60문항 — 3라운드 서버 실측(1e0e641) 채점 + 수렴 판정 — 2026-09-02

> 채점자: 에이전트 B(도메인·온톨로지 심사). 코드는 손대지 않았다.
> 대상: `eval/probe_recheck_2026-09-02_r5.json` 중 KG-001~035 + 형제 X1~X25(60건, 배포 1e0e641) ↔ 2R `eval/probe_kg_structure_2026-09-02_r2.json`(6bad723) ↔ gold `eval/kg_structure_questions.jsonl` · X 는 2R §⑤ 의도별 기대값 + DB 실측.
> 기준: `docs/domain/public_funds.md`(최우선) · 설계 `kg_structure_probe_design_2026-09-02.md` · 1R·2R 채점서. 코드 근거는 `git show 1e0e641:src/runtime/pipeline.py`(가드·Ground 전부 이 파일 — 6bad723 대비 +278/−53) 읽기만. 판정 JSON: `eval/verdicts_kg_2026-09-02.json`(`"3라운드(1e0e641)"` 키 추가, X 신규).
> A 가 반영했다고 한 것: N1(교차 플래그와 무관한 테이블 범위) · N2(이름 리터럴 토큰 핀) · N4(FundAttribute 전 축 canon·동일 라벨 병합·테마/섹터 이름 병기) · M(코드 핀 개별 조회 묶기) · I(Country 독립 낱말). 미착수: N3+S7+R4 · N5/R1-c · N6~N8 · R6~R9 · R14/15 · S2 · S9.

**집계: ✅ 25 · 🟡 4 · ❌ 31** — KG 35: ✅ 17 · 🟡 4 · ❌ 14 (2R ✅17 🟡6 ❌12) / X 25(신규): ✅ 8 · 🟡 0 · ❌ 17.
KG 2R 대비 — 개선 1(024 ❌→✅) · **회귀 3(023 ✅→❌ · 005 🟡→❌ · 012 🟡→❌ 거짓 0)** · 형태 변화 4(026 거짓 단언→오거절 · 028 환각→오거절 · 010 오답→오거절 · 034 거짓 부재→미조회 유보) · 유지 27. **2R ✅17 회귀 전수: 1건(KG-023)** — 나머지 16건은 값·문구까지 동일.

**수렴 판정: 수렴하지 않았다.** N1·N2 는 겨냥한 환각(028)·오타 오독(034)을 닫았지만 같은 질문이 **오거절**로 옮겨갔고(구성종목 4/4 거절), N4 는 024 를 닫는 대신 **FundAttribute 라벨이 Region·상품군 명사와 충돌하는 N9 를 🟢→🔴 로 발현**시켰다(023 회귀 · 'ETF'→M113 이 교차 4문항 + X16 을 오염). I 는 겨냥한 4R 회귀를 잡았으나 국가어가 소분류 값 안에 붙은 012 의 R10 경로를 잃었다. 감점 직결 결함(환각·거짓 값·거짓 0·거짓 부재)이 **2R 5건 → 3R 14건**으로 늘었다(§②). 6라운드를 "수리 없이 확인만" 으로 가려면 §③ 의 프리즈 전 필수 6항을 먼저 닫아야 한다.

---

## ① 판정 표 (60행)

마커: `ABSENT`(enums absent_properties→게이트) · `range`(risk_grade range_by_table) · `slot`(Organization 라벨 슬롯·former_names) · `Country`/`FundAttr`/`Region`(token/eq alias Ground) · `precheck`(`_sql_precheck` — 컬럼 실존→모호→**N1 테이블 범위**→코드 리터럴 순) · `N2`(상품명 필터 주입·토큰 핀) · `N4`(`ensure_fund_attr_tag` 전 축) · `M`(개별 조회 펀드 묶기 MIN/MAX) · `I`(`_country_in_question` 독립 낱말) · `R1`(0행 정책 문구). gold 값은 판매중·공모(2026-08-22) 펀드/클래스.

### KG-001~035 (2R → 3R)

| qid | 축 | 2R | 3R | 변화 | 마커 | 요지 (gold ↔ 3R 실측) | 원인 층(잔여) |
| :-- | :-- | :-: | :-: | :-: | :-- | :-- | :-- |
| KG-001 | A | ✅ | ✅ | 유지 | slot | 823/2,066 + 역외 5(00130006) 병기. 2R 과 문구까지 동일 | — |
| KG-002 | A | ❌ | ❌ | 유지(문구 변형) | slot(IN 00040022·00040007) + M | 결정층 옳음(27행·판매중클래스수 병기). HCX 가 Ground 주석(우리자산운용 후계)을 버리고 **"프랭클린템플턴이 운용 … 20개 모두 판매 중지"** — 판매중 13펀드(우리템플턴…)를 판매 중지로 단언(2R 은 반대로 판매완료를 판매중으로). must '우리자산운용' 0회 | 조립(N6 — 개별조회 목록 기계 조립·후계 주석 병기 미착수) |
| KG-003 | A | ✅ | ✅ | 유지 | slot(former_names→KCGI) | 27/133 + 구상호 안내. 기계 말투("조회한다") 그대로(🟢 S9) | — |
| KG-004 | A | ✅ | ✅ | 유지 | slot(_flex_match) | 143/541 + 코드 2건 합산 고지 | — |
| KG-005 | A | 🟡 | ❌ | **회귀** | (N7 미착수) | **352/1,179** (gold 217/906 = 207+10). HCX 가 `(sale_yn='판매중' OR '판매완료')` + `itm_abrv_nm LIKE '%삼성%'` 를 넣어 판매완료·약어 포함까지 셌고, 기본모수 가드는 'sale_yn 이 있다' 고 보고 불개입. 2R 의 217 마저 잃음. 2문(삼성자산운용 207·삼성액티브 10) 여전히 부재 | 가드(기본모수 = 값까지 검사: `sale_yn` 절이 있어도 '판매중' 단독이 아니면 교체) + 플래너·조립(N7) |
| KG-006 | B | ❌ | ❌ | 유지 | precheck(모호 컬럼) | 1차 `mtco_nm·trusc_nm` 환각 → 재생성 `mgmt_co_nm + trusc_xtn_itt_cd` 는 실행 가능했는데 `itm_no IN (SELECT itm_no FROM public_funds …)` 를 모호로 기각(2R 과 동일 지점) | 가드(N8 서브쿼리 스코프) + 온톨로지(S2) |
| KG-007 | B | ✅ | ✅ | 유지 | — | 823/2,066 | — |
| KG-008 | B | ❌ | ❌ | 유지 | precheck(컬럼 실존) | `trusc_itt_nm` 환각 2회 → 거절 | 온톨로지(S2 ref_organization) |
| KG-009 | B | ✅ | ✅ | 유지 | slot(trustee '0016022') + 목록 기계 조립 | 2개(14클래스) iM에셋 타이거 포커스 1,456억·머스트원앤온리 968억 | — |
| KG-010 | B | ❌ | ❌ | 형태 변화(오답→오거절) | precheck(모호 컬럼) | 2R 은 형제 모펀드 2개를 답했는데 이번엔 `itm_no IN (SELECT itm_no …)` 모호 기각 2회 → 거절. **X18(정식명)은 같은 데이터로 정답** — 차이는 HCX 가 JOIN 대신 IN-서브쿼리를 썼다는 것뿐 | 가드(N8) + R15(stem 완전일치) |
| KG-011 | B | ✅ | ✅ | 유지 | R1(a) | 0개 + "개체·값 실재, 교집합 0". 이웃 분포 미병기(R8) | — |
| KG-012 | C | 🟡 | ❌ | **회귀(거짓 0)** | I · R1(a) | **"0개(클래스 0개) — 각 조건의 개체·값은 데이터에 실재"** (gold 205/522). Ground 매칭 없음('중국' 이 '중국주식' 안에 붙어 독립 낱말 실패) → HCX 가 `fd_ivst_rgn_desc LIKE '%중국%'`(지역 컬럼에 '중국' 값 없음 — 실측 0행) → 국가 확정식 ⓐ 는 `fd_ivst_rgn_desc = '…'` 등호만 치환하고 **LIKE 는 안 잡는다** → 통과 → 0 → R1(a) 가 "실재" 라고 거짓 사유까지 붙임. 2R 은 HCX 가 등호를 써서 우연히 살았다 | 가드(국가 확정식 ⓐ 패턴 = 등호 한정 · R10 분기는 있으나 도달 못함) + precheck(LIKE 리터럴은 값사전 미대조) + R1(a) 오진 |
| KG-013 | C | ✅ | ✅ | 유지 | range | 게이트 즉답 HCX 0회 | — |
| KG-014 | C | ✅ | ✅ | 유지 | range | 게이트 기각 · NULL≠0 | — |
| KG-015 | C | ✅ | ✅ | 유지 | slot(RiskGrade alias raw) | 995/2,994 | — |
| KG-016 | C | 🟡 | 🟡 | 유지 | — | 389/933(약관) · 기준 미고지 | 조립(R8) |
| KG-017 | C | ✅ | ✅ | 유지 | FundAttr_C104 | 3/6 | — |
| KG-018 | C | ❌ | ❌ | 유지 | N4 · R1(c) | canon C102∧C103 주입됐으나 `itm_abrv_nm LIKE '%단위%' AND '%개방%'` 잔존('단위형' 통칭 기준이라 어근 '단위' 절 못 걷음) + sale_yn 미주입 → 0행 → **"상품 자체가 없다"**(실재 31/189) — 2R 과 동일 | 가드(N5) + R1-c |
| KG-019 | D | ✅ | ✅ | 유지 | Country_JPN + 목록 기계 조립 | 31/79 · 피델리티재팬 239억 1위 | — |
| KG-020 | D | ✅ | ✅ | 유지 | Country_USA | 98/333 | — |
| KG-021 | D | ✅ | ✅ | 유지 | Country_TWN 희소→이름 폴백 | 피델리티대만펀드 1 · 문구 소폭 변형 | — |
| KG-022 | D | 🟡 | 🟡 | 유지 | Region_Europe | 61/193 · 기준 미고지 | 조립(R8) |
| KG-023 | D | ✅ | ❌ | **회귀** | Region_Asia ✓ → **N4 가 W108 로 덮어씀** | Ground 는 `fd_ivst_rgn_desc='아시아'` 를 옳게 잡았는데 `ensure_fund_attr_tag` 가 FundAttr_W108(라벨 '아시아', 3자 경계 판정) 에 걸려 **지역 절을 걷어내고 `LIKE '%,W108,%'` 로 교체** → 마이다스아시아리더스 635·609·309 / 삼성퇴직연금아세안40 308 / 미래에셋아시아테크 206 (gold KB중국본토A주 1,453·차이나솔로몬1호 1,393·한투베트남그로스 1,066·신한중국의꿈2호 1,038·다이와넥스트AP 669). 2R 이 예고한 N9 라벨 충돌(V101 국내·W101 글로벌·W104 북미·**W108 아시아**·M113 ETF)이 그대로 발현. 답변 5줄도 클래스명(C-Pe2·W클래스) 그대로 | **가드(N4 — 라벨이 Region/AssetClass/상품군 명사와 같은 FundAttribute 는 매칭 키 제외 = N9)** |
| KG-024 | D | ❌ | ✅ | 개선 | FundAttr_N144 + N4(이름 병기) | **12/78** — `(',' ‖ prfd_attr_cds ‖ ',' LIKE '%,N144,%' OR REPLACE(itm_nm,' ','') LIKE '%반도체%')` 정확 | — |
| KG-025 | E | ❌ | ❌ | 유지(오거절) | precheck(코드 리터럴) · Ground 'ETF'→M113 오염 | 1차 `or_co IN ('삼성','삼성KODEX')` 기각 → 재생성이 public_funds 에 `pd_sale_yn` 를 써 기각. 두 SQL 모두 `LIKE '%,M113,%'` 를 달고 있음(질문의 'ETF' 낱말이 펀드 태그로 풀림) | 가드(R4 교차 확정식) + N9 |
| KG-026 | E | ❌ | ❌ | 형태 개선(거짓 단언→오거절) | precheck(컬럼 실존) | 2R "2,888 · ETF 수록 안 됨" 거짓은 소멸. 이번엔 `pd_sale_yn` 을 public_funds 에 써 2회 기각. Ground 는 여전히 auto 노드 `Idx_a_fa21f0fb15`(cu_base_index='KOSPI200' — 판매중 0건) | KG(S7) + 가드(N3) + N9 |
| KG-027 | E | ✅ | ✅ | 유지 | ABSENT(hasUnitsOutstanding) | 게이트 즉답 | — |
| KG-028 | E | ❌ | ❌ | 형태 개선(환각→오거절) | precheck(문법 TOP) | **'IBK K-AI반도체코어테크 ETF 57.12%' 환각 소멸** — N1 이 아니라 그 앞 검사(`SELECT TOP 1` 문법)가 먼저 잡았다. HCX 는 2회 모두 `domestic_etfs JOIN ext_etf_holdings … pd_abrv_nm LIKE '%미래에셋 코어테크%'` — 펀드 구성종목 조인(`ext_fund_holdings.grp = mtco_itm_no AND or_co`)은 근거문서에 실려 있는데도 ETF 조인만 고른다. gold 삼성전자 24.95 는 `ext_fund_holdings`(KR5153450780, 2026-06-01) 에 실재 | 플래너(구성종목 조인 선택) → **가드(구성종목 질의는 estb_dt 처럼 JOIN 템플릿 기계 주입 — 신규 N11)** |
| KG-029 | F | ✅ | ✅ | 유지 | — | 88(rptt) | — |
| KG-030 | F | 🟡 | 🟡 | 유지 | 미특정→사후 보정 | 17행 정확 조회 후 HCX 가 2호 5(gold 7)·3호 5(gold 8)·합 12(17) 로 오산 — 2R 과 동일 | 조립(R14 호수별 GROUP BY 확정식) |
| KG-031 | F | ❌ | ❌ | 유지 | precheck(00080030 실존 통과) · `_POP_WIDEN` | 167/350 + "역외 47개는 포함하지 않았다"(질문은 '포함하면') — 2R 과 동일 | 가드(R6) |
| KG-032 | G | ✅ | ✅ | 유지 | 시점 게이트 | 기준일 2026-08-22 명시 후 거절 | — |
| KG-033 | G | 🟡 | 🟡 | 유지 | ext JOIN | KB K-성장과 지배구조 30 ✓ · LIMIT 1 동률 누락 — 2R 과 동일. **X20(2개 요청)은 동률 2건 정답** — LIMIT 을 사용자가 정하면 맞고 '가장' 만 쓰면 1건 | 플래너·조립(R9 동률 전부) |
| KG-034 | G | ❌ | ❌ | 형태 개선(거짓 부재→미조회 유보) | **N2 발동**(상품명 필터 주입 '코어테크') · M | '코어텍' 오타 소멸 — 6펀드 정확 조회. 그러나 SELECT 에 `fd_daily_bas_dt`(평가일)만 있고 `estb_dt` 조인이 없어 **"운용 기간이 명시되어 있지 않다"** 로 유보(실제 ext_fund_page.estb_dt 2019-10-23 실재 — 부재 진술은 여전히 거짓) | 라우터(R9 '운용한 지' 경과 트리거 → ext JOIN) + 조립(LLM 산술 금지·julianday) |
| KG-035 | G | ❌ | ❌ | 유지 | ext LEFT JOIN 주입 | "124개"(클래스, gold 39/124) — JOIN 경로 펀드단위 미적용 그대로 | 가드(R7) |

### X1~X25 (신규 — 2R §⑤ 의도별 기대값 대조)

| qid | 시험 규칙 | 3R | 마커 | 요지 (기대값 ↔ 3R 실측) | 원인 층 |
| :-- | :-- | :-: | :-- | :-- | :-- |
| X1 | N1/N2 구성종목 | ❌ 오거절 | precheck(컬럼 실존 → N1 미도달) | 기대 삼성전자 24.95·SK하이닉스 15.90·삼성전기 7.96(ext_fund_holdings 2026-06-01). HCX 2회 모두 `domestic_etfs JOIN ext_etf_holdings … h.holding_nm`(`holding_nm` 은 ext_fund_holdings 컬럼) → "스키마에 없는 컬럼" 기각. **N1 테이블 범위 검사는 컬럼 검사 뒤라 메시지조차 안 나옴** — 재생성 피드백이 "컬럼이 다른 테이블 것" 이라 HCX 는 테이블을 못 바꾼다 | 플래너 + 가드(N11 구성종목 JOIN 템플릿 주입 · precheck 순서: 테이블 범위를 컬럼 검사 앞에) |
| X2 | N1 형제 | ❌ 오거절 | precheck("허용 테이블 밖: ext_etf_holdings") | 기대 ZIJIN MINING 4.97·LUXSHARE 4.26·HANGZHOU CHANG CHUAN 4.25(KR5127450020, 2026-06-01). HCX 가 `ext_etf_holdings JOIN ext_ovs_etf_holdings` 2회 → 화이트리스트 기각. **ETF 테이블 유출 0(의도 충족)** 이나 정답 경로로 못 감 | 플래너 + 가드(N11) |
| X3 | N2/R1-c 오타 | ❌ 거짓 부재 | R1(c) | 기대 "「코어택」을 식별하지 못했다 · 가까운 표기: 코어테크". 실측 **"조건 중 일부는 … 상품 자체가 없습니다"** — Ground 에 상품 고유명 토큰이 없어(오타라 DB 이름에 없음) N2 핀 미발동, HCX 가 '코어텍' 로 옮겨 0행 → 부재 단언. `_suggest_similar_products` 후보(코어테크) 미병기 | 조립(R1-c: 죽은 절 리터럴이 검증 집합 밖이면 '식별 실패' 문구) |
| X4 | N4 병합 | ✅ | FundAttr_N119 + N4(N168 병합·이름 병기) | 6/23 — `(N119 OR N168 OR itm_nm '친환경')` 정확(이름-only 0) | — |
| X5 | N4 이름 병기 | ✅(값) | FundAttr_N145 | 1/12 정확. 단 SQL 에 이름 OR 절 없음 — 라벨이 '2차전지/배터리'(슬래시 포함)라 `_attr_word_map` 키가 질문 낱말 '2차전지' 와 안 맞아 N4 미발동(canon 은 Ground 가 이미 실음). 이름-only 0행이라 값은 무해(🟢 라벨 분절) | — |
| X6 | N5 직교 | ✅(값) | N4(C101∧C103) | 2,874/8,355 정확. 머리줄 "펀드 … (판매중 기준)" — `prvo_pbff_desc` 미주입(기본모수 가드가 sale_yn 만 넣음). 공모 조건이 값을 안 바꿔 무해(🟢) | 가드(기본모수 반쪽 주입) |
| X7 | S7 지수 alias | ❌ 오거절 | Ground Idx_a_fa21f0fb15 · precheck(컬럼) | 기대 50(ref_base_index 'KOSPI 200 CR' 25·'F-KOSPI 200' 16·'KOSPI 200 TR KRW' 9, 계열 94). HCX `pd SALE_YN`(오타 컬럼) 2회 → 거절. 통과했어도 `cu_base_index='KOSPI200'` 은 판매중 0 | KG(S7) + 플래너(재생성이 동일 SQL 반복) |
| X8 | N3+S7 분기별 모수 | ❌ 거짓 값 | Ground 'ETF'→M113 · 라우터 public_funds 단독 | 기대 펀드 57/188(bmrk_nm S&P500 계열) · ETF 46~49. 실측 **"공모펀드 63개"** = `prvo_pbff_desc='공모' AND M113`(S&P500 조건 0개, sale_yn 없음 — 63 은 판매완료 M113 행 수) + "ETF 정보 없음". 라우터가 'S&P500 추종 국내 ETF' 를 tables 에 안 넣음(KG-025·X9·X15 는 넣음) | N9(M113) + 라우터(교차 머리명사 누락) + N3 |
| X9 | R4 운용사 교차 | ❌ 환각 | Ground 코드+alias 혼재 · precheck(컬럼) | 기대 823 · 188. 실측 **"각각 1개, 191개"** — 1차(펀드 분기, `or_co IN ('미래에셋','TIGER',…)`+`pd_sale_yn`) 기각 → 재생성은 ETF 분기만(191 = 미래에셋 188 + 미래에셋TIGER 3, alias 집합 기준이면 허용) → HCX 가 **없는 펀드 분기를 '1개' 로 지어냄**(조회 1행을 개수로 읽음) | 가드(R4 교차 확정식·분기 기계 조립) + 조립(분기 결측 시 '미조회' 고지) |
| X10 | N7 두 집계 | ❌ | ext JOIN 주입(mgmt_co_nm LIKE 'KB%' 날조) · R7 | 기대 129/625 · 그중 KB자산운용 129. 실측 "625개"(클래스) + "KB자산운용 운용 수는 확인할 수 없다" — LEFT JOIN 경로라 펀드단위 가드 불발, 2집계 미생성 | 가드(R7 JOIN 경로 펀드단위) + N7 |
| X11 | N6 구상호 목록 | ❌ 거짓 부재 | slot(former_names 동양→Org_00040007 ✓) · R1(c) | Ground 가 **옳게** 우리자산운용(00040007)으로 해소했는데 HCX 가 코드를 버리고 `ext_fund_page.mgmt_co_nm LIKE '%동양%'` → 0행 → **"상품 자체가 없다"**. 기대: "동양자산운용은 구상호 → 현재 우리자산운용 기준 235펀드/403클래스 목록 + 이름에 '동양' 남은 판매중 펀드 0(판매완료 28)". M 코드 핀은 개별조회 묶기 경로에만 붙어 JOIN 목록엔 불개입 | 가드(M 코드 핀을 목록·JOIN 경로로 — Ground 코드가 SQL 에 없으면 주입) + N6 + R1-c |
| X12 | R6 역외 합산 | ❌ 오거절 | Ground 매칭 없음('슈로더') · '역외펀드' 를 상품 고유명으로 오인 · precheck(코드 리터럴 10000000) | 기대 슈로더투자신탁운용(00040013) 15/46 + 역외 00130003 13/13 = 28/59(+ 키움투자자산운용 00080052 에 '슈로더' 이름 29/67 은 흡수 법인 — 병기). KG: Org_00040013 라벨 '키움슈로더'·alias 에 '슈로더' 단독 없음 → 미매칭 → HCX 가 `or_co = 10000000` 날조 2회 → 거절. 부수: 잔여 토큰 규칙이 도메인 통칭 **'역외펀드' 를 상품 고유명**으로 잡아 `itm_nm LIKE '%역외펀드%'` 강제 | KG(S1 브랜드 슬롯 — 3자 브랜드 alias) + Ground(잔여 토큰 불용어: 상품군 통칭) + R6 |
| X13 | S2 수탁사 분포 | ❌ 코드 노출 | (S2 미착수) | 기대 씨티 170·HSBC 120·신한 116·우리 99…(코드→이름). 실측 **"00020088(42개), 00020020(15개) …"** 코드 원문 + 유형별 분할(SQL 이 zrin_btyp_nm 로 나눔)이라 총분포도 아님. 환각은 아님(행 값 그대로) | 온톨로지(S2 ref_organization) + 조립(코드 컬럼 이름 치환) |
| X14 | S2/N8 이름 | ❌ 거짓 부재 | M(개별 조회 묶기 — `MAX(trusc_xtn_itt_cd)` 실림) | 조회 2행에 수탁 코드 00020081(하나은행)·00020020(우리은행)이 **실려 있는데** HCX 가 "수탁사 정보가 수록되어 있지 않다" — 코드 값을 부재로 오독 | 온톨로지(S2) + 조립(코드 값 = 부재 아님) |
| X15 | N10 Country↔Region | ❌ 거짓 값 | Ground '중국'→Region_China(ETF 컬럼만) · 'ETF'→M113 | 기대 펀드 248/560(CHN) · ETF 53(wu_inv_rgn '중국'). 실측 **"공모펀드 63개, ETF 확인 불가"** — 63 = M113 행(중국 조건 0). Country_CHN 은 교차 라우팅에서 Region_China 에 밀려 소실(N10 의 역방향) | N9 + N10 + N3 |
| X16 | N9 'ETF' 태그 | ❌ 거짓 0 | FundAttr_M113 · R1(a) | **"0개(클래스 0개) — 각 조건의 개체·값은 데이터에 실재"**. M113 은 전체 63행 전부 판매완료 → 기본모수 0. 이름에 'ETF' 든 판매중 공모펀드 20/112(한국투자적격TDF알아서ETF포커스·미래에셋ETF로자산배분·KB RISE 미국고배당ETF모아드림 …) 실재. Country 의 희소 폴백(기본모수 0행 → 이름 OR)이 FundAttribute 에는 없고, R1(a) 가 '실재' 라고 씀 | KG 빌더(판매중 0행 태그 노드 생성 — 제외 규칙 미적용) + 가드(희소 폴백을 FundAttr 로 일반화) + R1(a) |
| X17 | R14 호수 | ❌ 거짓 부재 | 미특정→사후 보정 · N2 통과 · GLOB 주입 | 기대 7(W5 는 정식명으로 7 정답). HCX 이름 리터럴 `'%미래에셋차이나솔로몬2%'` — Ground 토큰 '차이나솔로몬' **포함이라 N2 통과**했지만 호수 '2' 가 붙어 '…신탁2호' 와 불일치 → 0행 → "정보 없음" | 가드(N2 — 이름 리터럴은 토큰과 **동일**해야, 호수는 GLOB 전담) + R14 |
| X18 | R15 모펀드 | ✅ | ext JOIN(정식명) | 미래에셋코어테크증권모투자신탁(주식) ✓ · '재간접' 미노출 | — |
| X19 | R7 JOIN 펀드단위 | ❌ | ext LEFT JOIN 주입 | "305개"(클래스, gold 107/305) — KG-035 와 동일 결함 | 가드(R7) |
| X20 | R9 동률 | ✅ | ext JOIN LIMIT 2 | 2026-06-12 동률 2건(KB K-성장과 지배구조 30 · 미래에셋국민참여형) ✓ · 커버리지 미병기(🟢) | — |
| X21 | 희소 경계 | ✅ | Country_ESP 희소→이름 폴백 · R1 | 판매중·공모 ESP 태그 0·이름 0 → "각 조건은 있으나 동시 만족 없음" — 참(판매완료 미래에셋디스커버리스페인밸류 3클래스·사모 4 는 모수 밖). 이웃(판매완료) 미병기(🟢 R8) | — |
| X22 | R8 0+이웃 | ❌ 거짓 부재 | (count 기계 조립 미적용 — SELECT 2열) | 0 은 맞게 조회됐는데 HCX 가 "개수 정보가 포함되어 있지 않다 · 수탁사 '미수록'" 으로 부재 단언. KG-011 이 ✅ 인 이유(1열 → 기계 조립)와 정확히 대비 | 조립(0행·0값 기계 조립을 다열에도) + R8 |
| X23 | S5 운용역 | ✅ | ABSENT(hasFundManager) | 게이트 즉답 + 운용사 대체 안내 | — |
| X24 | S5 기준가 추이 | ✅ | ABSENT(hasNavHistory) | 게이트 즉답 + 스냅샷·기간수익률 대체 안내 | — |
| X25 | S5 대조군 기준가 | ❌ 거짓 값 | M(`MAX(bns_bpr)` + `MIN(itm_nm)`) | 기대 종류A 4,726.41원. 실측 **"종류 A: 4902.86원"** — 4,902.86 은 종류F 값. 개별 조회 묶기가 값 컬럼을 MAX 로 감싸고 대표 이름은 MIN 으로 뽑아 **다른 클래스의 값을 A 에 귀속**. 6펀드 전부 같은 오귀속. ABSENT 게이트는 옳게 침묵(bns_bpr 스냅샷은 마스터에 있음 — X24 와 정확히 갈림) | 가드(M — 클래스마다 다른 값 컬럼(기준가·수익률·보수)은 MIN~MAX 범위 병기 또는 대표행 값·이름 동행) |

---

## ② 감점 위험(환각·거짓 값·거짓 0·거짓 부재·오거절) 현황

2R 채점 기준(감점 상위 = 환각 > 거짓 단언 > 거짓 부재/유보 > 오거절)으로 60문항을 다시 센다.

| 등급 | 3R 문항 | 건수 | 2R(35문항) 대비 |
| :-- | :-- | :-: | :-- |
| 🔴 환각·거짓 값 (있는 값을 다른 값으로) | X9(펀드 1개) · **X25(기준가 오귀속)** · X8(63) · X15(63) · KG-005(352) · KG-002(13펀드를 판매 중지로) | 6 | 2R 2(026·028) → **6**. 028·026 은 닫혔으나 교차(X8·X9·X15)·묶기(X25)·모수(005)에서 신규 |
| 🔴 거짓 0 | **KG-012**(0 vs 205 — 회귀) · X16(0 vs 이름 20/112) | 2 | 2R 0 → 2. 둘 다 R1(a) 가 "실재" 사유를 붙여 신뢰도를 더 떨어뜨림 |
| 🟠 거짓 부재·유보 ("상품 자체가 없다"/"수록 안 됨") | KG-018 · X3 · X11 · X14 · X17 · X22 · KG-034(약) | 7 | 2R 2(018·034) → 7 |
| 🟠 단위 오답 (클래스를 펀드로) | KG-035(124) · X10(625) · X19(305) | 3 | JOIN 경로 R7 한 자리 |
| 🟡 오거절 | KG-006 · 008 · 010 · 025 · 026 · 028 · X1 · X2 · X7 · X12 | 10 | 2R 4 → 10(028·026 환각→거절 이동 포함). 구성종목 4/4(028·X1·X2)·교차 3/3·모호 2 |
| 🟡 코드 노출 | X13(00020088…) | 1 | S2 |

**닫힌 것(2R 🔴 → 3R)** — KG-028 환각·KG-026 거짓 0 은 소멸(오거절로), KG-024 회귀 해소, KG-034 오타 오독 해소. **열린 것** — 위 표 🔴 8건 중 7건이 이번 배포의 세 규칙(N4 라벨 충돌·M 묶기·교차 경로)에서 났다. 특히 X25 는 **가장 흔한 질문형("○○펀드 기준가/수익률 알려줘")에서 값이 다른 클래스로 귀속**되는 결함이라 재검 R/S/T 의 개별 조회 전부에 잠재한다(R4·S12 는 수익률을 범위로 병기해 살았고, 기준가는 범위 컬럼 목록 밖).

---

## ③ 프리즈 전 필수 vs 이월 — 일반 규칙 · 층 · 의존

사용자 원칙(부류·일반 규칙 단위 · 하드코딩 금지 · 층 지정)대로 적는다. "프리즈 전 필수" 는 §② 🔴·🟠 을 직접 닫는 것만.

### 프리즈 전 필수 (6항 — 감점 직결)

| # | 일반 규칙 | 층 | 닫히는 문항 | 의존 |
| :-- | :-- | :-- | :-- | :-- |
| **F1 = N9↑** | **FundAttribute 매칭 키에서 라벨 충돌 노드를 뺀다** — 라벨이 Region·AssetClass·Country 라벨 또는 상품군 명사(ETF·ETN·펀드·채권)·'국내/해외/글로벌' 과 같으면 Ground 매칭 키·`_attr_word_map` 에서 제외하고 alias(코드)로만 산다. 빌더 V-검증으로 충돌 목록을 만들고, 런타임은 그 목록을 읽는다(하드코딩 아님 — 충돌은 KG 실물에서 계산) | KG 빌더 + 가드 `_attr_word_map` | KG-023 회귀 · KG-025/026 · X8 · X9 · X15 · X16 | 없음 — 즉시 |
| **F2 = M 보강** | **클래스별로 값이 다른 컬럼은 단일 MAX 로 대표하지 않는다** — 개별 조회 묶기에서 값 컬럼이 클래스 종속(기준가·수익률·보수·설정일)이면 `MIN~MAX` 범위 + "클래스에 따라 다름" 을, 클래스 공통(순자산 합·위험등급·수탁 코드)이면 대표값을. 대표 이름(MIN itm_nm)에 개별 클래스 값을 붙이지 않는다. 컬럼의 클래스 종속 여부는 enums 의 컬럼 메타(이미 수익률 범위 목록이 있음)를 확장 | 가드 `ensure_fund_lookup_grouping` + enums 컬럼 메타 | X25 · (R/S/T 개별 조회 전부 잠재) | 없음 |
| **F3 = N11(신규)** | **구성종목·설명서 질의는 JOIN 템플릿을 기계 주입한다** — 라우터가 '보유/담은/구성종목' 을 잡으면 SQL 의 FROM/JOIN 을 `public_funds p JOIN ext_fund_holdings f ON f.grp = p.mtco_itm_no AND f.or_co = p.or_co_xtn_itt_cd` 로 확정하고(estb_dt LEFT JOIN 주입과 같은 기계), HCX 는 WHERE·ORDER 만 채운다. `_sql_precheck` 는 **테이블 범위 검사를 컬럼 검사 앞**으로 옮겨 재생성 피드백이 "테이블이 틀렸다" 가 되게 한다 | 가드(JOIN 주입) + precheck 순서 | KG-028 · X1 · X2 (오거절 3) · KG-034(estb_dt 도 같은 기계) | R9 트리거와 병행 |
| **F4 = R1 정정** | **0행 문구의 세 갈래를 리터럴 검증으로 가른다** — (a) 죽은 절의 리터럴이 값사전·KG alias·Ground 코드 안 **이고 기본모수에서 1행 이상** 이면 "교집합 0" · (b) 리터럴이 값사전 안이나 기본모수 0행(M113·ESP)이면 "판매중 0 · 전체 n(판매완료)" + 이름 폴백 · (c) 리터럴이 검증 집합 밖(오타 '코어텍'·LIKE '%중국%' on 지역 컬럼)이면 "「X」를 데이터 표기로 식별하지 못했다(가까운 표기: …)" — '없다/실재' 금지. precheck 의 값사전 대조를 LIKE 리터럴에도 적용 | 조립 `diagnose_zero_rows.user_text` + precheck | KG-012 · X16 · KG-018 · X3 · X11 · X17 | N2 |
| **F5 = 교차 분기 기계 조립(N3+R4 최소분)** | **교차 집계는 상품군별 SQL 을 따로 만들고 기계로 합친다** — Ground 는 상품군별 정본 필터를 **컬럼별로 분리**해 싣고(펀드 `or_co='코드'` / ETF `TRIM(cu_fund_mgmt_co) IN (alias) AND pd_sale_yn=1`), 각 분기에 그 상품군의 기본모수·펀드단위 집계를 주입, 결과는 "펀드 n · ETF m" 을 기계 조립. 한 분기가 기각·미조회면 그 분기를 **'미조회'** 로 표기하고 값을 지어내지 않는다 | 가드·조립 | X9(1개 환각) · X8 · X15 · KG-025 · KG-026 | S7 은 이월 가능(지수 분기만 미조회 표기) |
| **F6 = 기본모수 값 검사 + JOIN 펀드단위(R7)** | 기본모수 주입은 "`sale_yn` 절 존재" 가 아니라 **`sale_yn='판매중'` 단독 절 존재** 로 판정하고(OR '판매완료'·IS NULL 은 교체), 펀드단위 COUNT 치환은 LEFT JOIN·서브쿼리 경로에도 적용(키 식은 `p.` 한정) | 가드 | KG-005 · X10 · X19 · KG-035 · X6(공모 절) | 없음 |

### 이월 (구조 판단 필요 — 6라운드 이후)

| # | 항목 | 층 | 일반 규칙 | 문항 | 비고 |
| :-- | :-- | :-- | :-- | :-- | :-- |
| S2 | 역할 분리 + `ref_organization`(수탁사 코드→이름) | ttl·yaml·build_db | 1R·2R 그대로 | KG-006·008·X13·X14 | X14 는 F4/조립으로 "코드 00020081(이름 미수록)" 까지는 먼저 갈 수 있음 |
| S7 | 지수 alias 를 ETF 정본 컬럼(ref_base_index)에 + auto 노드가 curated 조상이 되는 역전 기각 | KG·빌더 | 2R 그대로 | KG-026·X7 | KG 재빌드 1회 |
| S1-b | **브랜드 슬롯(label_ko_short) 를 3자까지** — '슈로더'·'키움' 처럼 3자 브랜드는 4자 하한에 걸려 매칭 안 됨. 흡수 법인(00040013 ↔ 00080052 키움투자자산운용)은 former_names/sameAs 로 | KG `organization.yaml` | X12 | 2R "미비" 항목 그대로 |
| N8 | 서브쿼리 스코프 모호 검사 | 가드 | `IN (SELECT … FROM 단일테이블)` 내부는 외부와 분리 | KG-006·010 | 코드 3줄 수준이나 오거절만 닫힘(값은 S2) |
| N5 | 절 제거 어근('단위'·'개방') + 목록 경로 모수 | 가드 | 2R 그대로 | KG-018 | F4 와 같이 가면 좋음 |
| N6·N7 | 개별조회 목록 기계 조립·후계 주석 · 복수 집계 2행 | 조립·플래너 | 2R 그대로 | KG-002·X11·KG-005·X10 | |
| R6 | 역외 = 코드 집합 확장(0013 계열) + Ground 밖 코드 리터럴 기각 | 가드·KG related_codes | 2R 그대로 | KG-031·X12 | 잔여 토큰 불용어('역외펀드' 통칭)도 여기 |
| R8·R9·R14 | 축 고지·이웃 분포 · 경과 트리거·동률 · 호수별 GROUP BY | 조립·라우터·가드 | 1R·2R 그대로 | 012·016·022·X21·X22 / 033·034 / 030·X17 | |
| N10 | Country↔Region 동음 — 교차 라우팅에서 Country 가 Region 에 밀림(X15) | KG `fund_country_auto.yaml region:` | 2R 그대로(방향 추가) | X15 | F5 뒤에 |
| S9·🟢 | 기계 말투 · `tag_sparse` 원천 통일 · 라벨 '2차전지/배터리' 분절(X5) · 기본모수 '공모' 절(X6) · X20 커버리지 | yaml·조립 | | 003·X5·X6·X20 | |
| I 잔여 | 국가어가 상품명에 **붙은** 경우(W2 '미래에셋베트남'·W3 '피델리티재팬')는 여전히 Country 로도 매핑됨 — 4R 회귀 계열이 완전히 닫히진 않음(타 심사관 영역이라 관찰만) | Ground `_country_name_component` | W2·W3 | |

**적용 순서 제안**: F1(즉시, KG 재빌드 없이 런타임 목록 계산 가능) → F2·F6(가드 값 검사) → F4(문구) → F3·F5(조인·교차 기계) → 6라운드 확인 실측 → 이월 S2·S7·S1-b 는 KG 재빌드 묶음으로.

---

## ④ 관찰

1. **이번 배포 다섯 규칙의 실측 효과** — N1: 겨냥한 ETF 환각(028)은 소멸했으나 셋 다 그 앞 검사(문법·컬럼)에서 기각돼 N1 메시지는 X2 한 번만 보였다. 환각→오거절로 등급이 내려간 것이지 정답에 다가간 게 아니다. N2: 034 의 '코어텍' 오타 재현은 닫혔고(6펀드 정확 조회), X17 은 토큰 **포함** 검사만으론 호수 접미가 새는 것을 보여줬다. N4: 024 를 정확히 닫았고(78) X4 병합도 확인됐지만, 3자 라벨 경계 판정이 '아시아'(W108)·'ETF'(M113) 을 삼켜 회귀 1건 + 오염 5건을 만들었다 — 2R 이 🟢 로 둔 N9 를 🔴 로 올린다. M: X14·X25·034 에서 발동했고 X25 에서 값 오귀속을 만들었다. I: R3·S6·T13·V14(독립 낱말) 은 정상, 012 의 R10 경로는 잃었고, 붙은 국가어(W2·W3)는 여전히 Country 매핑이 남는다.
2. **거절 문구 하나가 세 원인을 가린다** — "질의를 안전하게 실행할 수 없어" 10건의 원인은 ① 컬럼 환각(006·008·X7) ② 모호 컬럼(006·010) ③ 코드 리터럴(025·X12) ④ 문법(028) ⑤ 테이블 범위(X2) ⑥ 재생성이 같은 SQL 반복(X1·X7·028 — 피드백이 SQL 에 반영되지 않음). ⑥ 은 별도 관찰: 재생성 2회 중 SQL 이 글자 하나도 안 바뀐 경우가 4건이다(X1·X7·028·X2). 재생성 프롬프트가 "문제를 붙여 다시" 인데 HCX 가 같은 답을 내면 예산만 소진된다 — 동일 SQL 이면 즉시 종료하고 F3 류 기계 주입으로 넘기는 게 맞다.
3. **정식명 vs 통칭의 차이가 정답을 가른다** — 010(코어테크 펀드) ❌ vs X18(코어테크증권자투자신탁) ✅ · X17(차이나솔로몬 2호) ❌ vs W5(정식명 2호) ✅ · 033(가장 최근) 🟡 vs X20(2개) ✅. 데이터는 같고 SQL 형태(IN-서브쿼리 vs JOIN · 리터럴 접미 · LIMIT)만 다르다. 이는 결정층이 아직 SQL 형태에 의존한다는 뜻이고, F3(JOIN 템플릿)·N2 동일성·R9 가 그 의존을 끊는 자리다.
4. **R1(a) '실재' 문구의 위험** — 012·X16 둘 다 "각 조건의 개체·값은 데이터에 실재하며 그 교집합이 0" 이라고 썼는데 012 는 `LIKE '%중국%'` 이 지역 컬럼에 0행, X16 은 M113 이 기본모수 0행이다. 사유 문장이 확신을 더하면서 틀렸다 — 거짓 0 보다 나쁘다. F4 의 (b)/(c) 분기 없이는 이 문구를 꺼두는 게 안전하다.
5. **온톨로지 정합성(N4 병합·I 경계의 타 도메인 영향)** — 이번 배포에 KG 변경은 없다(kg_* 실물 동일). 5R 61문항(R/S/T/V/W) trace 에서 `Country_*`/`FundAttr_*`/`Region_*` Ground 는 펀드 질의 15건에서만 발생, ETF·채권 라우팅 누출 0 — 2R 과 같다. 속성 태그 확정식 발동은 KG 밖 문항에서 관찰되지 않았다(어휘가 없어서). I 규칙 표본: 독립 낱말 6/6 정상, 붙은 국가어 2/2(W2·W3) 는 Country 매핑 잔존 — W3 는 `prfd_attr_cds LIKE '%JPN%'` + LIMIT 1 로 한 클래스 값만 답했다(타 심사관 판정 영역).
6. **X24/X25 대조군이 보여준 ABSENT 경계는 옳다** — '기준가 추이' 는 hasNavHistory 로 즉답, '기준가' 는 bns_bpr 스냅샷으로 조회. 도메인 문서 §1.3 은 "기준가 없음" 이라 적었으나 마스터에 `bns_bpr`(평가일 2026-08-21) 이 있어 조회가 맞다 — 도메인 문서 정정 큐(S9): "기준가 시계열 없음, 기준일 스냅샷 1값 있음".
7. **X21 은 희소 태그 규칙의 경계 사례로 통과** — ESP 태그·이름 모두 판매중 0 이라 "동시 만족 없음" 이 참이다. 다만 판매완료 3클래스가 있어 R8 이웃 병기가 있으면 만점.
8. **6라운드 확인 권고** — F1~F6 반영 뒤 6R 은 다음 세트만 다시 돈다: 회귀 3(005·012·023) + 🔴 8(X8·X9·X15·X16·X25·002·012·005) + 오거절 중 F3 대상 3(028·X1·X2) + 2R ✅17 전수(회귀 감시). 나머지는 값이 두 라운드 연속 동일해 재실측 가치가 낮다.

---

## ⑤ 형제 검증 질문 (다음 라운드 — `ID<TAB>질문`)

```
Z1	미래에셋코어테크 펀드 종류A 기준가 알려줘
Z2	KB중국본토A주 펀드 기준가 얼마야?
Z3	미래에셋코어테크 펀드 3년 수익률 알려줘
Z4	북미에 투자하는 공모펀드 중 순자산 큰 3개 알려줘
Z5	글로벌 주식형 공모펀드는 몇 개야?
Z6	인프라 테마 공모펀드는 몇 개야?
Z7	미래에셋코어테크 펀드가 두 번째로 많이 담은 종목은 뭐야?
Z8	한국투자베트남그로스 펀드가 가장 많이 보유한 종목 5개 알려줘
Z9	미래에셋코어테크 펀드는 언제 설정됐어?
Z10	해외주식형 중에서 인도주식 유형인 공모펀드는 몇 개야?
Z11	중국주식 유형 공모펀드는 몇 개야?
Z12	이름이 삼성으로 시작하는 판매중 공모펀드는 몇 개야?
Z13	KB자산운용이 운용하는 공모펀드와 KB자산운용 국내 ETF는 각각 몇 개야?
Z14	삼성자산운용이 운용하는 국내 ETF는 몇 개야?
Z15	슈로더투자신탁운용이 운용하는 공모펀드는 몇 개야?
Z16	키움투자자산운용이 운용하는 공모펀드는 몇 개야?
Z17	미래에셋차이나솔로몬증권투자신탁 2호 클래스 몇 개야?
Z18	ETF로 자산배분하는 공모펀드 알려줘
Z19	나스닥100을 추종하는 국내 ETF는 몇 개야?
Z20	미래에셋 코어태크 펀드 위험등급 알려줘
Z21	삼성코리아대표 펀드 수탁 코드 알려줘
Z22	2024년에 설정된 공모펀드는 몇 개야?
Z23	미래에셋코어테크 펀드의 수탁사는 어디야?
Z24	호주에 투자하는 공모펀드 있어?
Z25	프랑스에 투자하는 공모펀드는 몇 개야?
```

주석(시험 규칙 · 기대값 — DB 실측, 판매중·공모 2026-08-22):

- Z1 F2 — 클래스 지정 시 단일 값 4,726.41원(평가일 2026-08-21) · 다른 클래스 값 귀속 0
- Z2 F2 형제 — 14클래스 범위 병기 또는 대표행 값·이름 동행
- Z3 F2 — 범위 병기가 이미 되는 수익률 경로가 기준가 수리 뒤에도 유지되는지(회귀 감시)
- Z4 F1 — '북미' 는 FundAttr_W104(494행) 라벨이자 Region_NorthAmerica · 지역 컬럼엔 '남미/북미'(489) — 축 고지 필수, W104 canon 이 지역 절을 덮으면 재발
- Z5 F1 — '글로벌' FundAttr_W101 ↔ Region_Global(지역 컬럼 '글로벌') 충돌 · '주식형' 은 zrin_btyp_nm
- Z6 N4 병합 — 인프라 N116/N155 병합 ∪ 이름
- Z7 F3 — SK하이닉스 15.90% (ORDER BY weight_pct DESC LIMIT 1 OFFSET 1)
- Z8 F3 형제 — ext_fund_holdings 조인 템플릿이 다른 운용사 코드(00040024)에도 붙는지
- Z9 F3/R9 — estb_dt 2019-10-23(종류F 10-21 · C-I 10-29 병기)
- Z10 F4/R10 — zrin_ptn_nm='인도주식' 소분류 값 · '인도' 붙은 낱말이라 독립 낱말 실패 경로
- Z11 F4/R10 — '해외주식형' 없이 소분류만 · 205/522
- Z12 F6 — '판매중' 을 질문에 명시해도 217/906 · 판매완료 OR 절 0
- Z13 F5 — 펀드 129 · ETF cu_fund_mgmt_co 'KB' 판매중 수 병기, 분기 미조회 시 '미조회' 표기
- Z14 F5 대조군 — ETF 단독 224(alias 집합 240 은 기준 병기 시 허용)
- Z15 S1-b — 정식명으로 00040013 15/46(+역외 13 별도 병기)
- Z16 S1-b — 00080052 97/308 (+00040013 흡수 병기 가점)
- Z17 N2 동일성 — 정식명 + '2호' 표기 변형 7
- Z18 F4(b) — M113 판매중 0 → 이름 'ETF' 20/112 폴백 목록
- Z19 S7 — ref_base_index 'NASDAQ 100 …' 계열 실측 병기(cu_base_index 로 0 이면 재발)
- Z20 F4(c) — "「코어태크」식별 실패 · 가까운 표기 코어테크" · '없다' 0회
- Z21 S2 전 단계 — 코드 00020081/00020020 을 '미수록' 이 아니라 코드 그대로 + 이름 미수록 고지
- Z22 F6/R7 — JOIN 경로 펀드/클래스 이원 표기
- Z23 S2 — 신한은행(00020088) · 코드만이면 부분
- Z24 희소 폴백 — AUS 태그 0 → 이름 '호주' 폴백(있으면 목록, 없으면 X21 형 문구)
- Z25 희소 경계 — FRA 태그 n행 vs 이름-only 역외 병기 규칙

---

## ⑥ 표본 5건 원문 대조 (무작위 seed 0x1e0e641 → X5 · X16 · X9 · KG-001 · KG-002)

**X5 — 2차전지 테마 공모펀드는 몇 개야?**
- 3R answer 전문: `조회 조건에 해당하는 공모펀드는 1개(클래스 12개)입니다 (판매중·공모 기준, 기준일 2026-08-22).`
- gold(실측): N145 태그 1펀드/12클래스 · 이름 '2차전지' 판매중 0행 → 1/12. 판정 ✅ (이름 OR 절 미주입은 값 무해 🟢).

**X16 — ETF에 투자하는 공모펀드는 몇 개야?**
- 3R answer 전문: `조회 조건에 해당하는 공모펀드는 0개(클래스 0개)입니다 (판매중·공모 기준, 기준일 2026-08-22).\n(각 조건의 개체·값은 데이터에 실재하며 그 교집합이 0입니다)`
- gold(실측): M113 태그 전체 63행 전부 판매완료(기본모수 0) · 이름에 'ETF' 든 판매중 공모펀드 20펀드/112클래스(한국투자적격TDF알아서ETF포커스 · 미래에셋ETF로자산배분적격TDF · KB RISE 미국 고배당 ETF 모아드림 …). 정답 형태 = "태그 기준 판매중 0(판매완료 63행) · 이름 기준 20/112". 판정 ❌ 거짓 0 + '실재' 오진.

**X9 — 미래에셋자산운용이 운용하는 공모펀드와 국내 ETF는 각각 몇 개야?**
- 3R answer 전문: `미래에셋자산운용이 운용하는 공모펀드와 국내 ETF는 각각 1개, 191개입니다.`
- gold(실측): 펀드 823/2,066(00080008) · 국내 ETF 188(TRIM(cu_fund_mgmt_co)='미래에셋', pd_sale_yn=1; alias 집합 '미래에셋TIGER' 3 포함 191 은 기준 병기 시 허용). 판정 ❌ 환각 — 펀드 분기는 기각돼 조회된 적이 없는데 '1개' 로 단언.

**KG-001 — Mirae Asset이 운용하는 공모펀드는 몇 개야?**
- 3R answer 전문: `미래에셋자산운용이 운용하는 공모펀드는 823개(클래스 2,066개)입니다 (판매중·공모 기준, 기준일 2026-08-22).\n종목명이 '미래에셋' 로 시작하는 역외펀드 5개(클래스 5개, 해외 운용법인 코드 00130006)는 별도 법인이라 이 수에 포함하지 않았습니다.`
- gold: 펀드 823개(클래스 2,066개) — 미래에셋자산운용(00080008). must '823'·'미래에셋자산운용' ✓ · must_not 0회. 판정 ✅ (2R 과 동일).

**KG-002 — 프랭클린템플턴이 운용하는 공모펀드 알려줘**
- 3R answer 전문(요약 아님, 원문): `프랭클린템플턴이 운용하는 공모펀드는 다음과 같습니다.\n\n1. 프랭클린템플턴포커스자주식C\n2. 템플턴밸런스드20채권혼합1호\n3. 템플턴프런티어마켓증권자투자신탁(주식)A\n4. 우리템플턴글로벌증권자투자신탁(E)(주식)\n5. 템플턴프런티어마켓증권자투자신탁(UH)(주식)A\n6. 템플턴퇴직연금메이져아시아증권자투자신탁(채권혼합)\n7. 템플턴프런티어마켓증권자투자신탁(UH)(주식)Ce\n8. 우리템플턴글로벌증권자투자신탁(E)(채권-재간접형)\n9. 우리템플턴퇴직연금글로벌40증권자투자신탁(채권혼합)\n10. 우리템플턴퇴직연금글로벌증권자투자신탁(채권-재간접형)\n11. 프랭클린템플턴파워리서치증권자투자신탁[주식]클래스C-F\n12. 우리템플턴글로벌증권자투자신탁(채권-재간접형)ClassA\n13. 우리템플턴차이나드래곤증권자투자신탁(E)(주식-재간접형)\n14. 템플턴퇴직연금글로벌라이프2025증권자투자신탁[채권혼합]\n15. 템플턴퇴직연금글로벌라이프2035증권자투자신탁[채권혼합]\n16. 템플턴퇴직연금글로벌라이프2045증권자투자신탁[채권혼합]\n17. 우리템플턴이스턴유럽증권자투자신탁(주식-재간접형)ClassA\n18. 우리템플턴퇴직연금글로벌채권50증권자투자신탁(채권-재간접형)\n19. 우리템플턴월지급글로벌증권자투자신탁(채권-재간접형)ClassA\n20. 우리템플턴연금저축글로벌증권자투자신탁(채권-재간접형)ClassC\n\n이상 20개의 공모펀드가 있으며, 모두 판매 중지된 상태입니다.`
- gold: 프랭클린템플턴(00040022) 판매중·공모 0 · 이름 잔존 판매중 펀드는 전부 우리자산운용(00040007) 운용 — "현재 프랭클린템플턴 명의로 운용 중인 공모펀드는 없고 해당 상품군은 우리자산운용이 운용" + 목록. 조회 27행 중 판매중클래스수 ≥1 인 펀드 13개(우리템플턴글로벌·이스턴유럽 7클래스 …). 판정 ❌ — must '우리자산운용' 0회 · 판매중 13펀드를 "모두 판매 중지" 로 단언(거짓 값).
