# 규칙 7. 배타·분리 — 한 축에 놓으면 안 되는 것들

> 겉보기에 같은 컬럼인데 **서로 다른 종류**가 섞여 있다. 정렬·집계 전에 갈라야 한다.

> 🔴 생성물입니다. 서술·체크리스트는 `scripts/_ontology_rules_data.py`,
> 목록·수치는 `scripts/gen_ontology_rules_doc.py` 가 yaml·DB 에서 매번 새로 뽑습니다.

| | |
| :-- | :-- |
| 목록 | [규칙 12종 색인](README.md) |
| 컬럼 단위 상세 | [`../data_dictionary/`](../data_dictionary/README.md) |

---

## 1. 데이터가 이랬다

- `srfc_irt` 한 컬럼에 **세 의미** — 고정 이표(발행 시 약속·불변), 변동금리(기준일 스냅샷), 할인채(발행 할인율).
- ETF 와 ETN 은 같은 테이블인데 구성종목·기초지수 구조가 다르다.
- 같은 종목이 장내·장외 양쪽에 있어 시장별 group-by 는 구성 효과에 교란된다.

## 2. 그래서 이렇게 정했다

- ETF `DisjointWith` ETN — 온톨로지 수준의 배타 선언.
- `이자유형분리` — 표면금리 정렬은 `bd_intp_tcd`·`bd_inrt_tcd` 로 유형을 나눈 뒤에만.
- `시장집계금지` — `pd_exg_mkt` 단독 group-by 금지.

## 3. 전수 인벤토리 — 이 규칙이 실제로 어디에 선언돼 있나

#### (1) 배타·분리 관련 규칙 — 전수

| 도메인 | 규칙 | 내용 |
| :-- | :-- | :-- |
| 채권 | `이자유형분리` | 표면금리 정렬은 bd_intp_tcd·bd_inrt_tcd 로 유형 분리 — 할인채(발행 할인율)·변동금리(스냅샷)·srfc_irt=0(구조) 을 고정 이표채와 한 축에 놓지 말 것 |
| 채권 | `시장집계금지` | pd_exg_mkt 단독 group-by 금지 — 구성 효과 교란 (§9.5). 같은 종목이 양 시장에 있을 수 있음 |

#### (2) DisjointWith · 상품군 선언

- **국내ETF** `product_group` — {'column': 'pd_grp_no', 'values': {'ETF': 1235, 'ETN': 545}, 'note': "'국내ETF마스터'에 ETN 30.6% 혼입 (2차). ETF 전용 질의는 pd_grp_no='ETF' 필수. 보조신호: cu_strtegy='C'(422건 전부 ETN), ETN은 cu_charge_rt·ref_*·pd_ticker·분배정보 전부 NULL.", '_closed_2026-08-25': 'ETF/ETN 클래스 구조 — ontology.ttl 에 :ETF owl:disjointWith :ETN 으로 확정(build_ontology emit_ttl). 공통상위는 :DomesticETF 테이블 클래스'}
- **해외ETF** `product_group` — {'column': 'pd_grp_no', 'values': {'ETF': 5972, 'ETN': 65}, 'secondary': "cu_etn_yn='Y' (65건), cu_inverse_short_yn='Y' (183건, 인버스/숏)", 'note': "ETN 1.1%로 국내(30.6%)보다 경미. ETF 전용 질의는 pd_grp_no='ETF'. (2차 2026-08-22)"}

#### (3) 실측 — 한 컬럼에 섞여 있는 종류들

| 이자유형 | n | 최소 | 최대 | 평균 |
| :-- | --: | --: | --: | --: |
| 이표채 | 18,059 | 0.0 | 20.0 | 3.37 |
| 복리채 | 2,867 | 0.0 | 5.89 | 2.05 |
| 할인채 | 689 | 0.0 | 6.23 | 2.73 |
| 단리채 | 267 | 0.0 | 20.0 | 4.75 |

| 상품군 | n | 기초지수없음 |
| :-- | --: | --: |
| ETF | 1,235 | 27 |
| ETN | 545 | 545 |

## 4. 근거 (라이브 DB 실측)

**표면금리 한 컬럼, 네 가지 이자 유형 — 섞어 정렬하면 의미가 없다**

| 이자유형 | n | 최소 | 최대 | 평균 |
| :-- | --: | --: | --: | --: |
| 이표채 | 18,059 | 0.0 | 20.0 | 3.37 |
| 복리채 | 2,867 | 0.0 | 5.89 | 2.05 |
| 할인채 | 689 | 0.0 | 6.23 | 2.73 |
| 단리채 | 267 | 0.0 | 20.0 | 4.75 |

**ETF/ETN 구조 차이 — 기초지수 수록 여부**

| 상품군 | n | 기초지수없음 |
| :-- | --: | --: |
| ETF | 1,235 | 27 |
| ETN | 545 | 545 |

## 5. 안 지키면

‘표면금리 높은 채권’ 에 **할인율과 이표금리가 뒤섞여** 비교 불가능한 순위가 나온다.

## 6. 검토 체크리스트

> 전수조사에서 나온 규칙이므로 **규칙 단위로 판정**한다. 각 항목에 결론과 근거를 적고,
> 판정이 바뀌면 `ontology/enums/*.yaml` 또는 `ontology/shared/*.yaml` 을 고친 뒤 재생성한다.

| # | 검토 항목 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- |
| 1 | ETF/ETN DisjointWith 가 `.ttl` 에 실제로 있는가 — 제출 규격에서 OWL 어휘를 본다. |  |  |
| 2 | 이자유형 분리를 답변이 실제로 하는가 — 플래너 프롬프트에 이 규칙이 전달되는지 확인. |  |  |
| 3 | 펀드에도 배타 축이 필요한가(주식형/채권형/재간접이 배타인가 중첩인가). |  |  |
| 4 | 해외ETF `pd_grp_no` 에 ETN 이 섞여 있는지 실측 — 국내와 같은 문제가 있는가. |  |  |
| 5 | ‘한 축에 놓으면 안 되는’ 다른 컬럼이 더 있는가 — 수익률(누적 vs 연환산), 순자산(KRW vs USD). |  |  |

---

← [색인으로](README.md)
