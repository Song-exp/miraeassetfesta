# 규칙 9. 금지 규칙 — 이 컬럼으로는 답하지 마라

> 존재하지만 **정보량이 없거나 신뢰할 수 없는** 컬럼이 있다. 배제를 명시적으로 선언한다.

> 🔴 생성물입니다. 서술·체크리스트는 `scripts/_ontology_rules_data.py`,
> 목록·수치는 `scripts/gen_ontology_rules_doc.py` 가 yaml·DB 에서 매번 새로 뽑습니다.

| | |
| :-- | :-- |
| 목록 | [규칙 12종 색인](README.md) |
| 컬럼 단위 상세 | [`../data_dictionary/`](../data_dictionary/README.md) |

---

## 1. 데이터가 이랬다

- 값이 전부 0 인 컬럼이 있다 — 채우려다 만 것으로 보이나 근거가 없어 단정할 수 없다.
- 2차에서 100% 결측이 된 컬럼이 있다. 필터에 쓰면 **전 행이 탈락**한다.
- 다른 컬럼과 99% 동일값이라 정보량이 0 인 컬럼이 있다.

## 2. 그래서 이렇게 정했다

- `전부0컬럼금지`·`과세수익률금지`·`더티금지`·`등급일사용금지` — **컬럼 이름으로 금지 목록을 선언**.
- 금지 사유를 함께 적어 답변이 “데이터가 제공되지 않았습니다” 로 나가게 한다.

## 3. 전수 인벤토리 — 이 규칙이 실제로 어디에 선언돼 있나

#### (1) 금지 규칙 — 전수

| 도메인 | 규칙 | 내용 |
| :-- | :-- | :-- |
| 채권 | `과세수익률금지` | avg_annual_tax_yield 사용 금지 (전량 0) |
| 채권 | `더티금지` | dirty·ndy_dirty 사용 금지 — 99.0%가 eval_price 동일값(정보량 0). '실제 결제금액' 답변 금지 |
| 채권 | `등급일사용금지` | crd_grd_dt 로 등급 최신성·신선도 판단 금지 — 스키마: '등급 미변경 시 과거 일자로 유지될 수 있음' |
| 채권 | `시장집계금지` | pd_exg_mkt 단독 group-by 금지 — 구성 효과 교란 (§9.5). 같은 종목이 양 시장에 있을 수 있음 |
| 채권 | `유동화위험금지` | 유동화 = TRIM(bd_knd) IN ('MBS','유동화회사채','Conduit회사채') 3,949 + 코드 밖 SPC 96(발행사명 유동화/신용보증/기술보증 SPC) = 4,045행 (🔴 2026-08-26 정정 — Conduit회사채 1,025행을 bd_knd 코드로 편입. 이전 3,036행은 Conduit 중 16행만 발행사명 패턴으로 회수해 1,009행이 누락돼 있었다). 발행사명으로 위험 판단 금지 — 발행자 = SPC. 조건식: TRIM(bd_knd) IN ('MBS','유동화회사채','Conduit회사채') OR pd_pbcm LIKE '%유동화%' OR pd_pbcm LIKE '%신용보증%' OR pd_pbcm LIKE '%기술보증%' OR pd_nm LIKE '%유동화%' |
| 국내ETF | `전부0컬럼금지` | cu_charge_etc_rt · pd_dvid_inc_dist · fn_average_maturity · fn_effective_duration · fn_modified_duration 사용 금지 |

#### (2) 답변 정책에 '금지' 가 들어간 컬럼 — 전수

| 도메인 | 컬럼 | 한글명 | answer_policy |
| :-- | :-- | :-- | :-- |
| 채권 | `exrt_grte_ern_r_tcd` | 만기보장수익률구분코드 | 🔴 코드 01~05 의 의미는 코드북 미제공(주최 확정) — "구분 있음(코드 0X)" 이상 해석 금지. 만기보장수익률 질의는 tcd<>'99' 544행 모수 + exrt_grte_ern_r>0 인 행만 값 제시. |
| 채권 | `exrt_grte_ern_r` | 만기보장수익률 | 값 >0 인 256행만 답변 대상. tcd='99' 인데 >0 인 4행은 모순 — 사용 금지. |
| 채권 | `crd_grd` | 적용신용등급 | "확인할 수 없습니다" ❌ → 국공채는 "국채·지방채는 신용등급이 부여되지 않습니다" ✅. 결측 4,020행(18.4%) = 국공채 2,840 + 특수채 254 + 회사채 926 — 회사채·특수채 결측 1,180은 "등급 미수록". 등급 조건 질의는 모수 17,862/21,882 명시. 등급 답변에 평가일(crd_grd_dt)을 병기하되 신선도 판단은 금지 (crd_grd_dt 참조). |
| 채권 | `crd_grd_dt` | 신용등급 적용일자 | 등급 답변에 병기 가능. 🔴 스키마 코멘트 명시 — "등급 미변경 시 과거 일자로 유지될 수 있음" → 이 일자는 '최종 평가일'이 아니라 '최종 변경·부여일'. 신선도(오래된 등급) 경고·최신성 판단 근거로 사용 금지. 1차 "5년 초과분 경고 승격" 정책 폐기 (5년 초과+잔존>0 은 2,291행이나 정상 상태). 등급일만 있고 등급값 없는 16행은 "과거 이력은 있으나 현재 등급값이 데 |
| 채권 | `dirty` | 이자부단가 (Dirty Price) | 🔴 사용 금지 확정(검수 C 2026-08-25) — 21,655/21,866행(99.0%)이 eval_price 와 동일값(경과이자 0). 1차 94.6% → 2차 더 악화. 컬럼은 존재하나 정보량 없음. "실제 결제금액" 질의에 이 컬럼으로 답하지 않는다. NULL 16. |
| 채권 | `ndy_eval_price` | 익일 평가일단가 | 일반 질의는 eval_price. 1차 "사용 금지(위장결측 8,503)" → 2차 위장결측 16행으로 완화 — 익일 질의에만 사용. |
| 채권 | `ndy_dirty` | 익일 이자부단가 | dirty 와 동일 사유로 사용 금지(검수 C 2026-08-25). 단위 기재는 비교·정렬 금지와 무관하게 남긴다. |
| 채권 | `after_tax_yield` | 개인 세후 운용수익률 | 개인 수익률 질의의 기본값 — "개인 기준 세후" 명시. ✅ depo_equiv_yield_154 × 0.846 = 이 컬럼, 634/634 성립. 🔴 세후 > 매수수익률(buy_yield) 인 행 72 — 매매차익 비과세 구조(할인 매수)로 성립 가능, 오류 단정 금지 (§9.4). |
| 채권 | `corp_pretax_yield` | 법인 세전 투자수익률 | 법인 투자자 질의에만. 값은 컬럼 그대로 인용하고 산식·세율은 서술 금지 — 검수 C 2026-08-25 실측 buy_yield 대비 비율 1.00~1.08 로 분산(단일 세율 아님), 산식 확정 불가 → 배제. |
| 채권 | `buyable_quantity` | 매수가능수량 | 🔴 사용 금지 — 주최 공지(8/24) "값 무효, 신경 쓰지 말 것. 상폐/리스팅종료 제외 전부 구매가능 가정". >0 296 · =0 338 이지만 어느 쪽도 판매 가능 여부의 근거가 아님. |
| 국내ETF | `du_last_aum` | 최종AUM(순자산총액) | AUM 질의 답변 가능 — ETF 기준 결측 4.8%(59건)·0값 6건. 단위 KRW(원). 해외ETF(USD)와 통합 정렬 금지 — 상품군별 분리 제시. |
| 국내ETF | `ru_mkt_volume` | 거래량(=일거래량 복사) | 🟢 답변 가능 — du_vol_1d 와 1,776/1,780 동일. '실시간' 표현 금지, '8/21 일거래량'으로. |
| 해외ETF | `wu_core_yn` | 핵심ETF여부 | 🔴 '핵심 ETF' 질의에 이 컬럼 사용 금지 — 98.2% 결측에 유효값이 전부 'N'(Y 는 0건)이라 판별 정보가 없다. '데이터 미제공'. |
| 해외ETF | `pd_lst_price` | 상장가격 | '상장가격' 질의는 데이터 미제공으로 응답. 0값을 실제 가격으로 서술 금지. |
| 펀드 | `std_itm_no` | 표준종목번호 | 결측 시 sale_yn 을 직접 읽어 병렬 서술 — '판매완료 상품이며 표준종목번호는 조회되지 않습니다'. 인과로 서술 금지(판매완료 12,714 중 12,054 는 값 보유). 식별은 itm_no 로 한다. |
| 펀드 | `rptt_ksd_itm_no` | 대표예탁원종목번호 | 결측 시 '확인할 수 없음' + sale_yn 병렬 서술. 인과 서술 금지. 대체 식별자는 ksd_itm_no(결측+더미 2,370, 전체 10.0%). |
| 펀드 | `fss_itm_no` | 금융감독원종목번호 | 더미를 값으로 취급 금지(조인 시 한 값에 1만 행이 뭉친다). 결측 시 '확인할 수 없음'. |
| 펀드 | `mtco_itm_no` | 운용사종목번호 | 🔴 단독 키로 조인 금지 — 415종의 값이 2개 이상 운용사에 걸친다. 펀드 단위 키는 (or_co_xtn_itt_cd, mtco_itm_no). zero-pad 는 7자리 기준(길이 8 이상의 이상값은 자르지 말 것 — query_rules.펀드단위_근거). |
| 펀드 | `itm_eabrv_nm` | 종목영문약어명 | 결측 23,528/23,676(99.4%). 필터·조인 금지. 영문 표기는 itm_eng_nm(결측 0) 으로. |
| 펀드 | `thco_sale_yn` | 당사판매여부 | 빈칸의 뜻은 sale_yn·prvo_pbff_desc 와 함께 읽을 것. "당사 미취급이라 정보가 부족" 식 인과 서술 금지. |
| 펀드 | `zrin_fd_ivst_risk_gcd` | 제로인펀드투자위험등급코드 | 비교 전 CAST(… AS INTEGER). 숫자가 작을수록 위험이 크다는 방향을 답변에 밝힐 것. 결측 시 '평가 데이터가 제공되지 않았습니다'. '소규모라서' '신규라서' 사유 서술 금지(1차 판정 유지 — 충분조건 아님). 모수는 판매중·공모 8,969 중 8,547 등 필터 조건과 함께 밝힌다. |
| 펀드 | `fd_nast_suma` | 펀드 순자산 | 순자산 Top-N 은 sale_yn='판매중' (+공모) 한정, 모수·기준일(fd_daily_bas_dt) 병기. 🔴 펀드 단위 집계는 **MAX** — 이 값은 펀드 순자산이 클래스마다 반복 기록된 것이다 (2차 실측: std_itm_no 안에서 값이 갈리는 경우 0건. 펀드 키(or_co, mtco) 단위 재검증은 미실시 — 1차 75.6% 동일). SUM 은 과대계상(1차 3.31배)  |

— 총 **22개 컬럼**

#### (3) 실측 — 정말 못 쓰는 컬럼인가

| 컬럼 | non_null | 값이_0 |
| :-- | --: | --: |
| domestic_bonds.avg_annual_tax_yield | 634 | 634 |
| public_funds.fd_wk1_ern_r | 0 | 0 |
| domestic_etfs.cu_charge_etc_rt | 217 | 217 |

## 4. 근거 (라이브 DB 실측)

**전량 0 — non-null 이지만 값이 전부 0 이라 정보가 없다**

| non_null | 값이_0 |
| :-- | --: |
| 634 | 634 |

**100% 결측 — 필터에 쓰면 전 행 탈락**

| 전체 | fd_wk1_ern_r_결측 |
| :-- | --: |
| 23,676 | 23,676 |

## 5. 안 지키면

‘기타비용 낮은 ETF’ 처럼 **전부 0 인 컬럼으로 순위를 매기면 아무 의미 없는 답**이 나간다.

## 6. 검토 체크리스트

> 전수조사에서 나온 규칙이므로 **규칙 단위로 판정**한다. 각 항목에 결론과 근거를 적고,
> 판정이 바뀌면 `ontology/enums/*.yaml` 또는 `ontology/shared/*.yaml` 을 고친 뒤 재생성한다.

| # | 검토 항목 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- |
| 1 | 금지 컬럼이 SQL 가드(`validate_sql`)에서 **기계적으로 차단되는가**, 아니면 프롬프트 문구뿐인가. 후자면 지켜진다는 보장이 없다. |  |  |
| 2 | 금지 목록이 2차에서도 유효한가 — 1차엔 전부 0 이었으나 2차에 채워진 컬럼이 있다(해외ETF `du_er_1d` 가 그 사례). |  |  |
| 3 | 해외ETF·펀드에도 전량 0/상수 컬럼이 있는가 — `auto.yaml` 재생성 후 전수 확인. |  |  |
| 4 | 금지 대신 ‘조건부 사용’ 이 맞는 컬럼이 있는가(예: 장내 한정으로는 유효한 컬럼). |  |  |
| 5 | 금지 사유가 답변 문구로 잘 번역되는가 — ‘제공되지 않았습니다’ vs ‘해당 없습니다’ 구분. |  |  |

---

← [색인으로](README.md)
