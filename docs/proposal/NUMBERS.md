# 📊 제안서 수치 — 단일 출처 (GENERATED)

> `python scripts/gen_proposal_numbers.py` 로 재생성. **제안서 문서는 이 파일만 인용한다.**
> 문서마다 수치를 따로 적으면 데이터 갱신 때 어긋난다 — 8/25 초안이 실제로 그렇게 낡았다.
> 생성 시각 기준 DB: `financial_products.db`

## 1. 마스터 (주최 제공)

| 테이블 | 소스 파일 | 행 | 컬럼 | 버전 | 기준일 |
| :-- | :-- | --: | --: | :-- | :-- |
| `domestic_bonds` | prbd01n001_data.xlsx | 21,882 | 58 | v2_20260824 | 2026-08-22 |
| `domestic_etfs` | pref01n001_data.xlsx | 1,780 | 98 | v2_20260824 | 2026-08-22 |
| `overseas_etfs` | pref02n001_data.xlsx | 6,037 | 49 | v2_20260824 | 2026-08-22 |
| `public_funds` | prfd01n001_data.xlsx | 23,676 | 75 | v2_20260824 | 2026-08-22 |
| **합계** | | **53,375** | | | |

### 상품군 구성

- `domestic_etfs`: ETF 1,235 · ETN 545
- `overseas_etfs`: ETF 5,972 · ETN 65

### 공모펀드 기본모수 · 식별 키

| 항목 | 값 |
| :-- | --: |
| 전체 행 (= 클래스) | 23,676 |
| 사모 행 | 8,960 |
| 판매중 행 | 10,962 |
| **기본모수 행** (판매중 AND 공모) | **8,969** |
| **기본모수 펀드** (펀드단위 키) | **3,040** |
| 펀드단위 키 distinct — 전체 (더미 제외) | 14,409 |
| 펀드단위 키 distinct — 판매중 (더미 제외) | 4,305 |
| 위험등급 미수록 (NULL ≠ 0등급) | 14,987 |

**식별 코드 더미** — `NULL` 만 결측으로 보면 틀린다. 조인·개체 키에서 제외하는 행:

| 컬럼 | 더미 행 | 비율 |
| :-- | --: | --: |
| `fss_itm_no` | 11,655 | 49.2% |
| `kofia_fd_ccd` | 11,476 | 48.5% |
| `rptt_ksd_itm_no` | 7,086 | 29.9% |
| `std_itm_no` | 4,668 | 19.7% |
| `ksd_itm_no` | 2,370 | 10.0% |
| `mtco_itm_no` | 1,057 | 4.5% |

> `mtco_itm_no` **단독 조인 금지** — 펀드단위 키 기준 85,649쌍 중 **5,616쌍(6.6%)** 이 다른 운용사의 펀드에 붙는다 (2개 이상 운용사에 걸친 값 748종). 키는 `(or_co_xtn_itt_cd, mtco_itm_no)` 복합키다.

### 국내채권 기본모수 · 등급 · 판정 컬럼

| 항목 | 값 |
| :-- | --: |
| 전체 행 / 종목 (`pd_no` distinct) | 21,882 / 20,497 |
| 장내·장외 2~4행 중복 종목 | 1,078 |
| **구매가능 행 / 종목** (`curr_cd='KRW' AND mat_dt >= 20260824`) | **21,814 / 20,431** |
| 판정 기준일 경계 — 8/22·8/23 만기 종목 (모수 밖) / 8/24 당일 만기 (모수) | 14 / 20 |
| **공식 예시 #1 모수** — 구매가능 AND AA- 이상 (종목) | **15,792** |
| 신용등급 결측 행 / 비율 | 4,020 / 18.4% |
| 신용등급 결측 분해 — 국공채(미부여) / 특수채 / 회사채(미수록) | 2,840 / 254 / 926 |
| 신용등급 있는 행 (등급 조건 질의 모수 · yaml `answerable_n`) | 17,862 |
| 신용등급 표기 종수 (데이터 실재) | 15 |
| 위험등급 `'00'`(해당없음) 행 / 6등급 `'16'` 행 | 19 / 8,929 (40.8%) |
| 발행사 `pd_pbcm` distinct (TRIM) | 1,818 |
| 발행국 KR / 통화 KRW 행 | 21,881 / 21,881 |
| 할인채 행 (`bd_intp_tcd`) / 그중 `srfc_irt`>0 | 689 / 668 |
| 영구채 행 / 종목 (종목명 신종·영구) | 266 / 237 |
| 되묻기 규모 — 구매가능 1등급 종목 / C0 종목 | 1,394 / 103 |

> 신용등급 결측 41.1%·41.6% 는 **1차(7/11) 데이터** 수치다 — 2차는 위 값. 국공채는 등급 '미부여'(정상), 특수채·회사채 결측은 '미수록' 으로 말한다.

#### 국내채권 결측 유형 표 (2차 재실측) — 1차 EDA 7유형 대비

| # | 유형 | 2차 실측 | 1차 EDA(7/11) | 판정 · 선언 자리 |
| :-: | :-- | :-- | :-- | :-- |
| ① | 구조적 부재 — 국공채 신용등급 (해당없음) | 국공채 2,840행 중 결측 2,840 (완벽 대응) / 전체 결측 4,020 | 16,044 | `crd_grd.missing_reason: not_applicable` · `answer_policy` |
| ①′ | 같은 컬럼의 미수록 — 특수채·회사채 등급 결측 | 254 + 926 = 1,180 | (미분리) | 같은 NULL 을 '미수록' 으로 따로 답함 |
| ② | 상태 표현 — 판매정보 **13컬럼이 한 덩어리로** 없음 = 지금 매물 아님 | 13컬럼 동시 NULL 21,248 = 하나라도 NULL 21,248 (완전 일치) · 값 있는 634행은 전부 장외 634 (`buyable_quantity`>0 296) | 41,513 / 881 | `buyable_quantity` 무효 · 규칙 `구매가능` 은 만기로 판정 · 규칙 `판매행` · `answer_rules` 모수 고지 |
| ③ | 미산출 — 평가정보 **8컬럼이 한 덩어리로** 없음 | 8컬럼 동시 NULL 16 = 하나라도 NULL 16 (완전 일치) · 전부 장외 · 그중 중복종목의 장외행 8 · **만기 경과와 교집합 0** (별건: 만기 경과 67행) · `remaining_days` NULL 5(= 만기일 미수록분) | 13,376 / 10,645 | 규칙 `듀레이션정상` — 🔴 1차의 '만기 경과분 미산출' 이라는 유형 이름은 2차에 맞지 않는다(그 16행은 만기가 미래다) |
| ④ | 복구 가능 — 평가사 등급 컬럼 병합 | 컬럼 없음 → 소멸 | 1,600 | 2차 스키마에서 유형 소멸 |
| ⑤ | 센티넬·위장 결측 | `avg_annual_tax_yield` 0·NULL 21,882(전량) · `curr_cd`='000' 1 · `dur`≥90 3 · `mat_dt` 0 5 | 전량 0 · 99 · 99991231 | `zero_as_missing` 11컬럼 · 게이트 `curr_cd` 상수 · 규칙 `외화채없음` |
| ⑥ | 복사 위장 — `dirty` = `eval_price` | 21,655 / 21,866 | 27,430 | 금지 컬럼 (조립기 숨김) |
| ⑦ | 정상값 오해 — 비어 보이지만 값 | `srfc_irt`=0 579 (할인채·주식연계) · `pd_risk_gcd`='00' 19 | 2,758 | 규칙 `무이자질의` · `range_by_table` 0~6 |

> 1차 열은 `docs/eda/domestic_bonds_notes.md` §B.2 의 42,394행 기준 값(재실측 아님) — 비교용으로만 싣는다. 판정 순서: ⑦ 정상값인가 → ⑤⑥ 위장인가 → ④ 복구 가능한가 → ①②③ 어떤 부재인가.

#### 국내채권 결측 블록 (2026-09-04 전수 실측) — 결측의 단위는 컬럼이 아니라 행

| 블록 | 컬럼 수 | 통째로 빈 행 | 값이 있는 행 | 검증 |
| :-- | --: | --: | :-- | :-- |
| 판매정보 (`buy_yield`·세후·법인·예금환산·매매단가·판매채널·판매기준일…) | 13 | 21,248 | 634 (전부 장외 LOT) | 동시 NULL = 하나라도 NULL (21,248 = 21,248) |
| 평가정보 (`dur`·`cov`·`dirty`·`ndy_*` 5) | 8 | 16 | 21,866 | 동시 NULL = 하나라도 NULL (16 = 16) · 만기 경과와 교집합 0 |

> 판정: 이 13+8컬럼의 NULL 은 컬럼 21개의 결함이 아니라 **사실 두 개**다 — '지금 파는 물건이 아니다' 와 '장외 평가정보가 없다'. 그래서 답변 정책도 컬럼별이 아니라 블록별로 하나씩이다: 판매 축 질의는 모수 634/21,882 를 밝히고, 평가 축 질의는 규칙 `듀레이션정상` 이 16행을 정렬에서 뺀다.

## 2. 외부 수집 (L2)

| 수집물 | 행 | 커버리지 | 기준일 |
| :-- | --: | :-- | :-- |
| 국내 ETF 구성종목 | 75,859 | ETF 1,160/1,235 (93.9%) | 2026-08-21 |
| 해외 ETF 구성종목 | 906,848 | ETF 1,356/5,972 (22.7%) | 2025-10-31~2026-06-30 (8종) |
| 펀드 구성종목 | 59,206 | — | 행별 `bas_dt` |
| 펀드 웹 페이지 | 10,565 | — | — |

> 🔴 해외 구성종목 보고기준일이 **2025-10-31~2026-06-30 로 8종** — 최대 8개월 시차. 답변에 `report_date` 병기 필수.

## 3. 온톨로지 · 지식그래프

| 항목 | 값 |
| :-- | --: |
| `kg_node` | 41,580 |
| `kg_alias` | 66,591 |
| `kg_edge` | 7,414 |
| `kg_closure` | 9,966 |

### 개체 종류별 노드

| 개체 | 노드 수 |
| :-- | --: |
| Security | 27,996 |
| Fund | 7,584 |
| Index | 3,172 |
| Organization | 2,526 |
| FundAttribute | 179 |
| Region | 60 |
| CreditGrade | 22 |
| Country | 17 |
| AssetClass | 9 |
| Currency | 8 |
| RiskGrade | 7 |

> KG `Fund` 7,584 = `rptt_ksd_itm_no` 묶음 6,867 + 마스터 밖 MotherFund 717. 🔴 펀드단위 집계 키 `(or_co_xtn_itt_cd, mtco_itm_no)` 와는 **다른 축**이다 — 집계·순자산은 복합키, 개체 조회는 rptt.

### 제출 ttl 5분할 (규격 p.9)

| 파일 | 줄 |
| :-- | --: |
| `ontology/common.ttl` | 58,971 |
| `ontology/bond_kr.ttl` | 36 |
| `ontology/etf_kr.ttl` | 34 |
| `ontology/etf_gl.ttl` | 21 |
| `ontology/fund_pub.ttl` | 36 |

## 4. 평가셋

- `questions_cross_and_unanswerable.jsonl`: 9문항 (사람 검증 8)
- `questions_domestic_bonds.jsonl`: 46문항 (사람 검증 46)
- `questions_domestic_etfs.jsonl`: 36문항 (사람 검증 31)
- `questions_final30.jsonl`: 30문항 (사람 검증 0)
- `questions_official_sample.jsonl`: 8문항 (사람 검증 6)
- `questions_overseas_etfs.jsonl`: 28문항 (사람 검증 27)
- `questions_public_funds.jsonl`: 20문항 (사람 검증 0)
- `questions_top_safety_probe.jsonl`: 10문항 (사람 검증 10)

**합계 187문항 · 사람 검증 128건**

