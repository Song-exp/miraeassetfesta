# 재검 2026-09-02 라운드 1 — A 수리 코드 리뷰 (에이전트 B · 배포 전)

> 대상: `git diff 0dae625..04df729 -- src ontology tests` (A 커밋 7개: 84e094d P1 · c8f3f12 P2 · d9ee7ec P3 · 73f06b3 P4 · e4d808e P5 · cbd8cbd P6 · 04df729 P7).
> 근거 문서: `docs/domain/public_funds.md`(도메인 판단 우선) · `docs/recheck_2026-09-02_round1.md`(내 1라운드 수정안).
> 전부 실측 — DB `data/financial_products.db` 직접 실행 · `answer_question` 에 가짜 플래너를 물려 가드 체인 통과 후 SQL/답변 입력/trace 를 읽었다.
>
> **기준선 재확인**: pytest **264 passed** · `eval/run_gold_check.py` **137/137 통과** · heredoc `\b`→0x08 손상 — 최종 5개 파일(pipeline.py·router.py·main.py·public_funds.yaml·test_guard_v2.py) 제어문자 **0건** (P2 복구 확인).
>
> **총평**: 결정층 처방 7종은 의도대로 내려갔고 정답 경로(R1~R7 + 003·019·021·023 랭킹 + 016·032·034·038 계열) 회귀 없음. 다만 **발동 조건이 넓어서 정상 질의에 오개입하는 가드 3건**(`_NAME_FILTER` 오탐 · 분포 조립기의 top-N 오발동 · 전사 교정의 다른 상품 치환)과 **기계 조립 문자열의 거짓 문장 2건**(사모 질의에 "공모펀드" · 합산 문장) 이 있어 **조건부 승인**. ②의 🔴 5건은 전부 1~5줄 수리라 배포 전에 닫는 것을 권한다. R6 펀드키는 **규약 자체의 결함**으로 판정(④) — 배포 전 키 변경은 금지, 배포 후 1순위 KG 과제.

---

## ① 항목별 판정

| 항목 | 판정 | 근거 실측 |
| :-- | :-- | :-- |
| **P1** GROUP BY 주입 + `_FUND_KEY_EXPR` COALESCE | ✅ 승인 (ROUND 1건 🟡) | 발동 조건 확인: `_FUND_TBL`(`FROM public_funds`) 단독 · JOIN/UNION 배제 · `_fund_sort_target` 랭킹 컬럼 · GROUP BY 부재 · SELECT 에 COUNT/SUM/AVG 없음 · 질문에 '클래스' 없음 — 전부 코드에 있고 테스트가 덮는다. **채권 SQL**(`FROM domestic_bonds ORDER BY … DESC`) 불개입 실측. bare SQL 5경로를 체인에 통과시켜 gold 형(펀드키 GROUP BY + MAX/MIN) 과 **행·값 완전 일치**: 003 1년 top5(387.66/362.53/361.3/257.14/256.89) · 019 3등급 top3(126.08/110.41/109.22) · 023 혼합형 top5(118.45…102.3) · 021 순자산 top5(삼성MMF법인제1호 12.4조…) · S2 3년 bottom3(MIN 경로 −80.36/−78.08/−77.8). 기본모수→대표행→기점오류 NOT IN→근거컬럼 순서가 GROUP BY 주입 뒤에도 WHERE 에 올바르게 들어간다. 서브쿼리(`> (SELECT AVG…)`)·`OFFSET`·다중 ORDER BY 키·`itm_no = …` 단일 조회 모두 유효 SQL. COALESCE 키: 기본모수 `COUNT(DISTINCT 키)` **3,040** 확인(테스트 포함). 🟡 **결함**: 정렬 컬럼이 함수 안에 있으면(`ROUND(fd_yr1_ern_r,2) … ORDER BY 2`) `ROUND(MAX(fd_yr1_ern_r) AS fd_yr1_ern_r,2)` 로 문법 오류 → "데이터 조회 중 오류" 무응답 실측. 과거 probe·paired 결과에 `ROUND(` 0건이라 저빈도. ②-5 |
| **P2** 개별 조회 펀드 묶기 + gcd 역방향 병기 | 🟡 조건부 승인 | R4 → 6행·본체 첫 행(클래스 10 · 189.77/187.09) · R6 → 6행(전 행 '높은 위험'·2, 클래스수 합 7) 테스트 통과 · gcd 역방향 병기 ✅ · '클래스'·'보수'·'수수료' 불개입 ✅ · 033 클래스 열거 11행 유지 ✅. **결함 1(🔴 ②-1)**: `_NAME_FILTER` 가 `itm_nm … {0,40}자 … LIKE` 라 **SELECT 의 itm_nm 과 WHERE 의 다른 컬럼 LIKE** 가 40자 안에 들면 이름 조회로 오인 — `SELECT itm_no, itm_nm FROM public_funds WHERE or_attr_desc LIKE '%주식%' …`("주식형 공모펀드 알려줘") 가 **개별 조회 묶기**로 발동해 `ORDER BY 최단 이름` → 표시 30펀드 **전부 역외 1클래스 펀드**('젬스 리커버리'·'글로벌주식펀드'…) 실측. `zrin_attr_nms LIKE`·`itm_nm NOT LIKE` 도 같은 오탐. 목록 묶기(P3-b, 순자산순)가 대신 발동해야 할 경로다. **결함 2(🟡 ②-6)**: 수치 컬럼 일괄 `MAX/MIN … _최고/_최저` — `fd_nast_suma` 가 `734810470588.0 / 1948080000.0`(원, 소수점 .0, 억원 병기 없음) 로 답변 입력에 실린다 — 자릿수 훼손 계열 재료. 도메인상도 틀림: 이 DB 의 `fd_nast_suma` 는 **클래스별 값**(다클래스 키그룹 1,387개 중 값 상이 1,360 · 코어테크 본체 10클래스 합 2조9,148억 vs 최대 클래스 7,348억) 이라 "펀드 순자산" 은 SUM 이 정답. `zrin_fd_ivst_risk_gcd` 는 schema 타입이 text 라 `MAX(gcd) AS gcd` 로 나가 무해(우연). **A 의 P2 기본모수 건너뜀은 타당** — 이름 조회에 판매중·공모를 박으면 판매완료·사모 14,707행 개별 조회가 0행 오거절. 대신 `SUM(sale_yn='판매중') AS 판매중클래스수` 병기(②-7)가 무해한 보완. R6 기본모수 없는 경로 실측: 6행 전부 판매중이라 이번엔 무해 |
| **P3-a** 커버리지 병기 | ✅ 승인 | R3 → `(조회 결과: 전체 560행 / 248펀드 중 30펀드 표시 — …)` · LIMIT 미도달(5행)엔 미발동 · 타 도메인은 행수만 · 분포 집계는 None. SQLite 1회 재실행 확인 |
| **P3-b** 목록 펀드 묶기 | ✅ 승인 (P2 오탐에 가려짐) | R3 → 30개 서로 다른 펀드 · 1행 KB중국본토A주 14클래스 1,453억원 ✅. 단 위 `_NAME_FILTER` 오탐 때문에 유형·태그 목록의 상당수가 이 가드에 **도달하지 못한다**(②-1 수리로 해소). `ORDER BY MAX(fd_nast_suma)` 는 '최대 클래스' 순 — 펀드 합계(SUM) 순과 다를 수 있으나 대표행 선정 목적엔 허용(gold FND-001 도 MAX) |
| **P3-c** 상품명 전사 교정 | 🟡 조건부 승인 | FOSS→FOCUS ✅ · 정확 일치 무변경 ✅ · 서술 토큰 불개입 ✅ · 채권 `국고채권03125…`/`01500…` 불개입 ✅ · 긴 숫자 토큰(순자산 원 단위·기준일) 불개입 ✅ · A 의 3-c "0.6 미만 줄 제거" 미적용은 **옳은 판단**(한국어 서술 8자 토큰이 문장을 죽인다). **결함(🔴 ②-4)** — 실측 2건: ① `'삼성KODEX200TR증권상장지수투자신탁' → '삼성KODEX200증권상장지수투자신탁'`(유사도 0.95) — **다른 실제 상품**으로 치환. 같은 계열: 1호/2호/3호 · 1.5배/2배 · H/UH · (주식)/(채권) 접미 — 상위/하위 문자열 관계이거나 숫자만 다른 짝은 전부 별개 펀드다(§3.3 종목명 구조). ② `"…신탁3호는 조회되지 않았습니다"` → `"…신탁2호 조회되지…"` — **조사 '는' 삭제 + 부정문의 주어가 조회된 상품으로 뒤바뀜**(의미 반전). 토큰 정규식이 조사를 삼킨다. 4도메인 공통 자체는 안전(이름 컬럼 없으면 불개입 · ETF 테이블은 `_NAME_COL` 에 해당 컬럼명이 없어 사실상 불발) |
| **P3-d** 내부코드 숨김 | ✅ 승인 | `prfd_attr_cds` 답변 입력에서만 제거 · retrieved_context 보존 · 유일 컬럼이면 유지 ✅ |
| **P4** 개수 답변 기계 조립 | 🟡 조건부 승인 | R5 → `143개(클래스 541개) (판매중·공모 기준, 기준일 2026-08-22)` + 합산 1줄 · HCX 0회 ✅. **결함(🔴 ②-3)** 실측 2건: ① "한국투자신탁운용 **사모**펀드는 몇 개야?" → `_POP_WIDEN` 이 기본모수를 막아 SQL 은 `prvo_pbff_desc='사모'` 인데 답은 **"조회 조건에 해당하는 공모펀드는 265개(클래스 273개)"** — 라벨이 SQL 과 모순. ② 합산 문장이 **ground_lines 의 코드 수**로만 판단 — SQL 이 `='00040024'` 한 코드만 썼는데 KG 가 2코드면 `142개 … 운용사 코드 2건을 합산했습니다` 거짓 문장 실측. 부수: 답에 운용사 이름이 없다("조회 조건에 해당하는") — 채점자는 질문을 함께 보므로 치명은 아니나 ground 라벨을 넣으면 완결적(③) |
| **P5** 분포 3열 + 조립기 | 🟡 조건부 승인 | R1 → 19범주 · 클래스 8,969 · 펀드 3,040 · 복수 범주 182 · 합(3,222) 문구 · 2열 회귀 보호 ✅. 도메인상 "미수록도 범주" 로 182 를 설명한 것은 맞다. **결함(🔴 ②-2)**: 발동 조건이 "GROUP BY + (라벨, COUNT(*))" 뿐이라 **운용사 top-N**(`SELECT or_co_xtn_itt_cd, COUNT(*) … GROUP BY 1 ORDER BY 2 DESC LIMIT 5`, JOIN 없이 나온 R2 형)에 3열이 붙고 조립기가 통째로 답을 만든다: `"조회 결과 5개 범주 · 클래스 4,486개 · 펀드 3,040개 … 클래스별 유형이 갈리는 펀드 1,632건은 복수 범주에 계수되어 범주별 펀드 수의 합(1,408)은 전체 펀드 수(3,040)와 다릅니다"` — **top5 절단을 '복수 범주' 로 설명한 조작 통계** + 코드만 나열(이름 없음). 2열 조립기(FND-038 수리) 때부터 있던 오발동이지만 3열 확장이 거짓 문장을 추가했다. 축이 개체(`or_co_xtn_itt_cd`)이거나 명시 LIMIT < 30 이면 분포가 아니다 |
| **P6** 면책 패턴 + 거짓 유보 | ✅ 승인 | R2 꼬리 2문장 제거·값 5줄 보존 ✅. 정당한 주의문 보존 실측: `"과거 수익률은 미래 수익을 보장하지 않습니다. 레버리지 펀드는 변동성이 큽니다."` **유지** · `"더 많은 펀드가 있을 수 있습니다"`·`"자세한 내용은 운용사에 문의하세요"` 제거. `자세한 (내용|사항)은` 을 꼬리 요구(문의·확인·상담·참고·참조)로 좁힌 A 조정 타당("자세한 내용은 다음과 같습니다" 보존 테스트 있음). 🟢 관찰: P2 로 개별 조회에도 GROUP BY 가 생겨 `strip_false_hedge` 가 개별 조회 답변에도 켜진다 — `일부만`("클래스 일부만 판매중") 류 정당 문장이 지워질 수 있음(③) |
| **P7-a** 라우터 '펌드' | ✅ 승인 | '공모펌드'·'코어테크 펌드' → public_funds · '펀 드' 미특정을 테스트로 못 박음 ✅ |
| **P7-b** SQL 사후 라우팅 보정 | ✅ 승인 | 미특정 → `FROM public_funds` 단일이면 tables 갱신 + `residual_name_token` 재계산("미래에셋코어테크 종류A" 미특정 경로에서 `[Ground] 잔여 상품 고유명 '코어테크' (사후 보정 경로)` → 이름 필터 주입 실측). **역방향**: 미특정 → `FROM domestic_bonds` → `[Route] SQL 사후 보정 — FROM domestic_bonds` → 채권 답변 규칙만 적용, 펀드 가드 0건 발동 실측(FROM 기준이라 원래 안전). ext_* 조인은 `TABLES` 밖이라 `used` 집합을 흔들지 않음 |
| **P7-c** 재시도 가시화 | ✅ 승인 | `attempt == 2` 에만 `0. [Retry]` 접두 · 1차 성공 경로 무표시 테스트 ✅ |
| **KG yaml** 펀드단위·워크드 예시 COALESCE | ✅ 승인 | 코드 `_FUND_KEY_EXPR` 와 동형 — 프롬프트/가드/gold 3자 정합 |

**회귀(리뷰 관점 3)**: R1~R7 은 테스트가 파이프라인 통합으로 덮는다(`test_r3_pipeline_markers`·`test_count_answer_assembled`·`test_distribution_fund_count_three_columns`·`test_post_route_correction_from_sql`). §6-2 표의 016(=R4 경로)·032(=R6)·034(=R5)·038(=R1) 은 그 테스트에 포함, 019·021·023 은 위 체인 실측으로 gold 형 SQL 과 일치 확인. `test_fund_rank_representative`(FND-015 GROUP BY 기존 경로)·`test_fund_distinct_count_replaced`(207/850) 유지.

---

## ② 배포 전 반드시 고칠 것 — A 용 체크리스트 (전부 `src/runtime/pipeline.py`)

### 🔴 ②-1 `_NAME_FILTER` 오탐 — 유형·태그 목록이 개별 조회 묶기로 빠짐
- **함수**: `_NAME_FILTER`(모듈 상수) · `ensure_fund_lookup_grouping` · `ensure_fund_list_grouping`
- **바꿀 로직**: 이름 필터 판정을 **WHERE 절 안에서, 좌변이 itm_nm 인 LIKE/GLOB** 으로 좁힌다.
  - 검색 범위: `sql[frm.end():]`(FROM 뒤)만.
  - 패턴: `(?:REPLACE\(\s*itm_nm\s*,\s*' '\s*,\s*''\s*\)|\bitm_nm\b)\s*(?:NOT\s+)?(?:LIKE|GLOB)\b` — 좌변이 itm_nm 인 경우만. (`NOT LIKE` 는 제외 필터라 이름 조회가 아니다 — `NOT\s+` 가 붙으면 불일치로 두는 편이 안전.)
  - 같은 40자 창을 쓰는 기존 `_ITM_NM_LIKE`(`ensure_fund_name_filter` 의 "이미 이름 필터 있음" 판정)도 같은 패턴으로 통일 권장(부수).
- **회귀 테스트**: `SELECT itm_no, itm_nm FROM public_funds WHERE or_attr_desc LIKE '%주식%' AND sale_yn='판매중' AND prvo_pbff_desc='공모' LIMIT 30` + "주식형 공모펀드 알려줘" → `ensure_fund_lookup_grouping` **불발** · `ensure_fund_list_grouping` **발동**(`ORDER BY fd_nast_suma DESC`) · 첫 행이 역외 1클래스 펀드가 아님. `SELECT itm_nm FROM public_funds WHERE zrin_attr_nms LIKE '%중국%' LIMIT 30` 동일. R4·R6 SQL 은 종전대로 lookup 발동.

### 🔴 ②-2 분포 조립기 top-N 오발동 — 조작 통계 문장
- **함수**: `ensure_fund_distribution_fund_count` · `_distribution_answer`
- **바꿀 로직**:
  - 두 함수 모두 불개입 조건 추가: ⓐ SQL 에 `ORDER BY` 와 **명시 LIMIT k < MAX_ROWS** 가 함께 있음(top-N 꼴) ⓑ GROUP BY 축(첫 SELECT 항목)이 `or_co_xtn_itt_cd`·`itm_no`·`mtco_itm_no` 등 **개체 식별 컬럼**(분포 축은 `*_desc`·`*_nm`·`*_cd` 분류 컬럼).
  - `_distribution_answer` 3열 머리줄의 "펀드 N개(전체 DISTINCT)" 와 "복수 범주 …" 문장은 **n < MAX_ROWS(절단 없음)** 일 때만. 절단이면 머리줄을 `조회 결과 상위 {n}개 범주(전체 중 일부) · …` 로 굽고 복수 범주 문장 생략.
- **회귀 테스트**: `SELECT or_co_xtn_itt_cd, COUNT(*) FROM public_funds WHERE sale_yn='판매중' AND prvo_pbff_desc='공모' GROUP BY 1 ORDER BY 2 DESC LIMIT 5` + "펀드를 가장 많이 운용하는 운용사 상위 5개" → 두 함수 불발(HCX 답변기로 감) · R1 SQL(LIMIT 30 · `zrin_btyp_nm` 축) 종전 결과.

### 🔴 ②-3 `_count_answer` 거짓 문장 2종
- **함수**: `_count_answer`
- **바꿀 로직**: ⓐ 라벨 — `prvo_pbff_desc = '사모'` 면 "사모펀드", `'공모'` 면 "공모펀드", 둘 다 없으면 "펀드"(테이블이 공모펀드 마스터라도 사모 행 1,993개가 있다). ⓑ 합산 문장 — `_ORG_CODES` 로 뽑은 ground 코드 중 **SQL 문자열에 실제로 등장하는 코드**만 세어 2개 이상일 때만 붙인다. ⓒ(권장) ground 라벨(`'한국투자신탁운용' →`)을 주어로: `한국투자신탁운용이 운용하는 공모펀드는 143개(클래스 541개)입니다`.
- **회귀 테스트**: 사모 SQL → "사모펀드는 265개(클래스 273개)" · SQL 코드 1건 + ground 2코드 → 합산 문장 없음 · R5 종전.

### 🔴 ②-4 `verify_product_names` 다른 상품 치환·조사 삭제
- **함수**: `verify_product_names._fix`
- **바꿀 로직**: ⓐ 토큰 끝 조사를 `_PARTICLE`(모듈에 이미 있음)로 떼어 **어간만** 대조하고 치환 시 조사를 되붙인다. ⓑ 근사 후보 `close` 에 대해 **치환 금지 조건**: `tok in close or close in tok`(상위/하위 문자열 — TR·2호·H/UH 계열은 별개 상품) **또는** 두 토큰의 숫자열(`re.findall(r'\d+')`)이 다름(1호/2호·1.5배/2배·200/200TR). 이 두 조건을 통과한 것만 치환(FOSS→FOCUS 는 통과: 부분문자열 아님·숫자 없음).
- **회귀 테스트**: `"삼성KODEX200TR증권상장지수투자신탁"` + rows(KODEX200) → 무변경 · `"…신탁3호는 조회되지 않았습니다"` + rows(2호) → 무변경 · `"…신탁2호는 …"` + rows(2호(주식)C2) → 무변경(정확) · FOSS 케이스 종전 교정.

### 🔴 ②-5 함수 안 정렬 컬럼 — 문법 오류 무응답 (1줄)
- **함수**: `ensure_fund_rank_representative` GROUP BY 부재 분기(GROUP BY 존재 분기도 동일 코드)
- **바꿀 로직**: `m` 앞 문자가 `\w+\(` 로 끝나면(함수 인자 위치) `{agg}({col})` 만 넣고 별칭은 붙이지 않는다 → `ROUND(MAX(fd_yr1_ern_r),2)`. 위치 ORDER BY 는 그대로 유효.
- **회귀 테스트**: `SELECT itm_nm, ROUND(fd_yr1_ern_r,2) FROM public_funds WHERE sale_yn='판매중' AND prvo_pbff_desc='공모' ORDER BY 2 DESC LIMIT 5` → DB 실행 성공 · 값 387.66 첫 행.

### 🟡 ②-6 개별 조회 묶기의 순자산 — SUM + 억원 (권장)
- **함수**: `ensure_fund_lookup_grouping`
- **바꿀 로직**: `fd_nast_suma` 는 `_최고/_최저` 대신 `SUM(fd_nast_suma) AS fd_nast_suma`(펀드 합계 — 이 DB 는 클래스별 값) 로 두고, 뒤의 `ensure_fund_evidence_columns` 억원 병기가 그 별칭을 잡게 한다(현재 억원 병기는 정렬 대상일 때만 — 조건을 "SELECT 에 fd_nast_suma 별칭 존재" 로 넓히거나 이 가드에서 `printf('%d억원', SUM(fd_nast_suma)/1e8)` 를 직접 굽는다). `_최고/_최저` 는 수익률 8종에만.
- **도메인 근거**: `public_funds.md` §3.1 "순자산 클래스마다 동일" 은 **이 DB 와 맞지 않는다** — 삼성MMF법인제1호 4클래스가 12.4조 / 1,051억 / 2,012억 / 274억(문서는 "전부 12.44조"). 문서 정정 필요(③).

### 🟡 ②-7 개별 조회 묶기에 `판매중클래스수` 병기 (권장 · P2 기본모수 미확장의 보완)
- `SUM(CASE WHEN sale_yn='판매중' THEN 1 ELSE 0 END) AS "판매중클래스수"` 한 항목 추가 — 0행 오거절 위험 없이 "클래스 7개 중 판매중 7개" 재료.

---

## ③ 배포 후 관찰할 것

1. **S1 "1년 수익률 가장 높은 공모펌드 5개"** — 현행 키로 4·5위가 **같은 펀드**(NH-Amundi 1.5배 Ci 257.14 · Ae 256.89 — mtco `DP067`/`00DP058`, 대표예탁원번호는 둘 다 `0325700DP058`). 답이 5위를 "다른 펀드"로 말하면 ④ 키 결함의 첫 실전 노출. gold(FND-003)도 같은 키라 채점상은 일치 — 주최 정답이 다를 수 있는 지점.
2. **S9 피델리티** — 00080029 106펀드(rptt 기준 37) + 역외 00130001. 키 결함이 가장 크게 부풀리는 운용사(3배).
3. **R6 재검** — P2 이후에도 **6행**(클래스마다 mtco 상이)이 답변기에 간다. 답이 "6개 펀드" 로 나열하면 ④.
4. `strip_false_hedge` 의 `일부만` — 개별 조회 답변("클래스 일부만 판매중") 오삭제 여부.
5. `_DISCLAIMER` `자세한 내용은 … 참고` — 표 앞 안내문("자세한 내용은 아래를 참고") 삭제는 무해하나 빈 줄 처리 확인.
6. 미특정 → 채권/ETF 사후 보정 빈도(trace `[Route] SQL 사후 보정 — FROM domestic_bonds`) — 펀드 질의가 채권으로 새는 반대 사고는 가드가 아니라 SQL 생성 문제이니 별도 집계.
7. HCX 가 `ROUND(`·`printf(` 를 정렬 컬럼에 쓰는 빈도(②-5 미적용 시 무응답 지표).
8. 3열 분포 답변의 "복수 범주 182건" 문장 — 채점자가 오히려 혼란스러워하면 축약 검토.
9. `_count_answer` 에 운용사 이름 없음(②-3ⓒ 미적용 시).
10. **도메인 문서 정정 큐**: `public_funds.md` §3.1·§4.8 순자산 서술(클래스별 값임) · §3.4 "mtco_itm_no = 운용 단위(모펀드) 키" 서술(④ — 운용사 21곳 이상이 클래스마다 번호를 매김).
11. gold 재검 중 `fd_yr1_ern_r > -100` 하한: 가드는 NOT IN 3클래스만 주입하고 −100 하한은 주입하지 않는다(gold 는 둘 다). KCGI 베트남(−4,382)이 기본모수 밖이라 현재 무해 — 판매중 전환 시 bottom-N 에 노출.

---

## ④ R6 펀드키 결함 — 판정과 권고

### 판정: **이 펀드만의 특이가 아니라 펀드키 규약 자체의 결함** (KG/키 결함 확정)

실측(기본모수 8,969행):

| 식별 기준 | distinct | 비고 |
| :-- | --: | :-- |
| 현행 키 `운용사/zero-pad mtco_itm_no (COALESCE itm_no)` | **3,040** | gold·테스트·HANDOFF 기대값의 정본 |
| `rptt_ksd_itm_no` 대표예탁원종목번호 (`public_funds.md` §3.4: "여러 클래스의 **대표** 번호") | **1,809** (+NULL 110 역외 → 1,919) | 결측 110행 = 역외 전부 |
| 이름 줄기(첫 괄호 앞 · 공백 제거 · 운용사별) | 1,986 | §3.3 종목명 구조의 직관적 펀드 |

- **쪼개짐**: 대표번호 그룹 **380개**가 현행 키로 **1,516개**로 갈라진다(이름 줄기 기준 428 → 1,653). 솔로몬 2호(531101~531107, rptt 는 전부 `031910531100`)는 그 하나일 뿐.
- **운용사 편중** — 클래스마다 `mtco_itm_no` 를 새로 매기는 운용사가 있다: 우리자산운용 235 → rptt **88**(우리Best중소형 1펀드 10클래스 = mtco 000Q376~Q385) · iM에셋 205 → **29** · 피델리티 106 → **37** · 미래에셋 823 → 407. 삼성(207→179)·한투(142→120)는 근접. **→ R2 "펀드를 가장 많이 운용하는 운용사 top5" 의 2위 우리자산운용(235)·4위 iM에셋(205)은 키 산물**이다. rptt 기준 top5 는 미래에셋 407 · 삼성 179 · 00040035 123 · 한투 120 · 00040067 91.
- **랭킹 누출**: 1년 top5 에서 NH-Amundi 1.5배가 4·5위 중복(③-1). 3년 bottom3 · 순자산 top3 은 두 키가 같다.
- **개별 조회**: R6 는 P2 를 거쳐도 6행(rptt 로 묶으면 1행). 코어테크(R4)는 두 키 모두 6행(영향 없음).
- **분포**: 한투 143 → rptt 121 · CHN 248펀드 → 106.
- rptt 의 결함: 역외 110행 NULL(COALESCE 로 해결 가능) · 더미 mtco(`00`·`9999999999`) 12키가 여러 rptt 를 합치던 것은 rptt 가 오히려 바르게 가른다 · H/UH 쌍은 rptt 가 분리(수익률이 다르니 랭킹엔 옳음).

### 권고

1. **배포 전에는 키를 손대지 않는다** — gold_sql 137 · 테스트 · HANDOFF 기대값 · yaml 펀드단위 규칙이 전부 현행 키에 묶여 있고, 주최 정답 규약(운용사 종목번호인지 대표번호인지 행수인지)이 미상이라 한쪽으로 바꾸는 것 자체가 도박이다. A 의 "키는 손대지 않고 테스트는 값 일치·클래스수 합" 처리는 옳다.
2. **배포 후 1순위 KG/평가 과제로 등재**: `COALESCE(rptt_ksd_itm_no, 현행키)` 를 후보 키로 gold 를 병렬 재산정(FND-003·FND-001·운용사 top5·운용사 개수 계열이 갈린다 — 위 표) 하고 리드 판정으로 정본을 정한다. 판정 근거로 이 절의 수치를 쓴다.
3. **정본 확정 전 무해한 완화 2건**(선택): ⓐ 개별 조회 묶기(`ensure_fund_lookup_grouping`)만 `GROUP BY COALESCE(rptt_ksd_itm_no, 현행키)` — 카운트·랭킹 gold 와는 무관한 경로라 회귀 없고 R6 가 1행이 된다(R4 는 6행 그대로). ⓑ 개수·분포 조립 문자열에 기준 고지 한 토막 굽기: `(펀드 = 운용사 종목번호 기준, 클래스 = 판매 단위)` — 주최가 다른 기준을 써도 "무엇을 셌는지" 는 답에 남는다.
4. `docs/domain/public_funds.md` §3.4 의 "`mtco_itm_no` = 운용 단위(모펀드) 키" 서술을 "운용사 내부 번호 — 운용사에 따라 펀드 단위이기도, 클래스 단위이기도 하다(실측 표)" 로 정정하고, 대표 키 후보로 `rptt_ksd_itm_no` 를 명기한다.
