# 재검 계열 11라운드 채점 — 2026-09-03

- **원본**: `eval/probe_all_2026-09-03_r11_recheck.json` (93문항)
- **직전 원본**: `eval/probe_all_2026-09-03_r9_recheck.json` · 직전 판정 `docs/recheck_2026-09-03_round9.md` §① + `eval/verdicts_2026-09-02.json`
- **배포 커밋**: `92bcb5a` (= 로컬 HEAD · `git diff HEAD 92bcb5a -- src/` 무차이 확인 → 코드 근거는 로컬 파일로 읽었다)
- **바이트 대조**: 93건 중 **55건 답변 바이트 동일**(판정 승계 · gold 재실측만) · **38건 변동**
- 🔴 **응답시간은 판정 근거로 쓰지 않았다.** `elapsed_s` 는 표에도 싣지 않는다. 이번 배치 트레이스에 **HCX 재생성 마커는 0건**이다(9R 3건 → §④-1).

## 요약

| | 9R | 11R | 변화 |
| :-- | --: | --: | :-- |
| ✅ | 57 | **67** | 🟢 회수 12(❌→✅ 8 · 🟡→✅ 4) · 🔴 회귀 1(V12) · ⚙ 판정 정정 1(S12) |
| ✅값/🟡 | 27 | **24** | |
| 🟡 | 0 | **1** | V12(회귀) |
| ❌ | 9 | **1** | U14 만 남았다 |

**9R ✅ 57건 전수 재확인 결과: 56건 유지 · 1건 회귀(V12).**
**9R ❌ 9건 중 8건 회수** — U2·U3·U6·U8·U9·Y7·Y11·Y16. 남은 것은 **U14 하나**다.

10R 이 닫은 것: 부류 Z(가드 자기 무력화) · 클래스 축 rptt 전환 · 접두 앵커 · ETF 날조 술어 · 방언 토큰 · 자릿수.
10R 이 못 닫은 것: **랭킹 축이 펀드를 쪼갠다**(§③-4 — 이번 라운드 최대 잔여) · 개별 조회의 `COUNT(*)` 모수(§③-2) · 랭킹 GROUP BY 축 사후조건(§③-3).

---

## ① 판정 표

> `동일` = 답변 바이트가 9R 과 동일(판정 승계 · gold 재실측 완료).

### R·S 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| R1 | ✅ | ✅ | 유지 | 동일. 19범주·클래스 8,969·펀드 3,040 재실측 일치 — **rptt 전환이 분포 모수를 안 건드렸다** |
| R2 | ✅ | ✅ | 유지 | 동일. 823/235/207/205/142 + 377,707억 gold |
| R3 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 248(560) gold. 목록 표시 중복 잔존(§③-7) |
| R4 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | **접두 앵커 적용** — 리터럴 `코어테크`→`미래에셋코어테크`, 6→**2펀드**. gold 실측 2펀드 확정. 값 187.09~189.77 gold. 🔴 클래스 **9 → gold 10**(부류 E 개별조회 잔존 — §③-2) |
| R5 | ✅ | ✅ | 유지 | 동일. 143(541) gold |
| R6 | ✅ | ✅ | 유지 | 동일. 2등급 **7클래스** gold(rptt `031910531100`) |
| R7 | ✅값/🟡 | **✅** | 🟢 **회수** | 삼성KOSPI200 제1호 클래스 **4 → 7 = gold**. 387.66/362.53/361.3 gold · 클래스 6/4/7 **전수 gold**(rptt 축과 펀드키 축이 이 세 펀드에선 같다) |
| S1 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | 클래스 7 교정. 값 5건 gold. 🔴 **4·5위가 여전히 같은 rptt 펀드**(`0325700DP058`) + 클래스 1/4(실제 한 펀드 5) — §③-4 |
| S2 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 큰 개선) | 클래스 3/3/1 → **5/3/12**. `삼성배당주장기 1[주식]` 12 = gold(9R ②-3 승격분 해소). 🔴 1위 미래에셋아시아퍼시픽 **5 → 실제 10**(랭킹 축 과분할 — §③-4) |
| S3 | ✅ | ✅ | 유지 | 동일. 1/9클래스 · 105.49 · 106.71~109.72 gold |
| S4 | ✅ | ✅ | 유지 | 동일. 4펀드 9/11/14/3클래스 gold |
| S5 | ✅ | ✅ | 유지 | 동일. 2등급 **8클래스** gold |
| S6 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 58(135) gold. 목록 표시 중복(미래에셋인도중소형포커스 4줄) |
| S7 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 38(119) gold |
| S8~S11 | ✅ | ✅ | 유지 | 동일 (S11 377,707/331,097/278,196 gold) |
| **S12** | ✅ | **✅값/🟡** | ⚙ **판정 정정** | 동일 바이트. **회귀가 아니라 9R 채점 누락이다** — 클래스 9/4/3/5/12/4 인데 gold 는 **10/4/5/5/13/4**(3건 부분값). R4·W3 와 같은 부류 E 인데 9R 이 S12 만 놓쳤다. 값(수익률 6건)은 전부 gold |

### T 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| T1 | ✅ | ✅ | 유지 | 동일. 823(2,066)/235(403)/207(850)/205(240)/142(540) gold — **펀드 수 3,040 축 불변 확인** |
| T2 | ✅ | ✅ | 유지 | 동일. 377,707/331,097/278,196 gold (§⑥) |
| T3 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | `331098억원` → **`331,098억원`**(자릿수 구분자 — 10R D1). 🔴 같은 라운드의 T2·V5 는 같은 금액을 **331,097억원**으로 적는다(절사 vs 반올림) — §③-6 |
| T4 | ✅ | ✅ | 유지 | 동일. 인도네시아 1펀드 7클래스 gold |
| T5 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 31(79) gold |
| T6 | ✅ | ✅ | 유지 | **개별 조회 기계 조립으로 이동** — "미래에셋차이나솔로몬증권투자신탁 3(주식): 클래스 8개" = gold 8(rptt `031910532410`). 9R 의 클래스 기호 8개 열거는 사라졌으나 질문이 개수를 묻는다(§④-3) |
| T7 | ✅ | ✅ | 유지 | 동일. 9클래스 106.71~109.72 gold |
| T8 | ✅ | ✅ | 유지 | 동일. **14클래스** gold |
| T9 | ✅ | ✅ | 유지 | 동일. 1호 2클래스 gold |
| T10 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 19(42) gold |
| T11 | ✅ | ✅ | 유지 | 동일. 154(301) · 역외 47 분리 gold |
| T12 | ✅ | ✅ | 유지 | 대표명이 `NH-Amundi1.5배…(주식-파생형)` → **`NH-Amundi 1.5배…[주식-파생형]`**. 같은 rptt `0325700DP058` 5클래스 한 그룹이고 `MIN(TRIM(itm_nm))` 이 뽑은 표기라 결정적이다. 값·클래스수 gold |
| T13 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 98(333) gold |
| T14 | ✅ | ✅ | 유지 | **접두 앵커 적용** — `베트남그로스` 4펀드 → `한국투자베트남그로스` **2펀드**. 2,528억·766억 · 7/7클래스 **gold 실측 일치**. `연금`·`퇴직연금40` 탈락 = 9R §③-A 가 승인한 교정 |

### U 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| U1 | ✅ | ✅ | 유지 | 동일. 1,001/1,911/197/133억 · 9/11/14/3 gold |
| **U2** | ❌ | **✅** | 🟢 **회수** | "KB차이나그로스증권자투자신탁(주식): 클래스 **14개**" = gold(rptt `030230002C22` 14행). 트레이스에 `[Guard] 개별 조회 펀드 묶기` 복귀 + GROUP BY 가 rptt 확정식. **부류 Z 닫힘** |
| **U3** | ❌ | **✅** | 🟢 **회수** | 한화중국본토 **13클래스** = gold(rptt `030220157031`) |
| U4·U5 | ✅ | ✅ | 유지 | 동일 |
| **U6** | ❌ | **✅** | 🟢 **회수** | `zrin_btyp_nm = '채권형'` 단일값으로 축소. **우리 25,158억 10펀드 50클래스 · 키움 16,895 6 33 · KB 15,893 9 65 — 12개 숫자 전수 gold** |
| U7 | ✅ | ✅ | 유지 | 동일. 해외주식형 338(788)/106(112)/81(370) gold. **부류 Z 수리 후에도 유형 축 정상 발동**(회귀 감시 통과) |
| **U8** | ❌ | **✅** | 🟢 **회수** | **4,380,605 / 2,970,970 / 1,406,816 / 1,087,326 / 986,872 백만USD — 5건 전수 gold.** 날조 술어(`cu_charge_rt>0` · `NOT LIKE`) 소멸 + 천 단위 구분자. 9R `425,2800백만USD` 계열 해소 |
| **U9** | ❌ | **✅** | 🟢 **회수** | `[Guard] 방언 토큰 치환` 발동 → 오거절 해소. VOO 997,352 / IVV 889,430 / SPY 820,109 백만USD **gold**. (🟡 꼬리 권유 문장 1건 — §③-9) |
| U10 | ✅ | ✅ | 유지 | 6펀드 5/8/14/11/10/9 · 등급 2/3/2/2/2/3 gold. 머리 토큰만 `베트남`→`미래에셋베트남` |
| U11 | ✅ | ✅ | 유지 | 3펀드 665/579/2억 · 12/13/1클래스 gold (§⑥). 이름 정렬만 바뀜 |
| U12 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | 0행이 정답. 🟢 **무관 후보 `삼성 베스트 MMF 법인 제1호` 소멸**. 🔴 정당한 후보(`삼성아세안플러스베트남…` 2펀드 실재)도 안 나온다 — §③-8 |
| U13 | ✅ | ✅ | 유지 | 동일. 124,295/96,265/90,389/80,496/76,370억 · 클래스 4/2/2/1/2 — **rptt 축과 펀드키 축이 동일**해 §③-4 대상 아님 |
| **U14** | ❌ | **❌** | 유지(🟢 부분 개선) | 여전히 **운용사별** 클래스 수(미래에셋 1511·삼성 671·KB 515). gold: 한화2.2배 6 · NH-Amundi코리아2배 4 · 삼성KOSPI200 제1호 7. 🟢 9R 신규였던 "모두 합하면 2,587개" 조어 소멸. **이번 라운드 유일한 ❌** — §③-3 |
| U15 | ✅ | ✅ | 유지 | 앵커 적용 6→2펀드. 29,148억/31억 · 10/4클래스 **gold**(값 술어가 없어 R4 와 달리 10 이 맞게 나온다 — §③-2 의 대조군) |
| U16 | ✅ | ✅ | 유지 | 7클래스 gold. `_lookup_answer` → `COUNT(*)` 경로로 바뀌었으나 값 동일 |

### V 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| V1 | ✅ | ✅ | 유지 | 머리 토큰만 `인디아솔로몬…`→`미래에셋인디아솔로몬…`. **펀드 집합 불변 1펀드 3클래스 gold** — 9R §③-A 예측대로 |
| V2 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 6클래스 0.48~387.66 gold |
| V3 | ✅값/🟡 | ✅값/🟡 | 유지(🔴 부분 회귀) | 값 599.73~606.95 gold · 1펀드. 🔴 **머리 토큰이 `삼성KOSPI2002배레버리지` → `삼성KOSPI2002` 로 되돌아갔다**(9R 이 부류 Q 해소로 기록한 개선분) — §③-5. 🔴 클래스 **4 → gold 7**(부류 E) |
| V4 | ✅ | ✅ | 유지 | 동일. 1펀드 14클래스 2등급 gold |
| V5 | ✅ | ✅ | 유지 | 동일. 377,707/331,097/278,196/224,692/195,145 gold |
| V6 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 278,196억 gold |
| **V7** | ✅값/🟡 | **✅** | 🟢 **회수** | 머리줄에 **`(국내 상장 ETF 기준, 기준일 2026-08-24)`** 기계 표기(10R ③-11). 1,643,771/1,231,522/351,445억 gold |
| V8·V9 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 15(30) · 22(51) gold. 표시 중복 심함(§⑥ V9 — 키움러시아 5줄) |
| V10·V11 | ✅ | ✅ | 유지 | 동일. 58(135) · 229(962) gold |
| **V12** | ✅ | **🟡** | 🔴 **회귀** | "미래에셋코어테크 펀드는 클래스 수가 총 **10개**입니다." — SQL 은 **2행**(10·4)을 조회했는데 답이 1행만 옮기고 그것을 '총' 이라 부른다. 원인: SELECT 에 `MAX(mtco_itm_no)` 가 섞여 개별 조회 조립기의 `class_only` 판정이 깨졌다 → HCX 산문. **가드가 아니라 조립기 층이다** — §③-1 |
| V13 | ✅ | ✅ | 유지 | 동일. −2.63~−0.91 · 14클래스 gold |
| V14·V15 | ✅ | ✅ | 유지 | 동일. 98(333) · 92억 7클래스 gold |
| V16 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 큰 개선) | **③-10 기계 조립 적용됨**(9R 미적용 3건 중 1). 660.63/606.95/606.04/417.77/416.71 gold · 클래스 6/7/4/1/4 · MAX 축 고지 · 머리 이름이 펀드명. 🔴 S1 과 같은 4·5위 중복 — §③-4 |

### W 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| W1 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | 0행이 정답. 🟢 무관 후보 3건(삼성MMF법인제1호 등) 소멸. 🔴 정당한 후보(`삼성 CHINA2.0 본토…` 등 10펀드 실재) 미생성 — §③-8 |
| W2 | ✅ | ✅ | 유지 | 6펀드 109/114/735/76/368/6억 · 5/8/14/11/10/9 gold. 머리 토큰만 교정 |
| W3 | ✅값/🟡 | ✅값/🟡 | 유지 | 동일. 34.36~36.28% gold · 클래스 **13 → gold 14**(부류 E) |
| W4 | ✅ | ✅ | 유지 | 동일. 48억 5클래스 gold |
| W5 | ✅ | ✅ | 유지 | 동일. **7클래스** gold. 10R R1+R2 동시 집행으로 `rows` 7 유지 — 예측대로 |
| W6 | ✅ | ✅ | 유지 | 동일. 4호 2클래스 2등급 gold |
| W7 | ✅ | ✅ | 유지 | 머리 토큰 `코리아2배레버리지`→`NH-Amundi코리아2배레버리지`. **펀드 집합 불변 4클래스 gold** |
| W8 | ✅ | ✅ | 유지 | 동일. 77(309) gold |
| **W9** | ✅값/🟡 | **✅** | 🟢 **회수** | **접두 앵커 적용** — 3→**2펀드**, `한국투자연금베트남그로스…` 탈락. 2등급 7/7클래스 gold. 9R §③-A 가 "회귀가 아니라 교정" 이라 한 판정이 gold 실측으로 확인됐다 |
| **W10** | ✅값/🟡 | **✅** | 🟢 **회수** | 국내 한정 머리줄 + 억원. 1,643,771/1,231,522/351,445/328,533/221,039 gold |
| W11·W12 | ✅ | ✅ | 유지 | 동일. 3,344억 14클래스 · 38(119) gold |

### Y 계열

| 문항 | 직전 | 이번 | 변화 | 요지 |
| :-- | :-- | :-- | :-- | :-- |
| Y1 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 큰 개선) | **③-10 적용됨**. 값 5건 gold · 클래스 6/7/4/1/4 · MAX 축 고지. 🟢 HCX 추측 문장("기준가 변경 등으로…") 소멸. 🔴 4·5위 중복 — §③-4 |
| Y2 | ✅ | ✅ | 유지 | 동일. 124,295/96,265/90,389억 · 4/2/2 gold |
| Y3 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 큰 개선) | 클래스 3/1/9/2/3 → **5/8/13/15/3 = 9R 이 낸 gold 그대로**. 값 5건 gold. 🔴 1위 실제 클래스는 10(§③-4) |
| Y4 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | 클래스 1/3/4 → **1/5/5 = gold**. 🔴 1위 `미래에셋성장유망중소형주1호` **클래스 1개**로 나가는데 실제 9다 — §③-4 의 가장 나쁜 얼굴 |
| Y5 | ✅값/🟡 | ✅값/🟡 | 유지(🟢 개선) | **③-10 적용됨**(9R 미적용 3건 마지막). 58.71/56.24/48.3 gold · 클래스 4/11/8 · MAX 축 고지. 🔴 삼성WTI원유 4 → 실제 5 |
| Y6 | ✅ | ✅ | 유지 | 동일. 823/235/207 gold |
| **Y7** | ❌ | **✅** | 🟢 **회수** | `zrin_btyp_nm = '주식형'` 확정식으로 교체. **미래에셋 69,336억 142펀드 295클래스 · NH-Amundi 46,152 28 72 · 신영 41,914 18 60 — 9개 숫자 전수 gold.** 2·3위 운용사도 gold |
| Y8~Y10 | ✅ | ✅ | 유지 | 동일. 876억 13 · −7.06~−5.64 14 · 561/50/74억 10/11/2 gold |
| **Y11** | ❌ | **✅** | 🟢 **회수** | 7줄 → **3펀드 12/13/1클래스 = gold**. 판매완료 1행 제거(`펀드 기본모수 사후조건`) + rptt 축 묶기 둘 다 발동. 9R §③-B+§③-8 이 함께 닫혔다 |
| Y12~Y15 | ✅ | ✅ | 유지 | 동일. Y13 머리 토큰만 교정(1펀드 4클래스 gold) · Y15 3펀드 11/11/2 불변 gold |
| **Y16** | ❌ | **✅** | 🟢 **회수** | 4,380,605 / 2,970,970 / 1,406,816 백만USD **gold** + 천 단위 구분자 |

---

## ② 회귀·회수 분석

### ②-1 🟢 부류 Z 는 닫혔다 — 술어가 실제로 「불일치 시 교체」로 바뀌었다 (코드 확인)

오케스트레이터가 지시한 **가드 5개 코드 확인** 결과:

| 가드 | 함수 | 술어 형태 | 판정 |
| :-- | :-- | :-- | :-- |
| 펀드 기본모수 | `ensure_fund_base_population` → `_strictify_base_population` (L548) | 모수 컬럼**만** 든 최상위 AND 절이 확정식이 **아니면 교체**. 다른 컬럼과 섞인 절·의도적 별개 모수(`= '판매완료'`)는 불개입 | ✅ **교체형** |
| ETF 기본모수 | `ensure_etf_base_population` (L739~746) | 같은 `_strictify_base_population` 기계 + 누락분만 주석에 명시 (`pd_sale_yn IN (0,1)`·`pd_grp_no <> 'ETF'` 대응) | ✅ **교체형** |
| 유형 축 | `ensure_fund_type_axis` (L3552~3573) | 축 절이 있어도 질문의 열거값이 **정확히 1종**이면 확정식으로 교체(9R Y7 `IN ('주식형','해외주식형')` 대응). 이미 확정식이면 불개입 | ✅ **교체형** |
| 호수 경계 | `ensure_fund_series_boundary` (L3723~) | 8R 부터 이미 "주입은 교체다" — SQL 의 **모든** 호수 술어를 걷어내고 GLOB 확정식 한 절 주입 | ✅ **교체형** |
| 개별 조회 묶기 GROUP BY | `ensure_fund_lookup_grouping` (L1530~1540) | 축이 `_FUND_GROUP_EXPR`/`_FUND_KEY_EXPR` 면 불개입, **펀드 식별 컬럼(또는 `GROUP BY 1` 위치 표기)만으로 된 축이면 교체**(9R U2 대응). 분포 축은 존중 | ✅ **교체형** |
| 랭킹 대표행 GROUP BY | `ensure_fund_rank_representative` (L905~925) | 축이 정본식이 아니고 펀드 식별 컬럼만이면 정본 펀드키로 교체 | ⚠ **부분** — §③-3 |
| 억원 병기 | `ensure_amount_eok_columns` (L1174) | `if any(col in e or header in e for e in existing): continue` — **표시 열이 있기만 하면 불개입** | 🔴 **Z 형태 잔존** — §③-10 |
| LIMIT | `ensure_limit` (L413) | `if re.search(r"\blimit\s+\d+", sql): return sql, False` — Z 형태 | ⚪ **무해** — 실행부 `cur.fetchmany(MAX_ROWS)`(L5413)가 상한을 별도로 강제한다 |

**실측 확인**: U2 트레이스에 `[Guard] 개별 조회 펀드 묶기` 가 복귀했고(9R 에 사라졌던 줄), Y7 트레이스에 `[Guard] 유형 축 주입` 이 복귀해 `zrin_btyp_nm = '주식형'` 으로 렌더됐다. U7(유형 축 정상 발동 중) · T6·W5(호수 경계) · V7·W10·V14(ETF) 는 전부 ✅ 유지 — **회귀 감시 통과**.

### ②-2 🟢 클래스 축 rptt 전환 — 값이 맞고 3,040 이 안 움직였다

| 문항 | 답변 | gold(rptt 실측) | 판정 |
| :-- | --: | --: | :-- |
| W5 미래에셋차이나솔로몬 2호 | 7 | 7 (`031910531100`) | ✅ |
| T6·S5 미래에셋차이나솔로몬 3호 | 8 | 8 (`031910532410`) | ✅ |
| R6 미래에셋차이나솔로몬 2호 등급 | 7 | 7 | ✅ |
| U2 KB차이나그로스 | 14 | 14 (`030230002C22`) | ✅ |
| U3 한화중국본토 | 13 | 13 (`030220157031`) | ✅ |
| Y11 피델리티차이나 | 12/13/1 | 12/13/1 | ✅ |

**`COUNT(DISTINCT 펀드키)` 불변 확인**: R1 = 클래스 8,969·펀드 3,040 · T1/T2/V5/Y6 = 823(2,066)/235(403)/207(850)/205(240)/142(540). DB 재실측 `SELECT COUNT(*), COUNT(DISTINCT _FUND_KEY_EXPR) … → (8969, 3040)`. **10R 계획 3번(3,040 유지) 지켜졌다.**

### ②-3 🟢 접두 앵커 — 9R 에 내가 낸 판정이 옳았다 (gold 전수 검증)

| 문항 | 9R 예측 | 11R 실제 | gold 실측 | 판정 |
| :-- | :-- | :-- | :-- | :-- |
| R4·U15·V12 | 6 → 2 | **2** | `미래에셋코어테크증권자투자신탁`(10클래스) · `…청년소득공제`(4) | 🟢 교정 확인 |
| T14 | 4 → 2 | **2** | `한국투자베트남그로스증권자투자신탁`(7·2,528억) · `…UH`(7·766억) | 🟢 교정 확인 |
| W9 | 3 → 2 | **2** | 같은 2펀드 · 둘 다 2등급 7클래스 | 🟢 교정 확인 |
| V1 | 불변 | **불변** | 1펀드 3클래스 −10.73~−9.73 | ✅ |
| W7 | 불변 | **불변** | 1펀드 4클래스 588.14~606.04 | ✅ |
| Y15 | 불변 | **불변** | 3펀드 11/11/2 클래스 | ✅ |
| S12 | 불변(브랜드 없음) | **불변 6펀드** | 6펀드 | ✅ |
| U10·W2 | 불변 | **불변 6펀드** | 6펀드 | ✅ |
| U11·Y11 | 개선 | **3펀드** | 3펀드(+역외 1 — §④-4) | ✅ |

**7R §③-15 를 뒤집은 9R 판정(연금·퇴직연금 접두는 별개 상품)도 유지한다.** W9 질문은 `한국투자베트남그로스증권자투자신탁` 이고 답은 정확히 그 2펀드다.

### ②-4 🟢 기계 조립 `COUNT(*)` 오염(9R 이 A 로 승격한 것) — **랭킹 경로는 닫혔고 개별 조회 경로는 안 닫혔다**

`_class_count_off_value_predicate`(L954)가 값 술어를 WHERE→HAVING 으로 옮긴다. 그런데 **호출부가 `ensure_fund_rank_representative` 한 곳뿐**이고, `ensure_fund_lookup_grouping` 은 L1523 에서 `having` 을 아예 불개입 사유로 쓴다.

| 경로 | 문항 | 9R | 11R | gold |
| :-- | :-- | :-- | :-- | :-- |
| 랭킹 | S2 | 3/3/**1** | **5/3/12** | 5/3/12 ✅ |
| 랭킹 | Y3 | 3/1/9/2/3 | **5/8/13/15/3** | 동 ✅ |
| 랭킹 | Y4 | 1/3/4 | **1/5/5** | 동 ✅ |
| 랭킹 | R7·S1·V16·Y1 (삼성KOSPI200) | 4 | **7** | 7 ✅ |
| **개별 조회** | R4 | 9 | **9** | **10** 🔴 |
| **개별 조회** | V3 | 4 | **4** | **7** 🔴 |
| **개별 조회** | W3 | 13 | **13** | **14** 🔴 |
| **개별 조회** | S12 | 9/4/3/5/12/4 | 동 | **10/4/5/5/13/4** 🔴 |

**대조군 실측**: 같은 펀드를 값 술어 없이 묻는 U15(`순자산`)는 **10클래스**를 정확히 낸다. 술어(`fd_yr1_ern_r IS NOT NULL AND > -100`)가 있는 R4 만 9다. 원인이 술어라는 것이 문항 쌍으로 확정된다 — §③-2.

### ②-5 🔴 회귀 1건 — V12, 원인 층은 **조립기**다

```
SQL:  … MAX(mtco_itm_no) AS mtco_itm_no, MIN(rptt_ksd_itm_no) AS 대표번호 … → [Execute] 2행
답변: 미래에셋코어테크 펀드는 클래스 수가 총 10개입니다.        ← 2행 중 1행만, 그것도 '총'
```

`_lookup_answer`(L1745)의 `class_only` 는 `set(cols) - noise - 설정일 <= _LOOKUP_HEAD | {"대표번호"}` 다.
`noise` 는 **클래스 종속 컬럼**(`han_clas_nm` 등)만 담는다. `mtco_itm_no` 는 클래스 종속도 아니고 값 컬럼도 아니라
어느 쪽에도 안 걸려 `class_only=False` → 조립기가 `None` 을 돌려주고 HCX 산문으로 갔다.

W5·T6 가 같은 개수 질의인데 살아남은 것은 우연이다 — 그쪽 SELECT 에 섞인 것이 `han_clas_nm`(noise 목록에 있음)이었을 뿐이다.
**HCX 가 SELECT 에 무슨 식별 컬럼을 섞느냐에 조립기 발동이 달려 있다 — 부류 Z 와 같은 성질이 조립기 층에 남아 있다.** §③-1.

### ②-6 🔴 이번 라운드 최대 잔여 — 랭킹 축이 펀드를 쪼갠다 (부류 **AB**)

랭킹 GROUP BY 는 `_FUND_KEY_EXPR`(운용사코드/모펀드번호) 그대로다. 9R §③-B 가 실측한 380그룹 2,599행 과분할 구간이 랭킹에 그대로 나온다. 9R 은 클래스수가 답에 없어서 안 보였고, 10R 이 클래스수를 실으면서 **틀린 숫자로 밖에 나왔다**:

| 문항 | 답변이 말한 클래스수 | 그 펀드의 실제 클래스수(rptt) |
| :-- | --: | --: |
| S2·Y3 1위 미래에셋아시아퍼시픽소비성장1호 | **5** | **10** |
| Y4 1위 미래에셋성장유망중소형주1호 | **1** | **9** |
| Y4 2위 미래에셋아시아퍼시픽소비성장1호 | **5** | **10** |
| Y5 1위 삼성WTI원유특별자산 1 | **4** | **5** |
| S1·V16·Y1 4·5위 NH-Amundi1.5배 | **1 + 4** (두 줄) | **5** (한 펀드) |

9R 이 "`삼성배당주장기 1[주식]` 을 클래스 1개라 말하는 것은 상품 사실의 오진술" 이라며 A 로 승격한 것과 **같은 성질의 오진술**이다. 그래서 R7 을 뺀 랭킹 7문항을 ✅ 로 올리지 않았다.

---

## ③ 남은 결함 — 전부 (A 용 · 이월 없음)

> 🔴 전부 런타임 결정층(`pipeline.py`) 안에서 닫힌다. **온톨로지 변경 요구 0건.**
> 우선순위 **A = 값이 틀리거나 못 답함** · **B = 값은 맞고 서술 결함**.

### ③-1 [A] 🔴 부류 AA — 조립기 발동 조건에서 **식별 컬럼도 잡음이다** (V12 회귀)

- **일반 규칙**: `_lookup_answer` 의 `class_only` 판정에서 제외하는 잡음 집합에 **클래스 종속 컬럼뿐 아니라 펀드 식별 컬럼(`_FUND_ID_COLS`)도 넣는다.** 식별 컬럼은 사용자가 물은 값이 아니므로 조립을 막을 근거가 없다. 같은 이유로 **조립기가 받은 행 수가 답변에 전부 실렸는지**를 조립 후 사후조건으로 검사한다(`ensure_rows_answered` 가 이미 있는 자리).
- **닿는 층**: 조립기 `_lookup_answer`(L1743~1747).
- **닫히는 문항**: **V12**(회귀). 회귀 감시: W5·T6·U16(같은 개수 질의 경로).
- 근거: §②-5. `_FUND_ID_COLS`(L1355)가 이미 정의돼 있어 한 줄이다.

### ③-2 [A] 🔴 부류 E — `COUNT(*)` 모수 분리를 **개별 조회 경로에도** (9R ③-2 의 미집행 절반)

- **일반 규칙**: 개별 조회 묶기 SQL 의 `COUNT(*) AS 클래스수` 도 값 컬럼 술어(`IS NOT NULL`·`> -100`·`<> 0`)와 무관하게 기본모수 전체를 센다. 이미 만들어 둔 `_class_count_off_value_predicate` 를 `ensure_fund_lookup_grouping` 의 재작성 끝에서도 호출한다. 개별 조회는 `MAX`·`MIN` 을 **둘 다** 싣으므로, 옮겨도 값이 안 바뀌는 형태는 `IS NOT NULL` 계열뿐이다 — **방향 술어(`> -100`·`< 0`)는 옮기지 말고 그대로 WHERE 에 두되 `COUNT(*)` 만 `COUNT(*) FILTER` 없이 세도록 서브쿼리 없이는 불가능하므로, 실무적으로는 `IS NOT NULL`·`<> 0` 만 HAVING 으로 옮기고 `> -100` 은 HAVING 으로 옮긴다**(`MAX(col) > -100` 은 원 술어와 동치가 아니지만 대표값 판정으로는 정합이고, 실측상 `> -100` 에 걸리는 행은 없다).
  진입 조건 L1523 의 `having` 불개입도 함께 푼다(가드가 스스로 만든 HAVING 이므로).
- **닿는 층**: SQL 가드 `ensure_fund_lookup_grouping`.
- **닫히는 문항**: **R4**(9→10) · **V3**(4→7) · **W3**(13→14) · **S12**(9/3/12→10/5/13).
- 회귀 감시: U15·V13·U1·S4·Y10·W11(값 술어가 없어 이미 맞는 문항 — SQL 이 안 바뀌어야 한다).

### ③-3 [A] 🔴 부류 V′ — 랭킹 GROUP BY 축 사후조건 (U14 · 9R ③-9 미착수 확인)

로컬 실측으로 **왜 안 걸리는지**를 특정했다:

```
U14 실행 SQL:  … GROUP BY or_co_xtn_itt_cd, prvo_pbff_desc … ORDER BY SUM(fd_yr1_ern_r) DESC
_fund_sort_target()            → ('fd_yr1_ern_r','DESC')   ← 축은 알아본다
가드가 본 축 gcols             → {or_co_xtn_itt_cd, prvo_pbff_desc}
  ⊄ _FUND_ID_COLS  (prvo_pbff_desc 때문)  → 정본 교체 분기 진입 실패
  → 폴백 분기(`or_co_xtn_itt_cd` 가 있으니 정렬 컬럼만 감싼다)
  → 그런데 그 시점 SELECT 에 fd_yr1_ern_r 이 없다(근거컬럼 보강이 아직 안 돌았다)
  → _wrap_sort_col → wrapped=False → return (sql, False) · 트레이스에 마커 없음
```
(`ensure_fund_rank_representative(U14 원 SQL)` 로컬 호출 = `changed False` 로 재현 확인. 값 컬럼이 실린 최종 SQL 로 부르면 `True` 가 된다.)

- **일반 규칙 3항**:
  ⓐ **GROUP BY 축을 판정할 때 기본모수 컬럼(`sale_yn`·`prvo_pbff_desc`)은 축에서 뺀다.** 기본모수는 전 행이 같은 값이라 축으로서 정보가 0이다. 빼고 나면 `{or_co_xtn_itt_cd}` ⊆ `_FUND_ID_COLS` 가 되어 정본 펀드키 교체가 걸린다.
  ⓑ **랭킹 컬럼의 `SUM`·`AVG` 정렬은 대표값이 아니다** — `_wrap_sort_col`/`_wrap_order_by_col` 이 `MAX`/`MIN` 으로 교체한다. 클래스 수익률의 합은 도메인상 아무 뜻이 없다.
  ⓒ **`ensure_fund_evidence_columns`(근거컬럼 보강)를 대표행 가드보다 앞으로 옮긴다.** 지금은 값 컬럼이 없어 대표행 가드가 빈손으로 돌아가고, 그 뒤에 값 컬럼이 실린다.
- **닿는 층**: SQL 가드 `ensure_fund_rank_representative` + `_apply_sql_guards` 의 호출 순서(L5507 ↔ L5539 계열).
- **닫히는 문항**: **U14**(gold: 한화2.2배 6 · NH-Amundi코리아2배 4 · 삼성KOSPI200 제1호 7).
- 회귀 감시: R7·S1·S2·V16·Y1·Y3·Y4·Y5(대표행 가드가 이미 도는 문항 전부) · T1·V5·R2·Y6(운용사 집계 — 축이 `or_co` **하나뿐**이 아니라 운용사 확정식 템플릿이 따로 잡으므로 불개입이어야 한다. ⓐ 가 `or_co_xtn_itt_cd` 단독 축을 펀드키로 바꾸지 않도록 **`ensure_fund_manager_ranking` 이 먼저 도는 현재 순서를 지킨다**).

### ③-4 [A] 🔴 부류 AB — **랭킹의 묶기 축을 rptt 로.** A 의 보류 ① 에 대한 답: 보류 근거가 실측으로 반증된다

**A 의 보류 사유**(10R 계획 §보류): *"랭킹 GROUP BY 를 rptt 로 바꾸면 정본 펀드 수 3,040 → 1,919 로 움직여 R1·T1·V5 가 흔들린다."*

**반증 1 — 두 축은 서로 다른 코드 자리다.** R1 은 `GROUP BY zrin_btyp_nm` + `COUNT(DISTINCT 펀드키)`, T1·V5·R2·Y6 은 운용사 확정식의 `GROUP BY 1` + `COUNT(DISTINCT 펀드키)` 다. **셋 다 펀드 단위 GROUP BY 를 쓰지 않는다.** `ensure_fund_rank_representative` 가 주입·교체하는 GROUP BY 는 이들 SQL 에 존재하지 않으므로, 그 축을 바꿔도 `COUNT(DISTINCT 펀드키)` 는 건드리지 않는다. 3,040 은 그대로다.

**반증 2 — 실측: 축을 바꿔도 펀드도 값도 안 바뀐다. 클래스수만 참값이 된다.**

```
                       펀드키 축                          rptt 축
S2 3년하위3   -80.36(5) / -78.08(3) / -77.8(12)   -80.36(10) / -78.08(3) / -77.8(12)
Y3 1년하위5   -83.96(5) -79.07(8) -73.98(13)      -83.96(10) -79.07(8) -73.98(13)
              -71.59(15) -71.06(3)                 -71.59(15) -71.06(3)
Y4 5년하위3   -84.41(1) / -83.38(5) / -81.51(5)   -84.41(9) / -83.38(10) / -81.51(5)
Y5 6개월상위3  58.71(4) / 56.24(11) / 48.3(8)      58.71(5) / 56.24(11) / 48.3(8)
R7 1년상위3   387.66(6) / 362.53(4) / 361.3(7)    387.66(6) / 362.53(4) / 361.3(7)   ← 동일
```
**펀드 이름과 값이 한 건도 바뀌지 않는다.** 바뀌는 것은 클래스수(참값이 된다)와, 아래 두 문항의 5위뿐이다:

```
S1 1년상위5 rptt 축 → 4위 NH-Amundi 1.5배 257.14(5클래스, 중복 해소)
                      5위 하나 코리아증권자투자신탁[주식] 203.13(8)
V16·Y1 3년상위5 rptt 축 → 4위 NH-Amundi 1.5배 417.77(5)
                      5위 미래에셋차세대Fun소득공제증권자투자신탁[주식] 294.63(1)
```
즉 **8라운드 미해결이던 S1 4·5위 중복이 여기서 닫힌다.**

- **일반 규칙**: 랭킹·대표행 가드가 주입·교체하는 GROUP BY 축을 `COALESCE(NULLIF(TRIM(rptt_ksd_itm_no),'KR0000000000'), <_FUND_KEY_EXPR>)`(= 이미 있는 `_FUND_GROUP_EXPR`)로 한다. **분포·운용사 집계의 `COUNT(DISTINCT <_FUND_KEY_EXPR>)` 는 손대지 않는다**(3,040 유지).
- **닿는 층**: SQL 가드 `ensure_fund_rank_representative` 의 축 리터럴 + 조립기 `_fund_rank_answer` 의 `GROUP BY <정본식>` 발동 조건(같이 바꾼다).
- **동결선**: 랭킹 머리줄의 `펀드 = 운용사 종목번호 기준` → `펀드 = 대표예탁원번호 기준`(개별 조회 경로와 같은 문구). S1·V16·Y1 의 5위 행이 바뀐다 — **이것은 의도된 값 변경이고 gold 도 함께 정정한다**(위 실측이 새 gold다). 나는 이 변경을 승인한다.
- **닫히는 문항**: **S1 · S2 · V16 · Y1 · Y3 · Y4 · Y5**(7건 ✅값/🟡 → ✅ 예상). 회귀 감시: R7·U13·Y2(축을 바꿔도 동일해야 한다 — 위 실측대로).

### ③-5 [B] 부류 Q 재발 — 앵커 결합형이 사용자 문자열을 중간에서 자른다 (V3)

- **증상**: 질문 `삼성KOSPI200 2배레버리지 펀드 3년 수익률` → 리터럴·머리 토큰이 **`삼성KOSPI2002`**. 9R 은 `삼성KOSPI2002배레버리지` 였다(부류 Q 해소로 기록한 개선분).
- **일반 규칙**: 이름 검색 리터럴의 결합형은 「KG 라벨 + **사용자 문자열의 잔여 전체**」다. KG 가 `KOSPI200`(Index)을 소비한 뒤 남은 문자열을 **앞에서 한 글자만 붙이고 끊지 않는다.** 결합 후 DB 실재 검사(0행이면 결합 해제)는 그대로 둔다.
- **닿는 층**: Ground — `residual_name_token` 의 결합 지점.
- **닫히는 문항**: **V3**. 값은 이미 gold 이므로 서술만 바뀐다. 회귀 감시: V1·W7·Y15·T14·W9·R4(결합형이 이미 맞는 문항).

### ③-6 [B] 억원 표시의 반올림 규칙이 경로마다 다르다 (T3 vs T2·V5)

- **증상**: 같은 33,109,784,036,579원(삼성자산운용 순자산 합)을 T3 은 **331,098억원**(`ROUND`), 같은 라운드 T2·V5·S11 은 **331,097억원**(`CAST(.../100000000 AS INTEGER)` 절사)으로 적는다.
- **일반 규칙**: 억원·백만USD 표시 열은 **전부 `ROUND`** 로 굽는다(0.84억을 버리는 절사보다 정확하다). `ensure_amount_eok_columns` 의 세 분기 중 첫 분기(L1178)만 절사이고 나머지 둘은 이미 `ROUND` 다 — 그 하나를 맞추고, `ensure_fund_manager_ranking` 템플릿의 `CAST(SUM(...)/100000000 AS INTEGER)` 도 같이 맞춘다.
- **닿는 층**: SQL 가드 `ensure_amount_eok_columns` · `ensure_fund_manager_ranking` 템플릿.
- **닫히는 문항**: **T3**. 🔴 **동결선**: T1·T2·V5·S11·R2·Y6·U6·Y7 의 순자산 표시가 최대 ±1억 움직인다(377,707→377,707 · 331,097→331,098 · 278,196→278,196 실측 확인 필요). gold 도 같이 정정한다.

### ③-7 [B] 목록 경로의 표시 중복 (V9·S6·S7·T5·T10·R3·T13·V8)

- **증상**: V9 22행 중 `키움러시아익스플로러…1[주식]` 이 **5줄**(실제 1펀드 14클래스), `미래에셋브라질러시아업종대표…1호` 가 **5줄**(실제 1펀드 6클래스). S6 은 `미래에셋인도중소형포커스…1호` 가 4줄.
- **일반 규칙**: 목록 답변은 **같은 `대표번호`(rptt) 행을 한 줄로 접고 클래스수를 합산**한다(개별 조회 조립기 `_lookup_answer` 가 이미 하는 것과 같은 접기). 순자산은 접은 행들의 `MAX` 를 유지한다(머리줄이 이미 "대표 클래스 기준(MAX)" 라 정합).
  🔴 **머리줄의 「전체 N개」는 `COUNT(DISTINCT 펀드키)` 그대로 둔다**(R3 248 · S6 58 · V9 22 — 6라운드 gold). 접기는 **표시 층에서만** 한다.
- **닿는 층**: 조립기 `_list_answer`.
- **닫히는 문항**: **R3 · S6 · S7 · T5 · T10 · T13 · V8 · V9**(8건 ✅값/🟡).
- ⚠ 이렇게 하면 "전체 22개" 인데 표시가 8줄이 되어 숫자가 어긋나 보인다. 그래서 **§③-보고-1 과 한 묶음**이다 — 축을 정하지 않으면 이 어긋남은 표시 문구로만 덮을 수 있다("전체 22개(대표번호 기준 8건)").

### ③-8 [B] 부류 W1′ — 0행 근접 후보의 분해 기준이 공백이라 한 번도 안 걸린다 (U12·W1)

- **증상**: 10R 이 AND 조합을 넣었는데(`_nearest_fund_names` L2830) `toks` 를 **공백·문자종 경계**로 만든다. 그런데 리터럴은 공백 무시 가드를 지난 한 덩어리(`삼성차이나본토`·`삼성베트남대표`)라 `len(cands) >= 2` 가 **영원히 거짓**이다. 그래서 접두 축소 루프만 돌고, 하한이 토큰의 2/3(5자)이라 `삼성차이나본` `삼성차이나` 까지만 줄어 0행 → 후보 없음.
- **실측**: `%삼성%` AND `%베트남%` → **2펀드**(`삼성아세안플러스베트남증권자투자신탁 UH[주식]` 외) · `%삼성%` AND `%본토%` → **10펀드**(`삼성 CHINA2.0 본토증권자투자신탁 제2호[주식]` 외). 후보가 실재한다.
- **일반 규칙**: 리터럴 분해는 공백이 아니라 **Ground 가 이미 아는 KG 라벨 경계**로 한다 — 리터럴이 KG Organization/Index 라벨로 시작하면 `[라벨][잔여]` 두 성분으로 갈라 AND 조합을 만든다. 라벨을 못 찾으면 종전 접두 축소.
- **닿는 층**: 조립기 `_nearest_fund_names` — Ground 가 넘긴 브랜드 라벨을 인자로 받는다.
- **닫히는 문항**: **U12 · W1**(둘 다 0행 자체는 정답이므로 서술만 개선).

### ③-9 [B] 면책·권유 문형 잔여 (U9)

- **증상**: U9 꼬리 `"투자 결정 전에 추가 정보를 확인하는 것을 권장합니다."` — `[Guard] 면책 문구 제거` 가 U14 에는 발동했는데 U9 에는 안 걸렸다(사전이 '금융기관 문의·전문가 상담' 계열만 담는다).
- **일반 규칙**: 금지 문형 판정을 **어휘 목록이 아니라 문형**으로 한다 — 답변 마지막 문장이 `근거 없는 권유·주의 환기`(주어가 사용자, 서술어가 권장/확인/유의)면 제거한다. 데이터에서 나온 주의 문구(수익률 누적 고지 등)는 조립기가 굽는 것이라 대상이 아니다.
- **닿는 층**: 조립 후처리(면책 제거와 같은 자리).
- **닫히는 문항**: **U9**(값은 gold).

### ③-10 [B] 억원 병기 가드에 부류 Z 형태가 남았다

- **일반 규칙**: `ensure_amount_eok_columns` 의 "표시 열이 이미 있으면 불개입"(L1174)을 **"표시 열의 분모·단위가 확정식과 다르면 교체"** 로 바꾼다. 다른 여섯 확정식 가드와 형태를 맞춘다.
- **닿는 층**: SQL 가드 `ensure_amount_eok_columns`.
- **닫히는 문항**: 없음(현재 전부 정합). **회귀 방지 항목** — 9R U2·Y7 이 정확히 이 형태에서 터졌다.

### ③-11 🔴 정본 축 보고 (구현안 아님 — 별도 판단 필요)

1. **펀드 축 3,040(`_FUND_KEY_EXPR`) vs 1,919(rptt)** — 9R §③-14-1 그대로. **이번 라운드에 목록 경로 실측이 추가됐다**: 같은 국가 태그를 두 축으로 세면 중국 **248 vs 106** · 인도 **58 vs 23** · 러시아 **22 vs 8** · 미국 **98 vs 55** · 베트남 **38 vs 19** · 일본 **31 vs 18** 이다. 지금 답변은 «전체 248개» 라 말하면서 목록에는 같은 펀드를 여러 줄로 보인다(§③-7). 주최 채점 기준 축 확인이 필요하고, 확인 전까지는 **3,040 축을 유지**한다(6라운드 gold 통과분).
2. **`rptt_ksd_itm_no = 'KR0000000000'` 플레이스홀더** — 판매중·공모 모수에 0건. `NULLIF` 폴백으로 무해. 원천 데이터 사안.
3. **역외 법인 코드 `00130001`** — `피델리티차이나포커스펀드`(rptt NULL)가 U11·Y11 에서 `or_co` 필터로 빠진다. S9 는 "종목명이 '피델리티' 로 시작하는 역외펀드 47개…는 별도 법인이라 포함하지 않았다"고 고지하는데 U11·Y11 은 고지가 없다. 값 판정은 고지 있는 쪽(S9)을 정본으로 봐서 ✅ 로 뒀다. 4도메인 영향: 공모펀드만.

---

## ④ 관찰 — 값은 맞지만 구조적으로 위험한 것

1. **HCX 재생성 마커가 이번 배치에 0건이다.** 9R 은 V7·W10·U9 3건이었다. U9 는 `[Guard] 방언 토큰 치환` 이 재생성 자체를 없앴고, V7·W10 은 `[Route] SQL 사후 보정` 뒤에 한 번에 통과했다. 9R §④-1 이 요청한 "기각 사유를 마커에 남기기"는 재생성이 안 일어나 이번엔 검증 불가.

2. **LIMIT 가드는 부류 Z 형태로 남았지만 무해하다.** `ensure_limit` 은 `LIMIT` 이 있기만 하면 불개입인데, 실행부 `cur.fetchmany(MAX_ROWS)`(L5413)가 30행 상한을 별도로 강제한다. **가드 하나가 아니라 두 층이 같은 성질을 지키는 형태** — 다른 확정식 가드도 이 구조를 참고할 만하다.

3. **개별 조회 조립기가 T6 의 클래스 기호 열거를 잃었다.** 9R 은 HCX 산문이 `C5·C 1·C-e·C3·C4·A·C-w·C2` 8개를 실행 결과와 순서까지 맞게 열거했다. 11R 은 기계 조립이라 "클래스 8개" 뿐이다. 질문이 개수를 묻고 값은 gold 이므로 결함이 아니지만, **HCX 를 안 부르는 이득과 함께 맞는 정보도 사라진다**는 9R §④-2 와 같은 성질이 다른 경로에서 반복됐다.

4. **`ext_fund_page` 조인 없이 운용사명을 굽는 경로가 늘었다.** U6·Y7 의 확정식이 `LEFT JOIN ext_fund_page` + `_minority_mgmt_names` 로 최빈 이름을 뽑는다. 신영자산운용(00040014)·키움투자자산운용(00080052)이 처음으로 답에 나왔고 둘 다 정본 이름이다. 합병 코드가 아닌 운용사에서도 이름이 맞는지는 이번에 확인됐다.

5. **`fd_ivst_rgn_desc` 오염 · `zrin_btyp_nm` NULL 혼입** — 9R §④-3·④-4 그대로 유지. 이번 라운드에 새 증상은 없다.

6. **U11 과 Y11 이 드디어 같은 답을 낸다.** 9R 관찰(같은 질의 형태인데 U11 만 맞음 — 정답이 HCX 의 우연에 달림)이 §③-8(기본모수 사후조건) 집행으로 닫혔다. 두 문항 모두 3펀드 12/13/1 로 정합이다. **"정답이 우연에 달린" 문항은 이번 라운드에 남아 있지 않다** — 부류 Z 수리의 실질적 성과다.

---

## ⑤ 형제 검증 질문

**없음.** 질문 수는 345개로 확정됐다(2026-09-03 사용자 확정). 새 질문을 만들지 않았다.

---

## ⑥ 표본 5건 원문 대조

`random.seed(20260904)` · `random.sample(sorted(qids), 5)` → **S6 · T2 · U11 · V9 · V10**

### T2 — 순자산 합계가 가장 큰 자산운용사 3곳

**답변**
```
조회 결과 순자산 상위 3개 운용사입니다 (판매중·공모 기준, 펀드 = 운용사 종목번호 기준, 클래스 = 판매 단위, 기준일 2026-08-24).

1. 미래에셋자산운용(00080008): 순자산 377,707억원 · 펀드 823개(클래스 2,066개)
2. 삼성자산운용(00040010): 순자산 331,097억원 · 펀드 207개(클래스 850개)
3. KB자산운용(00040035): 순자산 278,196억원 · 펀드 129개(클래스 625개)
```
**DB 실측** (`sale_yn='판매중' AND prvo_pbff_desc='공모'`, `GROUP BY 운용사코드`, `ORDER BY SUM(fd_nast_suma) DESC`)
```
00080008  823  2066  377707
00040010  207   850  331097   (정확히는 33,109,784,036,579원 = 331,097.84억)
00040035  129   625  278196
```
**판정 ✅ 유지** — 9개 숫자 전수 일치. 운용사 이름 3건 정본.
⚠ 같은 라운드 T3 은 같은 삼성 순자산을 **331,098억원**(ROUND)으로 적는다 — §③-6 이 지키려는 자리다.

### S6 — 인도에 투자하는 공모펀드 알려줘

**답변**(머리 + 상위 6줄)
```
조건에 해당하는 공모펀드는 전체 58개(클래스 135개)이며, 순자산 상위 30개 펀드는 다음과 같습니다
(기준일 2026-08-24, 펀드 = 운용사 종목번호 기준·클래스 = 판매 단위·순자산 = 대표 클래스 기준(MAX)).

1. 미래에셋인도중소형포커스증권자투자신탁1호(주식): 순자산 673억원 · 클래스 2개
2. 미래에셋인도중소형포커스증권자투자신탁1호(주식): 순자산 565억원 · 클래스 8개
3. 미래에셋연금인디아업종대표증권자투자신탁1호(주식): 순자산 407억원 · 클래스 3개
4. 미래에셋인디아디스커버리증권자투자신탁 1(주식): 순자산 399억원 · 클래스 1개
5. 미래에셋인도중소형포커스증권자투자신탁1호(주식): 순자산 392억원 · 클래스 1개
6. 미래에셋인도중소형포커스증권자투자신탁(주식): 순자산 389억원 · 클래스 1개
```
**DB 실측** (`',' || prfd_attr_cds || ',' LIKE '%,IND,%'`, 판매중·공모)
```
COUNT(DISTINCT 펀드키) = 58 · COUNT(*) = 135      ← 머리줄 gold 일치
COUNT(DISTINCT rptt)   = 23                        ← 같은 펀드가 최대 4줄로 쪼개진다
```
**판정 ✅값/🟡 유지** — 58(135)은 gold. 🔴 `미래에셋인도중소형포커스…1호` 가 1·2·5·10위에 4줄(§③-7).

### V10 — 인도에 투자하는 공모펀드는 몇 개야?

**답변**
```
조회 조건에 해당하는 공모펀드는 58개(클래스 135개)입니다 (판매중·공모 기준, 기준일 2026-08-24).
```
**DB 실측** — S6 과 같은 술어, `(58, 135)`.
**판정 ✅ 유지** — S6 와 **같은 라운드에서 같은 숫자**를 낸다(경로가 달라도 정합). 개수 질의에는 목록 중복이 없어 🟡 요소도 없다.

### U11 — 피델리티차이나 펀드 순자산

**답변**
```
'피델리티차이나' 이름의 공모펀드 3개가 조회됐습니다 (기준일 2026-08-24, 펀드 = 대표예탁원번호 기준·클래스 = 판매 단위).

- 피델리티 차이나 증권 자투자신탁 (주식-재간접형): 순자산 665억원 (클래스 합계) · 클래스 12개(전부 판매중)
- 피델리티 차이나 컨슈머 증권 자투자신탁 (주식-재간접형): 순자산 579억원 (클래스 합계) · 클래스 13개(전부 판매중)
- 피델리티차이나컨슈머재형증권자투자신탁(주식-재간접형): 순자산 2억원 (클래스 합계) · 클래스 1개(전부 판매중)
```
**DB 실측** (`or_co='00080029' AND REPLACE(itm_nm,' ','') LIKE '%피델리티차이나%'`, 판매중·공모, rptt 축)
```
033180043411  피델리티 차이나 증권 자투자신탁 (주식-재간접형)      12행  SUM = 665억
033180048111  피델리티 차이나 컨슈머 증권 자투자신탁 (주식-재간접형) 13행  SUM = 579억
033180048151  피델리티차이나컨슈머재형증권자투자신탁(주식-재간접형)   1행  SUM =   2억
```
**판정 ✅ 유지** — 3펀드·클래스 3건·금액 3건 전수 일치. 머리 토큰이 `차이나`→`피델리티차이나` 로 교정됐다(접두 앵커).
⚠ `or_co` 필터 밖에 `피델리티차이나포커스펀드`(코드 `00130001` 역외 · rptt NULL · 1행)가 있다. S9 는 역외 분리를 고지하는데 여기엔 고지가 없다 — §③-보고-3.

### V9 — 러시아에 투자하는 공모펀드 알려줘

**답변**(머리 + 22줄 중 발췌)
```
조건에 해당하는 공모펀드는 전체 22개(클래스 51개)이며, 순자산 순으로 다음과 같습니다 (…).

1. 키움러시아익스플로러증권투자신탁 1[주식]: 순자산 148억원 · 클래스 10개
2. 키움러시아익스플로러증권투자신탁 1[주식]: 순자산  69억원 · 클래스 1개
…
14. 키움러시아익스플로러증권투자신탁 1[주식]: 순자산 0억원 · 클래스 1개
15. 키움러시아익스플로러증권투자신탁 1[주식]: 순자산 0억원 · 클래스 1개
17. 키움러시아익스플로러증권투자신탁 1[주식]: 순자산 0억원 · 클래스 1개
21. 미래에셋러시아업종대표증권자투자신탁1호(주식): 순자산 미수록 · 클래스 1개
```
**DB 실측** (`',' || prfd_attr_cds || ',' LIKE '%,RUS,%'`, 판매중·공모)
```
COUNT(DISTINCT 펀드키) = 22 · COUNT(*) = 51        ← 머리줄 gold 일치
rptt 축 8펀드:
  키움 러시아 익스플로러 증권 투자신탁 제1호[주식]      14클래스   ← 답변엔 5줄로 쪼개져 있다
  신한러시아증권자투자신탁(H)[주식]                     9
  미래에셋러시아업종대표증권자투자신탁 1(주식)           8
  미래에셋브라질러시아업종대표증권자투자신탁1호(주식)     6   ← 답변엔 5줄
  한화러시아증권자투자신탁(주식-재간접형)                5
  미래에셋연금러시아업종대표증권자투자신탁1호(주식)       4
  NH-Amundi러-브증권자투자신탁3호(주식)                 3
  미래에셋러시아인덱스증권자투자신탁(주식)               2
```
**판정 ✅값/🟡 유지** — 22(51)은 gold이고 각 줄의 순자산·클래스수도 그 펀드키 그룹의 실값이다. 🔴 **한 상품이 5줄로 반복되고 "순자산 0억원 · 클래스 1개" 가 세 줄** 나오는 것은 투자자가 읽는 답으로서 결함이다 — §③-7 이 닫는 자리이자 §③-보고-1 이 결정해야 하는 자리다.

---

## 부록 — 이번 라운드에 새로 쓴 부류

| 부류 | 뜻 | 문항 |
| :-- | :-- | :-- |
| **AA** | 조립기 발동 조건에 남은 Z 형태 — SELECT 에 섞인 **식별 컬럼** 하나에 개별 조회 조립기가 꺼진다 | V12 |
| **AB** | 랭킹 묶기 축(`_FUND_KEY_EXPR`)이 한 펀드를 클래스 단위로 쪼개, 클래스수가 부분값이 되고 같은 펀드가 두 순위를 먹는다 | S1·S2·V16·Y1·Y3·Y4·Y5 |

## 부록 — A 의 보류 ②(부류 T 운용사 축) 확정

**DB 실측 (`domestic_etfs`)**

| 술어 | 전체 | `pd_grp_no='ETF' AND pd_sale_yn=1` |
| :-- | --: | --: |
| `TRIM(ref_fund_mgmt_co) = 'Samsung Asset Management Co Ltd'` | 243 | 240 |
| `TRIM(ref_fund_mgmt_co) = 'Samsung Active Asset Management'` | 25 | 25 |
| `TRIM(ref_fund_mgmt_co) LIKE 'Samsung%'` | **268** | **265** |
| `TRIM(cu_fund_mgmt_co) LIKE '삼성%'` | 338 | 265 |

**결론: A 의 265/268 도 내 9R 240/243 도 둘 다 맞다.** 데이터가 어긋난 게 아니라 **잰 술어가 다르다** — A 는 접두 `LIKE`, 나는 정식명 등호였다.

**판정: 접두 `LIKE` 를 쓰면 안 된다.** `LIKE 'Samsung%'` 은 삼성자산운용(00040010)과 **삼성액티브자산운용**(별개 법인, 25행)을 한 운용사로 합친다. 이 코드베이스는 이미 `_minority_mgmt_names` 에서 `'00040010/삼성액티브자산운용'` 을 소수 이름으로 **분리**해 두었고(트레이스 L978 의 `NOT IN` 목록에 실재), 접두 LIKE 는 그 분리를 되돌린다. `cu_fund_mgmt_co LIKE '삼성%'` 도 같은 이유로 안 된다 — 전체 338행에 **삼성증권(주)**(판매사, 70행)이 섞인다.

- **일반 규칙**: ETF 운용사 매칭은 **KG Organization 노드가 매핑한 정식명과 `TRIM(ref_fund_mgmt_co) = '<정식명>'` 등호**로 건다. 접두·부분 `LIKE` 금지. 한글 브랜드 ↔ 영문 정식명 매핑은 KG 별칭에서 가져온다(하드코딩 아님).
- **부수 확인(10R T1 근거 재검증)**: `cu_base_index` 공백 **1,724/1,780 = 96.9%** · `ref_base_index` 공백 **572/1,780 = 32.1%**. 지수 축을 `ref_base_index` 로 잡은 10R 판단은 실측으로 유지된다.
