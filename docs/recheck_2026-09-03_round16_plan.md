# 16R 수리 계획 — 간섭 지도 (2026-09-03)

대상: `docs/recheck_2026-09-03_round15.md` §③ · `docs/kg_structure_probe_round9_2026-09-03.md` §③(20항) ·
`docs/gold_probe_2026-09-03_g5.md` §③(18항).

---

## 🔴 0순위 — 보고 신뢰성 규약 (이 문서부터 적용)

14R 계획서 §⑤ 「구현·실측 확인」 3건이 프로덕션에서 재현되지 않았다. 원인은 **검증 방식**이다 —
로컬에서 함수를 직접 부른 결과를 「실측 확인」이라 적었고, 프로덕션은 HCX 가 다른 SQL 을 내고 다른
가드가 끼어들고 답변기가 다른 열을 읽는다.

**이 라운드부터 이 문서의 §⑤ 는 두 칸으로만 적는다.**

| 표기 | 뜻 |
| :-- | :-- |
| `단위 검증(로컬)` | 이 저장소에서 함수를 직접 호출하거나 pytest 로 확인했다. **프로덕션 경로가 같다는 보장은 없다.** |
| `프로덕션 미확인` | 배포 후 서버 응답으로 확인하지 않았다. 16R 의 모든 항목이 여기 해당한다(배포는 오케스트레이터 몫). |

**값 표기 규약**: 펀드 축과 클래스 축을 반드시 함께 적는다 — `57펀드/188클래스` 처럼.
`188` 단독 표기 금지(14R `X8` 오판의 직접 원인).

**14R 오보 3건 원인 확정 (이번 라운드가 실제로 고치는 자리)**

| 항목 | 14R 오보 | 실제 원인 | 16R 조치 |
| :-- | :-- | :-- | :-- |
| `KG-008` | "714·516·465 기계 조립" | `_entity_count_rank_answer` 가 **정의만 되고 호출부에 배선되지 않았다**(`answer_question` 조립기 체인에 없음) + 가드가 HCX 의 `COUNT(*) as 펀드수` 를 안 지우고 `__g` 접미로 피했다 | P3-c |
| `Z16` | "112/354" | `ensure_org_label_codes` 가 `mgmt`(역조회) 가 잡히면 **조기 반환**하고, `_OR_CO_EQ` 는 등호만 봐서 `IN ('00080052')` 형을 못 본다 | P3-d |
| `X8` | "188/24" | gold 는 **57펀드/188클래스** — 값 검증이 축을 구분하지 않았다 | 값 표기 규약 |

---

## 🔴 1순위 — 자산군 `IN` : **집행하지 않는다** (코드 변경 0)

재검 §③-6 판정을 그대로 따른다. `zrin_btyp_nm` 축은 **질문이 지명한 값과 정확 일치(`=`)**,
질문이 합산을 명시할 때만 `IN`. gold ③-15 의 접두 `IN` 처방과 gold ③-24 는 **버린다.**

현행 `ensure_fund_type_axis`(`pipeline.py:4260`)가 이미 `zrin_btyp_nm = '<값>'` 정확 일치로 주입한다 —
**변경 없음**이 판정과 일치한다. 회귀 감시선: `R1`(19범주 559/953) · `Y7`(69,336) · `U6`(25,159) ·
`U7`(338펀드/106클래스) · `V5` · `S10`.

> 단, `Z5` 는 이 축의 **오탐**이다(아래 P3-e ⓐ) — 판정과 무관한 별개 결함.

---

## ① 간섭 지도

| # | (a) 부류 → 일반 규칙 | (b) 닿는 층 / 충돌 지점 → 회귀 테스트 이름 | (c) 경로가 바뀔 수 있는 기존 ✅ |
| :-- | :-- | :-- | :-- |
| **P2-a** | **부류 AE** — 목록 접기는 표시 층이 아니라 SQL 층에서 한다. 목록 경로의 `GROUP BY` 를 랭킹·개별 조회와 같은 `_FUND_GROUP_EXPR`(rptt) 로 통일하고 `LIMIT` 을 그 위에. 조립기의 `MIN(rptt)` 사후 접기는 제거. 머리줄의 「대표번호 기준 M건」은 화면 행 수가 아니라 `COUNT(DISTINCT _FUND_GROUP_EXPR)` **전체값** | SQL 가드 `ensure_fund_list_grouping` + 조립기 `_list_answer` + `_coverage_counts`. 🔴 충돌: `_list_answer` 발동 조건이 `GROUP BY _FUND_KEY_EXPR` 리터럴이라 축을 바꾸면 **조립기가 통째로 꺼진다** — 같은 커밋에서 함께 바꿔야 한다. `_coverage_counts` 는 두 축을 모두 허용하므로 무사 → `test_round16_repairs.py::test_list_grouping_uses_rptt_axis` · `::test_list_answer_no_post_fold` | **V8**(14행)·**V9**(8행) 전수 gold — 행 수·클래스수·순자산 불변이어야 한다 · **T4**(1펀드 7클래스) · **V10·V14·W12·S8**(개수 질의 — 3,040 축 불개입) |
| **P2-b** | **부류 AF** — 답변에 싣는 **기관 이름**은 KG 정본(`COALESCE(label_official, label_ko, canonical_name)`)으로 치환한다. HCX 에 번역을 맡기지 않는다. KG 매핑이 없으면 DB 원값 그대로 | 조립기 답변 입력 — **이미 같은 일을 하는** `label_code_columns`(코드 컬럼)를 **이름 컬럼까지** 확장한다(새 가드 금지). 🔴 충돌: 해외 ETF 는 영문 법인명이 정본이라 번역하면 안 된다 → KG 별칭에 있는 값만 치환하므로 `BlackRock Fund Advisors`(KG 매핑 없음)는 불변 → `::test_org_name_column_canonicalized` | **U8·Y16**(해외 ETF 영문명 유지) · R2·S11·T1·V5·Y6·U6·U7(펀드 경로 — 이미 정본) |
| **P3-a** | **펀드단위 집계 교체를 SELECT *항목* 단위로** — `COUNT(*)` 항목을 찾아 그 자리에서 치환한다. 위치·앞선 라벨 리터럴·별칭 무관 | SQL 가드 `ensure_fund_distinct_count`. **새 분해기 금지** — `_split_select_items` 재사용. UNION 은 `apply_union_branch_guards` 가 이미 가지로 쪼개 넘긴다 → `::test_distinct_count_labeled_union_branch` | 단일 SELECT 개수 문항 `KG-001`·`KG-012`·`Z6`·`X16`·`AA8` 의 SQL 이 한 글자도 안 바뀌어야 한다 |
| **P3-b** | **구조 게이트의 「SELECT 로 시작」 판정은 선행 여는 괄호를 벗기고** 한다 | SQL 가드 `validate_sql` 첫 구조 검사. 🔴 `_single_select`(SELECT 목록 **편집** 불개입 조건)와는 다른 물음이다 — `_single_select` 는 그대로 둔다 → `::test_validate_sql_accepts_parenthesized_union` | 교차질의 전부(`X8`·`X9`·`X15`·`KG-025`·`KG-026`) — 지금 통과하는 문장은 계속 통과 |
| **P3-c** | **별칭 유일화는 충돌한 원 항목을 지우는 것으로 끝낸다** + **`ORDER BY` 가 있는 랭킹 결과는 조립기가 재정렬하지 않는다** | SQL 가드 `ensure_fund_entity_count_ranking` + 조립기 `_entity_count_rank_answer` **배선**(14R 미배선). 🔴 충돌: 체인에서 `_manager_rank_answer` 뒤·`_lookup_answer` 앞에 놓는다(운용사 템플릿 5열과 헤더가 배타) → `::test_entity_rank_drops_conflicting_alias` · `::test_entity_rank_answer_wired` | `AA16`(별칭 충돌 없는 형제 — 지금 ✅) · `_manager_rank_answer` 경로(R2·S11·T1·V5·Y6) |
| **P3-d** | **`label_official` 형제 코드 `IN` 은 등호·`IN` 양쪽 술어에, 역조회 경로에서도 건다** | Ground/SQL 가드 `ensure_org_label_codes`. 🔴 충돌: `ensure_mgmt_code_predicate`(브랜드 역조회)가 먼저 돌아 `IN` 을 만든다 → 뒤에서 형제 코드를 **합집합**으로 넓힌다(교집합·재작성 아님) → `::test_org_label_codes_expands_in_predicate` | `X12`(2코드 확장 성공 — 불변) · `S3`·`T7`·`U1`·`Y8` 브랜드 질의 |
| **P3-e** | **부류 H** — ⓐ 약관분류(`zrin_ptn_nm`) 값 대조는 **공백을 지우지 않은 질문 원문**에서 한다(「글로벌 주식형」이 `글로벌주식` 으로 붙는 오탐 제거) ⓑ 질문이 약관분류 값을 지명하면 `zrin_ptn_nm = '<값>'` 확정식을 주입한다 ⓒ 스키마에 없는 컬럼을 쓴 최상위 술어는 **그 리터럴이 확정식으로 이미 심겼을 때만** 제거한다 | SQL 가드 `ensure_fund_type_axis`(같은 함수 — 새 가드 금지) + `_ptn_value_in_question`. 🔴 충돌: ⓐ 를 고치면 `Z5` 가 **btyp 축 경로로 돌아간다** → 1순위 판정대로 `zrin_btyp_nm='주식형'` 10펀드/18클래스. ⓒ 는 값을 넓힐 수 있으므로 **확정식이 같은 리터럴을 이미 덮은 술어만** 지운다 → `::test_ptn_axis_no_space_join` · `::test_ptn_axis_injected` | `KG-012`(같은 값 205펀드/522클래스 — 다른 경로, 불변) · `R1`·`Y7`·`U6`·`U7`(btyp 축) · `S10` |
| **P3-f** | **근거 밖 산술 제거** — `retrieved_context` 에 없는 **백분율 수치**를 담은 문장은 답변에서 걷어낸다 | 답변 조립기 반환 직전(`strip_disclaimer` 와 같은 자리 — 가드 중복 0). 🔴 기계 조립 경로는 조기 반환이라 닿지 않는다(HCX 산문 경로 전용) → `::test_strip_unsourced_percent` | `X2` · 백분율을 인용하는 모든 산문 답변(행에 있는 값은 남는다) |
| **P4-a** | **기각 메시지가 계산해 둔 조치를 기계로 집행** — 실행 전 검사가 FROM/JOIN 에 없는 테이블을 짚었을 때 그 술어가 **OR 가지**면 그 가지를 제거하고 한 번 더 검증한다. 남은 술어가 0이면 그때 기각 | SQL 가드(`validate_sql` 사유 → 가드 체인 뒤 사후 조치). 🔴 충돌: AND 술어를 지우면 모수가 넓어진다 — **OR 가지만** 걷는다 → `::test_drop_undeclared_table_or_branch` | `OFFICIAL-004`(13R 답 원복) · 재생성이 도는 모든 질의 |
| **P4-b** | **브랜드 어간 → 운용사 코드 역조회는 기본모수 안에서만** — 코드 후보 각각에 기본모수 행이 0건이면 `IN` 에서 뺀다 | Ground `_brand_or_co_codes`(DB 실측 SQL 한 줄). 🔴 충돌: `S3`(삼성) 은 두 코드 모두 모수 안이라 불변 → `::test_brand_codes_base_population_only` | `S3`·`T7`·`U1`·`Y8`·`V11` |
| **P4-c** | **지수 확정식은 JOIN 문장에서도 컬럼 소속이 확정되면 개입** — 술어의 컬럼에 테이블 한정자가 붙어 있으면 소속이 확정되므로 개입한다 | SQL 가드 `ensure_etf_index_canon`. 🔴 충돌: 한정자가 없는 문장은 지금처럼 불개입(X8 자가 오거절 방지) → `::test_index_canon_qualified_in_join` | `X7`(KOSPI200 34) · `Z19`(NASDAQ100 16) · `AA22`(S&P500 24) — 단일 문장 경로 불변 |

---

## ② 이번 라운드에서 **집행하지 않는 것** (보류 — 구현하지 않는다)

계획서와 구현을 일치시킨다. 아래는 **코드를 건드리지 않는다.**

| 항목 | 사유 |
| :-- | :-- |
| 재검 ③-6 / gold ③-15 / gold ③-24 (자산군 `IN`) | **판정: 집행하지 않음**(1순위). 처방 폐기 |
| 재검 ③-3(부류 Q · V3) · ③-4(부류 W1′ · U12·W1) · ③-7(면책 감시) | B급(값은 맞고 서술) — 예산상 후순위 |
| KG ③-8(X15 상품군별 축) · ③-9(벤치마크 정본 비교식) · ③-10(사후조건) · ③-11(부류 U) · ③-12(역외) · ③-13(2단 요구) · ③-14(수탁 코드) · ③-15(2축 분포) · ③-17(재생성 동일 사유) · ③-18(클래스 표기) · ③-19(근접 표기) · ③-20(머리줄 축) | 예산 |
| gold ③-4(정렬축 검증) · ③-5(holdings 접기) · ③-6(UNION 행 제외) · ③-8/③-9(랭킹 조립·‰) · ③-10~③-14 · ③-16~③-18 | 예산 |

> 🔴 gold ③-8·③-9(`FND-005` 랭킹 기계 조립 + 보수 ‰)와 gold ③-4(정렬축 검증)는 **3라운드 연속 미착수**다.
> 다음 라운드 1순위로 올릴 것을 권고한다.

---

## ③ 실행 순서

P0(문서) → P1(무변경 확인) → P2-a → P2-b → P3-a → P3-b → P3-c → P3-d → P3-e → P3-f → P4-a → P4-b → P4-c.

항목마다: 동결선 `tests/test_snapshot_round6.py` → 전체 pytest → `eval/run_gold_check.py` → 커밋 1개.
동결선이 깨지면 그 항목은 되돌리고 「보고」로 남긴다.

---

## ⑤ 구현·검증 결과

> 🔴 표기 규약은 §0 을 따른다. **13항 전부 `단위 검증(로컬)` 이고 `프로덕션 미확인` 이다.**
> 값은 이 저장소의 DB 를 직접 조회해 얻었다 — 프로덕션은 HCX 가 다른 SQL 을 내고 다른 가드가 끼어들 수 있다.

| # | 상태 | 커밋 | 단위 검증(로컬) 값 — **펀드 축 / 클래스 축 명시** | (b) 예측 충돌이 실제로 났는가 |
| :-- | :-- | :-- | :-- | :-- |
| P2-a 부류 AE | **구현** | `af185ea` | 대표번호 축 총계 중국 106 · 미국 55 · 인도 23 · 베트남 19 · 일본 18 · 브라질 11 · 홍콩 14 · 러시아 8 (전부 심사관 gold 일치) · T10 미래에셋삼바브라질 13억·5클래스 복귀 · 하이인컴채권 5클래스 · S7 `KB베트남인덱스40` 복귀(18→19행) | 🔴 **났다.** `_list_answer` 발동 조건이 `GROUP BY _FUND_KEY_EXPR` 리터럴이라 축만 바꾸면 조립기가 통째로 꺼졌다 — 같은 커밋에서 함께 바꿨다. 추가로 **가드가 「주입」만 하고 「이관」을 안 해서** HCX 가 옛 축을 복사해 오면 조립기가 꺼진다는 것을 스냅샷 재생에서 발견 → 기존 `GROUP BY 펀드키` 도 rptt 로 옮기게 했다 |
| P2-b 부류 AF | **구현** | `3b60e05` | `Mirae Asset Global Investments Co Ltd` → 미래에셋자산운용 · `Samsung Asset Management Co Ltd` → 삼성자산운용 · `KB Asset Ltd` → KB자산운용 | 예측대로 해외 ETF 는 불변(`label_official` 이 없고 `label_ko` 가 영문명 자신) |
| P3-a 항목 단위 교체 | **구현** | `a471594` | X8 **57펀드/188클래스** · ETF 24 · X9 **823펀드/2,066클래스** · ETF 230 · KG-026 **421펀드/1,266클래스** · ETF 34 | 예측 밖 충돌 2건: ⓐ 열 수가 어긋나 UNION 실행이 죽는다 → 짧은 가지 NULL 패딩 추가 ⓑ `_Q_FUND_COUNT` 창(20자)이 좁아 X8·KG-026 이 애초에 발화 못 함 → 45자 |
| P3-b 구조 게이트 | **구현(처방 정정)** | `a471594` | 괄호 친 UNION 정규화 후 `validate_sql` 통과·실행 성공 | 🔴 **처방이 틀렸다.** SQLite 3.50.4 실측 `(SELECT 1) UNION ALL (SELECT 2)` = `near "(": syntax error`. 게이트를 열어도 EXPLAIN 이 다시 죽인다 → **정규화(괄호 제거)** 로 바꿨다 |
| P3-c KG-008 | **구현** | `e8dcf3e` | 홍콩상하이은행 **714펀드**(1,827클래스) · 국민은행 **516**(1,656) · 한국씨티은행 **465**(1,466) | 🔴 **났고, 예측보다 컸다.** 조립기를 운용사 템플릿 **앞**에 놓으면 `_MGR_COLS` 5열도 `AS "펀드수"` 라 순자산 축을 가로챈다(Y7·S11·스냅샷 5건 동시 실패) → 뒤로 옮겼다. 계획서 (b) 열이 예고한 자리 그대로 |
| P3-d Z16 | **구현** | `e8dcf3e` | Z16 **112펀드/354클래스**(gold) · X12 **28펀드/59클래스** 불변 | 🔴 **났다.** 합집합 확장이 X12(브랜드 '슈로더')에 키움 코드를 끌고 왔다 → 확장 조건을 「질문이 그 그룹의 정본 이름을 부를 때」로 좁혀 해소 |
| P3-e 부류 H | **구현** | `58d9979` | Z5 **10펀드/18클래스** · Z11 **205/522** · Z10 **34/98** · AA6 **25/84** · AA7 **37/91** (전부 gold) | 예측대로 Z5 가 btyp 축으로 복귀. 추가 발견: AA7 은 질문이 부르지 않은 `zrin_btyp_nm LIKE '%일본%'` 가 남아 교집합 0 → 함께 걷게 했다 |
| P3-f X1 | **구현** | `ca375b0` | '약 48.81%' 문장만 제거, 24.95·15.9·7.96 유지 | 소수점을 문장 끝으로 오인해 값을 자르는 문제 발견 → 분할 정규식 교정 |
| P4-a OFFICIAL-004 | **구현** | `8525ebc` | 기각 → 통과, 결과 **6종**(13R 답과 동일) | 없음 |
| P4-b FND-C02 | **구현** | `b1e7733` | '삼성' 후보에서 `00080032`(현대인베스트먼트자산운용) 제거 — 모수 226펀드/926클래스 → **217/906** · X12 28/59 불변 | 🔴 **났다.** 기본모수 필터만으로는 구상호 `00040013`(슈로더)이 함께 사라져 X12 가 깨졌다 → 라벨 **세 슬롯 전부**를 보는 방식으로 바꿔 해소 |
| P4-c CROSS-002 | **구현(절반)** | `dd9907b` | 단일 문장 S&P500 국내 순수추종 **24건** 불변 · CROSS-002 15R SQL 0행 → 30행 | 없음 |

### 🔴 리드 확인이 필요한 것

1. **동결선 갱신 1건** — `tests/snapshots/round6_fixedline.json` 11항(P2-a). 바뀐 값이 전부 심사관 DB 실측 gold 와
   일치하고, 감시선 `V8`(14행)·`V9`(8행)은 **15R 프로덕션 답변과 바이트 동일**이다(스냅샷의 r5 값 15/22 가 14R 표시 접기 이전이라 낡았다).
   `T4`·`KG-009` 는 무변경. 커밋 `af185ea` 본문에 qid 별 사유를 적었다.
2. **`FND-C02` gold 값 불일치** — gold 심사관은 207펀드/850클래스(`00040010` 단독)로 적었지만, 그 처방의 규칙
   (「라벨이 브랜드 어간을 품는 `Org_*` 노드」)을 그대로 집행하면 삼성액티브·삼성헤지 등 라벨에 '삼성' 이 든
   운용사가 함께 들어와 **217/906** 이 된다. 14R 재검 ③-1(브랜드 다코드 `IN`)과는 그쪽이 정합한다. 두 판정의 차이를 정해야 한다.
3. **gold ③-8·③-9(`FND-005` 랭킹 기계 조립 + 보수 ‰)와 gold ③-4(정렬축 검증)는 4라운드째 미착수**다. 다음 라운드 1순위 권고.
