# 17R 서버 실측 — enforce 슬롯 P0 3종 전환 후 (2026-09-03)

> 배포 `c8aa2b8` (내 몫은 `b69d9aa` — 조상 관계 확인). 서버 `/health` `agent_ready:true`.
> 문항 239 (`eval/probe_all_2026-09-03_r9.txt` 그대로) · 산출 `eval/probe_all_2026-09-03_r17.json`.
> 채점 `eval/score_round.py` → `..._r17_scored.json`.
> 전환 내역: `기본모수`(BASEPOP) · `펀드단위`(FUNDUNIT) · `기초지수`(IDXCANON) 슬롯 점등,
> 짝 가드 3종은 표식 침묵만 걸고 **삭제하지 않았다**(절차 §5 — 두 라운드 뒤).

---

## 0. 한 줄

**겨냥한 축(클래스↔펀드)은 닫혔다.** 슬롯 발동 60건(FUNDUNIT 52 · BASEPOP 7 · IDXCANON 1),
축 84.5%→93.0%·단위 98.5%→**100%** 를 회귀 0으로 얻었다.
남은 실패는 슬롯과 무관한 **검사기 기각 후 재생성 실패** 계열이다.

---

## 1. 회귀선 4개 — 전부 유지

| 회귀선 | 기준 | 17R | 판정 |
| :-- | :-- | :-- | :-: |
| 개별 조회 90% | 이름으로 상품 하나를 지정한 21문항(`eval/probe_h2_solo.txt`)에서 실행·거절이 정상인가 | **21/21 (100%)** — 15R 과 동일 | ✅ |
| 환각 방지 9/9 | 불가응답 9문항이 거절 문구로 끝나는가 (UNANS-001~005 · OFFICIAL-NA-001~003 · FND-R01) | **9/9** | ✅ |
| S8 · R1 병기 | 두 문항 답변이 펀드수·클래스수를 병기하는가 | S8 `129개(클래스 625개)` · R1 `19범주 · 클래스 8,969 · 펀드 3,040` — **15R 과 바이트 동일** | ✅ |
| 답변형 CLARIFY/REFUSE 오발동 0 | 답변형 문항이 거절로 새지 않는가 | 15R 대비 **새 오발동 0**. FND-C01·C02·R02·R03 은 15R 에서도 ❌ 였다(신규 아님) | ✅ |

🔴 환각 9/9 를 처음엔 7/9 로 잘못 셌다 — qid 접두(`UNANS-*`)로 모수를 잡아 `UNANS-006`("수익률 좋은 펀드 추천")이
실패로 걸렸다. 이 문항은 **리드 결정(08-31)으로 clarify→answer 전환**된 것이라 답변이 정답이다.
모수는 gold 의 `expected_behavior` 로 잡아야 한다.

---

## 2. 겨냥 문항 — 닫힘 목록

| 문항 | 15R | 17R | DB 실측 | 슬롯 |
| :-- | :-- | :-- | --: | :-- |
| **KG-025** 삼성자산운용 공모펀드와 국내 ETF | **850**(클래스) / 240 | **207** / 240 | 207 ✅ | FUNDUNIT |
| **X9** 미래에셋자산운용 〃 | **2,066**(클래스) / 230 | **823** / 230 | 823 ✅ | FUNDUNIT |
| **Z13** KB자산운용 〃 | — | **129** / 142 | 129 ✅ | FUNDUNIT |
| **X8** S&P500 벤치마크 펀드 / 추종 ETF | — | **57** / 24 | — | FUNDUNIT |
| **KG-026** KOSPI200 벤치마크 펀드 / 추종 ETF | **2,166**(클래스) / 34 | **807** / 34 (SQL) | — | FUNDUNIT |
| **S8** KB자산운용 공모펀드 | 129(클래스 625) | 129(클래스 625) | 129 ✅ | FUNDUNIT |

15R 최다 오답 유형(**클래스 수를 펀드 수로 답함**)이 닫혔다. 세 수치는 DB 대조로 확인했다.

🔴 **KG-026 — 채점상 축·단위 True 인데 답변은 거절이다. 한 줄로 맞추면:**
**SQL 은 닫혔지만(펀드키 GROUP BY 적용 · 807/34) 값 검사기가 `bmrk_nm` IN 목록의 리터럴 하나
(`'KOSPI200 5% + 회사채I-BBB종합 02Y 95%'`)를 "DB 에 없는 값" 으로 잡아 실행 전에 문장 전체를 기각했다** —
축은 고쳐졌고 발목을 잡은 것은 다른 층이다. (채점기의 `ran` 열이 이 구분을 담는다. §4 판정식 참조.)

---

## 3. 회귀 — 원인 한 줄씩

### 3-1. 🔴 먼저 정정 — 앞서 보고한 회귀 목록이 틀렸다

첫 채점에서 `OFFICIAL-001`·`AA19` 를 회귀로 보고했으나 **둘 다 성공한 문항**이다.
채점기가 `[Plan] SQL 생성` **첫 블록만** 읽고 `[Plan] 재생성 SQL` 을 무시해, 기각된 SQL 을 채점했다.

| 문항 | 실제 경로 | 결과 |
| :-- | :-- | :-- |
| `OFFICIAL-001` (공식 예시) | 컬럼 검사기가 `sale_yn` 기각 → **재생성 1회** → 30행 조회 | ✅ "전체 15,792종목 … 그중 30개" |
| `AA19` | `zrin_val` 기각 → 재생성 → 5행 조회 | ✅ 삼성전자 24.95% · SK하이닉스 15.9% … |

→ **"실행 전 컬럼 존재 검증기" 는 이미 있고 이미 작동한다**(`guard.unknown_columns`).
"없는 컬럼이면 재생성 1회, 그래도 없으면 거절" 까지 구현돼 있다. 새로 만들 것이 없다.

### 3-2. 정정 후 범위 회귀 6건 — 결과집합 기준 회수 여부

| 문항 | 15R | 17R | 원인 (한 줄) | 회수 |
| :-- | :-- | :-- | :-- | :-: |
| **KG-030** 미래에셋차이나솔로몬 시리즈 | 정상 실행 | 기각·종료 | HCX 가 펀드 문장에 채권 컬럼 `mat_dt` 를 써 컬럼 검사기가 기각, **재생성도 같은 컬럼 반복** | ❌ |
| **FND-R04** 내년 수익률 추천 | 정상 실행(거절 문구) | 기각·종료 | 〃 `mat_dt` — 같은 표현형 | ❌ |
| **OFFICIAL-002** 국민성장펀드 구조·동향 | 0행 | 0행 | 상품 고유명 토큰이 머리명사를 못 떼 `LIKE '%국민성장펀드%'` — 실제 이름은 `…국민성장혼합자산투자신탁` (§5 수리) | ❌ |
| **Z10** 해외주식형 중 인도주식 유형 | 기각 | 기각 | **회귀 아님** — 15R 도 같은 기각(`zrin_btyp_nm='인도주식형'` 없는 값). **답변 문구만 바뀌었다** | — |
| **AA6** 〃 베트남주식 유형 | 기각 | 기각 | 〃 | — |
| **CROSS-001** 삼성전자 보유 ETF·펀드 TOP10 | 답변 | 기각·종료 | 15R 도 `mtco_itm_nm` 컬럼 기각은 같았으나, 15R 은 재생성이 살아나 답했고 17R 은 못 살아났다 | ❌ |

**정리 — 진짜 회귀는 3건**(`KG-030`·`FND-R04`·`CROSS-001`)이고, 전부 같은 얼굴이다:
**검사기가 옳게 기각했으나 재생성 1회가 같은 실수를 반복해 무응답으로 끝났다.**
`Z10`·`AA6` 은 회귀가 아니라 **거절 문구 변화**다 —
15R `요청하신 조건의 값이 데이터에 없어 확인할 수 없습니다` → 17R `질의를 안전하게 실행할 수 없어 답변을 제공하지 못했습니다`.
뒤 문구는 사용자에게 **이유를 알려 주지 않는다.** 전체로 보면 이 문구가 15R 5건 → 17R 7건으로 늘었다.

### 3-3. 슬롯 귀속

회귀 6건 중 슬롯 표식이 있는 것은 `Z10`·`AA6` 둘뿐이고, 그 둘은 **15R 에도 같은 기각**이었다
(슬롯이 원인이 아니다 — 기각 사유가 `zrin_btyp_nm` 값이고 슬롯은 모수·집계축만 건드린다).
나머지 4건은 표식이 없다. **슬롯이 만든 회귀는 0건이다.**

### 3-4. CROSS-002 — 수리하지 않는다 (리드 판단 대기)

| | |
| :-- | :-- |
| 15R | 전면 거절 — "조건에 해당하는 상품이 데이터에서 확인되지 않습니다" |
| 17R | 부분 답변 — "국내 ETF 의 총보수는 수록되지 않았습니다. 해외 ETF 는 0.39%~1.0%" |
| 원인 | **두 ETF 테이블 JOIN 문장이라 P0-3 슬롯이 안 걸렸다.** 슬롯 `when.tables:[domestic_etfs]` 는 맞지만 `sql.has:["cu_base_index"]` 가 이 문장엔 없다(HCX 가 이미 `ref_base_index` 를 썼다). 남은 것은 데이터 한계 — 국내 ETF 총보수 `cu_charge_rt` 유효 67건 |
| 판단 | 개선이지 닫힘이 아니다. **프리즈 전 범위인지는 리드가 정한다** |

---

## 4. 4열 집계 — 분모와 판정식

판정식을 적어 둔다. 이 표는 **기계 채점**이고 15R 품질 보고서(심사관 판정 ✅170/231)와 층위가 다르다 — 직접 비교하지 말 것.

| 열 | 분모(대상 조건) | 판정식 |
| :-- | :-- | :-- |
| 값 | `gold_sql` 이 있는 문항 | 생성 SQL 결과집합 == gold 결과집합. 비교는 `run_paired.rows_match`(첫 컬럼 집합 일치, 또는 행 수 동일 + gold 첫 컬럼 값이 어느 컬럼엔가 전부 등장) — 전체 튜플 일치로 보면 컬럼 순서 차이가 오답으로 잡힌다 |
| 축 | SQL 에 `public_funds` 가 있고 질문이 개수형(`몇 개`·`개수`) | SQL 에 펀드키(`or_co_xtn_itt_cd` … `mtco_itm_no`)가 나타나는가 |
| 단위 | SQL 에 `public_funds` 가 있고 집계·랭킹형(`ORDER BY`·`COUNT/SUM/AVG`) | `sale_yn` 과 `prvo_pbff_desc` 가 **둘 다** 있는가 |
| 범위 | 전 문항 | **파이프라인이 실제로 실행했는가**(`[Execute] N행`)가 1순위. 실행 못 했으면(`[Decision] … 종료`) 거절 문구가 있어야 통과 |

| 열 | 15R | 17R | 회귀 | 이득 |
| :-- | --: | --: | --: | --: |
| 값 | 10/23 (43.5%) | 9/23 (39.1%) | 1 | 0 |
| **축** | 60/71 (84.5%) | **66/71 (93.0%)** | **0** | **+6** |
| **단위** | 201/204 (98.5%) | **204/204 (100%)** | **0** | **+3** |
| 범위 | 224/239 (93.7%) | 222/239 (92.9%) | 6 | 4 |

결과집합 기준 라운드 대비: 동일 181 · 결과 달라짐 35 · 둘 다 실행불가 15 · 회귀 4 · 개선 4.
실행 여부: `ran=True` 217 · `ran=False` 22.

🔴 회귀는 **SQL 문자열이 아니라 결과집합**으로 봤다. 문자열 diff 를 회귀로 세면 노이즈가 실제 회귀를 덮는다 —
H2 검증 실측에서 문자열 7건 변경 중 결과가 실제로 달라진 것은 1건뿐이었다.

### 채점기가 세 번 틀렸다 (다음 라운드를 위해 남긴다)

| # | 증상 | 원인 | 고친 뒤 |
| :-: | :-- | :-- | :-- |
| ① | 범위 6.7% | "마지막 SELECT" 로 뽑으니 trace 산문 `[Guard] SQL 검사 통과 (SELECT 단일문 …)` 이 잡혔다 | 92.9% |
| ② | 값 0/23 | 전체 튜플 일치 비교 — 컬럼 순서만 달라도 오답 | 9/23 |
| ③ | **성공 문항을 회귀로 보고** | `[Plan] SQL 생성` 첫 블록만 읽고 `[Plan] 재생성 SQL` 무시 | `OFFICIAL-001`·`AA19` 정상으로 정정 |

②③ 은 **결론을 바꾸는 오류**였다. ③ 때문에 없는 결함을 고칠 뻔했다.

---

## 5. 수리 — 항목 2·3 (17R 이후, 배포 전)

| 항목 | 대상 | 고친 것 | paired 4열 | 회수 |
| :-: | :-- | :-- | :-- | :-- |
| 2 | `KG-026` | `nearest_enum_value` 가 **내부 공백**도 흡수한다. DB 값 `'KOSPI200 5% +  회사채I-BBB종합 02Y  95%'`(‘+’ 뒤·‘95%’ 앞 두 칸)를 HCX 가 한 칸으로 써 48행 실재 값이 기각됐다 | 전 열 회귀 0 · 교정 1건 | ✅ 값 검사 위반 0 · 실행 **813펀드 / 34ETF** |
| 3 | `OFFICIAL-002` | 상품 고유명 토큰이 **꼬리 상품명사**를 뗀다. `'국민성장펀드'` → DB 실재 형태 `'국민성장'`. 사전이 아니라 DB 실재 여부로 고른다 | 값 9→10 · 범위 222→223 · 회귀 0 | ✅ 리터럴 교정 확인 (서버 실측은 배포 후) |
| 4 | 거절 문구 7건 | 재생성 뒤에도 기각이면 **기각 부류를 사람 말 한 절로 앞에 붙인다**(`refusal_reason_text`). 부류 단위 매핑 하나 — 문항별 분기 없음. ① 문장 끝 거절 어휘 보존 ② 내부 식별자 비노출 | 전 열 회귀 0 · 개별조회 21/21 · 환각 **9/9** · S8·R1 바이트 불변 | ✅ `KG-030`·`X15`·`Z10`·`AA6`·`FND-R04`·`CROSS-001`·`CROSS-003` 이 이유 있는 문구로 |

항목 4 실측 — 7건 전부 실제 기각 사유가 `스키마에 없는 컬럼` 이라 같은 부류로 묶인다
(`Z10`·`AA6` 은 값 검사 실패 뒤 **재생성이 환각 컬럼**(`asset_class`·`fund_type`)을 내 이 부류로 끝났다):

```
전: 질의를 안전하게 실행할 수 없어 답변을 제공하지 못했습니다.
후: 질문하신 항목이 이 상품 유형의 데이터에 없어 답변을 제공하지 못했습니다.
```

부류를 못 가리면 종전 문구 그대로다(안전한 기본값). 사유 원문은 `think_trace` 에 그대로 남는다 —
사용자 문장에는 컬럼명·테이블명을 넣지 않는다.

🔴 항목 2 의 원래 지시는 "실행 전 컬럼 존재 검증기 신설" 이었으나 **이미 있고 이미 작동한다**(§3-1).
그 자리에서 실제로 문장을 죽이고 있던 것은 **값 검사기 과잉 기각**이라 그쪽을 잡았다.
둘 다 "검사기가 옳게 잡았는데 재생성이 못 살렸다" 는 같은 얼굴의 다른 층이다.

## 6. 수리하지 않는 것 — 원인만 (항목 4)

| 문항 | 원인 | 왜 지금 안 고치나 |
| :-- | :-- | :-- |
| **KG-030** 미래에셋차이나솔로몬 시리즈 **[프리즈 후]** | HCX 가 펀드 문장에 채권 컬럼 `mat_dt` 를 썼다. 컬럼 검사기가 옳게 기각했고 **재생성이 같은 컬럼을 반복**해 무응답. 15R 에는 이 표현형이 없었다 | 문항별 수리가 아니라 **재생성 프롬프트가 기각 사유를 더 잘 쓰게 하는 것**이 처방이다. `FND-R04`·`CROSS-001` 도 같은 얼굴이라 셋을 한 번에 다뤄야 한다 |
| **CROSS-002** S&P500 국내·해외 ETF 총보수 **[프리즈 후]** | 15R 전면 거절 → 17R 부분 답변("국내 총보수 미수록 · 해외 0.39~1.0%"). **두 ETF 테이블 JOIN 문장이라 P0-3 슬롯이 안 걸렸다** — `when.tables:[domestic_etfs]` 는 맞지만 `sql.has:["cu_base_index"]` 가 이 문장엔 없다(HCX 가 이미 `ref_base_index` 를 썼다). 남은 것은 데이터 한계 — 국내 ETF 총보수 `cu_charge_rt` 유효 67건 | 개선이지 닫힘이 아니다. **프리즈 전 범위인지는 리드가 정한다** |

거절 문구 문제도 남긴다 — `Z10`·`AA6` 은 회귀가 아니지만 문구가
`요청하신 조건의 값이 데이터에 없어 확인할 수 없습니다`(이유 있음) 에서
`질의를 안전하게 실행할 수 없어 답변을 제공하지 못했습니다`(이유 없음) 로 바뀌었고,
이 문구가 15R 5건 → 17R 7건으로 늘었다. 사용자에게 무엇이 왜 안 되는지 알려 주지 않는다.

## 7. 다음

- ~~`OFFICIAL-002` 이름 토큰 수리~~ → **완료 (§5 항목 3)**
- `KG-030`·`FND-R04`·`CROSS-001` — **프리즈 후.** 문구는 항목 4 로 개선했고 원인 수리는 남는다. 셋 다 "검사기 옳게 기각 → 재생성 실패" 한 얼굴이고,
  처방은 문항별이 아니라 **재생성 프롬프트가 기각 사유를 더 잘 쓰게 하는 것**이다. 프리즈 전 범위인지는 리드가 정한다.
- `CROSS-002` — **프리즈 후** (§3-4·§6)
- 코드 가드 삭제는 두 라운드 뒤(절차 §5). **17R 이 1라운드째다.**
