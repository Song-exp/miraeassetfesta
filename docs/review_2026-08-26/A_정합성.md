# A. 데이터 정합성

> 값이 서로 말이 되는가 — 판정과 실측, 행 단위, 외부 대조, 기준일.

| | |
| :-- | :-- |
| **왜 보나** | 여기가 틀리면 아래 모든 판정이 잘못된 수치 위에 세워진다. |
| **어떻게** | 표의 자동 근거를 보고 어긋난 쪽이 판정인지 데이터인지 가른다. |
| **담당** | 채권 A-1-01~05·A-2-01·A-4-02 → **seohyun** · 국내ETF A-1-06~10·A-2-02·A-3-01·A-4-03 + 해외ETF A-1-11~17·A-2-03·A-3-02·A-4-04 → **Leebyungchul** · 펀드 A-1-18~27·A-2-04·A-3-03~04·A-4-05~06 → **song** · 공통 A-4-01 → song(리드) |
| **방법** | [`에이전트검토_프롬프트.md`](에이전트검토_프롬프트.md) 의 지시서를 LLM 에 넣고 카테고리 단위로 보고받아 승인 → 기입 |

> `판정` 칸에 **✅ 이상없음 · ⚠️ 확인필요 · ❌ 고쳐야함** 중 하나를, `근거·조치` 칸에 판단 근거와 고칠 곳(`ontology/enums/*.yaml` 등)을 적습니다.

---

## A-1. 판정 ↔ 실측 불일치

yaml 판정은 사람이 쓰고 수치는 배포본마다 바뀌므로 조용히 어긋난다. 어긋난 채로 두면 **답변 모수가 틀린다.**

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-1-01 | 채권 `avg_annual_tax_yield` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **0** vs 실측 non-null **634** | ✅ | `answerable_n=0` 은 non-null 634 가 아니라 **답변에 쓸 수 있는 값의 수**다. non-null 634 = 매수수익률 있는 판매LOT 634 와 정확히 같은 집합인데 그 634건이 **전부 0**(주최 미제공)이라 쓸 수 있는 값은 0건 → 기재값이 맞다. yaml 이 이미 `과세수익률금지`(전량 0, 사용 금지)로 선언. `note` 에 "non-null 634 = 판매LOT 634 동일 집합" 보강 |
| A-1-02 | 채권 `exg_close_price` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **1,270** vs 실측 non-null **17,746** | ✅ | `answerable_n=1270` 은 non-null 이 아니라 **장내 한정 모수**(`pd_exg_mkt='장내' AND exg_close_price > 0`). 장내 17,746행 중 16,476행은 종가 0 = "최근 거래 없음"이고 실거래 흔적은 1,270행(7%). 장외 4,136행은 개념 자체가 없어 NULL. `장내종가` 규칙에 "장내 1,270행만 유효 · 기준일 병기 필수"가 이미 있음 → 값이 틀린 게 아니라 `answerable_n` 의 뜻이 두 가지로 쓰이는 것이 문제. `note` 에 종가 기준일 분포 보강 |
| A-1-03 | 채권 `pd_nm` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **20,497** vs 실측 non-null **21,882** | ✅ | `answerable_n=20497` 은 행이 아니라 **개체(distinct ISIN) 수**. 결측 0 · 행 21,882 · 차이 1,385 = 같은 채권이 두 줄 이상(장내+장외). 종목명 distinct 는 20,499 로 ISIN 보다 2 많은데 같은 ISIN 인데 "제163회 (사)" vs "제163회(사)" 처럼 공백 하나가 다른 사례 → `trap` 에 2종 추가. `answerable_n` 이 이 두 컬럼만 개체 기준인 것은 규약 문제라 **B-4 로 승격** |
| A-1-04 | 채권 `pd_no` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **20,497** vs 실측 non-null **21,882** | ✅ | A-1-03 과 동일 원인. 결측 0 · 고정폭 패딩 0 · distinct 20,497 = `answerable_n` 과 일치. yaml 조치 없음 |
| A-1-05 | 채권 `srfc_irt` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **1**행 / 21,882 | ❌ | 결측 1행은 `XS3067881758` 뱅크오브아메리카 발행 달러채(2055년 만기, 무이표) — 국내 채권 DB 에 외국 발행채가 하나 섞였고 표면금리·만기일·발행일·통화(`'000'`)가 전부 비었다. `domestic_bonds.yaml` `columns.srfc_irt.missing_reason: none → mixed` + `missing_semantics.null` 신설. 부수 발견이 더 중요 — `query_rules.외화채없음` 의 "curr_cd = KRW 뿐"은 오기(`'000'` 1행 존재)라 **승인된 (가)안**대로 `curr_cd='KRW'` 를 전 질의 기본 조건으로 명문화. 재확인: NULL 1 · 공백 위장 0 |
| A-1-06 | 국내ETF `du_clpr` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **4**행 / 1,780 | ☐ |  |
| A-1-07 | 국내ETF `du_nav_rnf_amt` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ☐ |  |
| A-1-08 | 국내ETF `du_nav_yday` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ☐ |  |
| A-1-09 | 국내ETF `pd_curr_cd` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **3**행 / 1,780 | ☐ |  |
| A-1-10 | 국내ETF `pd_sect_cd` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ☐ |  |
| A-1-11 | 해외ETF `cu_fund_mgmt_co` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **12**행 / 6,037 | ☐ |  |
| A-1-12 | 해외ETF `du_er_1d` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ☐ |  |
| A-1-13 | 해외ETF `du_last_aum` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **205**행 / 6,037 | ☐ |  |
| A-1-14 | 해외ETF `du_val_1d` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ☐ |  |
| A-1-15 | 해외ETF `ru_mkt_price` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ☐ |  |
| A-1-16 | 해외ETF `wu_inv_ast_type` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **11**행 / 6,037 | ☐ |  |
| A-1-17 | 해외ETF `wu_inv_rgn` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **11**행 / 6,037 | ☐ |  |
| A-1-18 | 펀드 `fd_mm1_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,386** | ☐ |  |
| A-1-19 | 펀드 `fd_mm3_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,307** | ☐ |  |
| A-1-20 | 펀드 `fd_mm6_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,220** | ☐ |  |
| A-1-21 | 펀드 `fd_nast_suma` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **8,341** vs 실측 non-null **9,413** | ☐ |  |
| A-1-22 | 펀드 `fd_yr1_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,022** | ☐ |  |
| A-1-23 | 펀드 `fd_yr2_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **6,757** | ☐ |  |
| A-1-24 | 펀드 `fd_yr5_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **5,979** | ☐ |  |
| A-1-25 | 펀드 `thco_sale_yn` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **13,079**행 / 23,676 | ☐ |  |
| A-1-26 | 펀드 `trusc_xtn_itt_cd` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **42**행 / 23,676 | ☐ |  |
| A-1-27 | 펀드 `zrin_fd_ivst_risk_gcd` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **8,547** vs 실측 non-null **8,689** | ☐ |  |

## A-2. 행 단위(grain) — `COUNT(*)` 가 개체 수인가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-2-01 | 채권 (`pd_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **21,882** / 개체 **20,497** · 중복 1,385 | ✅ | `row_grain.대표행` 과 `query_rules.대표행`(GROUP BY pd_no)이 이미 있어 집계는 행이 아니라 개체를 센다 → 판정 자체는 ✅. 단 yaml 설명이 부정확 — "중복행은 속성이 같다(평가가 다른 8건 제외)"는데 그 8종목(KAMCO 163·164 · NH농협캐피탈 269-1~6)은 **장외행에 종류·신용등급·발행사·위험등급·듀레이션이 전부 NULL** 이고 평가가가 액면 수준(10,000.00~10,000.12). 어느 줄을 집느냐로 "AA-" 와 "정보 없음"이 갈린다 → 두 곳 모두 **병기 규칙**으로 교체(장내 우선 필수는 철회, 08-27 승인). 병기 조건 2가지 포함 — 값이 같으면 한 번만 답한다 · 장외 가격엔 "액면가 수준" 단서를 붙인다 |
| A-2-02 | 국내ETF (`pd_itm_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **1,780** / 개체 **1,780** · 중복 0 | ☐ |  |
| A-2-03 | 해외ETF (`pd_isin_cd`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **6,037** / 개체 **5,962** · 중복 75 | ☐ |  |
| A-2-04 | 펀드 (`itm_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **23,676** / 개체 **23,676** · 중복 0 | ☐ |  |

## A-3. 외부 대조 — 조인이 실제로 붙는가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-3-01 | `ext_etf_holdings` → `domestic_etfs` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **1,160** · 마스터 조인 **1,160** (100.0%) · 키 `etf_code`=`pd_itm_no` | ☐ |  |
| A-3-02 | `ext_ovs_etf_holdings` → `overseas_etfs` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **1,356** · 마스터 조인 **1,356** (100.0%) · 키 `isin`=`pd_isin_cd` | ☐ |  |
| A-3-03 | `ext_fund_holdings` → `public_funds` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **2,902** · 마스터 조인 **2,898** (99.9%) · 키 `grp`=`mtco_itm_no` | ☐ |  |
| A-3-04 | `ext_fund_page` → `public_funds` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **10,565** · 마스터 조인 **10,565** (100.0%) · 키 `itm_no`=`itm_no` | ☐ |  |

## A-4. 기준일 — 언제 기준의 사실인가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-4-01 | 마스터 4테이블 | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-22` | ☐ |  |
| A-4-02 | 채권 `info_base_dt` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `20260821 ~ 20260821` | ✅ | `info_base_dt` 21,882행 전부 `20260821` 단일값이고 `query_rules.기준일` 에 병기 규칙이 있음. 판매수익률 기준일(`sale_yield_base_dt`)도 판매LOT 634행 전부 동일. yaml 조치 없음. 부수: 등급일(`crd_grd_dt`) 0 인 행 1건 — `등급일사용금지` 규칙이 이미 덮고 있어 결함 아님 |
| A-4-03 | `ext_etf_holdings.as_of` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-21 ~ 2026-08-21` | ☐ |  |
| A-4-04 | `ext_ovs_etf_holdings.report_date` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2025-10-31 ~ 2026-06-30` | ☐ |  |
| A-4-05 | `ext_fund_holdings.bas_dt` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `20090202 ~ 20260601` | ☐ |  |
| A-4-06 | `ext_fund_page.retrieved_at` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-18T14:12:47+09:00 ~ 2026-08-18T14:12:47+09:00` | ☐ |  |