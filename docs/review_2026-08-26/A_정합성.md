# A. 데이터 정합성

> 값이 서로 말이 되는가 — 판정과 실측, 행 단위, 외부 대조, 기준일.

| | |
| :-- | :-- |
| **왜 보나** | 여기가 틀리면 아래 모든 판정이 잘못된 수치 위에 세워진다. |
| **어떻게** | 표의 자동 근거를 보고 어긋난 쪽이 판정인지 데이터인지 가른다. |
| **담당** | 채권 A-1-01~05·A-2-01·A-4-02 → **seohyun** · 국내ETF A-1-06~10·A-2-02·A-3-01·A-4-03 + 해외ETF A-1-11~17·A-2-03·A-3-02·A-4-04 → **Leebyungchul** · 펀드 A-1-18~27·A-2-04·A-3-03~04·A-4-05~06 → **song** · 공통 A-4-01 → song(리드) |
| **방법** | [`에이전트검토_프롬프트.md`](에이전트검토_프롬프트.md) 의 지시서를 LLM 에 넣고 카테고리 단위로 보고받아 승인 → 기입 |

> `판정` 칸에 **✅ 이상없음 · ⚠️ 확인필요 · ❌ 고쳐야함** 중 하나를, `근거·조치` 칸에 판단 근거와 고칠 곳(`ontology/enums/*.yaml` 등)을 적습니다.

---

## A-1. 판정 ↔ 실측 불일치

yaml 판정은 사람이 쓰고 수치는 배포본마다 바뀌므로 조용히 어긋난다. 어긋난 채로 두면 **답변 모수가 틀린다.**

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-1-01 | 채권 `avg_annual_tax_yield` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **0** vs 실측 non-null **634** | ☐ |  |
| A-1-02 | 채권 `exg_close_price` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **1,270** vs 실측 non-null **17,746** | ☐ |  |
| A-1-03 | 채권 `pd_nm` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **20,497** vs 실측 non-null **21,882** | ☐ |  |
| A-1-04 | 채권 `pd_no` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **20,497** vs 실측 non-null **21,882** | ☐ |  |
| A-1-05 | 채권 `srfc_irt` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **1**행 / 21,882 | ☐ |  |
| A-1-06 | 국내ETF `du_clpr` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **4**행 / 1,780 | ❌ | 결측 4건 = 판매종료(`pd_sale_yn=0`) 3건(ACE·TIGER 삼성전자SK하이닉스혼합50, BNK특수채) + ETN 1건('미래글로벌헬스케어제', 판매중인데 종가 NULL). `pd_curr_cd` 결측 3건과 동일 상품. **조치**: `domestic_etfs.yaml` `du_clpr.missing_reason: present → mixed`, answer_policy 에 '판매종료 상품은 시세 갱신 정지' 명시. ⚠️ ETN 1건은 판매중인데 시세 부재 — 별도 확인 필요 |
| A-1-07 | 국내ETF `du_nav_rnf_amt` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ❌ | 결측 182 = **ETN 123 + ETF 59**. ETF 59건 전부 `pd_sale_yn=0`(판매종료 75건 중 59), ETN 122/123 도 판매종료. 상장일 전부 2024년 이전 = 신규상장 아님(만기·청산 상품). `du_nav_yday`·`pd_sect_cd` 와 **완전 동일 집합(59/59·123/123)**. **조치**: `present → mixed` + '판매종료 상품은 시세 갱신 정지 → 시세 질의는 판매중 한정' |
| A-1-08 | 국내ETF `du_nav_yday` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ❌ | A-1-07 과 동일 집합·동일 원인(판매종료 182건). **조치**: `present → mixed`, A-1-07 과 함께 처리 |
| A-1-09 | 국내ETF `pd_curr_cd` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **3**행 / 1,780 | ❌ | 결측 3건 = 전부 판매종료 ETF, `du_clpr` 결측 4건 중 3건과 동일 상품. 통화코드는 상품 속성인데도 판매종료면 비어 있음 — 마스터가 판매종료 상품의 속성을 통째로 안 채우는 패턴. **조치**: `none → mixed` |
| A-1-10 | 국내ETF `pd_sect_cd` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **182**행 / 1,780 | ❌ | A-1-07 과 동일 집합(182 = ETN 123 + ETF 59). 🔴 **부수 발견**: `pd_sect_cd` 는 시세가 아니라 **분류 코드**인데도 판매종료면 결측 — 결측 원인이 '시세 갱신 정지'가 아니라 '판매종료 상품 속성 미기재' 임을 보여줌. **조치**: `present → mixed` |
| A-1-11 | 해외ETF `cu_fund_mgmt_co` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **12**행 / 6,037 | ❌ | 결측 12건, 전부 ETF(ETN 0). `du_er_1d` 결측 14건 집합에 포함되는 '시세 전무' 상품군 — 상품명이 티커와 동일(TAWN·BBLS·JPNU 등)해 정식 명칭조차 없음. **조치**: `overseas_etfs.yaml` `present → mixed` + A-1-12 와 함께 '통째 미수록 14건' 으로 일괄 서술 |
| A-1-12 | 해외ETF `du_er_1d` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ❌ | 결측 14건. `du_val_1d`·`ru_mkt_price` 와 **완전 동일 집합(14/14)** 이고 14건 전부 `du_clpr` 도 NULL = **시세가 통째로 없음**. 🔴 `pd_sale_yn=0` 은 0건 — 판매중으로 표시돼 있으나 실제로는 데이터 껍데기. **조치**: `present → mixed` + '시세·분류가 통째 부재한 14건은 어떤 질의에서도 제외' |
| A-1-13 | 해외ETF `du_last_aum` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **205**행 / 6,037 | ❌ | 결측 205건. A-1-12 의 14건과 **다른 집단** — 205건 전부 `du_clpr` 유효(정상 거래 중)이고 판매종료 0건. 즉 거래는 되는데 순자산만 미수록(소형 ETF 집계 누락 추정). **조치**: `present → mixed` + 'AUM 결측 205건은 거래중이나 순자산 미수록 → Top-N 에서 제외하고 모수 명시' |
| A-1-14 | 해외ETF `du_val_1d` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ❌ | A-1-12 와 동일 집합(14/14)·동일 원인(시세 전무). **조치**: A-1-12 와 함께 처리 |
| A-1-15 | 해외ETF `ru_mkt_price` | 판정 `present`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **14**행 / 6,037 | ❌ | A-1-12 와 동일 집합(14/14)·동일 원인. **조치**: A-1-12 와 함께 처리 |
| A-1-16 | 해외ETF `wu_inv_ast_type` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **11**행 / 6,037 | ❌ | 결측 11건, `wu_inv_rgn` 과 동일 집합(11/11)이며 **A-1-12 의 14건에 포함**(11/11). 시세뿐 아니라 자산군·지역 분류까지 없는 상품 = 마스터 수록 자체가 불완전. **조치**: `none → mixed`, A-1-12 와 함께 '통째 미수록' 으로 서술 |
| A-1-17 | 해외ETF `wu_inv_rgn` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **11**행 / 6,037 | ❌ | A-1-16 과 동일 집합·동일 원인. **조치**: `none → mixed` |
| A-1-18 | 펀드 `fd_mm1_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,386** | ☐ |  |
| A-1-19 | 펀드 `fd_mm3_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,307** | ☐ |  |
| A-1-20 | 펀드 `fd_mm6_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,220** | ☐ |  |
| A-1-21 | 펀드 `fd_nast_suma` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **8,341** vs 실측 non-null **9,413** | ☐ |  |
| A-1-22 | 펀드 `fd_yr1_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **7,022** | ☐ |  |
| A-1-23 | 펀드 `fd_yr2_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **6,757** | ☐ |  |
| A-1-24 | 펀드 `fd_yr5_ern_r` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **6,913** vs 실측 non-null **5,979** | ☐ |  |
| A-1-25 | 펀드 `thco_sale_yn` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **13,079**행 / 23,676 | ☐ |  |
| A-1-26 | 펀드 `trusc_xtn_itt_cd` | 판정 `none`(결측 없음)를 유지할지, 결측 사유를 적을지 | 실측 결측 **42**행 / 23,676 | ☐ |  |
| A-1-27 | 펀드 `zrin_fd_ivst_risk_gcd` | `answerable_n` 이 도메인 조건 포함 모수인지, 단순 non-null 인지 | 기재 **8,547** vs 실측 non-null **8,689** | ☐ |  |

## A-2. 행 단위(grain) — `COUNT(*)` 가 개체 수인가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-2-01 | 채권 (`pd_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **21,882** / 개체 **20,497** · 중복 1,385 | ☐ |  |
| A-2-02 | 국내ETF (`pd_itm_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **1,780** / 개체 **1,780** · 중복 0 | ✅ | 행 1,780 = `pd_itm_no` distinct 1,780 으로 **완전 유일**. 행이 곧 상품이라 `COUNT(*)` 를 그대로 써도 됨. `row_grain` 에 이미 동일 서술 있음 — 현행 유지 |
| A-2-03 | 해외ETF (`pd_isin_cd`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **6,037** / 개체 **5,962** · 중복 75 | ⚠️ | 행 6,037 vs `pd_isin_cd` distinct 5,962 — NULL 12 + 중복 ISIN 존재(예: `US00162Q3790` 2건). 유일 키는 `pd_itm_no` 뿐이며 `overseas_etfs.yaml` `_identifier_dup` 에 이미 기록돼 있음. **집계·조인은 `pd_itm_no` 로 강제**해야 하나, 현재 `query_rules` 에 명시 규칙이 없어 ⚠️. 리드 판단 요청 — B-2(판정 상속) 또는 D절(관계)로 승격 검토 |
| A-2-04 | 펀드 (`itm_no`) | 집계 질의가 행이 아니라 개체를 세도록 규칙이 걸려 있는가 | 행 **23,676** / 개체 **23,676** · 중복 0 | ☐ |  |

## A-3. 외부 대조 — 조인이 실제로 붙는가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-3-01 | `ext_etf_holdings` → `domestic_etfs` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **1,160** · 마스터 조인 **1,160** (100.0%) · 키 `etf_code`=`pd_itm_no` | ☐ |  |
| A-3-02 | `ext_ovs_etf_holdings` → `overseas_etfs` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **1,356** · 마스터 조인 **1,356** (100.0%) · 키 `isin`=`pd_isin_cd` | ☐ |  |
| A-3-03 | `ext_fund_holdings` → `public_funds` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **2,902** · 마스터 조인 **2,898** (99.9%) · 키 `grp`=`mtco_itm_no` | ☐ |  |
| A-3-04 | `ext_fund_page` → `public_funds` | 안 붙는 키가 보완 가능한지, 커버리지를 답변에 어떻게 밝힐지 | 수집 키 **10,565** · 마스터 조인 **10,565** (100.0%) · 키 `itm_no`=`itm_no` | ☐ |  |

## A-4. 기준일 — 언제 기준의 사실인가

| ID | 대상 | 확인할 것 | 자동 수집 근거 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- | :-: | :-- |
| A-4-01 | 마스터 4테이블 | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-22` | ☐ |  |
| A-4-02 | 채권 `info_base_dt` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `20260821 ~ 20260821` | ☐ |  |
| A-4-03 | `ext_etf_holdings.as_of` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-21 ~ 2026-08-21` | ☐ |  |
| A-4-04 | `ext_ovs_etf_holdings.report_date` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2025-10-31 ~ 2026-06-30` | ☐ |  |
| A-4-05 | `ext_fund_holdings.bas_dt` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `20090202 ~ 20260601` | ☐ |  |
| A-4-06 | `ext_fund_page.retrieved_at` | 답변에 이 기준일을 병기하는 규칙이 있는가 | `2026-08-18T14:12:47+09:00 ~ 2026-08-18T14:12:47+09:00` | ☐ |  |