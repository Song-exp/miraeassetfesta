# 재배포 후 재실측 체크리스트 (2026-09-01)

> 목적: 08-31 밤 수정분(커밋 `312614d` → `28e6a18`, 전부 push 완료)이 **실서버(HCX)에서 실제로 먹히는지** 확인.
> 역할: 배포는 리드 담당. 이 문서 작성자는 질문을 쏘고 아래 기준으로 판정만 한다.
> 질문 파일: `eval/probe_recheck_2026-09-01.txt` (18문항 — 프로브10 + 은행채 재검 + BND-S 7문항. BND-S-004·006은 Q5·Q10과 문구가 같아 중복 제거).

## 0. 리드에게 전달할 배포 요청 (그대로 복사)

> 커밋 `28e6a18`까지 main에 push 완료했습니다. **`bash deploy/deploy.sh --code-only`** 로 배포 부탁드립니다.
> - `src/runtime/`(guard·pipeline·router)이 바뀌어서 `--yaml-only`(restart만)로는 반영이 안 됩니다 — src는 이미지에 구워지므로 재빌드 필요.
> - `ontology/shared/*.yaml`은 안 건드렸으니 **DB 재전송(`--db-only`)은 불필요**합니다 (enums yaml·common.ttl은 마운트/pull로 따라감).
> 배포 끝나면 알려주세요. 재실측 18문항 돌리겠습니다.

## 1. 실측 방법

- **서버 경유(정식)**: 브라우저에서 `https://49.50.134.229.nip.io/answer?question_id=RC-001&question=<질문>` — JSON에 `think_trace`가 그대로 나오므로 Q7 게이트·Q9 지연·은행채 기각 사유 확인은 이 경로로. 팀 UI(`/chat?t=<CHAT_TOKEN>`)는 답변 확인용.
- **로컬 대안**: 배포가 늦어지면 `python -m src.chat -f eval/probe_recheck_2026-09-01.txt` 로 같은 파이프라인 + 실제 HCX를 로컬에서 돌릴 수 있음(.env에 HYPERCLOVA_API_KEY 필요, 로그는 `logs/chat-YYYYMMDD.jsonl`). 단, **응답시간·서버 환경 판정은 서버 실측만 인정**.
- 판정 기록은 이 파일의 표에 직접 채운다. 오답은 `eval/questions_domestic_bonds.jsonl` 승격 후보.

## 2. 프로브 10문항 — 합격 기준

원문 미기록 문항(1·3·6·8)은 이번 문구로 통일해 앞으로 재현 기준으로 삼는다.

| # | 질문 | 합격 기준 | 실측 | 판정 |
|---|---|---|---|---|
| 1 | 가장 안전한 채권 3개 추천해줘 | 전부 **6등급(매우낮은위험)**. 1위 한국수출입금융(구조) 1604바 6.231% · 2위 토지주택채권 330 5.859% · 3위 주금공 MBS2022-19 5.651%. SC은행·5등급 나오면 실패 | | |
| 2 | 한전 채권 중 만기가 가장 긴 건 뭐야? | 1184 · 2052-04-21 + 잔존 **"9375일(약 25.7년)"** 표기 그대로. "93.75년" 재발이면 실패 (÷100 환각 가드) | | |
| 3 | 제일 싼 채권 알려줘 | **되묻기**(가격 기준: 단가/수익률/최소투자금 무엇인지). 단정 답변이면 실패 — 직전 실측 오답, 가드 신설 후 첫 확인 | | |
| 4 | 달러로 발행된 채권 알려줘 | **"수록 없음"** (curr_cd USD 0행). `curr_cd='XS'` 류 환각·해외ETF 유도·BAC '000' 행 노출이면 실패 — 가드 신설 후 첫 확인 | | |
| 5 | 가장 안전한 회사채 3개 추천해줘 | 폴백 발동 → **SC은행 AAA 5등급 3종**(7.123·7.095·6.381%) + "6등급엔 회사채 없음·더 안전한 6등급 안내" 문구 + 6% 초과 주의 문구. 0행 "확인되지 않습니다"면 실패 | | |
| 6 | 기초지수를 추종하는 채권 알려줘 | **즉시 기각**(채권에 기초지수 개념 없음). 지어낸 목록이면 실패 | | |
| 7 | 신용등급 AAAA인 채권 알려줘 | 기각 + trace에 `[Gate]`/`[Refuse]`, **HCX 호출 0회** (배포 스모크와 동일 경로) | | |
| 8 | 국고채는 총 몇 종목이야? | **295종목** (국고채권 274 + STRIPS 21). 274나 "미수록"이면 실패 — ensure_ktb_kind 확장 검증 | | |
| 9 | A등급 이상 회사채 중 표면금리 높은 순으로 5개 알려줘 | 1위 **우리금융캐피탈458 7.5%** · 엠캐피탈355 7.5% · 한국토지신탁44-2 7.402%. SC은행 5.2%가 1위면 실패. **응답시간 기록**(목표 ≤15초) + 30초대면 trace에서 재생성 경로 여부 확인 | | |
| 10 | 보험사가 발행한 채권 중에 제일 안전한 걸로 추천해줘 | 0행 + **자연어 사유 문장**("보험회사채 99건·6등급 N건은 각각 수록돼 있으나 동시 만족 없음" 꼴). SQL 조각·"조건별 단독 조회:" 개발자 표기 노출이면 실패 — user_text 신설분 첫 확인 | | |

## 3. 은행채 기각 재검 (#11)

| 질문 | 합격 기준 | 실측 | 판정 |
|---|---|---|---|
| 은행채는 몇 종목이나 있어? | **695종목** (707행이라 하면 대표행 중복 오답). trace에서 **validate_sql 기각이 안 뜨는지** 확인 — 6차 실측 때 REPLACE·WITH 오탐 기각 추정으로 허용 처리했는데 실제 기각 사유를 서버 trace로 본 적 없음. 기각이 또 뜨면 사유 원문을 그대로 기록 | | |

## 4. BND-S 안전 최상급 프로브 (12~18번 줄)

기대 상세·gold SQL은 `eval/questions_top_safety_probe.jsonl` 참조. 요지만:

| qid | 질문 | 합격 기준 | 실측 | 판정 |
|---|---|---|---|---|
| S-001 | 가장 안전한 채권 5개 추천해줘 | 5행 전부 6등급. SC은행·5등급 금지 | | |
| S-002 | 리스크가 가장 낮은 채권 3개만 골라줘 | '리스크' 변종 트리거 — 3행 전부 6등급 | | |
| S-003 | 가장 안전한 은행채 3개 추천해줘 | 특수은행채 6등급 3종(수출입 6.231·산금채 5.247·5.192) + "특수은행채 포함" 병기. SC은행이면 실패 | | |
| S-005 | 가장 안전한 채권 중에 수익률 6.5% 넘는 것도 있어? | **"없다"** + 6등급 최고 6.23% 제시 권장. 지어내면 최대 감점 | | |
| S-007 | 가장 안전한 채권이랑 가장 위험한 채권 하나씩 | 6등급 수출입 6.231% + 1등급 신보2024 728.5% C0(주의 문구 필수). 방향 뒤집힘이 고전 오답 | | |
| S-008 | 저는 안정형 투자자인데 어떤 채권을 사면 좋을까요? | 안정형 = **16 단독** 5행. "5등급까지 포함"이 보이면 실패(안정추구형과 혼동) | | |
| S-009 | 원금 잃기 싫은데 채권 뭐 사면 돼? | **가드 사각지대 확인 문항** — 이상적: 6등급 단독. HCX가 15·16으로 내면 5·6등급 라벨 명시 + '더 안전한 6등급' 문구까지 있으면 부분 합격. 라벨 없이 섞으면 실패 | | |

## 5. 실측 후 후속 결정 (이 결과가 근거가 되는 것)

- **compose_answer 가드 설계** — Q2 단위·Q10 사유 문장 등 답변층 결과를 보고 rows 전건 대조 가드가 필요한지 판단.
- **build_derived 여부** — 규칙 승격이 이번에도 무시되는 문항이 나오면 발동.
- **"국고채 포함 국공채 전체" 잔여 위험(§4-3 ⚠)** — 이 질의형이 관측되면 재론.
- 감수 판정·오답은 이 표에 채운 뒤 `채권_프로브10_실측` 문서 방식으로 진단 기록을 남긴다.
