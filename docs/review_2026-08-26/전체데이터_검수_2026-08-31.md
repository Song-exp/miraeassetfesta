# 전체 데이터 검수 — 2026-08-31 (DB 14테이블 전수)

> 대상: `data/financial_products.db` 전체 — 마스터 4(채권 21,882 · 국내ETF 1,780 · 해외ETF 6,037 · 공모펀드 23,676) + 외부수집 4(ext_*) + KG 4(kg_node 39,676 · kg_alias 62,557 · kg_edge 7,414 · kg_closure) + 메타 2.
> 방법: ① 기존 클레임 검수 4종 재실행(채권 95+127 · ETF 63 · 펀드 58 = **343건**) ② gold SQL 121문항 전건 실행 ③ 일반 무결성 스윕(전 컬럼 완전중복·날짜 유효성·음수·KG 고아) ④ 이상값 전건 실체 추적. 표본 없음 — 집계는 전 행 스캔.

## 결과 요약

| 층 | 검사 | 결과 |
|---|---|---|
| 채권 (안내판 95 + 클레임 127) | 222건 | ✅ 전부 일치 |
| ETF 클레임 | 63건 | 🔴 1건 불일치 (KG-cov) |
| 펀드 클레임 | 58건 | 🔴 3건 불일치 (KG-Fund·KG-fake·KG-sent) |
| gold SQL (eval 7파일 121문항) | 121건 | ✅ 통과 (0행 정답 문항 2건은 설계상 ⚠️ 정보 표시) |
| 완전중복 (마스터 4테이블 전 컬럼) | 4건 | ✅ 0행 |
| KG 고아 (alias→노드 · edge→노드) | 2건 | ✅ 0건 |

**마스터 데이터 자체는 깨끗하다. 불일치 4건은 전부 KG(지식그래프) 적재 레이어다.**

## 🔴 불일치 4건 — 전부 KG 레이어 (원인: 생성기 더미 필터 구멍)

1. **펀드 Fund alias에 원본 쓰레기 값 17건 유입** — `rptt_ksd_itm_no` 유효 형식(12자리)이 아닌 값이 노드로 등록: `'sdf'`(원본 2행) · `'wtrewrwe'`(6행) · `'0'`(4행) · `'00'`·`'00000'`·`'KR'`·`'KR5000000'` · 11자리 절단 코드 7종(`03023004460` 등) · `'99999999999'` · 소문자 `kr0000000000` · `KRZ799999999`. 원본 `public_funds`에 실재하는 입력 오류 값들인데 생성기(`gen_fund_structure_auto.py`)의 센티넬 필터(KR0 공백형·반복문자·길이이탈)를 통과했다. alias 6,883 vs 유효 6,867 = +16 차이의 정체.
2. **센티넬 Organization 노드 3개 잔존** — `Org_trustee_99999999` · `Org_trustee_00000000` · `Org_fund_99999999`. E-3-17(08-30)에서 trustee 두 노드는 소멸 확인됐다고 기록됐으나 DB 에 다시 존재 — 이후 재생성에서 재유입됐거나 fund 쪽 경로가 미수정.
3. **⚠️ 오부착 지뢰: `Org_fund_99999999` 의 label 이 '현대와이즈03사'** — 더미 운용사코드 99999999(원본 27행)에 실제 회사명 라벨이 붙어 있다. "현대와이즈03사가 운용하는 펀드" 질의가 KG 를 타면 무관한 더미 27행이 오귀속된다.
4. **해외ETF `wu_inv_rgn` 3개 값 KG 미부착** — `Asia Pacific`(9행) · `EuroZone`(2행) · `Greater China`(2행). 유사 노드(`Asia Pacific ex Japan`·`China`·`Europe`)는 있으나 이 3개 표기의 alias 가 없다. (E-3-16 ☐ 미검토 항목이었음)

**조치 방향**: 생성기 필터 보강(12자리 `KR`/`K5` 패턴 강제 + 대소문자 무시 + 반복문자 규칙 ④ 적용) → yaml 재생성 → **DB 재적재까지 한 커밋**(CROSS-001 교훈). wu_inv_rgn 3건은 alias 추가. 라벨 오부착은 재생성 시 자동 해소되는지 확인 필수.

## ℹ️ 스윕에서 걸렸으나 문제 아님으로 판명 (전건 실체 추적)

- **채권 종가 기준일 공백 16,453행**: TEXT `'        '`(8칸) — 거래없음(종가 0)과 동행. yaml `exg_close_price_base_dt` missing_semantics 에 문서화 완료 ✅. "종가>0 인데 기준일 공백" 0행 재확인.
- **국내ETF `pd_lste_dt` = 99991231 (1,535행)**: 상장폐지 안 됨 센티넬 — 구매가능 규칙(08-25 신설)에 문서화 완료 ✅. 실제 상폐일 보유 242행(전부 판매중지)도 규칙 수치와 일치.
- **`ext_ovs_etf_holdings.report_date`**: TEXT 'YYYY-MM-DD' 형식(906,848행 전부) — 외부수집 테이블 컨벤션, 정수 YYYYMMDD 규칙은 마스터 `*_dt` 대상.
- **음수 값들**: ETF 괴리율(du_diff_rt 527행)·NAV 괴리금액(965행), 펀드 기타자산비중(zrin_etc_ast_cmst_rt 1,964행 — 파생 상계) — 도메인 정상.
- **완전중복 835/76/15/10,882 플래그**: 앞 20컬럼만 비교한 검사 설계 한계 — 전 컬럼 비교 시 4테이블 모두 0행 ✅.

## 🔸 신규 발견 이상치 2건 (주최 원본 데이터 — 수정 불가, 기록)

- **채권 장내 종가수익률 음수 1건**: `KR6169379E54` 메리츠캐피탈 256-4, `exg_close_yield` = **-10.287** (종가 10,135 — 액면 초과 프리미엄 거래). 조회 질의엔 사실대로, 정렬 모수에선 기존 규칙(장내종가 유효 1,270행 + 기준일 병기)이 커버.
- **국내ETF 상장일 1000년 1건**: '미래글로벌헬스케어제' `pd_lstg_dt` = **10001231** (판매중). 상장일 질의 시 "1000-12-31" 노출 위험 — 상장일 미수록으로 읽는 게 안전. 규칙 기재 후보.

## 검수 중 자체 사고 1건 (발견 즉시 수정)

- 앞선 날짜 오기 수정 때 PowerShell `Set-Content -Encoding UTF8`(PS 5.1 = BOM 부착)으로 3파일(`pipeline.py`·`domestic_bonds.yaml`·`questions_top_safety_probe.jsonl`)에 UTF-8 BOM 이 유입 → `run_gold_check.py` JSON 파싱 크래시. BOM 제거 후 gold 121 통과·tests 181 통과 재확인. **교훈: 파일 쓰기는 Write/Edit 도구로 — PowerShell 텍스트 재저장 금지.**
