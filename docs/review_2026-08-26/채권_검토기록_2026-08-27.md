# 📝 채권 검토 작업 기록 — 2026-08-27 (seohyun · 에이전트 동반 검토)

> **이 파일의 성격** — `에이전트검토_프롬프트.md` 지시서대로 채권 17건(A-1-01~05 · A-2-01 · A-4-02 · B-3-01~09 · C-4-01)을 **2차 DB 로 전부 재현**한 결과를 적은 **기록**이다.
> ~~**표와 yaml 은 한 글자도 고치지 않았다.**~~ → **✅ 2026-08-28 기입 완료.** 사용자 승인("괜찮아 기입해")을 받아 A·B·C 표 17칸과 `ontology/enums/domestic_bonds.yaml` 에 반영했다. 검사 4종 통과. 아래 "판정 제안" 은 **기입된 내용의 근거 기록**으로 남긴다.
> **🆕 08-27 진행 상태** — 사용자 문답으로 **결정 2건이 확정**됐다(§5-1 표면금리 (가)안 · §5-5 대표행 병기안 + 조건 2가지). **17건 전체 기입 승인("괜찮아 기입해")은 아직 받지 않았고, 표·yaml 은 여전히 무변경이다.** 이번 회차에서 바뀐 것 전부는 **§9** 에 모아 놨다.
> **🆕 08-28 진행 상태 (최종)** — §5-3(`remaining_days=0` 49행) 전수 재조회로 **종결** → 사용자 결정 0건. 회의 안건 3건은 `docs/meeting_agenda_2026-08-26.md` **§4** 로 이관. **17건 전체 기입 승인을 받아 표·yaml 반영 완료** — 검사 4종 통과(중복키 0 · 빌드 오류 0 · pytest 35 · gold 63/63). 최종 상태 총괄은 **§12**.
> 기준: DB `data/financial_products.db` = `build_info` v2_20260824 · 채권 21,882행 × 58컬럼 · 종목 20,497 · 기준일 2026-08-22(`info_base_dt` 20260821).

---

## 0. 작업 경위

| 순서 | 한 일 | 결과 |
| :-: | :-- | :-- |
| ① | `git pull` — main · `eda/domestic_bonds` · `eda/domestic_etfs` 세 브랜치 전부 (`--ff-only`) | main `cb95e22` · bonds `5ac9263`(최신) · etfs `db56259 → 008bb4e`(원격 추적 연결 후 갱신) |
| ② | 담당 문서 확인 | A 7건 · B-3 9건 · C-4 1건 = 17건 |
| ③ | 지시서 §1 을 읽고 "채권(seohyun)" 담당으로 수행 | — |
| — | **로컬 DB 가 1차(42,394행 × 40컬럼, 8/8 생성)임을 발견** → 검토 불가 | 사용자가 `mirae_data_20260825/` 배포본 제공 |
| — | 배포본 배치: `financial_products.db`(2차 정본) → `data/` · 1차 DB 는 `data/financial_products.v1_20260711.db` 로 보관 · `kg_coverage_report.md` · `external/holdings/*` · `external/holdings_overseas_20260824/*` · `holdings_overseas/SOURCES.md`(기존은 `.bak_20260827`) | `pytest` **35 passed** · `eval/run_gold_check.py` **63/63** |
| ④ | 카테고리 목록 제시 (§1) | 사용자 확인 대기 |
| ⑤ | 카테고리 ①·②·③ DB 재현 → 이 문서에 기록 | **기입·yaml 수정 없음** |
| ⑥~⑨ | 승인 · 기입 · 커밋 | **미진행** |
| 🆕 08-28 | §5-3 전수 재조회 → **종결** · 회의 안건 3건을 `meeting_agenda_2026-08-26.md` §4 로 이관 | **기입·yaml 수정 없음** (§8-(l) · §12) |

부수: `docs/BONDS_MASTER.md` 는 원래 `eda/domestic_bonds` 브랜치에만 있는 파일(삭제 아님). 08-27 에 쉬운 말로 다시 쓴 판을 main 작업 트리의 `docs/BONDS_MASTER.md` 에 untracked 로 놓아 두었고, 같은 내용이 `git stash@{0}` 에도 보관돼 있다.

---

## 0-A. 쉬운 말 설명 — 이 문서가 대체 무슨 작업인가

### 지금 하는 일이 뭔가

우리는 챗봇에게 **"이 데이터는 이런 뜻이야"** 를 적어 준 설명서(`domestic_bonds.yaml`)를 갖고 있다. 챗봇은 그 설명서를 보고 SQL 을 만들어 답한다.

문제는 **설명서를 사람이 손으로 썼다는 것**이다. 데이터가 1차 → 2차로 바뀌면서 숫자가 달라졌는데 설명서는 그대로일 수 있다. 그래서 팀이 자동으로 **"설명서 내용 vs 진짜 데이터"가 어긋나는 곳 408군데**를 뽑아 놨고(`A_정합성.md` 등), 그중 **채권 담당 몫 17건**을 사람이 하나씩 "이거 진짜 틀린 거야, 아니면 괜찮은 거야?" 판정하는 게 이 작업이다.

```
자동 검사가 "어? 여기 숫자 안 맞는데요" 하고 17군데를 찍어 줌
        ↓
나(에이전트)가 DB 를 직접 조회해서 왜 안 맞는지 알아냄
        ↓
✅ 괜찮음 / ⚠️ 더 봐야 함 / ❌ 진짜 틀림  중 하나로 판정 (← 지금 여기, 승인 대기)
        ↓
승인되면 검토 문서 표에 적고, ❌ 는 설명서(yaml) 를 고침
```

### 판정 세 글자의 뜻

| 판정 | 쉬운 뜻 |
| :-: | :-- |
| ✅ | **설명서가 맞다.** 자동 검사가 착각한 것 — 두 숫자가 서로 **다른 걸 세고 있었다** |
| ⚠️ | **내가 결정할 수 없다.** 답은 알지만 다른 사람(리드)이 정할 규약에 걸린다 |
| ❌ | **설명서가 틀렸다.** 고쳐야 한다 |

### 왜 "숫자가 안 맞는데 ✅" 가 많은가

자동 검사는 **"칸이 비어 있지 않은 행"** 을 세는데, 설명서에 적힌 숫자는 대개 **"실제로 답에 쓸 수 있는 행"** 이다. 이 둘은 원래 다르다.

> **예 (A-1-02 장내 종가)** — 자동 검사: "설명서엔 1,270 인데 실제로는 17,746 인데요?"
> 실제로 보니 — 거래소 상장 채권 17,746개는 **종가 칸이 다 있긴 한데 그중 16,476개는 값이 0**(= 최근 거래가 없었음). 진짜 거래된 적 있는 건 **1,270개**뿐.
> → 설명서의 1,270 이 맞다. 자동 검사가 "0 도 값이네" 하고 센 것.

이런 식으로 **8건이 ✅** 다. 다만 ✅ 8건 중 "0·빈칸의 뜻" 때문인 것은 **3건**(A-1-01 · A-1-02 · C-4-01)이고, 나머지 5건은 다른 이유다 — **세는 단위가 달라서**(A-1-03 종목명 · A-1-04 종목코드 · A-2-01 행 단위: "행 21,882 vs 종목 20,497") 또는 **그냥 설명서가 맞아서**(A-4-02 기준일 · B-3-04 LOT 순번).

### 실제로 틀린 건 뭔가 — 딱 두 종류

**① 단위를 안 적어 놨다 (6건)** — "이 숫자 3.42 가 3.42% 라는 뜻인지, 3.42원인지, 3.42년인지" 를 설명서에 안 써 놨다. 챗봇이 답할 때 "3.42" 라고만 하면 고객이 뭔지 모른다. 그래서 `percent`(%)·`krw`(원)·`years`(년) 을 적어 주자는 것.

**② 설명서 문장이 사실과 다르다 (2건)**
- **표면금리** — 설명서엔 "빈칸 없음" 이라 적혀 있는데 **1개 있다**. 미국 뱅크오브아메리카가 외국에서 발행한 채권 하나가 국내채권 목록에 섞여 있고, 이 채권은 표면금리·만기일·발행일·발행사가 전부 비어 있다. 그런데 **신용등급 AAA 는 붙어 있어서**, 고객이 "AAA 등급 채권 보여줘" 하면 이 정체불명 채권이 목록에 나온다.
- **중복 행 설명** — 같은 채권이 거래소(장내)와 창구(장외) 양쪽에 있으면 **줄이 두 개**다. 설명서엔 "두 줄은 내용이 똑같다" 고 돼 있는데, 실제로는 **8개 종목이 장외 줄에서 신용등급·발행사·채권종류가 통째로 비어 있다.** 챗봇이 어느 줄을 집느냐에 따라 "AAA 입니다" 도 "정보가 없습니다" 도 나온다.
  → **처리 방침(08-27 사용자 승인): 장내 우선으로 뭉개지 않고 두 줄을 나란히 답한다(병기).** "기록이 없다" 와 "AA-" 는 서로 다른 정보이므로, 없는 걸 장내 값으로 채우면 그건 우리가 만든 추측이 된다. 상세는 §3 A-2-01 · §9.

### 고쳐도 안전한가 — 이게 §8 의 내용

코드를 뒤져 보니 **설명서의 대부분은 챗봇 답변에 영향을 주지 않는다.** 챗봇에게 실제로 전달되는 건 `query_rules`(질의 규칙)와 `normalization`(정리 규칙) 두 덩어리뿐이고, 컬럼별 설명(`missing_reason`·`unit`·`note` 같은 것)은 **사람이 읽는 기록**이다.

> 즉 17건 중 **답변 동작이 실제로 바뀌는 건 규칙 문장 3개뿐**이고, 나머지는 고쳐도 챗봇은 어제와 똑같이 답한다. 그래서 위험이 낮다.
> (08-27 갱신: 원래 2개로 적었으나 `대표행` 을 병기로 바꾸기로 하면서 3개가 됐다 — `외화채없음` · `듀레이션정상` · `대표행`.)

### 검토하다 따로 발견한 것 중 중요한 셋

| 발견 | 쉽게 말하면 | 왜 문제인가 |
| :-- | :-- | :-- |
| **수익률 728%** | 고객이 "수익률 높은 채권 알려줘" 하면 **1~5위가 전부 신용등급 C0(거의 최하위) 부실 유동화 채권**이다. 수익률 728%, 평가가격이 액면 1만원짜리가 3천원 | 수익률이 높은 게 아니라 **떼일 위험이 커서** 싸게 거래되는 것. 지금 어떤 규칙도 이걸 안 막는다 |
| **외국채 1개** | 위에서 말한 BAC 채권 | AAA 라서 "우량 채권" 목록에 낀다 |
| **이름의 `(사)` 는 사모가 아니다** | 채권 이름 끝에 `(사)` 가 붙은 3,962개 중 **정확히 절반(1,981개)이 컬럼상 '공모'** | 1차 때 이름만 보고 "사모 15,261건" 이라 셌던 게 크게 부풀려진 이유 |

### 그래서 지금 서현님이 할 일

이 문서의 **§2 표(17건)** 를 보고 "이 판정 동의해?" 만 답하면 된다.
- 다 동의하면 → **"괜찮아 기입해"**
- 특정 항목이 이상하면 → **"A-1-05 는 ⚠️ 로, 이유는 ~"** (반려 이유가 곧 설명서에 들어갈 근거가 된다)

그리고 §5 에 **결정할 것 4가지**가 있는데, 이번 기입에 직접 걸리는 건 **1번 하나**다. 나머지 3개는 회의로 넘기면 된다.

---

## 1. 카테고리 목록 (투자자 질문 단위)

| # | 카테고리 | 항목 | 건수 |
| :-: | :-- | :-- | :-: |
| ① | "이 채권 사면 얼마 벌고, 지금 얼마에 거래돼요?" — 수익률·가격 | A-1-01 · A-1-02 · A-1-05 · B-3-01 | 4 |
| ② | "채권 몇 개예요? 살 수 있는 건요?" — 세는 단위·판매 LOT·기준일 | A-1-03 · A-1-04 · A-2-01 · A-4-02 · B-3-02 · B-3-04 | 6 |
| ③ | "내일 값이요?" — 익일(`ndy_*`)·컨벡시티·0 처리 | B-3-03 · B-3-05~09 · C-4-01 | 7 |

---

## 2. 판정 총괄 (17건) — ✅ 2026-08-28 기입 완료

| ID | 대상 | 제안 판정 | 한 줄 근거 | yaml 조치 (제안) |
| :-: | :-- | :-: | :-- | :-- |
| A-1-01 | 세후 연평균수익률 | ✅ | non-null 634 = 판매 LOT 634 동일 집합, 전량 0 → `answerable_n=0` 은 "쓸 수 있는 값" 기준이라 맞음 | `note` 보강 |
| A-1-02 | 장내 채권종가 | ✅ | `answerable_n=1270` = "장내 & 종가>0" 조건 모수(non-null 17,746 은 장내 행 수) | `note` 에 기준일 연도 분포 보강 |
| A-1-03 | 종목명 | ✅ | 결측 0 · `answerable_n=20497` = distinct ISIN(개체 수) · distinct 이름은 20,499 | `trap` 에 사례 2종 추가 |
| A-1-04 | 종목코드 | ✅ | 결측 0 · 패딩 0 · distinct 20,497 | 없음 |
| A-1-05 | 표면금리 | ❌ (경미) | 결측 1행 = XS 외국채(BAC) — 판정 `none` 은 사실과 다름 (`answerable_n` 은 이미 21,881). 08-27 재확인: NULL **1** · 공백 위장 **0** | `missing_reason: mixed` + `missing_semantics.null` + `외화채없음` 규칙 오기 정정. **✅ 08-27 (가)안 승인** — `curr_cd='KRW'` 를 전 질의 기본 조건으로 명문화 (§3-①-c) |
| A-2-01 | 행 단위 | ✅ (설명 정정) | 규칙(`대표행`·GROUP BY pd_no) 존재. 단 "중복행 속성 동일(8건 빼고)" 설명이 부정확 — 8종목은 장외행 등급·종류·발행사·듀레이션 NULL + 평가가 액면 수준 + 위험등급 `00` | `row_grain.대표행` + `query_rules.대표행` **두 곳** 을 **병기 규칙**으로 (08-27 승인, §3 A-2-01) |
| A-4-02 | 기준일 | ✅ | `info_base_dt` 전 행 20260821 단일값 · `query_rules.기준일` 존재 · 판매수익률 기준일도 634행 동일 | 없음 (부수: 등급일 `0` 1행) |
| B-3-01 | 세후 연평균수익률 단위 | ❌ (경미) | 값 전량 0 이라 실측 불가 — 형제 컬럼·스키마로 % 확정 | `unit: percent` 신설 |
| B-3-02 | 매수가능수량 단위 | ❌ (경미) | 값은 **액면금액(원)** — 전부 1,000 배수 · 발행잔액 이하 · "수량"이 아님. 컬럼 자체는 주최 무효 공지 | `unit: krw(액면금액)` 신설 |
| B-3-03 | 컨벡시티 단위 | ⚠️ | 단위는 **연²** 로 확정(무이표 dur 46.44² = 2,157 ≈ cov 2,151). 단 `unit` 어휘에 연² 가 없고 어휘 확정은 리드의 B-4 항목 → 어휘 정해진 뒤 기입 | `note` 에 "연²" 명기만 · `unit` 키는 B-4 결정 후 (08-27 재점검으로 ❌→⚠️ 정정) |
| B-3-04 | LOT 순번 단위 | ✅ | 1/2/3 순번, 무단위 · `value_semantics` 실측과 일치 | 없음 — `unit` 키 넣지 않음(어휘에 '순번' 없음, 08-27 재점검으로 정정) |
| B-3-05 | 익일 민평수익률 | ❌ (경미) | % (당일 `applied_yield` 와 21,619행 동일) | `unit: percent` 신설 |
| B-3-06 | 익일 컨벡시티 | ⚠️ | 연² — 당일값과 21,612행이 다름(하루 굴린 재계산, 중앙값 차 0.0105). `unit` 어휘는 B-3-03 과 동일 사유로 보류 | `missing_semantics.0` 20행 · `note` "연²" · `unit` 은 B-4 결정 후 (08-27 정정) |
| B-3-07 | 익일 이자부단가 | ❌ (경미) | 원(액면 10,000 기준) — 당일 dirty 와 34행만 같고 하루치 ≈1.07원 차 · `ndy_eval_price` 와 21,617행 동일 | `unit: krw` 신설 (사용 금지 유지) |
| B-3-08 | 익일 듀레이션 | ❌ (경미) | 연 — 센티넬 99 는 당일과 같은 3행 · 당일과 21,408행 다름(중앙값 차 0.0016) | `unit: years` 신설 · `듀레이션정상` 규칙이 `ndy_dur` 도 덮도록 |
| B-3-09 | 익일 평가일단가 | ❌ (경미) | 원 — 당일과 34행만 같음(≈1.07원 차) | `unit: krw` 신설 |
| C-4-01 | `zero_as_missing` 조건식 | ✅ | 목록 11컬럼의 0 건수가 yaml 주석과 전부 일치(§3-③-g) | `note` 에 미등재 0-보유 컬럼 4종 건수 보강 · **🆕 `remaining_days.note` 한 줄 보강**(0 = 만기 도래 49행, 실제 값이나 판매 대상 밖이라 영값배제 유지 — §5-3 종결) · **🆕 `듀레이션정상` 에 `remaining_days > 0` 추가 검토**(P-16) |

**요약 (08-27 재점검 후): ✅ 8 · ⚠️ 2 · ❌ 7 = 17건.** ❌ 7건의 내역은 `unit` 미기재 6(B-3-01·02·05·07·08·09) + 판정 문구 오류 1(A-1-05 표면금리). yaml 수정 제안 약 17곳. 답변 동작이 실제로 바뀌는 것은 `query_rules` 문구 **3개**(A-1-05 `외화채없음` · B-3-08 `듀레이션정상` 의 `ndy_dur` 확장 · **A-2-01 `대표행` 병기 전환**)뿐 — §8 참조.

> 🔧 **08-27 2차 정정** — 이 요약을 처음엔 "✅ 7 · ❌ 8", 그 전엔 "✅ 7 · ❌ 10" 으로 적었다. 둘 다 오기다(총합 17 만 맞고 내역이 틀림). 표를 기계로 다시 세어 **✅ 8 · ⚠️ 2 · ❌ 7** 로 확정. A-2-01 은 판정이 ✅ 이고 yaml `대표행` 문구만 고치는 건이라 ❌ 로 세면 안 된다.

### 🆕 08-27 문답으로 바뀐 것 — **판정은 하나도 안 바뀌었고, 조치만 2건 바뀌었다**

| ID | 판정 | 바뀐 것 |
| :-: | :-: | :-- |
| A-1-05 표면금리 | ❌ **그대로** | 조치의 (c)가 "**규칙에 넣을지 결정 필요**" → **"(가)안 승인 — 규칙에 명문화"** 로 확정 |
| A-2-01 행 단위 | ✅ **그대로** | 조치가 "**장내행 우선 필수**" → **"두 줄 병기"** 로 교체(제안 철회). 고칠 곳도 1곳 → **2곳**(`row_grain.대표행` + `query_rules.대표행`) |
| 나머지 15건 | — | **변화 없음** |

### 이 표(17건) **밖**인데 같이 기입할 것 — 1건

| 항목 | 내용 | 근거 |
| :-- | :-- | :-- |
| **`(사)` 표기** (P-3) | `query_rules.공모사모판정` 신설 — "공모/사모는 `bd_ofr_tcd` 로만 판정, 종목명 추론 금지" + `pd_nm.trap` 한 줄 | **✅ 08-27 승인** · 상세 **§11** |

> 이건 A~E 검토 채움표의 17건에 속하지 않는 **부수 발견**이라 표에 행이 없다. 다만 승인 시 yaml 에는 함께 들어간다. **따라서 기입 작업 전체로 보면 `query_rules` 실질 변경은 4개**(위 3개 + `공모사모판정`)다.

### 이 표 밖이고 **기입도 안 하는 것** — 1건

**§10 수익률 728%(P-2)** — 08-27 사용자 지시로 **회의 안건**. 분석은 §10 에 끝냈고 **yaml 은 건드리지 않는다.**

---

**아직 남은 결정 — 🆕 08-28 기준 없음.** §5-3 이 종결되어(§8-(l)) 사용자가 정할 것은 다 정해졌다. 확정: §5-1 · §5-3 · §5-4-ㄱ · §5-5. 회의 이관 완료(`meeting_agenda_2026-08-26.md` §4): §5-2 · §5-4-ㄴ · §5-6. **남은 것은 17건 전체 기입 승인 하나뿐이다.**

---

## 3. 카테고리별 상세 보고

### ① "이 채권 사면 얼마 벌고, 지금 얼마에 거래돼요?"

#### A-1-01 세후 연평균수익률 (`avg_annual_tax_yield`) → ✅
- **무엇인가**: 지금 사서 만기까지 들고 갔을 때 세금 떼고 1년 평균 몇 % 버느냐 — "실제 손에 쥐는 수익률".
- **실측**: 값 있는 행 634 = 매수수익률(`buy_yield`) 있는 장외 판매 LOT 634 와 **정확히 같은 집합**(어긋난 행 0). 장내 0건. 634행 **전부 0**(>0·<0 각 0). 형제 컬럼 개인 세후 운용수익률(`after_tax_yield`)은 같은 634행이 1.92~5.70 으로 정상.
- **판단**: "기재 0 vs 실측 634" 는 다른 걸 잰 것 — 634 는 칸이 있는 행, 0 은 쓸 수 있는 값의 행. yaml `missing`·`0: missing`·"데이터가 제공되지 않았습니다" + `과세수익률금지` 규칙으로 이미 맞게 돼 있음. 세후 질의는 `after_tax_yield` 로 답하게 됨.
- **조치**: `note` 보강 — "non-null 634 = 판매 LOT 634 동일 집합(장외 전량), 전부 0 — 세후 질의는 after_tax_yield".

#### A-1-02 장내 채권종가 (`exg_close_price`) → ✅
- **무엇인가**: 거래소 상장 채권이 마지막으로 실제 거래된 가격(민평 평가가와 다른 "진짜 손바뀜" 값).
- **실측**: 장내 17,746행 non-null(자동 근거의 17,746 = 장내 행 수) · **종가>0 1,270행(7.2%)** = 기재값 · 0 은 16,476 · 장외 4,136 전부 NULL · 1,270행 전부 기준일 있음 · 값 2,511~10,135원.
  기준일 연도 분포(종가>0): 2019 4 · 2020 12 · 2021 90 · 2022 250 · 2023 251 · 2024 253 · 2025 252 · **2026 158**. 기준일 당일(8/21) **2건**, 7월 이후 30건 → 종가의 **88% 가 2025년 이전 값**.
- **판단**: `answerable_n=1270` 은 "장내 & 종가>0" 조건 모수. `answer_policy`(모수 1,270 명시·기준일 병기) · `장내종가` 규칙 존재. 몇 년 전 종가를 오늘 값처럼 말하는 걸 규칙이 막고 있음.
- **조치**: `note` 에 연도 분포·당일 2건 보강(기준일 병기 규칙의 근거 수치).
- **부수 발견**:
  - (a) 종가 기준일 `exg_close_price_base_dt` 는 종가 0 인 16,476행 중 **16,453행이 공백 8칸**, 23행만 진짜 NULL. yaml `"": missing 16,453` ✅ 이미 반영, 23 NULL 은 미기재(경미). `IS NOT NULL` 로 세면 17,723.
  - (b) yaml "100종 distinct" — 종가>0 행의 기준일 distinct 는 **99**(공백 포함 100).
  - (c) `메리츠캐피탈 256-4`(KR6169379E54): 종가 **10,135(전체 최고)** 인데 종가수익률 **−10.287%** (8/21). "종가 높은 채권" 1위가 이 행. `exg_close_yield` yaml 은 "유효 1,269" 로 이미 이 행을 빼고 셈. 원본 확인 불가 — 기록만.

#### A-1-05 표면금리 (`srfc_irt`) → ❌ (경미 · 부수 발견은 중요)
- **무엇인가**: 액면에 적힌 약정 이자율 — "연 3% 준다"의 3%.
- **실측**: 결측 **1행** = `XS3067881758` **"BAC 0 05/14/55 Corp"** — 뱅크오브아메리카 유로시장(XS=Euroclear) 발행 2055년 만기 무이표. 장외 · 통화 `'000'`(유일, 나머지 21,881 KRW) · 표면금리·만기일·발행일·발행사명·채권종류·잔존일수·등급일 NULL. **그러나 신용등급 AAA · 위험등급 5(낮은위험) · 평가가 6,356.17 · 듀레이션 26.19년 · 민평수익률 5.074% · 발행잔액 700억 은 있음.** 대분류 회사채/기타사채 · 단리채 · 공모.
  참고: 표면금리 0 은 579행(이표 499·복리 57·할인 21·단리 2) = yaml 기재 일치, 양수 0.156~20.0.
- **판단**: `missing_reason: none` 은 사실과 다름(`answerable_n` 은 이미 21,881 — 자기모순). 더 중요: 이 행은 **조회·랭킹 질의에 섞여 나올 수 있다** — "AAA 회사채 중 수익률 높은 순", "듀레이션 긴 채권" 에 발행사·만기 없는 채권이 들어감. `구매가능`(mat_dt ≥ 기준일)은 NULL 이라 자연 제외되지만 조회 질의는 막는 규칙 없음.
- **조치** (`domestic_bonds.yaml`):
  - (a) `columns.srfc_irt.missing_reason: none → mixed`
  - (b) `missing_semantics.null: "XS 외국발행 1행(BAC 0 05/14/55, 통화 '000') — 표면금리·만기·발행일·발행사·종류 미수록"`
  - (c) `query_rules.외화채없음` 오기 정정: "curr_cd = KRW 뿐" → "원화 21,881행 + 통화 '000' 외국발행 1행(XS3067881758, 정보 미비). 모든 채권 질의는 `curr_cd='KRW'` 기본 조건으로 이 1행 제외. '외화채권' 질의는 '수록 없음'".
    **✅ 08-27 사용자 승인 — (가)안(규칙 조건으로 넣기) 확정.** 승인 조건이던 "정말 1행뿐인가" 를 재확인함: `srfc_irt IS NULL` = **1** · 공백·빈문자열로 위장된 결측 = **0** · 전체 21,882. 공백 위장이 없으므로 (가)안 그대로 진행.
    - 참고: BAC 채권의 만기·발행사는 **외부 데이터로 보완할 여지가 있다**(§9-② 규칙 §2 = 외부 수집 조건부 허용). 다만 1행 때문에 외부 수집 절차를 트는 건 비용이 커서 이번 기입에서는 제외하고 **회의 안건(P-12)** 으로 넘긴다.

#### B-3-01 세후 연평균수익률 단위 → ❌ (경미)
- **실측**: 값 전량 0 → 값으로 확정 불가. 같은 634행의 형제 컬럼 세후 운용수익률 1.92~5.70 · 매수수익률 2.08~5.29 · 민평수익률 2.83~5.29 — "3.42 = 3.42%" 표기, yaml `unit: percent`. 스키마 한글명 "세후 연평균수익률", double.
- **판단**: `unit` 미기재. 답변 불가 컬럼이라 실해 없음.
- **조치**: `unit: percent` + note "전량 0 이라 실측 검증 불가 — 형제 수익률 컬럼·스키마 기준".

### ② "채권 몇 개예요? 살 수 있는 건요?"

#### A-1-03 종목명 (`pd_nm`) → ✅
- **무엇인가**: 고객이 부르는 채권 이름. 검색의 첫 입구.
- **실측**: 결측 0 · 앞뒤 공백(패딩) **684행** · distinct 이름 **20,499** vs distinct ISIN **20,497** → 같은 ISIN 인데 이름이 두 가지인 종목 **2**: `KR352502GG80` "한국자산관리공사채권제163회 (사)" / "…제163회(사)", `KR352503GG89` 164회 동일 패턴 — 괄호 앞 공백 하나 차이(TRIM 으로도 안 합쳐짐). 이름 같은데 ISIN 다른 "동명이인" 은 **0**.
- **판단**: `answerable_n=20497` 은 non-null(21,882) 이 아니라 **개체 수(distinct ISIN)**. "이름으로 세면 2 과대". 규칙 `문자열비교`(TRIM) 있음.
- **조치**: `trap` 에 위 2종 사례 추가("같은 ISIN·다른 표기 — 이름 매칭은 공백 제거 후").

#### A-1-04 종목코드 (`pd_no`) → ✅
- **실측**: 결측 0 · 패딩 0 · distinct 20,497 · 행 21,882.
- **판단**: `answerable_n=20497` = distinct(주석에 "행 수 아님" 명시). 맞음.
- **조치**: 없음.

#### A-2-01 행 단위 → ✅ (설명 정정)
- **무엇인가**: "채권 몇 개예요?" 에 행 수(21,882)가 아니라 종목 수(20,497)로 답하는가.
- **실측**:
  - ISIN 당 행 수: 1행 19,419 · 2행 772 · 3행 305 · 4행 1 → 중복 종목 **1,078**, 초과 행 1,385.
  - 장내 17,746행(전부 유일) · 장외 4,136행 / 3,828 종목. 중복 종목 = 장내+장외 1,077 · 장외 LOT 만 1.
  - PK(`pd_no, pd_exg_mkt, info_base_dt, info_seq`) distinct = 21,882 ✅ 유일.
  - 중복행 간 속성: 만기·발행일·표면금리·대분류·이자유형·공모/사모 **전부 동일**. 다른 건 **8종목**(신용등급·평가가·채권종류·발행사·듀레이션·민평수익률·위험등급) — 전부 "장외 행에서 NULL 또는 다른 값, 장내 행엔 정상". 8종목 = 한국자산관리공사 163·164회 + **엔에이치농협캐피탈 269-1~6(KR6097242G84~247G89)**. 장외 행: 등급 NULL · 종류 NULL · 발행사 NULL · 듀레이션 NULL · 위험등급 `00 해당없음`(장내는 `15 낮은위험`/`16 매우낮은위험`) · **평가가 10,000.00~10,000.12 = 액면가 수준**(장내는 9,990.33~9,997.89).
  - **🆕 08-27 전수 재확인 — "장내·장외가 진짜 다른 종목"의 크기**:

| 확인 | 수 | 뜻 |
| :-- | --: | :-- |
| 장내·장외 양쪽에 다 있는 종목 | **1,077** | 병기 판단의 모수 |
| └ 평가가가 소수점까지 **똑같음** | **1,069** (99.3%) | 병기하면 같은 값을 두 번 말하게 됨 → **한 번만 답해야 함** |
| └ 평가가가 **실제로 다름** | **8** (0.7%) | 위 8종목과 **동일 집합** |
| 장외 줄 등급이 NULL 인 종목 | 828 | |
| └ 장내 줄도 NULL (어느 줄이든 등급 없음) | **820** | 병기해도 "둘 다 없음" — 대표행 문제 아님 |
| └ 장내 줄엔 값이 있음 (**줄에 따라 답이 갈림**) | **8** | 여기가 진짜 대상 |
| 장외 8행 중 평가가가 **정확히** 10,000.0 | 4 / 8 | 나머지 4는 10,000.001~10,000.118 |

- **판단**: 규칙(`row_grain` · `query_rules.대표행` "GROUP BY pd_no 또는 장내 우선") 존재 ✅. 다만 yaml `대표행` 의 "속성 동일(eval_price 다른 8건 뿐)" 은 부정확 — 8종목은 **장외 행이 통째로 속성 결측 + 평가가가 액면 수준**이라 챗봇이 어느 행을 집느냐에 따라 "AAA" 도 "정보 없음" 도 답할 수 있음.
  **🔴 처음엔 `장내행 우선 필수` 로 제안했으나 08-27 사용자 지적으로 철회.** 이유 — "장외 줄에 등급이 없다" 는 데이터가 말하는 전부이고, "그건 장내와 같다는 뜻" 은 **에이전트가 끼워 넣은 추측**이다. 추측을 규칙으로 못 박으면 안 된다. **"기록 없음" 과 "AA-" 는 서로 다른 정보**다.
  **되묻기(역질문)는 이 프로젝트에서 성립하지 않는다** — `.agents/rules/miraeasset-rules.md` §5 가 **Single-turn Q&A** 규격이라 되물으면 그게 곧 최종 제출 답변이 되고 고객의 대답을 받을 턴이 없다(§8-(f)). 따라서 **병기가 유일한 해**.
  또한 `10,000 파킹` 이라는 표현도 부정확 — 8행 중 정확히 10,000.0 인 건 4행이고 나머지는 10,000.001~10,000.118 이다. "액면가 수준" 이 정확한 서술.
- **조치 (08-27 승인 — 병기안)**: `row_grain.대표행` 과 `query_rules.대표행` **두 곳 모두** 아래 3줄로 교체.
  ```
  ① 장내·장외 두 줄의 값이 같으면 → 한 번만 답한다 (1,069종목 — 값 중복 출력 금지)
  ② 값이 다르거나 한쪽이 비어 있으면 → 두 줄을 나란히 병기한다 (8종목)
       예) "장내 기준 AA-, 장외 줄에는 신용등급이 수록돼 있지 않습니다"
  ③ 병기 시 장외 줄의 한계를 함께 밝힌다
       "해당 8종목(KAMCO 163·164 · NH농협캐피탈 269-1~6)은 장외 줄에
        등급·발행사·채권종류·듀레이션이 수록돼 있지 않고, 평가가는 액면가(10,000원) 수준으로 기록"
  ※ 판매 정보(buy_yield·trade_price)는 장외 LOT 행에만 있음 — 종전대로 buy_yield IS NOT NULL 행
  ```
  ⚠️ `query_rules` 는 플래너 프롬프트에 실제로 들어가므로 **이 변경은 답변 동작을 바꾼다**. 기입 후 `run_gold_check.py` **63/63** 유지 여부로 검증한다(§8-(b) 대비 세 번째 실질 변경).
- **부수 발견**: 평가가 정확히 10,000 인 행 장내 3 · 장외 18 — 1차의 "EVAL_PRICE=10000 파킹 1,813" 이 2차엔 21행으로 축소. 위 8종목 중 4행이 여기 포함.

#### A-4-02 기준일 (`info_base_dt`) → ✅
- **실측**: `info_base_dt` 전 행 **20260821 단일값** · `pd_std_info_update`(민평 갱신일) 동일 20260821 · `sale_yield_base_dt` 판매 634행 20260821, 나머지 NULL · 종가 기준일은 별도 컬럼(2019~2026, A-1-02) · 등급일 `crd_grd_dt` NULL 4,006 · 2010~2026 분포.
- **판단**: 기준일이 단일값이고 `query_rules.기준일`("info_base_dt 20260821 / 대회 기준 2026-08-22") 존재. 범위형 기준일(종가·등급일)은 각 컬럼 규칙(`장내종가` 병기 · `등급일사용금지`)이 따로 있음.
- **조치**: 없음.
- **부수 발견**: `crd_grd_dt = 0` **1행** — 한국주택금융공사MBS2011-20(1-1)(콜/변), 등급 AAA. 센티넬 0. `등급일사용금지` 라 답변엔 영향 없음, `missing_semantics` 미기재(경미).

#### B-3-02 매수가능수량 (`buyable_quantity`) 단위 → ❌ (경미)
- **무엇인가**: 우리 회사가 지금 팔 수 있는 양 — 주최 공지(8/24)로 **무효**.
- **실측**: 634행(장외 판매 LOT 전부) · 0 이 338 · >0 이 296 · 최대 **18,587,504,000** · 전부 **1,000 의 배수** · 발행잔액 초과 0건. 예: 한국지역난방공사 45-2 = 185.9억 vs 발행잔액 500억. → 값은 "장수"가 아니라 **액면금액(원)**. 한글명 "수량"이 오해 소지.
- **판단**: yaml `사용 금지` ✅. 단위 미기재.
- **조치**: `unit: krw` + note "액면금액 원(1,000 배수) — '수량' 아님. 사용 금지 유지".

#### B-3-04 LOT 순번 (`info_seq`) 단위 → ✅
- **실측**: 1 = 21,574(장내 17,746 + 장외 3,828) · 2 = 307 · 3 = 1 — yaml `value_semantics` 와 정확 일치. (종목, 시장) 당 LOT 수: 1개 21,267 · 2개 306 · 3개 1. LOT 2개 이상 307종목은 **전부 판매 조건이 서로 다름**(예: 울산지역개발채권 22-05 — LOT1 3.28%/10,274원/60만 · LOT2 3.385%/10,266 · LOT3 3.39%/10,266).
- **판단**: 순번(무단위). `판매행` 규칙 "LOT 2개 종목 307 은 조건 N건 병기" 실측 일치.
- **조치**: 없음. `unit` 키는 넣지 않음(어휘에 '순번' 없음 — B-4 소관). `value_semantics` 가 이미 단위 역할.

### ③ "내일 값이요?" — 익일(`ndy_*`)·컨벡시티·0 처리

공통 실측 — `ndy_*` 5컬럼 모두 NULL 16행(= `dur`·`cov`·`dirty` NULL 16행과 동일).

#### B-3-03 컨벡시티 (`cov`) 단위 → ⚠️ (08-27 재점검으로 ❌→⚠️)
- **무엇인가**: 금리가 크게 움직일 때 듀레이션이 놓치는 가격 곡률 — 일반 고객 질의엔 거의 안 씀.
- **실측**: non-null 21,866 · 0 = 102 · 최대 **2,151.41** = 국고채이자분리채권01470-7409(dur 46.44). 무이표는 컨벡시티 ≈ dur² 인데 46.44² = **2,157 ≈ 2,151** ✓. 전 행 cov/dur² 중앙값 1.16(범위 0.005~14).
- **판단**: 단위 **연²(years²)**. yaml `unit` 없음. **단, 4개 yaml 의 `unit` 어휘(percent·krw·grade·years·days·usd·percent_cumulative…)에 연² 가 없고, 어휘를 enum 으로 고정할지는 리드의 B-4-01~12 결정 사항** → 제가 새 어휘를 만들면 안 됨 → ⚠️.
- **조치**: `note` 에 "단위 연² — 무이표 dur² 와 일치" 만 기재. `unit` 키는 B-4 어휘 확정 후. 리드에게 "연²(컨벡시티) 어휘 필요" 를 B-4 부수 발견으로 전달.

#### B-3-05 익일 민평수익률 (`ndy_applied_yield`) → ❌ (경미)
- **실측**: 0 = 213 · 최대 **728.524** — 당일 `applied_yield` 최대와 동일. 당일과 비교: 둘 다 >0 인 행 중 **21,619 동일 · 34 다름**(중앙값 차 0.011) · 당일>0 인데 익일 0 = **15**(yaml "위장결측 15" ✓). 213 = 당일도 0 인 198 + 위 15.
- **판단**: 단위 % (당일과 같음). 익일값은 사실상 당일값 복사(99.8%).
- **조치**: `unit: percent`.
- **부수 발견 (중요)**: `applied_yield > 50%` 가 **96행** — 등급 **C0** · 평가가 2,740~4,482원(액면 10,000 의 27~45%) · 이름 "…1-2(사)" · 최대 728.5%. `수익률정상`(>0)·`특수구조제외`(`bd_ofr_tcd='사모'`)·`유동화위험금지`(위험 서술 금지) 어느 규칙도 **랭킹에서 빼지 않음** → **"수익률 높은 채권 Top N" 1~5위가 전부 이 행들**(96행 전부 `bd_ofr_tcd='공모'` 라 사모 제외로는 하나도 안 걸러짐 — 1위 신보2024제15차 728.5%). → **§5-2 (회의 안건 · 08-27 사용자 지시로 이번 기입에서 제외)**.
  > 🔧 **08-27 정정 2건** — ⓐ 처음에 "**전부 유동화회사채**" 라고 적었으나 실측은 **`bd_knd` NULL 90행 + 유동화회사채 6행**이다. 원인: 근거 SQL 이 `GROUP_CONCAT(DISTINCT TRIM(bd_knd))` 였는데 **이 함수는 NULL 을 건너뛴다** — 90개의 NULL 이 출력에서 사라지고 남은 1종만 찍혀 "전부 그것" 으로 읽었다. ⓑ "**전부 신보**" 도 부정확 — 신보 + **중진공(중소벤처기업진흥공단) 스케일업 유동화** + 기보 가 섞여 있다. 발행사(`pd_pbcm`)도 90행이 NULL 이라 이름(`pd_nm`)으로만 식별된다.

#### B-3-06 익일 컨벡시티 (`ndy_cov`) → ⚠️ (08-27 재점검으로 ❌→⚠️)
- **실측**: 0 = 121 · 당일 `cov` 와 **132 동일 · 21,612 다름**(중앙값 차 0.0105) · 당일>0·익일 0 = 20.
- **판단**: 연². 수익률과 달리 **진짜 하루 굴린 재계산값**(당일과 거의 다 다름). `unit` 어휘 문제는 B-3-03 과 동일 → ⚠️.
- **조치**: `missing_semantics.0: 20행(당일>0)` · `note` "연²". `unit` 키는 B-4 결정 후.

#### B-3-07 익일 이자부단가 (`ndy_dirty`) → ❌ (경미)
- **실측**: 0 = 16 · 당일 `dirty` 와 **34 동일 · 21,816 다름**, 중앙값 차 **1.07원**(하루치 경과이자 규모) · `ndy_eval_price` 와 **21,617 동일**. 참고 당일: `dirty = eval_price` 21,655/21,866(99.0% — `더티금지` 근거 ✓).
- **판단**: 원(액면 10,000 기준). 사용 금지 유지.
- **조치**: `unit: krw`.

#### B-3-08 익일 듀레이션 (`ndy_dur`) → ❌ (경미)
- **실측**: 0 = 72 · 최대 **99.0 = 센티넬 3행**(티웨이항공 4BW·3CB, 에코프로 24EB — 만기 2054~55, 잔존 10,290~10,592일; 그 외 최대 46.44) — 당일 `dur=99` 3행과 동일 · 당일과 386 동일 · 21,408 다름(중앙값 차 0.0016) · 당일>0·익일 0 = 20(yaml ✓).
- **판단**: 연. `듀레이션정상` 규칙은 `dur != 99` 만 명시 — 익일 질의에서 `ndy_dur` 을 쓰면 99 가 통과.
- **조치**: `unit: years` · `듀레이션정상` 을 "dur/ndy_dur" 로 확장.

#### B-3-09 익일 평가일단가 (`ndy_eval_price`) → ❌ (경미)
- **실측**: 0 = 16(당일>0, yaml ✓) · 당일과 34 동일 · 21,816 다름(중앙값 차 1.07원) · 범위 0~49,497.32.
- **판단**: 원. 익일값은 하루 이자 누적분만큼 움직인 재계산값.
- **조치**: `unit: krw`.

#### C-4-01 `zero_as_missing` 조건식 → ✅
- **무엇인가**: "0 은 값 없음" 으로 빼기로 선언한 컬럼들이 2차에서도 그 행을 잡는가.
- **실측** (행 21,882 · NULL / =0 / >0):

| 컬럼 | NULL | =0 | >0 | yaml 주석 | 일치 |
| :-- | --: | --: | --: | --: | :-: |
| exg_close_price | 4,136 | 16,476 | 1,270 | 16,476 | ✅ |
| exg_close_yield | 4,136 | 16,476 | 1,269 (+음수 1) | — | ✅ |
| applied_yield | 0 | 201 | 21,681 | 201 | ✅ |
| isu_dt | 1 | 25 | 21,856 | 25 | ✅ (국고채원금분리 21 · 산금채 2 · SH보상 2) |
| mat_dt | 1 | 4 | 21,877 | 4 | ✅ (SH보상 2 · 산금채 2 — 잔존일수 NULL, 듀레이션은 있음) |
| isu_bal_amt | 0 | 325 | 21,557 | 325 | ✅ |
| bd_tisu_a | 0 | 259 | 21,623 | — | ✅ |
| dur | 16 | 52 | 21,814 | 52 | ✅ (만기경과 49 = 잔존 0 과 정확 일치) |
| ndy_eval_price | 16 | 16 | 21,850 | 16 | ✅ |
| ndy_applied_yield | 16 | 213 | 21,653 | (15) | ✅ 213 = 당일 0 198 + 위장 15 |
| avg_annual_tax_yield | 21,248 | 634 | 0 | 전량 0 | ✅ |
| `zero_is_value` bdbns_abl_chnl_tcd | 21,248 | 634 ('온오프 겸용') | — | — | ✅ 범주 코드 |

  목록에 **없지만 0 을 가진 수치 컬럼**: `cov` 102 · `ndy_dur` 72 · `ndy_cov` 121 · `ndy_dirty` 16 · `remaining_days` 49(만기경과 — 진짜 0) · `exrt_rpy_r` 1 · (의도적 해제) `srfc_irt` 579 · `exrt_grte_ern_r` 21,626 · (사용 금지) `buyable_quantity` 338.
- **판단**: 기본 규칙("수치 컬럼은 `IS NOT NULL AND <> 0` 기본, 예외만 `zero_is_value`")이 **모든 수치 컬럼에 걸리므로** 목록 누락은 결함 아님. 선언 건수 전부 재현.
- **조치**: `zero_as_missing_columns` 주석에 `cov 102 · ndy_dur 72 · ndy_cov 121 · ndy_dirty 16` 보강(기록성). `remaining_days=0` 49행은 "만기 도래" 라는 실제 값이라 `zero_is_value` 후보 — 판단 요청(§5-3).

---

## 4. 부수 발견 모음 (기입 대상 아님 · 기록)

| # | 발견 | 실측 | 영향 | 처리 제안 |
| :-: | :-- | :-- | :-- | :-- |
| P-1 | **XS 외국채 1행이 조회·랭킹에 섞임** | AAA · 듀레이션 26.19 · 민평 5.074% · 발행사/만기 NULL | "AAA 회사채 수익률순", "듀레이션 긴 채권" 에 등장 | `외화채없음` 규칙 정정 (§5-1) |
| P-2 | **수익률 랭킹 1~5위 = 유동화 C0 후순위(728%)** | `applied_yield>50` 96행 · 등급 C0 · 평가가 액면의 27~45% · **전부 공모** · `bd_knd` 는 NULL 90 + 유동화회사채 6 · 발행사 신보/중진공/기보 | "수익률 높은 채권 추천" 이 부실 유동화 tranche 를 내놓음. 사모 제외로도 안 걸러짐 | **회의 안건 (§5-2).** 08-27 사용자 지시로 **이번 기입 제외** — 분석은 §10 에 완료 |
| P-3 | **이름의 `(사)` ≠ 사모 · `사모` 글자는 정확** | `(사)` 든 이름 **1,984행 → 공모 1,981(99.8%) · 사모 3** · `사모` 든 이름 **1,981행 → 전부 사모** · 두 집합 겹침 **3행**. `(사)` 는 특수채(MBS 985·일반특수법인채 596)에 붙고 `사모` 는 회사채(Conduit 1,006·일반회사채 731)에 붙음 | 이름으로 사모 판정 시 **오탐률 50.0%**(3,962건 중 1,981건 오탐 + 26건 누락). 1차 `is_private` 15,261 이 부풀려진 원인 | **✅ 08-27 확정 (§5-4-ㄱ)** — 공모/사모는 `bd_ofr_tcd` 로만 판정, 이름 추론 금지. `(사)` 의 뜻 자체는 주최 문의(§5-4-ㄴ) |
| P-4 | 종가 기준일 공백 8칸 16,453 + NULL 23 | A-1-02 | `IS NOT NULL` 오집계 | yaml 이미 반영, 23 NULL 만 미기재 |
| P-5 | 메리츠캐피탈 256-4 종가 10,135 / 종가수익률 −10.287% | 1행 | "종가 높은" 1위 | 기록만 |
| P-6 | 중복 8종목 장외행 평가가 **액면 수준(10,000.00~10,000.12)** · 위험등급 `00` | KAMCO 163·164 · NH농협캐피탈 **269-1~6** (08-27 오기 정정: 269-1~7 아님). 정확히 10,000.0 은 8행 중 4행 | 대표행 선택에 따라 "AAA" / "정보 없음" 이 갈림 | `대표행` 을 **병기 규칙**으로 (A-2-01, 08-27 승인) |
| P-7 | 등급일 `crd_grd_dt=0` 1행 | 주택금융공사MBS2011-20 AAA | 없음(`등급일사용금지`) | 경미 |
| P-8 | 평가가 정확 10,000 행 21(장내 3·장외 18) | 1차 1,813 → 2차 21 | 축소 확인 | 기록 |
| P-9 | 장외 등급 NULL 2,023 = 공모 1,101 + 사모 922 · 국공채 1,093 + 회사채 926 | `장외등급해석` "미평가" 와 정합 | — | 기록 |
| P-10 | `mat_dt=0` 4행에 듀레이션 값 있음(0.83~1.46) | SH보상 2 · 산금채 2 | 잔존일수 NULL 인데 듀레이션 산출 — 출처 불명 | 기록 |
| P-11 | `dur=99` 센티넬 3행은 신종(영구형) BW·CB·EB | 만기 2054~55 | `듀레이션정상` 이 dur 만 커버 | B-3-08 |
| **P-12** | **🆕 외부 데이터는 금지가 아니라 조건부 허용** | `.agents/rules/miraeasset-rules.md` §2: "마스터 데이터에 미포함된 정보는 **외부 수집이 허용**되나, 반드시 **2026-07-11 이전** 데이터만" | BAC 채권 만기·발행사, `(사)` 표기 등 "미수록" 처리한 것들이 **보완 가능한 후보**가 됨 | 회의 안건 — 이번 기입 대상 아님 |
| **P-13** | **🆕 규칙 파일의 기준일이 2차 DB 와 어긋남** | 규칙 파일(최종수정 **2026-08-09**) §2 = 기준일 **2026-07-11**. 현 2차 DB(`v2_20260824`) = `info_base_dt` **20260821** · 대회 기준 **2026-08-22** | 외부 보완을 실제로 하면 "7/11 이후 반영 시 감점" 조항의 적용 기준이 불명확 | **팀 확인 필요** — 외부 수집 착수 전 필수 |
| **P-14** | **🆕 Single-turn Q&A 규격 — 되묻기 후 후속 답변이 불가** | 규칙 파일 §5: "질의응답은 **Single-turn Q&A** 규격 준수". §3: 모르면 지어내지 말고 `"확인할 수 없음"` 반환 | 되묻기는 **답을 지어내는 것보다 나은 안전장치**이지, 답을 두 턴에 나눠 주는 도구가 아님. 값이 갈리는 경우엔 **병기**가 유일한 해 | A-2-01 병기안의 근거 |
| **P-15** | **🆕 텍스트 7개 컬럼에 고정폭 공백 패딩 — `=` 비교가 조용히 빗나감** | `pd_eng_nm` 21,842 · `pd_abrv_nm` 21,837 · `pd_abrv_eng_nm` 21,814 · **`bd_knd` 21,682** · `pd_pbcm` 21,282 · `exg_close_price_base_dt` 16,453 · `pd_nm` 684. `bd_knd` distinct 원문 **41종** vs TRIM 후 **32종** | `bd_knd='유동화회사채'` 로 쓰면 **1,528행 중 6행만** 잡힘(나머지 1,522행은 뒤 공백 18칸) | ✅ **현재 결함 아님** — `query_rules.문자열비교`("항상 TRIM 후 비교") 존재하고 `유동화위험금지` 도 `TRIM(bd_knd) IN (...)` 로 올바름. **새 규칙 작성 시 필수 준수 사항**으로 기록 |
| **P-16** | **🆕 만기 경과인데 `dur ≠ 0` 인 1행이 `듀레이션정상` 을 빠져나감** | `아우딘퓨쳐스3CB(사모/전환/풋)` (`KR6227611D67`, 장외) — 만기 **2026-06-28**(2개월 경과) · 잔존 **0** · `dur` **0.4319** · 평가가 10,049원. 규칙이 `NOT (dur = 0 AND remaining_days > 0)` 이라 `dur ≠ 0` 인 이 행은 안 걸리고, `dur` 의 영값배제도 0 이 아니라 안 걸린다 | "금리위험 낮은 채권" 정렬에서 **이미 만기가 지난 채권**이 상위에 남는다 | **17건 기입 범위 안에서 처리** — `듀레이션정상` 에 `AND remaining_days > 0` 을 더할지 판단(B-3-08 조치와 같은 규칙). 회의 안건 아님 |

---

## 5. 결정 필요 (사용자 · 회의)

1. ~~**A-1-05 (c)**~~ — **✅ 08-27 사용자 승인 완료: (가)안**. `curr_cd='KRW'` 를 모든 채권 질의의 기본 조건으로 `query_rules.외화채없음` 에 명문화한다. 승인 전제였던 "정말 1행뿐인가" 재확인 완료(NULL 1 · 공백 위장 0).
2. ~~**P-2 수익률 랭킹**~~ — **✅ 08-29 결정·반영 완료.** 사용자 결정: *"도메인상 사면 안 되는 선만 쳐내고, 그 외는 넣되 너무 높은 수익률은 위험할 수 있다는 주의 문구를 붙인다."* → 제외선 = `pd_risk_gcd='11' OR crd_grd='C0'`(추천·랭킹만) · 주의 문구 트리거 = `applied_yield > 6%` 또는 위험등급 2·3등급. `query_rules.고위험제외` 신설. 결정 내용·실측은 **§10-⑦**.
3. ~~**C-4-01**~~ — **✅ 08-28 종결: 현행(영값배제) 유지 · 회의 불필요.** 쟁점은 "잔존일수 0 이 값 없음인가, 만기가 오늘이라는 진짜 값인가" 였다.
   - **0 은 진짜 값이다** — 49행 전부 만기일 ≤ 기준일(2026-08-21)이고 `mat_dt <= 20260821` 집합과 **대칭차집합 0**. 만기가 미래인데 0 인 행은 **0건**. 하루 뒤(8/22) 만기인 7행에는 정확히 `1` 이 들어 있어 계산이 기준일에 맞물려 있음이 확인된다.
   - **그래도 모수에는 넣지 않는다** — 49행은 판매 관련 6개 컬럼(`buy_yield`·`bdbns_abl_chnl_tcd`·`bdbns_abl_chnl_nm`·`avg_annual_tax_yield`·`buyable_quantity`·`sale_yield_base_dt`) 전수 교차에서 **적중 0건**(각 non-null 634)이라 애초에 판매 대상이 아니다. `zero_is_value` 로 올리면 "만기 임박한 채권"(잔존 ≤30일) 모수가 578 → **627** 이 되고 **이미 만기 지난 49건이 상위**로 온다.
   - "만기 지난 채권" 류 질의는 `mat_dt` 로 답하면 되므로 예외 등록 없이도 답변 가능하다.
   - **조치**: `remaining_days.note` 한 줄 보강만(17건 기입 시 함께). `query_rules` 무변경. 재현 SQL·재점검 경위는 **§8-(l)**.
   - ⚠️ **확실하지 않은 것 하나 — "지금은 49행이 빠진다" 는 규칙 문구 기준이지 실증이 아니다.** `eval/` 63문항 전수 검색 결과 `remaining_days` 를 쓰는 gold 문항은 **0건**(채권 문항 자체가 1건뿐). 영값배제 기본이 이 컬럼에 실제로 걸리는지 확인해 준 평가 문항이 없다 → **플래너 연결 후 "만기 임박한 채권" 질의를 한 번 돌려 실측**해야 종결이 완결된다.
   - 🆕 이 검증에서 **P-16** 이 새로 나왔다(만기 경과인데 `dur ≠ 0` 인 1행이 `듀레이션정상` 을 통과) — 회의 안건이 아니라 **17건 기입 범위 안**에서 처리한다.
4. **P-3 `(사)` 표기** — **둘로 쪼갬**.
   - **(ㄱ) ✅ 08-27 사용자 승인 — 반영 대상**: `bd_ofr_tcd` 컬럼으로만 공모/사모를 판정하고 **종목명으로 추론하지 않는다**. 이름 판정은 오탐률 50.0% 라 우리 데이터만으로 결론이 남 → 주최 확인 불필요. yaml 조치는 §11 참조.
   - **(ㄴ) 주최/원천 문의 (회의)**: `(사)` 가 대체 무슨 뜻인가. 가설 — 유동화 상품의 **"사채(社債) 형태"** 표시(특수채·MBS 에만 붙고 85%가 특수채). **가설일 뿐 확증 없음.** (ㄱ)만 넣으면 챗봇 오답은 이미 막히므로 (ㄴ)는 `(사)` 를 *활용*하고 싶을 때만 필요.
5. ~~**A-2-01 대표행**~~ — **✅ 08-27 사용자 승인 완료: 병기안**(장내 우선 필수 → 철회). 병기 조건 두 가지도 승인 — ① "값이 같으면 한 번만 답한다", ② "장외 가격에 **액면가 수준** 단서를 붙인다". 상세는 §3 A-2-01 · §9.
6. **P-12/P-13 외부 데이터** — **✅ 08-29 기준일 확정: 2026-07-11.** 사용자 결정 *"외부 데이터는 7/11 기준이 맞아"* = 규칙 파일 원문(`.agents/rules/miraeasset-rules.md` §2 "반드시 2026-07-11 이전 데이터만") 그대로. 채권 외부 보완(ㄱ BAC 채권 만기·발행사, ㄴ `(사)` 표기)은 **7/11 이전 발행 자료만**으로 착수 가능.
   - ⚠️ **팀 문서와 어긋남** — `PROJECT.md` §2(line 39·122)·`docs/EXTERNAL_DATA.md` 공통 원칙 ①·`DATA_V2_2026-08-24_impact.md` 는 "주최 8/24 공지로 7/11 → **8/24 로 완화**" 라고 적혀 있다. 규칙 파일은 개정되지 않았으므로 채권은 7/11 을 따르되, **세 문서를 고칠지는 리드 확인 필요**(ETF Holdings 8월 재수집 계획이 8/24 완화에 기대고 있음).

---

## 6. 재현 SQL (주요 수치)

```sql
-- A-1-01: 세후연평균 non-null = 판매 LOT
SELECT COUNT(avg_annual_tax_yield), SUM(avg_annual_tax_yield=0),
       SUM((avg_annual_tax_yield IS NOT NULL) <> (buy_yield IS NOT NULL)) FROM domestic_bonds;   -- 634, 634, 0
-- A-1-02: 장내 종가 모수 / 기준일 연도
SELECT pd_exg_mkt, COUNT(*), COUNT(exg_close_price), SUM(exg_close_price>0) FROM domestic_bonds GROUP BY 1;  -- 장내 17746/17746/1270
SELECT SUBSTR(exg_close_price_base_dt,1,4), COUNT(*) FROM domestic_bonds WHERE exg_close_price>0 GROUP BY 1; -- 2026: 158
SELECT SUM(TRIM(exg_close_price_base_dt)=''), SUM(exg_close_price_base_dt IS NULL) FROM domestic_bonds WHERE pd_exg_mkt='장내' AND exg_close_price=0; -- 16453, 23
-- A-1-05: 표면금리 결측 1행
SELECT pd_no, pd_nm, curr_cd, crd_grd, dur, applied_yield, mat_dt FROM domestic_bonds WHERE srfc_irt IS NULL;  -- XS3067881758
-- A-1-03/04, A-2-01
SELECT COUNT(DISTINCT pd_nm), COUNT(DISTINCT pd_no), COUNT(*), SUM(pd_nm<>TRIM(pd_nm)) FROM domestic_bonds; -- 20499, 20497, 21882, 684
SELECT n, COUNT(*) FROM (SELECT pd_no, COUNT(*) n FROM domestic_bonds GROUP BY pd_no) GROUP BY n;  -- 1:19419 2:772 3:305 4:1
SELECT pd_no FROM domestic_bonds GROUP BY pd_no HAVING COUNT(*)>1 AND COUNT(DISTINCT COALESCE(crd_grd,'N'))>1; -- 8종목
-- 🆕 A-2-01 병기안 근거 (08-27): 양쪽 시장에 다 있는 종목 중 값이 진짜 다른 건 몇 개인가
SELECT COUNT(*), SUM(ABS(a-b)<0.005) same, SUM(ABS(a-b)>=0.005) diff FROM
  (SELECT pd_no, MAX(CASE WHEN pd_exg_mkt='장내' THEN eval_price END) a,
                 MAX(CASE WHEN pd_exg_mkt='장외' THEN eval_price END) b
   FROM domestic_bonds GROUP BY pd_no HAVING a IS NOT NULL AND b IS NOT NULL);  -- 1077, 1069, 8
-- 장외 등급 NULL 828종목의 쪼개기 — 820은 장내도 NULL(대표행 문제 아님), 8만 갈림
SELECT COUNT(*) FROM (SELECT pd_no FROM domestic_bonds GROUP BY pd_no
  HAVING SUM(pd_exg_mkt='장내')>0 AND SUM(pd_exg_mkt='장외')>0
     AND SUM(pd_exg_mkt='장외' AND crd_grd IS NULL)>0
     AND SUM(pd_exg_mkt='장내' AND crd_grd IS NOT NULL)>0);  -- 8  (>0 → IS NULL 로 바꾸면 820)
-- 8종목 장외행 실측 (평가가는 '파킹 10,000' 이 아니라 액면 수준)
SELECT SUBSTR(TRIM(pd_nm),1,20), pd_exg_mkt, pd_risk_gcd, crd_grd, eval_price FROM domestic_bonds
 WHERE pd_no IN (SELECT pd_no FROM domestic_bonds GROUP BY pd_no
   HAVING COUNT(*)>1 AND COUNT(DISTINCT COALESCE(crd_grd,'N'))>1) ORDER BY 1, 2;
   -- 장내: 위험 15/16 · 등급 AA-/AAA · 9,990.33~9,997.89
   -- 장외: 위험 00   · 등급 NULL     · 10,000.00~10,000.12 (정확히 10000.0 은 4/8)
-- 🆕 A-1-05 (가)안 승인 전제: 공백 위장 결측이 없는지
SELECT COUNT(*) FROM domestic_bonds WHERE srfc_irt IS NULL;                    -- 1
SELECT COUNT(*) FROM domestic_bonds WHERE TRIM(COALESCE(srfc_irt,'x'))='';     -- 0
-- A-4-02
SELECT info_base_dt, COUNT(*) FROM domestic_bonds GROUP BY 1;  -- 20260821: 21882
-- B-3-02 / B-3-04
SELECT SUM(buyable_quantity % 1000 <> 0), MAX(buyable_quantity) FROM domestic_bonds WHERE buyable_quantity>0; -- 0, 18587504000
SELECT info_seq, COUNT(*) FROM domestic_bonds GROUP BY 1;  -- 1:21574 2:307 3:1
-- B-3-03: 컨벡시티 ≈ dur² (무이표)
SELECT pd_nm, dur, cov, dur*dur FROM domestic_bonds WHERE pd_nm LIKE '국고채%분리%' ORDER BY dur DESC LIMIT 1; -- 46.44 / 2151 / 2157
-- B-3-05~09: 익일 vs 당일
SELECT SUM(ndy_applied_yield=applied_yield), SUM(ndy_applied_yield<>applied_yield) FROM domestic_bonds WHERE ndy_applied_yield>0 AND applied_yield>0; -- 21619, 34
SELECT SUM(ndy_eval_price=eval_price), SUM(ndy_eval_price<>eval_price) FROM domestic_bonds WHERE ndy_eval_price>0; -- 34, 21816
SELECT SUM(ndy_dur=99), SUM(dur=99) FROM domestic_bonds;  -- 3, 3
-- P-2: 초고수익률
SELECT COUNT(*), GROUP_CONCAT(DISTINCT crd_grd), GROUP_CONCAT(DISTINCT TRIM(bd_knd)) FROM domestic_bonds WHERE applied_yield>50; -- 96, C0, 유동화회사채
-- P-3: (사) vs 공모/사모
SELECT bd_ofr_tcd, COUNT(*) FROM domestic_bonds WHERE pd_nm LIKE '%(사)%' OR pd_nm LIKE '%사모%' GROUP BY 1; -- 공모 1981, 사모 1981
-- 🆕 P-3 정정 (08-27): 위 "정확히 절반" 은 성격이 다른 두 신호를 합쳐서 생긴 착시였다. 쪼개면 —
SELECT bd_ofr_tcd, COUNT(*) FROM domestic_bonds WHERE pd_nm LIKE '%(사)%' GROUP BY 1;  -- 공모 1981 · 사모 3   → (사) 는 사모가 아니다
SELECT bd_ofr_tcd, COUNT(*) FROM domestic_bonds WHERE pd_nm LIKE '%사모%'  GROUP BY 1;  -- 사모 1981           → '사모' 글자는 정확
SELECT COUNT(*) FROM domestic_bonds WHERE pd_nm LIKE '%(사)%' AND pd_nm LIKE '%사모%';  -- 3  (두 집합 거의 별개)
SELECT COUNT(*) FROM domestic_bonds WHERE TRIM(bd_ofr_tcd)='사모'
   AND pd_nm NOT LIKE '%(사)%' AND pd_nm NOT LIKE '%사모%';                             -- 26 (이름으로는 놓치는 사모)
-- 🆕 P-15: 텍스트 컬럼 공백 패딩 전수 (bd_knd 를 = 로 비교하면 1,528 중 6행만 잡힘)
SELECT COUNT(*) FROM domestic_bonds WHERE bd_knd<>TRIM(bd_knd);                          -- 21682
SELECT COUNT(DISTINCT bd_knd), COUNT(DISTINCT TRIM(bd_knd)) FROM domestic_bonds;         -- 41 vs 32
-- 🆕 P-2 회의용 근거 (§10) — 등급/위험등급 두 기준 비교
SELECT crd_grd, COUNT(*), ROUND(MAX(applied_yield),1) FROM domestic_bonds GROUP BY 1 ORDER BY 2 DESC;
   -- C0 103행 최고 728.5% · 그 밖 모든 등급의 최고는 28.2% (등급 NULL 4,020행)
SELECT pd_risk_gcd, TRIM(pd_risk_nm), COUNT(*), ROUND(MAX(applied_yield),1) FROM domestic_bonds GROUP BY 1,2;
   -- 11 매우높은위험 1,441행 최고 728.5% / 12 높은위험 74행 최고 9.8% / … / 00 해당없음 19행
SELECT COUNT(*) FROM domestic_bonds WHERE crd_grd='C0' AND COALESCE(pd_risk_gcd,'X')<>'11';   -- 0  (C0 ⊂ 위험등급11)
SELECT COUNT(*) FROM domestic_bonds WHERE applied_yield>10 AND COALESCE(crd_grd,'X')<>'C0';   -- 45 (C0 만으로는 샘)
SELECT TRIM(bd_ofr_tcd), COUNT(*) FROM domestic_bonds
  WHERE applied_yield>10 AND COALESCE(crd_grd,'X')<>'C0' GROUP BY 1;                          -- 사모 41 · 공모 4
-- C0 종목이 두 줄인데 한 줄만 C0 라 새는 경우 (A-2-01 문제의 재발 여부)
SELECT COUNT(*) FROM (SELECT pd_no FROM domestic_bonds GROUP BY pd_no
  HAVING SUM(crd_grd='C0')>0 AND COUNT(*)>SUM(COALESCE(crd_grd,'X')='C0'));                   -- 0  (누수 없음)
-- 가격 기준 필터가 왜 안 되는가
SELECT COUNT(*) FROM domestic_bonds WHERE eval_price>0 AND eval_price<7000 AND crd_grd='AAA'; -- 320 (한국남부발전 등 초장기 저이자)
-- C-4-01
SELECT SUM(applied_yield=0), SUM(isu_dt=0), SUM(mat_dt=0), SUM(isu_bal_amt=0), SUM(dur=0), SUM(ndy_eval_price=0), SUM(avg_annual_tax_yield=0) FROM domestic_bonds; -- 201,25,4,325,52,16,634
```

실행: `PYTHONIOENCODING=utf8 python -` 로 `sqlite3.connect('data/financial_products.db')`.

---

## 7. 반영하지 않은 것 (명시)

- `A_정합성.md` · `B_피처의미.md` · `C_결측의미.md` 의 `판정`·`근거·조치` 칸 — 전부 `☐` 그대로.
- ~~`ontology/enums/domestic_bonds.yaml` — 무변경.~~ → **2026-08-28 반영 완료** (§12-⑥ 에 실제 변경 목록).
- `scripts/gen_review_workbook.py` — 실행하지 않음.
- 커밋·푸시 — 없음. 이 기록 파일도 미커밋.

> ~~**🆕 08-27 2차 갱신분에 대해서도 동일하다.**~~ → **✅ 2026-08-28 해소.** "괜찮아 기입해" 를 받아 승인 내용이 전부 표·yaml 에 반영됐다. 이 절(§7)이 적은 "반영하지 않은 것" 중 **여전히 반영하지 않은 것은 §10 P-2(회의 안건)뿐**이다.

**승인 시 순서**: 표 17칸 기입 → yaml 수정(§2 조치) → `check_yaml_dupkeys.py` · `build_ontology.py --check` · `pytest` · `run_gold_check.py` → `git diff` 보고 → 커밋 `review(A·B·C): 채권 17건 판정`.

---

## 8. 자기 재점검 (08-27, 승인 요청 전) — "이 제안을 승인해도 안전한가"

제안을 의심하고 네 가지를 확인했다. 결과 **정정 3건**(B-3-03·04·06), 나머지는 유지.

| 검사 | 확인한 것 | 결과 | 제안에 미친 영향 |
| :-- | :-- | :-- | :-- |
| (a) `unit` 어휘 충돌 | 4개 yaml 의 `unit` 값 전수: percent 32 · krw 10 · grade 3 · yyyymmdd 2 · percent_cumulative 2 · years 1 · usd 1 · days 1 · krw_per_1000units 1 · unknown 1. **B-4-01~12(리드 담당, 전부 ☐)** 가 "어휘를 enum 으로 고정할지" 를 결정하는 항목 | 제안한 `years_squared`·`ordinal` 은 **존재하지 않는 어휘** — 지시서 "단위 표기는 B-4 에 손대지 말고 부수 발견에만" 위반 | **B-3-03·06 → ⚠️, B-3-04 → `unit` 키 삭제.** `percent`·`krw`·`years` 는 기존 어휘라 유지 |
| (b) 런타임 영향 | `src/runtime/loader.py`: 플래너 프롬프트에 넣는 것은 `query_rules`·`normalization` 뿐(83~94행). `columns` 에서 읽는 건 `crd_grd.value_semantics` 하나(140~143행). `gate.py`·`pipeline.py`·`planner.py` 는 `missing_reason`·`answerable_n`·`unit`·`answer_policy` 를 참조하지 않음 | `missing_reason`·`missing_semantics`·`unit`·`note`·`trap`·`row_grain` 변경은 **답변에 영향 0**. 답변이 바뀌는 건 `query_rules` 문구뿐 | A-1-05 (a)(b)·B-3 unit 6건·note 보강 = 무위험. **`외화채없음`·`듀레이션정상`·`대표행` 문구만 실질 변경** (🔧 08-27 갱신: 원래 2개로 적었으나 `대표행` 병기 전환으로 **3개**가 됨 — §9-⑤) |
| (c) 728% 행을 막는 규칙 유무 | `특수구조제외` 원문: "조회·사실확인 질의에서는 제외하지 않는다". `수익률정상`: `applied_yield > 0` 만. `유동화위험금지`(main 08-26 정정판, 4,045행): "발행사명으로 위험 판단 금지" — 랭킹 제외 아님. `name_encoding` 플래그 11종에 유동화 플래그 없음 | **P-2 확인** — 어떤 규칙도 신보 유동화 C0 728% 를 랭킹에서 빼지 않음 | 회의 안건 유지 (§5-2) |
| (d) 평가셋 정합 | `eval/` 에 채권 전용 문항 파일 없음. 주최 예시 `OFFICIAL-001`("판매 가능한 원화채권 중 AA- 이상") 의 gold_sql 이 **이미 `curr_cd='KRW'` 를 조건으로 씀** | A-1-05 (c) "모든 채권 질의 `curr_cd='KRW'` 기본" 은 **gold 와 같은 방향** — 새 동작이 아니라 gold 가 이미 쓰는 조건을 규칙으로 명문화하는 것 | (c) 승인 권고 근거 |

**결론 — 승인 권고 (정정 3건 반영 후)**
- ✅ 8 · ⚠️ 2 · ❌ 7 전부 승인 가능. 위험 순서: `외화채없음` 규칙 문구(gold 와 일치, 1행 제외) > `듀레이션정상` 에 `ndy_dur` 추가(익일 질의에서만 작동) > 나머지(기록성, 런타임 무관).
- 승인 뒤 `run_gold_check.py` 63/63 이 유지되는지로 두 규칙 변경을 검증한다.
- 결정 필요 §5 의 2·3·4 는 이번 기입과 무관 — 회의로.

### 🆕 08-27 추가 재점검 (사용자 문답 중 발견) — 검사 2건, **에이전트 판단 정정 2건**

| 검사 | 확인한 것 | 결과 | 제안에 미친 영향 |
| :-- | :-- | :-- | :-- |
| (e) 외부 데이터 가부 | 에이전트가 "이 프로젝트는 외부 데이터를 안 쓴다" 고 단언했으나, 근거로 든 것은 yaml 의 `미수록` 표기뿐이었음. 실제 규칙 파일 `.agents/rules/miraeasset-rules.md` §2 를 읽음 | **에이전트 오답.** 원문은 "마스터 데이터에 미포함된 정보는 **외부 수집이 허용**되나 2026-07-11 이전 데이터만". `미수록` 은 *못 채우는 것*이 아니라 *아직 안 채운 것* | **P-12 신설.** BAC 채권·`(사)` 표기가 외부 보완 후보로 격상. 단 **P-13**(규칙 파일 기준일 7/11 vs 2차 DB 8/22)이 선결 |
| (f) 되묻기 가능 여부 | 같은 규칙 파일 §5 = **Single-turn Q&A** 규격 · §3 = 모르면 `"확인할 수 없음"` 반환 | **되물으면 그게 곧 최종 제출 답변** — 고객의 대답을 받을 턴이 없음. 되묻기는 *할루시네이션보다 나은 안전장치*이지 2턴 도구가 아님 | **A-2-01 을 병기안으로 확정하는 결정적 근거.** P-14 신설 |

### 🆕 08-27 3차 재점검 (부수 발견 처리 중) — **에이전트 판단 정정 2건 + 근거 오류 3건**

| 검사 | 확인한 것 | 결과 | 영향 |
| :-- | :-- | :-- | :-- |
| (g) **P-2 기준선 역방향 검증** | "C0 만 빼면 깔끔하다" 를 의심하고 **반대 방향**(C0 가 아닌데 고수익률인 것)을 전수 조회 | ❌ **불충분.** 수익률 10% 초과 **45행**이 샘 — 41행이 사모(기존 규칙이 잡음), **4행(에스엘엘중앙 B-)은 공모라 그냥 통과**. 게다가 그 4행은 데이터상 이미 **위험등급 1등급(매우높은위험)** | 기준을 **`crd_grd='C0'` → `pd_risk_gcd='11'`** 로 교체 권고(§10). "B- 하이일드는 남겨도 된다"던 판단도 철회 |
| (h) **P-3 "정확히 절반" 재검증** | 우연치고 이상해서 `(사)` 와 `사모` 를 **분리**해 셈 | ❌ **착시였음.** `(사)` 1,984행 중 **1,981행(99.8%)이 공모** · `사모` 1,981행은 **전부 사모**. 두 집합 교집합 3행 | "절반이 공모" 라는 서술이 **뜻을 정반대로 읽히게** 함 → §11 로 전면 재작성. 처리 방향도 "주최 문의 대기" → **"지금 확정 가능"** 으로 바뀜 |
| (i) 근거 SQL 신뢰성 | P-2 근거였던 `GROUP_CONCAT(DISTINCT TRIM(bd_knd))` 를 원문(`repr`)으로 재조회 | ❌ **NULL 90행이 출력에서 증발**해 "전부 유동화회사채" 로 오독. 실제는 NULL 90 + 유동화회사채 6 | §3 B-3-05 부수 발견 정정 |
| (j) 문자열 비교 안전성 | 텍스트 컬럼 **전수** 패딩 조사 | `bd_knd` 21,682행 등 **7개 컬럼**에 고정폭 공백. `bd_knd='유동화회사채'` 는 1,528 중 **6행만** 잡음 | ✅ yaml `문자열비교` 규칙이 이미 있어 **현재 결함 아님** — P-15 로 기록 |
| (k) 대체 기준 배제 검증 | "가격이 싼 것을 빼면 되지 않나" 를 실측 | ❌ **위험.** 7,000원 미만 599행 중 **320행이 AAA**(한국남부발전 등 표면금리 1.29%·만기 2049년) | §10-④ 에 "쓰면 안 되는 기준" 으로 명시 |

> 📌 **3차 교훈** — **"이 기준이면 충분하다" 는 순방향으로는 절대 확인되지 않는다.** 상위 몇 줄만 보면 언제나 깔끔해 보인다. (g)·(h) 둘 다 **"이 기준을 빠져나가는 게 뭔가" 를 전수로 물었을 때만** 드러났다. 사용자가 "다시 한 번 확인해봐" 라고 하지 않았으면 둘 다 그대로 나갔을 것이다.

> 📌 **이번 회차의 교훈 2가지**
> 1. **"규칙이 이렇다" 고 말하기 전에 규칙 파일을 읽는다.** yaml 안의 관행(`미수록` 표기)을 보고 프로젝트 규칙을 역추정했다가 틀렸다. 규약은 `.agents/rules/miraeasset-rules.md` 가 원본이다.
> 2. **결측을 "같은 값" 으로 메우는 것은 데이터가 아니라 추측이다.** `장내행 우선 필수` 제안이 그것이었고, 사용자 지적("기록 안 된 것과 AA- 는 다르다")으로 철회했다. 없는 것은 **없다고 말하되 있는 쪽과 나란히 보여준다**(병기).

### 🆕 08-28 4차 재점검 (§5-3 종결 전) — 검사 3건, **에이전트 판단 정정 1건 + 신규 발견 1건**

| 검사 | 확인한 것 | 결과 | 영향 |
| :-- | :-- | :-- | :-- |
| (l) **`remaining_days=0` 이 결측인가 값인가** | 49행의 `mat_dt` 를 전수로 봄 + "0 인데 만기가 미래" 인 행을 반대 방향으로 조회 | **진짜 값 확정.** 만기 미래 + 0 인 행 **0건.** `mat_dt <= 20260821` 과 **대칭차집합 0.** 하루 뒤 만기 7행은 `1` — 기준일에 정확히 맞물림 | §5-3 종결. **모수에는 안 넣음**(아래 (m)) |
| (m) **49행이 판매 대상인가** | 처음엔 채널코드 하나만 봤다가, 스키마에서 매수수익률 컬럼을 찾아 **판매 관련 6컬럼 전수 교차** | 각 non-null **634** · 49행 중 적중 **0건** (`buy_yield`·`bdbns_abl_chnl_tcd`·`bdbns_abl_chnl_nm`·`avg_annual_tax_yield`·`buyable_quantity`·`sale_yield_base_dt`) | "판매 대상 밖" 근거가 컬럼 1개 → **6개 전수**로 보강 |
| (n) **`dur` 교차** | 49행의 `dur` 분포를 셈 | ❌ **"49행 전부 `dur=0`" 은 틀렸다 — 48행이다.** 1행(`dur=0.4319`)이 `듀레이션정상` 을 **통과**함 | **P-16 신설** |
| (o) **평가셋으로 실증되는가** | `eval/*.jsonl` **63문항 전수** 검색 | ❌ **실증 없음.** `remaining_days` 를 쓰는 gold 문항 **0건** · 채권 문항 자체가 **1건**(`questions_official_sample.jsonl`) | §5-3 결론은 **규칙 문구 기준**임을 명시. 플래너 연결 후 실측 필요 |

**❌ 에이전트 판단 정정 1건 — 부수 발견을 철회했다**

| 무엇을 틀렸나 | 어떻게 드러났나 | 교훈 |
| :-- | :-- | :-- |
| yaml `remaining_days.note` 의 *"만기상태는 `mat_dt` 로 판정하든 `remaining_days` 로 하든 어느 쪽을 써도 됨"* 이 **부정확하다**고 지적하고, 근거로 `mat_dt <= 2026-08-**22**` = 56행 vs `remaining_days=0` = 49행을 들었다. P-13(기준일 7/11 vs 8/22)과도 엮었다 | 사용자가 "확실해?" 라고 되물어 같은 기준일로 다시 셈 — `remaining_days` 는 **8/21 기준**이므로 `mat_dt <= 20260821` 로 맞춰야 했다. 그러면 **49 = 49, 대칭차집합 0** 으로 완전 일치 | **비교 대상의 기준일을 맞추지 않고 "불일치" 라고 말하지 않는다.** 내가 8/22 를 넣어 만든 착시였다. yaml 410줄은 **정확하며 정정 대상이 아니다.** yaml 240줄("기준일 2026-08-22 · 만기 경과 49행")도 8/22 **이전** = 49 로 맞다. P-13 과 엮은 것도 근거 없어 철회 |

> 📌 **4차 교훈** — **"불일치를 찾았다" 는 주장은 스스로 한 번 더 반증해 본 뒤에 낸다.** 이번엔 사용자의 "확실해?" 한 마디가 없었으면 없는 결함 하나가 그대로 기입될 뻔했다. 다만 같은 재점검에서 **진짜 결함(P-16)이 하나 나왔다** — 재점검 자체는 값이 있었고, 틀린 것은 첫 판정 쪽이었다.

---

## 9. 🆕 08-27 사용자 문답으로 확정·정정된 것 (이번 회차 변경 총괄)

> 다음 세션은 **이 절만 읽어도 무엇이 바뀌었는지** 알 수 있다. 판정 17건의 ✅/⚠️/❌ 구성(**✅ 8 · ⚠️ 2 · ❌ 7**)은 **바뀌지 않았다.** 바뀐 것은 **조치 문구와 근거**다.

### ① 사용자 승인 2건 (확정 — 기입 시 이대로 반영)

| # | 항목 | 확정 내용 |
| :-: | :-- | :-- |
| 1 | **A-1-05 표면금리** (§5-1) | **(가)안** — `curr_cd='KRW'` 를 모든 채권 질의의 **기본 조건으로 `query_rules.외화채없음` 에 명문화**한다. 승인 전제였던 "정말 1행뿐인가" 를 재확인함: NULL **1** · 공백 위장 **0** |
| 2 | **A-2-01 대표행** (§5-5) | **병기안** — `장내행 우선 필수` 제안은 **철회**. `row_grain.대표행` 과 `query_rules.대표행` **두 곳** 을 병기 규칙 3줄로 교체 (§3 A-2-01 조치 참조). 부속 조건 2개도 승인: **① 값이 같으면 한 번만 답한다** (1,069종목 중복 출력 방지) · **② 장외 가격에 "액면가 수준" 단서를 붙인다** |

### ② 에이전트가 틀렸던 것 2건 (스스로 정정 — 같은 실수 반복 금지)

| 무엇을 틀렸나 | 어떻게 드러났나 | 교훈 |
| :-- | :-- | :-- |
| **"이 프로젝트는 외부 데이터를 안 쓴다"** 고 단언 | 근거가 yaml 의 `미수록` 표기뿐이었음. 실제 규칙 파일 §2 는 **"외부 수집 허용(7/11 이전 자료만)"** | **규약을 말하기 전에 `.agents/rules/miraeasset-rules.md` 를 읽는다.** yaml 관행으로 프로젝트 규칙을 역추정하지 말 것 |
| **`장내행 우선 필수`** 를 조치로 제안 | 사용자 지적 — "**등급이 기록 안 된 것**과 **AA-** 는 다른 정보다" | **결측을 '옆 줄과 같은 값' 으로 메우는 것은 데이터가 아니라 추측이다.** 없는 것은 없다고 말하되, 있는 쪽과 나란히 보여준다(병기) |

### ③ 기록 자체의 오기 2건 (정정 완료)

| 어디 | 틀린 기재 | 실측 |
| :-- | :-- | :-- |
| §3 A-2-01 · P-6 | NH농협캐피탈 **269-1~7** | **269-1~6** (`KR6097242G84`~`KR6097247G89` = 6종목). "총 8종목" 은 맞음 |
| §3 A-2-01 · P-6 | 장외 평가가 **"10,000 파킹"** | **10,000.00~10,000.12 = 액면가 수준.** 정확히 10,000.0 인 건 8행 중 **4행** |

### ④ 새로 발견해 안건으로 올린 것 3건

| # | 발견 | 성격 |
| :-: | :-- | :-- |
| **P-12** | 외부 데이터는 **조건부 허용**(7/11 이전) — 채권 쪽은 아직 미사용. BAC 채권 만기·발행사, `(사)` 표기가 보완 후보 | 회의 안건 |
| **P-13** | 규칙 파일(최종수정 08-09) 기준일 **7/11** vs 2차 DB **8/21(대회 기준 8/22)** — 어긋남 | **팀 확인 필요** (외부 수집 착수 전 선결) |
| **P-14** | **Single-turn Q&A** 규격이라 되묻기 후 후속 답변이 없음 | 설계 제약 — 병기안의 근거 |

### ⑤ 이번 변경으로 커진 위험 하나

`대표행` 을 병기로 바꾸면 **`query_rules` 실질 변경이 2개 → 3개**가 된다(`외화채없음` · `듀레이션정상` · `대표행`). `query_rules` 는 플래너 프롬프트에 실제로 들어가는 유일한 덩어리이므로(§8-(b)), 기입 후 **`run_gold_check.py` 63/63 유지**를 반드시 확인하고, 깨지면 즉시 보고·되돌린다.

### ⑦ 2차 문답 — 부수 발견 3건의 처리 (08-27 후반)

사용자 지시: **"1번과 3번은 반영, 2번은 반영하지 말고 회의안으로."**

| # | 발견 | 처리 | 어디 |
| :-: | :-- | :-- | :-- |
| **1** | BAC 외국채 (P-1) | ✅ **반영** — `curr_cd='KRW'` 기본 조건(§5-1 에서 이미 승인된 (가)안으로 종결) | §3 A-1-05 |
| **2** | 수익률 728% (P-2) | 🔴 **회의 안건 — 이번 기입 제외.** 분석·검증은 끝냈고 규칙만 보류 | **§10** |
| **3** | `(사)` 표기 (P-3) | ✅ **반영** — "공모/사모는 `bd_ofr_tcd` 로만 판정, 이름 추론 금지" 한 줄. `(사)` 의 의미는 주최 문의로 분리 | **§11** |

이 회차에서 **에이전트 판단을 두 번 더 정정**했다(§8-(g)). 둘 다 **반대 방향으로 훑어봤을 때만** 드러났다.

### ⑧ 그래서 다음 할 일

1. **17건 전체 기입 승인**("괜찮아 기입해") ← **여기서 막혀 있다**. §2 표의 17건 판정에 동의하는지만 답하면 된다
2. 승인 시: A·B·C 표 17칸 기입 → `domestic_bonds.yaml` 수정(§2 조치 + ①의 확정 2건 + **§11 의 `(사)` 규칙**) → 검사 4종 → `git diff` 보고 → 커밋 `review(A·B·C): 채권 17건 판정`
3. ~~**회의 안건**~~ — **🆕 08-28 이관 완료.** `docs/meeting_agenda_2026-08-26.md` **§4** 에 상정했다: §4-1(**§10 P-2 초고수익률**) · §4-3(`(사)` 의 뜻 주최 문의) · §4-4(P-12/P-13 외부 데이터). ~~`remaining_days`~~ 는 **§5-3 에서 종결**되어 안건에서 뺐다(§8-(l))

---

## 10. P-2 초고수익률 — **✅ 08-29 결정 · `query_rules.고위험제외` 반영 완료**

> **08-27 사용자 지시: "2번은 반영하지 말고 회의안으로 빼줘."** → ①~⑥ 은 회의 자료로 작성한 것(그대로 보존).
> **08-29 사용자 결정** → **⑦** 에 결정 내용과 반영 실측. yaml 은 `고위험제외` 한 키만 신설했고 `특수구조제외` 등 기존 규칙은 손대지 않았다.

### ① 문제

`applied_yield > 50%` 인 **96행**이 "수익률 높은 채권" 랭킹 1~5위를 독식한다. 최대 **728.5%**.

- 전부 등급 **C0**, 평가가 **2,740~4,482원**(액면 10,000 의 **27~45%**)
- 수익률이 높은 게 아니라 **원금 손실 위험이 가격에 반영돼 싸진 것**. 계산상 YTM 이 폭등한 것뿐
- 96행 **전부 `bd_ofr_tcd='공모'`** → 기존 `특수구조제외`(사모 제외)로는 **하나도 안 걸러짐**
- 발행사는 신보 + 중진공 + 기보. `bd_knd` 는 90행이 NULL, `pd_pbcm` 도 90행 NULL → **이름으로만 식별 가능**

### ② 후보 기준 3개를 전수로 비교한 결과

| 안 | 조건 | 제외 행 | 랭킹 1위에 남는 것 | 판정 |
| :-- | :-- | --: | :-- | :-- |
| (가) | `crd_grd='C0'` + 사모 제외 | 2,110 | ❌ 에스엘엘중앙23 **B- · 위험등급 1등급** 17.1% | **불충분** |
| (나) | `pd_risk_gcd='11'`(매우높은위험) | 1,441 | ⚠️ 코오롱 신종자본증권 **사모** 12.4% | 사모 규칙 병행 필요 |
| (다) | `pd_risk_gcd='11'` + 사모 제외 | 2,420 | ✅ 중진공 유동화 **BBB- · 높은위험** 9.8% (가격 액면의 99%) | **권고** |

**(다) 로 하면 수익률 10% 초과가 하나도 안 남는다.**

### ③ 왜 신용등급(C0)이 아니라 **위험등급**인가

| 근거 | 실측 |
| :-- | :-- |
| **결측 차이** | 신용등급 결측 **4,020행**(여기 숨으면 필터 통과) vs 위험등급 결측 **19행** |
| **포함 관계** | C0 103행이 **전부** 위험등급 11 (`C0 AND 위험등급<>11` = **0건**) |
| **누수 차이** | C0 만 걸면 수익률 10% 초과 **45행**이 샘 → 그중 **43행이 위험등급 11** (나머지 2행은 둘 다 사모) |
| **구조 위험 포착** | 위험등급 11 안에 신용등급 **AA- 188행 · AA0 45행** — 신용은 좋은데 구조가 위험한 것(예: 코오롱 신종자본증권 = 신용 AAA·만기 2055·이자지급 임의연기 가능). **신용등급만 보면 "AAA니까 안전" 오판** |
| **설명 가능성** | 위험등급은 **회사가 고객에게 공식 고지하는 등급** — 임의 기준이 아님 |
| **경계 선명도** | 위험등급 11 최고 **728.5%** vs 12(높은위험) 최고 **9.8%** — 낭떠러지 |

### ④ 쓰면 안 되는 기준 (검증 결과)

- ❌ **가격으로 거르기**(예: 평가가 7,000원 미만) — 599행 중 **320행이 AAA**. 한국남부발전 49-2(표면금리 **1.29%** · 만기 **2049년** · 잔존 8,393일) 처럼 **이자를 거의 안 주고 만기가 먼 우량 채권**이 싼 것이다. 부실이 아니다.
- ❌ **유동화 전체 제외** — 유동화 조건식 4,045행 중 **AAA 가 3,198행**(주택금융공사 MBS 등). 통째로 빼면 가장 안전한 상품이 사라진다.

### ⑤ 회의에 올릴 권고안 (합의되면 그때 yaml 반영)

```
① 조회·사실확인 질의  → 그대로 답한다. 728.5% 는 사실이다.
                        (기존 특수구조제외 규칙의 "🔴 조회·사실확인 질의에서는 제외하지 않는다" 와 동일 방침)
② 추천·랭킹 질의     → pd_risk_gcd='11'(매우높은위험) 기본 제외 + 사모 제외(기존 규칙 유지)
③ 제외 사실을 반드시 밝힌다  ← 기존 특수구조제외 규칙이 이미 "제외 사실 명시" 를 요구한다
     "신용등급 C0·위험등급 1등급 채권 N건은 제외했습니다. 수익률이 높아서가 아니라
      원금 손실 위험이 커서 액면의 1/3 가격에 거래되는 것입니다."
④ 안전장치: 조건을 pd_risk_gcd='11' OR crd_grd='C0' 로 둔다
     현 데이터에선 C0 ⊂ 위험등급11 이라 중복이지만, 향후 위험등급이 비는 행이 생기면 C0 가 받쳐 준다.
⑤ 가격 기준 필터는 쓰지 않는다 (④ 참조 — AAA 320행 오폭)
```

### ⑥ 검증에서 통과한 것 (누수 없음 확인)

| 의심 | 결과 |
| :-- | :-- |
| C0 종목이 장내·장외 두 줄인데 한 줄만 C0 라 새는가 (A-2-01 문제의 재발) | ✅ **0건.** C0 103행 = 장내 5 + 장외 98, 두 줄 다 C0 |
| 등급 칸이 비어서 숨은 부실채권이 있는가 | ✅ **없음.** 등급 NULL + 평가가 5,000원 미만 = 103행인데 전부 **국고채 분리채권**(최고 수익률 4.9%) |
| 등급 값에 공백 변종이 있는가 | ✅ **없음.** distinct 16종 전부 깨끗(`'C0'`·`'AAA'` …). 단 **`bd_knd` 는 변종 있음 → P-15** |
| C0 인데 정상인 것이 섞였는가 | ✅ **없음.** 103행 전부 유동화 후순위. 수익률 0 인 7행도 같은 물건(평가가 3,395~3,580원) |
| 데이터에 C0 보다 나쁜 등급(CCC·CC·D)이 있는가 | ✅ **없음.** 최하가 C0. 단 향후 유입 대비해 조건은 등급 하나가 아니라 위험등급으로 두는 편이 안전 |

### ⑦ 🆕 08-29 결정 — 무엇이 반영됐나

사용자 결정 원문: *"도메인 상에서 구매하면 안 되는 정도의 선은 쳐내주고 그 외는 넣되, 답변으로 너무 높은 수익률은 위험할 수 있으니 주의하세요 같은 문구도 넣어주면 돼."*

| 결정 | 반영 | 권고안(⑤)과의 차이 |
| :-- | :-- | :-- |
| "사면 안 되는 선" = **위험등급 11(매우높은위험) OR C0** | 추천·랭킹 질의 기본 제외 + 제외 사실·이유 명시. 사모 제외(`특수구조제외`)는 그대로 병행 | 없음 — ②③④ 그대로 |
| "그 외는 넣는다" | 위험등급 12(높은위험)·13(다소높은위험)·BBB 급은 **제외하지 않는다** | 없음 — ⑤ 도 11 만 뺐음 |
| "너무 높은 수익률엔 주의 문구" | 🆕 답변에 실린 종목 중 **`applied_yield > 6%` 또는 위험등급 12·13** 이 있으면 *"수익률이 높은 채권은 원금 손실 위험도 높을 수 있습니다. 신용등급·위험등급을 함께 확인하세요."* — 조회 답변에도 동일 | **신설.** 6% 는 에이전트가 고른 값(아래) — 조정 가능 |
| 조회·사실확인 | 제외하지 않고 등급 병기 | 없음 — ① 그대로 |

**6% 를 고른 근거** (KRW · `applied_yield > 0` · 위험11∪C0∪사모 제외 후 = 추천 모수 **16,359행**):

| 지점 | 값 |
| :-- | --: |
| 중앙값 / p90 / p95 | 4.12% / 4.72% / 4.94% |
| p99 / p99.9 | 5.51% / 6.38% |
| > 5% | 707행 |
| **> 6%** | **34행 = 34종목** (BBB~A 급·위험등급 2~4 가 30행, AAA 4행 — SC은행 콜 2·토지신탁 등) |
| > 7% / > 8% / > 10% | 9 / 4 / **0** |
| 최고 | 9.82% 중진공2024제2차스케일업유동화전문2-3 (BBB- · 높은위험 2등급 · 공모 · 평가가 9,897원) |

5% 로 두면 707행(4.3%)에 문구가 붙어 "주의" 가 상투어가 되고, 7% 로 두면 9행뿐이라 위험등급 2·3등급 상위(8~9%대)만 남는다. 6% 는 **상위 0.2%** 이면서 위험등급 2·3등급 상위와 AAA 특이 케이스(콜옵션부)를 모두 덮는다.

**제외 규모 재실측 (08-29, ②표와 기준이 다름 — ②는 전체 행, 여기는 KRW·수익률>0)**

| 단계 | 행 |
| :-- | --: |
| KRW · `applied_yield > 0` | 21,680 |
| − 위험등급 11 OR C0 | −1,244 |
| − 사모 (`특수구조제외`) | −979 |
| = 추천 모수 | **16,359** |

**누수 재확인**: `C0 AND pd_risk_gcd <> '11'` = **0건** · 96행 중 이름에 `후순위` 0건 · `(후`/`/후` 5건 → `special_structure_flags` 로는 91행이 안 잡힘 = 신설 규칙이 필요한 이유.

재현 SQL:
```sql
-- 추천 모수 · 최고 수익률
SELECT COUNT(*), MAX(applied_yield) FROM domestic_bonds
 WHERE curr_cd='KRW' AND applied_yield IS NOT NULL AND applied_yield<>0
   AND NOT (pd_risk_gcd='11' OR crd_grd='C0' OR bd_ofr_tcd='사모');            -- 16359, 9.824
-- 주의 문구 트리거 규모
SELECT COUNT(*), COUNT(DISTINCT pd_no) FROM domestic_bonds
 WHERE curr_cd='KRW' AND applied_yield>6
   AND NOT (pd_risk_gcd='11' OR crd_grd='C0' OR bd_ofr_tcd='사모');            -- 34, 34
-- 96행이 구조 플래그에 안 잡히는지
SELECT SUM(pd_nm LIKE '%후순위%'), SUM(pd_nm LIKE '%(후%' OR pd_nm LIKE '%/후%'), COUNT(*)
  FROM domestic_bonds WHERE curr_cd='KRW' AND applied_yield>50;               -- 0, 5, 96
```

---

## 11. 🆕 P-3 `(사)` 표기 — **반영 대상** (08-27 사용자 승인)

> **승인 범위**: "공모/사모는 컬럼으로만 판정, 이름 추론 금지" **한 줄만**. `(사)` 의 의미 확정은 주최 문의(§5-4-ㄴ)로 남긴다.
> ⚠️ **아직 yaml 에 쓰지 않았다.** 17건 기입 승인을 받을 때 함께 반영한다.

### ① 무엇이 문제였나

기록에 "3,962행 중 **정확히 절반**이 공모" 라고 적어 놨는데, 이 "정확히 절반" 이 **성격이 다른 두 신호를 한 자루에 담아 세서 생긴 착시**였다. 쪼개면 이렇다.

| 이름에 든 글자 | 행 수 | 공모 | 사모 | 뜻 |
| :-- | --: | --: | --: | :-- |
| `(사)` | 1,984 | **1,981 (99.8%)** | 3 | **`(사)` 는 사모가 아니다** |
| `사모` | 1,981 | 0 | **1,981 (100%)** | `사모` 글자는 정확하다 |
| 두 집합 교집합 | **3** | | | 거의 완전히 별개 |

우연히 양쪽이 각각 1,981행이라 합치니 50:50 으로 보였다.

### ② 이름으로 판정하면 얼마나 틀리나 (전수)

```
이름으로 "사모" 라고 찍은 것 3,962건
   맞게 잡음 1,981  ·  잘못 잡음(공모를 사모로) 1,981  ·  놓침(사모인데 못 잡음) 26
   → 오탐률 50.0%
```

1차의 `is_private` **15,261건**이 2차 실측 **2,007건**으로 줄어든 원인이 이것이다(장외 정리 효과 + 이름 정규식 오탐).

### ③ `(사)` 는 뭘 가리키나 — **가설, 확증 없음**

| | `(사)` 가 붙은 1,984행 | `사모` 가 든 1,981행 |
| :-- | :-- | :-- |
| 채권종류 | MBS 985 · 일반특수법인채 596 · 유동화회사채 114 · 특수은행채 80 | Conduit회사채 1,006 · 일반회사채 731 · 유동화회사채 69 |
| 대분류 | **특수채 1,683 (85%)** · 회사채 301 | 회사채 |

특수채·MBS 에만 붙는 점으로 보아 유동화 상품의 **"사채(社債) 형태"** 표시일 가능성이 있으나 **추정이다.** 주최 확인 전에는 `(사)` 를 어떤 판정에도 쓰지 않는다.

### ④ yaml 조치 (승인 후 반영)

```
query_rules.공모사모판정 (신설)
  "공모/사모는 bd_ofr_tcd 로만 판정한다 (공모 19,875 · 사모 2,007).
   🔴 종목명으로 추론 금지 — 이름의 '(사)' 는 사모가 아니다(1,984행 중 1,981행이 공모).
   이름 정규식 판정은 오탐률 50.0%(오탐 1,981 · 누락 26).
   '(사)' 의 의미는 미확정 — 주최 문의 전까지 어떤 판정에도 사용하지 않는다."

columns.pd_nm.trap 에 추가
  "종목명 끝의 '(사)' 는 사모 표시가 아니다. 공모/사모는 bd_ofr_tcd 참조."
```

**런타임 영향** — `query_rules` 신설이므로 플래너 프롬프트에 들어간다. 다만 **금지 규칙**이라 기존 동작을 바꾸지 않고 잘못된 SQL 생성을 막는 쪽이다. 기입 후 `run_gold_check.py` 63/63 로 확인한다.

---

## 12. 🆕 08-28 최종 상태 — "지금 어디까지 왔고 무엇이 남았나"

### ① 사용자가 정할 것 — **0건**

| # | 사안 | 상태 |
| :-: | :-- | :-- |
| §5-1 | A-1-05 표면금리 — `curr_cd='KRW'` 기본 조건 | ✅ 확정 (08-27, (가)안) |
| §5-3 | C-4-01 `remaining_days=0` 49행 | ✅ **확정 (08-28) — 현행 유지 · 회의 불필요** |
| §5-4-ㄱ | `(사)` — `bd_ofr_tcd` 로만 공모/사모 판정 | ✅ 확정 (08-27) |
| §5-5 | A-2-01 대표행 — 병기안 + 조건 2가지 | ✅ 확정 (08-27) |

### ② 회의로 간 것 — **3건** (`meeting_agenda_2026-08-26.md` §4)

| 안건 | 내용 | 준비 상태 |
| :-- | :-- | :-- |
| §4-1 | P-2 초고수익률 728% — 추천·랭킹 제외 여부 | ✅ 전수 분석 + 권고안 완료 (§10) |
| §4-3 | `(사)` 표기의 뜻 — 주최 문의 여부 | 급하지 않음 (§11 · 오답은 §5-4-ㄱ 로 이미 차단) |
| §4-4 | 외부 데이터 기준일 7/11 vs 8/22 (P-12/P-13) | 규칙 원문 확인 완료 · 외부 보완의 선결 조건 |

`remaining_days` 는 08-28 에 안건에서 **뺐다**(§5-3 종결). `(사)` 는 §4-3(뜻 문의)만 회의로 갔고, **판정 규칙(§5-4-ㄱ)은 17건 기입에 포함**된다.

### ③ ~~남은 것 — 17건 전체 기입 승인 하나뿐~~ → **✅ 2026-08-28 기입 완료**

`"괜찮아 기입해"` 를 받으면 아래를 한 번에 진행한다. 이견 있는 항목만 `"B-3-05 는 ⚠️ 로, 이유는 ~"` 식으로 반려하면 된다.

```
1. ✅ A_정합성.md 7칸 · B_피처의미.md 9칸 · C_결측의미.md 1칸 = 17칸 기입
2. ✅ ontology/enums/domestic_bonds.yaml — 컬럼 10곳 + query_rules 4곳 + normalization 1곳
3. ✅ 검사 4종  중복키 0 · 빌드 오류 0(경고 8, 기존) · pytest 35 passed · gold 63/63
4. ✅ git diff 보고 → 커밋
```

**기입 중 잡은 것 2건 (기록)**

| 무엇 | 어떻게 |
| :-- | :-- |
| **yaml 중복 키를 내가 만들었다** | `columns.exg_close_price` 에 이미 `note` 가 있는데 새 `note` 를 또 넣어 앞엣것이 조용히 죽을 뻔했다. `check_yaml_dupkeys.py` 가 잡아 **기존 note 에 병합**. 작성법 §3 이 경고한 바로 그 함정이다 |
| **`unit: krw(액면금액)` 제안 철회** | §2 조치에 그렇게 적혀 있었으나 `krw(액면금액)` 은 **존재하지 않는 어휘**다 — B-3-03·04 에서 새 어휘 금지(B-4 리드 소관)라고 판단해 놓고 B-3-02 에서 어길 뻔했다. `unit: krw`(기존 어휘) + `note` 에 "값은 수량이 아니라 액면금액" 으로 분리 |

### ④ 답변 동작이 실제로 바뀌는 곳 — `query_rules` **4개**

| 규칙 | 무엇이 바뀌나 | 출처 |
| :-- | :-- | :-- |
| `외화채없음` | `curr_cd='KRW'` 를 전 질의 기본 조건으로 명문화 (기존 "KRW 뿐" 오기 정정) | §5-1 |
| `대표행` | 장내 우선 → **병기** (`row_grain.대표행` 과 두 곳) | §5-5 |
| `공모사모판정` | 신설 — 이름으로 사모 추론 금지 (금지 규칙이라 기존 동작은 안 바뀜) | §11 |
| `듀레이션정상` | ⓐ `ndy_dur` 까지 덮도록 확장 (B-3-08) · ⓑ `remaining_days > 0` 추가 (P-16). 조건식을 동치인 `dur != 0 AND remaining_days > 0` 으로 정리 | §2 · P-16 |

**🆕 `듀레이션정상` 변경의 부수 효과** — `remaining_days IS NULL` 5행도 함께 빠진다: BAC 외국채 1(`curr_cd='KRW'` 조건으로 이미 제외) + `mat_dt=0` 인데 `dur` 값이 있는 4행(SH보상 2 · 산금채 2 — 잔존일수 없이 산출돼 출처 불명, P-10). **의도된 제외**로 두고 yaml 규칙 본문에 명시했다.

### ⑥ 실제로 고친 곳 (2026-08-28)

| 파일 | 위치 |
| :-- | :-- |
| `A_정합성.md` | A-1-01~05 · A-2-01 · A-4-02 (7칸) |
| `B_피처의미.md` | B-3-01~09 (9칸) |
| `C_결측의미.md` | C-4-01 (1칸) |
| `domestic_bonds.yaml` 컬럼 | `srfc_irt`(결측사유) · `pd_nm`(trap) · `exg_close_price`(note) · `avg_annual_tax_yield`(unit·note) · `buyable_quantity`(unit·note) · `cov`(note) · `ndy_dur`(unit) · `ndy_cov`(missing_semantics·note) · `ndy_eval_price`·`ndy_applied_yield`·`ndy_dirty`(unit) · `remaining_days`(note) |
| 〃 `row_grain` | `대표행` → 병기 규칙 |
| 〃 `normalization` | `zero_as_missing` 주석에 미등재 0-보유 컬럼 건수 + gold 미검증 한계 |
| 〃 `query_rules` | `대표행` · `듀레이션정상` · `외화채없음` · `공모사모판정`(신설) |

나머지 yaml 수정은 `unit`·`note`·`trap`·`missing_semantics` 로 **기록성**이며 런타임 동작과 무관하다.
기입 후 **`run_gold_check.py` 63/63 유지**를 반드시 확인하고, 깨지면 즉시 보고·되돌린다.

### ⑤ 이 검토에서 에이전트가 스스로 정정한 것 — 총 **5건**

| 회차 | 정정 |
| :-- | :-- |
| 2차 (§8-e·f) | "외부 데이터를 안 쓴다" 단언 · `장내행 우선 필수` 제안 |
| 3차 (§8-g·h) | "C0 만 빼면 충분" · `(사)` "정확히 절반" 착시 |
| **4차 (§8-n)** | **"yaml 410줄이 부정확하다" 지적 — 기준일을 안 맞춰 생긴 착시. 철회** |

> 세 번 다 **사용자가 "다시 확인해봐" · "확실해?" 라고 되물었을 때** 드러났다. 순방향으로 보면 언제나 깔끔해 보인다.
