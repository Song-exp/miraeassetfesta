# 채권 query_rules 원문(압축 전) — 2026-08-30 밤 I 작업

> `ontology/enums/domestic_bonds.yaml` 의 `query_rules` 26개를 SQL 생성기용으로 압축하면서(9,105자 → 아래 표 참조) 뺀 **이력·근거·검증 수치**를 여기 보존한다. 규칙의 조건식은 압축본과 동일하다(스크립트가 CASE 식 동일성을 검증). 근거 SQL 은 `채권_검토기록_2026-08-27.md` · `채권_전수조사_2026-08-30.md`.

## 대표행 (319자)

종목 수를 세는 집계는 GROUP BY pd_no — 1,078종목이 2~4행 (row_grain). COUNT(*) 를 종목 수로 쓰지 말 것. 🔴 2026-08-27 정정 — 속성 답변에서 '장내행 우선' 으로 한 줄을 고르지 말 것. eval_price 가 다른 8종목은 장외행의 종류·등급·발행사·위험등급·듀레이션이 NULL 이라, 어느 줄을 집느냐로 'AA-' 와 '정보 없음' 이 갈린다. 두 줄을 나란히 병기한다 — ① 값이 같으면 한 번만 답한다 ② 장외 가격에는 '액면가 수준' 단서를 붙인다. 결측을 옆 줄 값으로 채우는 것은 데이터가 아니라 추측이다.

## 판매행 (95자)

판매 조건(매수수익률·매매단가·세후수익률) 질의는 buy_yield IS NOT NULL (634행, 전부 장외 LOT) — LOT 2개 종목 307은 조건 N건으로 병기

## 구매가능 (160자)

🔴 주최 공지(8/24): buyable_quantity 무효 — 사용 금지. '살 수 있나' = mat_dt >= 20260822 (21,828행) 면 전부 구매가능. 판매 조건(수익률·단가)이 수록된 것은 그중 634행 — 조건 없는 종목은 '구매 가능하나 당사 판매 조건은 미수록'

## 문자열비교 (54자)

범주형·이름 컬럼은 항상 TRIM 후 비교 (bd_knd·pd_pbcm·영문명 고정폭 패딩 잔존)

## 위험등급방향 (483자)

🆕 2026-08-30 — pd_risk_gcd 는 문자열 '11'~'16' + '00'. 뒷자리가 등급이며 🔴 숫자가 클수록 안전하다: 11=매우높은위험(1등급) 1,441행 · 12=높은위험 74 · 13=다소높은위험 146 · 14=보통위험 1,424 · 15=낮은위험 9,849 · 16=매우낮은위험(6등급) 8,929 · 00=해당없음 19. '위험 낮은·안전한' = pd_risk_gcd='16' (넓게 IN ('15','16')) · '위험 높은' = '11' (넓게 IN ('11','12','13')). '1등급' 은 가장 위험한 등급이지 최우량이 아니다. 답변엔 pd_risk_nm 문구('매우낮은위험(6등급)')를 그대로 인용한다. 신용등급(crd_grd)과는 별개 축 — '등급' 만 있으면 어느 쪽인지 밝힌다. 근거: 국공채 2,839/2,840 = '16' · 위험 11 평균 수익률 16.6% vs 16 3.97% (전수조사 §2-D).

## 등급서열 (610자)

🆕 2026-08-30 — crd_grd 서열(높→낮): AAA > AA+ > AA0 > AA- > A+ > A0 > A- > BBB+ > BBB0 > BBB- > BB0 > BB- > B+ > B- > C0 (2차 15종, 공백 변종 0). 'AA- 이상' = TRIM(crd_grd) IN ('AAA','AA+','AA0','AA-') · 'A 이상' = 앞 7종 · '투자등급(BBB- 이상)' = 앞 10종 · '투기등급' = BB0 이하 5종. 사용자 표기 AA·A·BBB·BB·B 는 DB 의 AA0·A0·BBB0·BB0·B0 (등급정규화). 표준 등급 중 BB+ · B0 · CCC · CC · D 는 2차 데이터에 0건 — '존재하지 않는 등급' 이 아니라 '해당 채권 없음' 으로 답한다. 무등급 4,020행(국공채 2,840 전부 + 특수채 254 + 회사채 926)은 등급 조건 질의의 모수에서 빠진다 — 국공채는 '신용등급이 부여되지 않음' 으로 답하고, 결측을 '낮은 등급' 으로 취급하지 않는다. 근거: value_semantics 15종 count 전수 일치(전수조사 §1-A) · data/external/lookups/credit_grade_scale.csv.

## 등급정규화 (54자)

crd_grd 조회 시 'AA'→'AA0' 등 접미사 변환 후 비교. 국공채는 등급 없음 = 정상

## 영값배제 (127자)

수치 정렬·필터 기본 IS NOT NULL AND <> 0 (normalization.zero_as_missing). 예외 컬럼은 zero_is_value. 0 인 행은 '해당 값이 수록되어 있지 않습니다'로 응답 (주최 공지)

## 수익률정상 (49자)

applied_yield > 0 — =0 201행은 주식연계채 등 YTM 산출 대상 아님

## 듀레이션정상 (511자)

dur IS NOT NULL AND dur != 99 AND dur != 0 AND remaining_days > 0. 🆕 2026-08-28 — ndy_dur 에도 같은 조건을 적용한다(익일 질의). 센티넬 99 는 양쪽 같은 3행. 🆕 remaining_days > 0 추가 이유 — 만기가 지났는데 dur 이 0 이 아닌 행이 1건 있어(아우딘퓨쳐스3CB, KR6227611D67, 만기 2026-06-28 · dur 0.4319) 종전 조건을 그대로 통과했고, '금리위험 낮은 채권' 정렬에 만기 경과 채권이 남았다. 조건식은 종전 NOT(dur=0 AND remaining_days>0) 와 동치이면서 더 읽기 쉬운 형태로 정리했다. 부수 효과 — remaining_days IS NULL 5행도 함께 빠진다: BAC 외국채 1(curr_cd 조건으로 이미 제외) + mat_dt=0 인데 dur 값이 있는 4행(SH보상 2 · 산금채 2, 잔존일수 없이 산출돼 출처 불명 — P-10). 의도된 제외다.

## 익일값 (66자)

ndy_* 는 '익일' 질의에만. 일반 질의는 당일 컬럼 (1차 '사용 금지' → 2차 위장결측 15~20행으로 완화)

## 과세수익률금지 (33자)

avg_annual_tax_yield 사용 금지 (전량 0)

## 더티금지 (69자)

dirty·ndy_dirty 사용 금지 — 99.0%가 eval_price 동일값(정보량 0). '실제 결제금액' 답변 금지

## 등급일사용금지 (63자)

crd_grd_dt 로 등급 최신성·신선도 판단 금지 — 스키마: '등급 미변경 시 과거 일자로 유지될 수 있음'

## 이자유형분리 (101자)

표면금리 정렬은 bd_intp_tcd·bd_inrt_tcd 로 유형 분리 — 할인채(발행 할인율)·변동금리(스냅샷)·srfc_irt=0(구조) 을 고정 이표채와 한 축에 놓지 말 것

## 장내종가 (90자)

exg_close_price/yield 는 장내 1,270행만 유효 — 기준일(exg_close_price_base_dt) 필수 병기. 장외는 NULL(해당없음)

## 구조표시 (1858자)

🆕 2026-08-30 결정 — 추천·랭킹에서 특수구조(콜·풋·후순위·전환·영구·코코·분리채권·물가채)를 제외하지 않는다. 도메인상 살 수 없는 것만 뺀다 = 고위험제외 규칙(위험등급 1등급 · C0 · 사모). 나머지는 수익률 순서 그대로 답한다. 근거(KRW·구매가능 실측 2026-08-30): 주식연계 CB·EB·BW·전환 557행 · 풋 528 · 신종영구 266 은 사모·위험등급1·C0 로 이미 거의 전부 빠지고(남는 것 0·5·12), 남는 후순위 87·코코 63 은 전부 위험등급 2·3등급(주의 문구 대상), 콜 1,992 중 1,944 는 5·6등급(은행채·MBS 의 콜), 분리채권 209 는 전부 국고채 6등급 — 종전 특수구조제외 는 안전한 국고채 215행(분리 209 · 물가 6)을 빼고 있었다. 🔴 대신 구조를 답변에 표시한다. 채권 목록을 SELECT 할 때 아래 식을 '구조' 열로 그대로 붙인다(정규식 없이 LIKE 만 사용 · 우선순위 순): CASE WHEN TRIM(bd_knd) IN ('특수은행채','일반은행채','금융지주회사채') AND pd_risk_gcd IN ('11','12','13') THEN '은행 자본성증권(후순위·조건부자본·영구)' WHEN pd_nm LIKE '%신종%' OR pd_nm LIKE '%영구%' THEN '영구채' WHEN pd_nm LIKE '%(후)%' OR pd_nm LIKE '%(후/%' OR pd_nm LIKE '%/후)%' OR pd_nm LIKE '%/후/%' THEN '후순위' WHEN pd_nm GLOB '*[0-9]CB*' OR pd_nm LIKE '%(전환%' OR pd_nm LIKE '%/전환%' THEN '전환사채' WHEN pd_nm GLOB '*[0-9]EB*' THEN '교환사채' WHEN pd_nm GLOB '*[0-9]BW*' THEN '신주인수권부' WHEN pd_nm LIKE '%분리채권%' THEN '국고채 STRIPS' WHEN pd_nm LIKE '%물가%' THEN '물가연동' WHEN pd_nm LIKE '%콜%' AND pd_nm NOT LIKE '%콜마%' THEN '콜옵션부' WHEN pd_nm LIKE '%(풋)%' OR pd_nm LIKE '%(풋/%' OR pd_nm LIKE '%/풋)%' OR pd_nm LIKE '%/풋/%' THEN '풋옵션부' ELSE '' END AS 구조 실측 분포(21,882행): 없음 18,303 · 콜옵션부 2,260 · 전환사채 382 · 은행 자본성증권 278 · 국고채 STRIPS 209 · 영구채 158 · 교환사채 134 · 후순위 109 · 신주인수권부 31 · 풋옵션부 12 · 물가연동 6. 첫 줄(은행 자본성증권)은 단어가 아니라 컬럼 두 개(발행 종류 + 위험등급)로 판정한다 — 은행·금융지주가 발행한 채권이 위험등급 1~3등급이면 전부 후순위·조건부자본(원금 상각 조건)·영구 구조다(278행 중 후순위 표기 273 · 코코 표기 264, 은행 4~6등급에 그런 이름 0건). 이름의 코코/조건상각/(상) 은 판정에 쓰지 않는다. 콜은 '콜' 앞뒤에 한글이 없을 때만 옵션이다 — 한국콜마 5건 제외, '(신종-영구-10콜)'·'변/콜' 포함(경계식 2,678 = LIKE 식과 동일). 답변에서는 이 열의 값을 종목 옆에 한 단어로 붙이고, 영구채는 '만기일 = 콜 개시일' · 은행 자본성증권은 '원금 상각·이자 미지급 조건 가능' 을 덧붙인다. ESG 라벨 — (녹)=녹색채권 · (사)=사회적채권 · (지)=지속가능채권 (name_encoding.esg_labels, LIKE 4형 조건식은 거기에. 주최 확인 대기 — '종목명 표기 기준' 병기). 조회·사실확인 질의는 종전과 같이 아무것도 빼지 않는다.

## 고위험제외 (1147자)

🆕 2026-08-29 결정 · 🔴 2026-08-30 조건식 정정 — 추천·랭킹 질의는 WHERE 에 pd_risk_gcd <> '11' AND COALESCE(TRIM(crd_grd), '') <> 'C0' AND bd_ofr_tcd <> '사모' 를 넣는다. ❌ NOT (pd_risk_gcd='11' OR crd_grd='C0') 로 쓰면 안 된다 — crd_grd 가 NULL 인 4,020행(국공채 2,840 전부)이 NULL 로 떨어져 '안전한 채권 추천' 이 0건이 된다 (08-30 전수조사에서 발견 · 08-29 기재 '추천 모수 16,359' 는 이 함정이 섞인 수치라 폐기). 이유: applied_yield > 50% 인 96행(최고 728.5%, 신보·중진공·기보 유동화 후순위)이 전부 C0·위험등급 11 인데 공모라 사모 제외에 안 걸린다. 평가가가 액면의 27~45% — 수익률이 좋은 것이 아니라 원금 손실 위험이 가격에 반영된 것. 사모는 일반 투자자가 살 수 없어 추천에서 뺀다. 제외했다는 사실과 이유를 답변에 밝힌다: '위험등급이 매우 높은(1등급) 채권과 사모 채권은 제외했습니다.' 🔴 조회·사실확인 질의에서는 제외하지 않는다 — 728.5% 는 사실이다. 신용등급·위험등급을 함께 답한다. 🔴 주의 문구 — 답변에 실린 종목 중 applied_yield > 6% 또는 pd_risk_gcd IN ('12','13') 이 있으면 '수익률이 높은 채권은 원금을 돌려받지 못할 위험도 높을 수 있습니다. 신용등급·위험등급을 함께 확인하세요.' 를 붙인다(조회 답변도 동일). 실측(KRW · mat_dt>=20260822 · applied_yield>0 · 위험11∪C0∪사모 제외): 추천 모수 19,411행(18,074종목) · 최고 9.82% (중진공 스케일업유동화 2-3 · BBB- · 2등급 · 콜/중순위) · 6% 초과 34종목 · 10% 초과 0 · 국공채 최고 4.89%. 왜 신용등급(C0)이 아니라 위험등급인가 — 신용등급 결측 4,020 vs 위험등급 결측 19 · C0 103행은 전부 위험등급 11. ❌ 가격 기준 필터 금지(AAA 320행 오폭) · ❌ 유동화 전체 제외 금지(AAA 3,198행). 근거·SQL: 채권_검토기록 §10 · 채권_전수조사_2026-08-30 §2-A.

## 신용보강 (1629자)

🆕 2026-08-30 — '정부가 보증하는/안전한 공공 채권' 류 질의는 아래 식을 '보강' 열로 붙여 층을 함께 답한다. 컬럼 3개(대분류·발행사·종류)만 쓴다 — 단어 필터 없음. CASE WHEN TRIM(std_pd_mcls_nm)='국공채' OR COALESCE(TRIM(pd_pbcm),'')='한국은행' THEN 'A 정부·지자체·한은 직접 발행' WHEN pd_nm LIKE '%(정부보증)%' THEN 'B 정부보증 명시' WHEN TRIM(pd_pbcm) IN ('한국주택금융공사','한국토지주택공사','한국산업은행','(주)중소기업은행') THEN 'C 법정 손실보전 기관' WHEN TRIM(std_pd_mcls_nm)='특수채' THEN 'D 특별법 공공기관(정부 보증 표기 없음)' WHEN COALESCE(TRIM(bd_knd),'') IN ('MBS','유동화회사채','Conduit회사채') OR COALESCE(pd_pbcm,'') LIKE '%유동화%' OR COALESCE(pd_pbcm,'') LIKE '%신용보증%' OR COALESCE(pd_pbcm,'') LIKE '%기술보증%' OR pd_nm LIKE '%유동화%' THEN 'E 유동화(기초자산 담보)' ELSE 'F 민간 회사채' END AS 보강 실측(21,882행 전부, 누락 0): A 2,873(1,808종목) · B 7 · C 2,968(2,950) · D 3,171(3,112) · E 2,648 · F 10,215(9,972). 근거: A = 대분류 국공채 2,840 + 한국은행 통안채 33(데이터가 특수채·중앙은행채로 분류) — 정부 성격 채권종류 9종 전부 A 확인, A 밖에 정부·지자체 발행사 0. C = data/external/lookups/bond_issuer_background.md 에 법 조문이 확인된 4곳(주금공법 §34·§51 · LH법 §11② · 산은법 §32 · 기은법 §43). 주금공 MBS 1,396 은 E 가 아니라 C(지급보증+손실보전). D = 특수채 57개 기관 — 법정 손실보전 조항 미확인(상위 15곳이 77%: 한전 403 · 도로공사 338 · 수출입은행 221 · 장학재단 218 · 중진공 197 …). 확인되는 기관은 C 목록에 추가한다. 🔴 답변 규약 — ① D 에 '정부 보증' 이라고 말하지 않는다('특별법 공공기관 발행, 정부 보증 표기 없음'). ② 구조 열(구조표시 규칙)이 은행 자본성증권·후순위·영구채면 A·B·C 라도 '손실보전 대상 아님' 을 병기한다 — C 층 위험등급 1~3 은 41행이고 전부 이 조건에 걸린다(누락 0 · 4~6등급 오표시 0). ③ 발행사 칸이 빈 행(149)은 층은 대분류로 정해지지만 (A 20 · D 4 · E 95 · F 30) '발행사 미수록' 을 병기한다. F 30 중 6(NH농협캐피탈 269-1~6 장외 행)은 같은 pd_no 의 장내 행에 발행사가 있으므로 종목 단위 답변에선 그 값을 쓴다, 나머지 24 는 사모 23 + 외국채 1(XS, KRW 규칙으로 제외). ④ E 는 정부와 무관 — 담보는 기초자산이며 부실 tranche(C0·1등급 354행)는 고위험제외가 처리. 근거·검증 SQL: docs/review_2026-08-26/채권_전수조사_2026-08-30.md §8.

## 시장집계금지 (65자)

pd_exg_mkt 단독 group-by 금지 — 구성 효과 교란 (§9.5). 같은 종목이 양 시장에 있을 수 있음

## 장외등급해석 (62자)

장외 등급 결측 48.9%(2,023/4,136)는 미평가 — 저품질·고위험으로 해석 금지 (장내는 11.3%)

## 유동화위험금지 (391자)

유동화 = TRIM(bd_knd) IN ('MBS','유동화회사채','Conduit회사채') 3,949 + 코드 밖 SPC 96(발행사명 유동화/신용보증/기술보증 SPC) = 4,045행 (🔴 2026-08-26 정정 — Conduit회사채 1,025행을 bd_knd 코드로 편입. 이전 3,036행은 Conduit 중 16행만 발행사명 패턴으로 회수해 1,009행이 누락돼 있었다). 발행사명으로 위험 판단 금지 — 발행자 = SPC. 조건식: TRIM(bd_knd) IN ('MBS','유동화회사채','Conduit회사채') OR pd_pbcm LIKE '%유동화%' OR pd_pbcm LIKE '%신용보증%' OR pd_pbcm LIKE '%기술보증%' OR pd_nm LIKE '%유동화%'

## 외화채없음 (231자)

🔴 2026-08-27 정정 — 'KRW 뿐' 은 오기다. curr_cd='000' 인 외국발행채가 1행 있다 (XS3067881758 뱅크오브아메리카, 표면금리·만기·발행일 미수록). 모든 채권 질의에 curr_cd='KRW' 를 기본 조건으로 건다 — 원화채 21,881행이 답변 모수다. 그 1행은 정보가 미비해 답변에 사용할 수 없다. '외화채권' 질의는 여전히 '수록 없음' (해외ETF 로 유도 금지).

## 기준일 (60자)

답변의 데이터 기준일 = info_base_dt 20260821 (영업일) / 대회 기준 2026-08-22

## 발행사조회 (455자)

🆕 2026-08-30 (E) — 발행사 질의는 KG 개체 매핑에 pd_pbcm 값이 있으면 TRIM(pd_pbcm) 정확 일치, 없으면 pd_pbcm LIKE '%이름%'. pd_pbcm 은 뒤에 공백이 붙은 값이 있어(예 '한국전력공사(주)   ') 정확 일치엔 TRIM 이 필요하고, '(주)' 위치가 제각각이라 짧은 이름은 LIKE 가 맞다(한국전력 → '한국전력공사(주)' 403행 · 산업은행 → '한국산업은행' 504행 · 기업은행 → '(주)중소기업은행' 405행). 약칭(한전·LH·산은·기은·주금공)은 동의어 표를 쓴다. MBS·유동화는 발행자가 SPC 라 원회사 이름으로 검색되지 않는다(유동화위험금지 참조). 발행사 칸이 빈 149행은 '발행사 미수록' 으로 답한다(신용보강 ③). 🔴 다른 상품군(ETF 구성종목·펀드)의 같은 이름 값을 끌어오지 않는다 — 상품군은 이미 채권으로 정해졌다.

## 공모사모판정 (323자)

공모/사모는 bd_ofr_tcd 로만 판정한다 (공모 19,875 · 사모 2,007). 🔴 종목명으로 추론 금지 — 이름의 '(사)' 는 사모가 아니다. '(사)' 가 붙은 1,984행 중 1,981행(99.8%)이 공모이고, '(사)' 는 특수채·MBS 에 붙는다(특수채 1,683 = 85%). 이름 정규식 판정은 오탐률 50.0%(오탐 1,981 · 누락 26). ✅ 2026-08-30 — '(사)' = 사회적채권 ESG 라벨(영문 (S), 녹(G)·지(T) 와 짝, name_encoding.esg_labels). 공모/사모 판정엔 쓰지 않는다. 주최 확인 요청 중.
