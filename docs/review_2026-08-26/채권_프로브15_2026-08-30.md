# 채권 프로브 15문항 — HCX 실측 1라운드 (2026-08-30)

> 실행: `python -m src.chat -f eval/probe_bonds_2026-08-30.txt` → 답변·SQL·think_trace 가 `logs/chat-YYYYMMDD.jsonl` 에 남는다.
> 보는 법: 문항마다 **[Route] → [Ground] → [Gate] → [Plan] SQL → [Execute] 행수 → [Answer]** 순으로 "기대 경로" 와 대조. 틀린 문항은 `eval/questions_domestic_bonds.jsonl` 로 승격.
> 기존 eval 24문항(BND-*)과 겹치지 않게 골랐다. 정답 수치는 2026-08-30 DB 실측(`scripts/audit_bonds_rules.py` 와 같은 DB).

## 심사위원 스타일 (복합 조건·자연어) — 1~7

| # | 질문 | 노린 약점 | 기대 경로 | think_trace 확인 포인트 | 정답 근거 (DB 실측) |
| :-: | :-- | :-- | :-- | :-- | :-- |
| 1 | 위험이 낮으면서 수익률이 7% 이상인 채권 추천해줘 | **08-29 회의 오류 재현.** '위험' 다의어(부도/금리) + 추천 → 고위험제외 + 주의 문구 | 되묻기 **안 함**(위험등급 축으로 해석) → `pd_risk_gcd IN ('15','16')` + 고위험제외 + `applied_yield>=7` | 되묻나? 1등급(`'11'`)을 "낮은 위험"으로 읽나? 6% 초과 주의 문구 붙나? 등급 라벨을 답에 밝히나? | **2행** (SC은행 단기채 AAA 7.12·7.10%, 5등급). 1라운드 실측: HCX 가 `'16'` 단독으로 짜서 0행 → 규칙을 협회 등급 명칭·적합성 매핑 근거로 `IN ('15','16')` 기본으로 정정(3552356 이후). `'16'` 만이면 0행이므로 5등급 포함 사실을 밝혀야 함 |
| 2 | 만기까지 들고 갈 건데, 3년 안에 만기되는 안전한 채권 몇 개만 골라줘 | **되묻기 남용.** clarify `보유기간`·`안전의_정의` 에 걸리지만 단서("만기까지"=신용위험만)가 있음 | 되묻기 없이 SQL. `remaining_days BETWEEN 1 AND 1095` + 위험 15/16 + 고위험제외 | 단서가 있는데 CLARIFY 로 빠지면 **남용** | 12,657행 / 12,008종목 → LIMIT 로 몇 개, 제외 사실 명시 |
| 3 | 정부가 책임지는 채권 중에서 수익률 높은 순으로 5개 알려줘 | **구조+보강 CASE 받아쓰기** (SQL 1,700자대 — max_tokens 1536 검증) · D 층을 '보증' 이라 말하기 금지 | `신용보강` CASE 를 `보강` 열로, A·B·C 층 필터 + 고위험제외 + ORDER BY yield | SQL 이 잘리지 않았나? 층 문구? 은행 자본성증권이면 '손실보전 아님' | 1위 토지주택채권 330(변) 5.86% · 2~5위 주금공 MBS(사)(콜) 5.50~5.65% — (사)를 사모로 읽으면 감점 |
| 4 | 2028년 12월까지 만기가 돌아오는 국고채 알려줘 | **미래 시점 사후검사** — 2028 이 `mat_dt` 조건에 쓰여야 통과 | Gate 통과(`future_tokens=['2028']`) → SQL `mat_dt BETWEEN 20260822 AND 20281231` → 사후검사 OK | `[Guard]` 가 "시점·전망" 으로 오판해 기각하면 실패 | 국고채권 **44종목** |
| 5 | 내년에 만기되는 회사채 중 신용등급 AA 이상인 것 알려줘 | 상대 시점 '내년'→2027 · 'AA 이상' = AAA·AA+·AA0 (3종, AA- 제외) · '회사채' = 대분류(동의어는 bd_knd `일반회사채` — **함정**) | `mat_dt 2027*` + `TRIM(crd_grd) IN ('AAA','AA+','AA0')` + `std_pd_mcls_nm='회사채'` | 'AA 이상' 에 AA- 를 넣나? 회사채를 `bd_knd='일반회사채'` 로 좁히나? | 대분류 회사채 기준 **2,077종목** |
| 6 | 신용등급 AA인 채권 중 잔존만기 1년 이내인 것 알려줘 | **등급정규화** 'AA' → `'AA0'` (DB 에 'AA' 단독 0건) | `TRIM(crd_grd)='AA0' AND remaining_days BETWEEN 1 AND 365` | `crd_grd='AA'` 로 쓰면 0행 → "없다" 오답 | **356종목** / 361행 |
| 7 | A등급 이상 회사채 중 표면금리가 5% 넘는 것 알려줘 | 등급서열 7종 · '표면금리' 동의어(→표면이자율 `srfc_irt`) · 이자유형분리 | `IN (AAA…A-)` + `srfc_irt>5` + 대분류 회사채 | 할인채·변동금리를 같은 축에 놓나? | 대분류 회사채 **613행**(bd_knd 일반회사채로 좁히면 66) |

## 약한 쪽 — 8~15

| # | 질문 | 노린 약점 | 기대 경로 | 확인 포인트 | 정답 근거 |
| :-: | :-- | :-- | :-- | :-- | :-- |
| 8 | 위험등급 1등급인 채권 알려줘 | **위험등급 방향** — 1등급 = 최위험. 조회 질의라 고위험제외 **안 함** | `pd_risk_gcd='11'`, 제외 없음, `pd_risk_nm` 인용 + 주의 문구 | "1등급=최우량" 으로 말하면 오답. 추천이 아닌데 제외하면 오답 | **1,441행 / 1,395종목**, 등급은 AAA~C0 섞여 있음 |
| 9 | 한전 채권 중 만기가 가장 긴 건 뭐야? | **약칭 동의어 + 발행사 LIKE** (한전 → `pd_pbcm LIKE '%한국전력공사%'`) · E(주식 노드 오매핑) 재발 여부 | Ground 가 ETF 구성종목 노드를 안 잡아야 함 → `ORDER BY mat_dt DESC` | `[Ground]` 에 ext_* 가 뜨면 E 회귀 | 한국전력공사채권1184 · 만기 **2052-04-21** |
| 10 | 표면금리가 0%인 채권은 어떤 거야? | **0값 = 의도된 값**(영값배제 예외) — 조회 질의는 0 을 빼면 안 됨 · "구조상 값" 설명 | `srfc_irt=0` 조회, 구조 열로 CB·EB 표시 | 영값배제 규칙을 잘못 적용해 0행이 되나? 이유를 지어내나(스키마 근거 없음) | **579행**: CB 323 · EB 135 · 변동금리 3 등 |
| 11 | 은행채는 몇 종목이나 있어? | **대표행 중복** — COUNT(DISTINCT pd_no) · 동의어 은행채→`일반은행채` · ⚠️ 오프라인 확인: **라우팅 미특정** — '은행채**는**' 처럼 조사가 붙으면 경계 검사(앞뒤 한글 없음)에 안 걸려 4테이블(25k자 안내판)로 간다. HCX 가 FROM 을 맞추는지 본다 | `COUNT(DISTINCT pd_no) WHERE TRIM(bd_knd)='일반은행채'` | 707(행) 이라 답하면 오답 · `[Route] 미특정` 이면 조사 처리(router) 후보 | **695종목** (707행) |
| 12 | 지금 파는 채권 중에 국고채는 몇 종목이야? | **답변모수** (판매행 634 vs 시장 전체) + 판매행은 등급 NULL(국공채 미부여) | `buy_yield IS NOT NULL` + 국고채 + DISTINCT | 시장 전체로 세면 오답. 되묻기(답변모수)로 가도 유효하나 단서 "지금 파는" 이 있음 | 소분류 국고채 **35종목** (대분류 국공채로 넓히면 256종목/502행) |
| 13 | 신종자본증권 중 만기가 가장 짧은 것 알려줘 | **영구채 mat_dt = 콜 개시일** 병기 · 동의어 신종자본증권→'신종' · 은행 자본성증권 문구 | `pd_nm LIKE '%신종%'` + `mat_dt>=20260822 ORDER BY mat_dt` | "만기 2026-09-15" 라고만 하면 오답 — '콜 개시일' 이라 해야 함 | DGB금융지주 조건부자본증권(상) 5 · **2026-09-15**(잔존 25일) |
| 14 | 공모 사회적채권 중 만기 2년 이내인 것 알려줘 | **(사) ≠ 사모** — 공모/사모는 `bd_ofr_tcd`, (사)는 ESG 라벨 4형 LIKE · "표기 기준" 병기 | `bd_ofr_tcd='공모'` AND (사) 4형 + `remaining_days<=730` | (사)를 사모로 읽어 모순("공모인데 사모")이 되나? | **768종목** |
| 15 | 달러로 발행된 채권 알려줘 | **외화채없음** — 수록 없음으로 답하고 해외ETF 로 유도 금지 · '000' 1행(BAC 0 05/14/55) 사용 금지 | `curr_cd='USD'` 0행 → "수록 없음" | 해외 ETF 를 추천하거나 '000' 행을 보여 주면 오답 | USD **0행** (원화 21,881 · '000' 1) |

## 라운드 뒤에 할 일
1. 오답은 SQL 과 think_trace 로 **조건식 문제(yaml)** 인지 **매핑·라우팅 문제(코드)** 인지 가른다 — `result.sql`·`grounding` 이 로그에 있다.
2. 3번에서 SQL 길이·토큰(`chat-tokenize`)을 적어 두면 "이름 정보 → 파생 컬럼" 결정의 실측 근거가 된다.
3. 1·2·12 의 되묻기 여부를 보고 clarify 규칙의 "단서" 문구를 조정한다.
