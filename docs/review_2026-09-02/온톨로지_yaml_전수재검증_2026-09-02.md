# 온톨로지·yaml 전수 재검증 — 2026-09-02 (2차 DB 기준일 2026-08-22)

대상: 손으로 쓴 yaml 12개(도메인 4 + `_refusal` + 공유 7)·생성물 vocab 4개·ttl 5분할·`build_ontology.py` 하드코딩 문구·`PROJECT.md` §5 제약 서술.
판정은 전부 `data/financial_products.db` 실측(읽기 전용). 채권 yaml 은 05e1962(09-02 오전, ~150건) 이후 **전수(약 430건)** 로 다시 봤다.

## 1. 자동 검사 (기준선)

| 검사 | 결과 |
| :--- | :--- |
| `check_yaml_dupkeys.py` (손 yaml 12) | 문제 0 |
| `build_ontology.py --check` | 오류 0 · 경고 8 = 전부 `status: pending` 죽은 alias(표준등급표 BB+·B0·CCC·CC0·C, 1차 외화채 USD·JPY·EUR) — 의도된 보류 |
| KG coverage | pd_pbcm 1,818/1,818 · crd_grd 15/15 · 지수·지역·자산군 매핑 결손 0(센티넬·결측 코드만 미매핑) |
| `*.vocab.yaml` 38컬럼 | 값·건수 drift 0 |
| `audit_bonds/etf/funds_claims.py` | 127/55/58건 — 실질 불일치 0 (ETF 4·펀드 1 은 규칙 본문의 산문·SQL 별칭·"없는 컬럼" 부정 언급을 식별자로 오인한 스크립트 오탐) |

## 2. 수치 주장 전수 대조 (병렬 5축)

| 파일 | 검사 | 불일치 | 성격 | 처리 |
| :--- | :---: | :---: | :--- | :--- |
| `enums/domestic_bonds.yaml` | 360식(≈430수치) | 10 | 오기 6·기준차이 4 (pd_risk_gcd '00' 를 NULL 로 서술·1,069→1,070·STRIPS 188+21·고정+변동 열거에 특수은행채 4 누락·잔여 90→91·비율구간 604→587·338→296·71→79 등) | 10곳 정정 |
| `enums/domestic_etfs.yaml` | 356 | 32 | 오기 21·기준차이 10·1차잔재 1 (인버스 113→117·부호소실 88→90%·결측 분해 182+13 → pd_sale_yn 194+ETN 1·`(H)` 259→260·TR/PR 255/93→214/44·pd_sect_cd 1차 합계 1,733·axis count 390/63/70 등) | 36곳 정정 |
| `enums/overseas_etfs.yaml` | 231 | 25 | 오기·1차잔재 (신규 395/소멸 20=상폐 → v1 대조 신규 391·소멸 0(ISIN 변경 20)·AI 오탐 865/811→840/786·환율 1,504.92→1,384.23 교체 완료·VOO/SPY·KODEX200 1차 값·ETN 계열 추정 → 전부 ETF·pd_itm_no LIKE 'VOO.%' 0행 등) | 31곳 정정 |
| `enums/public_funds.yaml` | ≈435 | 11 | 오기 (M111 9,637→10,510(88%)·'폐쇄' 태그 C104 195행 실재·'기타' 170 은 사모 169·판매완료 결측률 컬럼별 78.5~99.1%·사모 1,979→1,993·구식 스냅샷은 판매중이 더 많음·715 등) | 14곳 정정 |
| `shared/region·index·organization` | 336 | 96 | alias evidence 1차 건수(펀드 모수 1/4 축소 등) 38건·헤더 1차 시점·index 구조변경 미반영·organization codebook_v1 접두점유 45건(1차 코드북) | region 20·index 22·organization 6+헤더 주석 |
| `shared/risk_grade·currency·asset_class·credit_grade` | 수동 전수 | 24 | 1차 시점 헤더·evidence(미분류 58→19·20054 오염값 소멸·변형 163/75→20/8·통화·자산군 건수 전부 1차) | 3파일 정정, credit_grade 일치 |
| `_refusal.yaml` | — | 0 | 수치 주장 없음 | — |

원칙: 1차 수치는 지우지 않고 `2차 N (1차 M)` 로 병기 — 이력 추적과 "왜 바뀌었나" 를 파일 안에서 읽을 수 있게. 규칙 조건식(SQL)은 전 파일 실행 가능·수치 재현 확인, 미존재 컬럼 참조 0.

## 3. ttl · 생성기 · 문서

- `build_ontology.py` 하드코딩 3곳: 할인채 172건(1차) → 689행 · dur "조기상환권부 98.0% 가 0"(1차) → 2차 0 표기 52행·콜 2,683행 중 4 · "6등급 채권 24.6%" → 40.8%(8,929행/7,803종목)·'00' 19행.
- `region.yaml absent_in` → `bond_kr.ttl` ABSENT 주석: PD_CTRY_CD 42,393/42,394 → 21,881/21,882(XS 1).
- `PROJECT.md` §5: ETN 532/59 → 545/65 · 6등급 10,408건(24.6%)·펀드 2,397·채권 58건 → 8,929/355/19 (규칙 파일과 정합).
- `common.ttl` 검증: ETF ⊥ ETN disjoint · inverseOf 3종 · riskGrade 0~6 + 테이블별 1~6/0~6 사영 — 정합. `etf_gl.ttl` 5,972건·67건 ✓.

## 4. 재생성·테스트

- `build_ontology.py` 전체 실행 → ttl 5분할·kg_node 41,580·kg_alias 66,592·kg_edge 7,414·kg_closure 9,965 재생성.
- pytest **315/315 통과**. 병합 직후 실패하던 9개(country_tag·org_label_slots·attr_tag·cambricon 등)는 **로컬 DB 의 KG 가 팀원 커밋 이전 빌드**였던 것이 원인 — 재빌드로 해소. ⇒ 배포 전 `build_ontology.py` 실행이 필수(deploy.sh 가 요구하는 순서).

## 5. 남긴 것 (보완 대상)

1. `organization.yaml` codebook_v1 alias 45건의 "접두점유·N종목" 은 1차 코드북 수치 — 헤더에 명시만 했다. 코드북(`public_funds_asset_manager.derived.csv`) 재생성 시 함께 갱신.
2. `audit_etf_claims.py`·`audit_funds_claims.py` 의 식별자 추출이 규칙 본문 산문·SQL 별칭을 컬럼으로 오인(5건 오탐) — 스크립트 보정 여지.
3. domestic_etfs.yaml 수익률 결측 분해(1d/1m/3m vs 6m/ytd)가 '판매종료' 를 두 기준(pd_sale_yn=0 / du_nav_yday NULL 182-집합)으로 섞어 계산했던 것 — 두 기준을 병기해 두었고, 한 기준으로 재산출은 ETF 담당 판단에 맡긴다.
4. index.yaml "해외ETF 지수" 절은 노드 0개(08-25 자동 노드로 이관) — 제목만 남겨 사유를 적었다. 삭제 여부는 담당 결정.
5. 검증 불가(외부 근거) 항목: 키움 챗봇 인용 커버드콜 6,646억(DB 6,369억), 서버 응답 실측 수치, 코드북 생성 시점 수치 — 문서 기록으로만.

## 6. 후속 — 구매가능 판정 기준일 8/24 분리 (리드 결정 2026-09-02 밤)

서버 실측: 재배포 전 구버전이 "한전 낮은 순" 에 1063호(만기 8/20) ×2 를 그대로 내놓았다(가드 부재 확인). 리드 지시 "만기 지난 건 다 제외, 기준일은 8/24".
- 실측: `info_base_dt` 전건 8/21(금) · `remaining_days` = mat_dt − 8/21 · mat_dt 8/22 7종목·8/23 7종목·8/24 20종목. as-of(8/22 토) 기준 하한이면 주말 만기 14종목이 '살 수 있는 채권' 에 남는다.
- 결정: **데이터 as-of(`DATA_CUTOFF` 8/22, 답변 표기)** 와 **구매가능 판정일(`BUYABLE_CUTOFF` 8/24, `mat_dt >= 20260824`)** 을 분리. 만기 하한 주입·경계 교정·랭킹 제외·만기 정렬 가드 전부 8/24 로, `ensure_cutoff_inclusive` 는 8/20~8/23 하한 리터럴(>·>=)을 8/24 이상으로 교정.
- 전파: `domestic_bonds.yaml` 구매가능(21,814행)·만기윈도우 규칙, 규칙 파일 §2, PROJECT.md 구매가능 행, gold 33문항 하한 교체(결과 변경 4: BND-D-011·013·032·OFFICIAL-001 — 032 '만기 가장 짧은' 은 8/22 7종목 → 8/24 20종목), 테스트 픽스처. pytest 317/317.
- ⚠ 주최 공지 기준일은 8/22 다. 답변 문구의 "기준일 2026-08-22" 는 유지했다 — 이것까지 8/24 로 바꿀지는 리드 확인 필요(상수 1개).

## 7. SQL 과정 재점검 (2026-09-02 밤, HCX 0회 재투입)

방법: ① 서버 프로브 원본 10개 파일의 최종 HCX SQL 115문항(질문 중복 제거)을 가짜 플래너로 새 가드 체인에 재투입 — 실행 오류·채권 목록 GROUP BY·만기 하한 8/24·결과 중복 종목·행수 0↔양수 변화·새 게이트 기각을 검사. ② 채권 gold 30문항의 gold SQL 을 플래너 출력으로 넣어 가드 후 행수 = gold_rows 확인. ③ `run_gold_check.py` 147문항.

| 검사 | 결과 |
| :--- | :--- |
| 프로브 115 재투입 | 실행 오류 0 · 중복 종목 0 · 8/24 미만 만기 하한 0 · 행수 회귀 0. 새 게이트 기각 2(KG-014 0등급 펀드·KG-027 좌수 ABSENT — 팀원 의도 변경) |
| 채권 gold 30 재투입 | **처음 1건 불일치 → 수리 후 0** |
| gold check 147 | 통과 147 · 실패 0 (BND-S-005/006 의 0행은 '없다' 가 정답인 설계) |

수리 2건:
1. **라우터 한글 경계가 조사를 거부** — '국고채는 총 몇 종목이야?' 가 `국고채`+`는` 경계에 걸려 미특정 → HCX FROM 판단 → 팀원의 새 "라우팅 범위 밖 테이블 기각"(1df414b) 이 정답 SQL 을 거절. `router._bound_in` 에 조사 허용(`_PARTICLE`, 뒤에 한글 불가) 추가. `전환사채(CB) 알려줘` 는 어휘 자체가 없어 미특정 — `domestic_bonds.yaml synonyms` 에 구조 용어 11개(전환사채·CB·교환사채·EB·신주인수권부사채·BW·스트립·STRIPS·유동화·콜옵션·풋옵션) 신설. 라우터 테스트 6케이스 추가.
2. **WHERE 의 윈도우 함수** — V5 '순자산이 가장 큰 자산운용사 5곳' 에 HCX 가 `COUNT(...) OVER(PARTITION BY …)` 를 WHERE 에 써 SQLite 실행 오류('조회 중 오류', 재생성 없음). `validate_sql` 이 실행 전에 기각해 재생성 1회를 주도록 함.

pytest 323/323.
