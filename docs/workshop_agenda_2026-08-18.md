# 🗳️ 워크샵 안건 통합 — 2026-08-18 기준

> 세 곳에 흩어진 미결 안건을 한 장으로 취합했다:
> ① 감사 문서 3건 (`docs/eda/*_audit_2026-08-18.md`) ② 각 enums yaml 의 workshop/pending 절
> ③ KG pending (`data/kg_coverage_report.md` 하단 — 빌드마다 자동 갱신).
> **안건마다 "결정이 무엇을 풀어주는지"를 적었다.** 우선순위는 배점 영향 순.

---

## A. 인프라 — 전 도메인 공통 (최우선)

### A-1. 🔴 정본 DB 확정 — 배포본(dtype=str)과 재빌드본(REAL)의 dtype 이 다르다

- 증거: 펀드 zrin 등급 — 구 빌드에선 `='6'` 0건(문자열), 현행 재빌드에선 2,397행(REAL).
  ETF `pd_dvid_cycl` "유효 183건" 주장도 현행 DB 에서 재현 불가(전 행 공백) — 같은 계열 의심.
- 결정할 것: ⓐ 정본 지정 ⓑ 8/30 서버 재기동 시 DB 재반입 여부 ⓒ build_db.py dtype 명세 고정
- 풀리는 것: 문자열 정확일치 코드의 이식성 · 감사 재현성 · 배포-로컬 불일치 리스크
- 출처: 펀드 감사 §4-안건1 · ETF 감사 §4-2

### A-2. 수익률정상 규칙(질의 컬럼 매개변수형)의 런타임 반영 방식

- 펀드·ETF 모두 "질의 대상 기간 컬럼에 건다"로 개정됨 — 고정 문자열 주입이 아니라
  SQL 생성기가 정렬/필터 컬럼을 인식해 끼워 넣어야 한다.
- 풀리는 것: 기간별 Top-N 의 센티넬(-100)·NULL 함정 (펀드 74종목 부당탈락 · ETF 전기간 154건)
- 출처: 펀드 감사 §3-② · ETF 감사 §2-④

---

## B. 판정 확정 — 값의 의미 (주최 측 질문 ⑰과 연동)

| # | 안건 | 지금 아는 것 | 풀리는 것 | 출처 |
| :-: | :--- | :--- | :--- | :--- |
| B-1 | 만기 전 `EVAL_PRICE` 정확히 10000.0 **1,813건** | 액면 파킹(미평가) 의심. 만기도래 파킹 1,011건은 확정 | 가격 질의 정확도 | 채권 감사 §2-③ |
| B-2 | `PD_RISK_GCD=0` 58건 (채권 전용) | 미분류 추정. KG pending | 위험등급 축 완결 | 채권 yaml workshop #2 |
| B-3 | 펀드 `fd_nast_suma=0` 센티넬 여부 | 원본 대조로도 안 갈림 — 주최 측 질문 Q5 | 순자산 Top-N 정확도 | DATA_NEEDS ⑰ |
| B-4 | `CRD_GRD_DT` 정의 (평가일 vs 변경일) | 37.3%가 2년 초과 — 정기평가 제도와 상충 | 등급 신선도 경고 임계 | 채권 yaml workshop #6 |
| B-5 | ETF `pd_dvid_cycl` "유효 183건" 불일치 원인 | 현행 DB 전 행 공백 — A-1 에서 배포 DB 대조로 판별 | 배당주기 답변 가부 | ETF 감사 §4-2 |
| B-6 | 기준일 2026-07-11 vs 02-24 | 갱신일 max 02-24 (채권) · ETF 갱신일도 6월 중순 | 만기상태 4분할 경계 (만기종료 16,180 vs 11,940) | 채권 yaml workshop #1 |

## C. 규칙·패턴 확정

| # | 안건 | 내용 | 출처 |
| :-: | :--- | :--- | :--- |
| C-1 | 🔴 채권 플래그 패턴 정밀화 4종 | is_private(패턴 7,766 vs 주장 10,067 — 어느 쪽도 재현 안 됨, '(PCBO사모/' 접두결합) · is_perpetual(473/450) · is_convertible(2,355/2,337) · is_coco 오탐 2건('아세코코리아') | 채권 감사 §2-⑤ |
| C-2 | ETN NAV 활용 방식 | du_nav_yday·rnf 가 ETN 379건에서 정상으로 판명 — 답변에 쓸지, ETF 와의 비대칭 서술 | ETF 감사 §4-1 |
| C-3 | 평가사 의견 갈린 258건 | 최저 vs 다수결 → 등급병합 규칙 | 채권 yaml workshop #3 |
| C-4 | DIRTY·NDY_* 사용 가부 | 복사 의심 94.6% · 경과이자 음수 21건(감사 정정: DIRTY 자체 음수 아님) | 채권 yaml workshop #4 |
| C-5 | 특수구조 15,306건 기본 제외 목록 | 추천·랭킹 질의의 제외 셋 확정 | 채권 yaml workshop #5 |
| C-6 | couponRate 속성 분리 | 지급 이자율 vs 명목·할인율 (.ttl 설계) | 채권 yaml workshop #7 |

## D. KG·온톨로지 축 (coverage report pending 과 연동)

| # | 안건 | 내용 |
| :-: | :--- | :--- |
| D-1 | 🔴 ISIN 중복 50종의 정체 | 티커 변형(사실상 중복 상장) vs 별개 상품. **Holdings 수집에서 ISIN 을 키로 쓸 계획이면 선행 확정 필수** (ETF 감사 §4-3) |
| D-2 | 합성 벤치마크 분해 | 'KOSPI200 25% + 종합채권01Y 75%' 등 374종 — 성분 지수 edge 로 분해할지 (Index 축 37.4% → 상향의 관건) |
| D-3 | 자산군 pending | 국내ETF '기타' 18건 ↔ 해외 'Alternatives' 동치 여부 · 펀드 자산군 유도(or_attr_desc 3축 압축) |
| D-4 | 지역 pending 3건 | Asia Pacific(오세아니아) · Greater China(홍콩·대만) · EuroZone(≠유럽) 범위 |
| D-5 | 통화 '000' 계열 | 채권 '000' 1건 · ETF 'CURR_CD_000' 1건 — 센티넬 확정 |
| D-6 | 운용사 잔여 | 코드북 D/E 등급 21코드(2,791행) — 외부 코드북 회신 대기 · 00080135 삼성액티브 귀속 · ETN 발행 증권사를 Organization 축에 넣을지 (ETF 미매핑 대부분) |
| D-7 | 구상호 동의어 층 | '동양자산운용'→우리 같은 질의측 동의어는 kg_alias 에 못 넣음(DB 값이 아님) — kg_synonym 별도 층 설계 |

## E. 외부 요청 (수집·질문 — DATA_NEEDS.md 관리)

- ⑰ 주최 측 확인 질문 5건 (A4 한 장 — 비용 최저, B-3 포함) → **워크샵에서 질문지 확정 후 발송**
- ① 수탁사 18종 이름 (운용사는 asset_manager.csv 로 해결됨 — DATA_NEEDS 상태 갱신 필요)
- ⑧ prfd_attr_cd 정의표 · ⑪ kofia 20자리 정의서 · ②③ Holdings+모자관계 (D-1 선행)

---

## 처리 순서 제안

1. **A-1 정본 DB** — 다른 안건(B-5 등)의 판별 수단이기도 함. 배포 DB 한 번 열면 끝
2. **⑰ 질문지 발송** — 회신 대기 시간이 기니 먼저 쏘기
3. **C-1·B-1** — 채권 답변 정확도 직결, 내부 데이터만으로 결정 가능
4. **D-1** — Holdings 수집 착수 전 필수
5. 나머지는 워크샵 당일 일괄
