# 도메인 원고 템플릿 — 공모펀드 (담당: 리드)

> **이 파일이 곧 제안서의 펀드 몫이다.** §A~§E 를 채우면 절 번호 그대로 이어 붙인다.
> 펀드는 결함 밀도가 가장 높고(소재 10건), 펀드만 닿는 공유 개체가 3종(Fund·FundAttribute·Country)이라 스포크가 가장 많다. 본문은 6건으로 압축하고 나머지는 부록.
> `▶` 가 채울 곳. 규칙: ① 수치는 NUMBERS ② 결함→선언 인과 ③ Federated·그래프 탐색·추론 금지 ④ "펀드"라는 말은 **운용단위 `(or_co, mtco)`** 인지 **KG Fund 노드(`rptt` 묶음)** 인지 절마다 표기.
> 마감: 초안 9/3, 완성 9/4. **진행: §A ✅ · §E ✅ (2026-09-03) — §B.2~B.3 · §C · §D 는 `▶` 잔여.**

| 이 파일의 절 | 전체 보고서 위치 | 분량 |
| :-- | :-- | :-- |
| §A | 2.1.1 데이터 단락 중 펀드 | 반 쪽 |
| §B | **2.2.4 공모펀드** | 3쪽 |
| §C | 4.3 흐름도 펀드 | 1쪽 |
| §D | 5.2 현업 적용 지점 펀드 | 1단락 |
| §E | 부록 A·B·C 펀드분 | 별도 |

---

## §A. 데이터 (→ 2.1.1) — 반 쪽

공모펀드 마스터는 23,676행 × 75컬럼이며 **행 단위가 펀드가 아니라 클래스**다. 2차 데이터에는 사모 8,960행이 섞여 들어와, 기본 모수를 `sale_yn='판매중' AND prvo_pbff_desc='공모'` 로 못 박았다 — **8,969행 / 3,040펀드**. 모든 집계·Top-N 답변이 이 모수를 함께 밝힌다. 행과 펀드를 구별하지 않으면 한 펀드의 클래스들이 상위 목록을 도배한다 — 실제로 한화2.2배레버리지 A·C1~C4·Ce 6클래스가 같은 펀드이고, 행 단위로 세면 TOP 5 의 세 자리를 이 한 펀드가 차지한다.

마스터에 없는 것 둘을 외부에서 보강했다. 간이투자설명서 구성종목 59,206행(`ext_fund_holdings`)과 **미래에셋증권 웹 펀드 상세 페이지** 10,565행(`ext_fund_page` — 설정일·보수·환매 규칙, 2026-08-18 관측). 웹 수집분에서 순자산·수익률 같은 **시계열 값은 일부러 적재하지 않았다** — 관측일이 마스터 기준일(2026-08-22)과 어긋나 근거로 쓸 수 없기 때문이고, 설정일·환매 규칙처럼 불변·준정적인 사실만 남겼다. 펀드 단위 집계·조인 키는 `(or_co_xtn_itt_cd, mtco_itm_no)` **복합키**다 — `mtco_itm_no` 는 운용사가 매긴 번호라 운용사 안에서만 유일하고, 단독으로 조인하면 85,649쌍 중 **5,616쌍(6.6%)** 이 다른 운용사의 펀드에 붙는다(2개 이상 운용사에 걸친 값 748종).

정제에서 **값을 고치지 않고 판정만** 한 것이 셋이다. `rptt_ksd_itm_no` 더미 7,086행(29.9% — 이 중 `KR0000000000` 이 5,308), `fss_itm_no` 더미 11,655행(**49.2%**), `std_itm_no` 더미 4,668행(19.7% — `'00000       '` 같은 0패딩 위장 결측 170행 포함). 이 값들은 `NULL` 이 아니라서 결측 처리를 하지 않으면 값으로 살아남고, 조인하면 한 값에 1만 행이 뭉치며, LLM 프롬프트에 원값이 닿으면 "등록번호는 000000000000입니다" 라고 답한다. 그래서 더미 판정은 정제 단계가 아니라 **온톨로지의 선언**(`normalization.dummy_as_missing`)으로 올렸다. 🔴 다만 이 규칙은 컬럼 화이트리스트 안에서만 쓴다 — 같은 패턴을 채권에 복사하면 최고 신용등급 `AAA` 8,722행이 "반복 문자" 로 걸려 통째로 사라진다.

✅ 수치 검증 (2026-09-03 `gen_proposal_numbers.py` 재생성분과 대조) — 8,969 / 3,040 / 49.2% / 59,206 / 10,565 전부 NUMBERS 일치.
🔴 초안 대비 정정 4건 — ① 조인 오결합 `179,333쌍 중 5,099(2.84%)` → **85,649쌍 중 5,616(6.6%)** ② `rptt` 더미 6,970 → **7,086** ③ `fss` 더미 11,611 → **11,655**(비율 49.2% 는 동일) ④ `std` 위장결측 145 → **170**. ①~④ 는 전부 1차 데이터 시점 값이었다. 재계산 근거식은 NUMBERS `§1 공모펀드 기본모수 · 식별 키` 에 생성기로 박아 두었다 — 이제 손으로 옮겨 적지 않는다.

---

## §B. 온톨로지 엔지니어링 — 공모펀드 (→ 2.2.4) — 3쪽

### B.0 도입 (3줄)

> 공모펀드는 공유 개체 11종 중 **9종에 닿는** 도메인이고, 셋(Fund·FundAttribute·Country)은 펀드만 쓴다. 결함의 성격도 다르다 — 채권·ETF 가 "값이 없다/틀리다"라면 펀드는 **"행이 펀드가 아니다"**. 클래스 도배·더미 키·클래스별 중복 기재가 전부 여기서 나온다. 그래서 펀드 온톨로지의 첫 결정은 개체가 아니라 **grain(행 단위)** 이었다.

### B.1 구조도 (그림 B-3)

```mermaid
flowchart LR
  PF["fp:PublicFund<br/>23,676행(클래스) · 75컬럼<br/>기본모수 8,969행 / 3,040펀드<br/>query_rules 31 · ABSENT 5"]
  subgraph HUB[공유 개체 — A 와 같은 순서]
    ORG[Organization]; CG[CreditGrade]; RG["RiskGrade<br/>1~6 · NULL≠0"]; AC[AssetClass]; IDX[Index]; REG[Region]; CTY[Country]; CUR[Currency]; SEC["Security<br/>ext_* 경유"]; FND["Fund (rptt 묶음)<br/>6,867 + MotherFund 717"]; FA["FundAttribute 179<br/>token alias"]
  end
  PF == "or_co_xtn_itt_cd (275) 운용사 / trusc_xtn_itt_cd (50) 수탁사" ==> ORG
  PF -- "rptt_ksd_itm_no (6,867)" --> FND
  FND -- "feedsInto (1,704) → 모펀드" --> FND
  PF == "prfd_attr_cds (179 속성 / 17 국가) — 콤마 다중값" ==> FA
  PF -- "prfd_attr_cds" --> CTY
  PF -- "bmrk_nm (389)" --> IDX
  PF -- "zrin_btyp_nm (18)" --> AC
  PF -- "fd_ivst_rgn_desc (9)" --> REG
  PF -- "zrin_fd_ivst_risk_gcd / _grd_nm (6/8)" --> RG
  PF -- "curr_cd (7)" --> CUR
  PF -- "ext_fund_holdings (11,893)" --> SEC
  PF -. "⊘ 채권형 펀드도 등급은 구성종목 단위" .-> CG
```

하단 주석 두 줄: ① `⊘` 속성·시계열 부재 5건 — `hasUnitsOutstanding`(좌수) · `hasFundManager`(운용역) · `hasNavHistory` · `hasHoldingsHistory` · `hasCreditGrade`. ② **펀드 단위 키 `(or_co_xtn_itt_cd, mtco_itm_no)` 14,467 은 KG Fund 노드(rptt 6,867)와 다른 뜻** — 집계·순자산은 복합키, 개체 조회는 rptt.

| 연결 | 컬럼 | 값 수 | 비고 |
| :-- | :-- | --: | :-- |
| Organization | `or_co_xtn_itt_cd` / `trusc_xtn_itt_cd` | 275 / 50 | 코드 → 코드북 `asset_manager.csv`(275 ✅)·`trustee.csv`(**18 — 50종 확장 미완**). `label_official` 슬롯 |
| Fund | `rptt_ksd_itm_no` | 6,867 | 클래스→펀드 묶음. MotherFund 717, `feedsInto` 1,704 |
| FundAttribute | `prfd_attr_cds` | 179 | 태그 210종 15축 중 KG 노드 179. `match_kind='token'` alias 179 |
| Country | `prfd_attr_cds` | 17 | 같은 컬럼의 국가 태그 |
| Index | `bmrk_nm` | 389 | 벤치마크 |
| AssetClass | `zrin_btyp_nm` | 18 | |
| Region | `fd_ivst_rgn_desc` | 9 | 해외 여부는 `ovrs_fd_desc`(4) 별도 |
| RiskGrade | `zrin_fd_ivst_risk_gcd` / `_grd_nm` | 6 / 8 | **1~6**, NULL 14,987 ≠ 0, 띄어쓰기 변형 2종 alias |
| Currency | `curr_cd` | 7 | |
| Security | `ext_fund_holdings` | 11,893 | distinct `isin` 기준 |
| ⊘ CreditGrade | — | — | |

### B.2 결함 → 선언 (6건 채택) — 템플릿은 `04_도메인_채권.md` §B.2

**완성 예시 — B.2-1:**

#### B.2-1. 행이 펀드가 아니라 클래스이고, 묶음 키로 보이는 `std_itm_no` 는 묶음 키가 아니다 → grain 선언 + 복합키 (2층)

- **데이터가 이랬다**: 23,676행은 클래스 단위다. 펀드 묶음 키처럼 보이는 `std_itm_no` 는 distinct 18,948 중 18,909가 `itm_no` 와 1:1 이고, 2개 이상을 가리키는 값이 39개뿐이다. 결측 515 + 12자 패딩 위장결측 145 = 660행은 전부 판매완료. 즉 이 컬럼으로는 클래스를 묶을 수 없다.
- **그래서 이렇게**: `row_grain` 을 "행 = 클래스"로 선언하고, 펀드 단위 집계 키를 `(or_co_xtn_itt_cd, mtco_itm_no)` 복합키로 못 박았다(`query_rules.펀드단위`, zero-pad 7자 정본). `mtco` 단독은 415종이 2개 이상 운용사에 걸친다. 순자산 `fd_nast_suma` 는 클래스별 중복 기재라 **SUM 금지·펀드 단위 MAX**(`fp:navTotal` 주석).
- **런타임에서**: Plan 이 `펀드단위` GROUP BY 템플릿을 always_on 으로 주입, 가드가 클래스 종속 값의 단일 MAX 를 금지(6R F2), 조립이 "펀드 N개 / 클래스 M개" 를 병기.
- **실측 사례**: "순자산 큰 펀드 TOP 5" — 클래스 행으로 세면 같은 펀드가 5자리를 도배. ▶ probe ID·응답 채우기.
- **안 지키면**: `GROUP BY mtco_itm_no` 단독 → `grp='00'` 이 운용사 34곳에 걸려 남남 펀드가 한 펀드로 합산.

**소재 (B.2-2 ~ B.2-6 채택, 7~10 은 부록):**

| # | 결함 | 선언 | 층 | 실측 사례 후보 |
| :-: | :-- | :-- | :-: | :-- |
| 2 | 사모 8,960행 혼입 | 기본모수 `판매중 AND 공모` 강제, 답변 모수 명시 | 2 | 모수 없이 "펀드 몇 개" → 2.6배 부풀림 |
| 3 | 더미 키(`KR0000000000` 5,308 · fss 49.2%) | `normalization.dummy_as_missing` — 조인·개체 키에서 제외 | 2 | 더미를 rptt 묶음으로 쓰면 5,308 클래스가 한 펀드 |
| 4 | `prfd_attr_cds` 콤마 다중값 210종 15축, `or_attr_desc` 는 3축 압축 | `match_kind='token'` alias + 확정식 `','||col||',' LIKE '%,raw,%'`, `axis_mapping` 분리, `class_hierarchy`(펀드유형은 하위클래스이지 개체가 아님) | 1·선언 | "ESG 펀드", "연금저축 펀드" 태그 질의 |
| 5 | 좌수·운용역·시계열 없음 | ABSENT 5건 → Gate 즉시 기각 + 대체 안내(`fd_nast_suma`·`bns_bpr`) | 3 | KG-027: 설정유형코드 '10'을 "10좌"로 6펀드 단언한 사고 → ABSENT 도입 |
| 6 | 운용사·수탁사가 코드뿐, 약칭 라벨('삼성' 2자)이 매칭 하한에 걸림 | 코드북 275·50종 + `label_official` 슬롯 분리 | 1 | FND-034 "삼성자산운용 펀드" Ground 0 → 복구 |
| 7 | 위험등급 NULL 14,987, 이름 띄어쓰기 변형('높은위험' 20행) | NULL≠0 → `range_by_table` 1~6, 변형 alias | 3 | 부록 |
| 8 | 자펀드→모펀드 구조 | `feedsInto` 1,704 edge, MotherFund 717 노드 | 1 | 공식 예시 #2 국민성장펀드 구조 |
| 9 | 헤지펀드 플래그가 사모 등록분만 표시 | `hdge_fd_yn` 금지, 사모투자재간접 공모펀드 형태로 판정 | 2 | 부록 |
| 10 | 1주 수익률 전건 결측 등 | `schema_exclude` 4컬럼 — 스키마 프롬프트에서 제거 | 2 | 부록 |

권장 채택: 1·2·3·4·5·6 (8 은 §C 흐름도에서 다룸).

### B.3 한계 (3줄)

▶ 채울 것. 후보: ① **Fund 두 뜻**을 분리하지 않았다 — ttl 클래스명·도해가 함께 바뀌는데 현재 오답을 만들지 않아서. FundClass 노드 0·`belongsToFund` edge 0 은 결함이 아니라 컬럼이 담고 있어 중복 생성을 피한 설계 ② 비정형 설명서 파싱은 구성종목·페이지 항목까지만 — "투자전략 동향" 류 서술은 범위 밖 ③ 위험등급 NULL 63% 는 보완 불가.

---

## §C. 기능 흐름도 — 펀드 (→ 4.3) — 1쪽

**질의**: 공식 예시 #2 *"국민성장펀드의 구조와 투자전략 동향 등 찾아서 알려줘"*

```bash
# eval/probe_fund_flow.txt:  FUND-FLOW-1<TAB>국민성장펀드의 구조와 투자전략 동향 등 찾아서 알려줘
export PYTHONIOENCODING=utf-8
./.venv/Scripts/python.exe eval/probe_server.py eval/probe_fund_flow.txt -o eval/probe_fund_flow.json
```

| 단계 | 이 질의에서 일어나는 일 | 읽는 온톨로지 요소 |
| :-- | :-- | :-- |
| Route | "펀드" 머리명사 → `public_funds` | 라우팅 어휘 |
| Ground | "국민성장펀드" → Fund 노드(rptt 묶음) + 상품 고유명 슬롯(residual_name_token), 모펀드면 `feedsInto` 후손 | 1층 Fund · edge feedsInto |
| Gate | "구조·투자전략 동향" 중 ABSENT 해당 항목(운용역 등) 판정 → 있는 것만 진행 | 3층 ABSENT 5 |
| Plan | `펀드단위` 복합키 GROUP BY, `설명서항목` 규칙 → `ext_fund_page` JOIN(`itm_no`, 컬럼 전부 한정), 기본모수 | 2층 + 조인키 |
| 검증 | JOIN 템플릿·별칭 한정 검사, 값 사전 대조 | 3층 |
| 실행 | 클래스 목록 → 펀드 단위 요약 + 설정일·환매조건 | — |
| 조립 | 구조(모/자·클래스 수·운용사·수탁사) + 페이지 항목 + **"투자전략 동향은 제공 데이터에 없음"** 명시 | answer_rules · ABSENT 안내 |

▶ 채울 것: 실제 think_trace 원문, answer 첫 3줄. "동향"을 지어내지 않고 범위를 밝힌 부분을 강조.

---

## §D. 현업 적용 지점 (→ 5.2) — 1단락

**예시 문장:**
> 클래스 도배와 더미 키를 판정 규칙으로 격리했기 때문에 "펀드 단위 순자산·클래스 수·운용사별 집계"가 현업 보고서와 같은 기준으로 나온다. 속성 태그 15축(`prfd_attr_cds`)을 token alias 로 열어 두어 "연금저축 가능한 ESG 해외 펀드" 같은 다축 필터가 상품명 검색 없이 성립한다.

▶ 한 문장 더: 판매 현장(적합성·설명의무)에서 위험등급 NULL 을 "없음"으로 답하는 것의 의미.

---

## §E. 부록 펀드분

### E.1 `ontology/fund_pub.ttl` 원문 (부록 A) — 스키마부 그대로

`fund_pub.ttl` 은 34줄이고 **손으로 쓰지 않는다** — `ontology/enums/public_funds.yaml` 에서 `scripts/build_ontology.py` 가 생성한다(파일 첫 줄이 `# GENERATED … DO NOT EDIT`). 고칠 곳은 언제나 yaml 이다. 아래는 스키마 선언부 전문이고, `# ABSENT:` 5줄은 E.2 표와 같은 내용이라 여기서 뺐다.

```turtle
# GENERATED by scripts/build_ontology.py — DO NOT EDIT
# 원천: ontology/enums/*.yaml + ontology/shared/*.yaml — 고칠 것은 yaml 이다
# fund_pub.ttl — 공모펀드 (SQLite public_funds)
@prefix fp:   <http://mafest.ai/product#> .
@prefix owl:  <http://www.w3.org/2002/07/owl#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix xsd:  <http://www.w3.org/2001/XMLSchema#> .

fp:PublicFund rdfs:subClassOf fp:Product ; rdfs:comment "SQLite 테이블 public_funds"@ko .

fp:navTotal a owl:DatatypeProperty ;
    rdfs:domain fp:PublicFund ;
    rdfs:range  xsd:decimal ;
    rdfs:comment "순자산 — 컬럼 fd_nast_suma. 클래스별 중복 기재라 SUM 금지, 펀드 단위 MAX 집계"@ko .

# 값 범위 — fp:PublicFund 의 fp:riskGradeValue 는 1~6 (0등급 없음 — 등급 미수록 클래스는 NULL(0 아님))
fp:riskGradeValue_PublicFund rdfs:subPropertyOf fp:riskGradeValue ;
    rdfs:domain fp:PublicFund ;
    rdfs:range [ a rdfs:Datatype ; owl:onDatatype xsd:integer ;
                 owl:withRestrictions ( [ xsd:minInclusive 1 ] [ xsd:maxInclusive 6 ] ) ] ;
    rdfs:comment "1(매우 높은 위험)~6(매우 낮은 위험) — 0등급 없음 — 등급 미수록 클래스는 NULL(0 아님)"@ko .
```

읽는 법 두 줄. ① `fp:navTotal` 의 `rdfs:comment` 는 장식이 아니라 **런타임이 읽는 금지 조항**이다 — 순자산을 SUM 하면 클래스별 중복 기재분이 그대로 더해진다. ② `riskGradeValue` 를 `PublicFund` 전용 하위 속성으로 다시 선언한 것은 값 범위가 테이블마다 다르기 때문이다(공모펀드 1~6, 국내ETF 1~6, 채권 11~16). 상위 속성 하나에 범위를 걸면 어느 한쪽이 틀린다.

### E.2 ABSENT 표 (부록 A) — `# ABSENT:` 5줄의 사람용 판본

| 속성 | 사유 (ttl `why` 원문) | 대체 |
| :-- | :-- | :-- |
| `hasCreditGrade` | 신용등급 컬럼 없음 — 채권형 펀드도 등급은 구성종목 단위(미수록) | — |
| `hasUnitsOutstanding` | 마스터·설명서 수집분 어디에도 좌수(설정·잔존·잔고 좌수) 컬럼이 없음 | `fd_nast_suma`(순자산) · `bns_bpr`(기준가) |
| `hasFundManager` | 펀드 운용역(펀드매니저) 정보가 마스터·설명서 수집분에 없음 | `or_co_xtn_itt_cd`(운용사 코드 → 법인명) |
| `hasNavHistory` | 기준가는 기준일 단일 스냅샷(`bns_bpr`)뿐 — 시계열 미수록 | `bns_bpr` + 기간 수익률(1개월~5년 누적) |
| `hasHoldingsHistory` | 보유 종목은 설명서 수집 기준일 1시점뿐 — 시계열 미수록 | `ext_fund_holdings`(수집 기준일 보유 종목·비중) |

🔴 이 표가 **좌수 환각(KG-027)의 결정층**이다. `fd_set_pcd` 의 값 `'10'`(설정 형태 = 추가형)을 "10좌" 로 읽어 6개 펀드에 단언한 사고가 있었다. ABSENT 는 게이트 어휘(정규식)로 사영되어 `좌수·설정좌·잔존좌` 가 들어온 질의를 **LLM 호출 0회로 즉시** 기각하고 대체 컬럼을 안내한다. 기준가(`bns_bpr`)는 **있으므로** ABSENT 어휘를 '추이·변동' 에만 걸었다 — "기준가 얼마" 는 답한다.

### E.3 `ontology/enums/public_funds.yaml` 발췌 (부록 B)

**(1) 개체 정의 — `entities.Fund` 원문.** 제안서에서 "펀드" 라는 말이 두 뜻으로 갈리는 지점이고, 이 note 가 그 판정문이다.

```yaml
entities:
  FundClass:
    kind: entity
    role: 주 노드
    source: itm_no
    count: 23676
    note: >-
      판매 단위(클래스) = 행. 🔴 KG 에 FundClass 노드는 없고 belongsToFund edge 도 0건이다.
      이것은 결함이 아니라 설계다 — 클래스→묶음 관계를 마스터 컬럼 rptt_ksd_itm_no 가 이미
      담고 있어 23,676 노드와 edge 를 중복 생성하지 않는다. 클래스 단위 조회·집계는 KG 가
      아니라 itm_no 로 한다.
  Fund:
    kind: entity
    source: (or_co_xtn_itt_cd, mtco_itm_no) 합성키
    count: 14467                 # 원값 기준 · zero-pad 시 14,409 (판매중 4,305)
    relation: FundClass -belongsToFund-> Fund
    note: >-
      '모펀드' 가 아니다 — 클래스를 걷어낸 '펀드' 단위(운용사 내부 관리 단위). 순자산은 이 단위 MAX.
      🔴 이름 충돌 주의 — 여기의 Fund(운용 단위 14,467)와 KG 의 Fund 노드는 **다른 축**이다.
      KG node_type='Fund' 7,584 = rptt_ksd_itm_no 묶음 6,867 + 마스터 밖 MotherFund 717.
      집계·순자산은 (or_co, mtco) 기준, KG 개체 조회는 rptt 기준으로 읽을 것.
    용어규칙: mtco 단위는 '펀드'. '모펀드' 는 모자형에만 쓰고 이 테이블에는 없다고 명시할 것
```

**(2) `class_hierarchy` — 펀드 유형은 개체가 아니라 하위클래스다.** FIBO 원칙이고, 주최가 제시한 `axis_fundType` 축이 자본시장법 5분류 층위와 그대로 맞는다. 유형을 개체로 세우면 "주식형" 이라는 노드가 펀드 3,000개와 이어지는 무의미한 허브가 된다.

```yaml
class_hierarchy:
  note: FIBO 원칙 — 펀드 유형은 owl:Class 하위클래스이지 개체가 아님.
  Fund:
    SecuritiesFund: [EquityFund, BondFund, EquityMixedFund, BondMixedFund]
    MoneyMarketFund: []
    MixedAssetsFund: []
    RealEstateFund: []
    SpecialAssetsFund: []
```

🔴 `derivation_rules.assetClass` 와 **층위가 다르다** — `fundType` 은 자본시장법 5분류, `assetClass` 는 실제 투자자산 축이다. 합치지 말 것.

**(3) `FundAttribute` 15축 — 한 컬럼에 EAV 가 들어 있다.** `prfd_attr_cds` 는 `<문자><3자리숫자>` 태그의 콤마 목록이고, **문자가 축·숫자가 축 안의 값**이다. 같은 컬럼에 ISO 3자 대문자(국가)가 섞여 있어 형태로 갈라 `Country` 17종과 `FundAttribute` 210종으로 보낸다(태그 227종 = 17 + 210, 판정률 100%). 축 해독은 `ontology/codebooks/fund_attr_code.csv` 로 완료했다.

| 축 | 축 이름 | 값 수 | 축 | 축 이름 | 값 수 |
| :-: | :-- | --: | :-: | :-- | --: |
| C | 설정형태(추가/단위·개방/폐쇄) | 4 | Q | 실물자산 | 5 |
| D | 판매채널 | 5 | R | 특수자산·전략자산 | 10 |
| E | 가입대상 | 3 | S | 운용전략 | 16 |
| F | 세제혜택유형 | 7 | T | TDF 빈티지 | 11 |
| G | 세제특례·연금상품 | 15 | V | 투자지역구분(국내/국외/국내외) | 3 |
| M | 펀드구조(모자/종류형/FoFs 등) | 16 | W | 세부투자지역 | 35 |
| N | 테마·컨셉 | 60 | O | 섹터 | 10 |
| P | 투자스타일·채권듀레이션 | 10 | | **합계 15축** | **210** |

태그 보유 11,280/23,676행(판매중 8,550). KG 에는 이 중 179종이 `FundAttribute` 노드로 서고 `match_kind='token'` alias 179건이 붙는다 — 다중값 컬럼이라 `LIKE '%ESG%'` 로는 부분 문자열이 오탐하므로 **확정식으로 양끝을 콤마로 감싼다**: `',' || prfd_attr_cds || ',' LIKE '%,C103,%'`.

**(4) `query_rules` 31건 중 대표 4 + `clarify`.** 값 목록만 싣고 의미를 안 실으면 규칙이 안 되는 축들이다.

```yaml
row_grain: 행 = 종목(itm_no) — 클래스 단위 마스터. 23,676행 = 23,676 distinct itm_no · NULL 0
query_rules:
  기본모수: "sale_yn = '판매중' AND prvo_pbff_desc = '공모'  # 모수 8,969 를 답변에 밝힐 것"
  펀드단위: >-
    GROUP BY or_co_xtn_itt_cd,
             COALESCE(CASE WHEN length(mtco_itm_no) >= 7 THEN mtco_itm_no
                           ELSE substr('0000000' || mtco_itm_no, -7) END, itm_no)
    # 🔴 JOIN 시 itm_no 는 반드시 별칭으로 한정한다(p.itm_no) — 비한정이면 모호 컬럼으로 실행이 죽는다
  개인법인: >-
    pers_corp_desc 의 '해당없음'(기본모수 6,864행)은 결측이 아니라 개인·법인 전용이 아닌 **일반 펀드**다.
    '개인이 가입할 수 있는' = IN ('개인','해당없음'). '개인'만 걸면 일반 펀드 6,864행이 통째로 빠진다.
  해외구분: >-
    ovrs_fd_desc('해외' 4,483 · '국내외혼합' 878 · '국내' 3,498 — 기본모수) — '해외 투자 펀드' 는
    IN ('해외','국내외혼합') 이 기본(혼합 포함 여부를 답에 밝힌다). 세부 지역은 fd_ivst_rgn_desc.
  설명서항목: >-
    마스터에 없는 설정일·환매조건·설명서 보수는 ext_fund_page JOIN (e.itm_no = p.itm_no).
    커버리지가 100% 가 아니므로 "N건 중 M건에 수록" 을 답에 밝힌다 —
    estb_dt 93.7% · redemption_fee_desc 3.3% · class_desc_ko 92.2%.
clarify:
  다의어:
    수익률: 기간 8종 — 미지정이면 되묻지 말고 1년 기본값 + 명시 (리드 결정 08-31)
    보수: 운용·판매·수탁·사무관리 4항목 / 총보수 — 미지정이면 총보수 + 항목별 병기
```

🔴 **보수는 단위가 두 개다** — 마스터 4종은 ‰, `ext_fund_page.total_fee_pct` 는 %. 마스터 합계 ÷ 10 이 설명서 총보수와 맞는지로 단위를 확정했고(8,925건 대조, 비율 10.0), 마스터 합계는 기타비용 미포함이라 TER 질의에는 설명서 값을 병기한다.

### E.4 값 수 표 (부록 C, 매트릭스 펀드 열) — §B.1 표 그대로

§B.1 의 연결 표를 그대로 옮긴다. **2026-09-03 재실측으로 4칸을 정정했다** — 운용사 274→**275**, 수탁사 48→**50**, `fd_ivst_rgn_desc` 7→**9**, `ext_fund_holdings` Security 11,971→**11,893**(distinct `isin` 기준). ▶ 조판 직전 한 번 더 재실측.

🔴 부록 B 편집자(ETF 담당) 확인 1건 — 코드북 `ontology/codebooks/trustee.csv` 가 **18행뿐**이다. 수탁사 코드는 50종이라 32종이 라벨 없이 코드로만 남는다(`asset_manager.csv` 는 275종 확장 완료). 부록에 "50종 중 18종 라벨 확보" 로 적든가, 조판 전 확장하든가 둘 중 하나가 필요하다.

---

## 제출 전 체크

- [ ] §B.2 6건, 각 건 다섯 줄 템플릿
- [ ] "펀드"가 나올 때마다 운용단위/KG 노드 중 어느 뜻인지 표기됐다
- [x] 수치가 NUMBERS 와 같다 (8,969/3,040 · 85,649/5,616 · 6,867 · 717 · 179) — 09-03 재생성분 대조, §A 정정 4건
- [x] "Federated / 그래프 탐색 / 추론" 0건 (본문 기준 — 규칙 줄·체크리스트 제외)
- [ ] §C 에 실제 think_trace 원문, "동향 없음" 명시 부분 강조
- [ ] 그림 B-3 개체 순서가 그림 A 와 같다
