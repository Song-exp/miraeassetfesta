# 🎙️ 오프라인 설명회 전사 반영 점검 (2026-09-04 · 병철)

> 원천: 2026 AI Festival 오프라인 설명회 **녹음 전사본** — ① 네이버클라우드 정낙수 상무(환경 구성) ② **이오랑 수석 테크세션**(온톨로지·KG·리트리버 25분) ③ 이오랑 수석 과제 설명·Q&A.
> 지금까지 우리가 참고한 것은 이 세션의 **슬라이드 파생 메모**였다(21번). 전사본으로 대조하니 **제출 규격 1건이 통째로 빠져 있었다.**

## 🔴 A. 빠져 있던 것 — 즉시 반영함

| 발언 | 우리 상태 | 조치 |
| :-- | :-- | :-- |
| *"**PDF와 워드파일을 같이 주세요.** PDF를 일단 평가하겠지만 이건 나중에 추후 편집을 막기 위한 내용이고 워드파일도 같이 첨부되어 있어야 합니다."* | 🔴 **어느 문서에도 없었음** — PDF 만 준비 중이었다 | ✅ `PROJECT.md` §제출 항목에 명시. **조판 담당(채권)에게 전달 필요** |
| *"TTL일 필요도 없습니다. 온톨로지를 적는 다양한 방법이 있고 그 방법에 온톨로지가 적혀 있으면 됩니다."* | ttl 5분할 제출 예정 | ✅ 요건 충족(오히려 권장 형식) — PROJECT.md 에 근거 기록 |
| *"권장 목차는 권장인 거고 추가·삭제 자유. 가장 중요한 건 제안 방법·시스템 구성도·주요 기능 흐름도 그리고 **5번**. 수상에서는 아마 5번이 중요한 평가 요소."* | 목차 유지 중 | ✅ 기록. §5 정량화는 `22_기대효과_정량화_재료.md` |

## B. 이미 맞게 하고 있던 것 (전사본으로 재확인)

| 발언 | 우리 구현 |
| :-- | :-- |
| *"위험등급 99등급이 나오면 감점. 차라리 '이 상품은 위험등급이 없어요'가 고객에게 낫다"* | 부재 선언(ABSENT) + 게이트 기각. **없는 것을 만들지 않는다**가 우리 3층의 뼈대 |
| *"ETF는 ETN과 구분됩니다. 둘 중 한 가지 값만"* (disjointWith 예시) | `fp:ETF ⟂ fp:ETN` + 테이블 축/상품종류 축 분리 |
| *"온톨로지는 문서가 아니라 **런타임 통제 계층**. LLM에게 추출 실행할 때 같이 넣으면 정확도가 올라간다"* | 규칙을 매 질의 프롬프트에 주입(140개, 선별 주입으로 58% 절감). **문서에만 적으면 챗봇은 모른다**를 우리도 사고로 배웠다(분배금 100배·현재가 라벨) |
| *"SQL이 실행됐다고 정답은 아니다. 코스피 관련 ETF를 이름 LIKE로만 찾으면 누락. **정답 확률 45%**"* | 그 누락이 우리 `어휘_표기정규화` 실측과 정확히 같다 — 상품명 21건 · 지수명 95건 · 교집합 2건 → 두 컬럼 OR + 공백 제거 |
| *"RDB는 컬럼 간 관계를 안 담는다(총보수 = 운용보수 + 기타보수)"* | 파생 규칙(`derivation_rules`)·`enforce` 슬롯이 그 자리 |
| *"NL2SQL은 스키마만 주면 8.3%, **비즈니스 의미까지 주면** 정확도가 높다"* | 스키마 + 규칙 + 값 사전을 함께 주는 것이 우리 Plan 단계 그 자체 |
| *"엔티티 리졸루션 — 청크마다 '이 ETF는'으로만 나와 식별이 안 된다"* | 우리는 문서 청킹 대신 **구조화 데이터 + 값 사전**이라 이 실패 유형이 원천적으로 없다 (제안서에 쓸 대비 포인트) |
| *"근거 표시 필수(양식 자유) · 확인 불가는 '확인할 수 없음' 명시 · 수익률 전망·'무조건 좋다' 금지"* | `retrieved_context`·`think_trace` · 거절 7사유 · answer_rules 권유 표현 금지 |
| *"답변불가 질의는 점수에 포함 안 되지만 **답변하면 감점**"* | 답변불가 문항 회귀 + 게이트 HCX 0회 |
| *"멀티턴 없음. 단 '모르는 질문입니다' 또는 '이렇게 얘기해 주시면 대답해 드릴 수 있어요'까지는 인정"* | 거절·되묻기 둘 다 정답 형태로 구현 |
| *"응답 시간도 평가 항목. 60초면 사실상 감점. 실서비스 기준 15초. 전체 평균 기준 가감점 가능"* | PROJECT.md §타임아웃 3층(실무 15초 target) |
| *"채점은 **답변에 기대하는 엔티티와 릴레이션이 전부 포함되어 있느냐** + 근거가 유의한가. 코스피 상위 5개에 KODEX 200 없으면 감점"* | 우리 eval 의 `must_include`/`must_not_include` 가 정확히 같은 채점 모델 |
| *"외부 데이터는 쓰되 **기준일 이후 데이터는 의미 없다**(9월 3분기 보고서 등)"* | 코드북 `as_of ≤ 2026-08-24` 를 빌드 V4 가 기계 검사 |
| *"UI는 예선 평가 대상 아님. 본선 라이브 데모는 가점"* | 예선은 API 만 — 현행 유지 |

## C. 남은 확인 1건

*"인텐트 분석(3번)은 HCX 를 쓰시면 되고, Answer Generator 는 여러분들이 알아서"* → 이후 8/31 공통 공지로 **인텐트 분석·답변 생성 두 구간 HCX 필수**로 정정됨. 현행 구현은 규칙 라우터 + 미특정 시 HCX 판단 + 답변 생성 HCX 이므로 정정본 기준으로 적합(PROJECT.md 확인 완료). **공지가 전사본보다 최신이다.**
