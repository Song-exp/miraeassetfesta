# 저장소 파일 명세서 — 무엇을 남기고 무엇을 정리하는가 (2026-09-04 작성 · **2026-09-06 16:40 재계수** · 병철)

> **이 문서가 답하는 것** — ① 기술제안서를 쓰는 데 필요한 파일 ② 주최 저장소에 올릴 파일.
> **이 목록에 없는 것은 정리 후보**다. 다만 아래 §D 는 **소유자별로 나눠** 두었다 — 남의 트랙 문서를 내가 지우자고 판단하지 않는다.
>
> 🔴 **마감 2026-09-06 23:59, 이후 커밋·push 는 실격.** 정리도 커밋이므로 **마감 전에 끝내거나 하지 않는다.**
> 🔴 이 문서 자체는 아무것도 지우지 않는다. 분류만 한다.
>
> 도메인 단위 파일 명세는 서현의 `채권_필요파일.md` 가 이미 있다. 이 문서는 **저장소 전체** 관점이다.

전체 추적 파일 **670개 49.9MB** (`.git` 은 로컬 3.4GB 지만 옛 `external.zip` 이 로컬 브랜치
`backup/pre-strip` 에만 붙어 있어 주최 저장소 push 에 따라가지 않는다)

| 구분 | 개수 | 크기 | 처분 | 9/4 대비 |
| :-- | --: | --: | :-- | :-- |
| **A. 제출 (코드)** | 234 | 23.0MB | **반드시 남긴다** | +70 (테스트 55 · 스크립트 8 · eval 5 · src 1 · API_SPEC) |
| **B. 제안서 재료** | 102 | 3.0MB | 원고 확정까지 남긴다 | +19 (기술제안서 13 · 그림 6) |
| **C. 보류** | 1 | 0.04MB | 아래 판단 | 과제소개 pdf 는 추적 대상에서 빠졌다 — `PROJECT.md` 만 남음 |
| **D. 정리 후보** | 333 | 24.0MB | D-1 은 정리 가능 · D-2 는 소유자 판단 | +44 (eval 라운드 로그 30 · docs 최상위 18) |

> 계수 기준은 `scripts/make_submission.py` 의 포함 규칙과 같다(`git ls-files` → `src·ontology·scripts·tests·deploy·data` +
> 최상위 8 + `docs/API_SPEC.md` + `eval/*.jsonl|*.py`). 명세서와 추출기가 어긋나면 추출기가 정본이다.

---

## A. 주최 저장소에 올릴 것 — 234개 23.0MB

주최 제출 항목은 3종이고(안내 07쪽), 아래가 그 실체다.

### A-1. 소스코드 (제출 항목 1)

| 경로 | 개수 | 크기 | 왜 필요한가 |
| :-- | --: | --: | :-- |
| `src/` | 16 | 1.03MB | 구현체. `Dockerfile` 의 `COPY` 대상. 9/6 추가분: 의도 분석(`hcx/planner.parse_intent`)·채팅 화면(`api/chat_ui.py`) |
| `ontology/` | 46 | **19.7MB** | yaml 단일 원천 + 생성된 ttl 5분할. **주최가 ttl 제출을 요구**. ttl 은 `scripts/build_ontology.py` 생성물이며 9/6 02:01 재생성분이 최신 — 그 뒤 yaml 변경(ETF 통화 축·TR 규칙)은 게이트·규칙 층이라 ttl 출력에 들어가지 않는다 |
| `scripts/` | 49 | 0.71MB | 빌드·수집 재현 (`build_ontology` · `build_db` · `fetch_*`). 주최 Q&A 가 "수집 방법·적재 Flow 기재" 를 요구했다. `make_submission.py` 도 여기 |
| `tests/` | 81 | 0.90MB | **회귀 1,538건**(9/7 새벽 전량 통과). 제출 요건은 아니나 **정성평가 '기술완성도'의 증거**. ETF 분 13파일 |
| `eval/*.jsonl` · `eval/*.py` | 29 | 0.47MB | 평가셋과 실행기. **부록 E 평가 재현 절차가 이걸 가리킨다**. ETF 는 국내 36 · 해외 28 문항 |
| `deploy/` | 2 | 0.01MB | 배포 스크립트 |
| `data/` | 2 | 0.01MB | 코드북 csv + 외부수집 출처 기록 |
| 🔴 **`docs/API_SPEC.md`** | 1 | 0.01MB | **제출 항목 3 그 자체.** `docs/` 밑에 있어 자동 분류로는 D 로 떨어진다 — **예외로 A 에 둔다** |
| 최상위 8개 | 8 | 0.01MB | `Dockerfile` · `requirements.txt` · `requirements-dev.txt` · `docker-compose.yml` · `.dockerignore` · `.env.example` · `.gitignore` · `.gitattributes` |

`ontology/` 가 20MB 인 것은 `shared/security_auto.yaml`(10MB)과 `common.ttl`(4.7MB) 때문이다.
전자는 값 사전의 실체이고 후자는 제출 요구 항목이라 **둘 다 뺄 수 없다.**

> 🔴 **`docs/API_SPEC.md` 주의** — 이 명세서 초판은 `docs/` 직속 파일을 전부 D(정리 후보)로
> 분류했는데, 그 사이 서현이 `docs/API_SPEC.md` 를 만들었다. **제출 항목 3 필수물이 삭제
> 대상에 들어가 있었다.** 정리 실행 전에 반드시 A 로 옮겨서 판단할 것. `scripts/make_submission.py`
> 의 `INCLUDE_FILES` 에도 추가해 두었다.

### A-2. 아직 없는 것 — 만들어서 넣어야 한다

| 무엇 | 요건 | 담당 | 2026-09-04 확인 |
| :-- | :-- | :-- | :-- |
| **`README.md`** (루트) | 제출 항목 1 — "환경 구성 · 실행 명령어 + 🔴 **API End-point 주소**" | **리드** (9/4 확정) | 🔴 **9/6 16:40 현재 없음.** 엔드포인트 주소가 들어가는 유일한 자리라 이게 빠지면 제출 항목 3 이 반쪽이 된다. `make_submission.py` 는 없으면 경고만 내고 넘어가므로 추출만으로는 안 잡힌다 |
| **기술제안서 PDF + 워드** | 제출 항목 2 — 설명회에서 **둘 다** 요구 | 조판 채권 | 원고는 둘 다 확정: `27_…ETF판_전체원고`(62K) · `28_채권_최종원고`(73K). **28 만 html·pdf 가 있고 27 은 렌더 산출물이 없다.** 어느 판을 본 제안서로 낼지, 워드(.docx)를 누가 뽑을지 **리드 결정 대기** |
| **API 명세서** | 제출 항목 3 — End-point URL + 요청/응답 JSON 스키마 **필수 명시** | 채권 | ✅ `docs/API_SPEC.md` (9/6 갱신 — think_trace 예시에 `[Intent]` 단계 반영) |

`docs/HOW_IT_WORKS.md` 가 README 초안 재료로 쓸 만하다(§B 에 남겨 둠).

---

## B. 제안서를 쓰는 데 필요한 것 — 102개 3.0MB

**제출물이 아니다.** 원고가 PDF·워드로 나가면 역할이 끝난다. 다만 **원고 확정 전에는 지우지 않는다.**

### B-1. `docs/기술제안서/` 52개 2.18MB — 원고 본체와 규율

9/4 대비 13개 증가: 그림 6(`figures/*.svg`) · `28_채권_최종원고` 3형식 · ETF 실측 기록 3종 · 채권 점검문항.

원고 문서 `00`~`27` 전부. 그중 성격이 다른 것만 짚는다.

| 문서 | 역할 |
| :-- | :-- |
| 🔴 **`27_기술제안서_ETF판_전체원고`** | **기술 제안서 본체.** 주최 6대 목차 전체를 ETF 예시로 채운 워드 조판본. **이 파일이 PDF·워드가 된다 — 절대 지우지 않는다** |
| `01_작성방향_분담` · `02_목차` | 분담·용어·쪽수. **원고 쓰기 전 필독** |
| `03`~`14` | 절별 초안. 27 번을 만든 재료 |
| `15_주최QA` · `21_테크세션` · `23_설명회` | 주최 요구 ↔ 반영 위치 대조. **빠뜨림 점검용** |
| `17`·`18`·`19`·`20_구축과정_*` | 04 흐름도 재료 (축 결정 후 사용) |
| `24_확장성_구조분석` · `22_정량화_재료` | 05 기대효과 재료 |
| `26_ETF_최종원고` | 2.2.3 ETF 절 단독 조판본. 27 번에 흡수됐으나 **27 번 확정 전까지 남긴다** |
| `채권_필요파일` | 서현의 도메인 단위 파일 명세 |
| 🔴 **`28_채권_최종원고`** | 채권판 전체 원고. md·html·pdf 세 형식. **27 과 같은 6대 목차를 채권 예시로 채운 판** — 둘 중 무엇을 제출하는지는 리드가 정한다 |
| `figures/` 6 | 원고 삽화 svg(3층·결측 유형·채권 개체·파이프라인·타임라인·방어 층) |
| **실측 기록 8종** | ETF: `ETF_오답_마스터기록`(68K · 라운드 14까지 전건) · `ETF_튜닝문항_20`(정답표) · `ETF_재투입_리스트_2026-09-06` · `ETF_오답기록_2026-09-03` · `최종점검_채점기록` · `서버점검_미검증_질문유형` · `최종점검_30문항` / 채권·펀드: `채권_오답기록`·`채권_회신`·`채권_점검문항_10`·`펀드_회신`·`예상질문_15문항` |

> 🔴 **27 번은 B 가 아니라 사실상 A 다.** 다른 B 문서는 PDF 가 나오면 역할이 끝나지만,
> 27 번은 **PDF 의 원본**이다. 제출 항목 2(기술 제안서) 그 자체이므로 조판이 끝난 뒤에도 남긴다.

🔴 실측 기록은 **원고가 끝나도 남긴다.** §2.1.3 신뢰성과 §05 기대효과의 근거이고,
심사에서 "실제로 재어 봤나" 를 물으면 여는 자리다.

### B-2. 근거 문서 50개

| 경로 | 개수 | 무엇 | 어느 절에 |
| :-- | --: | :-- | :-- |
| `docs/eda/` | 23 | 도메인별 EDA 기록 | 2.1 정제 원칙 · 2.2 각 도메인 절 |
| `docs/research/` | 18 | 선행연구 정리(논문 11 + FIBO + 벤치마크 + 수집 과제 메모) | **부록 E 선행연구 인용표** |
| `docs/proposal/` | 5 | `NUMBERS.md`(**수치 단일 원천**) · 3층 프레임 · ETF 원고 8건 | 전 절 |
| `docs/rule_audit/` | 3 | 인버스 규칙 감사 csv | 2.2.3 ETF ④ |
| `docs/HOW_IT_WORKS.md` | 1 | 원리 설명 | README 초안 재료 |

---

## C. 보류 1건 — 판단이 필요하다

| 파일 | 크기 | 의견 |
| :-- | --: | :-- |
| ~~`(배포용)과제소개자료_금융상품Agent.pdf`~~ | — | 9/6 재계수에서 추적 목록에 없다. 판단할 것이 없어졌다 |
| `PROJECT.md` | 0.04MB | 팀 계획서. 제출물은 아니지만 **마감·API 기간·제출 요건이 정리돼 있어** "요건을 읽었다" 는 증거는 된다. 넣어도 손해는 없다 |

---

## D. 정리 후보 — 333개 24.0MB

### D-1. 지워도 안전하다고 판단한 것 — 107개 16.7MB

| 묶음 | 개수 | 크기 | 근거 |
| :-- | --: | --: | :-- |
| `eval/probe_*.json` · `*.txt` · `verdicts_*` · `enforce_*` · `rule_delivery_*` · `qids_*` | 103 | **16.0MB** | 라운드별 **원시 실행 로그**다. 평가 재현에 필요한 것은 `questions_*.jsonl`(A 에 포함)과 실행기이고, 이 로그는 그때의 출력이다. 결론은 오답기록·채점기록으로 옮겨졌다 |
| `notebooks/` | 4 | 0.72MB | 탐색용 주피터. 결론이 `docs/eda/` 로 옮겨졌고 런타임·빌드가 참조하지 않는다 |

⚠️ `eval/.gitkeep` 은 남긴다(빈 디렉터리 유지용).

### D-2. 소유자 판단이 필요한 것 — 223개 7.3MB

**내가 지우자고 말하지 않는다.** 작성자가 정한다.

| 묶음 | 개수 | 크기 | 작성자 | 성격 |
| :-- | --: | --: | :-- | :-- |
| `docs/` 최상위 md | 134 | 4.63MB | 리드·채권 위주 | 라운드 로그가 대부분(`recheck_*` 21 · `kg_structure_probe_*` 10 · `gold_probe_*` 5). `recheck_loop_2026-09-02`(786K)·`kg_structure_loop_2026-09-02`(396K) 둘이 1.2MB |
| `docs/핵심문서모음/` | 35 | 0.56MB | 리드 | 이름대로 큐레이션본. 다만 `review_2026-08-26`·`data_dictionary`·`ontology_rules` 를 **복사해 둔 것**이라 원본과 겹친다 — 어느 쪽을 남길지 판단 필요 |
| `docs/review_2026-08-26/` | 19 | 0.40MB | 전원 | 8/26 상호 검토 |
| `docs/ontology_rules/` | 13 | 0.09MB | 리드 | 온톨로지 규칙 12조. **2.2.1 설계 원칙의 근거일 수 있다** — 리드 확인 필요 |
| `docs/bench/` `docs/domain/` `docs/data_dictionary/` `docs/superpowers/` `docs/review_2026-09-02/` `docs/research/`(notes 밖) | 23 | 0.61MB | 혼재 | `data_dictionary` 는 2.1 에서 인용될 수 있다 |
| `.claude/agents/` `.agents/rules/` | 3 | 0.01MB | 전원 | 작업 도구 설정. 제출 불필요하나 **지우면 워크플로가 깨진다** — 남기기를 권함 |

### 내 트랙(ETF)에서 원고 후 역할이 끝나는 것 — 20개

위 D-2 의 `docs/` 최상위 116개 중 **아래 21개가 내 것**이다. 원고 확정 뒤 내가 정리한다.

| 묶음 | 파일 |
| :-- | :-- |
| 인계·요청 6 | `HANDOFF_ETF_2026-08-29` · `ask_lead_2026-08-28` · `ask_lead_2026-08-31` · `redeploy_request_2026-08-31` · `redeploy_request_2026-09-01` · (기술제안서 폴더의) `ETF_초안완료_전달_2026-09-03` |
| 시점 점검 로그 7 | `etf_v2_recheck_2026-08-25` · `etf_domain_check_2026-08-26` · `eval_gold_verify_etf_2026-08-26` · `eval_gold_verify_cross_official_2026-08-26` · `gold_review_2026-09-01` · `kg_dom_ticker_11_2026-08-30` · `kg_security_gap_2026-08-28` |
| 세션 메모 4 | `OUT_OF_BAND_CONTEXT_2026-08-29` · `PENDING_DECISIONS_ETF` · `meeting_2026-08-29` · `ontology_audit_2026-09-02` |
| 흡수돼 중복 3 | `notice_2026-08-24_impact` · `spec_audit_2026-08-24` · `qna_discord_2026-08-31` — 셋 다 `15_주최QA_반영점검` 에 정리됨 |

---

## E. ETF 트랙 최종 정리 (2026-09-06 16:40 · 병철)

내 트랙에서 무엇이 어디에 있고 무엇이 남았는지. **네 묶음으로만 본다.**

### E-1. 기술 제안서 — 원고는 확정, 렌더가 없다

| 무엇 | 상태 |
| :-- | :-- |
| `27_기술제안서_ETF판_전체원고.md` 62K | ✅ 6대 목차 전 절 완성(01 아키텍처 · 02 제안 방법 3문 · 03 구성도 · 04 흐름도 · 05 기대효과 · 06 부록 A~F) |
| PDF · 워드 | ❌ 없음. 28(채권판)만 html·pdf 가 있다 — **판 선택과 조판은 리드·채권** |
| 수치 최신성 | ✅ 27 은 리드가 최종본으로 갱신(섹터 축·TR 3표기·편입 표기 고정·처리 9단계 반영). 🔶 남은 것은 부재 선언 개수(해외 7·국내 4 · 합계 24)와 회귀 1,538 — 9/7 새벽 라운드 16 과 채권 추가분으로 또 바뀌었다 |

### E-2. 제안서가 참조하는 ETF md — 전부 `docs/기술제안서/`

| 파일 | 원고 어느 자리 |
| :-- | :-- |
| `ETF_오답_마스터기록.md` 68K | **부록 F** — 서버 실측 전건(라운드 14까지). §4.1 사고 서술·§5.3 한계의 원문 |
| `ETF_튜닝문항_20_2026-09-05.md` | 부록 F 정답표. 로컬 DB 계산값과 정정 이력 |
| `ETF_재투입_리스트_2026-09-06.md` | 재배포마다 무엇을 다시 던지는지. §5.4 개선 방안 3 |
| `ETF_오답기록_2026-09-03.md` · `최종점검_채점기록` · `최종점검_30문항` · `서버점검_미검증_질문유형` | 9/3~9/4 실측. §4.1 앞부분 |
| `05_도메인_ETF` · `16_ETF_본문원고` · `17`·`19_구축과정_ETF` · `26_ETF_최종원고` | 27 을 만든 재료. **27 확정 뒤에도 원고 대조용으로 남긴다** |
| `docs/proposal/ontology_engineering_etf.md` · `NUMBERS.md` | 온톨로지 엔지니어링 서술 · 수치 단일 원천 |

### E-3. 제출에 들어가는 ETF 코드 — 전부 A 묶음 안에 있다

| 무엇 | 경로 | 비고 |
| :-- | :-- | :-- |
| 런타임 | `src/runtime/pipeline.py` · `gate.py` · `guard.py` · `router.py` · `loader.py` · `wording.py` | 공용 파일이라 세 도메인이 함께 쓴다. ETF 확정식·가드는 여기 |
| 온톨로지 | `ontology/enums/domestic_etfs.yaml`(121K) · `overseas_etfs.yaml`(69K) + 각 `.vocab.yaml`·`.auto.yaml`·값 사전 txt | 규칙·게이트·범위의 단일 원천 |
| ttl(제출 요구) | `ontology/etf_kr.ttl` · `etf_gl.ttl` (+ 공통 `common.ttl`) | 생성물. 손으로 쓰지 않는다 |
| 코드북 | `ontology/codebooks/etf_sector.csv` | 섹터↔상품 축 |
| 외부 수집 | `scripts/fetch_etf_holdings.py` · `fetch_overseas_holdings.py` · `load_external_holdings.py` · `data/external/holdings/SOURCES.md` | 구성종목 질의의 원천. 출처·수집일 기록 포함 |
| 감사 | `scripts/audit_etf_claims.py` | 원고 수치 주장 전수 대조 |
| 회귀 | `tests/test_etf_*.py` 15파일 | 가드마다 정답 케이스·불개입 조건 |
| 평가셋 | `eval/questions_domestic_etfs.jsonl` 36 · `questions_overseas_etfs.jsonl` 28 | 부록 E 재현 절차가 가리킨다 |

### E-4. 원고가 끝나면 역할이 끝나는 내 것 — 19개 (`docs/` 최상위)

`HANDOFF_ETF_2026-08-29` · `ask_lead_2026-08-28` · `ask_lead_2026-08-31` · `redeploy_request_2026-08-31` ·
`redeploy_request_2026-09-01` · `etf_v2_recheck_2026-08-25` · `etf_domain_check_2026-08-26` ·
`eval_gold_verify_etf_2026-08-26` · `eval_gold_verify_cross_official_2026-08-26` · `gold_review_2026-09-01` ·
`kg_dom_ticker_11_2026-08-30` · `kg_security_gap_2026-08-28` · `OUT_OF_BAND_CONTEXT_2026-08-29` ·
`PENDING_DECISIONS_ETF` · `meeting_2026-08-29` · `ontology_audit_2026-09-02` · `notice_2026-08-24_impact` ·
`spec_audit_2026-08-24` · `qna_discord_2026-08-31` (+ 기술제안서 폴더의 `ETF_초안완료_전달_2026-09-03`)

🔴 **정리도 커밋이다.** 마감(9/6 23:59)까지 남은 시간에 제출물 손질과 정리를 맞바꾸지 않는다 —
이 19개는 제출 추출 대상(A)이 아니므로 **남겨 둬도 제출물에는 들어가지 않는다.** 지우는 편익이 0 에 가깝다.

### E-5. 남은 일 — 내 손 밖

1. 루트 `README.md`(엔드포인트 주소 포함) — 리드
2. 27 또는 28 을 PDF·워드로 — 리드·채권
3. 3차 재배포 뒤 재투입 채점 → 27 §4·§5 수치 일괄 갱신 — 내가
4. `00_README`·`02_목차` 에 27 등록 — 문서 소유자(채권)에게 요청

---

## 미해결 2건 — 원고 착수 전 결정 필요

**04 흐름도의 축이 주최 정의와 다르다.** 주최 04 는 "데이터 수집 → 온톨로지 설계 → 그래프 구축 → 검색"(구축 과정)인데
`02_목차` 의 04 는 질의 4개의 **응답** 흐름이다. 03 이 이미 파이프라인을 다루므로 겹치고, "수집→설계→구축" 이 어느 절에도 없다.
재료는 `17`·`18`·`19`·`20_구축과정_*` 넷이 이미 갖고 있다. **결정 주체: 리드**(04 취합 담당이 채권이라 임의로 바꾸지 않는다).

**테크세션 A-2 가 01 에 없다.** *"SQL 실행 성공률 ~100% vs 정답률 45%"* — 심사자가 자기 벤치마크를 찾는 자리인데
01 아키텍처에 정면 대응 서술이 없다. `21_테크세션…` 에 🔴 로 남아 있다. **담당: 리드.**

---

## 정리 순서 (제안)

1. ~~**지금** — D-1 만 정리~~ → **9/6 재판단: 하지 않는다.** D-1 107개(16.7MB)는 A 추출 대상이 아니라
   제출물에 애초에 들어가지 않는다. 마감 당일에 정리 커밋을 얹어 얻을 것이 없고, 되돌릴 시간은 없다
2. **제출 직전** — `python scripts/make_submission.py --out <경로>` 로 A 229개만 추출해 주최 저장소에 push.
   🔴 추출 전 루트 `README.md` 가 있는지 확인 — 없으면 경고만 내고 넘어간다
3. **09-06 23:59 이후** — 손대지 않는다
