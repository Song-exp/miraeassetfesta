# 최종 제출 명세서 — 주최 저장소(fin-120)에 올릴 것 (2026-09-07 · 트리플에이치)

> **이 문서가 답하는 것** — 제출 저장소에 **무엇을 올리고 무엇을 올리지 않는가**, 왜 그런가, 어떻게 옮기는가, 옮긴 뒤 무엇으로 확인하는가.
> **이 문서가 답하지 않는 것** — 도메인 하나를 처음부터 다시 만드는 절차(→ 서현 `채권_필요파일.md`) · 작업 저장소 전체의 분류(→ `25_문서_명세서.md`) · 마감 절차 전반(→ `최종제출_체크리스트_2026-09-06.md`).
>
> 저장소는 둘이고 그렇게 쓰기로 했다. **작업 저장소** `Song-exp/miraeassetfesta` 에는 전부 남기고,
> **제출 저장소** `miraeasset-aifestival-2026-product/fin-120` 에는 아래 목록만 올린다.
>
> 실측 기준 2026-09-07 새벽 · 합계 **234개 23.0MB** · 회귀 1,538건 통과(e7f5424).

---

## 1. 주최가 요구한 것과 우리 파일의 대응

| 주최 제출 항목 | 우리 파일 | 담당 |
| :-- | :-- | :-- |
| ① 소스코드 + 재현 가능한 환경 정의 | `src/` · `scripts/` · `requirements*.txt` · `Dockerfile` · `docker-compose.yml` | 전원 |
| ① 부속 — **README.md**(환경 구성 · 실행 명령어 · **API 엔드포인트 주소**) | 🔴 아직 없음 | 리드 |
| ① 부속 — **도메인별 온톨로지 파일** | `ontology/` (ttl 5분할 + yaml 원천) | 도메인별 |
| ② 기술 제안서 (PDF + 워드) | 저장소 밖 산출물 — 원고는 `27`·`28` | 조판 |
| ③ 평가용 API 명세서 | `docs/API_SPEC.md` | 채권 |

②는 저장소가 아니라 별도 제출물이라 아래 목록에 없다. ③은 `docs/` 밑이라 폴더 규칙으로는 안 잡혀 **예외로 포함**한다.

---

## 2. 올릴 것 — 234개 23.0MB

### 2.1 `src/` 16개 — 실제로 도는 코드 (공용)

| 파일 | 하는 일 |
| :-- | :-- |
| `api/main.py` · `api/chat_ui.py` · `chat.py` | `GET /answer` 계약 · 팀 확인용 채팅 화면 |
| `hcx/client.py` · `hcx/planner.py` | 하이퍼클로바 호출 · 의도 분석 · 조회식 생성 |
| `runtime/router.py` · `loader.py` | 상품군 판정 · 온톨로지와 지식그래프 적재 |
| `runtime/gate.py` | 답변불가 판정 — 부재 축 · 값 범위 밖 · 판정 불가 |
| `runtime/pipeline.py` | 9단계 본체와 가드 전부 (세 도메인 공용) |
| `runtime/guard.py` · `wording.py` · `qa_log.py` | 조회식 검사 · 답변 문구 · 처리 이력 |

**세 도메인이 한 파일을 공유한다.** 도메인 판정은 코드가 아니라 yaml 에 있고, 코드에는 문형별 확정식만 남아 있다.

### 2.2 `ontology/` 46개 20.0MB — 판단의 원천과 생성물

| 경로 | 개수 | 무엇 | 손으로 고치나 |
| :-- | --: | :-- | :-- |
| `enums/` | 10 | 도메인별 컬럼 판정 · 조회 규칙 · 부재 선언 · 값 사전 (채권 2 · 국내ETF 2 · 해외ETF 2 · 펀드 2 · 거절 1) | ✍️ **여기만 고친다** |
| `shared/` | 15 | 공유 개체(운용사 · 지수 · 지역 · 등급 · 종목)와 값 사전 | ✍️ 일부 · ⚙️ 일부 |
| `codebooks/` | 15 | 밖에서 가져온 지식 — 등급 서열 · 운용사 · 수탁사 · 속성 코드 · 지수 · ETN 발행사 | ✍️ 출처와 기준일 필수 |
| `axes/` | 1 | 축 선언 | ✍️ |
| `*.ttl` | 5 | **제출 요구 항목.** `common` + 도메인 4개 | ⚙️ 생성물, 손대지 않는다 |

ttl 은 `build_ontology.py` 의 출력이다. yaml 을 고치고 빌드를 돌리면 다시 만들어진다.

### 2.3 `scripts/` 49개 — 재현과 수집

| 무엇 | 파일 |
| :-- | :-- |
| 빌드 | `build_db.py` · `build_ontology.py` |
| 외부 수집 | `fetch_etf_holdings.py` · `fetch_overseas_holdings.py` · `fetch_prospectus_*.py` · `load_external_*.py` |
| 주장 검증 | `audit_etf_claims.py` · `audit_bonds_*.py` · `audit_funds_claims.py` |
| 제출 추출 | `make_submission.py` |

주최 Q&A 가 "수집 방법과 적재 흐름을 기재하라" 고 했다. 그 답이 이 폴더와 제안서 2.1 절이다.

### 2.4 `tests/` 81개 — 회귀 1,538건

ETF 15 · 채권 4 · 펀드 4 · 라운드별 48 · 라우터 1 · 나머지 공용.
제출 요건은 아니지만 **정성평가에서 "제대로 만들었나" 의 증거**다. 가드마다 정답 케이스와 불개입 조건이 붙어 있다.

### 2.5 `eval/` 29개 — 평가셋과 실행기

문항 파일 9개(jsonl)와 실행기 20개(py)만 올린다. 같은 폴더의 라운드 실행 로그 103개(16MB)는 **제외**한다 — 그때의 출력이지 재현에 필요한 것이 아니다. 결론은 오답기록으로 옮겨져 있다.

### 2.6 나머지 13개

`deploy/Caddyfile` · `deploy/deploy.sh` · `data/external/holdings/SOURCES.md` · `data/external/lookups/credit_grade_scale.csv` ·
`docs/API_SPEC.md` · 최상위 8개(`Dockerfile` · `requirements.txt` · `requirements-dev.txt` · `docker-compose.yml` · `.dockerignore` · `.env.example` · `.gitignore` · `.gitattributes`).

---

## 3. 올리지 않는 것과 그 이유

| 무엇 | 개수 | 왜 |
| :-- | --: | :-- |
| 원본 엑셀 · SQLite DB | — | 애초에 추적하지 않는다. 스크립트로 다시 만든다 |
| `eval/` 라운드 실행 로그 | 103 (16MB) | 그때의 출력. 재현에 필요한 것은 문항과 실행기다 |
| `docs/` 대부분 | 400여 | 오답기록 · EDA 노트 · 라운드 로그 · 제안서 재료. 제출물이 아니다 |
| `notebooks/` | 4 | 탐색용. 결론이 `docs/eda/` 로 옮겨졌다 |
| 주최 배포 PDF | 2 | 주최가 준 자료다. 되돌려줄 이유가 없다 |

**그래서 작업 저장소를 정리할 필요가 없다.** 안 올리는 것들은 애초에 추출 대상이 아니다.

---

## 4. 옮기는 절차

경로가 깨질 일은 없다. 추출기가 `git ls-files` 의 **상대 경로 그대로** 복사하므로 폴더 구조가 원본과 같게 재현된다.

```bash
# 0) 무엇이 뽑히는지 먼저 본다 — 복사도 삭제도 하지 않는다
python scripts/make_submission.py --list

# 1) 제출 저장소를 빈 폴더에 새로 clone 한다
git clone https://github.com/miraeasset-aifestival-2026-product/fin-120.git ../fin-120

# 2) 그 안으로 추출한다 (원본은 그대로 둔다)
python scripts/make_submission.py --out ../fin-120

# 3) 옮긴 쪽에서 임포트가 되는지 본다 — 경로가 온전하다는 뜻이다
cd ../fin-120 && python -c "import src.runtime.pipeline; print('ok')"

# 4) 커밋하고 올린다
git add -A && git commit -m "제출: 트리플에이치 금융상품 Agent" && git push
```

🔴 **지금 작업 저장소에서 remote 만 바꾸지 않는다.** 오답기록과 라운드 로그까지 전부 따라간다.
🔴 README 가 없으면 추출기가 **경고만 내고 넘어간다.** 만든 뒤 다시 뽑아야 한다.

---

## 5. 올린 뒤 확인할 것

| # | 확인 | 통과 기준 |
| :-: | :-- | :-- |
| 1 | 파일 수 | 234개 + README = 235개 |
| 2 | 임포트 | `import src.runtime.pipeline` 성공 |
| 3 | 온톨로지 | `ontology/*.ttl` 5개 존재 |
| 4 | API 명세 | `docs/API_SPEC.md` 존재 |
| 5 | README | 최상위에 있고 **엔드포인트 주소가 적혀 있다** |
| 6 | 서버 | `GET /answer` 가 5필드로 응답 |

제출본만으로 처음부터 재현하려면 `build_db.py` → `build_ontology.py` 순으로 돌린다(제안서 부록 E).

---

## 6. 담당별 마감 시점 잔여

| 담당 | 남은 일 |
| :-- | :-- |
| 리드 | 루트 README(엔드포인트 주소) · 구글폼 제출 · fin-120 반영 |
| 조판(채권) | 제안서 PDF + 워드 두 파일 |
| 서현 | 제안서 채권 절 부재 선언 개수 확인(문서 11 · yaml 8) |
| 병철 | 재배포 후 ETF 20문항 재검 · 27 원고 ETF 잔여 수치 |
