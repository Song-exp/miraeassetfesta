# 채권 도메인 원고 회신 — `04_도메인_채권.md` (2026-09-03 · 채권 담당)

> **대상** [`ETF_초안완료_전달_2026-09-03.md`](ETF_초안완료_전달_2026-09-03.md) 의 "채권 `04` §A + ttl·yaml 발췌" 요청 · [`펀드_회신_2026-09-03.md`](펀드_회신_2026-09-03.md) §0 "미도착" 행
> **커밋** `560e47a` (1차) → `4cb262b`·`e4ed4e5`·`eaaa74d` (09-03 밤 보강·재점검) · 브랜치 `main`
> **읽는 순서** §1 이 원고의 내용 요약, §2 가 정정 목록, §3 이 결정 요청이다. 원고 자체는 `04_도메인_채권.md`, 수치 정본은 `../proposal/NUMBERS.md §1 국내채권`.

## 0. 요청 대비 처리

| 요청 | 상태 | 위치 |
| :-- | :-: | :-- |
| §A 데이터 단락 (→ 2.1.1, ETF 담당이 통합) | ✅ | 04 §A · 13 통합본에 기계 조립 |
| ttl·yaml 발췌 (→ 부록 A·B·C) | ✅ | 04 §E.1~E.4 · 14 취합본 |
| §B 결함→선언 · §C 흐름도 · §D 현업 지점 | ✅ | 04 §B~§D |
| 수치 NUMBERS 대조 | ✅ | 생성기 편입, 전건 일치 (§2) |
| "EDA → 결측 판정 → 선언" 프로세스 서술 (주최 8/26 답변 대응) | ✅ | 04 §A.1·§A.2·§B.1 (09-03 밤 추가) |
| 횡단 장 채권 몫 — 흐름도 취합 · 기대효과 · API 명세 | ✅ 초안 | `10_흐름도_취합.md` · `11_기대효과_확장성.md` · `../API_SPEC.md` |
| 결측·판정 전수 대조 (DB ↔ yaml ↔ 원고) | ✅ **반영 완료 (09-04)** | §1.5 — 컬럼 단위 재대조 36 중 문서 밖 18 → 블록 2줄로 §A 8줄 · yaml 누락 2건 반영 |
| **문서 전수 검사 — §1.2~§1.6·§2~§5 · 타 문서 교차** | ✅ **09-04** | 정정 #17·#18 신설 · 결정 ⑫⑬⑭ 신설 · §4 에 18 초안 답 3건 |
| yaml 판정 → 챗봇 전달 점검 (런타임 실측) | ✅ **09-04 신설** | §1.6 — `columns:` 는 런타임 미사용 · 미전달 9컬럼 중 7 을 `answer_rules` 1줄로 닫음 · 잔여 결정 §3-1 ⑪ |
| 리드 결정 | 🟡 | §3-1 (**14건** — 09-04 에 ⑪~⑭ 추가) |
| ETF 담당 확인 | 🟡 | §3-2 (3건) |

---

## 1. 채권 온톨로지 — 원고가 말하는 것

### 1.1 데이터에서 판정으로 (§A → 2.1.1)

국내채권 마스터는 21,882행 × 58컬럼이고, 4개 상품군 중 유일하게 외부 수집이 없다. 대신 마스터에 없는 지식 하나를 코드북으로 들여왔다 — **신용등급 서열표**(`credit_grade_scale.csv`, 한국기업평가, as_of 2026-08-20). 값을 고친 것은 없고 **판정**만 했다.

| 데이터가 이랬다 | 판정 | 선언 자리 |
| :-- | :-- | :-- |
| 한 종목이 장내·장외 2~4행 — 1,078종목 | 행 ≠ 종목, 종목 수는 `COUNT(DISTINCT pd_no)` | `row_grain` · 규칙 `대표행` |
| 할인채 689행의 표면금리 란은 발행 할인율 | 이표금리와 한 축에 놓지 않는다 | 규칙 `이자유형분리` |
| 위험등급 `'00'` 19행 실재 · 6등급 40.8% | 범위 0~6, 0 은 결측이 아니라 값 | `range_by_table` |
| 신용등급 결측 4,020행(18.4%) | 국공채 2,840 은 "미부여"(정상) · 특수채·회사채 1,180 은 "미수록" | `crd_grd.answer_policy` |
| 발행국 KR·통화 KRW 가 상수 | 지역·통화 축 없음 | `kg_entity: null` · ABSENT |
| `buyable_quantity` >0 296 · =0 338 | 주최 공지로 무효, 판매 가능 = 만기 미경과 | 규칙 `구매가능` |
| 판매정보 13컬럼이 같은 21,248행에서 통째로 NULL — 값 있는 634행은 전부 장외 LOT | 컬럼 13개의 결측이 아니라 사실 하나("지금 파는 물건이 아니다") — 판매 축 답변은 모수 634/21,882 고지 | 규칙 `판매행` · `answer_rules` (09-04 신설) |
| 평가정보 8컬럼이 같은 16행에서 통째로 NULL — 전부 장외, 만기는 미래 | 만기 경과가 아니라 장외 평가정보 미수록 — 정렬·비교 제외 | 규칙 `듀레이션정상` · `row_grain.대표행` |
| 장내 종가 `exg_close_price` — 장외 4,136행 NULL, 장내 17,746행 중 값 있는 행 1,270(7.2%) | 장외는 "해당없음", 장내는 모수 1,270 명시 + 종가 기준일 병기(월말 정체 종목 다수) | `exg_close_price` `mixed` · `exg_close_price_base_dt` |
| 채권종류 `bd_knd` 결측 152행 · 발행사 `pd_pbcm` 결측 149행 (**149행이 같은 행** · 종류만 결측인 3행 별도) | "미수록" + ISIN 앞 3자리로 대분류만 보완(STRIPS 원금분리 21행 포함) | `bd_knd`·`pd_pbcm` `missing` · `name_encoding.isin_prefix` |
| 문자열 패딩 — 2차에 실제로 패딩이 낀 **7컬럼**(`pd_abrv_nm` 21,837 · `pd_eng_nm` 21,842 · `pd_abrv_eng_nm` 21,814 · `bd_knd` 21,682 · `pd_pbcm` 21,282 · `exg_close_price_base_dt` 16,453 · `pd_nm` 684) — `trim_columns` **선언**은 13컬럼(나머지 6은 예방) | 값은 안 고치고 조회 시 TRIM (없으면 정확일치가 조용히 실패) | `normalization.trim_columns` · 규칙 `문자열비교` |
| 표면금리 한 컬럼에 세 의미 — 고정 20,904 / 변동 830 / 할인 689 | 변동금리 830행은 "기준일 현재 적용금리" 스냅샷 — 셋을 한 축에 정렬 금지 | `srfc_irt.trap` · `bd_inrt_tcd.value_semantics` |
| 등급 부여일 `crd_grd_dt` — 최댓값 20260821 · 등급 없는데 일자만 있는 16행 | '최종 변경·부여일' 이지 평가일이 아님 — 신선도 판단 근거로 사용 금지 | `crd_grd_dt.answer_policy` |
| 세후수익률 > 매수수익률 72행 (세후 = 예금환산154 × 0.846, 634/634) | 할인 매수 비과세 구조라 정상 — 오류 단정 금지 | `after_tax_yield` |
| 만기보장수익률 — 코드 `'99'` 97.5% · 값 >0 256행 · 코드 `'99'` 인데 값 있는 4행 | 256행만 답변 대상, 모순 4행 사용 금지 | `exrt_grte_ern_r`·`exrt_grte_ern_r_tcd` |
| 거래가 `trade_price` = 평가가 `eval_price` 인 행 0 | "얼마에 살 수 있나" 는 민평이 아니라 거래가 | `trade_price` |
| 중복 8종목은 장외행의 종류·등급·발행사·듀레이션이 NULL | 대표행은 장내행 우선 — 장외행을 고르면 결측으로 답함 | `row_grain.대표행` |
| 장외 판매 LOT — `info_seq` 2 가 307행 · 3 이 1행 | 같은 종목이 매수수익률·단가만 다른 줄 — 값 다르면 병기 | `info_seq` · 규칙 `대표행` |
| 이름에서 읽는 것 — 특수구조 10종 3,581행 · ESG `(사)` 1,984 · 퇴직연금 Y 1,931 · 발행일 결측 26 · 만기일 결측 5 | `(사)` 는 사모가 아니라 사회적채권 라벨 · 특수구조는 제외하지 않고 표시만 | `name_encoding` · `pd_pen_tr_yn`·`isu_dt`·`mat_dt` |

위 표의 앞 8줄이 원고 §A 본문에 실린 것이고(09-04 에 블록 2줄을 넣어 6→8줄), 뒤 11줄은 yaml 에는 있으나 원고에 없던 판정이다(대조 과정은 §1.5). 원고 §A.1 유형표에 이미 있는 `dirty` 복사 위장 · 세후평균수익률 전량 0 · `dur` 센티넬 · 통화 `'000'` 은 여기서 뺐다.

**결측 판정 프로세스(§A.1·§A.2, 09-03 밤 추가).** "비어 있다" 의 뜻을 일곱 유형으로 갈라 판정했고, 2차 데이터에서 같은 조건식을 다시 돌려 유형표를 갱신했다(④ 복구 가능은 컬럼이 사라져 소멸). 대표 사례는 신용등급 결측 — 국공채 2,840행 중 2,840 이 결측이라 "해당없음", 나머지 1,180 은 "미수록". 같은 NULL 이 두 뜻으로 갈리므로 답변 문장이 달라야 하고, 이것이 yaml `missing_reason`·`answer_policy` 한 줄로 선언되어 런타임에 그대로 나온다. 기각한 대안(대치 · "확인 불가" 일괄 · 조용한 제외)과 그 근거 실측도 적었다.

### 1.2 결함 → 선언 (§B.2 → 2.2.2)

| # | 데이터의 결함 | 선언 | 층 |
| :-: | :-- | :-- | :-: |
| 1 | 등급 서열이 데이터에 없다 · 사용자 `AA` = DB `AA0` | 표준표 rank 1~19 + 밴드 2 + closure · 규칙 `등급서열` · 가드 `expand_grade_comparison` | 1+2+3 |
| 2 | `'00'` 19행 실재 · 6등급 40.8% · 펀드는 NULL≠0 | `range_by_table` 채권만 0~6 — ttl 제약과 게이트 상수를 한 선언에서 생성 | 3 |
| 3 | 발행국 KR 21,881/21,882 · 통화 KRW | `hasRegion` ABSENT + 규칙 `외화채없음` + 게이트 `curr_cd` 상수 | 3+2 |
| 4 | "위험한 채권" 이 세 축(투자위험등급/신용등급/금리위험)이고 ②③ 은 정반대 | `clarify.다의어.위험` + 결정층 되묻기 `risk_ambiguity_clarify` | 2+결정층 |
| 5 | "판매 가능" 정의 없음 · `buyable_quantity` 무효 · as-of 8/22 ≠ 결제일 8/24 | 규칙 `구매가능` = `curr_cd='KRW' AND mat_dt >= 20260824` | 2+가드 |
| 보조 | 할인채 표면금리 · 영구채 `mat_dt` = 콜 개시일 | `couponRate` 주석 · 규칙 `이자유형분리`·`듀레이션정상` | 2 |

건마다 "데이터가 이랬다 / 그래서 이렇게 / 기각한 대안 / 런타임에서 / 실측 사례 / 안 지키면" 여섯 줄이다.

**🔴 이 6건은 전수가 아니라 선별이다 (09-04 점검).** 채권에 붙은 선언은 `query_rules` **41**(09-04 오후 `정렬축` 추가 — 다른 세션) · `answer_rules` 21 · 결정층 가드 19인데, 위 6건이 이야기하는 가드는 `expand_grade_comparison`·`ensure_cutoff_inclusive`·`risk_ambiguity_clarify` **3개뿐**이다(19 중 3). 원고 §B.2 도입부에 채택 기준은 있었지만 ⓐ 회신 §1.2 에는 그 문장이 없어 6건이 전부로 읽혔고 ⓑ 원고의 배제 목록("대표행·수익률 다의어·이자유형 분리")은 낡아 있었다 — 이자유형 분리는 실제로는 **B.2-6 보조로 채택**됐고, 빠진 것은 그 셋이 아니라 아래 7부류다. 09-04 에 원고 §B.2 도입부를 정정하고 미채택 표를 넣었다(§2 정정 #15).

| 채택하지 않은 결함 부류 | 선언 | 층 | 원고에 있나 |
| :-- | :-- | :-: | :-- |
| 행 ≠ 종목 · 장외행 NULL | `row_grain` · `대표행`·`시장집계금지` | 2 | §A 표 ✅ |
| 적재 형식 — 날짜 REAL · 고정폭 패딩 7컬럼 | `날짜표기`·`문자열비교`·`trim_columns` + 가드 3 | 2+3 | 🔴 "패딩" 은 원고에 **0회** · 날짜는 B.2-5 "안 지키면" 에만 |
| NULL 이 논리식을 삼킨다 (`NOT(a OR b)` → 국공채 2,840행 탈락) | `고위험제외` COALESCE 확정식 + `ensure_reco_exclusions` | 2+3 | 🔴 "COALESCE" 원고 **0회** |
| 값이 통칭과 다르다 (통안증권·STRIPS `bd_knd` 결측) | `종류필터` 확정식 3종·`bd_knd_alias` + `ensure_ktb_kind` | 2+3 | 🔴 거의 없음 |
| 표기가 컬럼에 없다 (정부보증·ESG·특수구조는 이름에만) | `신용보강`·`ESG라벨`·`공모사모판정`·`구조표시`·`name_encoding` + `ensure_credit_backstop` | 2+3 | 🔴 "정부 보증"·ESG·`name_encoding` 원고 **0회** |
| 컬럼이 정보를 안 담는다 (`dirty` 복사 · 장내종가 0값 16,476 · 판매/평가 블록) | `더티금지`·`장내종가`·`판매행`·`answer_rules` | 2 | §A·§A.1 ✅ (블록은 09-04) |
| 다의어 '싸다' | `clarify` + `price_ambiguity_clarify` | 2+결정층 | §B.0 한 마디 |

판단: 분량(2.5쪽) 때문에 **선별 자체는 옳다**. 문제는 "6건이 전부처럼 보이는 것" 이었고, 미채택 표 한 장으로 닫았다 — 오히려 "결함마다 층이 붙어 있고 그 수가 40·21·19" 라는 사실이 제안서에는 더 센 주장이다. 한계(§B.3)에는 등급 서열 확장 가드의 목록이 코드 상수라 KG rank 와 이원화된 사실(§3-1 ①)과, 공식 예시 #1 답변이 무등급 모수 밖을 아직 고지하지 않는 사실(§3-2 ②)을 그대로 적었다.

### 1.3 KG 구성 (§B.1)

| 연결 | 컬럼 | 값 수 | 비고 |
| :-- | :-- | --: | :-- |
| Organization (hasIssuer) | `pd_pbcm` | 1,818 alias → 노드 1,817 | `gen_shared_auto.py` 가 `(주)`·`주식회사`·공백을 뗀 법인 키로 병합 |
| CreditGrade | `crd_grd` | 15 (표준등급 20 · 노드 21 = 등급 19 + 밴드 2) | 코드북 rank → `skos:broader` closure |
| RiskGrade | `pd_risk_gcd` | 7 (0~6) | `range_by_table` |
| Currency | `curr_cd` | 1 (KRW) | `'000'` 1행은 등재 금지 |
| ⊘ **ABSENT 선언** — AssetClass · Index · Region (+ 속성 `CreditGradeHistory`) | — | — | ttl ABSENT 4줄 = 개체 3 + 속성 1 · 게이트가 어휘로 HCX 0회 기각 |
| ⊘ **컬럼은 있는데 개체로 안 올림** (`kg_entity: null`) — `pd_ctry_cd` · `std_pd_mcls_nm` · `pd_exg_mkt` | 3 | — | 세 번째 상태다. 발행국은 `Country` 노드 17이 실재하는데도 안 이었다 — 상수라 축이 안 되고, 대신 `Region` ABSENT + 게이트가 받는다 |
| — **해당 없음** — Country · Security · Fund · FundAttribute | — | — | 다른 상품군 개체. 원고 E.4 는 ⊘ 와 — 를 갈라 적는다 |

노드는 전부 빌드 산출물이고, 사람이 쓴 것은 "어느 컬럼 값이 이 개체인가" 와 코드북뿐이다. 단 그 포인터가 두 군데로 갈린다 — `pd_pbcm`·`curr_cd`·`pd_risk_gcd` 는 **enums 의 `kg_entity`** 로, CreditGrade 는 **`shared/credit_grade.yaml` 노드별 `aliases`** 로 적혀 있다. 이 비대칭 때문에 `crd_grd.kg_entity` 가 `null` 인 채 남아 있었고(주석도 "채권 전용 → SQL 컬럼으로 충분" 이라는 08-30 코드북 도입 전 판단), 실제로는 노드 21·closure 19 를 쓰는 §B.2-1 의 1층 주인공이다. **09-04 에 `kg_entity: CreditGrade` 로 정정**했다(§2 정정 #16 · `build_ontology.py --check` 변경 전후 errors 0 · warnings 8 동일) — alias 는 shared 에서 오므로 KG 산출물·런타임은 그대로고, 커버리지 매트릭스(부록 C)가 이 컬럼을 세게 된다. 빌드 검증 V1~V7(죽은 alias · raw 값 충돌 · 포인터 미완 · 코드북 출처·기준일 · 컬럼 실존 · ABSENT 인데 컬럼 실재 · alias 확장 대상 없음)이 산출물을 거부한다. 🔴 단 **V4(코드북 출처·기준일)는 채권 코드북을 보지 않는다** — `build_ontology.py` 는 `ontology/codebooks/`(15개) 만 순회하는데 `credit_grade_scale.csv` 는 `data/external/lookups/` 에 있다. 파일 자체는 규정의 뜻은 지킨다(행마다 `source_url` 과 `as_of` 2026-08-20 ≤ 기준일 2026-08-24). 다만 V4 는 컬럼 이름을 `source` 로 찾아서, 경로를 고쳐도 이 폴더 7개가 전부 걸린다(09-04 실측). 결정 §3-1 ⑭.

### 1.4 흐름도 · 현업 · 부록 (§C~§E)

- **§C 흐름도(→ 4.1)** — 공식 예시 #1 "판매 가능한 원화채권 중 AA- 이상". 09-03 서버 `think_trace` 10단 원문, 2.4초, 30행. Route → Ground(`CG_AAm`·`Curr_KRW`) → Gate 통과 → Plan → **[Guard] 등급 서열 확장** → 검증 → 기계 조립(HCX 0회). 표 형식은 리드의 4.4 와 같은 7단 세로표.
- **§D 현업 지점(→ 5.2)** — 등급 체계 교체 = 코드북 교체 + 빌드 1회. 결측 등급을 "미부여 / 미수록" 으로 구분해 답하는 것이 설명의무 관점의 가치. 현장 접점은 고객 적합성 확인이며, 규제 문구가 코드가 아니라 `answer_rules` 에 있어 컴플라이언스가 직접 고칠 수 있다.
- **§E 부록** — E.1 `bond_kr.ttl` 전문(원문과 바이트 일치 확인) · E.2 ABSENT 4건 표 · E.3 yaml 발췌(**축약본** — 규칙 원문은 수백 자라 핵심 절만, 각주로 레포 yaml 지시) · E.4 값 수 표 · §F 재현 SQL 21건(제안서 미수록). **09-04 재확인** — E.1 ttl 은 `ontology/bond_kr.ttl` 과 2,426자 바이트 일치 ✅ · §F 21건 전건 실행 성공 + 주석 기대값 전건 일치 ✅(괄호 기대값 4건 `18.4%`·`15,806`(8/22 판정)·`668`(할인채 `srfc_irt`>0)·`101`(dur 위반 중 영구채)도 별도 실측 일치). §1.4 는 손볼 것 없음.

### 1.5 전수 대조 — DB ↔ yaml ↔ 원고 (09-03 밤 · **09-04 재대조로 갱신**)

§A 본문 표가 판정의 전부인지 확인하려고 세 방향으로 대조했고, 09-04 에 컬럼 단위로 다시 셌다.

| 대조 | 방법 | 결과 |
| :-- | :-- | :-- |
| DB ↔ yaml | 58컬럼 전부 NULL·빈 문자·0값·패딩을 실측해 `missing_reason`(해당없음 16 · 미수록 14 · 혼합 6 · 정상 22)과 대조 | **어긋난 컬럼 0.** 09-04 재실측에서도 58/58 동일 |
| yaml 수치 ↔ DB | yaml 에 적힌 2차 실측치 26건 + 특수구조 이름 플래그 11종 count 재현 | **전건 일치** |
| yaml ↔ 원고 §A | yaml 판정 블록을 §A 표·§A.1 유형표와 대조 | 09-03 판정 11건 누락 → §1.1 표로 회수 |
| **컬럼 단위 재대조 (09-04)** | 위 회수분을 컬럼으로 환산해 58컬럼 각각이 원고·회신에 이름으로 나오는지 셈 | **결측 판정 36컬럼 중 문서화 18 / 문서 밖 18** (+ `missing_reason: none` 이나 위장결측·함정 판정이 있는 4컬럼) |

**"11건 회수" 는 표를 채운 것이지 전수가 아니었다.** 판정을 컬럼으로 환산하면 22컬럼이 여전히 문서 밖이었고, 그 대부분이 두 덩어리로 묶인다.

| 덩어리 | 실측 | 문서화 / 밖 | 문서 밖 컬럼 |
| :-- | :-- | :-: | :-- |
| 판매정보 블록 13컬럼 | 13컬럼 **동시 NULL 21,248 = 하나라도 NULL 21,248** · 값 있는 634행 전부 장외 | 6 / 7 | `corp_pretax_yield` `corp_after_tax_yield` `pref_tax_yield` `depo_equiv_yield_495` `bdbns_abl_chnl_nm` `bdbns_abl_chnl_tcd` `sale_yield_base_dt` |
| 평가정보 블록 8컬럼 | 8컬럼 **동시 NULL 16 = 하나라도 NULL 16** · 전부 장외 · 만기 경과와 교집합 0 | 2 / 6 | `cov` `ndy_dur` `ndy_cov` `ndy_dirty` `ndy_eval_price` `ndy_applied_yield` |
| 장내 종가 쌍 | `exg_close_price`·`exg_close_yield` 가 NULL 4,136·0값 16,476 로 같은 행 | 1 / 1 | `exg_close_yield` |
| 이름 3컬럼 | 결측 14 · 5 · 14 — 정식명 대체 표기 | 0 / 3 | `pd_abrv_nm` `pd_eng_nm` `pd_abrv_eng_nm` |
| 잔존일수 | NULL 5 · 0값 49(만기 도래분, 값) | 0 / 1 | `remaining_days` |
| (none 인데 판정 있음) | 위장결측 0값 `applied_yield` 201 · `isu_bal_amt` 325 · `bd_tisu_a` 259 / `pd_exg_mkt` 🔴 trap | — / 4 | 위 4컬럼 |

**결측의 단위는 컬럼이 아니라 행이다** — 이것이 09-04 대조의 결론이고, 원고 §A·§A.1·NUMBERS 에 반영했다(§2 정정 #11·#12). 판매정보 13컬럼의 NULL 은 결함 13개가 아니라 "지금 파는 물건이 아니다" 라는 사실 하나이고, 평가정보 8컬럼도 "장외 평가정보가 없다" 하나다.

**yaml 에조차 없던 것 — 2건, 둘 다 09-04 반영 완료.** ⓐ 등급값은 있는데 부여일이 없는 3행 → `crd_grd_dt.answer_policy` 에 "일자 병기 생략" 한 줄. ⓑ `remaining_days` NULL 5행(만기일 미수록 1 + `mat_dt` 0 센티넬 4) → `missing_semantics: {null: not_applicable}` 한 줄. yaml 변경이므로 push 뒤 `deploy.sh` 필요.

### 1.6 그 판정이 지금 챗봇에 닿고 있나 — 런타임 전달 점검 (09-04 신설)

컬럼 판정이 **문서에 있다**와 **챗봇이 그대로 행동한다**는 다른 문제다. `src/runtime/loader.py` 를 실측해 enums yaml 중 런타임이 실제로 읽는 슬롯을 셌다.

| yaml 슬롯 | 어디로 가나 | 채권 분량 |
| :-- | :-- | --: |
| `query_rules` 41 | 플래너(HCX SQL 생성) `planner_context` | 17,863자(40 기준 실측 · 41번째 `정렬축` 반영 전) |
| `answer_rules` | 답변 생성기 `answer_context` | 2,780 → **3,15x자** (09-04 +1줄) |
| `clarify` · `synonyms` · `normalization` | 플래너 · 되묻기 | 1,207 · 487 · 727자 |
| `gate_constants` · `absent_properties` | 게이트(HCX 0회 기각) | 425 · 163자 |
| `*.vocab.yaml` | 값 사전(리터럴 검사) | 2,686자 |
| **`columns:` 23,358자** | **읽지 않는다 — 딱 하나 `crd_grd.value_semantics`(등급 화이트리스트)만** | — |

즉 `missing_reason`·`missing_semantics`·`answer_policy`·`trap`·`note` 는 **사람이 읽는 판정문**이고, 챗봇의 행동이 되려면 그 판정이 규칙 슬롯(주로 `query_rules`·`answer_rules`)으로 한 번 더 옮겨져야 한다. 대부분은 이미 옮겨져 있었지만, **결측·함정 판정 40컬럼 중 9컬럼은 어느 슬롯에도 없었다.**

| 컬럼 | yaml 판정 | 런타임 전달 | 09-04 조치 |
| :-- | :-- | :-: | :-- |
| `corp_pretax_yield`·`corp_after_tax_yield`·`pref_tax_yield` | "값만 인용, 산식·세율 서술 금지 — 산식 확정 불가" | ❌ | `answer_rules` 신설 1줄에 편입 |
| `depo_equiv_yield_154`·`depo_equiv_yield_495` | 예금·은행 환산 세율 기준 | ❌ | 〃 |
| `bdbns_abl_chnl_nm` | "'온라인으로 살 수 있나' 는 634건 전부 온·오프 겸용으로" | ❌ (답변 지시인데 전달 안 됨) | 〃 |
| `sale_yield_base_dt` | 634행 전부 20260821 | ❌ | 〃 |
| `cov`·`ndy_cov` | "일반 질의에 거의 안 씀" | ❌ | 무해 판정 — 그대로 둔다 |

신설한 `answer_rules` 한 줄이 판매정보 13컬럼의 **모수 고지**(634/21,882)까지 함께 싣는다. 지금 이 부류는 공식 예시 #1 의 잔여 🟡("무등급 4,020행 모수 밖 미고지", §3-2 ②)과 **같은 결함 부류**다 — 모수를 안 밝히고 "가장 높은" 을 말하는 것.

**이건 채권만의 일이 아니다.** 같은 날(09-04 13:19) ETF 담당이 `8eee63f` 로 같은 부류를 고쳤다 — `ru_mkt_price` 가 이름과 달리 실시간이 아니라 종가 복사본(국내 1,774/1,780 동일)이라는 판정이 `columns` 메타에만 있어 프롬프트에 안 실렸고, 답변기가 8/21 종가를 실시간 시세로 팔 수 있었다. 두 도메인이 독립적으로 같은 구멍을 밟았으니 **`columns` 판정 → 규칙 슬롯 승격 여부**는 4도메인 전수로 한 번 훑을 값이 있다(§3-1 ⑪ 과 함께 결정).

🔴 **남은 구멍 하나 — 스키마는 58/58 전부 HCX 에 노출된다.** `public_funds` 만 `schema_exclude` 4컬럼을 쓰고 채권은 0이라, 97.1%가 비어 있는 판매정보 컬럼도 플래너가 자유롭게 고를 수 있다. 결정 요청 §3-1 ⑪.

---

## 2. 정정 사항 — 수정 전 → 후

| # | 자리 | 수정 전 | 수정 후 | 근거 |
| :-: | :-- | :-- | :-- | :-- |
| 1 | 템플릿(9/2) 신용등급 결측률 | 41.1% | **18.4%** (4,020/21,882) | 41.1·41.6% 는 1차(7/11) 데이터 값 |
| 2 | 템플릿 채권 `query_rules` 수 | 38 · 오전 회신 39 · 03_구성도 25 | **40** | 9/3 오후 `무이자질의`·`영구채필터` 2건 추가. 39 는 주석 줄을 센 오기, 25 는 구본 |
| 3 | 템플릿 신용등급 표준표 | "21등급" | 표 21행 · **표준등급 20**(C0/C 동일) · KG 노드 21 = 등급 19 + 밴드 2 | D 등급은 2차 0건이라 노드 없음 |
| 4 | gold note OFFICIAL-001 모수 | 15,806 (8/22 판정) | **15,792** (8/24 판정) | 서버 답과 일치 |
| 5 | 04 §A.1 EDA 가이드 인용 | §2 | **§3** | 결측 판정 절은 "3. 2단계 — 개인 탐색" 아래 |
| 6 | 04 §B.3-1 | 무등급 고지 미집행 언급 없음 | G-C′ 미집행 사실 추가 | §A.2 가 §B.3-1 을 가리키는데 해당 문장이 없었음 |
| 7 | 프로브 문항 수 | 회신 27 · 04 체크리스트 22 | **29** (BR-P 11 · BR-X 11 · BND-S 7) | `eval/probe_bond_recheck.txt` 실측 |
| 8 | NUMBERS §1 | 등급 조건 질의 모수 없음 | 17,862 (`answerable_n`) 추가 | §A.2 가 인용하는데 정본에 없었음 |
| 9 | 04 §E.3 | 축약 표시 없는 yaml 블록 · 회신 "그대로 넣으면 된다" | **축약본** 명시 + 각주 지시 | 원문 대조에서 5개 중 4개 불일치 — 낡은 게 아니라 발췌 |
| 10 | 03_구성도 §3-1 격자 · 01_작성방향 §03 | "query_rules 25 · clarify 1" / "채권 25" | **41** · 다의어 6 | 🔴 미수정 — 리드·조판자 몫. 09-04 오후 `정렬축` 추가로 40→41 (04 는 3곳 반영됨) |
| 11 | 04 §A.1 유형 ③ 이름 | "미산출 — **만기 경과분**의 듀레이션·잔존일수 / `dur` NULL 16" | "평가정보 **8컬럼이 한 덩어리로** 없음 / 동시 NULL 16 · **만기 경과와 교집합 0**" | 그 16행은 만기가 2027~2056년 미래이고 만기 경과 67행과 한 행도 안 겹친다 (09-04 전수 실측) |
| 12 | 04 §A 본문 표 · §A.1 ② | 결측을 컬럼별로만 서술 | 판매정보 13컬럼·평가정보 8컬럼 **블록 2줄** 신설 (§A 6→8줄) | "동시 NULL = 하나라도 NULL" 로 검증 — 결측의 단위가 행이다 |
| 13 | 회신 §1.1 8번 | `bd_knd` 152 · `pd_pbcm` 149 "(같은 행)" | **149행이 같은 행** · 종류만 결측인 3행 별도 | yaml `bd_knd.note` 는 원래 맞게 적혀 있었고 회신이 압축하며 틀림 |
| 18 | `13_데이터절_통합.md`·`14_부록_취합.md` (기계 조립본) | 13:44 에 **09-04 이전 04** — 정정된 ③ "미산출 — 만기 경과분의 듀레이션" 이 살아 있고 §A 표도 6줄 · 14 E.3 은 `query_rules 40` | `assemble_proposal.py` 재실행 | 🔴 미수정 — **틀린 문장이 통합본에 살아 있다**. ETF 담당 몫(§3-2 ③ 과 같은 실행) |
| 17 | `01_작성방향_분담.md` 채권 행 | "한국기업평가 **21등급** 2밴드" | 표 21행 · 표준등급 20 · KG 노드 21 = 등급 19 + 밴드 2 | 🔴 미수정 — 정정 #3 이 템플릿만 고치고 이 파일을 안 봤다. 리드·조판자 몫 |
| 16 | `enums/domestic_bonds.yaml` `crd_grd.kg_entity` | `null` + 주석 "채권 전용 → SQL 컬럼으로 충분" | **`CreditGrade`** + 주석 갱신 | 선언과 빌드 산출물이 어긋난 유일한 자리 — 실제로는 노드 21·alias 15·closure 19 (kg_alias 실측). 런타임 영향 0(alias 는 shared 발). 🟡 yaml 작업본에 있음 — 같은 파일을 다른 세션이 편집 중(`정렬축` 규칙)이라 그 세션 커밋에 함께 실린다 |
| 15 | 04 §B.2 도입부 채택 기준 | "그 밖의 소재(대표행·수익률 다의어·**이자유형 분리**)는 부록으로" | 미채택 7부류 표 신설 · 이자유형 분리는 B.2-6 보조로 **채택**된 것 | 배제 목록이 낡아 실제 배제분(패딩·날짜 REAL·NULL 함정·통칭·정부보증 표기)을 가림 · 가드 19 중 §B.2 가 다루는 것 3 |
| 14 | 회신 §1.1 9번 | "패딩 … 13컬럼" | 실제 패딩 **7컬럼** · `trim_columns` **선언** 13 | 나머지 6(`std_pd_mcls_nm`·`std_pd_scls_nm`·`pd_exg_mkt`·`curr_cd`·`pd_ctry_cd`·`crd_grd`)은 2차 패딩 0 — 예방적 선언 |

**재발 방지.** `scripts/gen_proposal_numbers.py` 에 `국내채권 기본모수 · 등급 · 판정 컬럼` 절과 `결측 유형 표(2차 재실측)` 블록을 편입했다. 04 §A·§B.1·§E 와 03 그림 B-1 채권 열은 전부 이 절을 인용하며, 조판 직전 재생성 한 번이면 된다. 09-03 밤 기계 대조: §A.1·§A.2 수치 ↔ NUMBERS 전건 일치, §F SQL 21/21 일치, E.1 ttl 바이트 일치. **09-04 추가 편입** — `국내채권 결측 블록` 표(판매 13컬럼·평가 8컬럼, '동시 NULL = 하나라도 NULL' 검증식 포함)를 같은 생성기에 넣어 04 §A·§A.1 이 인용하게 했다. 재생성 1회로 갱신되고, 3차 데이터가 와도 블록 일치 여부가 자동으로 다시 검증된다.

---

## 3. 결정 요청

### 3-1. 리드

각 항목은 배경 → 선택지 → 원고의 현재 기본값 순이다.

**① `_GRADE_SCALE` 코드 상수 (`src/runtime/pipeline.py:557`)** — 등급 서열 확장 가드의 목록 15종이 하드코딩이다. 규칙 문서와 KG rank 는 코드북에서 오는데 3층 가드만 코드 상수라 "값 사전 = 값 검사기" 원칙에 어긋난다. 지금은 15/15 일치하고 2차 0건 등급은 게이트 `no_data` 가 먼저 받아 실해는 없다.
- (a) 이대로 두고 한계 절 서술 유지 — 코드 변경 0. (b) `kg_node` rank 로드로 교체 — 로더 10줄 + 테스트, 프리즈 전 회귀 1회.
- 기본값 (a). (b) 면 채권 담당이 수리한다.

**② 재배포 후 서버 재실측** — 04 §B.2-2(7등급)·§B.2-3(투자지역·달러 채권)은 오프라인 파이프라인 실측이고, 8/31 체크리스트 18문항은 전 칸이 비어 있다. `eval/probe_bond_recheck.txt` 29문항을 배포 뒤 한 번 돌리면 §C 캡션·§B 실측 사례를 서버 원문으로 바꾸고 오답기록 §5 를 닫는다. BR-X04 "국고채 포함 국공채 전체 몇 종목" 은 정답 1,774 이고 295 가 나오면 `ensure_ktb_kind` 오폭이다.

```bash
export PYTHONIOENCODING=utf-8
./.venv/Scripts/python.exe eval/probe_server.py eval/probe_bond_recheck.txt -o eval/probe_bond_recheck.json
```

**③ 4.3 국민성장펀드 — 서버 전체 거절 vs 06 §C 부분 답변 설계** — 서버 실측(`OFFICIAL-002`, 2.3초)은 Plan 단계 전체 거절인데 `public_funds` 에 `itm_nm LIKE '%국민성장%'` 4행이 있다.
- (a) 실측 그대로(거절도 정답형). (b) `_refusal` 판정 범위를 좁혀 부분 답변으로 수리 후 재실측.
- 기본값 (a) 로 취합본 작성.

**④ 4.4 답변불가 — "세 경로 모두 HCX 0회" 가 아니다** — 실측은 AAAA 만 HCX 0회(0.3초). Kimi 는 미특정 라우팅 → HCX 가 `LIKE '%김미%'` SQL → 0집계 기계 조립(48.9초), KODEX AI로봇은 정확일치 0행 → 유사 후보 4건 되묻기(2.1초). 답은 셋 다 정답.
- (a) 문구를 실측대로 정정(취합본에 반영 완료). (b) Ground 매칭 0 + 머리명사 없음이면 HCX 전에 끝내는 결정층 추가.
- 기본값 (a).

**⑤ '위험' 되묻기 사각 2건** — "가장 위험한 회사채·공모 채권" 은 `_RISK_CUE` 가 종류 낱말을 축 단서로 취급해 되묻지 않고, "위험도 높은 순" 은 `_RISKY_Q` 접사에 `도` 가 없다. 수정은 정규식 두 줄 + 테스트 3문항. 서버 확인 문항 BR-X05·X06. `pipeline.py` 를 리드 세션이 수정 중이라 채권 담당은 손대지 않았다.

**⑥ 채권 단일 COUNT 기계 조립** — "3년 뒤 만기" 14,676 오답은 가드 `ac4835d` 로 닫혔지만, 채권 단일 `COUNT(DISTINCT pd_no)` 답변 문장은 아직 HCX 가 쓴다. 펀드 `_count_answer` 의 채권판(함수 1 + 테스트 3, `win_note` 머리줄 포함) 제안. 리드 커밋 뒤 채권 담당이 넣을 수 있다.

**⑦ 발행사 되묻기 후보 오류 — 수정 완료, `--code-only` 재배포 필요** — "삼성전자가 발행한 채권" 의 되묻기 후보가 엉뚱했던 것은 재생성 리터럴 `'(주)삼성전자'` 의 접두 `(주)` 를 못 뗀 탓. `_ISSUER_SUFFIX` 에 접두 제거 추가(코드 1줄 + 테스트).

**⑧ "달러로 발행된 채권" — 3층 수정 완료, yaml+code 재배포 필요** — HCX 가 오염값 `curr_cd='000'` 을 골라 BAC 1행을 `SELECT *` 58컬럼으로 노출했다. 수정: yaml `gate_constants curr_cd`(HCX 0회) · `forbidden_literal_use`(`'000'`·`<>'KRW'` 기각) · 목록 조립기 핵심 항목만 + 금지 컬럼 숨김. 판단 2건: `SELECT *` 를 SQL 층에서 표준 컬럼으로 치환할지(전 도메인 공통) · 값 사전에 "실재하지만 사용 금지" 표식을 둘지.

**⑨ 값 검사 컬럼 자동 교정 가드** — "이자를 아예 안 주는 채권" 오거절은 HCX 가 `bd_inrt_tcd='할인채'` 로 쓰고 값 검사가 소유 컬럼을 알려줘도 재생성이 같은 오류를 낸 것. 2층에 `무이자질의` 규칙·동의어 3 을 넣었다. 판단: 값 검사 위반 시 소유 컬럼으로 자동 교정하는 가드를 넣을지.

**⑩ 09-03 밤 추가 — 프로세스 서술 관련 3건**
- 결측 유형 어휘 통일: 13 §2.1.4 의 4유형(미입력 0·센티넬·더미·구조적 부재)은 "값의 형태", EDA 가이드 4유형은 "값의 뜻" 분류다. 04 §A.1 은 EDA 노트 7유형 번호를 썼다. 하나로 통일할지, "형태(2.1.4) → 뜻(도메인 §A)" 두 단계로 갈지.
- ~~`17_구축과정_채권.md` 작성 여부~~ → **09-04 오후 `18_구축과정_채권_초안.md` 가 이미 올라왔다**(커밋 `63646c4`, 리드 검토 전 초안). 문서가 채권 담당에게 물은 3건은 §4 표에서 답했다. 아래는 원래 안건이었다. ETF 담당이 `17_구축과정_ETF.md`(여섯 걸음 + 3층 실례 3건)를 올렸다. 채권 실례는 이미 있다 — ① 신용등급 결측 두 뜻(→ 2층) ② 등급 서열 코드북·closure(→ 1층) ③ 통화 상수 게이트(→ 3층). 작성 결정이 나면 채권 담당이 9/4 오전에 쓴다. 04 와 중복이라 부록·레포 문서로 두는 것을 제안.
- 03 빌드 도해·2.2.1 한 단락: 08 §4 그림 4 의 EDA 상자를 `프로파일러(탐지기 12종) → 노트 → yaml 판정 → 빌드 검증 V1~V7` 로 펼칠지. 2.2.1 문장 초안 — *"온톨로지의 선언은 EDA 발견의 형식화다. 프로파일러가 낸 결측률은 사실이고, 그 결측이 무슨 뜻인지는 사람이 교차표로 판정해 yaml 한 줄로 적으며, 빌드가 그 한 줄을 ttl 제약·KG 노드·프롬프트 문서·게이트 상수로 동시에 내보낸다."*

**⑪ 09-04 추가 — 채권에도 `schema_exclude` 를 둘지** — 플래너에게 주는 스키마는 채권 58/58 전 컬럼이다. `public_funds` 만 4컬럼을 빼고 있다. 판매정보 13컬럼은 97.1%(21,248/21,882)가 비어 있어 플래너가 고르면 거의 항상 빈 답이 되는데, 09-04 에 넣은 `answer_rules` 는 **답변 단계**에서만 모수를 붙인다(SQL 생성 자체는 못 막는다).
- (a) 그대로 두고 `answer_rules` 모수 고지로 충분하다고 본다 — 법인·세금우대 질의는 여전히 답할 수 있다. (b) `bdbns_abl_chnl_nm`·`sale_yield_base_dt`·`pref_tax_yield` 처럼 답변 가치가 낮은 것만 `schema_exclude` 로 뺀다 — yaml 한 줄, 재배포 필요.
- 기본값 (a). 프리즈 전 프롬프트 예산이 문제되면 (b).

**⑫ 09-04 추가 — 'N년 뒤 만기' 의 뜻이 어디에도 정의돼 있지 않다** — 오답기록 §5 가 🟡 로 남긴 것을 회신에 올린다. "3년 뒤에 만기" 가 ① 2029년 전체(2,611종목) ② 2029-08 근방 ③ 되묻기 중 무엇인지 규칙·코드 어디에도 없다. 지금은 규칙 `만기윈도우` 가 'N년 **안에**' 만 정의한다("뒤" 는 미정의).
- (a) '1년 뒤 = 내년' 처럼 **해당 연도 전체**로 확정(권고 — `만기윈도우` 에 한 줄, 가드 회귀 1문항). (b) 되묻기로 돌린다. (c) 미정의로 두고 HCX 판단에 맡긴다(현 상태 — 오답 #53 의 뿌리).
- 기본값 (a). 결정되면 채권 담당이 규칙·테스트를 넣는다.

**⑬ 09-04 추가 — `remaining_days` 의 기준일이 `mat_dt` 판정과 다르다** — 잔존일수는 `info_base_dt` **8/21** 기준이고(실측: `mat_dt` 20260824 → `remaining_days` 3 · 20260822 → 1), 구매가능·만기 판정은 **8/24** 기준이다(리드 결정 09-02). 두 축을 한 답변에 같이 쓰면 "잔존 3일인데 오늘 만기" 같은 문장이 나온다. 오답기록 §5 에 🔴 로 있고 처방 9번("잔존일 재계산")이 🟡 리드 확인 대기다.
- (a) 답변에서 잔존일은 `mat_dt − 20260824` 로 재계산해 표기(가드 `_cell` 한 곳). (b) 컬럼값 그대로 쓰고 "8/21 기준" 을 병기. (c) 그대로 둔다.
- 기본값 (b) — 값을 고치지 않는다는 원칙에 맞고 한 줄이면 된다. 리드 판단.

**⑭ 09-04 추가 — 빌드 검증 V4 가 채권 코드북을 안 본다** — `build_ontology.py` 는 `ontology/codebooks/`(15개) 만 순회하는데 채권 코드북 `credit_grade_scale.csv` 는 `data/external/lookups/` 에 있다. 제안서 §B.1·§D 는 "코드북 출처·기준일을 빌드가 검사한다" 고 말한다. 파일 자체는 규정을 지킨다(행마다 `source_url`·`as_of` 2026-08-20).
- 🔴 **더 정확히는, 경로를 고쳐도 통과 못 한다.** V4 를 `data/external/lookups/` 에 그대로 돌려보니 **7개 파일 전부 `[V4] source 없음 · as_of 없음` 으로 errors** 가 났다(09-04 실측, `build_ontology.load_codebooks` 그대로 재현). 이유는 규정 미준수가 아니라 **컬럼 이름**이다 — V4 는 `source`·`as_of` 두 컬럼을 찾는데 이 폴더는 `source_url`·`as_of` 를 쓴다. `credit_grade_scale.csv` 도 마찬가지라, 제안서가 모범으로 내세우는 그 코드북이 정작 그 검사에 걸린다.
- (a) 문장에 단서를 달고 넘어간다(09-04 회신 §1.3·04 §D 에 반영 완료). (b) V4 를 `source` **또는** `source_url` 로 넓히고 순회 경로에 `lookups/` 를 더한다 — 두 줄. 그러면 7개 중 `credit_grade_scale`·`etn_issuers`·`index_master_draft` 는 통과하고 나머지 4개(`collateral_type_map`·`fund_facts_from_prospectus`·`issuer_industry_map`·`issuer_industry_top200`)는 실제로 `as_of` 가 없어 **빌드를 거부시킨다** — 그 넷에 `as_of` 를 채우는 일이 딸려 온다. (c) 채권 코드북만 `ontology/codebooks/` 로 옮기고 컬럼을 `source` 로 바꾼다 — `loader.GRADE_SCALE_CSV` 경로도 함께.
- 기본값 (a) — 프리즈 전에는 문장 단서로 충분하다. 프리즈 후 (b) 권고(검사가 채권 코드북까지 닿는 것이 원래 의도다).

### 3-2. ETF 담당

**① 13 §2.1.4 파악 표 정정 (원문 `assemble_proposal.py` 107행)** — "더미 코드 · 채권 `AAAA`" 는 데이터 값이 아니라 4.4 답변불가 테스트의 가짜 등급이다. 채권의 실제 더미·센티넬은 통화 `'000'` 1행(BAC 0 05/14/55 Corp, 발행국 XS)과 `avg_annual_tax_yield` 전량 0(21,882행). 같은 표에 한 행 추가 제안:

| 유형 | 뜻 | 실측 예 |
| :-- | :-- | :-- |
| 같은 NULL 의 두 뜻 | 한 컬럼의 결측이 해당없음/미수록으로 갈림 | 채권 신용등급 4,020 = 국공채 미부여 2,840 + 회사채·특수채 미수록 1,180 — 답변 문장이 다르다 |

② 보강 표는 그대로(채권은 보강 0, 한계 줄에 있음).

**② OFFICIAL-001 잔여 🟡 2건의 부록 E 표기** — g1~g5 내내 ✅값/🟡 이고 사유가 같다: 정렬축 미표기(SQL 에 ORDER BY 없음) · 무등급 4,020행 모수 밖 고지 없음. gold 결함으로 보류 처리. (a) "✅값 · 🟡 표기 2건 · gold 보류" 그대로 적는다 (b) 리드가 G-C′ 를 프리즈 전에 넣으면 ✅ 로 올린다. 기본값 (a).

**③ §A 분량** — 결측 판정 프로세스 추가로 §A 가 반 쪽 → 3/4쪽. 2.1 예산 3쪽에서 1/4쪽이 늘어난다. 13 은 04 §A 를 `assemble_proposal.py` 로 기계 조립하므로 재실행이면 반영된다.

---

## 4. 함께 올린 문서

| 파일 | 내용 | 남은 입력 |
| :-- | :-- | :-- |
| `10_흐름도_취합.md` | 04장 4.1~4.4 한 형식(7단 표 + 서버 trace 원문 + 시간·HCX 횟수) + 층 개입 한 장 표 | §3-1 ③④ 리드 판단 · ETF 지역 필터 16건 재확인 |
| `11_기대효과_확장성.md` | 05장 — 5.1 실측 수치(환각 방지 9/9 · 답변 문장 기계 81% · HCX 미호출 6% · ✅170/231 · KG35 ✅5→24 · 60초 초과 7건) · 5.2 실증 2 + 현업 3 · 5.3 한계 7 | 06 §D 리드 한 문장 |
| `../API_SPEC.md` | 부록 D · 제출 항목 3 — 규격·요청·5필드·실측 예시 3·오류 처리·기준일 규약·로컬 재현 | 최종 URL · 500/200 정책(안건 A-7) |
| `18_구축과정_채권_초안.md` (09-04, 다른 세션 초안) | 04·BONDS_MASTER 를 "EDA → yaml → 코드북 → yaml 완성 → 온톨로지·KG → 질의 답변" 흐름으로 재배열 | **채권 담당 답 3건 (09-04 실측)** — ⓐ `▶` §5.3 closure 밴드→등급: **19 확인**(`select count(*) from kg_closure where ancestor_id like 'CG_%'` = 19 · 투자등급 후손 10) ⓑ `[리드 검토]` V4 경로: **채권 코드북은 V4 대상 밖**이 맞다 → 결정 §3-1 ⑭ ⓒ §5.4 "빌드가 실제로 걸러낸 사례": `--check` 현재 warnings **8건**이 그 사례다 — 2차에서 사라진 등급 표기 5(`BB+`·`B0`·`CCC`·`CC0`·`C`)와 외화 3(`USD`·`JPY`·`EUR`)의 **죽은 alias(V1)**. 1차 yaml 을 그대로 뒀으면 없는 값으로 필터가 나갔을 자리다 |
| [`채권_오답기록_2026-09-03.md`](채권_오답기록_2026-09-03.md) | 08-29 첫 관측부터 09-03 까지 사고 58건 총괄표(질문 → 오답 → 정답 → 원인 층 → 수정·커밋 → 상태) + 테스트셋 44문항·안전 프로브 10문항 회귀 매핑 + gold 결함 + 미실측 목록 | 서버 재실측으로 정답 확인한 사고는 9건뿐 → §3-1 ② 재실측 필요 |

---

## 5. 잔여 (채권 담당)

| 시점 | 할 일 |
| :-- | :-- |
| 재배포 후 | 프로브 29문항 결과 수령 → 04 §B·§C 서버 원문 교체 · 오답기록 §5 갱신 |
| 리드·ETF 담당 답 후 | 10·11·API_SPEC 의 `▶` 닫기 · 06 §D 한 문장 반영 · 17 채권판 작성 여부 · §3-1 ⑪ `schema_exclude` 결정 |
| **push 후 (yaml 변경)** | `deploy.sh` 재배포 — `answer_rules` 판매정보 블록 1줄 · `crd_grd_dt` 3행 · `remaining_days` NULL 5행. 배포 뒤 확인 문항: "법인 세후 수익률 높은 채권"·"온라인으로 살 수 있는 채권"(모수 634 고지 여부) |
| 미착수 | 부록 **E.5 58컬럼 판정 전수표** 생성기 — §1.6 의 '판정이 전 컬럼에 있다' 를 증거로 세우는 자리 |
| 🔴 **지금** | `assemble_proposal.py` 재실행 — `13_데이터절_통합.md` 이 09-04 이전 04 를 담고 있어 **정정된 문장(③ "만기 경과분의 듀레이션")이 통합본에 살아 있다** · §A 표도 6줄 · `14` E.3 은 `query_rules 40`. ETF 담당 몫(§3-2 ③ 과 같은 실행) |
| 리드 확인 | 루트 `README.md`(엔드포인트·재현 절차) 신설 여부 — 지금 레포 루트에 README 없음 |
| 9/5 조판 | `gen_proposal_numbers.py` 재실행 · E.4 값 수 표 재실측 · `02_목차.md` 순서 합본, 그림 A·B·C 는 03 §5 규칙 |
