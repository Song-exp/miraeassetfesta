# 최종 제출 체크리스트 (2026-09-06 · 트리플에이치)

세 곳을 대조해 만들었다 — ① 디스코드 공지·기술 Q&A 전문 ② 주최 배포 PDF(과제설명·기술세션) ③ 8월 6일 오프라인 설명회 **녹음 전사본**.
셋이 어긋나면 **공지가 최신**이다(예: 의도 분석에 HCX 를 쓰라는 것은 8월 31일 공지가 정정본).

---

## 🔴 A. 오늘 23:59 까지 — 이걸 놓치면 제출 자체가 성립하지 않는다

| # | 항목 | 무슨 말이냐면 | 담당 | 상태 |
| :-: | :-- | :-- | :-- | :-- |
| A1 | GitHub push | **push 한 것이 곧 제출이다.** 마감 뒤에 올리면 부정행위로 처리한다고 못박았다 | 전원 | ETF 분 6584eb8 까지 완료 |
| A2 | 루트 `README.md` | 저장소 맨 위에 README 를 두고 **환경 구성·실행 명령어와 API 주소를 적어야 한다.** 주최가 우리 서버 주소를 찾는 유일한 자리가 여기다 | 리드 | 🔴 **아직 없음** |
| A3 | Endpoint URL 구글폼 | 저장소에 올리는 것과 **별개로**, 구글폼에 서버 주소를 한 번 더 내야 한다. 저장소만 올리고 폼을 빼먹으면 호출을 못 받는다 | 리드 | 🔴 **제출했는지 확인** |
| A4 | 제안서 PDF + 워드 | **두 파일을 다 내야 한다.** PDF 로 평가하고, 워드는 나중에 몰래 고치지 못하게 대조용으로 받는 것이다 | 조판(채권) | 🔴 27 은 렌더 없음 · 28 은 워드 없음 |
| A5 | API 명세서 | 우리 서버에 어떻게 물어보고 어떤 모양으로 답이 오는지 적은 문서 | 채권 | ✅ `docs/API_SPEC.md` |
| A6 | 온톨로지 파일 | 도메인마다 온톨로지가 있으면 되고 **파일 형식은 자유다.** `ontology.ttl` 이라는 이름일 필요도 없다 | 병철·서현 | ✅ ttl 5개 |
| A7 | 소스코드 + 실행 환경 | 코드와 함께 `requirements.txt`·Dockerfile 처럼 **남이 그대로 돌려볼 수 있는 것**을 넣는다 | 전원 | ✅ |
| A8 | **파일 정리는 하지 않는다** | 지워도 되는 파일이 400개 넘게 있지만 **마감 당일에는 손대지 않기로 했다.** ① 그 파일들은 제출 추출 대상이 아니라 남겨 둬도 제출물에 안 들어가고 ② 대량 삭제는 실수하면 되돌릴 시간이 없다(실제로 명세서 초판이 제출 필수인 API 명세서를 삭제 대상으로 잘못 분류한 적이 있다). 정리는 평가가 끝난 뒤에 | 전원 | 결정 완료 |

> ⚠️ 저장소 맨 위에 주최가 준 PDF 두 개(네이버클라우드 소개·크레딧 가이드)가 **깃에 올라가지 않은 채** 놓여 있다.
> `git add -A` 로 전부 담으면 같이 딸려 들어간다. 제출물이 아니니 그대로 두거나 다른 폴더로 옮긴다.

## B. 9월 7일(월) 10:00 ~ 11일(금) 15:00 — 평가 서버를 켜 두는 기간

| # | 항목 | 무슨 말이냐면 |
| :-: | :-- | :-- |
| B1 | 서버를 계속 켜 둔다 | 이 기간 중 언제 물어볼지 알려주지 않는다. 꺼져 있으면 그 문항은 0점이다 |
| B2 | 주소는 `/answer` 로 고정 | 주최가 `GET /answer?question_id=…&question=…` 로 부른다. 다른 경로를 쓰면 못 받는다 |
| B3 | 답은 다섯 칸을 문자열로 | 질문 번호 · 질문 · 근거 · 생각 과정 · 최종 답변. 숫자로 내보내면 규격 위반이다 |
| B4 | 모른다고 답할 때도 정상 응답 | 답을 못 해도 오류가 아니라 **200 으로 같은 다섯 칸**을 채워 보낸다 |
| B5 | 우리 서버에서 밖으로 나가지는지 확인 | 서버 안에서 `curl -s http://34.47.123.221/health` 를 쳐서 `{"status":"ok"}` 가 오면 통신이 정상이다 |
| B6 | 방화벽을 쓰면 그 주소를 열어 준다 | 실제 평가 호출도 같은 주소(34.47.123.221)에서 온다 |
| B7 | 포트는 80 또는 443 | 도메인은 필요 없고, 인증서는 직접 만든 것이어도 된다 |
| B8 | 한 번에 한 문항씩 | 동시에 쏟아붓지 않는다. 답이 300초를 넘으면 미응답으로 처리하고 최대 2번 다시 부른다 |
| B9 | 답이 빠를수록 좋다 | 몇 초까지 만점인지는 알려주지 않는다. 60초면 사실상 감점이고 담당자 기준은 15초다 |
| B10 | 서버가 죽어서 다시 켜는 건 괜찮다 | 다만 **내용을 고치는 것은 안 된다.** 재기동만 허용된다 |

## C. 평가 문항 — 총 35개 (상 10 · 중 10 · 하 10 + 답변불가 5)

| # | 규칙 | 무슨 말이냐면 · 우리는 어떻게 하고 있나 |
| :-: | :-- | :-- |
| C1 | 한 번 묻고 한 번 답한다 | 되물어도 다시 물어봐 주지 않는다. 조건이 부족해도 **가능한 선에서 해석해 답을 낸다** |
| C2 | 답할 수 없는 질문은 답하지 않는다 | "없습니다" 도 정답이고 "이렇게 물어봐 주시겠어요" 도 정답이다. **억지로 답을 만들면 감점.** 우리는 게이트가 SQL 도 답변도 만들지 않고 끊는다 |
| C3 | 값이 0이거나 비어 있으면 빼거나 없다고 말한다 | 총보수 0 은 공짜가 아니라 미입력이다. 우리는 "수록되어 있지 않다" 로 답한다 |
| C4 | 코드 값의 뜻은 묻지 않는다 | 주최가 코드표를 못 준다고 확정했으니 그런 질문은 안 나온다 |
| C5 | 밖에서 모은 자료는 8월 24일까지 것만 | 그 뒤에 나온 자료는 기준일에 존재하지 않았으므로 쓰면 안 된다 |
| C6 | 주최가 준 4개 표가 기준이다 | 우리가 모은 값과 충돌하면 주최 값이 이긴다. 단 "기준 데이터만 묻지는 않는다"고 했다 |
| C7 | 한 질문에 상품군이 둘 이상일 수 있다 | 예: 삼성전자를 담은 ETF 와 펀드를 각각. 우리는 상품군별로 따로 세서 합친다 |
| C8 | 해외 ETF 의 1년 수익률은 안 묻는다 | 해외에는 1일 수익률뿐이다. 물어도 해외를 빼고 답해도 된다 |
| C9 | 근거를 반드시 보여 준다 | 형식은 자유다. 우리는 조회 결과와 단계별 기록을 함께 보낸다 |
| C10 | 없는 값을 만들지 않는다 | "위험등급 99" 같은 답은 감점이고, **없다고 말하는 편이 낫다** |
| C11 | 채점은 답에 들어가야 할 것이 다 들어갔는지로 본다 | 말투는 상관없다. 예를 들어 코스피 상위 5개에 KODEX 200 이 빠지면 감점 |
| C12 | 애매한 질문은 되도록 내지 않는다 | 개수와 기준을 문장에 넣어 준다고 했다 |

## D. 어떤 AI 를 써야 하나

| # | 규칙 | 무슨 말이냐면 |
| :-: | :-- | :-- |
| D1 | 의도 분석과 답변 생성은 하이퍼클로바 X 필수 | 질문을 읽는 자리와 답을 쓰는 자리는 반드시 이 모델이어야 한다. 우리는 SQL 을 만드는 자리까지 세 군데 다 쓴다 |
| D2 | 나머지는 자유, 유료 모델은 지양 | 전처리 같은 데는 다른 방법을 써도 되지만 돈 내는 외부 모델은 권장하지 않는다. 우리 서버 코드에는 타사 모델이 하나도 없다 |
| D3 | 비필수 구간은 점수에 안 들어간다 | 임베딩 같은 것을 어떻게 쓰든 평가하지 않는다 |

## E. 제안서에서 점수가 큰 자리 (40점)

| # | 항목 | 무슨 말이냐면 |
| :-: | :-- | :-- |
| E1 | 목차는 권장일 뿐 | 필요하면 더하거나 빼도 된다 |
| E2 | 특히 5번(기대효과·확장성) | 담당자가 *"수상에서는 아마 5번이 중요한 평가 요소"* 라고 했다. 제안 방법·구성도·흐름도도 중요하다 |
| E3 | 데이터를 어떻게 모았는지 적는다 | 수집 방법, 넣는 과정, 쓰는 방식을 문서에 적어야 한다. **저장소에 데이터만 두면 정성 점수를 못 받는다** |
| E4 | 결측치를 어떻게 채웠는지 적는다 | 빈 값을 어떻게 찾아내고 어떻게 메웠는지가 정성 평가 대상이다 |
| E5 | 모은 데이터의 신뢰성도 과제의 일부 | 크롤링은 허용하지만 원본이 틀리면 답도 틀린다 |

## G. 배점과 하면 안 되는 것 (녹음본에서 확인)

| # | 내용 |
| :-: | :-- |
| G1 | **배점 100점 = 소스코드 20 · 제안서 40 · API 답변 40** |
| G2 | 마감 뒤 push 는 **부정행위 처리** |
| G3 | 답변불가 문항은 점수에 안 들어가지만 **답하면 감점**이다. 담당자가 몇 개 넣겠다고 했다 |
| G4 | 모르는 것을 "확인할 수 없다"고 말하지 않으면 감점 |
| G5 | 데이터에 없는 수익률 전망이나 "무조건 좋다" 같은 말은 금지 |
| G6 | 근거 표시는 필수, 양식은 자유 |
| G7 | 채점은 답에 들어가야 할 항목과 관계가 다 있는지, 근거가 말이 되는지로 본다 |
| G8 | 응답 시간도 점수다. 60초면 사실상 감점, 기준은 15초. 전체 팀 평균으로 가감점할 수도 있다 |
| G9 | 예선에서 화면(UI)은 평가하지 않는다. 본선 라이브 시연은 가점 요인 |
| G10 | 온톨로지는 형식 자유. 대신 **어떻게 만들었고 어떤 데이터를 썼는지** 제안서에 적어야 한다 |
| G11 | 한 팀에 한 문항씩 순서대로 물어본다 |
| G12 | 모든 팀에 **같은 질문**을 던진다 |

> ⚠️ **기준일 조심** — 녹음본(8월 6일)은 데이터 기준일을 7월 11일이라고 말한다.
> 그 뒤 8월 24일에 데이터를 다시 배포하면서 **기준일이 2026-08-24 로 바뀌었다**(영업일 8/22, 해외 8/23).
> 밖에서 모은 자료도 8월 24일까지 것만 쓴다. 녹음본과 공지가 다르면 공지가 맞다.

> 💰 **크레딧은 9월 30일에 만료된다** — 서버는 9월 11일까지 켜 둬야 하고, 그 뒤에도 자원을 켜 두면
> **돈이 청구된다.** 평가가 끝나면 서버를 내린다.

## F. 우리 트랙에 남은 일

| # | 항목 | 담당 |
| :-: | :-- | :-- |
| F1 | ~~라운드 15 수리 5건 push~~ ✅ 6584eb8 (회귀 1,405 통과) → **3차 재배포 요청** | 병철 → 형 |
| F2 | 재배포 뒤 제출 전 점검 20문항 다시 던지기 | 병철 |
| F3 | 27 원고의 서버 성적 수치 갱신(지금은 1차 재배포까지만 적혀 있다) | 병철 |
| F4 | 27 또는 28 을 PDF·워드로 뽑기 | 리드·채권 |
| F5 | `00_README`·`02_목차` 에 27 등록 | 문서 소유자 |

---

### 마감 직전 순서

1. ~~라운드 15 회귀 통과 → push~~ ✅ 완료 (6584eb8)
2. 형이 3차 재배포 → 20문항 다시 채점 (F2)
3. 루트 README 작성 — **API 주소 포함** (A2)
4. 제안서 PDF·워드 만들어 push (A4)
5. **구글폼에 서버 주소 제출** (A3)
6. 23:59 이후에는 저장소에 손대지 않는다 (A1·A8)
7. 9월 7일 오전에 서버가 떠 있는지, 통신이 되는지 확인 (B1·B5)
