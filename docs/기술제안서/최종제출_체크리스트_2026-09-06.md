# 최종 제출 체크리스트 (2026-09-06 · 트리플에이치)

출처를 셋으로 대조했다 — ① 디스코드 공지·기술 Q&A 전문 ② 주최 배포 PDF(과제설명·기술세션) ③ 8/6 오프라인 설명회 **녹음 전사본**.
공지가 전사본보다 최신이면 공지를 따랐다(예: 의도 분석 HCX 필수는 8/31 공지가 정정본).

---

## 🔴 A. 오늘 23:59 까지 — 이걸 놓치면 제출이 성립하지 않는다

| # | 항목 | 근거 | 담당 | 상태 |
| :-: | :-- | :-- | :-- | :-- |
| A1 | **GitHub push 완료** — 제출 = push. 기한 뒤 수정은 **부정행위 처리**이고 코드 검증도 예정 | 9/4 공지 · 전사 | 전원 | ETF 분 6584eb8 까지 push 완료 |
| A2 | **루트 `README.md`** — 환경 구성·실행 명령어 **+ API 엔드포인트 주소 명시(필수)** | 8/12 공지 "End-point 주소는 README.md 에 명시(필수)" | 리드 | 🔴 **없음** |
| A3 | **Endpoint URL 구글폼 제출** — 저장소와 **별도 제출물**이다 | 9/1 공지 · 기한 오늘 23:59 | 리드 | 🔴 제출 여부 확인 |
| A4 | **기술 제안서 PDF + 워드 둘 다** — PDF 만 내면 요건 미충족 | 설명회 전사 "pdf와 워드파일을 같이 주세요" | 조판(채권) | 🔴 27 은 렌더 없음 · 28 은 html·pdf 만 |
| A5 | **API 명세서** — 엔드포인트 + 요청·응답 5필드 스키마 | 제출 항목 3 | 채권 | ✅ `docs/API_SPEC.md` |
| A6 | **도메인별 온톨로지 파일** — ttl 형식은 필수 아님(온톨로지가 적혀 있으면 됨). 우리는 5분할 ttl | 전사 "TTL일 필요도 없습니다" | 병철·서현 | ✅ common·bond_kr·etf_kr·etf_gl·fund_pub |
| A7 | 소스코드 + 재현 환경(`requirements.txt`·Dockerfile) | 제출 항목 1 | 전원 | ✅ |
| A8 | **정리 커밋을 하지 않는다** — 마감 당일 추가 커밋은 위험만 크고 제출물에 영향 없음 | 명세서 25 §정리 순서 | 전원 | 결정 완료 |

> ⚠️ 저장소 루트에 주최 배포 PDF 2개(네이버클라우드 소개·크레딧 가이드)가 **추적되지 않은 채** 놓여 있다.
> `git add -A` 를 하면 딸려 들어간다. 제출물이 아니므로 그대로 두거나 옮긴다.

## B. 9/7(월) 10:00 ~ 9/11(금) 15:00 — 평가 서버 운영

| # | 항목 | 규격 |
| :-: | :-- | :-- |
| B1 | 서버 **상시 가동** | 답변 수집이 이 기간에 순차 진행 |
| B2 | 경로 `GET /answer` **고정** · 쿼리스트링 `question_id`·`question` | 헤더·인증 없음 |
| B3 | 응답 JSON 5필드 **전부 문자열** | question_id · question · retrieved_context · think_trace · answer |
| B4 | 확인 불가 질의도 **200 + 같은 5필드** | 스키마 유지 |
| B5 | **아웃바운드 헬스체크** — 팀 서버 안에서 `curl -s http://34.47.123.221/health` | `{"status":"ok"}` 확인 |
| B6 | 방화벽 쓸 경우 **발신 IP 34.47.123.221 허용** | 실제 호출도 같은 IP |
| B7 | HTTP 80 / HTTPS 443 표준 포트 · self-signed 무방 | 도메인 불필요 |
| B8 | 타임아웃 **300초** · 5xx·타임아웃 시 **최대 2회 재시도** · 팀당 **순차 1건** | 동시 요청 없음 |
| B9 | 응답 시간 — **기준은 비공개**. 300초는 물리적 미응답이고 늦은 응답과는 **점수 차등** | 전사: 60초면 사실상 감점 · 실서비스 15초 |
| B10 | 장애로 인한 **재기동은 실격 아님**. 단 결과물 변경은 금지 | 8/12 공지 |

## C. 평가 대응 — 35문항 (상10·중10·하10 + 답변불가 5)

| # | 규칙 | 우리 구현 |
| :-: | :-- | :-- |
| C1 | **단일 턴**. 멀티턴 없음 — 가능한 범위에서 조건을 해석해 최종 답변 | 되묻기는 답변불가 문항의 정답 형태로만 |
| C2 | 답변불가 질의는 **불가응답·역질문 둘 다 정답**, 멘트 자유. **답을 만들면 감점** | 게이트가 SQL·답변 생성 없이 기각 + 대체 축 안내 |
| C3 | 값이 **0이거나 없는 행은 빼거나 "없다"** 고 답한다 | 총보수 0 = 미입력, 유효 67건 모수 고지 |
| C4 | **코드 값 의미는 질의하지 않는다**(코드북 미제공 확정) | 전문투자자 코드 등 조건·정렬 금지 |
| C5 | 외부 수집 데이터는 **2026-08-24 까지 발행분**만 | 수집 기록에 as_of 검사 |
| C6 | 주최 제공 4마스터가 **기준 데이터**, 충돌 시 우선. 단 "기준 데이터만 묻지는 않는다" | 외부 Holdings 는 별도 출처로 표기 |
| C7 | **교차질의** — 한 호출에서 2개 이상 상품군 비교·검색 | 상품군별 UNION ALL + 구분 열 |
| C8 | 해외 ETF **1년 수익률 질의는 없을 예정** · 물으면 해외 제외 무방 | 기간 수익률 부재를 게이트가 안내 |
| C9 | **근거 표시 필수**(양식 자유) | retrieved_context 에 조회 결과 · think_trace 에 단계 |
| C10 | 없는 값을 만들지 않는다 — "위험등급 99" 류는 감점, 없다고 말하는 편이 낫다 | 부재 선언 + 게이트 기각 |
| C11 | 채점은 **기대 엔티티·릴레이션이 답변에 다 들어갔는가** + 근거의 유의성 | 모수·기준 고지를 기계가 붙인다 |
| C12 | 모호 질의는 주최가 **최대한 내지 않는다**(개수·기준을 명시) | 조건 해석 분기를 과설계하지 않음 |

## D. LLM 제약

| # | 규칙 | 우리 상태 |
| :-: | :-- | :-- |
| D1 | **질의 의도 분석 · 답변 생성**은 HyperCLOVA X 필수 | 의도 분석(HCX-005)·SQL 생성·답변 생성 3구간 전부 HCX |
| D2 | 그 외 구간은 제약 없음 · **유료 SaaS LLM 지양** | 런타임에 타사·유료 LLM 0건 |
| D3 | 임베딩 등 비필수 구간은 점수 미반영 | 해당 없음(구조화 검색) |

## E. 제안서 내용 — 배점이 큰 자리 (40점)

| # | 항목 | 근거 |
| :-: | :-- | :-- |
| E1 | 권장 목차는 **권장**이다. 추가·삭제 자유 | 전사 |
| E2 | 특히 중요한 것 — **제안 방법 · 시스템 구성도 · 주요 기능 흐름도 · 5번(기대효과·확장성)**. *"수상에서는 아마 5번이 중요한 평가 요소"* | 전사 |
| E3 | **데이터 수집 방법 · 수집·적재 Flow · 활용 로직**을 보고서에 적는다. 저장소에 데이터만 두면 정성평가를 못 받는다 | 8/24 Q&A |
| E4 | 결측치를 **어떻게 파악하고 어떻게 채웠는지**를 적으면 정성평가에 반영 | 8/26 Q&A |
| E5 | 크롤링 가능하되 **원천 데이터의 신뢰성·무결성 관리도 과제의 일부** | 8/24 Q&A |

## G. 배점과 금지 사항 (녹음본 전사에서 확인)

| # | 내용 | 근거 |
| :-: | :-- | :-- |
| G1 | **배점 100점 = 소스코드 20 · 기술 제안서 40 · 평가용 API 답변 40** | 전사 "총 100점 만점… 소스코드 20점, 기술 제안서 40점, 평가용 API 호출 및 답변 40점" |
| G2 | **마감 이후 push 는 부정행위 처리** | 전사 "마감 이후에 푸시 이런 거 하시면 당연히 부정행위 처리" · 9/4 공지 재확인 |
| G3 | 답변불가 질의는 **점수에 포함되지 않지만 답변하면 감점** — "당나귀 영광을 줄 정도" | 전사 |
| G4 | 확인 불가 질의에 **"확인할 수 없음" 을 명시**하지 않으면 감점. 몇 개 넣을 예정이라고 못박음 | 전사 |
| G5 | 데이터에 없는 **수익률 전망·"무조건 좋다" 류 서술 금지** | 전사 |
| G6 | 근거 표시는 **필수**, 양식은 자유 | 전사 |
| G7 | 채점 기준 — **기대하는 엔티티와 릴레이션이 답변에 다 있는가 + 근거가 유의한가**. 예: 코스피 관련 상위 5개에 KODEX 200 이 없으면 감점 | 전사 |
| G8 | 응답 시간은 평가 항목. **60초면 사실상 감점**, 담당자 기준은 실서비스 15초. 초 기준은 비공개이며 **전체 평균 기준 가감점 가능** | 전사 |
| G9 | 예선에서 **UI 는 평가하지 않는다**(API 만). 본선 라이브 데모는 가점 요인 | 전사 |
| G10 | 온톨로지는 **도메인별로 있으면 되고 `ontology.ttl` 이라는 파일명·형식은 필수가 아니다.** 다만 제안서에 **어떻게 만들었고 어떤 데이터를 썼는지** 적어야 한다 | 전사 |
| G11 | 질의는 팀당 **순차 1건**. 동시 호출 없음("동시에 하면 서버 바로 죽을 것 같은데") | 전사 |
| G12 | 모든 팀에 **같은 질문**을 던진다 | 전사 |

> ⚠️ **기준일 주의** — 녹음본(8/6)은 데이터 기준일을 **7월 11일**로 말한다. 그 뒤 8/24 재배포로
> **기준일이 2026-08-24(영업일 8/22, 해외 8/23)로 바뀌었다.** 외부 수집 자료의 허용 시점도 8/24 까지다.
> 녹음본과 공지가 어긋나면 **공지가 최신**이다.

> 💰 **크레딧 만료 9/30** — 서버는 9/11 까지 떠 있어야 하고 크레딧 유효기간은 9/30 이다.
> 그 뒤에도 자원이 살아 있으면 **비용이 청구된다**. 평가가 끝나면 서버를 정리한다.

## F. 우리 트랙에 남은 일

| # | 항목 | 담당 |
| :-: | :-- | :-- |
| F1 | ~~라운드 15 수리 5건 push~~ ✅ 6584eb8 (회귀 1,405 통과) → **3차 재배포 요청** | 병철 → 형 |
| F2 | 재배포 뒤 제출 전 점검 20문항 재검 | 병철 |
| F3 | 27 원고의 서버 성적 수치 갱신(현재 1차 재배포까지) | 병철 |
| F4 | 27 또는 28 을 PDF·워드로 조판 | 리드·채권 |
| F5 | `00_README`·`02_목차` 에 27 등록 | 문서 소유자 |

---

### 마감 직전 순서 (권장)

1. ~~라운드 15 회귀 통과 → push~~ ✅ 완료 (6584eb8)
2. 형이 3차 재배포 → 20문항 재검 (F2)
3. 루트 README 작성 — **엔드포인트 주소 포함** (A2)
4. 제안서 PDF·워드 생성 후 push (A4)
5. **Endpoint URL 구글폼 제출** (A3)
6. 23:59 이후 저장소에 손대지 않는다 (A8)
7. 9/7 오전 서버 기동 확인 + 헬스체크 (B1·B5)
