# 배포 요청 — 채권 QA 라운드1 수리 (2026-09-06)

담당(서현) → 배포 담당자. **main `f799c25` 를 배포해 주세요.** 아래 세 가지만 지켜 주시면 됩니다.

## 배포 담당자 확인 사항 3가지

1. **두 번에 나눠 배포합니다.** 지금은 이번 병합분만 올립니다(`deploy/deploy.sh --code-only`). 응답 시간 개선(템플릿 경로)은 다음에 따로 요청드립니다. 두 개를 합쳐서 한 번에 올리지 말아 주세요 — 문제가 생겼을 때 어느 쪽 때문인지 갈라야 합니다.
2. **배포 커밋 해시를 알려 주세요.** 받는 즉시 제가 87문항 재실측(라운드 2)을 돌립니다. **재실측 결과가 나오기 전에는 다음 배포를 올리지 말아 주세요.**
3. **되돌리기**: 이번 배포에는 끄는 스위치가 없습니다(전부 결정층 가드라 스위치 대상이 아님). 문제가 보이면 직전 커밋 `e942729` 로 재배포하면 종전 동작으로 돌아갑니다.

## 무엇이 바뀌나

채권 트랙만입니다. ETF·펀드 경로는 건드리지 않았습니다(가드마다 `domestic_bonds` 게이트, ETF·펀드 테스트 전건 통과).

| 부류 | 고친 것 | 닫히는 문항 |
| :-: | :-- | :-- |
| BL | 슬롯 주석(`/*M:…*/`)을 컬럼으로 오독하던 WHERE 판독 3곳 | 2024년 발행 회사채 평균 (1,420 → 2,672종목 · 3.651%) |
| BA | '국고채' 리터럴은 컬럼 불문 확정식으로, 값-컬럼 교정 뒤 종류 가드 재실행 | 국고채 개수 290 → **295** · 28년 12월 국고채 48 → **49** |
| BB+BI | 종류 확정식 과확장·모순 교체 · 비최상급 '안전한' = 5·6등급 | 통안채 3,059 → **33** · '망하지 않을 회사' 가 지방채로 새던 것 · 3년 내 안전한 채권 |
| BD | HCX 구문 슬립 기계 교정(IN 안 TRIM 리터럴·따옴표 불균형·환각 GROUP BY) | 가장 안전한 회사채(오거절 회수) · 코코본드 · 재생성 2회 절약 |
| BE | 구조 낱말(코코본드 등)은 선언 판정식 주입, 이름 LIKE 조각 제거 | 코코본드 **222종목** |
| BF | 되묻기 확장 — '등급' 다의어 · 종류 낱말 붙은 '가장 위험한' | 등급 낮은 채권 · 가장 위험한 회사채 |
| BG | 정렬 축 어휘로 끝나는 목록 요청도 랭킹으로 보고 고위험 제외 | 퇴직연금 편입 채권(1등급 코코본드가 1위였음) |
| BH | 추천 질의에 집계만 온 SQL 을 목록으로 재작성 · 다열 집계 1행 기계 조립 | 3년 내 안전한 채권 몇 개(오거절 회수) |
| BJ | 안전·위험 최상급이 한 질문에 오면 양방향 템플릿 | 가장 안전한 것과 위험한 것 하나씩 |
| BK | '이자 안 주는' = 할인채 확정식 · '왜' 는 설명형 답변 | 이자를 아예 안 주는 채권은 왜 (**686종목** + 설명) |
| BP | 같은 재생성 되풀이 중단 · 발행사 값 부재는 즉시 되묻기 · 429 대기 기록 | 60초 초과 9문항의 시간 원인 특정 |

## 기대 효과

| 항목 | 배포 전 (9/6 12:20 실측) | 배포 후 예상 |
| :-- | :-: | :-: |
| 정답 ✅ | 51 / 87 (58.6%) | **70~71 (약 81%)** |
| 오답·오거절 ❌ | 17 | **1** |
| 서술 결함 🟡 | 19 | 15~16 (다음 라운드) |

이 예상은 서버가 실제로 냈던 SQL 을 로컬에 재투입해 검증한 값입니다. 재실측에서 HCX 가 다른 형태의 SQL 을 내면 달라질 수 있어, 실측 기준으로는 75~80% 를 현실적인 기대치로 봅니다.

**응답 시간**: 이번 배포로 재생성 호출이 줄어 60초 초과 노출이 감소합니다. 다만 근본 원인(분당 토큰 한도 대기)은 다음 배포에서 다룹니다. `docs/bonds_latency_analysis_2026-09-06.md` 참고.

## 검증 결과 (병합 후 main `f799c25` 에서 재실행)

| 검사 | 결과 |
| :-- | :-- |
| 동결선 `tests/test_snapshot_round6.py` | 1 passed |
| 전체 pytest | **1,497 passed** (수리 전 1,327 → 회귀 42건 추가 + 타 세션분) |
| gold `eval/run_gold_check.py` | 문항 195 · 통과 195 · 실패 0 |
| 87문항 재투입 대조 | 오류 0 · 기존 정답 51 중 45 불변 · 6 변경(전부 의도된 결과, 값 오류 없음) |

변경된 6건 중 하나(내년 만기 AA 이상 회사채)는 맞교환입니다. 재생성을 생략해 60초 위험을 없앤 대신 목록 정렬축이 빠졌습니다. 답변이 정렬을 주장하지 않으므로 거짓 서술은 없고, 다음 라운드에서 기본 정렬과 함께 처리합니다.

## 이번에 안 한 것 (다음 라운드)

- 🟡 서술 결함 3부류(BM 구매가능 하한 · BN 이자유형 분리 · BO 조립기 고지 10건) — 값은 맞고 표기 문제
- 응답 시간 근본책(템플릿 경로 · 규칙 트리거 층) — 리드 결정 후
- 리드 결정 대기: 하이일드 정의 · 정렬축 없는 목록의 기본 정렬 · gold 정정 2건 사후 승인(`docs/gold_defects_2026-09-03.md` §D)
