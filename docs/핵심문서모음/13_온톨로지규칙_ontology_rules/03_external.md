# 규칙 3. 외부 데이터 병합 — 마스터를 고치지 않고 옆에 붙인다

> 마스터(L0) > 판정(L1) > 외부(L2). 외부는 **마스터가 비었을 때만**, 출처·기준일을 달고 별도 테이블에.

> 🔴 생성물입니다. 서술·체크리스트는 `scripts/_ontology_rules_data.py`,
> 목록·수치는 `scripts/gen_ontology_rules_doc.py` 가 yaml·DB 에서 매번 새로 뽑습니다.

| | |
| :-- | :-- |
| 목록 | [규칙 12종 색인](README.md) |
| 컬럼 단위 상세 | [`../data_dictionary/`](../data_dictionary/README.md) |

---

## 1. 데이터가 이랬다

- 마스터에 아예 없는 사실이 있다 — ETF 구성종목, 펀드 설정일·보수 분해.
- 외부 자료는 **기준일이 마스터와 다르다.** 국내 8/21, 해외는 상품마다 3/31~6/30.
- 커버리지가 100% 가 아니다 — 종목 수로만 말하면 대표성을 과소평가한다(해외는 종목수 22.7% 지만 AUM 가중 88.6%).

## 2. 그래서 이렇게 정했다

- 외부는 `ext_*` **별도 테이블**. 마스터 값을 덮어쓰지 않는다.
- `validate_sql` 이 `ext_*` **단독 조회를 금지** — 반드시 마스터와 조인.
- 답변에 `as_of`/`report_date` **병기 강제**. 해외 커버리지는 **AUM 가중**으로 말한다.
- 시계열 값은 **일부러 적재하지 않았다** — 관측 시점이 기준일과 달라 근거로 못 쓴다.

## 3. 전수 인벤토리 — 이 규칙이 실제로 어디에 선언돼 있나

#### (1) `ext_*` 4테이블 — 조인 커버리지 실측

| 테이블 | 수집키 | 마스터조인 | 마스터전체 |
| :-- | --: | --: | --: |
| ext_etf_holdings | 1,160 | 1,160 | 1,235 |
| ext_ovs_etf_holdings | 1,356 | 1,356 | 5,972 |
| ext_fund_holdings | 2,902 | 2,898 | 14,060 |
| ext_fund_page | 10,565 | 10,565 | 23,676 |

#### (2) 외부 사실을 참조하는 도메인 선언

- **펀드** `external_facts` — {'source': 'data/external/lookups/fund_facts_from_prospectus.csv', 'coverage': '1차 판매중 8,445종목 중 8,443 — 2차 판매중 10,962(사모 포함)와 모수가 다르다. itm_no 로 조인해 붙는 만큼만 사용', '제약': '발행 시점 불변 사실만 사용 — 시계열 수치 인용 금지. 🔴 주최 데이터 우선: 마스터에 값이 있으면 마스터, 없을 때만 설명서', '위상_2026-08-25': '2차 마스터가 보수 4종·개방/단위·자펀드 태그를 직접 제공하므로 이 파일은 (a) 보수 단위 확정용 대조 자료 (b) 마스터 결측분 보완 (c) 총보수(TER)·환매수수료 등 마스터에 없는 항목 — 로 강등. (a)는 2026-08-25 완료.', 

#### (3) 적재하지 않기로 한 것 (의도적 배제)

- `ext_fund_page` — 순자산·수익률 등 **시계열 값은 적재 제외**. 8/18 관측치라 기준일(8/22)과 어긋나 근거로 못 쓴다.
- 적재 대상은 설정일·보수 분해·환매 규칙·모펀드명 등 **불변 또는 준정적 사실**만.

## 4. 근거 (라이브 DB 실측)

**국내 ETF 구성종목 커버리지 — ETN 은 구조상 없다(‘해당 없음’)**

| 수집ETF | 전체ETF | ETN_구조상없음 |
| :-- | --: | --: |
| 1,160 | 1,235 | 545 |

**해외 Holdings 보고기준일 분포 — ETF 간 비중 비교 시 시점이 어긋난다**

| 보고기준일 | ETF수 |
| :-- | --: |
| 2026-04-30 | 463 |
| 2026-05-31 | 404 |
| 2026-03-31 | 366 |
| 2026-06-30 | 98 |
| 2026-05-29 | 20 |
| 2025-11-30 | 2 |

**외부가 채워 준 것 — 마스터에 없는 펀드 설정일**

| 설정일_보강 | 최초 | 최근 |
| :-- | --: | --: |
| 10,565 | 19971028 | 20260612 |

## 5. 안 지키면

외부 값을 마스터에 덮어쓰면 주최 규칙 위반이고, 기준일을 안 밝히면 **8/22 기준 답변에 3/31 자료가 섞인다.**

## 6. 검토 체크리스트

> 전수조사에서 나온 규칙이므로 **규칙 단위로 판정**한다. 각 항목에 결론과 근거를 적고,
> 판정이 바뀌면 `ontology/enums/*.yaml` 또는 `ontology/shared/*.yaml` 을 고친 뒤 재생성한다.

| # | 검토 항목 | 판정 | 근거·조치 |
| :-: | :-- | :-- | :-- |
| 1 | 🆕 주최 8/26 답변 — **결측 보완은 정성평가 가점**이다. 지금 구조(마스터 불변 + `ext_*` 보강)로 “결측률을 얼마나 줄였는지” 를 수치로 낼 수 있는가. |  |  |
| 2 | 국내ETF 총보수 유효 67/1,235(5.4%) — 외부 보완 대상 1순위. 출처·매칭 키·검증 방법을 정할 것. |  |  |
| 3 | `ext_fund_holdings` 의 `grp` 조인이 선행 0 때문에 몇 건 실패하는가. |  |  |
| 4 | 해외 `isin` 매핑 90.9% — 나머지 9.1% 는 마스터와 조인되지 않는다. 보완 가능한가. |  |  |
| 5 | 적재 제외한 시계열 값을 정말 안 써도 되는가 — 8/22 기준으로 재수집할 여지 확인. |  |  |

---

← [색인으로](README.md)
