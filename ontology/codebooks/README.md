# 📒 코드북 (codebooks) — 코드 ↔ 이름 매핑

> NEXT_STEPS §5.4 형식 준수: `code,name,short_name,source,as_of`
> **목적:** 상품군마다 운용사 표기가 달라 조인이 안 되는 문제 해결.
> - 펀드: `or_co_xtn_itt_cd = '00080012'` (숫자코드)
> - 국내ETF: `cu_fund_mgmt_co = '미래에셋'` (약칭)
> - 해외ETF: `cu_fund_mgmt_co = 'Mirae Asset…'` (영문)
> → 코드북이 셋을 이어줘야 *"미래에셋 운용 ETF+펀드"* 같은 교차질의가 됩니다.

---

## 1. `asset_manager.csv` — 운용사 코드 (37개) ✅

| 항목 | 내용 |
| :--- | :--- |
| 행 수 | **37개 운용사** (자산운용사) |
| 코드 | 금융투자협회 회원사 8자리 코드 (예: `00040010` = 삼성자산운용) |
| 커버리지 | 펀드 95,619행 중 매핑분이 **대부분** (미래에셋 24,374 · 삼성 8,932 · KB 6,126 …) |
| as_of | 2026-07-11 |

### 유도 방법 (재현 가능)

1. `public_funds.or_co_xtn_itt_cd` 는 `40010.0` (REAL) 로 오염 저장 → **8자리 0패딩 복원**
   (`40010.0 → 00040010`, NEXT_STEPS §5.4 지적대로 단사 복원 가능).
2. 펀드명(`itm_nm`) 접두어를 **알려진 운용사 약칭과 역매칭** → 코드↔약칭↔정식명 확정.
3. 코드가 여럿이면 **최빈 코드** 채택 (계열 분리사는 별도 처리: 삼성 vs 삼성액티브).

> ✅ 검증: `00040010 = 삼성`, `00080008 = 미래에셋` — NEXT_STEPS §5.4 예시와 일치.

---

## 2. 🔴 아직 코드 미확보 — 회원사표 직접 확인 필요

### (a) ETN 발행 증권사 (자산운용사 아님)

국내ETF 마스터의 `cu_fund_mgmt_co` 에는 **ETN을 발행한 증권사**도 섞여 있습니다.
이들은 펀드를 운용하지 않아 위 방식으로 코드가 안 잡힙니다 (금투협 **증권사** 회원 분류).

| 약칭(데이터) | 정식명(추정) | 상태 |
| :--- | :--- | :--- |
| 메리츠증권 · 신한투자증권 · 한국투자증권 · 삼성증권 · KB증권 · NH투자증권 · 키움증권 · 하나증권 · 대신증권 | 〃 | 🔴 증권사 코드 별도 확인 |

### (b) 운용사 8종 (펀드 코드 67개 중 미매핑분)

펀드 코드는 67개인데 37개만 약칭 매핑됨. 나머지 ~30개는 소형·해산사 또는
약칭 사전 미등록분. **주요 운용사는 37개로 커버**되므로 우선순위 낮음.

> 📮 (a)(b) 는 금융투자협회 회원사 조회(https://www.kofia.or.kr) 로 보완 예정.

---

## 3. `trustee.csv` — 수탁회사 코드 (18개) 🟡

| 항목 | 내용 |
| :--- | :--- |
| 코드 | `public_funds.trusc_xtn_itt_cd` (8자리 복원, 예 `00020054`) — 18종 |
| 이름 | 🔴 **데이터에 수탁사명 없음** (펀드명에도 안 나옴) → 금투협 회원사표 확인 필요 |
| 참고 | 0002 접두 = 은행 수탁(국민·신한·하나·우리·NH·IBK 등 추정), 0016 = 증권 |

> 코드(개체 ID)는 확정. **이름 라벨만 금투협 조회로 채우면 완성.**

---

## 4. `market.csv` — 거래소 코드 (6개) ✅

| code | 거래소 | 검증 |
| :--- | :--- | :--- |
| `EXG_MKT_NO_001` | 한국거래소 유가증권시장(KRX) | 국내ETF 1,733종 전부 |
| `AMX` | **NYSE Arca** | ✅ VOO·SPY·IVV·VTI(전부 Arca)가 AMX로 검증 |
| `NAS` | NASDAQ | ✅ QQQ가 NAS로 검증 |
| `NYS` | NYSE | 🟡 미검증 추정 (78건) |
| `101` / `102` | 🔴 기타 미국거래소 **미확정** | 31건, 종목명이 일반 US ETF라 특정 불가 |

> ⚠️ **정정 이력:** `AMX` 를 약자만 보고 "NYSE American(구 AMEX)"로 적었으나, 거래소가
> 확실한 ETF(VOO·SPY·IVV 전부 NYSE Arca)가 `AMX` 로 찍혀 있어 **NYSE Arca 로 정정.**
> 유명 ETF 역대조로 검증. `NYS`·`101`·`102` 는 미검증 → 확정 표기 안 함.

---

## 5. `fx_rate.csv` — 환율 (USD/KRW) 🔴 잠정

| 항목 | 내용 |
| :--- | :--- |
| 용도 | 해외ETF AUM(USD) ↔ 국내(KRW) 교차 정렬 |
| 기준일 | 2026-07-11(토) → 마지막 영업일 **7/10 종가** 필요 |
| 값 | 🔴 **잠정 1,485.30** (2026 연평균) — **정확값 아님** |
| 조치 | 웹 소스(exchange-rates/FRED) 접근 차단. **서울외국환중개·KRX 고시 7/10 종가로 교체 필요** |

> ⚠️ AUM 교차 정렬에 쓰기 전 **반드시 정확한 7/10 종가로 교체**. 잠정값은 순위 오류 유발 가능.

---

## 6. 코드북 5종 — 최종 상태

NEXT_STEPS §68 요구: **운용사 · 수탁사 · 금투협(펀드분류) · 거래소 · 환율**

| # | 코드북 | 파일 | 상태 |
| :-: | :--- | :--- | :--- |
| 1 | 운용사(67) | `asset_manager.csv` | ✅ 37개 매핑 (주요사 커버) |
| 2 | 수탁사(18) | `trustee.csv` | 🟡 코드 확정·이름 확인필요 |
| 3 | 금투협 펀드분류(20자리) | `kofia_fund_class.csv` | ⬜ **펀드팀 협업** (계층 확정) |
| 4 | 거래소 | `market.csv` | ✅ (101/102만 추정) |
| 5 | 환율 | `fx_rate.csv` | 🔴 잠정값·7/10 종가 교체필요 |

> **남은 것:** ③ 펀드분류(펀드팀)와, 외부 조회로 채울 라벨 2건(수탁사명·환율 종가).
