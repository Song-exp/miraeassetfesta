# 개발·탐색 전용 의존성 (EDA 노트북)
#
#   pip install -r requirements.txt -r requirements-dev.txt
#
# ⚠️ 제출용 Dockerfile 에는 넣지 마세요.
#    requirements.txt 만으로 API 서버가 구동되어야 합니다 (재현성 배점 항목).

-r requirements.txt

jupyterlab>=4.1.0
ipykernel>=6.29.0

# 노트북 「📊 기초 통계」 절의 dist() 용.
# 없어도 텍스트 막대로 동작하므로 필수는 아니지만, 분포는 그림으로 보는 편이 빠릅니다.
matplotlib>=3.8.0

# scripts/check_yaml_dupkeys.py (배포 전 기준선 3종 중 하나) — 없으면 스크립트가 sys.exit 한다.
ruamel.yaml>=0.18
